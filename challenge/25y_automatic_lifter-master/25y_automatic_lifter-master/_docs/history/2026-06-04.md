# 2026-06-04 변경 내역

> 리프터 모션 캘리브레이션 평가 도구 구현 (Subagent-Driven). 스펙: `docs/superpowers/specs/2026-06-04-lifter-motion-eval-design.md`, 계획: `docs/superpowers/plans/2026-06-04-lifter-motion-eval.md`. 커밋은 사용자 OK 전까지 보류.

## Task 0: eval 패키지 스캐폴드

**생성**: `eval/__init__.py`, `eval/results/.gitkeep`, `tests/eval/__init__.py`
**계획 외 추가(환경)**: `tests/__init__.py` + 작업루트 `conftest.py`(sys.path 추가) — pytest가 `tests/eval/`을 `eval` 패키지 하위로 오인하는 문제 해결. `pip install pytest`(pallet-pose env에 미설치였음 → 9.0.3 설치).
**되돌림**: A agent가 `pyproject.toml`(기존 추적 파일)에 `[tool.pytest] pythonpath` 추가했으나, conftest.py만으로 충분 확인 후 되돌림(사용자 setting 보호).
**import 검증**: `control.is_mock()=True`, `t(0.5)=3.7179`, `depth_cam/__init__.py`·`depth_cam/calib/__init__.py` 이미 존재.

## Task 1: eval/fit_fwd_model.py — t(d) refit (★보정3·5)

**생성**: `eval/fit_fwd_model.py`(`d_of_T`, `t_of_d`, `fit_fwd`, `_load_calib`, `main`), `tests/eval/test_fit_fwd_model.py`
**내용**: d=f(T)로 piecewise 적합(가속 0.5a(T-t0)² / 정속 d_acc+vmax(T-t0-t1)) 후 t(d) 해석적 역산. scipy curve_fit, R²/RMSE 출력. 방향별(fwd/back) 적합.
**결과**: 3 passed

---

## Task 2: eval/imu_yaw.py — RelYawEstimator 구현

**파일 생성**: `eval/imu_yaw.py`, `tests/eval/test_imu_yaw.py`

**내용**:
- `RelYawEstimator`: gyro.Y(rad/s) 적분 → rel_yaw(deg, [-180,180]). `depth_cam/main_rec.py` 동일 수식 이식 + `reset()`(ref 재스냅) 추가. pyrealsense2 import 부작용 회피를 위해 main_rec를 import하지 않고 이식.
- 테스트 3개: first frame → 0, gyro.y=1rad/s × 1s → 57.3°, reset 후 → 0

**결과**: 3 passed

---

## Task 3: eval/imu_yaw.py — RelYawReader 추가

**파일 수정**: `eval/imu_yaw.py` (RelYawReader + _rs() 추가), `tests/eval/test_imu_yaw.py` (test 1개 추가)

**내용**:
- `_rs()`: pyrealsense2 lazy import (없으면 None)
- `RelYawReader`: RealSense 파이프라인을 백그라운드 스레드로 폴링 → `rel_yaw` 프로퍼티 제공. `available()` False면 RealSense 미설치/미연결.
- `_process_motion()`: accel/gyro/ts_ms → estimator 업데이트 → `_rel` 갱신 (lock 보호)
- 테스트: `__new__`로 `__init__` 우회(파이프라인 미생성) → `_process_motion` 직접 호출로 estimator 경로만 검증

**결과**: 4 passed

---

## Task 4 & 5: eval/eval_motion.py — 순수 헬퍼 + 동작 시퀀스

**파일 생성**: `eval/eval_motion.py`, `tests/eval/test_eval_motion.py`

**Task 4 — 순수 헬퍼**:
- `EVAL_HEADER` / `CALIB_HEADER` 상수
- `wrap_to_180(deg)` — (deg+180)%360-180
- `append_eval_row(path, d)` — measured-target 으로 error 자동계산 후 CSV append (신규 파일이면 헤더 1회)
- `append_calib_row(path, ...)` — T_sec/d_measured/direction/power append
- `write_raw_rotate(ts_label, samples)` — raw 시계열 CSV 저장

**Task 5 — 동작 시퀀스**:
- `RotateResult` dataclass — target/turn_dir/imu_stop_deg/imu_settled_deg/elapsed_s/reached/raw
- `rotate_sequence(...)` — IMU closed-loop, stop 후 settle_s raw 연장(★보정2), timeout 지원, reader/issue_rotate/issue_stop 주입
- `drive_sequence(...)` — 개루프 전진 duration_s초, raw 미기록(★보정3)

**결과**: 5 passed (test_wrap_to_180, test_append_eval_row_writes_header_once, test_append_calib_row, test_rotate_sequence_stops_at_target, test_rotate_sequence_timeout)

---

## Task 6 & 7: CLI/메뉴/CAN·IMU 연결 + README

**파일 수정/생성**: `eval/eval_motion.py`(Task6 append), `eval/README.md`(신규)
**Task 6**: import(control/motion_models/RelYawReader), POWER_FWD=60/POWER_ROT=118, _estop_watch([space], keyboard 미설치 시 무시), run_rotate/run_drive_calib/run_drive_eval, _menu, main(argparse: --rotate/--drive/--calib/--dir/--timeout/--no-measure).
**버그 수정(계획 결함)**: 계획 Task6 import 블록에 `from eval.imu_yaw import RelYawReader` 누락 → `--rotate` 시 NameError. import 1줄 추가로 수정(계획 문서도 동기화).
**mock smoke**: import OK / argparse OK / `--calib 2.0` mock → calib_fwd.csv 기록(`..,2.01,1.23,fwd,60`) / `--rotate` mock → RealSense 미가용 SKIP(NameError 없음) / 전체 12 passed.
**Task 7**: eval/README.md — 워크플로 순서, 실험 프로토콜, scope, 논문 메모.

**남은 작업**: 실기(RealSense+Kvaser CAN) 연결 후 동작 검증. closed-loop 정렬 실험은 별도(메모리 lifter-closedloop-alignment-experiment-todo).

---

## GUI 추가 (eval/eval_gui.py) + estop 중단 보강

**사용자 요청**: 실험을 더 쉽게 하려고 GUI로. 오른쪽에 설명, 버튼으로 동작 실행. (AprilTag 자동검출 X — 실측값 수동 입력 유지)

**eval_motion.py 수정(보강)**: `rotate_sequence`/`drive_sequence`에 `stop_event=None` 인자 추가 → estop 시 루프 즉시 중단(기존엔 CAN STOP만 쏘고 루프는 타임아웃까지 돌던 결함). 기본 None이라 기존 테스트 호환(12 passed 유지).

**eval/eval_gui.py 신규**: tkinter. 좌측=조작(상태/회전/calib/eval 섹션 + 목표값 입력·방향 라디오·실행 버튼 + 결과·실측입력·기록저장 + 비상정지), 우측=설명 패널(워크플로 순서·회전 오차분해·주의사항). 기존 `eval_motion`(rotate_sequence/drive_sequence/append_*/write_raw_rotate)·`control`·`RelYawReader`·`motion_models` 재사용. 동작은 백그라운드 스레드(GUI 비블로킹), rel_yaw는 `after(100)` 실시간 표시, estop=`[Space]`/버튼→stop_event.set+STOP.

**검증(mock)**: 위젯 생성/init/close OK(CAN mock, IMU 미가용 감지). 전체 pytest 12 passed. 실행: `python eval/eval_gui.py`.

**디자인 개선(가볍게)**: Separator→LabelFrame 4섹션 박스, 폰트 11pt 통일, rel_yaw 18pt 파란 강조, 비상정지 13pt 빨강, 창 760×540. 한글 글리프 깨짐 수정 — `font=("",N)` 빈 family가 한글 미지원 폰트로 폴백 → `TkDefaultFont.actual("family")`(=gothic) 추출해 전 위젯 통일. PIL.ImageGrab으로 캡처 검증(/tmp/gui3.png).

**한글 렌더 문제 해결**: conda env의 Tk는 X11 코어 폰트(`gothic` 별칭)만 인식 → 한글 깨짐. `FONTCONFIG_PATH`로도 conda Tk가 시스템 fontconfig 미인식. 해결: `sudo apt install python3-tk` 후 **시스템 python(/usr/bin/python3) + 시스템 Tk(Xft)** 로 실행 → NanumBarunGothic 등 인식, 한글 정상(사용자 확인). GUI 폰트는 `["NanumGothic","NanumBarunGothic","Noto Sans CJK KR",...]` 우선 선택 로직. 실행 스크립트 `eval/run_gui.sh`(시스템 python으로 띄움). GUI는 numpy/scipy 불필요라 시스템 python으로 동작(canlib 없으면 mock). ※실기 회전은 시스템 python에 pyrealsense2 설치 필요.
