# generated by scripts/build_stage_dataset.py -- do not edit by hand
# absolute path computed at build time (Ultralytics resolves a relative `path`
# against SETTINGS['datasets_dir'], which is not this repo)
path: /home/minjae/Documents/github/pallet-pose/challenge/yolo_pose_one_model/datasets/stage_a
train: images/train
val: images/val

nc: 1

names:
  0: pallet

kpt_shape: [9, 3]

# data contract only; fliplr is 0.0 in every config this round
flip_idx: [1, 0, 3, 2, 5, 4, 7, 6, 8]
