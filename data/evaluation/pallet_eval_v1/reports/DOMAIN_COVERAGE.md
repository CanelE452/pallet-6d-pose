# Acquisition domain coverage

```text
INTERNAL PROVENANCE REPORT
NOT A PAPER-FACING DOMAIN TABLE
```

여기 나오는 `outside` · `noapril` · `cad` 는 **내부 capture id** 다.
논문 결과표의 조건 이름이 아니다 — 그건 `PAPER_DOMAIN_COVERAGE.md` 를 본다.


```text
internal id        N  Sessions   paper_domain 분포
----------------------------------------------------------------------
outside           70         3   daytime=70
night            106         4   nighttime=50, none=56
noapril           12         1   none=12
cad               18         1   none=18
unknown          113         4   none=113
----------------------------------------------------------------------
TOTAL            319
```

`paper_domain=none` 은 MAIN 표에 들어가지 않는다는 뜻이지 데이터가
버려진다는 뜻이 아니다. provenance 는 그대로 유지된다.

