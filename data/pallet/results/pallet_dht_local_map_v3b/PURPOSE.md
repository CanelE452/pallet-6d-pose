기존 v3 Hough 최종 가중치를 유지하고, 점 초기값에 대한 고정 MAP prior를 선 분포 선택 전에 추가하는 합성 전용 추론 변경 진단이다. 새 학습/실사 추론/실사 GT 사용은 하지 않는다. 합성 calibration256에서 사전 고정한 scale grid를 선택한 다음 synth_val512에서 개선 조건을 확인한다. 완료 PASS는 실행 검증이며 모델 개선 성공과 구별한다. 원래 v3 파일과 결과는 변경하지 않는다.
