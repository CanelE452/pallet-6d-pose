# VCR_REAL_RESULT — NOT RUN

real N87 architecture result 는 실행하지 않았다.

사유: **Gate 0 (session-held-out bias predictability) FAIL**.
사전 지정 규칙 "Gate 0 FAIL 이면 architecture 구현 금지" 에 따른다.

판정과 수치: `VCR_LOSO_GATE.md`, `vcr_gate0.json`, `vcr_loso_metrics.csv`
