# Achieved experiment results

Moved here on 2026-07-13 because these experiment outputs predate the current
PAPER-S2 result bundle. `paper_s2_scratch_diffpnp` and the recent
`paper_ready_ubuntu` bundle remain one level above.

Inference outputs are grouped by their producing model. Annotation-tool checks
and dataset audits, which do not belong to an inference model, are grouped under
`no_model_data_or_tooling`.
