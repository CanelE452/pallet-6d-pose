# negative_real_20260823

`data/pallet/raw_data/negative_real_20260823`

**팔레트가 없는 실촬영 프레임 모음.** AP 의 false-positive / precision 측정용이다.
GT box 가 0 개인 것이 정답이므로 라벨 파일은 없다.

## 규모

```
rgb         2689
depth npy     61   (일부 프레임만)
고유 이미지  2688   (중복 1 쌍: 000238.png == 000239.png)
용량       1.4 GB
```

## ★ 세 블록이 섞여 있다

```
frame              n      촬영일(mtime)   해상도       camera_info K 적용
000000~000023     24     2026-05-13     1280x720    ✗
000024~000295    272     2026-08-21     1280x720    ✗
000296~002688   2393     2026-08-21      640x480    ✓
```

전처리에서 해상도를 하나로 가정하지 말 것.

## ★ cam_K.txt 주의

`camera_info.json` 의 K 는 640x480 블록 전용이다. 1280x720 296 장에는 맞지 않는다.
또 `cy = 256.29` 인데 480/2 = 240 이라 640x480 블록에서도 중심과 어긋난다 —
pose/PnP 에 쓰기 전에 재검증할 것. **detection AP 는 K 를 쓰지 않으므로 무관하다.**

## 검증 범위

균등 간격 **25 장**을 육안 확인해 25/25 팔레트 없음. 박스 더미·야외 나무 벤치·
책상·선반 같은 **hard negative** 가 다수라 FP 측정에 적합하다.
**2,689 장 전수를 본 것은 아니다** — FP 가 나오면 그 프레임을 개별 확인한다.

## 기존 negative 와의 관계

`REAL_NEG_DEV_V1`(259 장, `forklift_raw_20260528_163408`)은 `max_conf < 0.20` 으로
선별된 **편향 표본**이라 거기서 잰 FP/image 는 하한이었다. 이 셋은 그런 선별을
거치지 않았으므로 그 편향은 없다. 대신 촬영 장소가 사무실·교내로 팔레트 작업장과
다르다는 점을 해석에 반영할 것.

## 출처

`~/Downloads/real_unlabeled.zip` (2026-08-23 18:47 수신) 을 확장자별로 그대로 풀었다.
원본 파일명·내용 무수정. 중복 1 쌍도 지우지 않고 기록만 했다.
