# Achieved pseudo-label data

Moved here on 2026-07-13. The current best PAPER-S2 model was trained from
synthetic and mask-supervised data and does not use these paper_base-era
pseudo-label datasets.

Each directory is grouped by the `source_model` and `weights` recorded in its
manifest or per-frame JSON metadata.
