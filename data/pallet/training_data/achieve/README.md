# Achieved training data

Moved here on 2026-07-13 because these datasets and generation logs are not
direct inputs to the current best model, `paper_s2_stageB/net_epoch_0057.pth`.

The active training inputs remain one level above:
`mixed_v8_train`, `v4_split_base`, `aug_squash_v2`, `aug_trunc_v2`,
`aug_scale_v2`, `paper_4pallet_mask_v1`, and validation set `val`.
