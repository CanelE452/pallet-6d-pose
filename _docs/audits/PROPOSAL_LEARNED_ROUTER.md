# PROPOSAL_LEARNED_ROUTER — NOT RUN

session-grouped 3-fold learned coordinate router 는 실행하지 않았다.

사유: Phase D2 oracle router gate 가 7 항목 중 4 항목 FAIL.
사전 지정 규칙("FAIL: learned router 학습 금지")에 따른다.
GT 를 보고 고르는 상한조차 기준에 못 미치므로, 그 상한을 재현해야 하는
학습 router 가 기준을 넘을 수 없다.

fold 구성(미사용, 기록용): A=[capturepallet08, capturenight05],
B=[capturepallet03, capturepallet02, capturenight07],
C=[capturepallet04, capturepallet05, capturenight06].

근거 수치: `PROPOSAL_ORACLE_ROUTER_GATE.md`, `proposal_oracle_gate.json`.
