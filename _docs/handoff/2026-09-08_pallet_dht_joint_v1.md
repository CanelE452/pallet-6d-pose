# Pallet DHT joint v1 — completed experiment

User explicitly corrected scope to a synthetic-supervised single point + Deep Hough architecture, without filters/self-training. This supersedes using the completed frozen `pallet_line_pose_v1` result as the answer. The old exact-frame Hough diagnostic and old frozen-branch experiment remain completed and preserved.

New code: `scripts/research/pallet_dht_joint_v1/`; results: `data/pallet/results/pallet_dht_joint_v1/`.

Frozen TRAIN_PROTOCOL SHA `d24da92515f381c02a9300391787c292e06a53b97f44e3e5646d25a1fd0902af`: `point_only`, `hough_features`, primary `hough_joint`, seeds1/2/3, all55,980 synth frames×2epochs, batch16 FP32/nbs16, 6,998 updates/cell, AdamW1e−4; final epoch EMA (no real selection). Entire YOLO trains. Global P4 DHT→Houghconv+12role maps→normalized transpose→P3/P4/P5 residual feedback before Pose26. New15,052 parameters. Effective operator KA with rho kernel[.25,.5,.25], seam rho flip, physically valid line footprint targets from actual transformed batch only. No predicted-point initialization, GT crop, WLS or pseudo labels.

Core training sources are frozen in protocol. Preserve all dirty unrelated repository changes. Do not commit. Existing local instructions already read; user authorized implementation/training/evaluation, HTML auto-open, and Discord completion message via `scripts/research/discord_notify.py`. Do not expose hook secrets or send unrelated messages.

CPU geometry/full-graph/EMA/checkpoint and source60,000 rehash checks PASS. AMP initial scales65536/1024 failed and are archived. AMP16 common smoke passed; final FP32 batch16 actual3arm smoke also executed, final SMOKE_CHECKS assembled by operator. FP32 B16 full-model resource probe ~193ms/step/4682MiB allocated. Expect about3–4hours full comparison on single RTX3080; user informed. Final actual outcome is recorded below; no overall superiority established.

Run entire chain using conda `pallet-yolo26` python and `-m scripts.research.pallet_dht_joint_v1.driver --run-dir <absolute results>`. Driver uses result-directory cwd with repository PYTHONPATH, logs each stage, verifies exact augmented tensor traces across arms and distinct EMA tensor states across seeds, then freshly infers319positive+2689negative/model. Standard paper2D and MAIN6D refs retained, all real population is reused DEV, not final. Controlled26image×3repeat runtime. Aggregate/report/finalize are implemented and block incomplete completion.

Before starting or resuming check `STATUS.json`, `DRIVER_BINDING.json`, `.driver.lock` and stage logs. Driver owns child process groups and records child PID; don't kill other GPU/desktop processes. First-epoch partial cell without last.pt is archived and replayed from same R0; completed epochs resume exact optimizer/RNG/EMA state. Never change bound training sources or protocol during main. On an actual blocker preserve original evidence, repair only the necessary scope and document new binding/replay; do not hide failed attempts.

Do not end with background training only. Finish actual training, evaluation, independent metric/image QA, browser open and Discord notification. Finalizer PASS means valid execution; improvement is separately judged vs equally trained stock control, all6metrics and coverage. Partial gain/failure must be stated candidly. Primary example to inspect: `eval_pallet07:1778652166837872128`, earlier filter-accepted symmetry/position error. Visualized line evidence is not a causal attention explanation.

## Launch

Started main driver 2026-09-08T01:33:49Z (10:33KST), exec session93958; `STATUS.json` records driver/owned child PID and stage logfile. All24CPUtestsPASS and finalFP32 three-arm smokePASS (SMOKE_CHECKS SHA19af8c1f4c9c9cbcd2c0a2de5ba3f5edd73ae6b7c756855180c6ce4ae03e0a10). Independent output/actual-browser QA stages are connected before finalizer and being implemented in parallel while training runs. Preserve driver/training sources and protocol; do not restart completed work on compaction.


## Completed outcome — 2026-09-08 15:32 KST

All9 training cells completed55,980 synthetic images×2epochs,6,998 actual updates/cell. All9 evaluations independently inferred319positive+2689negative images. Final EMA only; training/augmentation/protocol/source bindings remain unchanged. Original driver93958 exited on strict runtime parity; reporting-only repair was archived and rebound, then driver18841 resumed completed cells without training or replacing accuracy caches. `STATUS.json` is now complete at2026-09-08T06:32:20Z; no main work is running. Do not retrain or send duplicate Discord completion.

Primary joint versus matched-budget point-only,3seed means: pooled point median6.865→6.689px; P9040.395→38.690px; rotation2.376→2.319°; translation7.731→7.800cm; IoU3D.59534→.59488; ADDsymAUC.42359→.42423. All6 session95% CIs include0. Point matches[307,308,310]→[309,307,307]; MAIN pose coverage319/319 preserved. Overall/keypoint/pose improvement confirmed flags are allfalse. These are reused13-sessionDEV results, not independent final. Full arm/seed data, intervals and negative FP/AP remain in SUMMARY/VERDICT.

Original runtime tolerance1e−4/rtol0 is unchanged. All702 timed observations collected;7 strict failures, maxpoint.006103515625px. Actual diagnostic isolates CUDA sparse HT/BP accumulation variation; score and candidate counts/ranks exact. `RUNTIME.PASS=false` and `parity_PASS=false`; time summaries are reference observations with failed strict parity. `COMPLETION.PASS=true` denotes completed execution/honest reporting, while `all_checks_passed=false` and `runtime_strict_parity_PASS=false` retain the failure. Original sources/logs and four-file reporting repair are under provenance/runtime_parity_001; all29 frozen accuracy/protocol hashes unchanged. Independent actual runtime audit confirms failure records against original caches.

Independent final metric/checkpoint/negative/HTML-coordinate audit PASS. Actual browser QA verifies319rawimages,2871model overlays,84real line-evidence samples and all12role axes/backprojections;0broken images. Main HTML SHA d0913c620a27f0bdb3107f9448ec85c870259e608a19fd0f2c4eae7e59ca35ff. Chrome window0x62003cd confirmed visible; Discord sentHTTP204. Main page at data/pallet/results/pallet_dht_joint_v1/index.html. Root directly inspected actual home and problem-case screenshots; supplemental physical role7 screenshots for all3 joint seeds and the runtime warning were also directly reviewed. ROOT_VISUAL_REVIEW.json records all6 inspected images.

VIEW_DIAGNOSIS.json/.md completed from the predeclared319frame cohorts. Lower-elevation point tails improve while pose translation does not;≥30° P90 worsens; near-frontal proxy offers no clear improvement. This descriptive subgroup analysis does not change the primary verdict. CASE_DIAGNOSIS.json/.md verifies all9models retain large corner-ID confusion on eval_pallet07:1778652166837872128. Point-only/joint official per-image median acrossseeds258.042/262.047px. PriorGT-only fixedpermutation37.348/31.857px is diagnostic only. The visible far-left vertical is role7 GT4–GT7; frozen role3 QA is a different edge. Do not claim line maps are causal attention, visibility labels or calibrated confidence.

Optional gradient diagnostic remains unexecuted: predeclared14GiB memory guard blocked at12.59GiB before checkpoint load/forward/backward. Do not claim measured loss-gradient conflict. Do not lower the guard or close unrelated apps to force this optional work. Future symmetry-aware point/line correspondence, learned local reliability and longer synthetic-only controlled training are hypotheses for a new protocol, not trained improvements from this run.

See _docs/notes/pallet_dht_joint.md and scripts/research/pallet_dht_joint_v1/README.md for results/reproduction. Preserve unrelated dirty work; no commit was made.
