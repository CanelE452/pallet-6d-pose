# 2026-07-10

## wood f3_flip "좌표 버그" 조사 → 기각 (model L-R non-equivariance)

목적: wood 필터 f3_flip 이 GT-good 프레임까지 16~18px 로 부풀어 통과 0장 → "un-flip 좌표
offset 버그" 가설 검증·원칙적 수정·재평가.

### 진단 (신규 `scripts/stage0/_wood_flip_diag.py`, GT 45장)
per-corner residual `r = x_n + x_f - (W-1)`:
- GT-good corners: median **16.95px, std 6.30** (tight 상수) / y-residual **0.02px** (완벽).
- r/sx = 0.718 cell → 함의 model 수평 bias β = r/2 ≈ 9.2px.
- extract_keypoints_from_belief 는 x·y 대칭(+OFFSET 동일)인데 x=+18 / y=0 → decode/extract
  가 만들 수 없는 비대칭 = **model belief 출력 자체**.
- 사용자 제안 "belief-space mirror + 동일 decode"(OFFSET 원리 상쇄) 구현·측정 → residual
  **+20.5px (오히려 악화)**. 즉 가장 원리적 좌표-정합 un-flip 으로도 offset 안 사라짐.
- 세 규약 residual: `W-x` 17.4 / `(W-1)-x` 18.4 / belief-mirror 20.5 (좌표 자유도 ~3px)
  아래 ~18px model bias 공통. 원인 = 학습 horizontal-flip aug 미사용(프로젝트 규칙) →
  model 이 flip-equivariant 아님.

### 수정 (원칙적, 매직상수 無)
`scripts/stage0/wood_infer_filter.py` `flip_infer_squash`:
- Before: `kpf[i] = (W - pred8[i,0], ...)` (pred_c 동일)
- After:  `kpf[i] = ((W-1) - pred8[i,0], ...)` — cv2.flip 정확한 역변환(pixel-flip-axis).
- 효과: 1px 교정, f3 사실상 불변(~17→~18px). +18 하드코딩(δ-sweep)은 model bias 빼기 =
  fitted 상수(사용자 금지)라 미적용.
- 스코프: pad 버전 `s1_cad_9filters.flip_infer`(238/240줄, `W - x`)는 표준 파렛트
  파이프라인 → 미수정(동일 1px 규약·동일 latent model bias 이나 별도 트랙).

### 재평가 (`wood_gt_eval.py` 재실행, GT 45)
- 정확도 불변: det 34/45, corner_med 13.9px, GT-good 6/45(13%), GT-gross 12/45(27%).
- f3_flip 통과 **0장 (TAU=10, baseline ~18)**, COMBO all10 **0장**, 선택성 불변.
- de-bias(+18)로 f3 낮춰도 GT-good f3[4.5,10.7] ⊂ GT-bad[4.1,14.5] 완전 중첩 = 선택성 0.
  f3 는 L-R 대칭 검사라 wood 지배 실패모드(depth/rear collapse=앞뒤축) blind. collapse 만
  f3 폭증하나 reproj_selfcons 가 이미 포착.

### 결론 (이전 "선택성 없음" 유지 / 근본원인 교정)
- f3 16~18px = 좌표 버그 아님 = **model 좌우 비대칭(no-flip-aug)**. 원리적 좌표수정으로
  4~6px 못 내림. 4~6px 은 fitted bias subtract 필요(금지)+선택성 0.
- (구) "un-flip 좌표 offset 버그" → (신) "model L-R non-equivariance + flip 이 depth/rear
  blind". 유효 게이트 = reproj_selfcons. f3 는 이 weight 에서 배포 필터 부적합.
- caveat: N=45 소표본·unseen·heuristic·weight(Stage B ep0057) 고유 β.
- 산출: `data/pallet/eval_results/paper_s2_scratch_diffpnp/wood_gt_eval.md` (조사 섹션 append).

## 9필터 선택성 — REAL filterval(123) 추가 (cad/noapril 외 "다른 셋")

배경: testset17(cad11+noapril6, 고앙각·검출 4/17)만 9필터 검사돼 선택성 측정 불가.
사용자 요청 = 이 모델을 추론한 다른 셋(real filterval)도 필터. real_eval 은 정확도만.

신규 `scripts/stage0/paper_s2_filterval_9filters.py` [확인]: testset17 하네스(build_rec/
M.apply_filter/env) + `stage25.frames_filterval()`(outside44+night43+manual36, dom 라벨·GT)
재사용, 도메인별 선택성(accept vs GT-good corner_med<10px) 추가. flip=(W-1)-x(오늘 fix).
dims(1.1,1.3,0.12). 산출 `filterval_9filter_check.md`.

결과 [확인] (det 94/123=76%, GT-good 26/123=21%):
```
domain   N    det%  GT-good%  대표 self-sup Prec   COMBO strong(Prec/Rec)   판정
overall  123   76     21       ~0.27 (≈base rate)  0.354 / 0.885            약(base 수준)
outside   44   68     27       ~0.40, f9 0.60      0.579 / 0.917            선택성 有
night     43   67      7       ~0.10 (≈base 7%)    0.091 / 0.667            선택성 無(붕괴)
manual    36   97     31       ~0.31              0.417 / 0.909            중간
```
- 대부분 self-sup 필터(f1~f7) Precision ≈ base-rate = 검출프레임 거의 다 accept → 선택성 약.
  f9(bbox_iou, GT필요=비배포)만 단독 최고(overall 0.368). f3_flip: n_acc 적고 Recall 0.19~0.23
  (model L-R bias 로 대부분 reject, 오늘 조사와 정합).
- ★도메인 격차 = memory("indoor少PL→R1↑ vs outside/night多PL→R2↓")와 정합: **night 는
  base GT-good 7% 라 어떤 필터도 순도 못 냄**(Prec≈base), outside/manual 은 strong combo 가
  base 대비 Prec 0.27→0.58/0.42 로 유효. → 필터는 base 품질 좋은 도메인에서만 선택성.
- caveat: real 저앙각·domain-mixed·heuristic tau. 결론 = 배포 self-sup 필터 선택성은
  도메인 base 품질에 종속(night 불가), f9/strong combo 만 outside/manual 에서 실효.

## 나무 파렛트(wood) 어노테이션 + GT 생성 (7/10)
- 신규 `challenge/scripts/annotate_wood.py`: 공유 `annotate.py`/`annotate_pnp.py` **불변**(PALLET_DIMS 를 eval 20+곳 import → 소스변경 시 표준 eval 전멸), 런처 프로세스 내에서만 `annotate_pnp.PALLET_DIMS=(0.8,0.59,0.14)` 런타임 override 후 `annotate.main()` 호출.
- wood 실측 = **80×59×14cm**. RealSense D435I 촬영(2026-07-09 라이브 쿼리): 640×480 fx605.91/1920×1080 fx1363.29. **wood 1280×720 = 1080p 16:9 선형유도 fx908.86/cx636.39/cy384.44** (K_for_resolution 자동). jpg→rgb/*.png 심링크+cam_K.txt 자동.
- **wood GT 45장 어노 완료**: `challenge/data/wood_pallet_20260618_{183705(25),184309(20)}_manual_gt/`. reproj 1.75~4.08px = dims·K 정확성 실측 검증.

## Stage B 필터 선택성 종합 (testset17 / wood / filterval) + pass·fail 폴더
- 세 GT 셋 전부 9필터 검사 완료. **일관 결론 = 필터 천장 = base 코너 정확도(도메인별)**:
  outside(base good 27~40%) 선택성 有 / night·wood·cad(base 붕괴/unseen) 無.
- wood GT45: det 34/45, corner_med 13.9px, GT-good 6/45(13%). self-sup 필터 Prec≈base(0.18),
  reproj-gate 만 Prec0.20/Rec0.67(gross 방어). W/D swap·depth 붕괴는 self-sup 필터로 못 잡음.
- **pass/fail 폴더 (5도메인×2버전, GT-free, 프레임당 1이미지)**:
  `filterval_passfail/{outside,night,manual,cad,noapril}/{pass,fail,underdet}/` (f3 제외),
  `filterval_passfail_withf3/...` (f3 포함).
  - **① f3 제외(배포 = f1∧f2∧f4∧f5∧f6∧f7)**: 검출 98 중 **90 pass / 8 fail** = 거의 다 통과(선택성 낮음).
  - **② f3 포함**: **19 pass / 79 fail**. f3 탈락 79 중 **71(90%)이 나머지 조건은 통과**(집합차 실측) = f3 단독·spurious 오탈락(오늘 확인한 L-R 비대칭). → 배포는 ① 맞음.

## f3_flip 심층 (오늘): 좌표버그 아님 = model L-R non-equivariance
- residual x=+18px/y=0, belief-mirror(원리적 un-flip)로도 안 사라짐 → decode 아닌 model 출력.
  학습에 horizontal-flip aug 미사용(L↔R 코너순서 파괴 방지) → flip 비대칭. 1px 정정((W-1)-x)만 적용.
- ★FAIL 프레임(noapril ...432466, "절반=전체") **flip해도 같은 c1·c2 놓침(6/8 동일)** = flip-consistent
  실패 → f3 무력. 원인 = near-오른쪽 코너 appearance 어려움(좌우 편향 아님).

## "일부만 잡음(절반=전체)" 실패 진단 + 필터 방향 (coverage_heatmap/)
- 5프레임 belief 9채널 + mask-aux head + coverage 시각화(`stage_coverage_heatmap.py`).
- 실체 = **부분 검출(near-코너 2개 미검출)→PnP가 작은 상자**. 틀린 코너도 **sharp 단봉(confidently-wrong)**
  → f1/f2(peak·2차봉)·f5 다 통과 = confidence 신호로 못 잡음(stage19 정합).
- **mask-aux head 는 파렛트 전체 fire vs keypoint 는 절반** → `mask면적/kpbbox` FAIL 0.90 vs pass 0~0.36.
  **"mask>keypoint = under-cover" 가 이 실패의 유일한 후보 신호**(단 5장·밝기/크기 confound·mask 학습전용 = 미확정).

## ★ 핵심 통찰 (사용자, 7/10): geometric 필터는 DiffPnP와 순환 → blind
- **DiffPnP 가 학습에서 "PnP로 풀리는 기하일관 keypoint"를 강제** → 모델 출력이 이미 기하조건 만족 →
  형태학 필터(diag/topbot/ratio/frsep/posdepth)가 검사하는 기하 = loss가 맞춰놓은 기하 = **순환**.
  그래서 depth-collapse/W/D swap/half-collapse 같은 "기하적으로 valid한데 틀린" 오류를 못 잡음
  (memory `dimsfree-2d-geometry-filter-impossible` 강화).
- flip 은 기하 아닌 **변환-일관성(TTA)** 신호라 순환 밖이나, L-R 비대칭+flip-consistent 실패로 무력.
- **직교(순환 밖) 신호 후보**: ①TTA(scale f4/rotation/crop) ②**★mask-aux coverage**(appearance,
  head 존재, half=whole 직접 겨냥) ③temporal(영상 앞뒤 일관성) ④photometric render.
- **다음 레버 = mask-coverage 를 더 큰 셋 + raw-mask 저임계 coverage metric 으로 검증**(현재 미확정).

## 직교(orthogonal) 신호 필터 실험 — photometric/spread/ensemble (compact 후속)

배경: 사용자 통찰(geometric 필터=DiffPnP와 순환→blind) 후속. "flip처럼 형태학적 아닌 신호가
또 뭐가 있나" 사용자 제안 4계열 중 3개 실험. 신규 `scripts/stage0/orthogonal_filters_exp.py`
(T=testset17 하네스 재사용: infer_squash/eval_frame_squash/M필터, S=frames_filterval). Stage B
ep0057, filterval 123(det 94), squash-parity, dims(1.1,1.3,0.12). GT로 Spearman + "배포필터
통과했지만 GT-bad 잡기". 판정셋=base 품질 있는 **outside+manual**(night=어둠↔나쁨 confound 제외).

결과 [확인] (outside+manual accepted 60, good21/bad39, base precision 0.35):
```
signal          precision 0.35→   비고
spread_front    0.57 (최고)       but 50x50 belief 양자화로 std 0.84px=거의상수 → FRAGILE
f4(기존,tau0.8) 0.55              ★기존 배포필터 재튜닝(5.0→0.8)만으로 동급, non-fragile
photo_worst     0.48
photo_rear      0.44              ← 사용자 #1(rear photometric)=가장 약함
ens_rear        0.38             manual서 net -1 (s1 squash OOD confound 현실화)
```
- ★ **rear-specific photometric 가설 미확정**: collapse된 rear는 confidently-wrong일 뿐 아니라
  **stably-wrong**(gamma/WB/noise/JPEG/blur에도 안 흔들림, 모델이 일관 hallucinate). flip-consistent
  (f3)·confidently-wrong(stage19)와 같은 벽. "front안정/rear만흔들림"은 outside만 약하게, manual 무너짐(rho≈0).
- ★ **실질 결론 = 새 직교필터보다 기존 f4 threshold 조임**(5.0→~0.8): precision 0.35→0.55, fragile 아님.
  직교 신호는 이미 f4로 갖고 있었고 tau가 너무 느슨했을 뿐. spread는 belief 해상도↑ 선결.
- 어떤 신호도 precision ~0.57 못 넘음(accepted 43% 여전히 bad)=**"pass=truly-good" 실패**. 순환-밖
  신호도 결국 weak **global difficulty proxy**지 rear-collapse clean separator 아님(필터 천장=base 재확인).
- 순도 우선(memory) 관점: good 24% 희생·precision +20pp = f4 tau 조임 채택할 만한 low-cost 개선.
- 미검증 남음 = **temporal**(filterval fid=연속 timestamp 영상). 단 systematic collapse는 temporal도
  일관될 수 있어 transient 오류엔만 유효할 위험(photometric와 동일 논리). 별도 실험 필요.
- 산출: `orthogonal_filters_exp.{md,json}`. caveat: 도메인별 bad 소표본, heuristic tau, 단일 weight.

## 산출물 인덱스 (paper_s2_scratch_diffpnp/)
CONCLUSION.md(PARTIAL 전이) / regression_3to8_diagnosis.md / real_eval_summary.md /
{testset17,wood_gt,filterval}_9filter_check.md / wood_infer_filter.md /
filterval_passfail{,_withf3}/ / coverage_heatmap/ / wood_gt_overlays/.
스크립트: scripts/stage0/{paper_s2_*, wood_*, stage_coverage_heatmap, _wood_flip_diag}.py,
challenge/scripts/annotate_wood.py.

## Session Snapshot — pre-compact (필터 조사 후속 결정 대기)
- **현재 최선 모델** = `weights/paper_s2_stageB/net_epoch_0057.pth` (PAPER_S2 Stage B best). dims: 표준 (1.1,1.3,0.12) / wood (0.8,0.59,0.14).
- **사용자 reframe**: "모델이 대부분 필터 통과 = 대부분 쓸만 = 의미있음, 조금만 더 고치면 됨". (동의: outside/manual은 통과=쓸만, night/wood는 base붕괴로 약함.)
- **열린 결정 (다음 액션)**: 남은 "일부만=전체" 실패를 잡는 작은 고침 —
  (A) ★mask-coverage 필터 검증(더 큰 셋+raw-mask 저임계 metric): 통과했지만 나쁜 프레임(half=whole) 걸러 "통과=진짜좋음" 만들기. 순환 밖 직교신호. ← 추천·값쌈.
  (B) pad100 추론으로 미검출 near-코너 회복(단 Stage B=squash학습이라 OFF-dist, 실측필요).
- 필터 배포정의 = f1∧f2∧f4∧f5∧f6∧f7 (f3 제외=L-R broken, f8/f9=GT). pass/fail 폴더 = filterval_passfail{,_withf3}/{5도메인}/.

## [14:51:07] Manual Compact (사용자 실행)

- Working directory: `/home/minjae/Documents/github/pallet-pose`
- Trigger: `manual`
- Session ID: `003f8287-6598-41da-bd49-c0aa01f45962`
- Transcript: `/home/minjae/.claude/projects/-home-minjae-Documents-github-pallet-pose/003f8287-6598-41da-bd49-c0aa01f45962.jsonl`
- Custom instructions: 아래는 원문 디테일 유지해서 보존해줘:
1. 현재 최선 모델 = weights/paper_s2_stageB/net_epoch_0057.pth (PAPER_S2 Stage B best).
   dims: 표준(1.1,1.3,0.12) / wood(0.8,0.59,0.14). 추론=squash-parity.
2. 핵심 결론:
   - PAPER_S2 DiffPnP3D = PARTIAL 전이 (canonical: data/pallet/results/paper_s2_scratch_diffpnp/CONCLUSION.md)
   - 필터 천장 = base 정확도(outside/manual만 선택성, night/wood 無)
   - ★geometric 필터는 DiffPnP와 순환→blind. 직교신호(mask-coverage/temporal/TTA) 필요
   - f3_flip = model L-R 비대칭(좌표버그 아님)
3. 사용자 reframe: "모델이 대부분 필터 통과=쓸만=의미있음, 조금만 더 고치면 됨"
4. 열린 결정(다음 액션): (A)mask-coverage 필터 검증[추천] vs (B)pad100으로 미검출 near-코너 회복
5. 필터 배포정의 = f1∧f2∧f4∧f5∧f6∧f7. pass/fail 폴더 = filterval_passfail{,_withf3}/{5도메인}/
6. wood 어노 툴 = challenge/scripts/annotate_wood.py, wood GT 45장 존재
7. canonical 문서: _docs/history/2026-07-10.md, memory/filter-circular-with-diffpnp-need-orthogonal.md

요약 허용(압축):
- 오늘의 필터 조사 중간 back-and-forth, agent 위임 과정, 중간 디버깅 턴
- 히트맵/폴더 생성 세부 과정 (결과 수치는 위 문서에 있음)
- Note: 사용자가 명시적으로 /compact 실행 — 작업 단위가 마무리된 시점일 가능성 높음

**TODO (session resume)**: compact 이후 이 마커 위쪽의 작업 내역을 확인하고 이어서 진행

