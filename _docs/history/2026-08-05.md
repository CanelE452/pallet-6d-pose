# 2026-08-05

세션이 08-04 에서 자정을 넘겨 이어짐.  Stop hook 이 나열한 35 개 파일 중 대부분은
07-29 ~ 08-04 에 이미 커밋됐고(`_docs/history/2026-08-0{1,2,3,4}.md` 참조),
오늘 실제로 바뀐 것은 아래 3 개다.

```
git log --since=2026-08-05  -> 커밋 0건 (학습 진행 중이라 아직 미커밋)
git status --porcelain
   M scripts/stage0/paper_s2_eval56.py
  ?? Deep_Object_Pose/common/pfdr_adapter.py
  ?? Deep_Object_Pose/train/pfdr_pose_loss.py
```

## PFDR (Pallet Far-Decoupled Refinement) 구현 — 학습 진행 중

목적: static E2(near H6 / far H5)가 corner 를 15~16% 줄이고도 pose 로 전환되지 않은
문제를, far 채널만 학습하는 residual adapter 로 뚫는지 확인.
near·centroid 는 ep57 H6 로 bit-exact 고정.

### 1. `Deep_Object_Pose/common/pfdr_adapter.py` (신규)

- `PFDRAdapter`: 입력 171ch(F50 128 + H4 9 + H5 9 + H6 9 + A6 16, 전부 detach)
  → Conv3x3 64 → GroupNorm → ReLU → Conv3x3 64 → GroupNorm → ReLU → Conv1x1 4.
  마지막 Conv **zero-init**.  BatchNorm/sigmoid/tanh/clamp 없음.  **136,004 param**.
- `fuse_far` / `fuse_near`: N1·N2 는 far 만, N3 는 near 만 수정.
- `local_soft_argmax`: 학습 loss 전용 미분가능 좌표 디코더
  (gaussian sigma 2 → argmax detach → 11x11 window → raw belief softmax T=0.1).

⚠ **작성 중 버그 3회, 전부 판정 전 수정**:

```
1) grid_sample 로 window 를 뽑으니 채널이 섞여 좌표가 엉뚱했다.
   Before: grid_sample 후 대각 인덱싱
   After : padded[bi, ci, ry, rx] 명시적 advanced indexing
2) padding 값 0 이 배경(-3)보다 커서 softmax 를 오염시켰다.
   After : pad value=-1e4
3) ★결정적 — conv2d 의 zero padding 때문에 배경이 음수인 belief 에서
   테두리가 들려 argmax 가 (0,0) 에 고정됐다.
   Before: conv2d(..., padding=size//2)
   After : F.pad(mode="replicate") 후 padding=0 으로 conv2d
```
수정 후 검증: 단일 peak 4/4 정확 복원, 동률 셀에서 subpixel 30.5, 경계 (0,0),
gradient finite·nonzero.

### 2. `Deep_Object_Pose/train/pfdr_pose_loss.py` (신규)

9-point(8 corner + centroid) pose consistency.  GT pose seed 에서 GN 4 step
(damping 1e-3 / clip 0.5 / cond 1e8 / positive-depth·finite guard),
GT geometry 와 비교하는 `L_3d` + `0.25 * L_reproj_gt`.
**predicted 2D 자기적합 목적함수 아님** — 그게 앞선 predseed DiffPnP 실패 원인이었다.
유닛 검증: exact GT → 0.000e+00, far corner 1개 +25px → l3d 1.8e-3, 음수 depth → usable 0.

### 3. `scripts/stage0/paper_s2_eval56.py` (확장)

기존 runner 에 추가 (신규 result root 생성 안 함):
`SEED`, 모듈 레벨 `SCREEN` 로드, `PFDRTrainer`, `pfdr_batch_losses`,
`pfdr_train`, `pfdr_belief`.

⚠ **hook 차단 = 명령 전체 미실행**: `cat >> ...` 와 `nohup 학습` 을 한 명령에 묶었다가
purpose_gate hook 에 걸렸는데, **append 도 함께 실행되지 않았다**.
`pfdr_train` 이 없는 채로 러너가 돌아 AttributeError.
→ 파일 수정과 학습 실행을 분리하고, 붙은 뒤 `grep` 으로 존재를 확인하고 진행.
(2026-08-02 heredoc 미첨부와 같은 유형 — **차단된 명령은 아무것도 실행되지 않는다**.)

## Phase A~H 관문 (전부 통과 후 학습 시작)

```
eval56 parity   PnP 50/56  reproj 11.5578  corner 7.2411  near 4.6755  far 11.4063  OK
wood   parity   PnP 44/45  reproj  9.2839  corner 9.2255  near 6.7325  far 14.1798  OK
E2     parity   eval56 11.7433/6.3975/9.6422, wood 9.0329/8.7754/11.8776           OK
decoder parity  training vs canonical: median 0.022~0.039px, p90 0.151~0.285px     PASS
exact preserve  N1/N2 step0 vs E2 = 0.000e+00, N3 step0 vs E0 = 0.000e+00          PASS
frozen base     trainable param 0                                                   PASS
```

## Gradient calibration (8 batch, update 없음)

```
arm  |g| group    anchor     l3d        lreproj    lambda
N1   4.114e-03   0.000e+00  -          -          anchor 10(clamp)
N2   3.455e-03   0.000e+00  2.788e-01  1.085e-03  anchor 10(clamp) l3d 0.002478 lreproj 0.3184
N3   3.202e-03   0.000e+00  1.055e-01  1.628e-03  anchor 10(clamp) l3d 0.006072 lreproj 0.1967
```

★ **anchor λ 가 상한 10 에 clamp됐다.**  zero-init 에서 `Huber(0,0)` 의 gradient 가
정확히 0 이라 gradient-norm 측정이 성립하지 않는 지점이다.  규정상 결과를 보고
조정하지 않으므로 그대로 진행하되, **학습 전에 PURPOSE.md 에 리스크로 기록**했다.

## 중간 관찰 — N1 은 사실상 학습되지 않음

```
N1 3/3 epoch 완료 (각 17.9분)
group loss  0.00037 -> 0.00041   3 epoch 내내 평평, 감소 없음
anchor      0.00000              내내 0
adapter 출력층 |max| 8.178e-03   0 에서 벗어났으나 peak 를 옮기기엔 너무 작음
```
λ_anchor=10 이 residual 을 0 근처로 묶는다.  **N1 ≈ E2 로 수렴할 가능성이 높다** [추정].
N2 는 l3d 0.397 로 pose loss 가 실제로 들어가 있어 궤적이 다를 수 있다 — 확인 필요.

## 진행 중

`weights/paper_s2_pfdr/{N1,N2,N3}` 로 3 epoch 씩.  N1 완료, N2 진행 중, N3 대기.
이후 eval56/wood 평가 → Phase M gate → N1/N2/N3 비교 → 보고서/커밋.

## 학습 완료 → 평가 → 판정 (전 arm FAIL)

N1/N2/N3 각 3/3 epoch completed (18~19분/epoch, 총 약 2.9시간).

### 학습 궤적
```
arm  epoch  group      anchor    l3d     lreproj
N1     1    0.000401   0.000000   -        -
N1     3    0.000406   0.000000   -        -      ← 3 epoch 내내 평평
N2     1    0.000409   0.000000  0.140   0.00112
N2     3    0.000392   0.000000  0.113   0.00087  ← l3d 0.397→0.113 (-72%)
N3     3    0.000340   0.000000  0.110   0.00081
```
**N1은 λ_anchor=10에 눌려 사실상 학습 안 됨. N2는 pose objective로 움직였다** —
belief를 GT heatmap에 맞추라는 요구로는 안 움직이던 residual이
"9점이 GT pose와 맞아야 한다"는 제약으로는 움직임.

### 평가 (exact preservation 전부 0.00e+00)
```
eval56  arm  pnp  reproj   corner   near     far    t50 t100 nan
        E0    50  11.558   7.241   4.676  11.406    45   17  119
        E2    50  11.743   6.398   4.676   9.642    45   16  120
        N1    50  11.715   6.383   4.676   9.585    45   16  120
        N2    50  11.668   6.402   4.676   9.560    45   16  120
        N3    52  11.800   7.880   5.309  11.406    45   17  105
wood    E0    44   9.284   9.225   6.733  14.180    40   36   51
        N2    44   8.873   8.779   6.733  11.547    38   34   53
        N3    44   9.292   9.358   7.138  14.180    40   36   50
```

### Phase M gate — 6/6 FAIL
N2: far eval56 **-16.2%** / wood **-18.6%** (E2보다도 좋음)인데
reproj는 eval56 -0.95%(기준-10%) / wood +4.42%(기준+5%)로 미달.
bootstrap P(improve) 0.729 / 0.752 (기준 0.90 / 0.80) 미달.
→ **corner→pose 전환 실패, 이 프로그램에서 7번째 반복**.

### Phase N — 두 arm 비교
- N1 vs N2: PnP total 94→94, reproj 추가개선 eval56 +0.40% / wood +1.59%
  (기준 5%) → **9-point pose objective REJECT**. 단 N1이 미학습이라
  "pose objective가 belief보다 낫다"보다 "눌린 residual을 움직인 유일한 신호"로 읽어야 함.
- N2 vs N3: far는 두 셋 모두 +16~19% 우수(조건1 충족)이나
  **N3 PnP total 96 > N2 94** (조건2 위반).
  ★N3는 far를 안 건드리고 near만 바꿨는데 eval56 PnP 50→**52**.
  08-04의 F4(near만 stagewise)가 PnP 53으로 최고였던 것과 **같은 방향** —
  두 번 독립적으로 "PnP를 올리는 건 far가 아니라 near"라는 신호.

### 판정
PFDR adapter REJECT / 9-point pose consistency REJECT /
far-specific 가설 corner ACCEPT·pose REJECT / **최종 = base ep57 변경 없음**.

### 산출
`data/pallet/results/paper_s2_eval56/pfdr/`: PFDR_GO_STOP_GATE.md, pfdr_arms.csv,
pfdr_gate.json, pfdr_grad_calibration.json, pfdr_arm_comparison.json,
pfdr_bootstrap.json, belief npz 6, weights/paper_s2_pfdr/{N1,N2,N3}.
요약 복사 `_docs/audits/eval56_summary/pfdr/`.

pytest **323 passed**.

## [12:20:53] ⚠️  Auto Compact (컨텍스트 한계 도달)

- Working directory: `/home/minjae/Documents/github/pallet-pose`
- Trigger: `auto`
- Session ID: `1f06bfde-fd3e-4103-a3ef-711599683949`
- Transcript: `/home/minjae/.claude/projects/-home-minjae-Documents-github-pallet-pose/1f06bfde-fd3e-4103-a3ef-711599683949.jsonl`
- Note: 컨텍스트가 가득 차서 자동 트리거됨 — 다음 세션에서 이 마커 위쪽 작업을 반드시 복원할 것

**TODO (session resume)**: compact 이후 이 마커 위쪽의 작업 내역을 확인하고 이어서 진행


## Threshold audit — N3 = threshold-crossing 이라는 내 진단이 틀렸다 (학습 0)

목적: PFDR N3 의 eval56 PnP 50→52 가 decoder threshold 0.3 통과에 의한
no-response 복구인지 확정하고, base 재학습 없이 threshold 만 낮춰
검출 복구와 pose 정확도를 동시에 얻는지 감사.  base ep57 read-only, 학습 0 step.

### Phase B — threshold 가 실제로 걸리는 지점 (혼동 정리)

평가 경로는 `decode_all(...)["D0"]` 이고 그 실체는 `FZ.heatmap_stats` 다.

```
paper_s2_frozen_diagnostic.py:73   BELIEF_THRESHOLD = 0.3
paper_s2_frozen_diagnostic.py:546  peak = work[arg_y, arg_x]      ← raw(비평활) 최대
paper_s2_frozen_diagnostic.py:661  "detected": peak >= BELIEF_THRESHOLD
```

★ 흔히 같이 묶어 말하는 gaussian sigma=2 / NMS / 11x11 centroid / +0.4395 는
전부 `filter_pr_camfacing.extract_keypoints_from_belief`(= D2) 소속이고
**평가 경로는 D2 를 부르지 않는다.**  D0 은 7x7 (LOCAL_RADIUS=3) softargmax 다.
affinity grouping 도 이 경로에 없다 — `current_solve` 는 9 점을 그대로 받고
`valid < 4` 만 본다.  그래서 corner row 의 `affinity_association` 은 값을
지어내지 않고 None 으로 뒀다.

### 구현

`scripts/stage0/paper_s2_eval56.py` 확장 (신규 top-level root 없음):
`decode_thresholded`(FZ.heatmap_stats 그대로 호출, 비교만 채널별로 재적용 —
0.30 이면 D0 과 bit-identical), `threshold_arm_tables`, `threshold_common_success`,
`threshold_new_corner_precision`, `threshold_rescue_rows`, `threshold_gate`,
`threshold_audit`, `threshold_figures`, `threshold_overlay_figures`.
arm 9 개(T0~T4/R1~R3/C1) 는 실행 전 고정, 결과 보고 추가·삽입 없음.

### 결과

Phase A parity: T0 이 eval56/wood 양쪽 baseline 을 정확히 재현 (통과).

```
eval56  arm   near    far  pnp   reproj  corner  near_e   far_e  >50 >100  nan
        T0   0.300  0.300   50  11.5578  7.2411  4.6755 11.4063   45   17  119
        T2   0.250  0.250   50  11.2656  7.2686  4.7158 11.5215   45   17  116
        T4   0.200  0.200   51  11.5547  7.3634  4.8616 11.5476   48   19  111
wood    T0   0.300  0.300   44   9.2839  9.2255  6.7325 14.1798   40   36   51
        T4   0.200  0.200   44   8.9837  9.3646  6.8459 14.2283   45   41   44
```

- eval56 PnP 는 51 이 최대 — gate 가 요구한 **52 에 도달하지 못한다**(=N3 의 값).
- corner/tail 은 전 arm 단조 악화 (eval56 >50 45→48, wood >50 40→45).
- common-success: eval56 **전 arm 에서 worsened > improved** (T4 는 1 개선/6 악화).
  중앙값이 -2.53% 로 내려가는 건 한 프레임이 크게 좋아져서지 전반 개선이 아님.
- rescue 는 전체 1 건(cad `1778653017736058368`, T3/T4/R3 동일 프레임).
  correspondence 3→4 로 `valid<4` 만 넘긴 것이고 **reproj 235.2px / yaw 72.6°**.
- wood 는 0.25 부터 신규 corner 중앙값 **402px** — 대각 반대편 코너를 잡는다.

### ★ 판정 — 내 진단이 틀렸다

**어떤 arm 도 두 셋 동시 통과 못 함 → threshold 0.30 유지.**

그리고 이 감사가 **원래 전제를 반증**했다.  N3 가 0.30 을 넘긴 near corner 14 개의
**base raw peak 는 0.0087~0.0448** 이었다 (N3 후 0.324~0.949, delta +0.31~+0.94).
eval56 near 전체에서 [0.20,0.30) 구간에 있는 corner 는 **6 개뿐**이고
75 개가 0.20 미만이다.  즉 그 14 개는 gate 근처가 아니라 **무응답**이었고,
0.20 까지 낮춰도 닿지 않는다 (그래서 rescue 프레임도 서로 다르다).

원인: 내가 인용한 "peak median +0.0014" 는 near corner 224 개 전체 중앙값이라
이미 검출된 143 개가 지배한다.  실제로 넘어간 corner 의 delta 는 그보다
2~3 자릿수 컸다.  **모집단 중앙값을 per-corner 효과로 읽은 것이 오류**였고
거기서 나온 threshold-artifact 가설이 이번에 기각됐다.

다만 N3 에 대한 결론 자체는 안 바뀐다 — 신규 14 corner 는
**median 21.7px, 20px 이내 36%(5/14)** 로 이 감사 기준(70%) 에도 미달이다.
정정된 문장: **N3 의 PnP 증가는 threshold artifact 가 아니라
"품질이 어중간한 corner 의 검출 recall 증가"이고, pose 개선은 아니다.**

Phase I near-specific: R2 는 T2 대비 나쁘지 않으나(둘 다 -2.53%),
R arm rescue 가 C1 대비 +2 프레임 이상이어야 하는 조건에서 R1/R2 = 0, R3 = 1
→ **near-specific threshold REJECT**.

### 산출

`data/pallet/results/paper_s2_eval56/threshold_audit/`:
THRESHOLD_{ARCHITECTURE,CORNER_RECOVERY,POSE_RESULT,COMMON_SUCCESS,RESCUE_FRAMES,
GO_STOP_GATE,RUN_PROVENANCE}.md, csv 5, threshold_gate.json, png 5.
요약 복사 `_docs/audits/eval56_summary/threshold_audit/`.
테스트 `challenge/tests/test_threshold_audit.py` 18 건 신규 → 전체 **341 passed**.

⚠ 테스트 작성 중 같은 실수 반복: 감사 코드에 금지어가 있는지 **원문 문자열로**
훑어서 주석의 "groups by affinity" 와 컬럼명 `affinity_association` 이 오탐됐다.
→ tokenize 로 주석·문자열 리터럴 제거 후 검사 + AST 로 실제 대입/호출만 확인.
(2026-08-04 docstring 오탐과 같은 유형.)

## Decoder reconciliation — 기존 판정은 decoder-invariant, 단 배포 decoder는 ep57을 못 돌린다

학습 0 step. 동일 belief/affinity 를 P0(D0 mechanism) / P1(D2 coordinate) /
P2(실제 배포 경로) 세 decoder 에 먹여 기존 ACCEPT/REJECT 가 decoder artifact 인지 검증.

### Phase B — 실제 배포 경로 확정 (추측 금지)

세 entrypoint 가 **모두 같은 decoder 로 수렴** → 배포 경로는 모호하지 않다.
```
scripts/dope/run_dope_live.py:29                  from detector import ModelData, ObjectDetector
challenge/scripts/run_live.py:38                  동일
challenge/.../depth_cam/calib/dope_inference.py:36  동일 (포클리프트 FSM)
→ Deep_Object_Pose/common/detector.py :: ObjectDetector.find_object_poses
→ Deep_Object_Pose/common/cuboid_pnp_solver.py :: CuboidPNPSolver (SOLVEPNP_EPNP)
config: challenge/config/task.yaml inference.belief = FSM _DopeCfg 와 동일
        threshold/thresh_map/thresh_points 0.30, thresh_angle 0.50, sigma 3
```

★ **발견 1 — Cuboid3d 는 학습 convention 의 yaw-180 파트너**
`M_cuboid = M_camfacing @ Ry^T` (Ry=diag(-1,1,-1)) 잔차 **2.8e-17**,
index permutation `(5,4,7,6,1,0,3,2,8)` = `run_live.enforce_camera_facing:78` 의
swap_map = 알려진 YAW180_PERMUTATION. run_live 는 표시 index 만 swap 하고 R 은
그대로 둔다(자체 주석에 TODO). → P2 pose 에 상수 Ry 를 적용해 한 frame 으로 통일.

★ **발견 2 — 배포 전처리가 이 체크포인트와 안 맞음**
run_live 는 aspect 보존 h=400 resize, 이 프로그램/학습은 squash 400×400.
audit 목적이 decoder 만 바꾸는 것이므로 squash 를 고정하고 squash-space K 사용
(정확: (400/W,400/H) 스케일은 fx,cx / fy,cy 에 그대로 대응 → pose 불변).

### 구현
- 신규 `scripts/stage0/decoder_paths.py` — P1 은 `extract_keypoints_from_belief`,
  P2 는 `ObjectDetector.find_object_poses` **원본 호출**. 재구현 금지 테스트로 강제.
- runner 확장: `dec_forward_arm`(H4/H5/H6/A6 float32), `dec_evaluate_frame`,
  `dec_membership`, `dec_coordinate_rows`, `dec_paired`, `dec_gate`, `dec_analyse`,
  `dec_figures`, `dec_figures_overlay`.
- checkpoint 는 run_state.json 으로 확인: S1 99584084(5/5), C1 aad97f6b(5/5),
  N2 4b644fd8(3/3), N3 9db513f3(3/3).

### Parity
```
P0 baseline  eval56 50/11.5578/7.2411/4.6755/11.4063/45/17/119   정확 재현
             wood   44/ 9.2839/9.2255/6.7325/14.1798/40/36/ 51   정확 재현
arm P0 parity E2 far 9.6422·reproj 11.7433, S1 8.5191, C1 PnP 55, N2 11.6680, N3 PnP 52  전부 일치
P2 direct vs cache  최대 좌표차 0.000e+00px, 최대 pose차 0.000e+00 (기준 1e-6)
```

### ★ P2 = BLOCKED — object 를 하나도 못 만든다
```
set     P1 좌표  P2 object 포함  drop   object수  centroid raw  sigma3 후  0.30 통과
eval56    354          0         354       0        0.8561      0.2813     0/56
wood      328          0         328       0        0.8917      0.2866     0/45
```
`find_objects` 는 **gaussian sigma=3 평활 후** `map > thresh_map(0.30)` 인 peak 에서
centroid object 를 만든다(detector.py:684,701). ep57 centroid raw 0.856 → sigma3 후
0.281 로 **문턱 아래**. centroid peak 이 없으니 object 0 → association·PnP 미도달.

**wrapper 버그가 아님 (같은 wrapper·같은 config, 모델만 교체)**:
```
ep57            centroid raw 0.8520  sigma3 0.2708  objects 0
challenge0123   0.9485  0.6093  objects 6
challengenight  0.8378  0.5366  objects 6
```
배포 sigma 는 wide-belief challenge 모델 기준. ep57 은 blob 이 좁아 sigma3 에 뭉개진다.
규정대로 sigma/threshold 는 **조정하지 않음**.

### 판정 (6 arm × 3 path × 2 set = 36)
```
        P0      P1      P2              P0==P1
E2   REJECT  REJECT  INCONCLUSIVE        YES
S1   REJECT  REJECT  INCONCLUSIVE        YES
C1   REJECT  REJECT  INCONCL/REJECT      YES
N2   REJECT  REJECT  INCONCLUSIVE        YES
N3   REJECT  REJECT  INCONCLUSIVE        YES
```
verdict flip 후보 **NONE**.

**Q1 답**: corner 수준은 decoder-invariant (E2 far -15.5%(P0)/-18.6%(P1),
N2 -16.2%/-18.3%, corner↑pose↛ 패턴 동일). 그러나 **검출 수 기반 결과는 D0 전용**:
N3 PnP 50→52 가 P1 에선 46→46, C1 PnP 50→55 가 46→43 으로 **부호 반전**,
S1 을 막았던 near +30% 악화가 P1 에선 -2.9% 개선. (직전 threshold audit 의
"N3 이득 = 검출 recall" 결론과 정합.)

**Q2**: 답 불가 — grouping 이 아니라 **object 생성 단계에서 전량 소실**.
**Q3**: 뒤집힌 arm 없음.

### 부수 발견 — P1(D2 추출기)이 이 모델엔 더 나쁘다
동일 corner 기준 GT 방향 이동 33% 뿐, median +3.36px(eval56)/+5.07px(wood).
50×50 map 에 11×11 가중평균은 좁은 blob 에 너무 넓다 (D0 는 7×7 T=0.1).
단 tail 은 낮아짐(>50 45→32, 40→23) — 나쁜 corner 를 아예 출력 안 해서.

### 산출
`data/pallet/results/paper_s2_eval56/decoder_reconciliation/`: md 10, csv 6,
gate registry json, parquet, figures 9 + verdict_flip_candidates.txt(NONE).
요약 복사 `_docs/audits/eval56_summary/decoder_reconciliation/`.
테스트 `challenge/tests/test_decoder_reconciliation.py` 29 건 → 전체 **370 passed**.

⚠ 테스트 오탐 재발(3번째): docstring 의 "No GT is consulted" 와 주석의 "affinity"
가 금지어 검사에 걸림 → tokenize 로 주석·문자열 제거 후 검사. 또 tokenize 가
`GATES.evaluate_result` 를 `GATES . evaluate_result` 로 쪼개 부분 매칭 필요.
