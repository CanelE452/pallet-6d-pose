
## STAGE11 16k — addon 6k 효과 격리 학습 (B0 v3 10k vs B1 v3+addon 16k)
- 목적: addon 6k(flat/저카메라/소형 보강)가 real V=8 성능을 높이나 깨끗이 격리 검증. 둘 다 동일 init(`weights/challenge0123/final_net_epoch_0060.pth`)+동일 recipe finetune → addon 효과만 분리.
- recipe: batchsize=8, imagesize=448, lr=1e-4, sigma=4.0, epochs=75(=base60+15, 누적 [[dope-finetune-cumulative-epoch]]), save_every=5, heatmap+affinity only(geo/vis/rel/struct/seg/vec 전부 off). seed=2657. challenge0123 header recipe 그대로.
- B0 data=v3 batch_000..009(10000). B1 data=v3+addon_v1(16000), **1:1 balance**.
- **train.py 최소 변경(sampler만 추가)**: `--balance_substr` 인자 신설 → 경로에 substr 포함 샘플 vs 나머지에 동일 total weight 주는 WeightedRandomSampler(replacement, num_samples=len). default None=기존 shuffle 동작 불변. 검증: addon 6000/v3 10000 → epoch draw 50.4%/49.6%(1:1 확인). DataLoader 동작(loader 자체)은 불변.
  - Before: `DataLoader(..., shuffle=True)` only. After: balance_substr 주면 sampler 사용·shuffle off, 아니면 그대로.
- 함정: train.py 가 `sys.path.insert('../common')` 라서 **반드시 `Deep_Object_Pose/train/` 에서 실행 + data 경로 `../../` 상대**(train_dope.sh 관례). repo 루트서 실행하면 `ModuleNotFoundError: models`. launcher `data/pallet/eval_results/stage11_16k/run_train.sh` 가 cd 처리.
- "NaN or Inf found in input tensor" 1회(startup) = tensorboardX x2num.py 의 histogram 로깅 경고(빈 belief_peak), **loss 와 무관·무해**. loss healthy(~0.03-0.06).
- eval(`data/pallet/eval_results/stage11_16k/eval_stage11.py`): real=filter-val(collect_val_frames outside44+night43)+manual GT 36=123장. aspect 전처리(무패딩), heatmap-only decode. 지표=corner median(hungarian order-free)+검출(n_det≥6)+worst-2 corner+PnP(solve_pose, **per-frame K**+GT dims, SOLVEPNP_ITERATIVE). 분리=V=8/V<8 + 보강축(proj size small/mid/large, depth near/mid/far).
- baseline challenge0123(init): ALL corner_med 11.4 det58.5% / V=8 corner 11.3 det67% worst2 26.8 pnp 67.9% reproj3.0 / V<8 det5.9%. 보강축서 large·near(=근접/truncation)이 det 37-39%로 약(squash 한계, [[dope-inference-needs-reflect-padding]] 정합).
- (진행 중) B0→B1 백그라운드 학습. 결과는 학습 완료 후 추가.
- ⚠ 1차 launcher 가 `tee` 파이프로 로깅했는데 외부 kill 시 tee death 가 train.py 까지 죽임 → B0 epoch69(0065 ckpt 유효 저장됨)에서 중단. **resume launcher**(`run_train_resume.sh`)로 B0 0065→75 이어받기(net_path=0065 → start_epoch66, cumulative) + B1 0060→75. tee 대신 `>> log` 직접 리다이렉트 + nohup detach 로 재발 방지. 0065 ckpt 검증=184 keys 정상.

## v3 합성데이터 가져오기 + 다양성 EDA (data-engineer 위임)
- `~/Downloads/train_palletobj_v3.zip`(9.8G) → `challenge/data/training/v3/` 해제. 10000 프레임 = rgb+mask+overlay(30000png)+json. convention=camera_dynamic_0123_v4, palletobj_v1, dims 1.1x1.3x0.12m.
- 다양성 감사 결과(전수 N=10000): `data/pallet/results/v3_diversity_audit/` (리포트 v3_diversity_report.md + 그림29 + json5).
  - 4지표: azimuth 3클러스터(clock 3/6/9, 사이 공백, 4번째 긴변 0장) / elevation 12~60° median 36.8°(고름) / projected size 31~552px / V=8 74.6%·V<8 25.4%.
  - **마스크: mask_rle(COCO RLE)이 진짜 occlusion-aware visible mask(area 6/6 정확일치). mask/*.png 는 gradient라 학습에 쓰면 안 됨.** [확인]
  - split 미생성·누수 위험 없음(scenario 독립 draw, near-dup 0). 권고=heatmap 먼저(마스크 RLE 사용, 크기 mask bbox).

## STAGE10 M2 — v3 real-mask PVNet(seg+unit-vec) 32 overfit 구현 게이트 (PASS)
- 신규 `scripts/stage0/v3_overfit32.py` (offset_overfit32 변형). base=`weights/challenge0123/final_net_epoch_0060.pth` frozen(strict=False, m_vec/m_seg 헤드 48 missing만 학습), feat 캐시. mask_rle 디코드→400 정사각 resize(aspect 왜곡, offset_overfit32와 동일)→50 INTER_AREA. vec=unit(make_vector_field, real mask 내부만 supervise). loss=vec_loss + seg BCE(2스테이지). 결과 `data/pallet/eval_results/stage10_v3mask/overfit32/`.
- Sanity(000001): mask_rle area=json area=35718 정합, frac mask in cuboid bbox=1.0, vec unit max|cos-1|=0.0, vec offset recover err=2e-6 cell. overlay 눈검증=real 팔레트 실루엣 정확 + quiver centroid 정확 + 코너0123 정렬. (overlay `sanity_overlay_000001.jpg`)
- 32 overfit(iters=3000,lr=1e-3): **seg IoU median=1.0000(32/32 모두 1.0)**, GT-mask voting corner err median=0.984cell p95=18.66, pred-mask voting=동일(IoU=1이라). **GATE=PASS** (IoU>0.9 AND vote-med<1.0). loss L_vec=0.056 L_seg≈2e-5 수렴.
- ⚠ 12/32 프레임 per-frame median=NaN(코너 일부 voting 실패) + 일부 고오차(000027=19.9). 진단: **구현 버그 아님** — 완벽 GT unit-vec+GT mask로 voting하면 그 NaN 프레임들 전부 corner err=0.000/NaN 0개([확인]). 원인=작은 마스크(88~157px)·truncation(코너 grid밖)에서 학습 vec 잔여오차 × RANSAC n_hyp 부족의 상호작용. decode/RLE/좌표/loss는 정확. → quick screen 시 n_hyp↑(512+) 권장.
- 결론: STEP5 voting 실패의 마스크 confound(cuboid hull 과포함) 제거 + 구현 정합 검증 완료. 2-4k quick screen(M0/M1/M2) 진행 가능(★전체학습 아님).

## STAGE10 M2 — v3 real-mask quick screen (M0/M1/M2, ~3000×3ep) → ★vector 트랙 종료 판정
- 실행: `scripts/stage0/_run_v3mask_quickscreen.sh` (base frozen finetune, train=batch_000~007 subsample 3000, 3ep=750step/arm, lam_vec=0.1 ramp + lam_seg=1.0). held-out 평가 batch_009 200프레임, `eval_pvnet_heads.py --evalset v3 --ransac_hyp 512`. 산출 `data/pallet/eval_results/stage10_v3mask/eval_v3_synheldout.{txt,json}`.
- ★핵심표(corner median px, order-free hungarian / good<10 gross>20):
  - heatmap(base 효과): M0=5.9 / M1=6.9 / M2=7.0 (정상, good 94~105/200).
  - M2 GT-mask voting(real mask로 vote)= **62.8px** (good 1, gross 194/200). ← ★판정 대상.
  - M2 pred-mask voting(seg head)= 59.5px, hull voting=58.2px. (마스크 출처 무관 전부 ~60px.)
- **판정(정정): "최종 종료"는 과결론 — 이 screen은 undertrained라 INCONCLUSIVE.** GT-mask voting 62.8 ≫ heatmap 5.9 맞으나, seg 병목 아님(GT≈pred mask) + ★vec field 학습 안 됨(L_vec 0.50→0.30 미수렴 + seen batch_000도 53.8px=학습데이터조차 못맞춤=underfitting). 32-overfit(0.98)이 capacity 증명 → 차이=표현불리 아닌 "750step·frozen·lam0.1로 미학습". 마스크 confound는 제거했으나 undertraining confound 생겨 깨끗한 답 아님. lean-negative이나 clean 미확정. ⚠ diag1~7상 "작동되게" 레버는 voting 아닌 heatmap+데이터라 voting은 orthogonal.
- 진단(과결론 방지 위해 추가 검증함): (a) L_seg 정상 수렴 1.37→0.05~0.13, seg head는 학습됨. (b) **L_vec 미수렴**: 0.50→0.30~0.36 plateau(750step/3ep로 dense per-pixel unit-vec 학습 불충분). (c) ★seen-data 대조: M2를 **학습에 쓴 batch_000** 40프레임에 재평가 → GT-mask voting 여전히 **53.8px**(heatmap는 6.4px 정상). seen에서도 망함 = held-out 일반화 갭/좌표버그 아님([확인]). overfit32 PASS(0.98cell)는 32프레임×3000iter 순수 암기 capacity였지 학습가능성 아님. (`eval_v3_M2_on_train.{txt,json}`)
- 함의: real mask는 STEP5 voting 실패의 confound가 아니었음(real mask 줘도 60px). dimsfree-2D-geometry-PL-filter-impossible 메모리와 정합 — vector/voting 표현 자체가 이 셋·예산에서 불리. ⚠단일 config(750step·frozen base·lam_vec 0.1)이므로 "더 긴 학습/unfreeze면 다를 수 있음"은 미검증이나, task 지시(이 screen까지·full 10k 금지)대로 vector 트랙 종료가 결론.

## DIAG8 — B0/B1 real 검출붕괴 원인 진단 (diag8_forgetting.py) → forgetting 확정
3모델 raw top-1(threshold無)+score+old-synth(training_data/val) 비교:
```
model      set        det%  rawCornMed  score(det)
baseline   real       59%   21.3        0.772
baseline   old-synth  84%   101.5*      0.874
B0_v3      real        5%   179.4       0.470
B0_v3      old-synth  16%   174.1       0.548
B1_addon   real       25%    97.0       0.655
B1_addon   old-synth  52%   110.8       0.666
(*old-synth raw는 channel-identity convention 민감 noisy; det%가 robust)
```
- **(B)confidence/threshold 기각**: B0/B1 raw 좌표(threshold無)도 붕괴(179/97 vs 21px)+score 하락 → recalib로 안 고쳐짐.
- **(A)catastrophic forgetting 확정**: B0/B1이 학습 안 한 old-synth(원래 분포)서도 det 84%→16%/52% 붕괴 = v3 교체 finetune이 원래 feature 덮어씀.
- addon이 forgetting 완화(B1≫B0, real 25/5·old-synth 52/16) → 새 데이터 가치 있으나 교체로는 망함.
- → 처방 = B3 replay-mix(원본 base data + v3 + addon, batch 2:1:1, heatmap-only, encoder 보수적 LR/freeze-first), val 3분리(Val-Old/v3/addon), real은 최종평가용 일부 holdout.
