# 2026-07-15


## [01:05:09] Manual Compact (사용자 실행)

- Working directory: `/home/minjae/Documents/github/pallet-pose`
- Trigger: `manual`
- Session ID: `af5cc267-421b-4d00-86c9-0ed04afb90fd`
- Transcript: `/home/minjae/.claude/projects/-home-minjae-Documents-github-pallet-pose/af5cc267-421b-4d00-86c9-0ed04afb90fd.jsonl`
- Custom instructions: 다음은 원문 디테일을 유지해 보존:
1. self-training 실험 최종 결론: FULL7(순도39%)·reproj+flip(75%)·PL 5%/50%·전체/도메인별
   전부 R0(s2 diffpnp ep57) 개선 실패. self-domain도 못 넘음. corner 13.5→21px.
   원인=강한 base+자기예측 PL(no new signal). night=confidently-wrong(필터 0%).
2. 과거 성공(63.6→77.0)과 진짜 차이: 약한 base(pallet_category)+strict PL(ransac_loo)
   +PL 50%+FixMatch strong aug+self_train.py. PL 라벨은 둘 다 raw peak(재투영 아님).
3. reproj-gate > FULL7 순도(outside 45→75%, noapril 91%; night 0%). flip 추가=무개선.
4. annotate.py 개선: v(eval/train split), --default_split, frame slider+G goto,
   ANNOT-ONLY 클릭버튼, s='save-next'. (challenge/scripts/annotate*.py)
5. 평가 GT: outside117/night43/noapril18/cad44 (전체 GT, 누수 홀드아웃).
6. 산출물 폴더: data/pallet/results/paper_s2_fullpool_full7/ (cross_domain_*.png 등).
   스크립트: scripts/stage0/s2_fullpool_infer.py, s2_build_pl_reproj_flip.py,
   s2_perdomain_cross_domain.py, s2_gt_accuracy.py.
7. Discord: ~/.claude/config/discord-webhook(gitignored), hooks/discord-notify.sh.
8. 미해결 다음단계: 약한 base+과거 파이프라인 재현 / reproj-gate 단독 self-train.

요약 허용(압축): 초반 필터 결과표 조회·wood 이미지 진단 반복 턴·중간 디버깅·해결된 버그 수정 과정.
- Note: 사용자가 명시적으로 /compact 실행 — 작업 단위가 마무리된 시점일 가능성 높음

**TODO (session resume)**: compact 이후 이 마커 위쪽의 작업 내역을 확인하고 이어서 진행

---

## 랄프루프: self-training 개선 목표 (사용자: "랄프루프로 self-train 개선 goal, 문제 파악 후 자율 테스트")

목표·지표: `data/pallet/results/ralph_selftrain/GOAL.md`. 방향(사용자 확정)=과거 성공 재현 먼저.

### 진단 + 재현 (핵심)
- 강한 s2 base self-train 전부 실패 = base real 유능한데 PL이 confidently-wrong(새 신호 0).
- **memory 오류 정정**: 과거 성공(63.6→77.0) base = `pallet_category` 아님 → **`challenge0123`**(real 유능). Explore 파일조사(header/phase1_results.json/plot_phase2). pallet_category는 real 무능(nn20~0, PL 8/1500 reproj27px)라 H1 첫 시도 실패.
- **재현(base=challenge0123 ep60, ransac_loo, mixed_v8_train_2k anchor, self_train.py)**:
  - noapril self-domain: PL 19/170(11.2%, reproj4.99px)=과거 phase2와 동일. R1→R2 11.2→18.2% 부트스트랩.
  - eval(THRESH0.1): noapril corner_med 10.6→8.73→7.61px(-28%), cmDomAvg 18.24→11.76(R1,-36%), OVERALL nn20 29.3→32.0(R2). = self-training accuracy 개선 재현 O. nn20이 corner_med만큼 안 오르는 건 sigma2vs4 det 보수화 + self eval 18장 소표본.

### 신규 파일
- 스크립트: `scripts/stage0/ralph_build_pool.py`(leak-guard real pool), `ralph_eval_all.py`(RALPH_THRESH 오버라이드, corner_med/nn20/det).
- 데이터: `data/pallet/real_unlabeled_ralph{,_1500,_noapril,_outside,_night,_cad}`, `training_data/mixed_v8_train_2k`.
- 결과: `data/pallet/results/ralph_selftrain/{GOAL.md,eval_*.json,h1_ch0123_*/}`.

### 인과 검증 + paper 트랙 (헤드라인)
- **인과 분리(synthetic-only 대조군)**: corner_med 광범위 개선은 synthetic anchor 몫(대조군도 달성)=PL 아님. nn20 개선은 PL 몫. challenge0123 PL_night nn20 29.3→36.5(대조군 28.4 못넘김), night self 25.6→37.2(+11.6pp, 검출↑).
- **combined(295 PL)**: OVERALL nn20 36.5=night-only(145)와 동일 → "PL 많을수록"은 포화. 이득은 저-R0 도메인(night)에 집중.
- **★★ paper 트랙 s2(강한 base)도 개선**: ransac_loo strict PL로 night 41.9→58.1(+16.2pp,검출↑), cad 15.9→36.4, OVERALL 42.4→50.0(+7.6pp). 대조군은 night 악화 → 순수 PL 인과. **memory "s2 self-train 전부 실패" 반증(그건 FULL7/reproj dirty PL 한정, ransac_loo strict clean PL이 관건).** s2는 noseg 체크포인트로 로드.
- 결론: self-training 개선의 열쇠 = **ransac_loo strict clean PL(dims-known PnP+LOO, reproj clean)**. FULL7/reproj는 confidently-wrong 통과=dirty. best paper 모델 `h3_s2_night/round_02.pth`.

### 신규 파일(추가)
- 스크립트: `scripts/stage0/ralph_{matrix,causal_viz}.py`. weights: `paper_s2_stageB/net_epoch_0057_noseg.pth`.
- 결과: `causal_split{,_s2}.png`, `cross_domain_matrix_round_02.png`, `eval_{causal,combined,s2_causal,h3_*}.json`, `h1_ch0123_*/ h2_ch0123_combined/ h3_s2_night/ ctrl_{synonly,s2_synonly}/`.

### 남은 후보 (미해결)
- sigma2→4 맞춰 det 보수화 완화하면 nn20 더 오를지. cad self-domain PL(현재 cad는 R0 검출 낮음). R3 over-iteration 경계.

