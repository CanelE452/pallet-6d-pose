# 2026-07-09

## Stage B best → UNSEEN wood 파렛트 추론 + 배포필터 탐색 (3d-expert)

### 새 스크립트 `scripts/stage0/wood_infer_filter.py` [확인]
- Stage B best(`weights/paper_s2_stageB/net_epoch_0057.pth`)를 wood 파렛트(학습에 없던 파렛트, dims 0.8×0.59×0.14m, RealSense D435I 1280×720) 235장(2세션)에 추론.
- 전처리 = **SQUASH**(Stage B 학습 정합, `paper_s2_real_eval.eval_frame_squash` 방식). pad100 은 det 대조(Stage B엔 OFF-dist).
- 필터 = s1_cad_9filters helper 재사용 f1~f7 + **reproj self-consistency gate**(pred kp → solve_pose(wood dims, W/D auto-swap) → 재투영 vs pred kp median px, GT아님=자기일관). f8/f9 는 GT envelope/bbox 필요 → wood GT 없음 → 제외.
- dims override: `annotate_pnp.PALLET_DIMS = (0.8,0.59,0.14)` (solve_pose가 module 값을 call-time 에 읽음 — dims 인자는 무시됨).

### 결과 [확인] `data/pallet/eval_results/paper_s2_scratch_diffpnp/wood_infer_filter.md`
- 검출률: squash 171/235(73%), pad100 186/235(79%). unseen wood 를 모델이 검출함.
- 필터 pass%(/N): f1_peak 73, f2 68, f4_tta 71, f5_rear 69, f6_frsep 73, f7_posdepth 73 (≈검출률 = 검출되면 거의 통과). f3_flip **0%**(wood flip TTA 12~22px 전부 >10px 관례임계). reproj_selfcons 46%(주 판별자).
- COMBO strong(f1&f4&f5&reproj) accept 103/235(44%). all7&reproj=0(f3_flip 때문).
- 육안(오버레이 12장): accept = 코너가 wood 코너에 정확·PnP cuboid tight(indoor/outdoor 둘 다). reject = reproj 46~53px 프레임에서 cuboid 왜곡/과대·rear 코너 흩어짐 = 진짜 실패를 reproj gate 가 잡음.
- 오버레이: `data/pallet/eval_results/paper_s2_scratch_diffpnp/wood_overlays/` (pred=blue, PnP cuboid=red).
- caveat: wood UNSEEN(치수·텍스처 OOD), GT 없음(정확도 미측정=자기일관성·육안), 소표본, 필터 임계 heuristic.


## PAPER_S2 Phase 4: Stage A 완료 + Stage B 크래시 진단·수정·재실행

### Stage A (scratch 60ep, Arm A, DiffPnP λ0.005) = 완료 [확인]
- 14:00:00 소요(7/8 18:10 → 7/9 08:10), 60ep, save_every 3 → 22 ckpt.
- Stage A val-select(composite: rear_med+honest8 rank, guard) → **best = net_epoch_0042.pth** (`stageA_best.pth`).

### Stage B 크래시 → 근본원인 (systematic-debugging) [확인]
- 증상: Stage B가 09:29 시작 직후 죽음. `UnboundLocalError: local variable 'epoch' referenced before assignment` (train.py:760).
- 로딩은 전부 정상(index·60:40 sampler·mask-aux strict=False seg-head fresh·DiffPnP). "Starting at epoch 43" 직후 크래시.
- **근본원인 = 누적 epoch gotcha** (memory `dope-finetune-cumulative-epoch`): train.py:631 `start_epoch = net_epoch(42)+1 = 43`, train.py:717 `for epoch in range(start_epoch, opt.epochs+1)`. 드라이버가 Stage B `--epochs 15` 전달 → `range(43,16)` = **빈 루프** → epoch 미할당 → 마지막 저장서 UnboundLocalError.

### 수정 (단일) + 재실행
- `scripts/stage0/diffpnp3d_full_run.sh` Stage B: `--epochs`를 **누적값 동적 계산**으로. `A_EP=$(basename A_BEST의 4자리)`, `EPB=$((10#$A_EP + 15))=57`, `FINALB=final_net_epoch_0057.pth` (skip guard·final명도 동적). `10#` 로 leading-zero octal 함정 방지. 검증: EPB=57 → range(43,58)=정확히 15 epoch, bash -n OK.
- 재실행(detached setsid, 드라이버 bash 491489): Stage A **스킵**(final_0060 존재=14h 재학습 없음) → Stage A val-select 재실행 → Stage B(cumulative ep57=15 mask-FT epoch) → Stage B val-select.
- 워처 재장착(FULL RUN DONE 또는 드라이버 종료+Traceback 감지). GPU idle 확인 후 재실행(중복 프로세스 0).

### 상태
Stage A 보존. Stage B 재실행 진행 중(예상 ~6h: A val-select ~15min + Stage B ~5.5h + B val-select). 완주 후 P5 real eval.

## PAPER_S2 Phase 5: real eval (final transfer judgement) = PARTIAL transfer [확인]

- 스크립트 `scripts/stage0/paper_s2_real_eval.py` (신규, stage0 재사용). eval-parity 준수:
  s2 stageA/B=squash(400x400 aniso, belief×W/50,H/50) / s1·base=aspect no-pad(PAD=0,
  official A_nopad). downstream(order-free Hungarian corner+split front/back+solve_pose
  order-free W/D dims=(1.1,1.3,0.11)~측정1.1×1.3×0.12+honest8) 전 모델 동일.
- ★ 하네스 검증: s1/base 수치가 official eval_paper_s1_paired filterval 과 정확히 일치
  (s1 det70/rear20.5/corner12.5/honest8 17.7/good40.8; base 68/34.7/27.5/31.7/28.7).
  → s2 만 squash 차이 = parity 정당.
- real set: filterval N=123(outside44+night43+manual36, 저앙각 elev-7.7~10.4: <3° 46/
  3-8° 67/8-15° 10) = PRIMARY. handannot17 N=17 고앙각(정성). final-test SEAL.
- filterval Stage B vs paper_s1: rear 20.5→19.0(-1.5), det 70→76, pnp 72→76,
  gross 34.8→33.7, front 9.2→8.8, good 40.8→41.9 = 개선 / corner 12.5→13.5(+1.0),
  honest8 17.7→17.9(+0.2) = flat~약간악화.
  · <3°(n46): 광범위 승 rear16.7→15.0, corner12.6→10.6, det85→98, honest8 16.3→15.8,
    good+5.4, gross-2.8.
  · 3-8°(n67): rear27.5→23.7(-3.8) 이나 corner12.1→17.6, honest8 22.3→26.0, good-2.9
    = MIXED(rear median↓지만 overall corner/honest8 회귀).
- handannot17(고앙각 정성): rear9.8→8.2, honest8 12.6→10.3, corner-0.9, pnp+11 = 동일 방향.
- Stage A(mask FT 전) det 59% ↓ → mask-aux Stage B FT 가 detection 회복(59→76).
- 판정: STAGE16식 synth-only 실패 아님(real rear+detection 실제 개선, <3° 최강). 단
  honest8(pose 정확도) flat + 3-8° corner/honest8 회귀 → 깨끗한 승도 아님. = PARTIAL 전이.
  DiffPnP3D rear depth-collapse 완화가 real 저앙각(특히 <3°)+검출강건성으로 전이. 절대
  pose 정확도는 미개선(memory: rear=데이터/appearance 한계와 정합).
- 산출: data/pallet/eval_results/paper_s2_scratch_diffpnp/{real_eval_summary.md,
  real_eval.json, real_overlays/(impr5+regr5)}.

## 3-8° 회귀 진단 + PAPER_S2 결론 확정 (partial transfer)

- **3-8° 회귀 진단** (재학습 없이 raw vs PnP 분리, `scripts/stage0/paper_s2_regression_3to8_diag.py`):
  회귀는 **RAW heatmap 단계에서 이미 발생**(paired raw +1.0 / PnP는 +0.7만 추가), **PnP/DiffPnP 후처리 문제 아님**. decode-PnP mismatch도 아님(raw-PnP disp -0.74=stageB가 덜 당김). = 단단한 결론.
- **★agent 팩트오류 정정**: 진단 agent가 "Stage A=λ0, no DiffPnP"라 하고 "squash 단독범인/DiffPnP 중립"을 주장했으나 **틀림** — weights/paper_s2_stageA/header.txt 확인 결과 stageA도 `diffpnp_lambda=0.005`. → stageA는 λ0 baseline 아님. "squash 단독" 미분리(recipe 묶음 squash·scratch·데이터·DiffPnP 교란), "DiffPnP 중립" 미입증(λ0 full 모델 없음, DiffPnP 분리는 Q1 synth뿐). `regression_3to8_diagnosis.md` DISAMBIGUATION/DECISION 섹션 정정 완료.
- **PAPER_S2 결론 = PARTIAL 전이** (canonical: `data/pallet/results/paper_s2_scratch_diffpnp/CONCLUSION.md`). rear/검출/극저앙각(<3°) 전이 O, 전체 pose 정확도(honest8) X. claim/no-claim 문구 확정(CONCLUSION.md).
- **결정(사용자)**: ①partial-transfer 문서화 ②λ0 오류 정정 ③3-8° raw-vs-PnP montage → 다음 메인 = **각도-bin(<3/3-8/8-15/15+) 저앙각·near·transition 데이터 트랙**. λ0 full ablation 보류(회귀는 입력측이라 안 풀림, reviewer 요구 시만).
- 교훈: 표현/후처리(loss·PnP) 트랙은 rear 절대정확도 천장 못 뚫음 → 진짜 레버=데이터/appearance (STAGE15/16·corner01·stage22 정합).

## Stage B 9필터(f1~f9) 지정셋17 선택성 검사 — W/D swap 은 self-supervised 필터로 못 잡음
- s1_cad_9filters.py helper 재사용(재구현 X), Stage B squash-parity 추론으로 testset17(cad11+noapril6)에 f1~f9 적용. 스크립트 `scripts/stage0/paper_s2_testset17_9filters.py`, 산출 `data/pallet/eval_results/paper_s2_scratch_diffpnp/testset17_9filter_check.md`. dims (1.1,1.3,0.12).
- 검출 4/17(전부 noapril): ...411535(cm7.2/h8 7.8), ...432466(cm5.2/**h8 39.2=W/D swap**), ...442780(cm8.6/h8 7.3), ...447585(cm4.4/h8 9.7). 나머지 13 under-det(cad 11 near-field + noapril 2).
- ★불량 ...432466(2D corner 5.2px 우수, 3D pose 39.2 swap): 강신호 f1/f4/f5 전부 pass(confidently-wrong), 약신호 2D기하 f2/f6/f7 전부 pass(swap=valid cuboid라 blind). reject 한 건 GT참조 f9 bbox_iou(0.47)·f8 size(sr0.272)·잡음 flip f3(27.2)뿐. f9 만 clean(정상3 pass·불량 reject) 단 GT참조=PL 배포불가+n_det6 truncated bbox 우연 배제못함.
- ★S1(2026-07-07) 대비: appearance-conf 강/2D기하 약 경향 동일. 정제=강신호는 2D corner 품질엔 강하나 W/D swap(3D pose 모호성)엔 직교→배포가능 self-sup(f1~f7) 중 이 실패 잡는 필터 0. 결론=pose 모호성은 dims-known reproj-gate or GT로만 걸러짐(memory dimsfree-2d-geometry-filter-impossible 정합). caveat: N=4 극소·고앙각셋(real 대표 아님).

## 나무 파렛트(wood) 전용 어노테이션 런처 추가 — 공유 코드 불변
- 신규 `challenge/scripts/annotate_wood.py`. 기존 `annotate.py`/`annotate_pnp.py` **수정 안 함**(annotate_pnp.PALLET_DIMS 를 eval 20+곳이 import → 소스 변경 시 표준 파렛트 eval 전멸). 런처 프로세스 안에서만 `annotate_pnp.PALLET_DIMS = (0.8,0.59,0.14)` 런타임 override 후 `annotate.main()` 호출. eval 스크립트는 별도 프로세스라 불변.
- wood 파렛트 실측 = **80×59×14cm = (0.8,0.59,0.14)m**. dims W/D 순서는 solve_pose 가 (W,D,H)/(D,W,H) 둘 다 시도해 자동처리, H=0.14 고정. dry-run 검증: annotate 의 PALLET_DIMS copy=(0.8,0.59,0.14), 3D모델 범위 0.8/0.59/0.14.
- 프레임: `data/pallet/raw_data/wood/selected/<video>/*.jpg`(폰 아님, **RealSense D435I**). jpg 평면 → `<seq>/rgb/*.png` 심링크(cv2.imread 는 내용 디코드라 .png→jpg 심링크 OK) + cam_K.txt 자동 생성.
- ★K = 해상도별 RealSense 실측(2026-07-09 라이브 쿼리): 640×480(605.91/317.60/256.29, 4:3), 1920×1080(1363.29/954.59/576.66, 16:9). **wood 는 1280×720** → 1080p 에서 16:9 선형유도 **fx=908.86 cx=636.39 cy=384.44** [확인, 동일 FOV]. `K_for_resolution()` 자동선택. (옛 cad/noapril 어노 K=614.18 은 다른 캡처값 → wood 엔 현 카메라값 사용.) 640×480 은 4:3 라 1080p 와 선형관계 아님.
- 사용법: `python challenge/scripts/annotate_wood.py --video pallet_20260618_183705 --stride 5` (UI/키/저장은 기존 툴과 100% 동일). GT→`challenge/data/wood_<video>_manual_gt/`. 592×1280 세로영상(20260618_132917)은 16:9 아님→ --K/--hfov 필요. --K(파일)/--hfov(화각추정) override 지원.
- 미확인: 1280×720 직접 쿼리로 유도 K 재검(정확할 것이나 100% 확인). wood GT 아직 0장(툴만 준비).
