# 2026-08-02

세션이 08-01에서 자정을 넘겨 이어짐.  **08-01 커밋(`8f0a61f` 23:21)까지의 변경은
`_docs/history/2026-08-01.md` 에 기록되어 있다.**  Stop hook 이 나열한 14개 파일 중
대부분은 07-29 / 08-01 에 이미 커밋된 것이고, 오늘 실제 변경분은 아래 3건뿐이다.

## 코드/설정 변경 (미커밋 — 학습 진행 중)

```
파일                                                   상태    이유
──────────────────────────────────────────────────────────────────────────────
scripts/stage0/paper_s2_ppd_long_run.py                신규    PPD 3k/1k long-run runner
challenge/tests/test_paper_s2_palletgraph_line_screen  수정    weight 정책 테스트 교체 + 7건 추가
data/pallet/results/.../PURPOSE.md                     신규    purpose_gate hook 요구
```

### 1. `scripts/stage0/paper_s2_ppd_long_run.py` (신규)

L0/M0/M1 을 locked full split(train 3039 / val 1045)으로 20 epoch 학습하는 runner.

- **resume 가능**: epoch 마다 atomic save(tmp → `os.replace`)로
  `last.pth` / `epoch_NNN.pth` / `optimizer_last.pth` / `rng_state.pt` /
  `run_state.json` / `metrics_by_epoch.json` 저장.  재시작 시 optimizer·RNG·epoch 복원,
  `completed` 인 arm 은 재학습하지 않음.
- **root guard**: `guard_root()` 가 `paper_4pallet_mask_v1` 외 경로를 즉시 차단
  (banned: mixed_v8_train / v4_split_base / aug_*_v2).
- **arm 공정성**: 세 arm 이 동일 `init_line`/`init_mask` state_dict, 동일 sampler
  permutation(`seed*1000+epoch`), 동일 20 epoch 예산, early stop 없음.
- **checkpoint selection**: synthetic validation 만 사용 (`select_best` 안에
  `N87`/`mechanism_val`/`untouched` 문자열이 없음을 테스트로 강제).
- **candidate scoring**: 원본 해상도 T2 support 로 candidate 생성,
  learned map 은 native 100×100 에서 bilinear sampling (resize 경로 미사용).

⚠ **작성 중 실수 1건**: `cat >> ... <<'PYEOF'` 두 번째 블록이 파일에 붙지 않아
`main()`/`__main__` 이 없는 채로 실행됨 → exit 0 인데 출력·산출물 0.
`grep -n '__main__'` 로 확인 후 재추가.  **exit code 0 을 성공으로 믿으면 안 되는 사례.**

⚠ **purpose_gate hook 대응**: hook 이 `--output_dir` 인자가 없으면 CWD 를 보므로,
runner 에 `--output_dir` 를 추가하고 그 폴더의 `PURPOSE.md` 존재를 코드에서도 검사.
`cd` 로 우회하지 않고 인자로 실험 폴더를 명시하는 방식을 택함.

### 2. `challenge/tests/test_paper_s2_palletgraph_line_screen.py`

Before: `test_no_heldout_training_while_overfit_gate_fails` — weights 아래 `.pth` **0개**를 요구.
After: `test_checkpoints_live_only_in_the_approved_weights_root` —
checkpoint 는 로컬 산출물로 허용하되 **git tracked/staged `.pth` 0개**,
승인된 weights root 밖 PPD checkpoint 금지, ep57 SHA 불변을 요구.
이유: long-run 은 checkpoint 를 남겨야 하므로 "0개" 규칙이 더는 맞지 않음.
(`weights/` 는 `.gitignore` 대상이라 구조적으로도 커밋 불가 — 확인함.)

long-run 무결성 테스트 7건 추가: training root guard, 20 epoch·no-early-stop,
PURPOSE.md 요구, gate threshold, checkpoint selection 이 real/untouched 미참조,
provenance 기록, candidate-pair 와 scored 분리 보고.

**pytest 190 passed.**

### 3. `data/pallet/results/paper_s2_palletgraph_line_screen/PURPOSE.md` (신규)

[소비처] PalletGraph-6D architecture 결정(mask branch 유지/삭제, CGR 재개 여부).
[문장] "학습된 5-channel polarity line map 은 고정된 oracle SAI-U candidate set 에서
upright candidate 를 held-out synthetic 과 real N87 에서도 선택할 수 있다."
판단지표: primary = candidate polarity(H3), diagnostic = pixel/mask(H1·H2, 기존 FAIL 유지).

## 실행 중 (미완)

`--all-long-run` 진행 중 (detached nohup, PID 4140948).  **L0 epoch 18/20 시점.**

```
validation 754 candidate-pair frames (group-disjoint, train 미포함)
L0 epoch 1~18: polarity 0.962 ~ 0.980,  inversion 15~29/754
unsigned baseline indexed reproj = 105.58 px
high-res feature = vgg[17], 256 ch, 100x100 assert 통과
```

[확인] **32-frame 암기가 아니다** — 학습에 쓰지 않은 754 프레임에서 polarity 0.96~0.98 유지.
[확인] macro F1 0.29~0.39 / mask IoU 0.097 로 pixel 지표는 여전히 낮다.
overfit 에서 본 "선은 두껍지만 polarity 는 맞음" 패턴이 held-out 에서도 재현된다.

남은 단계: M0/M1 학습 → validation gate → untouched 5,916 test → real N87 one-shot
→ 조건부 CGR → 보고서 → commit/push.

## 세션 정리

모니터 셸 2개(`bo3yr5m9k`, `bzcd8ymy9`)는 TaskStop 으로 종료.
학습 프로세스(4140948)는 **detached nohup 으로 의도적으로 유지** — 세션과 무관하게
계속 돌아야 하며, 중단하면 20 epoch 예산이 깨진다.

## 무결성

final-test open 0, ep57 checkpoint SHA 불변, data root 수정 0건,
training root 는 `paper_4pallet_mask_v1` 단독, N87 membership 0,
git tracked/staged `.pth` 0개.

---

## (append) Long-run 완주 — untouched PASS, real FAIL, CGR NOT RUN

### 학습 완료

3 arm × 20 epoch = **60/60**, run_state 전부 `completed: true`.
validation(754/1045 candidate-pair): L0 ep4 0.980 / M0 ep8 0.980 / M1 ep20 0.977, 전 arm PASS.
checkpoint 를 이 시점에 고정하고 이후 바꾸지 않았다.

### untouched 첫 실행 OOM → chunk 재실행

Before: `evaluate_untouched` 가 5,916 프레임을 한 번에 적재 → **OOM Killed**(RAM 31GB).
After: `UNTOUCHED_CHUNK=400` 청크 처리 + `_merge()` 가중 병합.
**metric 정의·threshold·candidate set 불변, 메모리 상주 방식만 변경.**
OOM attempt 는 산출물을 남기지 못해(단일 파일 08:34:37 생성) partial merge 위험 없음.

### untouched 5,916 결과

```
arm  availability      conditional  inversion    indexed reproj  gate
L0   3716/5916 (0.628)   0.951      183/3716     1.16 px        PASS
M0   3716/5916 (0.628)   0.950      187/3716     1.16 px        FAIL(0.0003 미달)
M1   3716/5916 (0.628)   0.952      180/3716     1.15 px        PASS
baseline 98.14 px
```
availability 가 validation 0.721 → 0.628 로 하락 = **upstream SAI 문제**(selection 이전).

### ★ real N87 one-shot — 전 arm FAIL, 방향이 반대

```
L0 polarity 0.023 (inv 84/86)  reproj 60.69px  point-fail 1/17
M1 polarity 0.012 (inv 85/86)  reproj 64.65px  point-fail 0/17
참고: S0 30/86·155.6px / H0 26/86·16.5px / O0 3/86·7.39px
```

0.023 은 우연(0.5)이 아니라 **반-상관**이므로 계통 원인이 있다.

**결정적 실험**: 동일 N87·동일 candidate 에서 oracle 5-class map 을 long-run scorer 로 채점
→ **86/86 = 1.000**.  scorer·candidate·reference pose·좌표 규약 모두 정상이며
**learned map 자체가 real 에서 틀린다**는 것이 확정됐다.

**원인 = top↔base 스왑이 아니라 base 로의 붕괴**:
```
GT top_width 위치 → pred base 0.683 vs top 0.206
GT top_depth 위치 → pred base 0.781 vs top 0.146
predicted 면적: base 0.242~0.295 > top 0.183~0.187
```
[추정] target 클래스 불균형(base positive-frame 0.975~0.985 vs top 0.620~0.685)이
synthetic 에서는 가려졌다가 top 근거가 약한 real(94% 저앙각)에서 발현.

### CGR — NOT RUN

real gate 통과 arm 0개.  조건부 규칙 준수, 재학습·threshold 조정 없음.

### Mask 결정 — DROP

mask 는 pixel 지표만 올리고(macroF1 0.311→0.409, IoU 0.097→0.746) 목적 지표는
개선하지 않음(real 은 L0 0.023 > M1 0.012 로 L0 우세).

### 코드 변경 (추가분)

- `scripts/stage0/paper_s2_ppd_long_run.py` — `evaluate_untouched`(청크),
  `evaluate_real`, `_merge`, `load_best`, `gate_check`, `unsigned_baseline`,
  `--evaluate-test/--evaluate-real` 추가.
- `challenge/tests/...` — long-run 결과 검증 6건 추가.
  ⚠ `test_checkpoint_selection_never_reads_real_or_untouched` 가 정당하게 실패:
  `select_best` 이후 파일 끝까지를 슬라이스해 나중에 추가한 untouched 코드까지 포함했다.
  Before: `source[source.index("def select_best"):]` /
  After: `inspect.getsource(LR.select_best)` (함수 본문만).

### 판정

SAI-U ACCEPT / 5-class 표현 ACCEPT(oracle 상한) / L0 REJECT / M0·M1 DROP /
Learned PPD **REJECT**(현 학습 데이터·target 조건에서) / CGR NOT RUN / point uncertainty DEFERRED.

### 무결성

ep57 SHA 불변, training root `paper_4pallet_mask_v1` 단독, N87 membership 0,
untouched/real checkpoint selection 0, final-test 미개봉,
git tracked/staged `.pth` 0.  pytest **196 passed**.

## [14:49:59] ⚠️  Auto Compact (컨텍스트 한계 도달)

- Working directory: `/home/minjae/Documents/github/pallet-pose`
- Trigger: `auto`
- Session ID: `1f06bfde-fd3e-4103-a3ef-711599683949`
- Transcript: `/home/minjae/.claude/projects/-home-minjae-Documents-github-pallet-pose/1f06bfde-fd3e-4103-a3ef-711599683949.jsonl`
- Note: 컨텍스트가 가득 차서 자동 트리거됨 — 다음 세션에서 이 마커 위쪽 작업을 반드시 복원할 것

**TODO (session resume)**: compact 이후 이 마커 위쪽의 작업 내역을 확인하고 이어서 진행


---

## (append 2) Bootstrap · runtime · figures · 최종 보고서

### ⚠ bootstrap 첫 계산이 틀렸고 잡아냄

첫 시도가 real polarity 0.419 를 냈다 — 실제 0.023 과 모순.
원인: `inversion_frames` 가 소스에서 `[:50]` 으로 **잘린 목록**인데 이를 전수로 오인
(86-50=36 → 36/86=0.419).  frame_id 매칭 실패가 아니라 **내 해석 오류**.
Before: `inversion_frames` 로 per-frame 복원 → cluster bootstrap
After: 집계값(n_scored, inversion) 기반 Wilson CI + `slices_domain` per-domain CI.

cluster-level CI 는 계산하지 않았고 사유를 명시했다:
untouched 는 candidate-pair frame id 가 저장되지 않고, real 은 session 이
outside/night **2개뿐**이라 cluster bootstrap 이 성립하지 않는다.

```
set                  arm   n     correct   acc      95% CI
untouched            L0    3716  3533      0.9508   0.9433~0.9573
untouched            M1    3716  3536      0.9516   0.9442~0.9580
real_N87             L0      86     2      0.0233   0.0064~0.0809
real_N87             M1      86     1      0.0116   0.0021~0.0630
real domain=outside  L0      43     1      0.0233
real domain=night    L0      43     1      0.0233
```
gate 0.95 는 real CI 상한 0.081 **밖** = 표본 잡음 아님.
outside 와 night 가 **완전 동일** → 조도·야간 가설 기각.
truncation 유무도 안 갈림(0.014 vs 0.059) → 잘림 가설 기각.

### runtime (warmup 20 / measure 100, 1x3x400x400, cuda)

```
base only      21.05 ms
base + head    21.39 ms
head overhead   0.34 ms (1.6%),  head params 181,573
```
싸다는 사실이 채택 근거가 되지는 않는다(real gate FAIL).

### figures — `data/pallet/results/paper_s2_palletgraph_line_screen/figures/`

`ppd_validation_curves.png` / `ppd_synthetic_vs_real.png` / `ppd_real_scorer_ladder.png` /
`ppd_real_polarity_confusion.png` / `ppd_real_class_collapse.png` / `ppd_real_bootstrap_ci.png`

### 보고서

`PPD_VALIDATION_REPORT.md`, `PPD_UNTOUCHED_TEST_REPORT.md`, `PPD_REAL_ONE_SHOT_REPORT.md`,
`PPD_REAL_DOMAIN_GAP.md`, `PPD_MASK_DECISION.md`, `PPD_RUNTIME_REPORT.md`,
`PPD_ARCHITECTURE_DECISION.md` — 전부 `_docs/audits/` 에 복사.

### domain gap 결론

원인 1순위 = **target 클래스 불균형**(base positive-frame 0.975~0.985 vs top 0.620~0.685) [추정],
2순위 = **저앙각 분포 부재**(학습 root V=8 clean full-view 100%, real 94% <8° edge-on) [확인].
appearance sim2real 은 3순위.  재학습·threshold 조정 0회.

---

## (append 3) 오버레이 시각화 — real 실패의 1차 증상은 localization 소실

사용자 요청으로 결과를 이미지에 오버레이. `scripts/stage0/paper_s2_ppd_overlay.py` 신규.
`paper_s2_ppd_long_run.py` 의 real 프레임 구성을 `real_frames()` 로 추출해 재사용
(중복 구현 회피, evaluate_real 은 이제 이를 호출).

4-panel: ① 이미지+reference pose ② 예측 5-class map ③ upright(초록) vs 선택된(빨강)
candidate ④ base−top 차이 heatmap.  header 에 vertical-polarity 오차 표기.

**렌더러가 독립적으로 84/86 반전 재현** = 보고값 0.023(2/86)과 일치 [확인].

### ★ 숫자로는 안 보였던 것

real 프레임에서 예측 map 이 팔레트가 아니라 **나무·건물·울타리·바닥 전체**에 반응한다.
top/base 혼동 이전에 **대상 위에 있지도 않다.**

```
                positive 중 팔레트 위 비율   cuboid 면적비   enrichment
synthetic val60       0.456                   0.105         4.35x
real N87              0.086                   0.040         2.14x
```
real 은 positive 의 91%가 배경.  면적 보정한 enrichment 로도 절반(4.35→2.14x).
맞힌 2 프레임도 map 이 깨끗해서가 아니라 우연히 부호가 맞은 것.

→ §3 "base 로 붕괴"는 팔레트 위 현상이 아니라 **배경 활성이 line energy 를 지배**한 결과.
[추정] L0 는 활성을 대상에 묶는 장치가 없음.  단 mask arm(M1)도 real 0.012 라
mask head 만으로 해결되지 않음.

### 작업 중 수정 3건
`vertical_polarity_error_deg` dims 인자 누락 / `cv2.COLORMAP_COOLWARM` 부재(→JET) /
val manifest 는 `frames` 키(`files` 아님).

저장: `data/pallet/results/paper_s2_palletgraph_line_screen/figures/overlays/`
(inverted 3, correct 1, synthetic 3), 수치 `ppd_on_object_activation.json`.
보고서 `PPD_REAL_DOMAIN_GAP.md` §3b, `PPD_ARCHITECTURE_DECISION.md` [Visual evidence] 추가.

---

## (append 4) PGBC feasibility gate G0/G1/G2 — 전부 FAIL, 구현 중단

사용자 결정에 따라 PGBC 전체 구현·16-frame overfit 을 바로 시작하지 않고
**학습 없는 feasibility gate 3개**를 먼저 실행.
신규 `scripts/stage0/paper_s2_pgbc_screen.py`, 산출 `data/pallet/results/paper_s2_pgbc_screen/`.
기존 mechanism cache(87 frame) 재사용, G1 만 shared feature 위해 frozen forward 87회 추가.

### Phase A 재현 [확인]
strict 87 / GT-2D 87 / pred 70 / yaw 6.025216° / reproj 23.161629px / F2 35 / F1 24
— 전부 기대값 일치.  ckpt SHA c0055fe7 불변.

### 결과

```
gate                       metric                            값       기준     판정
─────────────────────────────────────────────────────────────────────────────────
G0 RESIDUAL_CAPACITY       far corner error 50% 감소 비율     0.283    >=0.80   FAIL
G1 FEATURE_OBSERVABILITY   fold 최소 AUC                     0.591    >=0.75   FAIL
                           fold 최소 GT>wrong 비율            0.600    >=0.70   FAIL
G2 GRAPH_INFORMATION       far median error 감소              0.018    >=0.20   FAIL
                           far signed bias 감소              -0.023    >=0.20   FAIL
```

### G0 — 산술적 불가능

```
 bin      n   belief@wrong  belief@GT   ±0.25후 GT승   argmax→GT   err median
 <5px     4      0.953        0.953        1.000        1.000     2.1 → 2.1
 5-20px  24      0.900        0.727        1.000        1.000    11.5 → 10.5
 20-50px 49      0.822        0.407        0.735        0.980    32.7 → 14.8
 >50px   61      0.778        0.003        0.033        0.131    76.3 → 76.3
```
>50px 에서 belief@GT = **0.003**.  ±0.25 후 GT 0.253 vs wrong 0.528 → 못 이김.
필요 amplitude > (0.778-0.003)/2 = **0.39**.  통계가 아니라 산술 문제.
이 구간(61/110)이 정확히 PGBC 표적인 confidently-wrong 레짐.
20-50px 는 argmax 98% 이동 = residual 메커니즘 자체는 작동.

★ **Phase C1 spec 결함 발견**: `T_global=1.0` spatial softmax 는 belief∈[0,1] 이라
exp 비가 2.16배뿐 → 거의 균등.  base 조차 global coord error **144.5px**.
T 낮춰도 top1(42.8px)로 수렴할 뿐 이득 없음 (T=0.01 → 42.1px).
G0 의 global read-out 이 전 구간 0.000 인 것은 residual 무능이 아니라 이 spec 때문.

### G1 — 종료 사유, 기전까지 규명

159 pair(far 110/near 49), F50 = vgg trunk 50×50 **128ch**, session-grouped 3-fold.
```
linear (spec)              AUC 0.613/0.642/0.591    GT>wrong 0.600/0.691/0.633
MLP-64 (진단)               AUC 0.585/0.642/0.581    → 비선형도 개선 없음
GT vs 무작위 위치 (진단)      AUC 0.864/0.769/0.858    → 명확히 우연 위
control: feature 제거        AUC 0.501/0.497/0.502    → 정확히 우연(구조상 당연)
```
[확인] control 이 0.50 = probe 정직함 입증(corner ID·dims 는 pair 내 동일).
[확인] frozen feature 는 **"팔레트 vs 배경"은 알지만 "이 위치가 corner k 인가"는 모른다.**
wrong peak 도 팔레트 위이기 때문.  → 옮길 목적지를 알 정보가 없음.

### G2
predicted 7 corner(GT 미사용) PnP → 제외 corner 재투영:
far 138, median 44.59→43.80px(1.8%), bias 20.58→21.06px(오히려 증가).
F2 는 나머지 7개도 함께 틀려 복원 신호 없음.

### 판정 및 fallback 처리

C1/C2/C3 전부 REJECT, PGBC REJECT, DiffPnP NOT RUN.  Final path = **base DOPE only**.
사전 지정 fallback `P_ref=(1-g)P_base+g·Q_global` 도 **구현하지 않음** —
G0 은 우회하지만 Q_global 재료가 frozen F50 이고 G1 이 그 F50 에 판별 정보가 없다고
말하기 때문.  결과 보고 component 되살리지 않는 규칙 적용.

### 작업 중 수정 4건
manifest 에 manual 탐색 population 혼재 → `population=="primary"` 필터 /
truncated GT corner 가 50×50 밖 → 버리지 않고 `gt_in_grid` flag(버리면 통과율 부풀려짐) /
`FZ.load_model(device)` 인자 / `FZ.project_corners` 부재 → `APNP.make_pallet_keypoints_3d`+`project_3d`.

### 산출
`PGBC_COMPONENT_DECISION.md`(주 판정) + OVERFIT/SESSION_CV/BIAS/DIFFPNP/ARCHITECTURE = NOT RUN 명시,
`PGBC_RUN_PROVENANCE.md`, CSV 4종, 그림 3장(`figures/`), 테스트 13건 추가 → pytest **209 passed**.

---

## (append 5) Corner proposal replacement 5-epoch screen — STOP (12개 중 10개 FAIL)

frozen corrector(PGBC)가 G1에서 막힌 뒤, corner identity를 feature 자체에 형성시키는
부분 end-to-end 재학습을 시험. ep57 initialization, canonical Stage-B 전체 dataset.

### 신규/변경 파일
- `Deep_Object_Pose/common/corner_proposal_replacement.py` (신규 architecture 1개)
- `scripts/stage0/paper_s2_corner_replacement_screen.py` (학습·평가 runner)
- `scripts/stage0/paper_s2_corner_replacement_report.py` (Phase E~G 분석·게이트)
- `Deep_Object_Pose/train/train.py` — dataset+sampler 구성을 `build_training_loader()`로
  **추출**(main()이 이를 호출, 동작 불변). 목적: screen runner가 canonical 구성을
  재구현하지 않고 그대로 재사용.
- `Deep_Object_Pose/common/utils_dataset.py` — `dims_m`/`dims_valid` **additive 키만** 추가
  (image/belief/affinity 파이프라인 불변). mixed_v8_train엔 dimensions_m이 없어
  조작 대신 validity flag 0.

### 실행
canonical 6 roots, loader 기준 **29,308 frames** / 2,443 batches @ batch 12,
60:40 sampler(19,308 : 10,000), F100=vgg[17] 256ch, F50=vgg[26] 128ch (runtime 탐색).
trainable = vgg_last 5,014,912 + stage4-6 12,567,579 + branch 334,081 = **17,916,572**.
5 epoch **77.9분** (15.2~16.5분/epoch, 29~33 samples/s, peak GPU 2,862MB, batch12 OOM 없음).

### 결과

```
arm            F2 far   signed   near     >20px  >50px  PnP    reproj    yaw
C0 (ep57)       44.59    20.58    6.88     203    120    70    23.16     6.03
C1-base         44.93    19.91    7.67     215    137    69    25.57     5.97
C1-proposal    160.34    71.57   18.97     462    387    87   108.30    42.59
C1-refined      44.93    19.91    7.67     215    137    69    25.57     5.97
```
게이트 12항목 중 10 FAIL → **STOP**.

### ★ 현상 1 — gate 붕괴로 replacement가 한 번도 작동 안 함
epoch5 gate: min 1.13e-09 / median 3.53e-09 / max 1.99e-08.
max|base−refined| = corner err 3.3e-08px → C1-refined ≡ C1-base.

**원인 = 사전 고정 상수의 산술**: calibration이 λ_ref=3.07e-04를 냈는데
λ_gate=0.01(사전 상수)이 **33배** 크다. g를 키워 얻는 이득(3.07e-4×O(1))이
비용(0.01×g)을 못 이기므로 g→0이 최적해. epoch1 step400에 이미 0.
금지항목 8·F2에 따라 조정하지 않고 완주 후 보고.

### ★ 현상 2 — proposal은 학습됐으나 decoder와 operating point 불일치
raw L_proposal 6.05(calib)→1.30(ep1)→1.02(ep5)로 실제 하강. 그런데
sigmoid(Q) peak가 **0.435~0.695**로 배경이 0 근처로 안 내려감 → decoder threshold 0.3을
전 cell이 통과 → 배경에서 peak. C1-proposal PnP 87/87은 정확도가 아니라 항상 8점을
내놓기 때문(far 160px, yaw 42.6°).
**교훈: operating range를 맞추는 것 ≠ operating point를 맞추는 것.**
belief range 감사(min −0.030 / max 1.004)는 통과했지만 희소성은 못 맞춤.

### 현상 3 — 후반 fine-tuning 자체가 real에서 소폭 악화
synthetic L_DOPE 0.001088→0.001018 계속 하강했으나 real은 reproj +10.4%, PnP 70→69,
>50px tail 120→137. **synthetic→real 전이 실패** (PPD long-run과 같은 형태).

### 판정
Trainable corner feature REJECT / Global corner proposal REJECT /
Replacement gate **INCONCLUSIVE**(작동 자체를 안 해 시험 안 됨) / 전체 STOP.
Final path = **base DOPE**(ep57 유지). 추가 epoch·LR/loss 수정 없음.

### 작업 중 수정 4건
extract한 함수가 `use_corner_quality` 참조 → 파라미터로 교체 /
trainable 경계를 `index>index_low`로 잡아 vgg_last=0 → `index>index_high`(마지막 50×50 block) /
signed dx/dy 미저장 → `--reevaluate`로 양쪽 재평가 /
`--reevaluate`가 epoch1~4도 허용하는 구멍 → BLOCKED 가드 + 테스트 추가.

pytest **228 passed**.

---

## (append 6) Proposal raw-Q 재평가 + oracle router — 재학습 0, router NOT RUN

앞선 screen에서 내가 `sigmoid(Q)`+절대 threshold 0.3으로 평가한 게 학습 objective
(spatial softmax)와 불일치했으므로, epoch-5 checkpoint를 **재학습 없이** raw Q로 재평가.

신규: `Deep_Object_Pose/common/corner_branch_router.py`(decoder 3종 + oracle + router MLP),
`scripts/stage0/paper_s2_proposal_router_screen.py`, 테스트 15건.
Phase A 전부 통과 (base SHA c0055fe7 / epoch5 SHA aad97f6b / run_state 5-5 completed /
C1-base PnP 69·reproj 25.57 / gate median 3.53e-09 / max|base−refined| 3.29e-08px).

### raw-Q decoder 결과 (interface는 원인의 "일부"였을 뿐)

```
arm       median   near     far      F2 far   >20px >50px   PnP   reproj
C0        13.24     6.88    22.08     44.59    203   120     70    24.91
C1base    13.95     7.67    24.32     44.93    215   137     68    25.03
Pargmax   80.07    17.87   119.91    161.92    453   387     87   112.11
Plocal    80.15    16.29   117.94    162.39    455   388     87   115.07
Pdsnt    115.89   109.93   119.65    135.08    611   533     86   121.47
```
far median이 160.3(sigmoid) → 117.9(raw local)로 나아졌으나 base 22.1px의 5배.
**P-ARGMAX ≈ P-LOCAL(80.07 vs 80.15) → 정제 문제가 아니라 top-1 위치가 틀림.**
P-DSNT near 109.9px → Q가 뾰족하지 않음(학습 objective 자신의 read-out이 최악).
PnP 87/87은 여전히 "미검출 경로 없음" artifact.

### 상보성 — 유일한 양성 신호

```
group             n     better   >3px    >10px   base med  prop med
all              519    23.5%    18.1%    9.8%     13.24     58.75
confident_wrong  181    34.3%    32.0%   22.7%     61.58    140.23
```
confident-wrong에서 22.7%가 10px 이상 우세 → C3 gate 조건 D 단독 PASS(A·B FAIL).
단 같은 그룹 median은 proposal 140.2 vs base 61.6 = **가끔 크게 맞고 대체로 크게 틀림**.

### Oracle router 상한 — FAIL (4/7)

```
oracle_margin: F2 far 44.59→36.94(-17.2%, 기준-20%) FAIL / signed bias -12.0% FAIL
               >50px 120→95(-20.8%) PASS / PnP 70→70(기준>=74) FAIL
               reproj 24.91→20.53(-17.6%) PASS / near 6.88→5.72 PASS / NaN PASS
proposal 채택률 exact 17.5% / margin 13.5%
```
GT를 보고 고른 상한조차 기준 미달, 특히 **PnP가 70→70으로 전혀 구조 안 됨**.
→ 규정대로 learned router **NOT RUN**.

### ★ harness parity 이슈 (실측 확인)
```
centroid 포함 (canonical) : PnP 70/87  reproj 23.161629 px
centroid 제외 (내 harness): PnP 70/87  reproj 24.913784 px
```
canonical DOPE PnP는 centroid를 correspondence로 **포함**한다. 지시문 금지항목 13을
따라 제외했으므로 C0 앵커가 canonical과 다름. 전 arm 동일 제외라 상대비교는 공정하나
절대 reproj를 23.162와 직접 비교 금지.

### 판정
Shared base fine-tuning REJECT / Raw proposal branch REJECT / decoder=LOCAL(최선이나 미달) /
Coordinate router NOT RUN / Map-level gate REJECT / **Final path = base DOPE**.

### 작업 중 수정 2건
decoder 금지어 테스트가 docstring을 코드로 오인 → ast로 docstring·주석 제거 후 검사 /
soft-mixture 테스트 assertion 논리 오류(`not in or in`) → 실행코드 검사 + dtype 확인으로 교체.

pytest **243 passed**.

---

## (append 7) Stage-wise bias loss 5-epoch screen — STOP (12개 중 9 FAIL)

architecture 불변(신규 branch 0), belief stage 4~6만 학습, legacy loss 유지.
신규 `Deep_Object_Pose/train/stagewise_corner_loss.py`(mass/rank/distance/progress),
`scripts/stage0/paper_s2_stagewise_bias_screen.py`, `..._report.py`, 테스트 20건.

### Phase A/D 통과
baseline 87/87/70, yaw 6.025216, reproj 23.161629.
stage 궤적 재현: far stage4 21.42 → stage6 22.08px, peak 0.695→0.804 (현상 확인).
Phase D synthetic gradient: wrong peak 하강방향·GT 상승방향·49셀 거리에서도 gradient·
progress stage4 detach·border cropped window·centroid gradient 0 — 전부 통과.

### Gradient-norm calibration (raw magnitude 아님)
기준 `m6_2.12.weight`, 8 batch, update 없음.
```
term      |grad| median   lambda      weighted/legacy
legacy    7.5922e-04       —           1.00
mass      9.710e-01        1.564e-04   0.20
rank      1.906e-01        5.976e-04   0.15
distance  3.012e-01        2.521e-04   0.10
progress  7.804e-01        4.864e-05   0.05
```
clamp 없음. 신규 loss gradient가 legacy의 250~1300배라 raw magnitude로 맞췄으면 크게 어긋났을 것.

### 실행
29,308 frames / 2,443 batches @ batch12, trainable 42 tensors **12,567,579** params,
frozen audit 전부 0, 5 epoch **58.9분**(11.5~12.6분/epoch, peak GPU ~2.9GB).

### 결과 — STOP

```
                     C0        C1        변화
median              13.24     12.36     -6.7%   개선
near median          6.88      6.81     -1.0%   개선
F2 far median       44.59     44.17     -0.9%   (기준-15%)
F2 signed bias      20.58     20.23     -1.7%   (기준-20%)
p90                101.16     93.04     -8.0%   개선
>50px tail            120       108    -10.0%   (기준-15%)
>100px tail            54        45    -16.7%   개선
reproj              23.162    21.975    -5.1%   (기준-10%)
canonical PnP       70/87     67/87       -3    악화(새 실패 3)
NaN corner            177       182       +5    악화
sharpen-no-correct    107       195    +82.2%   ★역방향
```

### ★ 겨냥한 현상이 오히려 심해짐
```
arm stage  far_err  far_peak  far_mass  far_wrong
C0    4     21.42    0.759     0.201     0.695
C0    6     22.08    0.855     0.215     0.772
C1    4     21.99    0.743     0.162     0.682
C1    5     21.18    0.843     0.195     0.760
C1    6     22.17    0.889     0.185     0.775
```
C1 stage6 peak가 0.855→**0.889로 더 상승**, wrong peak 0.772→0.775 불변,
GT mass 0.215→**0.185 감소**. stage5에서 21.18px로 개선됐다가 stage6에서 22.17px로 회귀
= progress 제약이 real에서 안 걸림.

### synthetic에서는 의도대로 작동
학습 raw mass loss 1.27→0.56 → synthetic GT mass ≈ 0.57. 같은 모델 real far GT mass 0.185.
**loss는 학습됐고 real로 전이되지 않았을 뿐** (PPD·corner replacement와 동일 형태).

### 판정
Stagewise GT-mass loss REJECT / Wrong-peak suppression REJECT /
Stage-progress constraint REJECT / DiffPnP **NOT RUN** / Final path = **base ep57 DOPE**.

### 작업 중 수정 1건
`calibrate`가 `net(images)[0], net(images)[1]`로 forward를 2회 실행 → 단일 forward로 교체
(lambda 확정 전에 수정).

pytest **264 passed**.

---

## (append 8) Predicted-seed DiffPnP inference screen — REJECT, 최종 "변경 없음" 확정

재학습 0 step, optimizer 생성 0. ep57 heatmap·decoded coordinate 불변.
canonical PnP가 이미 성공한 **동일 70 frame**(membership 10a8b40b…)에서만 D0 vs D1.
신규 `refine_pose_from_predicted_seed`(diffpnp3d_loss.py에 helper 추가),
`scripts/stage0/paper_s2_predseed_diffpnp_screen.py`, 테스트 13건.

### ★ 실행 전 확인 (사용자 [의심] 검증)
canonical `APNP.solve_pose`는 모든 candidate를
`cv2.solvePnP(useExtrinsicGuess=True, SOLVEPNP_ITERATIVE)`로 **이미 LM refine**한다 [확인].
사용자가 지적한 대로 추가 GN이 같은 최적점 반복일 가능성이 높다는 예측을 사전 기록 후 실행.

### 결과 — REJECT (12개 중 4 FAIL, primary가 역방향)

```
                              D0          D1         변화
observed reprojection      11.9591      6.3452     -47.0%   (목적함수)
fixed indexed GT reproj    23.1616     24.2043      +4.5%   ★악화 (기준-5%)
3D corner (m)               0.4516      0.3649     -19.2%   PASS
yaw (deg)                   6.0252      6.7828      +0.76   FAIL(기준+0.25)
signed rotation                                    +13.8%   FAIL
translation                                        -38.1%   PASS
improved/worsened          42 / 28  (기준 >2배=56)          FAIL
accepted/rejected GN step  95 / 185     fallback 0
```

**predicted 2D에 더 잘 맞출수록 진짜 pose가 나빠졌다** — 지시문이 미리 못박은
"observed 감소만으로 PASS 금지" 조항에 정확히 해당.

translation −38%인데 rotation +13.8% → 3D corner(translation 지배)는 개선,
reprojection(rotation 민감)은 악화. GN이 pose를 평행이동으로 끌어당기고 회전을 망침.

[추정] 66% step이 reject됐지만 나머지가 observed를 절반으로 낮췄다 =
canonical PnP 반환 pose는 이 9점 reprojection의 최소점이 아니다.
canonical solve_pose가 degeneracy guard·click violation 등 **복합 점수**로 후보를
고르기 때문으로 보이며, 그 추가 기준이 보호 장치였던 것.

### Parity
이번 harness D0 reproj **23.1616px = canonical 23.161629px 일치**
(직전 screen에서 centroid 제외로 24.91px였던 것과 달리, 지시문대로 centroid 포함).

### 최종 확정
```
A. base ep57 DOPE → canonical decoder → centroid 포함 canonical OpenCV PnP
최근 실험에서 채택된 변경: 0개
```
`FINAL_ACCEPTED_ARCHITECTURE.md`에 ACCEPT/REJECT 전체 표 기록.

### 관통 소견
여섯 시도 모두 synthetic 목적함수는 실제로 내려갔고(PPD mass, proposal 6.05→1.02,
stagewise 1.27→0.56, GN observed 11.96→6.35) real에서는 전이 실패 또는 역방향.
남은 레버는 decoder·solver·후처리가 아니라 **2D 예측의 계통 편향과 학습 분포**.

### 작업 중 수정 2건
`object_up_axis_index` 부재 → 기존 `PG.corner_error_sym` 재사용(재구현 회피) /
그 signature가 dict 2개+dims → 호출 형태 교정.

pytest **277 passed**.
