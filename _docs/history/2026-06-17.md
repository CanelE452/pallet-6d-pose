# 2026-06-17

## 조사 — paper_base 학습 데이터 출처 추적 + 문서 stale 정정 (코드 변경 없음)

세션 전체가 추적/질의응답. 아래는 **문서와 실제가 어긋난 사실을 실제 파일/로그로 확정**한 결과.

### 1. paper_base 실제 학습 데이터 (문서 stale 정정)

- `weights/paper_base_train.log` 기준 [확인]:
  ```
  총 17,362장(로더 17,340), scratch 60ep
  data/pallet/training_data/mixed_v8_train   9000  합성 base (camera-facing v4)
  data/pallet/training_data/aug_squash       2819  비율강건(독립축 warp)
  data/pallet/training_data/aug_trunc        3929  truncation(crop+reflect-pad)
  data/pallet/training_data/aug_scale        1592  isotropic zoom
  ```
- **`_docs/models/paper_base.md` 는 stale**: "미학습" + truncation 출처를 `challenge/data/training/truncation_crops_dope/pretrain` 으로 적었으나, 실제는 06-06 학습 완료 + 위 4폴더(전부 `data/pallet/training_data/` 아래)다. `truncation_crops_dope` 는 DOPE crop_pretrain 전신용이지 paper_base 가 쓴 게 아님.
- v1/v2(palletobj, 내 실제 파렛트)는 미포함 — 논문 트랙 제약 준수 확인.

### 2. mixed_v8_train 9000 출처 분해

- 문서(`_docs/experiments/self_training/archive/phase1_presentation_outline.md:75`)와 JSON `source_asset` 전수 집계가 일치 [확인]:
  ```
  Isaac 2000     train/ 계열(Isaac Sim 생성)        source_asset 없음(<none>)
  Blender 2000   blender_train_manydir/             scene*.usd
  Blender 4000   test_blender_4000/                 scene*.usd
  Blender 1000   test_blender_v1000/(variant)       scene*.usd
  ```
  - source_asset 집계: `<none>` 2000 + scene.usd 2940 + scene_1 1537 + scene_2 1318 + scene_3 1205 = 9000.
  - `scene*.usd` = `data/pallet/models_usd/` 팔레트 4종(Blender 가 USD 임포트 렌더). 4개 원본 폴더 모두 현존.
  - 비율 Isaac 22% : Blender 78%.

### 3. truncation 증강 방식 = aug_trunc (crop + reflect-pad)

- 생성: `challenge/scripts/augment_ratio_robust.py --kind trunc` (소스 mixed_v8_train) [확인]:
  1. L/R 위주 frame-edge clipping(top 거의 제외, memory truncation-side-cut-bias)
  2. 화면 밖 코너 sentinel 안 버리고 좌표 보존
  3. **reflect-pad** 로 캔버스(640x480) 안쪽에 되살림
  4. DOPE CreateBeliefMap 이 padding 영역 코너도 supervise (kp 가 50-map 안 ≥2σ, MARGIN_FRAC=0.20)
- camera-facing 0123 순서 보존(crop=orientation-preserving, **수평 flip 의도적 미사용**). 3D 필드 stale 통과(2D belief 만 학습 → 무해, 단 평가/PnP 금지 — 06-15 audit caveat).
- 별개: `gen_truncation_crops.py` 는 crop+resize ONLY(padding X) = YOLO/`truncation_crops_dope` 트랙. paper_base 와 무관.

### 4. 미해결 이슈 — 다른 에이전트의 camera-facing FRONT 판정 normal-facing 전환

- 진행 중인 다른(3d-expert 추정) 세션이 신규 데이터 생성 시 FRONT(0123) 판정을 **2D 투영 면적 최대 → normal-facing(front_cos)** 으로 변경(+front_cos≥0.40 게이트, margin 0.30→0.60).
- ⚠️ 리스크: 기존 `mixed_v8_train` 은 v4 **면적 기반**으로 라벨됐고 **paper_base 가 그 라벨로 학습 완료**. 새 데이터만 normal-facing 이면 0123 정의 불일치 → 합동 학습/self-training 라벨 충돌 + 2D 기하필터 불변량 붕괴 가능.
- 에이전트에 요구할 것: (a) mixed_v8 9000 에서 면적 vs normal-facing **disagreement rate 정량**(일화 말고 전수), (b) 유의미하면 기존 mixed_v8 재변환 + paper_base 재학습 여부 결정, (c) normal/view 부호가 데이터셋(USD vs OpenCV)별 일관(v3 폐기 원인 재발 방지), (d) front_cos 게이트가 학습 시점 다양성 과도 축소 안 하는지.

### 5. T0 파렛트 실측 + config height 정정 + W/D framing 정정

- T0 줄자: 내 파렛트(palletobj, claim B 단일) = **1.10 × 1.30 × 0.12 m** (110×130×12cm). 직사각형. 기존 config `1.1×1.1×0.15`(KS T-11 가정)은 둘 다 오류(정사각 아님 + H 0.15 아님).
- config 정정: `config/default.yaml` + `config/stage3_selftrain.yaml` **height 0.15 → 0.12 확정.** width/depth 는 `[SLOT]` 유지(주석으로 부정확 명시).
- **W/D swap framing 정정**(이전 과장 수정): 물리 치수는 상수(시점 무관) — 미결은 "1.1/1.3 중 어느 게 canonical_kp3d width(X) 인자냐" 라벨 1개, 한 번 확정하면 영구. keypoint-layer는 order-free Hungarian 흡수 → **4-arm 무관**, ADD/translation만 dims 민감(§3.1). 확정법 = GT cuboid 0-1 변 3D 거리(1100↔1.1/1300↔1.3), pose 평가 때 30분(3d-expert 난제 아님).
- 우선순위 불변: pose-layer라 4-arm 안 막음. 본선 = stage0 RUNBOOK 4줄 → pl_pass_count N.
- 갱신: config 2종, `metric_split_lock.md` §3.2, memory `paper-strategy-master-canonical`, 본 history.
