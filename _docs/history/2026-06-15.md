# 2026-06-15 — CPU 트랙 종료 (P3 audit + split lock + claim B)

논문 critical path의 CPU 파트(GPU 불필요)를 닫음. master/lock 한 번에 갱신.

## 작업 1 — mixed_v8_train 레이블 audit (P3 격리)
- `3d-expert` 위임, 숫자만(판정은 사용자). 스크립트: `scripts/data_prep/eval/v8_label_audit.py`, 결과 `data/pallet/eval_results/v8_audit/`.
- 결과: mixed_v8_train(with-orig 7205 + no-orig 1795) camera-facing 불변량 100% + cuboid↔projected reproj 0.0px → **clean**.
- 판정: P3 무혐의. "v8"=이름 잔재, object-frame 오염 아님. base 약함 = 천장 문제 → **base 보강 트랙** 확정.
- caveat: aug_trunc/scale reproj 25/15px = 3D 필드 stale(DOPE는 2D만 학습 → 무해, 평가/PnP 금지). squash는 자동검증 불가 → 오버레이 25장 눈검사(gross 오배치 없음, 23번만 극단warp 애매 — P3 안 막음).

## 작업 2 — session_inventory_v2 + split lock (P2 누수 차단)
- 원본(Codex 세션) 분실 → 문서 스펙으로 재구성 + 패치(root=data/{outside,night}, rgb-only, GT frame_id→raw 세션 역매칭, 세션별 good 분포). `session_inventory_v2.py`.
- 세션 구조: capturepalletNN(outside 10, 06=빈)/capturenightNN(night 10). rgb pool outside 8715/night 9134.
- honest 수치(held-out paper_base, n_detected≥6): good outside 9/night 1/forklift 4, detectable 51/42/26. → master "31/30"은 ft_s2 누수 낙관치(폐기).
- `split_lock.py` → `data/pallet/eval_results/split_lock/`: final_test_exclude.txt(5218), pl_pool_frames.txt(8031), split_assignment.json. disjoint PASS.
  - OUTSIDE test=p09,p07 / val=p08,p02,p03,p04,p05 / pool=p01,p10,p11
  - NIGHT test=n09,n08 / val=n06,n07,n05 / pool=n01~n04,n10

## 작업 3 — 결과 manifest (귀속 정정)
- `data/pallet/eval_results/_manifest/results_manifest.md` (35개 결과 귀속). provenance 정정: "논문 best=ft_s2" 오귀속(ft_s2=과제/누수), trunc/scale 3D stale.
- detectable 분모 불일치 경고: evaluate_on_val(corner≥4) vs 필터(n_detected≥6) vs eval_nn_matching(게이트 없음) = 비교 금지.

## claim 범위 = B (단일 파렛트)
- 근거(파일계보): capturepallet/night 전 세션 GT 치수 1.3×0.11×1.1m 동일 + challenge/data에 동일 세션 manual_gt = palletobj 동일개체(내 CAD 파렛트). cad 22 GT = 근접 캘리브성 분포 이질 → 드롭(유효 GT 107).
- 지지: sim→real 단일 파렛트 일반화 + cross-session self-training. 미지지: 새 모양 파렛트 → limitation + T1(future).

## 갱신 파일
- `paper_strategy_master.md` §0(blocker 해소+claim B), §3.6(honest 수치+split+claim B), §11(paper_base 미학습 stale 정정).
- `metric_split_lock.md` §1.6(split lock), §8(체크리스트 + eval 필드규칙 동결 + PL sanity check 추가).
- memory `paper-strategy-master-canonical` 갱신.

## 다음 (GPU 트랙 입구)
0단계: PL v1 추출(pool만) + filter-val τ 캘리브 + SLOT 동결 + PL sanity check. 병렬 T1 주말 촬영(파렛트 2~3개).
