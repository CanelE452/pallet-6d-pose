
## STAGE16 Phase A — B2 freeze + real V<8 failure report (2026-06-29)

truncation add-on 트랙 착수. 비싼 데이터생성 전에 real V<8 실패분포 측정.

### Step0 — B2 freeze
- baseline 동결 = `weights/stage11_16k_B2_maskaux/final_net_epoch_0084.pth`.
- 문서: `data/pallet/results/stage16_truncation_addon/b2_freeze_report.md` —
  recipe(header.txt: bs8/img448/lr1e-4/sigma4/ep84 cumulative, encoder_lr×0.1/freeze750,
  mask_aux w0.01 RLE-only/hard-gate無, replay base:v3:addon=2:1:1, truncation_aug_prob=0.0)
  + manifest(원본≈52,391: mixed_v8 9k/v1 9997/v2 9994/v3 18k/addon 5400)
  + eval setup(123 real, aspect무패딩, heatmap thr0.3, order-free) + baseline/B3/B2 수치 recap.

### Step1 — V<8 failure report
- 신규: `scripts/stage0/stage16_v8lt_failure.py` (B2 추론 1회, 학습X. eval_stage11 +
  eval_pvnet_heads + annotate_pnp.solve_pose/project_with_pose 재사용).
- 산출: `data/pallet/results/stage16_truncation_addon/v8lt_failure_report.json` +
  `v8lt_failure_aggregate.txt` + `v8lt_failure_montage/`(17장 오버레이, 눈검증 완료).
- 결과(real V<8 N=17, ⚠소표본): V_geom V4:4/V5:1/V6:11/V7:1. det_ok 2/17(=B2 V<8 붕괴 재확인).
- ★ 잘리는 코너 = **near face(nTL/nTR/nBR/nBL = idx0~3)** 압도(out빈도 nBL11·nTL9·nTR8·nBR8
  vs far face fTL/fTR/fBR/fBL 1~2). border = **L11·R9·T9·B3** (corner누적 L22·R18·T13·B3).
  → truncation-side-cut-bias memory(L/R 위주, top 적음)와 정합하나 **T(상단)도 9프레임**
  (스펙 "top 거의 제외"보다 많음 — 근접·저각도서 윗면 잘림 실재).
- 원인분리: in-frame 코너 raw_err(threshold無) med 28.3px, 15/17 프레임이 화면안인데도
  raw_err>10px → **단순 화면밖 NA 가 아니라 appearance+근접 왜곡으로 화면안 코너도 못맞춤.**
  near 13/17·large 10/17·elev 전부 음수(-4~-12°, 카메라가 pallet 중심보다 약간 아래=저각).
  honest full-8 reproj success 1/17. sel_reproj vs honest 괴리(sel7.2/honest49.4) = coplanar
  gate-only 과대평가 재확인(STAGE12 정합).
- 처방: addon 분포는 (a) near-face(idx0~3) 잘림 위주, (b) border L≳R>T≫B, (c) near+large+저각
  (depth 1.4~3.5m, elev -4~-12°, size 200~560px), (d) 화면안 코너도 근접왜곡 학습되게 full-view
  근접 prox도 포함. 스펙 V8:10/V7:20/V6:30/V5:25/V4:15% 검토 → 아래 caveat.

## Session Snapshot — pre-compact (2026-06-29)

### 현재 best 모델 = B2_maskaux (새 baseline)
- `weights/stage11_16k_B2_maskaux/` (B3_final ep0074서 mask aux 이어학습). real V=8: det 71.7% / good 55.3% / corner 9.6px / worst2 22.2 / PnP 77.4%.
- 진행: baseline(challenge0123) → B3(replay-mix old:v3:addon 2:1:1) → B2(+mask aux 0.01). 데이터-side 레버가 차곡차곡 real 개선(누적 good% 32.4→55.3, +22.9%p).

### 확정된 큰 결론
- 표현 트랙 전부 heatmap 못 이김: voting(STEP5)·affinity(STEP6)·sparse(STEP7)·center-offset(STEP8)·subset-PnP(STAGE12). → heatmap+데이터가 답(diag1~7 진단 확증).
- 원칙: 새 합성=교체 아닌 **replay 추가**(B0/B1 교체=forgetting 증명).
- V<8 truncation = 남은 약점(B2도 11.8%). STAGE12서 postprocess(subset-PnP) 원리적 불가(coplanar 4점) 확인 → **truncation 학습/증강이 유일 레버**.

### 진행 중 (compact 후 이어갈 것)
- ★ STAGE15 Gate1 (NVIDIA식 side-face-center anchor + corner offset ablation) 백그라운드 agent = **a7162205282cde19c**. Gate0+1(target 생성+projection convention 검증 p99<1px+audit, 학습X)만. 통과시 Gate2(32 overfit)→Gate3(3k screen)→Gate4(full replay). ★ "B2 이겨야 채택, 못 이기면 truncation_addon으로". undertraining시 "실패" 아닌 "inconclusive". 산출 `data/pallet/eval_results/stage15_face_center_offset/`.
- agent 완료 통지 오면: convention p99·valid face·overlay 눈검증 → Gate1 통과 판정 → Gate2 위임.

### 다음 메인 트랙 (STAGE15 후/병행)
- truncation_addon_v1 데이터 생성/학습(V<8 직접) — V<8 유일 레버.
- B2 config/manifest freeze(새 baseline 고정).

### 운영 메모
- agent 권한 고침: ~/.claude/settings.json allow에 Task/Agent 추가(전역). 
- 워처 self-kill 버그 주의(pkill이 자기 명령 매칭) — 마커 분할/파일감지로 회피.
- conda env pallet-pose, eval=eval_stage11.py(per-frame K, V=8/V<8/size/depth bin), real 123장(filter-val 87+manual 36, 소표본).

## STAGE15 Gate1 PASS (compact 직전 갱신)
- convention p99 0.043px(ordered=matched, ordering 정상) PASS. valid side face mean1.23/frame·valid0 3.2%·valid≥3 0%. overlay 면중앙 정합 눈검증 OK.
- ★발견: **back face 항상 valid=0**(camera_dynamic_0123_v4가 front{0,1,2,3}=카메라근접 long side 재인덱싱→back 정의상 안보임). front always+left/right 가끔.
- **결정: anchor=3채널(front/left/right), back 드롭**(죽은채널). front가 주 always-on anchor.
- 산출: `data/pallet/eval_results/stage15_face_center_offset/`(target_audit·convention_check·overlay30) + sidecar `*.face_centers.json`(16000, 비파괴) + `scripts/data_prep/validate/stage15_gen_face_centers.py`.
- ▶ 다음(compact 후 이어갈 것): **Gate 2 = 32장 overfit**(B2_final frozen, face_center_head 3ch + offset_head 24ch[8corner×2×... 주의: face당 8corner offset, 3face면 3×16=48ch] 학습, D1 oracle<1cell/D2 pred<2cell/face peak<1cell 통과기준). 통과시 Gate3 3k screen→Gate4 full replay. "B2 이겨야 채택, 못이기면 truncation_addon".

## [17:04:12] ⚠️  Auto Compact (컨텍스트 한계 도달)

- Working directory: `/home/minjae/Documents/github/pallet-pose`
- Trigger: `auto`
- Session ID: `a3b10045-b61a-43cf-9fd4-99aa66d66087`
- Transcript: `/home/minjae/.claude/projects/-home-minjae-Documents-github-pallet-pose/a3b10045-b61a-43cf-9fd4-99aa66d66087.jsonl`
- Note: 컨텍스트가 가득 차서 자동 트리거됨 — 다음 세션에서 이 마커 위쪽 작업을 반드시 복원할 것

**TODO (session resume)**: compact 이후 이 마커 위쪽의 작업 내역을 확인하고 이어서 진행


## STAGE15 Gate2 PASS — 32장 overfit (face-center+offset 표현 capacity gate)
- 판정 **PASS**(소표본 in-domain 암기 capacity 한정). 지표: D1 oracle decode 0.036cell / D2 predicted-peak decode 0.036cell / face peak localization 0.397cell / NaN 0. 전 32장·전 face서 buggy/실패 0건. [확인] 8000iter JSON.
- loss: offset_sl1 it750~ ≈0(빠른 완전수렴) / face_mse 단조감소(8000서 6.6e-5), peak는 it4000부터 0.397 plateau(=잔여 loss하강은 gaussian shape 미세조정, 국소화 무영향, undertraining 아님).
- ★중요 진단: 초기 plain MSE는 D1=0.027(완벽)인데 D2=21.9·peak=21.0cell → **face heatmap collapse**(sparse gaussian이 배경0에 묻혀 argmax 노이즈). 구현버그(oracle도 실패) 아니라 손실설계 문제로 격리. fix=target sigma 1→2 + positive-weighted MSE(w=1+50·target) → peak 21→0.40.
- 채널 확정: face_center_head=3ch(front/left/right, back 드롭) / offset_head=3face×4corner×2=24ch. face그룹 front{0,1,2,3} left{0,3,4,7} right{1,2,5,6}. v3 10000 sidecar valid분포: front863/left828/right822/front+left172/front+right165/none150, **최대 2면(≥3면 0%)** — Gate1 정합.
- 산출: `scripts/stage0/stage15_face_center_offset_overfit32.py`, `data/pallet/eval_results/stage15_face_center_offset/overfit32/{overfit32.json, sanity_overlay_0{0..4}_*.png}`.
- ⚠ 한계: capacity gate일 뿐 real 일반화·sim2real 보장 아님. 단일config(lr1e-3,sigma2,λ_hm10).
- ▶ 다음: Gate3 = 3k quick screen(held-out 일반화 신호). v3mask 교훈대로 seen-subset 대조로 underfit vs 일반화갭 구분. B2 못 이기면 truncation_addon으로.

## STAGE15 Gate3 (face-center+offset 3k quick screen) — FAIL/lean-negative

스크립트: `scripts/stage0/stage15_face_center_offset_quickscreen3k.py` (Gate2 head/decode/loss/target 재사용).
base=B2 frozen, 새 face_center(3ch)+offset(24ch) head만 학습. train=v3 batch_000~008+addon_v1 stratified 3000(V<8 frac 0.45), held-out=v3 batch_009 200(누수無), seen-eval=train 200. order-free Hungarian vs GT8, V=8/V<8 분리, heatmap-path=B2 belief→DOPE extract_keypoints 동시 비교.

결과(input-400 px, 40ep; 12ep도 거의 동일):
```
              seen V8  seen V<8  held V8  held V<8 off  held V<8 hm
12 epochs     12.4     23.7      14.0     40.7          5.1
40 epochs     10.1     14.9      13.8     36.1          5.1
```
- offset-path는 학습됨(seen V8 10px) + off-screen 코너 실재 확인(heatmap NA 83%). 그러나 **held V<8서 offset 36px vs heatmap 5px = heatmap 압승(-31px)**. off-screen 코너 recovery offset err=63px(쓸 수 없는 수준).
- seen→held V<8 거의 안 변함(14.9→36)=generalization gap(undertraining 아님). loss는 계속 하강하나 held 천장.
- 판정: 기존 "표현 트랙 전부 heatmap 못 이김"(diag1~7, STEP5~8, STAGE12)에 center-offset 3k 재확증. Gate4(full replay) 불필요 → **truncation_addon 으로 이동**.
- 산출: `data/pallet/eval_results/stage15_face_center_offset/quickscreen3k/` (quickscreen3k.json, SUMMARY.txt, vlt8_cmp_*.png 5장, run.log/run_ep40.log).
- caveat: synth held-out·단일 config — real 보장 아님.

## STAGE15 Gate3 FAIL → 표현트랙 종결 → STAGE16(truncation_addon) 결정
- Gate3 3k quick screen FAIL/lean-negative: held-out V<8 offset 36.1px vs heatmap 5.1px(-31px, 가설 본거지 압패), V=8도 13.8 vs 4.9 짐. undertraining 아님[확인](ep12→40 held V<8 40.7→36.1 정체=일반화갭, seen은 23.7→14.9). 화면밖코너 heatmap 83% NA(전제 사실)이나 offset이 채운값 63px=실사용불가. 산출 `data/pallet/eval_results/stage15_face_center_offset/quickscreen3k/`, 스크립트 `scripts/stage0/stage15_face_center_offset_quickscreen3k.py`. memory `pvnet-dense-vector-voting-negative-result`에 5종째 추가.
- **결정(사용자/외부모델 지시)**: 표현/후처리 트랙 종결(voting/affinity/sparse/center-offset/side-face-anchor 5종 전부 heatmap 못이김). 다음=**STAGE16 truncation_addon_v1**(V<8 데이터로 직접 공략). DR(V=8) 2순위 보류(B2 이미 V=8 det71.7로 우위). 외부 핸드오프 보류.
- STAGE16 순서: 0)B2 freeze+config/manifest 고정 → 1)B2 real V<8 전수 failure report(V_geom·missing-corner bitmask·border 방향·size·elev·azim·honest full-8 reproj) → 2)truncation_addon_v1 6000장 생성(실패분포 따라, V8:10/V7:20/V6:30/V5:25/V4:15%, border supervision=화면안 gaussian/화면밖 loss제외·clamp금지) → 3)B2 init replay-mix(old40/v3 20/addon20/trunc20, encoder저LR/BN freeze, val 4분리) → 4)honest V<8 평가.
- 성공기준: V=8 det 하락≤1%p·good 하락≤3%p, V<8 det 11.8%→25%+, honest reproj 개선, false accept 증가無. Fail=V=8 det 3%p+ 하락 or honest 개선無 or gate-only만 좋아짐.
- ★게이팅 원칙 유지: 비싼 생성/학습(Step2-3)은 싼 분석(Step0-1) 뒤에. 실패분포가 생성을 구동(블라인드 V<8 양산 금지).

## STAGE16 Phase A 완료 — B2 freeze + V<8 failure report
- Step0: `data/pallet/results/stage16_truncation_addon/b2_freeze_report.md`. B2=stage11_16k_B2_maskaux/final_net_epoch_0084(계보 challenge0123→B3_replay ep74→B2). recipe bs8/img448/lr1e-4/sigma4/ep84(cumulative)/encoder×0.1+freeze750/mask_aux w0.01(RLE,hard-gate안씀)/replay base:v3:addon=2:1:1/truncation_aug_prob=0.0. manifest 원본≈52,391(mixed_v8 9000/v1 9997/v2 9994/v3 18000/addon 5400). 지표 recap V=8 good 32.4→44.7→55.3, corner 11.3→9.6, V<8 det 5.9→0→11.8.
- Step1: `scripts/stage0/stage16_v8lt_failure.py` → v8lt_failure_report.json + aggregate.txt + montage 17장. real V<8 N=17(det_ok 2/17=붕괴 재확인).
  - V_geom: V4:4 V5:1 V6:11 V7:1. out-corner=near face(idx0~3) 압도 집중(far face 거의 안잘림). border L11 R9 T9 B3(corner누적 L22 R18 T13 B3). size 208~563px(med357), depth 1.4~3.5m(med1.9), elev 전부 음수(-4~-12°). near<3.24m 13/17, large 10/17.
  - ★핵심: in-frame raw_err(thr無) med 28.3px, **15/17이 화면안인데 raw_err>10px** = 단순 "화면밖 NA"가 아니라 근접·저각 왜곡으로 화면안 코너도 못맞춤=appearance 문제. honest full-8 success 1/17(sel 7.2 vs honest 49.4 = coplanar gate 과대평가).
  - 스펙 V-bin vs 실제(renorm): V7 22%/6%, V6 33%/65%(스펙 과소), V5 28%/6%(스펙 과다), V4 17%/24%. → 스펙 V6 과소·V5 과다. 단 N=17 극소표본이라 과적합 금지.
- ⚠ 함의: truncation_addon은 "코너 잘라내기"만으론 부족 — 근접왜곡 외형(near+저각+large, full-view 근접 포함)도 학습 필요. 생성 분포 확정 대기(사용자).

## STAGE16 Step2 중단 — 환경 블로커 (Ubuntu에 검증된 Blender 파이프라인 없음)
- agent가 추측 우회 안 하고 멈춤(옳음). 직접 재확인 결과:
  - Blender 미설치 [확인](which/snap/usr/home 없음). OS=Ubuntu(minjae-MS-7D17).
  - repo에 blender 생성기 `scripts/data_prep/blender/{render_blender_data,randomizers,blender_config,blender_math}.py` 존재하나 **옛 convention** [확인]: render_blender_data.py L64-76이 corner0~3=top(max_Z) Z=UP DOPE order = camera_dynamic_0123_v4 아님(v8 위험 레거시). synth_data_scene.blend 부재(있는 .blend는 scan_cleanup pallet 메시정리용).
  - addon_v1/v3 생성기(camera_dynamic_0123_v4 + frame_meta BG/HDRI/cargo randomization)는 repo에 없음 → Windows FoundationPose(`C:/Users/User/Documents/GitHub/FoundationPose`, Blender 5.1).
- 결론: 이 Ubuntu 세션에선 B2 appearance 분포 재현하는 검증 파이프라인 실행 불가. 결정 대기(사용자): (1)Windows에서 실행 (2)생성기+씬+자산 Ubuntu 이관+Blender 설치 (3)신규 경량 파이프라인(appearance/convention 어긋남 위험, 비권장).
- Phase A 분포 처방은 확정됨 — 환경만 풀리면 즉시 생성 가능. palletobj OBJ(data/pallet/scan_cleanup/pallet_full.obj)는 여기 있음.

## STAGE16 Step2 보류 중 — 핸드오프 스펙 작성 (Ubuntu-now 작업)
- 사용자 결정: Step2 환경 풀릴 때까지 보류, 다른 거 먼저. 전략 인식=두 실행레버(truncation_addon, DR) 둘 다 Blender 필요 → 임계경로=Blender 환경, Ubuntu서 전진은 분석/문서뿐.
- 작성: `data/pallet/results/stage16_truncation_addon/STEP2_generation_handoff_spec.md` — Windows 세션이 재유도 없이 Step2 실행하도록 환경/schema/convention/분포/border supervision/파이프라인 개조(visibility gate 완화+per-corner 라벨)/파일럿→감사→6000 게이트/성공기준 전부 고정. ★repo render_blender_data.py는 옛 Z=UP라 사용금지 명시.
- BN freeze 사실 정정 [확인]: train.py엔 별도 BatchNorm freeze 없음 — encoder_freeze_steps(VGG requires_grad 토글)+encoder_lr_scale만, BN running stats는 train모드라 계속 갱신. b2_freeze 미상 해소.
- memory `truncation-side-cut-bias` 갱신: forklift directive(B-heavy/top~5%)와 Phase A 실측(L22/R18/T13/B3=top꽤·bottom드묾) 시나리오 차이 명시(directive 보존+대조 추가, 무비판 적용 금지).
- 태스크 #20 pending(블로커 사유+핸드오프 경로 기재).

## capturecad 22 손라벨로 B2 검증 (사용자 요청 — noapril 손라벨 부재 확인 후 cad만)
- 데이터: capturecad 손라벨 = `challenge/data/capturepalletcad_manual_gt/` 22장(manual_kps 9, reproj~3px, 극근접 frame-filling). noapril(capture0403noapril) 손라벨은 부재 확인(0/188 매칭, history도 unlabeled pool). cad만 진행(사용자 "없으면 cad로만").
- 평가: B2(stage11_16k_B2_maskaux ep0084), eval_stage11 인프라(order-free Hungarian+solve_pose, per-frame K, honest full-8 reproj), squash vs reflect-pad100 비교. 산출 `data/pallet/eval_results/stage16_truncation_addon/capturecad_b2_eval/`.
```
variant  n   det%  cor_med  good%  worst2  PnP%   honest8
squash   22   9.1   19.8    50.0   96.2     9.1    44.7
pad100   22  27.3   13.5    33.3   19.3    45.5    27.0  ★
 by V(pad) V=8(1):det100 honest13.1 / V<8(21):det23.8 honest28.2
```
- V_geom 분포: V2:1 V3:2 V4:4 V5:7 V6:5 V7:2 V8:1 (median V=5). full-view 단 1장, 20/22 V≤6 = 극근접 확정. det 단조(V≤4 7장=det/PnP 0%, V≥5 회복).
- honest8(PnP 성공 10/22): [7.9,13.1,13.2,16.3,25.7,28.2,40.0,54.6,63.6,72.2] bimodal — 3장 양호(<15px)+나머지 flat/edge-on depth-collapse 긴꼬리. 오버레이 눈검증: worst=코너는 GT근처인데 PnP가 큐보이드 납작/비틀림(honest8가 corner_med 못본 실패 포착, diag-filter flat depth-collapse 정합).
- 판정: ★reflect-pad가 near-field서 결정적(squash det9%→pad det27%/PnP46%/honest8 45→27). **과거 "cad det≈0"=모델문제 오진, 실은 전처리(squash) 문제 [확인].** B2 cad 정직성능=det27%/PnP46%/honest med27px. 한계: N=22 극소(V8=1), 극근접 hard subset(메인 eval서 분포이질로 드롭됨), challenge 트랙(palletobj).

## capturecad 22서 벡터트랙 vs heatmap 재비교 (truncation-favorable 분포 최종확인)
- 목적: 과거 negative 벡터들을 벡터에 가장 유리한 분포(cad=극근접·V<8 잘림多)서 재평가. 공정축=같은 weights의 heatmap-arm vs voting-arm(표현 격리). 전부 reflect-pad100, B2 cad eval과 동일지표.
```
모델        arm           det%  cor_med  pnp%   honest8   vs 자기heatmap
S4-unit     heatmap       27.3   15.9   36.4    24.3      —
S4-unit     voting-hull   63.6   83.5   63.6   143.8      ★LOSE(honest 6x)
S5-unitseg  heatmap       18.2   13.1   22.7    21.7      —
S5-unitseg  voting-hull   50.0  115.0   50.0   121.6      ★LOSE
S5-unitseg  voting-seg   100.0  125.5  100.0   143.9      ★LOSE(det100%인데 전부 오답)
S7-sparse   heatmap       18.2   16.2   27.3    40.8      —
S7-sparse   edge-refine   18.2   37.1   27.3    69.9      ★LOSE(corner 16→37)
B2-heatmap  (pad)         27.3   13.5   45.5    27.0     ⚠confound(v3/addon 학습차)
STEP8 offset=skip(probe/teacher-forced), STAGE15=skip(weight 부재)
```
- 판정: **cad(가장 유리)서도 모든 벡터 arm이 자기 heatmap-arm에 패배 — 5종 negative 재확인(truncation-favorable 분포서도)** [확인]. 이긴 arm 없음.
- ★det/pnp% 착시: voting은 mask median으로 항상 8점 뱉어 n_det≥6 게이트 무조건 통과(voting-seg det100%)지만 corner 83~125px·honest8 121~144px=전부 오답("내긴 내는데 틀림", STEP15 패턴). det/pnp 단독 보면 안 됨. 오버레이서 voting 점 팔레트 전역 흩뿌려짐 확인.
- 메트릭 정합: heatmap-arm honest8(S5 21.7/S7-control 19.7)이 B2(27.0)와 같은 자릿수=경로 정상, 쓰레기 수치는 voting arm 국한=표현문제지 스크립트 버그 아님.
- 산출 `data/pallet/eval_results/stage16_truncation_addon/capturecad_vector_compare/`(eval_capturecad_vectors.py + 표/json + 오버레이). 한계: N=22 극소, 학습데이터 confound, STEP8/15 미평가, challenge 트랙.
- 결론: 추가 벡터/표현 투자 정당성 없음 확정. heatmap base 유지 + 데이터(DR/truncation_addon).

## STAGE17 코너 0,1 튐 진단 (B2 추론만) — ★전제 부정: 0,1 아니라 REAR가 병목
- 목적: 0,1 "완전히 보이는데 튐" 원인 A(허공코너)/B(대칭flip)/C(원근오해) 가름 → surface-point 재생성 전 헛수고 방지.
- ★발견: "0,1 튀고 2,3 정상" 전제 **데이터로 부정** [확인]. per-channel(channel i=corner i) GT대비: FRONT(0-3) spike 평균 30% vs REAR(4-7) 59%, 4도메인(outside/night/manual/cad) 일치. 0,1(top-front)=median 7~15px 가장 정확축. 진짜 붕괴=rear 4-7(61/45/61/69%).
- 진단1(GT대비): 0,1만 튀는 것 아님. real 0,1 median 7~15px 양호, 2(BF)가 43%로 오히려 나쁨. 진단2(합성): healthy syn 0,1=10~11px 특이성無(pooled 21px는 gross-broken 127/240=v3 held-out palletobj 오염). 진단4(flip): 기각, real swap 2%만 개선·악화(초기 auto-verdict가 real/syn 미분리로 B 과장→정정).
- 판정: B 기각(swap 악화)/C 기각(사다리꼴비 1.01=1.01)/**A변형 채택 단 대상=REAR·깊이축**: height 0.11m 납작→저앙각 edge-on서 예측 cuboid 깊이(front↔rear) 붕괴. top face 보이면 전코너 정확(zoom_ok vs zoom_spike). diag-filter flat depth 붕괴·trackb flatten·Oracle PnP systematic 정합. 진단5 recede 상관 0.26(완전 monotone 아님, 일부 어려운 프레임 집중).
- 처방: 0,1 surface-point 겨냥=헛수고(0,1 이미 안정). 진짜 레버=rear+flat/edge-on depth. surface-point 한다면 rear/깊이축(deck 뒷모서리). ⚠ real spike n=41 소표본·syn 오염→예비지만 front<rear 4도메인 일치=방향 견고.
- 산출 `data/pallet/eval_results/stage17_corner01_diagnosis/`(summary.md+corner01_diagnosis.json records519+overlay 30+zoom_spike/ok). 스크립트 `scripts/stage0/corner01_diagnosis.py`(추론만). SEAL 누수 0.

## STAGE18 저앙각 임계 정량 + (가)/(나) 판별 (STAGE17 예측 재사용, elevation만 GT계산)
- 임계: real rear 코너(4-7) ≲8° 붕괴(median 15-21px, spike 0.5-0.85) / ≳25° 회복(9.8px). front 전구간 9-19px 안정. ★전이대 8-25°에 real 0장(운용이 0-8°에 뭉침)→임계 real 확정불가. 정직상한 "≲8 붕괴/≳25 회복/8-25 미관측".
- ★(가)/(나) 판정 = **주로 (가) 데이터갭** [확인]: (a)frsep(front-rear 2D분리)↔rear_err 무상관 0.06, 저앙각서도 frsep 50-78px 충분한데 rear 틀림=관측degeneracy 아님 data-limited. (b)학습 elevation 커버리지 저앙각 거의0(v3<10°=0%, combined<10°=5.5%, 68%>25°) vs real 90%+<10°=train/deploy mismatch, 부감서 rear 회복=데이터갭 시그니처. (c)단 <2-3°(frsep≈10px 앞뒤겹침)만 진짜 (나)degenerate=단안한계. → STAGE16 저앙각 보강 유효, 극저앙각만 예외.
- STAGE16 elevation 처방: <3°:10/3-8°:30/8-15°:30/15-25°:15/25+:15%. >25° 68%→20-25% 축소, 8-15° 전이대 조밀표집. 저앙각+원거리+edge-on 최악조합 명시. <3°는 카메라 살짝 올려 회피 or depth fusion.
- 한계: real bin당 14-28장·전이대 0-1장→임계 real 확정불가(STAGE16 생성후 재측정), cad 회복 n=5 [추정], syn 제외(mixed_v8 convention+domaingap). SEAL 승계.
- 산출 `data/pallet/eval_results/stage18_elevation_threshold/`(elevation_threshold.json+tables.txt), 스크립트 stage18_elevation_threshold.py.
- ▶ 함의: 분석체인(STAGE17→18) 완료 — rear/depth 병목, 원인=데이터갭(고침), 처방=elevation 분포 확정. 이제 모든 실행 경로가 STAGE16 생성(Blender 블로커)으로 수렴.

## STAGE16-2 데이터 도착 + 감사 통과 (학습 GO)
- 데이터: `~/Downloads/trunc_addon_v1.zip`(5.4GB, Windows FoundationPose 생성) → `challenge/data/training/truncation_addon_v1/`(6000프레임 flat, 5.3GB). 계약 검증: convention camera_dynamic_0123_v4, dims[1.1,1.3,0.12], mask_rle, keypoint_in_frame, clamp 안됨, per-frame K(fx605.9).
- 감사(6000 전수, GT pose 재계산=_summary 안믿음, `scripts/stage0/stage16_trunc_addon_audit.py`): **판정 학습 GO, 재생성 불필요.**
  - elevation vs 처방: <3:15.2/10, 3-8:28.6/30, 8-15:27.2/30, 15-25:14.4/15, 25+:14.7/15%. 저앙각 3-15°=55.7%(목표60, -4.3pp 약간), 부감 14.7%(재생성선40 안전). median 9.5°. 유일편차 <3° +5pp(근degenerate tail 약간 과다, blocker 아님).
  - ★저앙각 rear(진짜 병목): frsep median <3:112.7 / 3-8:94.6 / 8-15:113.9px, 전부 STAGE18 기준(50-78) 상회. rear 4개 항상 in-front. 저앙각서 rear 충분 관측=학습가치 확인.
  - V_geom: V8:10.1/V7:20.0/V6:30.0/V5:24.9/V4:15.0% 처방 거의정확. near-face(0-3) 잘림 69.5%(Phase A 충족). trunc_mode lateral2043/diag1864/vertical1654/none439.
  - appearance: HDRI9/bg2(parking3188·industrial2812)/floor14=v3 동일 자산풀. 눈검증 20장 convention 정합·화면밖 clamp없음.
- ★한계: 감사=생성분포 검증(GO≠학습효과 보장). 학습 후 전이대(8-25°) real 재측정으로 rear-collapse 임계 확정 필요. appearance 2 bg뿐=sim2real은 별도 게이트(trackb).
- ▶ 다음: STAGE16-3 B2 replay-mix 학습(old40/v3 20/addon20/trunc20).

## STAGE16-3 학습 시작 (B2 init replay-mix, trunc_addon 편입)
- 런처 `data/pallet/eval_results/stage16_truncation_addon/run_train_S16.sh`. init=B2 final_ep0084, out=`weights/stage16_trunc_replay/`. B2 recipe 승계(bs8/img448/lr1e-4/sigma4/encoder×0.1+freeze750/mask_aux w0.01) + trunc 추가.
- replay 2:1:1:1 = base(mixed_v8+v1+v2 28968):v3(9000):addon_v1_train(5400):truncation_addon_v1(6000) = old40/v3 20/addon20/trunc20. balance_groups 매칭 unmatched=0 확인. epoch_size 49368, cumulative ep85→94(+10).
- ⚠함정 방지: v1/v2가 최상위 json 0으로 보였으나 part_*/train_palletobj_v1/ 하위구조(각 9997/9994)라 정상, /v1//v2/ substr 매칭 OK(거짓알람). tee 없이 nohup 직접 리다이렉트(B2 tee death 재발방지). NaN경고=tensorboardX 히스토그램 무해(B2 동일).
- 시작 로그: Loss 0.022→0.010(2%), encoder lr 1e-05, seg-head fresh strict=False. PID 3712755.
- 선택기준(STAGE16-4): V=8 회귀 없음(det -1%p·good -3%p 이내) + V<8/저앙각 rear 개선 + honest reproj. ★학습 후 전이대(8-25°) real 재측정으로 rear 임계 확정.

## STAGE16-3 학습 완료 (weights/stage16_trunc_replay/)
- ep85→94(+10) 완료, 6:04:18 소요(07/04 00:25→06:29). ckpt 11개(ep84~94), final_net_epoch_0094.pth. Loss ~0.01-0.02 안정.
- 다음=STAGE16-4 평가: old/B3/B2/S16 비교, real bins(V=8/V<8/V4-7/저앙각/near-large/border), det/good/corner/worst2/PnP/honest full-8. 성공=V=8 회귀없음(det-1%p·good-3%p 이내)+V<8 det 11.8→25%+·저앙각 rear 개선. cad 22도 별도.

## STAGE16-4 평가 — 판정 REJECT (저앙각 데이터 주입, rear/V<8 못 풂)
- S16=weights/stage16_trunc_replay, ckpt는 synthetic val로 ep90 선택(val이 ckpt 구분 못함=+10 replay가 synth 분포 거의 안바꿈, Val-Old cm8.5·V8 det91.8·Val-v3 +1.6). final94는 real서 V=8 det -5.6%p 회귀→ep90 채택 비교.
- real 123(pad100, order-free) B2 vs S16_ep90 vs final94:
```
bin        model    n_det  det%   rear   rspk%  honest8
V=8        B2       106    83.0   16.8   63.6   15.1
           ep90            88.7   17.0   62.8   15.2   det+5.7 회귀X
           final94         77.4   17.6   69.5   16.8   det-5.6 회귀
V<8(target)B2       4/17   23.5   52.4  100.0   53.0
           ep90     4/17   23.5   29.0   75.0   85.3   honest 악화(n=4 아티팩트)
elev3-8°   B2       57/67  85.1   17.8   70.2   15.0  ★주력
(주력)     ep90            89.6   21.1   73.3   18.4   rear+3.3 h8+3.4 악화
```
- ★판정근거: 검출충분 주력 3-8°(n57)서 rear·honest8 오히려 악화. honest full-8 V<8·3-8°·near-large·cad 전부 악화. V<8 "큰 rear개선(52→29)"은 n≤4+order-free 재배정 아티팩트(같은프레임 front 9.0→62.3 동시붕괴=pose 개선아님 매칭섞임)[확인]. 유일 실개선=detection rate(ep90 V=8 det+5.7 회귀없이)지만 pose 정확도 개선X=가설무관.
- 성공기준: V=8 회귀없음 ep90 PASS/final FAIL, V<8 25%+ 미달, honest 전bin 악화 FAIL, ★저앙각 rear 개선 FAIL, false accept 약간악화. → REJECT.
- ★교훈: STAGE18 (가)데이터갭 판정에 (다)sim2real 전이갭 빠짐. synth 저앙각 rear frsep 94px 멀쩡(감사통과)해도 real 저앙각 rear로 전이 안됨=trackb V=8 appearance gap과 같은 벽(양 아닌 도메인갭). 전이대(8-25°) real 0장=untestable=needs-more-data.
- 산출 `data/pallet/eval_results/stage16_truncation_addon/stage16_eval/`(real_eval.txt/json, ckpt_select, capturecad_s16, overlays_lowelev 14장), 스크립트 s16_ckpt_select.py·s16_real_eval.py.
