# 2026-08-04

## 오늘 한 일

1. 세션 종합 정리 (아래 "PAPER_S2 아키텍처 탐색 세션 종합")
2. **VCR-DOPE Gate 0 실행** — 신규 코드 3개, Gate 0 FAIL 로 architecture 미구현
   (문서 하단 참조)

---

# PAPER_S2 아키텍처 탐색 세션 종합 (2026-07-29 ~ 08-02)

상세 기록은 날짜별 파일에 있다.  이 문서는 **관통해서 읽히는 요약과 포인터**이며
같은 내용을 다시 적지 않는다.

```
날짜        파일                      내용
────────────────────────────────────────────────────────────────────────────────
07-29      2026-07-29.md             target-semantics audit / mechanism diagnostic /
                                     micro architecture screen
08-01      2026-08-01.md             PalletGraph MSL+DGP / SAI / PPD oracle /
                                     PPD target v1·v2 / 32-frame overfit
08-02      2026-08-02.md             PPD long-run / 오버레이 / PGBC gates /
                                     corner replacement / raw-Q router /
                                     stagewise loss / predseed DiffPnP
08-03      2026-08-03.md             변경 0, ep57 하이퍼파라미터 실측 조회
```

커밋 18 건 (`97e1219` … `c3557c0`).

## 한 줄 결론

**ep57(Stage-B, s2 diffpnp)에서 출발해 9 개 방향을 시험했고 전부 기각됐다.
최종 구조는 변경 없음이며, 채택된 아키텍처/loss 변경은 0 개다.**

## 최종 확정 구조

```
RGB → VGG19(scratch) → DOPE stage 1~6 → belief 9채널
    → canonical decoder(gaussian + NMS + 11x11 centroid + 0.4395)
    → canonical OpenCV PnP, correspondence 9개 (★centroid 포함)
    → R, t
```

canonical baseline (전 실험에서 매번 재현 확인): strict 87 / GT-2D PnP 87 /
predicted PnP 70 / yaw median 6.025216° / fixed indexed reproj 23.161629px /
F2 35 / F1 24.  checkpoint SHA `c0055fe7c4210f63…` 불변.

## 시험한 9 방향과 기각 사유

```
방향                          판정        결정적 근거
─────────────────────────────────────────────────────────────────────────────────
learned semantic line / PPD   REJECT      real N87 polarity 0.023 (84/86 반전).
                                          oracle map은 86/86 = 평가경로 정상
PGBC frozen corrector         REJECT      G1: frozen F50이 corner를 구별 못 함
                                          (AUC 0.59~0.64, MLP도 동일)
bounded ±0.25 belief residual REJECT      G0: >50px 구간 belief@GT=0.003 →
                                          필요 amplitude >0.39, 산술적 불가
12-edge graph correction      REJECT      G2: far median 1.8% 감소 (기준 20%)
global corner proposal        REJECT      raw-Q decoder로도 median 80px (base 13px)
map-level replacement gate    REJECT      g~1e-9 붕괴, 시험조차 안 됨
                                          (λ_gate 0.01 vs λ_ref 3.07e-4)
coordinate router             NOT RUN     margin oracle 상한이 gate 미달, PnP 70→70
stagewise mass/rank/progress  REJECT      real에서 GT mass 감소, sharpening +82%
predicted-seed DiffPnP        REJECT      observed −47%인데 GT reproj +4.5%
```

전체 ACCEPT/REJECT 표: `_docs/audits/FINAL_ACCEPTED_ARCHITECTURE.md`

## ★ 관통하는 소견 — 같은 벽에 여섯 번 부딪혔다

모든 시도에서 **synthetic 목적함수는 실제로 내려갔다**:

```
PPD mass loss              (synthetic GT mass 0.57 달성)
proposal loss              6.05 → 1.02
stagewise mass loss        1.27 → 0.56
GN observed reprojection   11.96 → 6.35 px
```

그런데 **real N87 에서는 매번 전이 실패이거나 역방향**이었다:

```
PPD polarity        synthetic 0.95   → real 0.023
stagewise GT mass   synthetic 0.57   → real 0.185
GN                  observed −47%    → GT reproj +4.5%
```

마지막 실험이 이를 pose 쪽에서 확증했다 —
**예측 2D 에 더 잘 맞출수록 진짜 pose 가 나빠진다.**
남은 레버는 decoder·solver·후처리가 아니라 **2D 예측의 계통 편향과 그 원인인 학습 분포**다
(기존 저앙각 flat-view 진단과 정합).

## 방법론적으로 남길 것

이번 세션에서 **판정을 뒤집거나 구해낸 것들** — 다음에도 같은 점검을 해야 한다.

```
사례                                    교훈
─────────────────────────────────────────────────────────────────────────────────
MSL REJECT → INCONCLUSIVE               에너지 지형을 그려보니 판정이 뒤집혔다.
(08-01 dbee7f5)                         집계 수치만으로 기각하지 말 것
SAI subsample leak                      MAX_SUPPORT_SAMPLES가 성분을 조각내
                                        observable axes 1.0→3.0으로 뒤집힘
inversion_frames [:50]                  잘린 목록을 전수로 써서 bootstrap이
                                        0.419(실제 0.023).  목록↔집계값 대조 필수
fp16 belief cache                       softargmax T=0.1이 fp16에 민감 →
                                        PnP candidate 선택이 뒤집힘.  belief는 fp32
raw-magnitude calibration               λ가 산술적으로 경쟁 불가한 값이 나와
                                        gate가 g=0을 최적해로 만듦 → gradient-norm 기준으로
sigmoid로 range만 맞춤                   operating range ≠ operating point.
                                        배경이 0으로 안 내려가 decoder가 전 cell 통과
centroid PnP correspondence             canonical 경로는 centroid 포함(23.162px).
                                        빼면 24.914px — harness parity 확인 필수
heredoc 미첨부 + exit 0                  main()이 없는 채로 실행돼 산출물 0.
                                        종료코드 0을 성공으로 믿지 말 것
```

## 무결성 (전 실험 공통)

final-test open 0 / ep57 checkpoint SHA 불변 / source dataset 수정 0 /
N87 training membership 0 / git tracked·staged `.pth` 0 / pytest 277 passed.

N87 은 전 구간에서 **mechanism capability screen** 으로만 사용했고
논문 일반화 수치로 쓰지 않았다.

## 다음 admissible experiment

1. **계열 재평가 먼저** — 같은 가설 계열에서 6 회 연속 실패했다.  다음 실험 제안 전에
   `[소비처]` 기준으로 이 계열을 계속할지 판단할 것 (CLAUDE.md 계열 재평가 규칙).
2. 계속한다면 레버는 **데이터 분포**다 — real 의 94% 가 <8° 저앙각 edge-on 인데
   학습 root 는 V=8 clean full-view 위주다.
3. canonical solver 의 후보 선택 점수를 로깅해 "복합 점수가 보호 장치" `[추정]` 을
   확인하는 것은 재학습 없이 가능하다.
4. decoder·solver·후처리 층위의 재시도는 이번 세션이 이미 배제했다.

---

## VCR-DOPE Gate 0 (viewpoint x corner-role bias repeatability) — FAIL, 구현 중단

교수님이 제기한 heatmap systematic bias 가설을, architecture를 짜기 전에
**leave-one-session-out으로 먼저 검증**. 학습 0 step, ep57 read-only.

### 신규 파일
- `Deep_Object_Pose/common/view_corner_bias.py` — view 규약(psi/elevation/scale,
  yaw double-angle) + B1/B2/B3 bias 모델(closed-form ridge, intercept 미벌점)
- `scripts/stage0/paper_s2_vcr_bias_atlas.py` — Gate 0 러너
- `challenge/tests/test_view_corner_bias.py` — 유닛 12 + 결과 무결성 8 = 20건
- 결과 root `data/pallet/results/paper_s2_vcr_dope_screen/` + PURPOSE.md

### Phase A/B 통과
baseline 87/87/70, yaw 6.025216, reproj 23.161629.
stage 궤적 far: stage4 20.19px → stage6 22.08px, peak 0.761 → 0.855.
view convention 테스트: yaw+180에서 target 동일 / top-bottom inversion은 비동일 /
elevation은 −local Y 사용 — 전부 통과.

### Gate 0 결과 — **FAIL** (B2·B3 모두 7항목 전패)

```
arm  median  near   far    F2far  F2signed  >20  >50  >100  PnP  reproj
B0   13.24   6.88  22.08   44.59   20.58    203  120   54    70   23.16
B1   22.58  20.17  22.89   39.09    5.51    282  115   45    70   27.28
B2   42.98  38.59  46.58   48.54   26.42    368  231   88    70   43.80
B3   51.57  51.56  52.82   68.91   20.03    376  270  155    70   47.88
```
corner rows 519, 8 session LOSO, ridge 1e-3, basis 8항 사전 고정.

### ★ 소견 1 — 평균 편향은 실재하나 산포가 2배
B1(role-constant)이 F2 far signed bias를 **20.58 → 5.51px (−73%)**. 교수님 지적의
"편향 존재"는 데이터가 지지한다. 그러나 오차는 커진다(near 6.88→20.17px).
```
F2 far  mean signed (−19.28,+7.20) |mean|=20.58px   per-corner std=39.87px
near    |mean|=14.38px             median err=6.88px
in-fold 잔차: B0 13.24 / B1 20.54(train) / B3 19.18(train)·30.92(test)
```
최소제곱은 평균제곱을 줄이는데 지표는 중앙값 → 학습 데이터에서조차 중앙값 악화.
fitting 버그가 아니라 **손실-지표 불일치 + 두꺼운 꼬리**.

### ★ 소견 2 — view 항이 해로운 이유는 데이터 구조
```
6/8 session이 azimuth 범위 6° 미만(단일 시점), 전 session elevation −7.7~9.0°
```
LOSO가 view regression을 **학습 support 밖으로 외삽**시킴(B3 train 19.18→test 30.92px).

→ 판정을 두 갈래로 분리 기록: **Gate 판정은 FAIL(구현 금지) 확정**이지만,
**가설 자체는 이 데이터로 검정 불가**(반증이 아니라 view 다양성 부재).
"viewpoint 가설이 틀렸다"고 단정하면 과한 결론.

### 판정
View-conditioned bias hypothesis REJECT(gate 기준, 단 검정불가 병기) /
role-constant mean bias 존재 확인하나 오차 감소 실패 /
role feature·view adapter **NOT TESTED**(Gate 0에서 중단) /
Final path = **base ep57 DOPE**.
Gate 1·VCR module·32-frame sanity·5-epoch screen 전부 미실행(사전 규칙).

### 작업 중 수정 1건
matplotlib 한글 폰트 없음 → 그림 제목 영문으로 재생성(깨진 글리프 방지).

pytest **297 passed**.

---

## PCR-DOPE (corner-role discriminative feature) Gate A — FAIL, Gate B 미실행

VCR(offset 보정) 실패 후, 사후 보정이 아니라 **feature identity를 직접 학습**하는 방향.
belief/affinity/decoder/PnP 불변, FiLM은 shared feature만 modulate.

### 신규 파일
- `Deep_Object_Pose/common/corner_role_adapter.py` — role encoder(32ch)+8 prototype
  (cosine/0.10, sigmoid 없음), zero-init FiLM, bilinear sampling, yaw180 permutation
- `Deep_Object_Pose/train/corner_role_loss.py` — proto/cross/wrong/teacher_wrong/local
  + teacher anchor, **frame 단위** symmetry 선택
- `scripts/stage0/paper_s2_corner_role_dope_screen.py` — Gate A 러너
- `challenge/tests/test_corner_role.py` — 20건
- 결과 root `data/pallet/results/paper_s2_corner_role_dope/` + PURPOSE.md

### 무결성 확인
flag-off forward = legacy forward **max|delta| 0.0** / FiLM zero-init = ep57 **0.000e+00**.
baseline 87/87/70, yaw 6.025216, reproj 23.161629.
trainable 17,950,843 (vgg_last 5,014,912 / stage4~6 12,567,579 / role 248,736 / FiLM 119,616).

### ★ 구현 결함 1건 — 판정 전 발견·수정
첫 실행에서 structural 지표가 전 구간 **정확히 0.000**, λ_cross가 상한 10에 clamp.
`cross_location_loss`의 gather 축 오류로 `at_other`가 `own`을 복제 →
`softplus(own−own+0.2)` 상수 → gradient 0.
```
Before: sampled.transpose(1,2).gather(1, index.transpose(1,2))
After : index = labels[:,None,:]; sampled.gather(2, index).transpose(1,2)
```
수정 후 structural 0.000 → 0.93. 회귀 방지 테스트 2건 추가(off-diagonal 값 + gradient 부호).

### ★ 선정기 defect — 두 실행 모두 보고
첫 실행 frame 선정이 명세(8 corner valid 우선/scale·azimuth 분산)를 구현하지 않고
index stride만 사용 → 초기 corner error 5.6px라 기준5(−50%)가 도달 불가.
선정기를 명세대로 고쳐 1회 재실행(threshold 불변).
```
arm  run1(결함) -> run2(명세)
C2   acc 0.250 -> 0.469   struct 0.632 -> 0.808
C3   acc 0.391 -> 0.469   struct 0.750 -> 0.830
```

### Gate A 결과 — **두 arm 모두 FAIL**
```
기준                        C2       C3      요구
proto accuracy            0.469    0.469   >=0.95   FAIL
structural GT>other       0.808    0.830   >=0.95   FAIL
GT>student wrong          1.000    1.000   >=0.90   PASS(표본 1/7)
GT>teacher wrong          1.000    1.000   >=0.90   PASS(표본 1/7)
corner error 감소         21.3%    21.3%   >=50%    FAIL
centroid                 -45.9%   -45.9%   +10%내   PASS
NaN                          0        0       0     PASS
```

**학습은 실제로 일어남**: chance 0.125 → proto 0.47~0.61, structural 0.69 → 0.93.
그러나 32 frame 600 step 암기 조건에서도 0.95에 크게 미달 = **일반화 이전에 암기 실패**.
C2==C3 (proto 0.469 동일) → FiLM은 role 학습에 기여 없음.

**기준 3·4의 PASS는 증거 아님**: 7개 로그 중 1개에서만 측정(나머지 nan) —
선정 frame이 clean이라 4셀 초과 wrong peak가 거의 없음.

### 판정
Corner-role auxiliary loss REJECT / Role-conditioned FiLM REJECT /
Final path = **base ep57 DOPE**.
Gate B(group-disjoint 3k, real one-shot)·Gate C 미실행, **N87 접근 0회**(허용 2회 중).

pytest **317 passed**.
