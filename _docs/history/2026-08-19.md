# 2026-08-19

## 데이터셋 수령 — negative 10,000 + EDGE untouched 2,000

Downloads 에서 프로젝트 체계로 이동. **경로를 추측하지 않고** 실제로 찾아 옮겼다.

```
신설  paper_release/negative/negative_synth_v1_train.zip   4.53GB  9,000 프레임
      paper_release/negative/negative_synth_v1_dev.zip     0.49GB  1,000
      paper_release/negative/negative_synth_v1_docs.zip    0.03GB  README·manifest·diagnostics
이동  paper_release/edge/edge_complement_v1_clean_untouched.zip  1,000  ★corner 평가 후보
      paper_release/edge/edge_complement_v1_trunc_untouched.zip  1,000  line-hard 평가용
      paper_release/oblique/corner_la_oblique_v1_packaging.json
```

무결성: 동봉 checksum 과 **전부 일치** — negative train `89da0b86…`, dev `07dca05e…`,
oblique zip 2개도 packaging.json 의 sha256 과 일치. 세 zip 모두 `unzip -t` CRC 전수 통과.
중복 다운로드분 `negative_synth_v1_train (1).zip` 은 전체 sha256 동일 확인 후 Downloads 에
남겨둠(삭제는 사용자 확인 필요).

⚠ **어제 census 의 구멍**: `data/pallet/training_data/` 만 훑고 `data/pallet/release/` 를
보지 않아 EDGE untouched 를 "미생성" 처럼 읽힐 수 있게 썼다. 실제로는 **미전달**이었고
지적받아 확인 후 문서 정정(`DATASET_MASTER_INVENTORY.md`, `FINAL_DATA_ROLE_DECISION.md`).
규모도 정정 — untouched 는 clean/trunc 두 벌 = **2,000 프레임**(1,000쌍)이고,
`clean` 은 게이트 통과(visible_kp>=4)라 **corner PnP 평가에 쓸 수 있다**.

## negative 감사 (PHASE 0) — 내 감사 코드 오류 2건 자기정정

첫 감사가 낸 두 경보가 **둘 다 내 테스트 오류**였다.

```
경보 1  "semantic 위반 18,000"  → 오류. keypoints/structural_lines 키의 존재만 보고
        위반으로 셌다. 실제로는 키가 있되 값이 빈 배열이고 그게 정상 contract.
        실제 값:  object_present=False · pose_valid=False · keypoints=[] ·
                  structural_lines=[] · objects=[] · projected_cuboid 없음
        → semantic 위반 0 / 10,000, fake pose·keypoint·line GT 없음

경보 2  "NEG train ∩ dev = 1000"  → 오류. 두 split 이 각자 f00000 부터 번호를 매겨
        파일명만 겹쳤다. train f00000~f08999 / dev f00000~f00999.
        같은 이름 RGB 10장 해시 비교 동일 0, neg_id 기준 교집합 0.
        → 실제 collision 0. 단 로더에서 split 을 네임스페이스에 포함시켜야 덮어쓰기 없음.
```

감사 결과:
```
train 9,000 / dev 1,000    중복 0 · rgb 결손 0 · label 결손 0 · NEG×MH_TRAIN 0
family    N00 3600 / N01 1965 / N10 1182 / N11 2253   (dev 400/222/128/250, 9:1 층화)
impostor  none 3600 / parallel_rail_bundle 1965 / junction_cluster 906
          / asset 276 / sparse_structural_hybrid 2253
해상도    640x480 4403 · 960x540 2364 · 720x480 1330 · 560x560 903  (BROAD 와 동일 4종)
```
카테고리명은 source(`release.negative_type` = N0_MATCHED_EMPTY 등) 그대로 사용,
브리프의 matched_empty/structural_hard 로 임의 rename 하지 않음.

## ★PHASE 1 — E3 의 active corner loss 를 source 에서 확인 (기억 복사 금지)

```
corner_loss(beliefs, target, valid, stages=TRAINABLE_BELIEF_STAGES=(4,5,6))
  belief 만 사용. affinity 는 heads_from_f50 이 계산하되 corner_forward 가 버린다
  → affinity INACTIVE 확정 → 되살리지 않음 (브리프 지시)

★실측:  valid 전부 False → corner_loss = 0.000000e+00
        valid 전부 True  → 9.967461e-01
```

즉 negative 를 `valid=[0,…,0]` 으로 넣으면 **손실이 정확히 0** 이라 억제가 전혀
일어나지 않는다 — 브리프가 금지한 구현이 현재 코드에서 실제로 발생함을 수치로 확인.
→ `object_present=False` 를 **별도 분기**로 읽어 dense zero suppression 을 계산해야 한다.

그리고 **gradient clipping 이 코드베이스에 0건**이다. AdamW 는 파라미터별 상태라
corner negative gradient 가 line 업데이트를 바꿀 경로가 없다 → line exact parity 가
구조적으로 보장된다(20-step replay 로 확인 예정).

## 변경 파일

```
신규  scripts/stage0/multihead/mh_fusion.py          F0~F4 fusion solver + unit test 7종
      scripts/stage0/multihead/mh_r0r1.py            canonical failure map / R0 / R1
      scripts/stage0/multihead/mh_lcurve.py          learning-curve + parameter-drift 감사
      scripts/stage0/multihead/mh_confirm4.py        D4 오염감사·manifest
      scripts/stage0/multihead/mh_recheck.py         C0/C1 재판정 (canonical yaw)
      scripts/stage0/multihead/mh_regime.py          40k 라벨 index + visibility contract
      scripts/stage0/multihead/mh_observable.py      관측가능 오라클·노이즈 sweep
      scripts/stage0/multihead/mh_resample.py        DCTRL/DANGLE 재표집
      scripts/stage0/multihead/mh_curriculum{,_report}.py  two-stream 커리큘럼
수정  scripts/stage0/multihead/mh_curriculum.py      C1_RESCUE arm 추가
                                                     (rescue_items: C0 슬롯 복사 + NON_LA 1,500 치환)
      scripts/stage0/multihead/mh_diagnose.py        cache 에 --populations 인자 추가
                                                     (기본값이 기존 튜플이라 기존 호출 불변)
      scripts/stage0/multihead/mh_fusion.py          T6 를 raw L2 → Huber cost 비교로 정정
                                                     (refit 이 최소화하는 목적함수와 맞춤)
문서  data/pallet/results/paper_s2_multihead/ 아래 판정·감사 MD 다수
      (FUSION_PURPOSE, POINT_LINE_FUSION_RESULT, FINAL_FUSION_DECISION,
       DATASET_MASTER_INVENTORY, FINAL_DATA_ROLE_DECISION, NEGATIVE_READY_TRAIN_CONTRACT,
       EVAL_LEAKAGE_AUDIT, C1_RESCUE_RESULT, RECHECK_CANONICAL_YAW_RESULT 등)
```

## 진행 중 (다음)

PHASE 0B 시각 QA(카테고리별 64장, 실제 팔레트 혼입 확인) → PHASE 2 negative loss 구현
+ gradient 격리 테스트 → λ_neg gradient calibration → 500-step screen → N0/N1 3k×2seed.

## negative suppression qualification (N0 vs N1) — NEGATIVE_HANDLING_SUPPORTED = False

신규 `mh_negative.py`, `mh_negscreen.py`, `mh_negeval.py`. `mh_curriculum.py` 에 N0/N1 arm.

### ★배선 버그 1건 (내 실수, 즉시 수정)

N0 seed1 이 step 0 마크만 찍고 죽었다 —
`build_batches("C0" if arm=="C0" else "C1", ...)` 에 arm 이 `N0` 라 `"C1"` 분기로 빠져
존재하지 않는 LA stream(`None`)을 참조. `positive_arm` 변수를 만들어두고 여기서 안 썼다.
→ `build_batches(positive_arm, ...)` 로 수정, 3-step smoke 로 두 arm 실제 진행 확인 후 재착수.
⚠ 교훈: 학습 시작 후 "프로세스 살아있음" 이 아니라 **마크가 진행되는지** 확인해야 한다.

### q calibration (optimizer step 0, autograd.grad 만 사용)

```
        g_pos median   g_neg median   r median   p10     p25      p75      p90
seed1     1.438e-04     1.053e-02     61.641    1.372   2.411   131.541  195.735
seed2     1.415e-04     6.000e-04      5.685    1.567   2.102    54.507   98.060
```
공통 hyperparameter 는 raw λ 가 아니라 **q = target weighted gradient ratio**,
λ_s = q/r_s. ⚠ratio 분산이 140배라 q 는 평균적으로만 강도를 맞춘다(한계 기록).

### 500-step q screen (seed1) → q* = 0.20

```
arm      AUROC   AUPRC   recall  FP/img   recall drop  FP 감소
N0      0.9585  0.9065   0.951   0.1520      -           -
q=0.05  0.9650  0.9225   0.951   0.1090    0.00pp     28.3%
q=0.10  0.9668  0.9323   0.951   0.1180    0.00pp     22.4%
q=0.20  0.9713  0.9407   0.951   0.1080    0.00pp     28.9%  ★
```

### 본실험 3,000 step × 2 seed

```
                        seed1                    seed2
negative FP 감소       +40.7%  OK              +88.1%  OK
AUPRC                  +0.0538 OK              +0.1021 OK
recall drop             0.00pp OK               0.00pp OK
pose R                 -2.06%  OK             -20.38%  NG
pose t                +11.19%  OK             -33.39%  NG
pose ADD-S            +11.36%  OK             -30.10%  NG
5cm5deg                -0.19pp NG              -2.73pp NG
```
`LINE_ISOLATION_EXACT = True` — 3,000 step 후 line_late max|diff| 0.000e+00, 두 seed.

**negative·detection 절은 두 seed 모두 통과**했고 떨어뜨린 것은 pose safety 하나다.
seed 방향이 정반대(seed1 t/ADD-S 개선, seed2 전면 악화)이고, negative 억제가 가장 강했던
seed2 가 pose 가 가장 나쁘다 → belief dense 억제가 positive peak·국소형상을 함께 누르는
부작용 의심 [추정, 인과 미확인].

### ★잔여 FP 소재 (ADDENDUM 4)

N1 negative score 상위 100장: seed1 `N1_STRUCTURAL_HARD 84 / N2 16`,
seed2 `N1 77 / N2 18 / N0 5`.
**팔레트를 닮은 N2 가 아니라 구조물 N1(평행 rail·교차 topology)에 몰린다.**
빈 장면 N0 는 거의 완전 억제.

산출 `NEGATIVE_DATA_AUDIT.md`, `NEGATIVE_LOSS_CONTRACT.md`,
`NEGATIVE_QUALIFICATION_RESULT.md`, `FINAL_ARCHITECTURE_DECISION.md`,
`NEG_GRAD_CALIBRATION.json`, `neg_lambda_screen.json`, `neg_q_star.json`,
`negative_presence_curves.json`, `negative_pose_safety.json`,
`negative_unit_tests.json`, `negative_hard_top100.json`, figure 5종.

## 프로세스 수정 — 드라이버가 학습에서 끝나던 문제

사용자 반복 지적(4회 이상): 학습이 끝나도 평가가 자동으로 시작되지 않고, "끝났어?"
라고 물어야 진행된다. 실제로 오늘 N0/N1 학습이 13:16 에 끝났는데 평가는 15:36 에
사용자가 물어서 시작됐다 — 2시간 20분 손실.

원인은 기억력이 아니라 **드라이버 설계**였다. 매번 `for arm/seed: train → notify
"학습 완료" → exit` 이라 평가·판정이 사람 턴으로 넘어갔다.

수정:
```
CLAUDE.md      "실험 드라이버 — 학습에서 끝내지 말 것 (필수)" 절 신설
               train → eval → verdict → notify 를 한 파일에서.
               알림은 '학습 완료'가 아니라 판정과 핵심 수치.
               완료 판정에 exit code·프로세스 존재 사용 금지(둘 다 거짓말한 이력).
               시작 직후 마크 진행 확인. 출력에 grep/tail 파이프 금지.
scratchpad     driver_template.sh (복사해서 쓰는 템플릿), PIPELINE_RULE.md
memory         driver-must-not-stop-at-training.md + MEMORY.md 인덱스
```

### 정정 — 프로젝트가 아니라 전역 규칙으로

사용자 지적: "다른 내 컴퓨터 cli 에서도 그러던데". 맞다 — 행동 습관이라 프로젝트가
아니라 전역이어야 한다. 처음에 프로젝트 CLAUDE.md 에만 넣은 건 범위 판단 오류였다.

```
~/.claude/CLAUDE.md   "장시간 작업은 결과까지 자동으로 — 사용자가 '끝났어?' 묻게 하지 말 것"
                      절 신설 (Workflow 와 Pre-change Trigger 사이). 게이트 항목은 동결이라
                      본문 규칙으로 넣었다.
프로젝트 CLAUDE.md    중복 제거하고 전역을 가리키게 축약. 프로젝트 고유값만 남김
                      (최종 마크 "3000", purpose_gate 때문에 결과 폴더에서 실행)
memory                전역 규칙 위치로 갱신
```

⚠ `~/.claude` 는 GitHub 공유이므로 commit/push 는 사용자 OK 후에만.

## [18:40] DETACHED PRESENCE GATE — invariance 검사 수정 + 판정

### 1. `scripts/stage0/multihead/mh_presence_verify.py` (수정)

PHASE 5 가 `POSE_INVARIANT_ALL=False` 로 막혀 있었다. seed1 이
`param diff 0.000e+00` / `grad 0` 인데 `corner out 1.853e-03`.

원인 추적 (전부 실측):
- 같은 모델·같은 입력 3회 연속 forward → diff 0.000e+00 (forward 자체는 결정적)
- 사이에 `torch.manual_seed` 만 → 0.000e+00
- 사이에 `fit_linear`(CPU, 2000 step) 만 → 0.000e+00
- 사이에 **더미 GPU 1 GiB 할당만** → **1.853e-03** (재현)
- 더미 3 GiB → 2.139e-03

즉 cuDNN 이 conv 알고리즘을 그때 남은 workspace 로 고른다.
`benchmark=False`·`deterministic=True` 로도 안 잡힌다. 옛 검사는 가중치가 아니라
allocator 상태를 재고 있었다 (F3 T6 의 Huber-vs-L2 와 같은 종류의 오설계 테스트).

Before → After:
- 출력 diff 를 GPU → **CPU** 에서 측정 (CPU conv 는 비트 재현)
- 모델 **사본이 아니라 같은 객체**를 CPU 로 이동 (사본이면 변조를 놓친다)
- `N_FRAMES 64 → 8` (CPU forward), `FLOOR_FRAMES=64` 로 noise floor 는 원 관측과
  같은 배치에서 측정해 기록
- 요구조건은 그대로 (param·corner·line diff 전부 정확히 0, grad 0)

결과: seed1/seed2 모두 `0.000e+00 / 0.000e+00 / 0.000e+00 / grad 0` → PASS.
noise floor @64 = 1.853e-03 을 gate 아닌 control 로 JSON 에 남김.

### 2. 신규 파일

- `mh_presence_curve.py` — gate 자신의 recall(0.98) 에서의 FP 를 보고 (gate 불변)
- `mh_presence_freeze.py` — score 정의·threshold freeze·sha256
  → `presence_score_definition.json`, `presence_threshold.json`,
    `threshold_sha256.txt` (`8165fbbbea5a7d30…`)

### 3. 결과

```
arm                    seed   recall0.95 FP/img   recall0.98 FP/img
────────────────────────────────────────────────────────────────────
P0_NO_GATE               -    1.0000              1.0000
P1_SCORE4KP              1    0.2040  (-79.6%)    0.2750  (-72.5%)
P2_DETACHED_LINEAR       1    0.1950  (-80.5%)    0.2680  (-73.2%)
P1_SCORE4KP              2    0.1430  (-85.7%)    0.2680  (-73.2%)
P2_DETACHED_LINEAR       2    0.1260  (-87.4%)    0.2660  (-73.4%)
```

paired bootstrap(B=10,000, 같은 negative dev 이미지): P2>P1 은 recall 0.95 에서만
두 seed 다 CI 가 0 배제, recall 0.98 에서는 두 seed 다 0 포함 → **gate 동작점에선
학습 9 파라미터의 이득 미확립**. → `presence_p2_vs_p1_bootstrap.json`

### 4. 판정 — 브리프 두 조항 충돌 (사용자 결정 대기)

선택규칙 "Recall ≥ 95% 중 FP 최소" 는 threshold 를 항상 95% 바닥으로 내리는데
gate 는 "Recall drop ≤ 2pp"(=98%) 를 요구한다. FP 동률일 때만 동시 성립.

- 읽기 A(사전등록 코드) → drop 5.00pp → FAIL → `NEGATIVE_REJECTION_NOT_ESTABLISHED`
- 읽기 B(gate 자신의 recall) → drop 2.00pp + FP -72.5~73.4% → P1·P2 둘 다 PASS

`presence_verdict.json` 은 A 그대로 둔다 (결과 보고 gate 수정 금지).
두 동작점 모두 freeze. `FINAL_ARCHITECTURE_DECISION.md` 에 절 추가.

전부 synthetic dev — real FP/AP 아님.

## [18:52] FINAL NEGATIVE DECISION — 기록 확정, ARCHITECTURE_SEARCH CLOSED

사용자 결정을 기록으로 확정. 사전등록 판정은 **수정하지 않음**.

### 1. 보존

`presence_verdict.json` 무수정. sha256 `ffa67e88d0c18e0eb33b586650382a42…`
→ `verdict_prereg_sha256.txt` 로 고정.
`FINAL_NEGATIVE_HANDLING = NEGATIVE_REJECTION_NOT_ESTABLISHED` 그대로.

실패 원인을 `MODEL_FAILURE` 가 아니라 **`PROTOCOL_CONFLICT`** 로 기록:
threshold selection 은 Recall≥95%, qualification 은 Recall≥98% 를 요구했다.

### 2. 교정 프로토콜 (FROZEN)

```
choose threshold minimizing negative FP/image subject to positive Recall >= 0.98
```
앞으로 모든 real/synthetic confirmation 에 고정. Recall≥95% 바닥 폐기.

`mh_presence_curve.py::point()` 수정 — Before: `np.quantile(pos, 1-target)`
→ 실현 recall 0.97997 (drop 2.003pp), 성능이 아니라 이산화로 제약 위반.
After: 관측 positive score 중 제약을 정확히 만족하는 최대 threshold 선택
(tie 처리 루프 포함). threshold 가 내려가 FP 는 불리해지는 **보수적** 방향.
→ 실현 recall 0.9801 (drop 1.99pp).

### 3. 신규/수정 파일

- `mh_presence_curve.py` (수정) — 제약 정확 threshold
- `mh_presence_bootstrap.py` (신규) — 인라인이던 bootstrap 을 스크립트화,
  교정 threshold 규칙 공유
- `mh_negative_final.py` (신규) — 결정 기록. 수치는 기존 산출물에서 읽어옴(수기 금지)
  → `NEGATIVE_FINAL_DECISION.json`, `negative_final_decision_sha256.txt`
- `presence_threshold.json` 재생성 sha256 `4a366185580d2021…`
- `FINAL_ARCHITECTURE_DECISION.md` — presence 절 최종본 교체 + 앞부분
  "negative path 미확정" 을 종결로 정리(자기모순 제거)

### 4. 교정 프로토콜 수치 (secondary analysis, prereg 미덮어씀)

```
arm                  seed   Recall 0.9801 (drop 1.99pp)   Recall 0.95 참고
                            FP/img    감소                 FP/img    감소
──────────────────────────────────────────────────────────────────────────
P0_NO_GATE             -    1.0000     —                  1.0000     —
P1_SCORE4KP            1    0.2760   -72.4%               0.2040   -79.6%
P2_DETACHED_LINEAR     1    0.2680   -73.2%               0.1950   -80.5%
P1_SCORE4KP            2    0.2680   -73.2%               0.1430   -85.7%
P2_DETACHED_LINEAR     2    0.2670   -73.3%               0.1260   -87.4%
```
P1·P2 모두 두 seed PASS. (사용자 요약의 -72.5~-73.2% 는 제약 정확 threshold
적용 후 **-72.4~-73.2%** 로 소폭 이동 — 보수 방향.)

paired bootstrap 재실행(교정 threshold): recall 0.9801 에서 두 seed 다 CI 0 포함
(seed1 -0.0080 [-0.0170,+0.0010], seed2 -0.0011 [-0.0100,+0.0080])
→ P2 이득 미확립. recall 0.95 에서만 P2 우세.

### 5. 결정

```
FINAL_NEGATIVE_HANDLING       SCORE4KP_THRESHOLD
P2 DETACHED_LINEAR_PRESENCE   REJECT_AS_UNNECESSARY_COMPLEXITY
DENSE NEGATIVE SUPPRESSION    REJECT
ARCHITECTURE_SEARCH           CLOSED
FINAL CORE                    SPLIT_LATE_2HEAD + F3 ROTATION_ONLY_TREFIT
                              + SCORE4KP REJECTION
CLAIM LIMIT                   synthetic negative validation only
                              real FP/AP = PENDING REAL EVALUATION
NEXT                          real positive/negative evaluation protocol 준비
                              새 architecture/training 실험 금지
```

## [20:05] DATASET COMPOSITION LOCK — 전수 감사·EDA·coverage·manifest (학습 0)

### 1. 신규 모듈 `scripts/stage0/dataset_audit/`

```
da_common.py            dataset registry + 프레임당 canonical row (zip 은 스트림, 해제 안 함)
da_features.py          PHASE 3-4 positive/negative feature parquet
da_leakage.py           PHASE 2 L1~L6 누수 감사
da_leakage_addendum.py  L5 기대값 정규화 + L4 정체 규명
da_coverage.py          PHASE 5/7/12 축 요약·cell coverage·다양성
da_figures.py           PHASE 5-6 그림 25장
da_manifest.py          PHASE 9/10/13/14 manifest + EDGE 비율 선정 규칙
da_report.py            master table + 4개 리포트
da_verdict.py           PHASE 15 판정 (CASE A 를 검사로)
da_real_reference.py    PHASE 11 real 참조 (관측축만)
```
드라이버: scratchpad `run_dataset_audit.sh` (features→leakage→coverage→figures→
manifest→report→verdict→notify 를 한 파일에서).

### 2. 라벨 계약을 실측으로 확인한 것 (이름 추론 금지)

- **세 카운트가 서로 다름** — `n_inframe`(계산, 최대 8) / `V_actual`(라벨, **최대 7**,
  볼록 육면체는 항상 ≥1개 자기폐색) / `V_vis_actual`(추가 외부폐색 제외) /
  `n_supervised`(= 9채널 화면안 판정, `mh_data.belief_target` 정의). BROAD 1500 표본에서
  `n_inframe − V_actual` = {0:16, 1:1079, 2:405}.
- **EDGE_HARD 는 한 이름에 두 계약** — trunc 3종 gate 0%(V_vis≤3 100%), **clean_untouched
  gate 100%**(corner 평가 가능). 이름으로 묶으면 틀린다.
- **EDGE 에는 `pnp_conditioning` 자체가 없음** → truncation/occlusion 플래그를 모든
  dataset 동일 기하 정의로 파생. README 의 72.7% 는 프레임 라벨이 아니다.
- negative 스키마는 `negative`/`release` 딕셔너리 (내 초기 추측 `negative_meta` 는 틀렸음).

### 3. 누수 (L1~L6)

```
L1 frame_id     11쌍 stem 충돌 — 전 pool 이 f00000 부터 번호매김. 이름 충돌뿐
L2 rgb sha256   68,000장 전수 → cross 0 / within 0
L3 label digest cross 0 / within 0
L4 seed         1,000 튜플이 2개 dataset 에 걸침 = EDGE clean_untouched × trunc_untouched
                ★ 설계상 쌍(같은 장면, look_at 만 다름). 누수 아님.
                ★ 결과: 두 untouched zip 은 **같은 1,000 장면** — 2,000 으로 세면 안 됨
L5 near-dup     aHash r=4. 무작위 쌍은 median 31/64, r≤4 는 0.035% → 임계는 타이트.
                BROAD 내부는 기대값과 동일(x1.0). 초과는 CORNER_LA 내부 x6.06 등
                좁은 시점대역 세트에 집중 = 설계상 닮은 것
L6 concentration 전 pool 배경이 **2종뿐**(industrial/parking_lot) → >50% 는 구조적
HARD_BLOCK 0    training manifest 에 dev/holdout 프레임 0
```

### 4. 결정

```
PAPER_CORE_V1          corner = line = BROAD_40K MH_TRAIN 33,758
DEPLOYMENT_CANDIDATE   corner 동일(CORNER_LA 미포함) / line = BROAD 0.95 + EDGE 0.05
PAPER_NEURAL_RETRAIN_REQUIRED = False  ← 검사로 확인:
    E3 meta 의 pool 33758 == 계약 33758, split_sha256 56b36d2a… 일치
DEPLOYMENT_RETRAIN_REQUIRED = True (line stream 만)
RETRAIN 판정 = DO_NOT_RUN (composition 만 lock)
```

### 5. ★ EDGE 비율 규칙의 결함을 발견하고 명시

clause 1("line-hard cell 이 BROAD 대비 2배")은 **BROAD 가 그 cell 을 0장 덮기 때문에
분모가 0** — 어떤 비율에서도 통과하는 무효 조항이었다(G1 게이트가 V_vis≥4 를 요구해
BROAD 에 V_vis≤3 이 정의상 0장). 통과로 적지 않고 `clause_1_is_vacuous=True` 로 기록하고,
선정 근거를 "보호 조항(broad mode 보존 ≥0.85, EDGE 반복 ≤3x) + 보수적 기본값" 으로
정정했다. AGGRESSIVE 0.20 은 보존 0.800 으로 탈락, CONSERVATIVE 0.05 채택
(보존 0.950, EDGE 반복 0.18x).

### 6. PHASE 11 real 참조 — 관측축만

```
              real                          synthetic
해상도        640x480 1954 / 1280x720 224   640x480 / 960x540 / 720x480 / 560x560
              2560x720 55 / 592x1280 55     ★ real 의 1280x720·2560x720·592x1280 미표현
luma p50      123 (night 24 / outside 76.5) 55.76
```
★ synthetic 이 real 보다 **체계적으로 어둡다**(55.8 vs 123). yaw/elevation 은 라벨이
없어 추정하지 않았다.

### 7. 산출물

`data/pallet/results/paper_s2_multihead/` 아래 `data_audit/`(14) · `eda/`(25 그림) ·
`dataset_release/`(9, checksums 포함). 물리 concat 0 — manifest 만.
