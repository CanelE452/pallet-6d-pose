# 2026-08-21

세션이 08-20 에서 자정을 넘겨 이어졌다. 08-20 작업은 `2026-08-20.md` 에 있고,
여기는 **00:00 이후** 변경분만 적는다.

> Stop hook 이 49개 파일을 "이번 세션 변경" 으로 지목했으나 mtime 이 전부
> **08-16 ~ 08-20** 이다 (`CLAUDE.md` 08-19 17:07, `mh_screen.py` 08-20 01:37,
> `mh_data.py` 08-16 02:35 등). 이전 세션에서 만든 뒤 커밋되지 않고 남아 있던
> 것들이고 오늘 열지 않았다. 아래가 오늘 실제로 바뀐 전부다.

## [01:10] PAPER_GENERIC_V1 60 epoch 학습 완료 (드라이버 자동)

`runs_paper/yolo26n_paper_generic_v1_seed42` — results.csv 60행, 270.0분,
batch 32 / patience 0 유지. 완료 판정 통과 후 드라이버가 덤프·판정까지 진행.

## [06:36] ★ 내 덤프 스크립트 버그로 판정이 무효였다

### 증상

재평가 결과가 `yolo26n_broad40k_5ep`(5 epoch 진단본)과 **소수점까지 완전히 동일**
(open R 2.92 / 5cm5 0.429, challenge R 7.84 / 5cm5 0.057, corner 32.3px).
60 epoch 이 5 epoch 과 같을 수 없다.

### 원인

`mc_dump_paper.py` 는 `mc_dump_expb.py` 를 `sed` 로 경로만 바꿔 만들었는데,
원본에서 경로 문자열이 **두 줄로 쪼개져** 있어 패턴이 안 맞았다.

```
Before (치환 실패, 5 epoch 체크포인트를 읽음)
  CK = os.path.join(ROOT, "challenge/yolo_pose_one_model/runs_broad40k/"
                          "b_yolo26n_broad40k_5ep/weights/last.pt")

After (한 줄로. 쪼개면 또 같은 사고가 난다)
  CK = os.path.join(ROOT, "challenge/yolo_pose_one_model/runs_paper/
                           yolo26n_paper_generic_v1_seed42/weights/last.pt")
```
검증: 두 체크포인트 md5 가 다름
(`f39ceece…` vs `925320e3…`) → 학습 산출물 자체는 정상.

### 조치

- `scripts/stage0/model_compare/mc_dump_paper.py` — 경로를 한 줄로 수정 + 사고
  경위를 주석에 남김
- 무효 판정 3건은 **지우지 않고** 사유와 함께 격리
  `evaluation/PAPER_YOLO_{VERDICT,NEXT,REAL_DEV_RESULT}_INVALID_5EP.json`
- 재실행 드라이버 `scratchpad/rerun_paper_eval.sh` — 덤프 직후 **weights 경로가
  runs_paper 인지 검사**해 아니면 중단(같은 사고 재발 방지)

## [06:37] 재평가 = **WEAK_PASS**

```
model                       open R  open 5cm5   chal R  chal 5cm5  chal corner
yolo26n_paper_generic_v1      2.25      0.571     5.63      0.152      18.3px
yolo26n_broad40k_5ep (5ep)    2.92      0.429     7.84      0.057      32.3px
yolo26n_synth (target 포함)    2.03      0.482     3.47      0.248      11.1px
yolo26n_ft   (real FT)        1.49      0.750     2.96      0.400       7.7px
FINAL40K_seed1                7.16      0.250    34.54      0.009      93.7px
```
5ep → 60ep 로 challenge 5cm5 **0.057 → 0.152 (2.7배)**. 학습량 효과 실재.

사전등록 게이트(사후 변경 없음):
```
challenge availability  0.629  >= 0.75  FAIL
challenge R median       5.63  <= 5.0   근소 FAIL
challenge 5cm5          0.152  >= 0.20  FAIL
open56 5cm5             0.571  >= 0.45  PASS
```
WEAK 조건(challenge 5cm5 ≥ 0.10, R ≤ 8, 5ep 대비 명확 개선) 전부 충족 → WEAK_PASS.

### 원인 분해

```
도메인      generic(outside/noapril/cad) 0.624 | target 0.218 | night 0.108
실패유형    OPEN_56    BOX_OK_KP_GOOD 0.839  BOX_OK_KP_BAD 0.054  NO_BOX 0.107
            CHALLENGE  BOX_OK_KP_GOOD 0.352  BOX_OK_KP_BAD 0.248  NO_BOX 0.371
```
generic 은 0.624 로 잘 되고 target/night 만 무너진다. 파이프라인·전처리 문제가
아니라 **pallet-family / appearance coverage** 문제이며, 학습 **전에** 기록해 둔
두께비 구멍(BROAD 에 target 0.0923 보다 얇은 프레임 0.29%)과 방향이 같다.

### NEXT

`BROAD_FAMILY_V2_SPEC_REVIEW` 하나. seed43/44 는 패키지가 준비돼 있으나
STRONG_PASS 가 아니라 스크립트가 스스로 차단(`AUTORUN_NEXT = False`).
WEAK_PASS 에서 seed 만 늘리지 않는다.

## 오늘 변경 파일

```
scripts/stage0/model_compare/mc_dump_paper.py          경로 버그 수정
scratchpad/rerun_paper_eval.sh                         신규 (덤프 출처 검사 포함)
challenge/yolo_pose_one_model/evaluation/              판정 3종 재생성 + 무효본 3종 격리
challenge/yolo_pose_one_model/analysis/                기전 분석 6종 생성
data/pallet/results/model_compare/kps_yolo26n_paper_generic_v1.json  재덤프
```

## [07:20] BROAD_FAMILY_V2 사양 확정 (렌더 0 / 학습 0 / 새 모델 0)

### PHASE 1 — mesh 배제 감사가 0/4 → **2/4**

`usd-core` 를 pip 설치해 `scene.usd` / `scene_1.usd` 를 실제 파싱했다.
canonicalization 규칙(병합·centroid 원점화·최대extent 정규화·회전불변 히스토그램
64bin)을 문서에 명시.

```
scene.usd    exact hash 불일치  회전불변 형상 L1 0.2556  치수비 L1 0.0455
scene_1.usd  exact hash 불일치  회전불변 형상 L1 0.3952  치수비 L1 0.0370
GLB 2 종     파일 부재 -> UNRESOLVED
MESH_EXCLUSION_EXACT = PARTIAL
```
이름이 CC 인터넷 모델처럼 보인다는 것을 근거로 쓰지 않았다.

### PHASE 2 — ★ unique mesh 가 4 개뿐

```
eur_pallet_bk_cc0.glb              10,182  25.5%
woodpallet_block_jtoastie_ccby.glb 10,099  25.2%
scene_1.usd                        10,095  25.2%
scene.usd                           9,624  24.1%
effective asset count 3.999   최대 share 0.2545
```
40,000 프레임은 이 4 개의 W/D/H 스케일 변형이다. **frame 다양성 ≠ mesh 다양성**
을 표에 못박았다.

### PHASE 3 — 실패 귀속

```
domain      outside NO_BOX 0.27 | noapril/cad GOOD 1.00 | pallet09 NO_BOX 0.50
            night08 NO_BOX 0.59 | night09 KP_BAD 0.40
luma        dark<60 GOOD 0.23 -> mid100-140 GOOD 0.67   (단조)
size        mid0.20-0.40 NO_BOX 0.59  vs >=0.40 NO_BOX 0.11
elevation   <3 NO_BOX 0.43 | 3-8 NO_BOX 0.48 | 8-15 KP_BAD 0.44 | 15+ GOOD 1.00
truncation  full KP_BAD 0.11 vs truncated KP_BAD 0.42
```

★ **elevation x luma 교차가 두 축을 실제로 가른다**:
```
elev<8  & bright   n=41  NO_BOX 0.46  GOOD 0.32   ← 밝아도 저앙각이면 못 찾는다
elev<8  & dark     n=30  NO_BOX 0.63  GOOD 0.20
elev>=8 & bright   n=23  NO_BOX 0.00  GOOD 0.70
elev>=8 & dark     n=11  NO_BOX 0.09  KP_BAD 0.73 ← 어둠은 검출보다 키포인트를 깎는다
```
그래도 **인과 분리는 주장하지 않았다** — target/night 세션이 같은 물체라
geometry 와 appearance 가 세션 수준에서 얽혀 있다. 문서에 명시.

### PHASE 4~6 후보

geometry 3 / appearance 3 / mixture 3. **frame 수는 정하지 않았다** —
geometry 후보의 축은 "서로 다른 mesh 를 몇 개 확보하느냐" 다.
mixture 는 총 40,000 고정(수 증가와 다양성 증가를 섞지 않기 위해).

### 권고 — 하나

`G_BALANCED(+20 mesh) + A_BALANCED(+6 strata)` 를 `MIX_BALANCED(55/25/20)` 로
40,000 고정 조립. **단 렌더보다 먼저 풀 것 3 가지**를 계획서에 적었다:
GLB 2 종 회수(4/4 로 닫기) / +20 mesh 확보 경로(실질 병목) / generic 0.624 의
허용 하락폭 freeze.

### 산출물

`challenge/yolo_pose_one_model/broad_family_v2/` — 어제 `paper_generic_pipeline/`
아래 만들었던 폴더를 브리프 지정 위치로 **옮기고 symlink** 로 이었다(중복 생성 안 함).
```
BROAD_FAMILY_V2_PURPOSE.md / _PLAN.md
TARGET_ASSET_EXCLUSION_AUDIT_V2.{json,md}   audit_v2.py
CURRENT_ASSET_FAMILY_AUDIT.{csv,md}         asset_family_audit.py
REAL_DEV_FAILURE_ATTRIBUTE.csv / _REPORT.md dev_failure_attribute.py
GEOMETRY_ADDON_CANDIDATES.md / APPEARANCE_ADDON_CANDIDATES.md
CONTROLLED_MIXTURE_CANDIDATES.{json,md}
```

RENDER = **DO_NOT_RUN**. 사용자 승인 대기.

## [08:10] BROAD_FAMILY_V2 렌더 사양 — **HARD BLOCK** 판정

2x2 factor 설계로 전환하고(geometry/appearance 를 배타적 pool 로 나누지 않음)
실제 렌더 가능성을 실측했다. 렌더 0 / 학습 0.

### ★ 렌더를 막는 것은 예산이 아니라 mesh 부재

```
독립 topology (로컬 실측)  6   ← 회전·평행이동·균일스케일 불변 서명으로 클러스터링
  BROAD 사용 4 / BROAD 밖 신규 4 (scene_2, scene_3, SM_PaletteA_01, _02)
G_CONSERVATIVE 요구 +8      → 도달 불가
license 확인 완료 0 건      ← Isaac=NVIDIA EULA, project USD=출처 미상
```

**THIN 층이 로컬에 0 개**이고 평가 대상 cell 이 바로 `MID/THIN` 이다.
```
aspect x thickness 9 cell
  BROAD 4 가 덮는 것          MID/MID                          1
  로컬 전체가 덮는 것          LOW/THICK, MID/MID, MID/THICK,
                             HIGH/THICK                       4
  평가 대상 cell              MID/THIN                         아무도 안 덮음
```
지금 렌더하면 THIN 없는 데이터를 20,000 장 더 만드는 것이고 현재 실패를 재생산한다.

### Isaac asset 에서 신규 mesh 2 개 발견

`SM_PaletteA_01`(verts 514, aspect 1.210, thick 0.1740) /
`SM_PaletteA_02`(verts 490, aspect 1.510, thick 0.1715) — 서로 다른 topology
(회전불변 hash 상이)이나 **둘 다 THICK** 이라 빈 cell 을 못 채운다.

### 신규 산출물

```
mesh_bank.py + GENERIC_MESH_BANK.csv / _AUDIT.md
GEOMETRY_COVERAGE_BEFORE_AFTER.csv
APPEARANCE_STRATA.json        6 strata + 확률적 요인
FACTOR_CELL_SPEC.json         G0A0/G1A0/G0A1/G1A1 + confound 통제 조항
MIXTURE_CANDIDATES.{json,md}  추천 M1_BALANCED (0.50/0.17/0.17/0.16, 40K 고정)
GENERIC_SAFETY_LOCK.json      ★결과 전 freeze
BROAD_FAMILY_V2_RENDER_PLAN.md
```

### GENERIC_SAFETY_LOCK (V2 결과 보기 전 고정)

```
generic 5cm5 >= 0.574 (baseline 0.624, absolute -5pp)
generic R median 악화 <= 10%
초과 시 GENERIC_SAFETY_FAIL
★ generic = 56 프레임이라 1~2 프레임이 ~2pp 를 움직인다. point estimate 만으로
  결론 내지 말 것을 lock 파일에 함께 기록.
```

### 렌더 전 선결 4 가지 (순서)

```
1. GLB 2 종 회수/서명 export  -> MESH_EXCLUSION_EXACT = PASS (현재 2/4)
2. THIN 층 mesh 확보          -> 현재 0 개. 최대 병목
3. 독립 topology 최소 8 개     -> 현재 신규 4 개
4. 확보 mesh 전부 license 확인 -> 현재 0 건
```

mesh 확보 경로 3 안을 제시하고 (다) 사실적 mesh + procedural 혼합을 권고했다.
THIN 은 procedural 로만 채울 수 있고 사실성은 공개 mesh 가 준다. 다만 이건 렌더
스펙이 아니라 **asset 파이프라인 신설**이라 착수 전 별도 합의가 필요하다.

## [09:10] PRE-V2 진단 — ★ STOP RULE 발동 (학습 0)

V2 렌더가 막힌 동안 "WEAK_PASS 가 정말 data gap 인가" 를 기존 checkpoint 와
REAL DEV 만으로 분해했다. 새 학습·architecture 0.

### 판정 = `INFERENCE_PIPELINE_ISSUE_FOUND` + `LABEL_SEMANTIC_ISSUE_FOUND`

```
STOP RULE 발동 근거
  D1  availability 잠재 +18.6pp   (기준 +10pp 초과)
  D3  gross frame 의 54.2% 가 semantic 으로 설명   (기준 30% 초과)
```
data gap 이 **유일한** 원인이라는 가설은 약해졌다. V2 spec 을 자동 변경하지 않고 보고.

### D1 — NO_BOX 45 중 30 (66.7%) 은 검출 실패가 아니다

conf 만 0.4 → 0.001 로 낮추면 IoU>=0.5 올바른 후보가 30 장에서 뜬다.
```
LOW_CONF_CORRECT_CANDIDATE  30 (66.7%) / WRONG_OBJECT_TOP1 8 / AMBIGUITY 7
```
confidence calibration 문제. 단 positive DEV 뿐이라 threshold 를 여기서 내리면
FP 를 못 보고 정하는 것 — real negative 와 함께 정해야 한다.

### D2 — 해상도는 병목이 아니다 (기각)

```
imgsz   OPEN56 avail/IoU>=0.5     CHALLENGE avail/IoU>=0.5
 640      0.893 / 0.786             0.629 / 0.486
 960      0.839 / 0.750             0.676 / 0.467
1280      0.696 / 0.446             0.533 / 0.229
```
960 은 availability +4.7pp 이나 올바른 검출 비율은 −1.9pp, 1280 은 붕괴.

### D3 — ★ near/far role 혼동이 절반

gross(R>10°) 24 장에서 최선 permutation:
```
identity 11 (45.8%) / near_far_swap 11 (45.8%) / top_bottom 2
role confusion 54.2%
```
D4 의 `far_only 29.0° vs near_only 14.3°` 와 정합. oracle permutation 은 진단
전용이며 main metric 으로 쓰지 않는다고 파일에 명시.

### D4/D5/D6 — 전부 기각

```
D4  top7 이 전체와 사실상 동일 (5.548 vs 5.629) → 나쁜 점 하나의 오염 아님
D5  GT 점에서 네 solver 모두 0.00~0.04° / 예측 점에서만 3.3~3.5° → solver 무결
D6  GT 재투영 median 0.42~2.46px, GT2D->pose R 불일치 0.000°
    night/target 만 residual 큰 패턴 없음 → calibration/annotation 탓 불가
```

### D6D — ★ 치수가 translation 바닥을 정한다

```
치수 +1% → t 0.0217m,  +2% → t 0.0435m,  +5% → t 0.1086m   (R 은 전부 0.0000°)
```
`5cm5deg` 의 t 기준이 0.05m 인데 **치수 2% 오차만으로 0.0435m** 가 생긴다.
실측 치수 정확도가 t 성능의 상한이다. REAL_TEST 촬영 시 줄자 실측이 필수인 이유.

### D7 — selective prediction 작동

`box_conf` Spearman −0.433 (최강). `pnp_reproj` 로 coverage 70% 로 줄이면
challenge R 5.63→3.87, 5cm5 0.24→0.33. threshold 확정은 real negative 후로 보류.

### D8 — real negative 후보 확보, 측정은 보류

배포영상 911 장 중 FT 가 쓴 259 장. PAPER_GENERIC_V1 은 real 0 장이므로 미사용 →
`REAL_NEG_DEV_CANDIDATE`. FP/image 는 재지 않았다 — threshold 확정과 묶이면 소진되므로
승인 후 별도. ★ `yolo26n_ft`/`yolo26m_ft` 는 이 negative 를 학습했으므로 FP 비교 금지.

### 산출물

`challenge/yolo_pose_one_model/analysis_pre_v2/` — NO_BOX_DISSECTION,
RESOLUTION_SCREEN, KEYPOINT_PERMUTATION_AUDIT, KEYPOINT_SUBSET_PNP,
PNP_SOLVER_SANITY, GT_CAMERA_CALIBRATION_AUDIT, DIMENSION_SENSITIVITY,
CONFIDENCE_ERROR_CALIBRATION, REAL_NEGATIVE_DEV_AUDIT,
PRE_V2_DIAGNOSTIC_REPORT.md, PRE_V2_DECISION.json
(+ 스크립트 d1d2_sweep.py, d3_to_d7.py)

### NEXT — 하나

D3 role 혼동을 V2 spec 에 반영. 현재 2x2(geometry x appearance)에 **앞뒤 면을
구분 가능하게 하는 viewpoint/비대칭 구조 축**이 필요하다. 저앙각에서 near/far 가
겹쳐 보이는 것이 유력한 기전 [추정]. D1(confidence)은 데이터가 아니라 추론 계약
문제라 별개 트랙이며 real negative 확보 후에 다룬다.

## [09:44:18] ⚠️  Auto Compact (컨텍스트 한계 도달)

- Working directory: `/home/minjae/Documents/github/pallet-pose`
- Trigger: `auto`
- Session ID: `742091d2-76ae-45b1-b584-8cfb1f1a04e0`
- Transcript: `/home/minjae/.claude/projects/-home-minjae-Documents-github-pallet-pose/742091d2-76ae-45b1-b584-8cfb1f1a04e0.jsonl`
- Note: 컨텍스트가 가득 차서 자동 트리거됨 — 다음 세션에서 이 마커 위쪽 작업을 반드시 복원할 것

**TODO (session resume)**: compact 이후 이 마커 위쪽의 작업 내역을 확인하고 이어서 진행


## [09:50] CONDITIONAL HOUGH — Direct-Hough 트랙 최종 종료

Direct-Hough 를 닫기 전 마지막 확인. "점이 좋을 땐 point-only, 점이 불안정할 때만
Hough/F3" 라는 조건부 fallback 이 catastrophic pose 실패를 회수하는가.

**재추론 0** — 저장된 YOLO 60ep 키포인트(`kps_yolo26n_paper_generic_v1.json`,
116/161 검출) + FINAL40K seed1 의 line theta/rho(161/161) 만 재사용.
★ 기존 hybrid 매트릭스의 B1 은 5 epoch 판이라 쓰지 않고 60ep 로 다시 계산했다.

### 새 파일

- `challenge/yolo_pose_one_model/analysis_pre_v2/conditional_hough.py` — PHASE 1 oracle
- `.../conditional_gate_cv.py` — PHASE 2~5 gate + session LOSO CV + 사전등록 판정
- `.../conditional_phase6.py` — PHASE 6 실패 해석 + 플롯 (NanumGothic 등록)
- 산출: `CONDITIONAL_HOUGH_ORACLE.json` / `_CV.json` / `_PHASE6.json` /
  `_PER_FRAME.csv` / `_PLOTS.png` / `_REPORT.md`

### PHASE 1 — oracle headroom (STOP RULE 통과)

```
population              arm       R med    R p90    5cm5
──────────────────────────────────────────────────────────
REAL_DEV_OPEN_56  n=50   Y0         2.25     5.54   0.640
                         YH         5.00     7.84   0.380
                         ORACLE     2.25     5.14   0.680
REAL_CHALLENGE_DEV_105   Y0         5.63    79.34   0.242
                  n=66   YH         8.06    90.11   0.182
                         ORACLE     4.78    51.91   0.288
```

challenge oracle R 개선 15.0% · Hough 승률 0.394 → 사전등록 STOP RULE 3 조건이
동시 성립하지 않아 `HEADROOM_SUFFICIENT_PROCEED`.

★ `YH`(항상 Hough)는 두 population 모두 `Y0` 보다 나쁘다. Hough 는 평균적으로
해롭고 가치가 있다면 선택적으로 켤 때뿐 — 그래서 gate 가 전부다.

### PHASE 2~5 — 예측 gate + session LOSO → REJECT

gate feature = `pnp_reproj` 하나(GT 미사용). tau 는 session LOSO 로 선택
(held-out 빼고 6 개에서 고른 뒤 held-out 에서만 평가). 선택 목적함수 사전등록:
R median 최소화 s.t. t 악화 ≤5%, 5cm5 악화 ≤0pp.

```
지표                      측정        게이트
──────────────────────────────────────────────────
challenge R 개선        -0.0644     >= +0.10 (지지) / < +0.05 (기각)  ← 기각
challenge 5cm5 이득     +0.0303     >= +0.03
challenge t 악화        +0.0510     <= +0.05
open 5cm5 악화          -0.0200     <= +0.02
open R 악화             +0.0000     <= +0.05
CV fold R 비악화        4/7         >= 5/7
```

**VERDICT = `HOUGH_TRACK_CLOSED`**

정직하게: 5cm5 는 올랐고(challenge +3.0pp, open +2.0pp) open 은 손상되지 않았다
(R 동일, t 100% 비악화). 그러나 1 차 지표는 rotation median 이고 그게 악화됐다.
결과를 보고 지표를 바꾸지 않는다.

### PHASE 6 — oracle 15% 가 gate 에서 −6.4% 가 된 이유

1. **gate feature 판별력 부재** — reproj 로 "Hough 승리 프레임" 예측 AUC **0.597**
   (0.5=무작위). PHASE 1 의 median 차이(11.36 vs 8.68px)는 분포 겹침을 가린
   집계 통계였다. ★ *집계 통계가 per-frame 정확성으로 전이되지 않는* 패턴 재발
   (memory `aggregate-scale-statistic-does-not-transfer-to-pose` 와 동형).
2. **이득이 소수에 몰림** — 승리 31 프레임 중 상위 5 개가 전체 이득의 **60.8%**.
   그 5 개를 못 집으면 이득 0, 손해만 남는다.
3. **켠 프레임 손익이 대칭** — 활성 19 중 도움 10 / 손해 9, 도움 median +0.81° ·
   손해 median −0.55°. median 이 개선될 이유가 없다.

### 적용 범위

real DEV 161 중 60ep YOLO 검출 116 장 기준. OPEN_56·CHALLENGE_DEV_105 는 **DEV**
(final test 아님). oracle 은 진단용 상한이며 deployment 결과가 아니다. 이 판정은
*조건부 Hough* 를 닫은 것이고 line branch 자체(SPLIT_LATE 구조 분리, theta-only
회전 이득)의 과거 판정을 뒤집지 않는다. gate feature 는 `pnp_reproj` 하나만
시험했다 — 다른 feature 로 다시 열려면 **새 brief** 가 필요하다(결과를 본 뒤
feature 를 늘리는 것은 이 판정의 무효화).

## [10:55] YOLO conf operating point 보정 — CONF_CALIBRATION_UNRESOLVED

질문 하나: conf=0.4 때문에 숨겨진 올바른 detection 을 real negative FP 를 폭증시키지
않고 얼마나 회수 가능한가. Hough/CIGM/F3 는 CLOSED 판정대로 미실행, 새 학습 0.

### 새 파일

- `challenge/yolo_pose_one_model/analysis_pre_v2/cc_dump.py` — PHASE 0 audit +
  conf=0.001 전수 후보 덤프 (pallet-yolo26 env). positive/negative 에 동일 전처리
  계약(PAD=100 REFLECT_101, imgsz=640, top-1 by conf)
- `.../cc_sweep.py` — PHASE 2~7 오프라인 sweep·판정·플롯
- 산출: `REAL_NEG_DEV_AUDIT.json`(덮어씀, 요약→per-frame 259 inventory) /
  `YOLO_CONF_SWEEP.json` / `YOLO_CONF_SWEEP_PER_FRAME.csv` /
  `YOLO_CONF_CALIBRATION_REPORT.md` / `plots/{PR_CURVE,FP_VS_RECALL,RECOVERED_LOWCONF_POSE}.png`
- 수정: `broad_family_v2/BROAD_FAMILY_V2_RENDER_PLAN.md` §9 추가,
  `broad_family_v2/FACTOR_CELL_SPEC.json` 에 `LOW_ANGLE_ROLE_DISAMBIGUATION_COVERAGE` 키 추가

### PHASE 0 — REAL_NEG_DEV_V1 역할 고정 (유효)

259 장, forklift_raw_20260528_163408. paper_generic 학습 사용 False (학습셋 =
BROAD 합성 40,000, real 0장). positive DEV 와 session overlap 없음 (촬영일
04-03/05-13/05-22 vs 05-28). 이 순간부터 threshold calibration DEV, final test 재사용 금지.

★ **표본 편향 발견** — `prepare_real_ft.py` 가
`frames = [r for r in rows if r["max_conf"] < 0.20]` 로 골랐다. 911 중 259.
팔레트 없는데 이전 모델이 conf>=0.2 로 오검출한 프레임은 체계적으로 배제됐다.
→ **FP/image 는 하한**이다. 학습 누수는 아니고 표본 편향이다.

### PHASE 2~3 — sweep, threshold UNRESOLVED

```
   conf   avail   recall   r_avail   corner   R med    5cm5   FP/img  frac FP
──────────────────────────────────────────────────────────────────────────────
  0.001   1.000    0.708     0.857    17.04    4.33   0.304    5.510    0.873
   0.05   0.925    0.689     0.727    14.96    3.78   0.329    1.004    0.649
   0.40   0.720    0.584     0.590    12.69    3.28   0.414    0.208    0.201
```

recall = top-1 survivor 의 IoU>=0.5 (배포가 실제 쓰는 것). **recall>=0.98 을
만족하는 threshold 없음** — 최대 0.7081 @ conf=0.001, 그 지점 FP/image 5.51(하한).
게이트를 0.95 로 낮추지 않았다. conf 0.40→0.001 로 availability +28.0pp 이지만
recall 은 +12.4pp, FP/image 는 26배.

### PHASE 4 — AUROC 0.8399 / AUPRC 0.8370 (frame-level presence, 161 vs 259)

### PHASE 5 — 회수분 pose 는 쓰레기 (확증)

conf 0.40 에서 안 잡히고 0.001 에서 잡히는 45 장:

```
지표                기존 검출(116)   회수분(45)     배율
────────────────────────────────────────────────────────
IoU>=0.5 비율          0.810          0.444
corner median (px)     12.69          84.72        6.7x
R median (deg)          3.28          10.81        3.3x
t median (m)          0.0682         2.1231         31x
5cm5                   0.414          0.022
gross R>10 비율        0.207          0.533       +32.6pp
```

사전등록 pose gate(+20pp 이내)를 +32.6pp 로 위반 → **presence gate 와
pose-quality gate 를 분리해야 한다**(brief 가 예고한 조건 성립).

### ★ 예상 밖 소득 — 병목은 threshold 가 아니라 top-1 랭킹

conf=0.001 에서 recall 0.708 vs 천장(recall_available) 0.857 → **14.9pp = 24 장**이
올바른 후보가 목록에 있는데 top-1 이 아니라서 버려진다. threshold 를 내려도 회수
불가. box ranking/NMS 문제. (conf=0.4 에선 격차 0.6pp — 낮출수록 랭킹이 무너진다.)

브리프 전제였던 "correct candidate 존재 66.7% → +18.6pp 회수" 는 *후보 존재*
기준이고, 실제 배포에 쓰이는 top-1 통과분은 +12.4pp, 그 중 pose 쓸 만한 건 거의 없다.

### PHASE 6~7

D3 role confusion 그대로 인용 (gross 24, near_far_swap best 11 → 회수비율 0.4583,
role_confusion_rate 0.5417) → V2 spec 에 `LOW_ANGLE_ROLE_DISAMBIGUATION_COVERAGE`
추가. near/far swap oracle 은 main inference 미사용.

**VERDICT = `CONF_CALIBRATION_UNRESOLVED`**. FP 기준을 결과 보고 만들지 않았다.
natural prevalence 미상이므로 FP/image 만 보고, 배포 threshold 는 별도.
conf=0.4 가 최적이라는 증거는 없지만 **낮춰서 얻을 것이 없다** — threshold 는
이 모델의 레버가 아니다.

### NEXT

BROAD_FAMILY_V2 **유지**(수정 아님) + §9 요구 추가. 새로 열린 트랙 둘:
(1) top-1 랭킹 24장 손실 — box ranking/NMS, (2) presence/pose-quality gate 분리.
둘 다 데이터가 아니라 추론 계약 축이므로 V2 렌더와 병렬 가능.

---

## [12:03] 논문 트랙 YOLO 를 배포영상 4프레임에 추론 (FINAL40K 짝)

사용자 요청: "논문용으로 만든 yolo 로 이 4개 사진 추론해줄 수 있어?"

### §1 어느 가중치가 "논문용" 인가 — args.yaml 로 확정

`model_compare` 에 YOLO 덤프가 5종 있어 이름만으로는 못 고른다. 학습 args 를 열어
데이터셋으로 갈랐다.

```
runs_paper/yolo26n_paper_generic_v1_seed42   data=datasets/broad40k/data.yaml  60 epoch  정본161 116/161
runs_broad40k/b_yolo26n_broad40k_5ep         data=datasets/broad40k/data.yaml   5 epoch  정본161  93/161
release/pallet-pose-yolo26n-ft               (challenge 트랙, real 파인튜닝)             정본161 157/161
```
**앞의 둘이 논문 트랙**이다 — FINAL40K(SPLIT_LATE)와 **같은 BROAD 합성 40,000**,
real 0장. `*_ft` 는 real 을 봤으므로 이 비교에 넣지 않는다(넣으면 "같은 데이터,
다른 아키텍처" 라는 비교 자체가 깨진다).

### §2 신규 파일

```
scripts/stage0/model_compare/mc_vdo_dump_yolo.py   pallet-yolo26 env, 키포인트만 덤프
scripts/stage0/model_compare/mc_vdo_draw.py        pallet-pose env, PnP + 그리기
data/pallet/results/model_compare/vdo_infer/PURPOSE.md
```
env 를 가르는 이유는 기존 `mc_dump_yolo.py` 와 같다 — yolo26 은 ultralytics
8.4.60, DOPE 계열은 8.0.120 이라 한 프로세스에 못 올린다. 좌표만 넘기고 pose 와
그림은 한쪽에서 처리해야 비교가 성립한다.

공정성을 위해 **K(`internet_pallet_infer.fit_K` HFOV 스윕) · 치수 가정 ·
그림 규약(`ft_vdo_infer.draw`)** 을 FINAL40K 와 동일하게 맞췄다. 못 맞춘 둘은
헤더에 박았다: pose 팔(FINAL40K=F3 line 재적합 / YOLO=점 PnP), det 정의
(DOPE=belief 0.3 / YOLO=박스 잡히면 9점 항상 출력 → kp_conf 0.5 로 셈).

Before/After 1건: `cv2.putText` 가 한글을 못 그려 헤더가 `??????` 로 나옴 →
`"검출 없음"` → `"NO DETECTION (0 instances)"` 로 교체.

### §3 결과 — 셋이 서로 다른 프레임에서 무너진다

```
프레임    FINAL40K(SPLIT_LATE)    paper_generic_v1(60ep)   broad40k_5ep(5ep)
──────────────────────────────────────────────────────────────────────────────
000037    det 8/8  F3  12.58m     NO DETECTION             det 8/8   12.34m
000076    det 0/8  skip           det 8/8  7.63m           det 8/8   10.50m
000483    det 0/8  skip           det 8/8  8.62m           NO DETECTION
001678    det 7/8  F3  14.03m     det 8/8  6.35m           det 8/8    6.70m
```
4프레임 중 셋 다 잡은 것은 **001678 하나뿐**. 같은 학습 데이터인데 실패 지점이
갈린다 = 아키텍처 차이보다 셋 다 이 분포 밖에서 불안정하다는 쪽.

★**검출됐다고 맞은 게 아니다.** 눈으로 보면 001678 만 실제 초록 팔레트를 감쌌고,
`paper_generic_v1` 의 000076·000483 은 큐보이드가 **빈 바닥**에 있다(000076 은
배경의 진짜 팔레트들을 두고 엉뚱한 데를 잡았다). `broad40k_5ep` 의 000037·000076
은 지게차 발밑·몸통 아래. `det 8/8 box_conf 0.87` 이 FP 를 가린다.

★**reproj 1.2~3.0px 는 정확도 증거가 아니다.** cam_K 가 없어 HFOV 를 스윕해
reproj 최소 K 를 채택하므로, 점이 어떻게 놓이든 맞는 K 가 나온다. 실제로 같은
카메라에서 HFOV 가 21deg(FINAL40K/001678) ~ 80deg(broad40k_5ep) 로 갈렸다.
FINAL40K 의 21deg 는 K 피팅 붕괴 신호로 보이고 거리 14.03m 도 그 위의 값이다 —
YOLO 둘은 같은 장면을 6.35 / 6.70m 로 본다. [추정] 실제 화각 미확인이라 어느
쪽이 맞는지는 단정 못 함.

**표본 4프레임.** 우열 판단 근거 아님. GT 0 이라 pose 정확도는 잴 수 없다.

## [14:35] RERANKING 축 — RERANKING_SUPPORTED (단, box 만)

질문: low-conf candidate list 안에 있는 correct candidate 를 top-1 confidence
ranking 이 놓치는 24 장을, GT 없이 rerank 해 **pose 까지** 회수할 headroom 이 있는가.
새 학습 0 · 새 architecture 0 · 재추론 0 (`_cc_raw_dump.json` 재사용).

### 새 파일

- `analysis_pre_v2/rr_oracle.py` — PHASE 1 top-K oracle + PHASE 2 분류
- `analysis_pre_v2/rr_features.py` — PHASE 3 단일 feature + PHASE 4 session LOSO
- `analysis_pre_v2/rr_deploy_check.py` — 배포 결합(conf 별 rerank 이득) + negative secondary
- `analysis_pre_v2/rr_plots.py`
- 산출: `RERANK_ORACLE.json` / `RERANK_FEATURES.json` /
  `RERANK_DEPLOY_COUPLING.json` / `RERANK_PER_FRAME.csv` / `RERANK_REPORT.md` /
  `plots/{TOPK_ORACLE,FEATURE_RANKING,RERANK_COUPLING}.png`

### PHASE 1 — top-K oracle (STOP 미발동, AND 조건)

```
  K    avail  corr_rec    R med    t med   5cm5(any)
─────────────────────────────────────────────────────
  1    1.000     0.708     4.33   0.1267     0.304
  2    1.000     0.752     3.60   0.1361     0.304
  3    1.000     0.789     3.24   0.1577     0.311
  5    1.000     0.826     3.11   0.1899     0.317
```

Top1→Top5: correct recall **+11.80pp**(기준 5pp) / 5cm5(any) **+1.24pp**(기준 3pp).
AND 라 STOP 미발동 → PROCEED. 그러나 **이 비대칭이 답이다** — 후보 목록에 "맞는
상자" 는 더 있지만 "쓸 만한 pose" 는 없다(5cm5 상한 +2장).

### ★ PHASE 2 분류 — 핵심

usable pose = IoU>=0.5 AND R<=10deg AND t<=0.10m (사전등록)

```
분류                             n     비율    OPEN/CH
──────────────────────────────────────────────────────
TOP1_ALREADY_GOOD               65    40.4%   40 / 25
A_GOOD_CANDIDATE_MISRANKED       9     5.6%    2 /  7   <- rerank 상금 전부
B_CORRECT_BOX_BAD_KP            59    36.6%   11 / 48   <- 진짜 병목
C_NO_CORRECT_CANDIDATE          28    17.4%    3 / 25
```

브리프의 "24장" 은 box 기준이고, pose 까지 따지면 상금은 **9장**. 압도적 다수는
**상자는 맞는데 keypoint 가 나쁜 59장** 으로 rerank 로 원리적으로 손댈 수 없다.

### PHASE 3 — 단일 feature (learned reranker 금지, 조합 금지)

```
feature              AUC(box)   AUC(use)
─────────────────────────────────────────
box_diag               0.9901     0.9043
box_area               0.9800     0.9088
box_conf               0.7248     0.8182
kp_conf_mean           0.6767     0.8215
neg_pnp_reproj         0.3454     0.6438
cuboid_plausible       0.2873     0.1926
depth_valid            0.2752     0.1818
```

1. **box 크기가 confidence 를 크게 앞선다**(0.98~0.99 vs 0.72) — 모델 conf 는
   *어느 상자가 팔레트인지* 를 잘 못 가른다. D1 calibration 과 같은 방향.
2. **neg_pnp_reproj 는 무작위보다 나쁘다**(0.345). 직전 조건부 Hough 의 gate
   AUC 0.597 에 이은 **독립 두 번째 확증** — reproj 는 신뢰할 신호가 아니다.
3. **depth_valid / cuboid_plausible 은 강한 역상관**(0.18~0.29) — 기하 sanity
   check 를 필터로 걸면 오히려 정답을 떨어뜨린다. 쓰지 말 것.

### PHASE 4 — session LOSO → RERANKING_SUPPORTED

7/7 fold 에서 `box_area`(또는 `box_diag`) 선택 — session 에 안 흔들린다.

```
지표             native    reranked     변화
──────────────────────────────────────────────
recall           0.708      0.814     +10.56pp
5cm5             0.304      0.311      +0.62pp   <- 161장 중 1장
R median (deg)    4.33       4.33      +0.00
```

사전등록 게이트(recall>=+5pp AND 5cm5 악화<=0 AND R median 악화<=0) 통과.

### ★ 배포에서 무엇을 사는가 — rerank 는 후보>=2 일 때만 작동

```
    conf   multi   nat rec   rr rec   +장   nat use   rr use   +장
──────────────────────────────────────────────────────────────────
   0.001    0.83     0.708    0.814    17     0.404    0.422     3
    0.05    0.38     0.689    0.727     6     0.404    0.416     2
    0.40    0.06     0.584    0.590     1     0.385    0.391     1
```

현재 운영점 conf=0.40 에선 후보 2개 이상 프레임이 **6% 뿐** → 실효 이득 box +1장,
pose +1장. 큰 이득(+17장)은 conf=0.001 에서만 나오는데 그 운영점은 직전 실험에서
pose 를 파괴함이 확인됨(t 31배). end-to-end 로는 conf=0.001+rerank usable 0.422
vs 현재 0.385 = +3.7pp(6장), 대신 negative FP 프레임 20.1%→87.3%(하한).

**rerank 는 negative FP 를 바꾸지 않는다** — threshold 를 먼저 적용하므로 살아남는
박스 수가 같고 어느 것을 보고할지만 달라진다. (첫 판에서 negative 만 "전체에서
최대 박스 고른 뒤 conf 확인" 으로 계산해 계약이 어긋났던 것을 바로잡음.)

### 결론

**RANKING 축 종료.** 상한이 9장이고 배포 가능한 규칙으로 얻는 건 1~6장이다.
`box_area` 는 "장면에 팔레트가 하나이고 그게 가장 큰 물체" 가정에 올라타 있어
다중/원거리 팔레트에서는 `[미검증]`.

### NEXT

진짜 축은 **B_CORRECT_BOX_BAD_KP 59장(36.6%)** = keypoint 품질 = 데이터 축.
ranking·threshold·solver 어디로도 못 간다. 이미 진행 중인 `BROAD_FAMILY_V2` +
§9 `LOW_ANGLE_ROLE_DISAMBIGUATION_COVERAGE` 가 정확히 그 축.
→ **BROAD_FAMILY_V2 유지, 수정 없음.**

---

## [14:53] 실패 원인 분리 감사 — 학습 0 / 추론 0

사용자 브리프: 남은 실패가 (1) box threshold (2) top-1 selection (3) metric scale/
dimensions (4) symmetry/near-far (5) topology/appearance 중 어디인지 기존 산출물만으로
분리. 새 파일 `scripts/stage0/model_compare/mc_failure_locus_audit.py`,
산출 `challenge/yolo_pose_one_model/audit_20260821T1449/` (기존 파일 무수정).

### PHASE A — inventory 8개 중 7개 실재, V2 만 부재

```
PAPER_GENERIC_V1 ckpt   EXISTS  last.pt sha 6a40a4d4…  results.csv 60행 완주
REAL_DEV_OPEN_56 / _105 DERIVED + paper_generic_pipeline/eval_manifest.json 에 물성화
negative 259            EXISTS  REAL_NEG_DEV_AUDIT.json (max_conf<0.20 선별 = 편향, FP 하한)
conf 0.001 전수 덤프      EXISTS  _cc_raw_dump.json (positive 161 + negative 259)
BROAD40K                EXISTS  40,000장 + manifest sha 검증 PASS
BROAD_FAMILY_V2         ABSENT  렌더 이미지 0장 (사양·계획 문서만)
```
BLOCKED_SOURCE_MISSING 미발동. 학습 프로세스 없음 확인(GPU 는 rustdesk 272MiB,
`runs/stage_a_gpu.csv` 는 nvitop 로거의 1분 append).

### ★ PHASE B — 기존 지표의 분모가 threshold 마다 바뀐다

`YOLO_CONF_SWEEP.json` 의 `success_5cm5` 는 **available 프레임 기준(conditional)** 이라
threshold 를 올릴수록 좋아지는 것처럼 보인다(0.3043 → 0.4138). 전체 positive 161 을
분모로 놓으면 착시가 사라진다. 그래서 재계산했다(재추론 0, `_cc_raw_dump` 의 kps 로
PnP 만 다시 풂).

또 하나: 현재 평가는 `eval_manifest item['dimensions_m']` = **per-frame exact label
치수**로 PnP 를 푼다. 배포에는 없는 정보다.

### PHASE C/D — threshold 는 레버가 아니다

```
conf      presence  top1_rec  oracle_any  FP/img   uncond5cm5   cond5cm5
0.001      1.0000    0.7081    0.8571     5.510      0.3043      0.3043
0.0094*    0.9938    0.7081    0.8012     2.073      0.3043      0.3063
0.40       0.7205    0.5839    0.5901     0.208      0.2981      0.4138
```
Rmax_top1 = **0.7081** → recall>=0.98 은 도달 불가(정의를 확인하기 전 쓰지 말 것).
브리프 규칙(Rmax−0.005 하 FP 최소)으로 tau* = 0.0094.

conf 0.4 → 0.0094 로 44장 회수 + FP/img 10배인데 **unconditional 5cm5 는 48→49/161
(+0.62pp, 1장)**. 회수분 품질: IoU>=0.5 0.455, corner 85.3px, R 10.86°, t **2.16m**
(기존 0.068m 의 31배). → `THRESHOLD_ONLY_NOT_HELPFUL`.

★ 후보 순위 분해: rank1 이미 정답 **114** / rank2~6 (selection 으로 회복 가능) **15** /
정답 후보 없음 **32**. 기존 RERANK_ORACLE 의 top-5 oracle 도 recall +11.8pp 인데
5cm5_any 는 +1.24pp 로 사전 stop_rule(+3.0pp) 미달 → selection 은 실재하나 상한이 낮다.

★★ 진짜 손실: **검출 114 → 5cm5 49, 65장이 pose 에서 죽는다.**
`eval_pallet07` 은 27/27 전부 검출하고도 5cm5 11. threshold·selection 이 못 건드린다.

### ★ PHASE E — dimensions 가 가장 큰 단일 레버

같은 keypoint 에 3D 치수만 갈아끼워 재계산(nominal = `annotate_pnp.PALLET_DIMS`, 사전 고정):
```
조건            R_med    t_med    5cm5 hits   uncond5cm5
exact label     4.288°   0.1257m     49        0.3043
nominal 고정    5.311°   0.2049m     31        0.1925   -11.18pp
nominal ±2%     5.311°   0.19~0.23m  21~23     0.13~0.14
nominal ±5%     5.311°   0.19~0.28m   4~5      0.025~0.031   -27.95pp
```
치수 오차는 R 을 거의 안 건드리고 **t 로만** 간다 → 5cm 게이트만 무너진다.
→ `KNOWN_SIZE_ASSUMPTION_REQUIRED`. 현재 0.3043 은 치수를 아는 덕이다.

★ 라벨 치수가 두 종뿐이고 서로 **W↔D 스왑**: (1.1,0.11,1.3) 89장 / (1.3,0.11,1.1) 72장.
고정 nominal 을 쓰면 한 무리는 18% 틀린 3D 모델로 PnP 를 푼다(±5% 섭동과 다른 이산 오류).

### ★ PHASE F — 180도가 아니라 90도다 (내 1차 서술 정정)

```
best_perm        n    identity_R 분포
near_far_swap   11    60.6 60.8 60.9 62.3 66.6 87.0 93.5 94.4 95.6 97.0 106.6
identity        11    10.8 11.2 11.2 12.6 14.2 17.1 18.2 18.4 57.0 78.3 80.4
top_bottom       2
```
`near_far_swap` 이 이긴 11장은 **전부 60~107° 구간, 180° 부근 0건**. permutation 이름이
앞뒤 혼동처럼 읽히지만 실제 크기는 **90° yaw** 를 가리킨다. 팔레트 aspect 1.3/1.1 =
**1.182** 라 긴변/짧은변 단서가 약하고, 이는 PHASE E 의 W↔D 라벨 스왑과 **같은 축**이다.

저앙각 가설은 이 표본에서 기각: best_perm × elev 교차가 near_far 6/5, identity 6/5 로
갈리지 않는다. → `SYMMETRY_AMBIGUITY_PRESENT`(90° yaw), `TRUE_NEAR_FAR_ROLE_CONFUSION`
은 **채택 안 함**.

### PHASE G / 최종

V2 렌더 이미지 0장 → `V2_NOT_RENDERED` → `V2_NOT_READY`.

```
최종 verdict
  KEYPOINT_LOCALIZATION_BOTTLENECK   지배적 (검출 114 중 65장 pose 실패)
  KNOWN_SIZE_ASSUMPTION_REQUIRED     최대 단일 레버 (-11.18pp / ±5% -27.95pp)
  CANDIDATE_SELECTION_BOTTLENECK     상한 9.3% / 5cm5 +1.24pp
  SYMMETRY_AMBIGUITY_PRESENT         gross 24 중 11, 전부 90° yaw
  V2_NOT_READY
미채택: CONFIDENCE_THRESHOLD_BOTTLENECK, TRUE_NEAR_FAR_ROLE_CONFUSION
GO/NO-GO = **NO-GO** (V2 렌더 대상 부재 + 치수 배포계약 미정)
```
(3)과 (4)는 별개 축이 아니라 **긴변 1.3 ↔ 짧은변 1.1 을 못 가른다**는 한 문제가 치수와
회전에 각각 드러난 것이다.

산출: `audit_20260821T1449/` — FAILURE_LOCUS_VERDICT.md, EVAL_CONTRACT_AUDIT.json,
CONF_SWEEP_{CURVES.json,FRAMEWISE.csv}, DIMS_{SENSITIVITY.json,FRAMEWISE.csv},
NEAR_FAR_AUDIT.csv, SYMMETRY_METRIC_COMPARE.json, V2_READINESS.json,
FAILURE_LOCUS_SUMMARY.json (10개)

## [17:26] Object-Frame Contract Audit — CASE C, GT 축 누수 확인 → V2 학습 BLOCK

새 학습 0 · 새 inference trick 0 · 재추론 0 · 기존 파일 수정/삭제 0 · commit 0.

### 새 디렉터리 (기존 timestamp audit convention 따름)

`challenge/yolo_pose_one_model/audit_20260821T1716/`
- `PURPOSE.md` / `SOURCE_INVENTORY.md`(23개 정본 파일 sha256 + STALE 판정)
- `axis_replay.py` / `AXIS_CONTRACT_REPLAY.{csv,json}`
- `AXIS_DATAFLOW.md` / `AXIS_CONTRACT_VERDICT.md`
- `V2_READINESS_VERDICT.md` / `DATA_GENERATOR_HANDOFF.md` / `FINAL_FLAGS.json`
- `plots/AXIS_CONTRACT.png`

★ 타 세션 산출물 `audit_20260821T1449/`(mtime 14:51~14:55)를 정본으로 채택.
  내가 만든 게 아니며 브리프의 새 수치(1.1x1.3 W<->D, 5cm5 48/161 등)의 출처다.

### ★ PHASE B — evaluator 가 GT 로부터 프레임별 축 배정을 받고 있다 [확인]

코드 dataflow 추적:
`mc_geom.gt_of()` -> `label["objects"][0]["dimensions_m"]` ->
`annotate_pnp.make_pallet_keypoints_3d_diagram(width, depth, height)`.
그 함수는 0~3 을 `-d/2`(near = 카메라에 가까운 면)로 **정의**한다.
`convert_to_camera_facing_v4.py:42` 는 pose_transform 을 보존하고 2D 순서만 바꾼다.

라벨 치수는 **(1.1,1.3) 89장 / (1.3,1.1) 72장** 두 종뿐이고 W<->D 스왑이다.

결정적 실측:
- **7 세션 중 6 세션에서 두 변종이 섞인다**
- 타임스탬프 정렬 시 **0.37초 간격에 변종이 뒤집힌다** (총 27회, 2초 이내 8회)
  -> 같은 물리 팔레트이므로 물리적 차이로 설명 불가
- object depth 축 vs 카메라 시선 각이 **최대 75.2도**, 83%가 45도 이내
  -> 세계 고정 좌표계라면 0~180도 전범위여야 한다

즉 `dimensions_m` 은 물리 속성이 아니라 **시점 의존 축 배정**이다.

```
S1 고정 object frame          FAIL
S2 GT 없이 W/D/axis 획득       FAIL
S3 GT-only permutation 불필요  PASS (index 순서만, 실질 의미 없음)
S4 동일 object frame 기준 R,t  FAIL
-> OBJECT_FRAME_CONTRACT_STRUCTURAL_PASS = False, REPRESENTATION_BLOCK = True
```

### E0 재현 게이트 = PASS (정본과 12자리 일치)

n_solved 160 / R_median 4.28806170406 / t_median 0.12568245313 / s5_hits 49
E1 nominal R_median 5.31055618417 / s5_hits 31 — 전부 정본과 동일.

### 조건별 결과 (tau*=0.0094 native top-1, rerank·Hough 없음)

```
population        cond    n    5cm5  hits   R med   t med  grossR
──────────────────────────────────────────────────────────────────
ALL_161             E0  161   0.304    49    4.29  0.1257   0.298
                    E1  161   0.193    31    5.31  0.2049   0.286
                   E1b  161   0.267    43    3.81  0.1831   0.267
                    E2  161   0.298    48    3.25  0.1609   0.248
GROSS_R24           E0   24   0.000     0   60.68  0.6983   1.000
                    E2   24   0.000     0   49.99  0.7349   0.875
NEARFAR_IMPROVED_11 E0   11   0.000     0   87.01  1.0764   1.000
                    E2   11   0.000     0   86.28  1.1744   1.000
B_CORRECT_BOX_BAD_KP_59 E0 59 0.000     0    9.66  0.4285   0.492
                    E2   59   0.000     0    6.70  0.4387   0.390
```

E0=현재(GT dims) / E1=배포가능 고정 nominal / E1b=재투영선택 탐침(GT 미사용) /
E2=ORACLE(NOT DEPLOYABLE).

session-cluster paired bootstrap B=10,000:
```
E1-E0 5cm5    -0.1118  CI95 [-0.3033, -0.0212]  0 배제 True
E1-E0 grossR  -0.0124  CI95 [-0.0738, +0.0392]  0 배제 False
```
사전등록 기준(|delta|>=0.03 AND CI 0 배제) 충족 -> **의미 있는 차이 있음**.

### 판정 = CASE C (GT_DEPENDENT_AXIS_LEAK_PRESENT)

동시에 CASE E(GENUINE_KP_FAILURE_DOMINANT)도 성립한다 — 축을 oracle 로 고쳐도
GROSS_R24 는 gross 0.875 / 5cm5 0, NEARFAR_11 은 어느 조건에서도 R med ~86도,
B59 는 E2 에서도 5cm5 0/59. **축 계약은 누수 문제이지 gross 실패의 원인이 아니다.**
-> `TRUE_NEAR_FAR_ROLE_CONFUSION = NOT_ESTABLISHED` 재확인.

CASE D 아님 — E2(oracle) 0.298 이 E0 0.304 와 사실상 같고 E1b 가 GT 없이 0.267 을
회수한다. 표현이 정보를 잃은 게 아니다. (E1b 의 올바른 배정 적중률은 0.588 로
동전던지기보다 조금 나은 수준 — "맞혀서"가 아니라 재투영이 일관된 해를 고르기
때문일 가능성 [추정]. **방법 제안 아님.**)

E3: real pallet 의 mesh/도면이 저장소에 없어 유효 symmetry 를 source 로 확인 불가
-> `NO_VERIFIED_NONIDENTITY_SYMMETRY`. 1.3x1.1 직사각형이므로 90도는 symmetry 아님.

### PHASE C — 실행 안 함 (상류 BLOCK) + V2 는 존재하지 않음 [확인]

이름 가정 없이 전수 탐색: `broad_family_v2/` 는 이미지 0 / 라벨 0, 내용은
md 12 / json 6 / py 5 / csv 4 뿐. 저장소의 다른 `*v2*` 는 전부 DOPE 시절 무관 자산.
타 세션 `V2_READINESS.json`(rendered_images=0)과 일치하나 **직접 세어 확인**했다.

-> `V2_DATA_FOUND=False`, `V2_READY_FOR_SCREEN=False`,
   PHASE D/E `NOT_RUN`. `DATA_GENERATOR_HANDOFF.md` 작성(데이터 생성 0).
   ★ 인수인계 문서 맨 앞에 **label contract 결정이 선행 조건**임을 명시 —
   현행 계약으로 렌더하면 시점 의존 축 배정을 40,000장 더 만드는 것이다.

### NEXT — 사용자 판단 필요

(가) 주장 제한: camera-facing / visible-face-aligned pose 로 명시 (현재 수치 유효)
(나) 표현 변경: 물리 고정 frame 으로 라벨 재정의 + 재학습 (90도 yaw 를 모델이 품)
(다) 계약 명시: known-size + known-orientation-class 를 가정으로 선언
자동 대량 relabel 하지 않았다.

## [19:26] Complementarity audit PHASE 0/A/B — 두 branch 는 상보적이지 않다

새 학습 0 · 새 architecture 0 · 기존 파일 수정 0 · commit 0.
산출: `data/pallet/results/paper_s2_multihead/complementarity/`
신규 러너: `scripts/stage0/multihead/mh_complementarity.py`(+`_boot.py`)

### PHASE 0 — 계약 확정

코드에서 읽음: IMAGE 400 / GRID 50 / **CORNER_SIGMA 2.0 cells**(메모리의 4.0 은
구 설정) / 1 cell = 8 px / FIRST_TRAINABLE_VGG 19 / AdamW lr1e-3 wd1e-4 batch8.
arm 3종 sha256 기록. 무결성 경고 3건:
`STEP_BUDGET_MISMATCH_E4`(E4 는 3k continuation → 25k 비교에서 제외),
`SPLITLATE_3K_SEED2_CHECKPOINT_MISSING`, `POSE_METRIC_CAVEAT`(축 계약).

### PHASE A — GATE PASS. Q1 답: SPLIT_LATE 는 corner 에서만 낫다

D2_MH_DEV512 n=512, paired, 10k bootstrap, seed 독립. delta = split − shared.

```
seed  metric        shared    split    delta    rel%    CI low   CI high  0배제
──────────────────────────────────────────────────────────────────────────────
1     corner_rms    0.9006   0.7129  -0.1877  -20.84   -0.2358  -0.1174   True
1     line_angle    2.2788   2.2093  -0.0695   -3.05   -0.2607   0.1377  False
1     pose_t        0.2244   0.1825  -0.0419  -18.67   -0.0808  -0.0079   True
2     corner_rms    0.8827   0.7243  -0.1584  -17.94   -0.2039  -0.1140   True
2     line_angle    2.3866   2.3186  -0.0680   -2.85   -0.2839   0.1303  False
2     pose_t        0.2397   0.1941  -0.0456  -19.01   -0.0775  -0.0092   True
5cm5deg  seed1 0.0781->0.1465   seed2 0.0938->0.1367
```

pose_R 은 −7.6%/−6.6% 이지만 CI 가 0 을 포함. line 은 네 경우 모두 CI 0 포함.

**게이트가 내 오류 2개를 잡았다**: (1) predcache 는 canonical(θ=radian)인데
`DH.measure` 는 centred MAP100(degree) 기대 → 2.30° 가 0.0409 로 읽혔다.
`DH.centred_from_canonical` 필수. (2) `mh_screen_meta_long25k_*.json` 에는
`A2_CORNER_LINE_MASK` 만 있다 — A1 정본은 `mh_screen_A1_CORNER_LINE_*`.
수정 후 4/4 가 소수점 6자리 일치 → `METRIC_REPRODUCTION = PASS`.

### PHASE B — causal perturbation. 판정 = 상보성 없음

r = 2×sigma = 4 cells = 32 px 하나에서 IC/IE/IR 전부 유도. 연산자 = 마스크 내부
Gaussian blur(sigma 8px, kernel 33) + 4px feather. IR 은 프레임별 면적정합
(IR/IC 0.997, IR/IE 0.998). IC∩IE soft 겹침 1.96%(feather 잔재, 기록).
내부 정합: 무섭동 I0 corner 가 PHASE A 의 SPLIT_LATE 값과 정확히 일치.

```
delta(median)  I0      IC       IE      IR0      IR1      IR2      IR3
──────────────────────────────────────────────────────────────────────
s1 corner    0.7129  +6.2647  +0.0042  +0.0003  +0.0000  +0.0004  +0.0000
s1 angle     2.2093  +3.1595  +0.0000  +0.0000  +0.0000  +0.0000  +0.0000
s2 corner    0.7243  +6.1558  +0.0103  +0.0006  +0.0000  +0.0004  +0.0000
s2 angle     2.3186  +3.7136  +0.0000  +0.0000  +0.0000  +0.0000  +0.0000

S_corner       +4.9895 / +4.2088   rel +700% / +581%   CI 0배제 True
S_line_angle   -2.6667 / -3.4776   음수                 False
S_line_offset  -1.8348 / -1.6013   음수                 False

CORNER_SPECIALIZATION            = True
LINE_SPECIALIZATION              = False
RANDOM_CONTROL                   = False
COMPLEMENTARY_EVIDENCE_SUPPORTED = False
```

**line 도 corner 근방에 지배된다** — edge interior 보다 corner 를 가릴 때 line 이
더 망가진다. 기존 메모리 `attention-is-not-the-line-bottleneck`,
`five-class-line-cannot-identify-corner-12edge-can` 과 같은 방향.

★ **검산**: line 출력은 격자 argmax 라 median delta 가 0 으로 눌린다(29% 프레임이
정확히 0). median 만 보고 "line 은 edge 를 안 본다" 고 읽으면 **틀린다**.
민감 통계로는 IE 가 angle 을 70.7%/71.1% 프레임에서 바꾸고 mean|Δ| 2.07/1.79 인
반면 면적정합 random 은 43.6%/43.0%, 0.68/0.58 이다. **line 은 edge 를 읽되
corner 보다 훨씬 약하게 읽는다.** 사전등록 gate 는 median 을 썼으므로 판정은
그대로 두고, 이 퇴화를 산출물에 명시했다.

★ 설계 비대칭: IE 는 edge 끝점을 남기고 IC 는 12 edge 전부의 끝점을 파괴한다.
S_line 이 음수인 데는 이 비대칭도 일부 기여한다 [추정]. 정의는 결과를 보고
바꾸지 않았다.

정성 시트 4장 + selection.json (목표 32 였으나 case 중복 제거로 **27** 프레임 —
숫자 부풀리지 않고 기록).

### NEXT — 사용자 승인 대기

gate 미통과이므로 **attention-diversity loss / orthogonality loss / 새 router 를
만들지 않는다.** C1/C2 training 도 시작하지 않았다.

## [19:53] Worker ZIP 패키징 PHASE S0/S1 — fixed-object 복원 source 확인, 3건 블로커 보고

SOURCE PC 역할. 저장소 읽기만 — 파일 수정·삭제·commit 0. ZIP 생성 아직 0.

### transfer root (repository 밖, 브리프대로)

`/home/minjae/pallet_worker_transfer_20260821T105141Z/` — staging/outgoing_common/outgoing_fixed/scripts/manifests/logs
산출: `manifests/SOURCE_INVENTORY.md`, `manifests/SOURCE_PATHS.json`

### S0 실측

- 5ep baseline: `runs_broad40k/b_yolo26n_broad40k_5ep` (args/results/best/last 실재)
- 60ep: `runs_paper/yolo26n_paper_generic_v1_seed42` (+ epoch{0,10,20,30,40,50}.pt — 전송 제외)
- ★ `paper_generic_v1/data.yaml` 의 path 가 **broad40k 를 가리킨다** — 동일 데이터셋
- dataset 안에 **symlink 없음** (전부 실제 파일)
- train 39,500 · val 500 · padded 840x680 · 36G
- 원본 source: `v2_prod40k_clean_merged/{rgb,labels}` 40,000 · 720x480 · 20G + 1.2G

### ★ fixed-object 복원 source 는 존재한다

원본 label 이 보유: `cuboid`(world 8x3), `pose_transform`(object→camera),
**`perm_v4`**(camera-facing 순열), `source_asset`, `efront_kp12.front_face_axis`,
`anchor_translation`, `pallet_shape_ratios`.

perm_v4 는 실측 **정확히 4종**이고 front_axis 와 정합한다:
```
(0,1,2,3,4,5,6,7) n=46 W  W/D 0.788     (5,4,7,6,1,0,3,2) n=49 W  W/D 0.799
(4,0,3,7,5,1,2,6) n=52 D  W/D 1.186     (1,5,6,2,0,4,7,3) n=53 D  W/D 1.197
```
object frame 원점 확정: cuboid centroid.z − anchor.z − h/2 의 max|.| = 3.1e-08
→ **바닥 중심, Z-up**.

### ★ 진행 전 사용자 결정 필요 3건

1. **브리프 S11 게이트 "same asset dimensions invariant" 는 성립 불가.**
   이 데이터셋은 프레임마다 팔레트를 의도적으로 찌그러뜨린다(pallet_shape_ratios).
   asset 하나당 고유 dims 종수 = 프레임 수(110프레임→110종). 라벨 오류가 아니라
   설계된 DR. 대체 불변량: "perm_v4 역적용 후 object-space 부호 패턴이 asset 내 동일".
2. **`make_pallet_keypoints_3d_diagram` + pose_transform 이 synthetic 재투영을
   재현하지 못한다** — median 106.0px, 1px 이내 0/200. 원인은 좌표계 불일치:
   synthetic 은 바닥원점·Z-up, 그 함수는 중심원점·Y-down·Z=depth.
   **fixed 라벨 생성기는 real 용 함수를 재사용할 수 없다.**
3. **전송량 선택**: (가) padded 36G/11shard, baseline 입력과 byte 동일 /
   (나) 원본 20G/6shard + worker 재생성. 기본값은 (가).

### 미실행

S2~S9(builder·ZIP·전송), S10 정식 smoke, S11/S12.
`FIXED_OBJECT_RELABEL_FEASIBLE = PROBABLE_NOT_YET_PROVEN` —
필요한 정보는 전부 있으나 좌표계 변환기를 새로 써야 한다(발견 2).

## [20:15] Worker 패키징 — 02 RGB shard 11개 + 10 fixed labels 완성, ★거울상 36장 발견

승인 반영: REVISED_INVARIANTS_ACCEPTED / FIXED_LABEL_METHOD=INVERSE_PERM_V4_REINDEX /
RGB_TRANSFER=PADDED_36G. 저장소 파일 수정 0, commit 0.
transfer root: `/home/minjae/pallet_worker_transfer_20260821T105141Z`

### 02_train_rgb shard (D3)

11 shard · logical 40,000 · **35.38 GiB** · ZIP_STORED · 2분22초.
전 shard **CRC OK + count OK**. membership sha256 `412e73a1e789347c`.
각 shard 안 \_BUNDLE/{BUNDLE_INFO.json,FILELIST.tsv,FILES.sha256} + 외부 .sha256 sidecar.

### ★ perm_v4 전수 census (40,000) — 4종이 아니라 **8종**

앞선 "정확히 4종" 은 200프레임 표본의 한계였다. 정정한다.
```
(5,4,7,6,1,0,3,2) x10328   (0,1,2,3,4,5,6,7) x10308
(1,5,6,2,0,4,7,3) x 9802   (4,0,3,7,5,1,2,6) x 9526   <- 지배 4종, 전부 det=+1 회전
(0,4,7,3,1,5,6,2) x   12   (4,5,6,7,0,1,2,3) x   10
(1,0,3,2,5,4,7,6) x    9   (5,1,2,6,4,0,3,7) x    5   <- ★희귀 4종, 전부 det=-1 **반사**
```
**반사는 강체 pose 가 아니므로 물리적으로 불가능한 거울상 라벨**이다(총 36장, 0.09%).
I9(변길이)로는 안 잡히고 행렬식 검사가 잡았다. membership 은 유지하고 목록만 남겼다
(baseline parity 보존) — `FIXED_OBJECT_DATA_AUDIT.json` 의 `★mirrored_frames.stems`.

### fixed label 전량 생성 + **전수 40,000** 검증 (표본 아님)

```
I1 perm bijection 위반   0     I2 round-trip 실패   0
I3 point set 불일치      0     I4 bbox/visible 불일치 0
I5 centroid[8] 변경      0     I6 visibility 왕복    0
I7 perm class 8종 (반사 4종)   I10 membership 변화   0
```
I9: un-permute 가 asset canonical 축을 복원 — **변동계수 0.0000**, 4 asset 전부.
실측 기준치수: eur_pallet [0.8469,0.1523,1.2687] / woodpallet [1.0018,0.1837,1.1782] /
scene [0.9919,0.1488,1.1903] / scene_1 [0.9909,0.1363,1.1922].

★ perm 방향(A: fixed[perm[i]]=cf[i] vs B: fixed[i]=cf[perm[i]])은 I9 로 구분되지
않는다 — 둘 다 canonical 축을 복원하며 박스 대칭만큼만 다르다. 승인된 A 를 쓴다.

### 10_fixed_trainval_labels ZIP

40,008 항목 · 14.1 MiB · CRC OK · sha256 `6191e03a9d39caf6`.
parent_common_bundle_id `common_20260821T110839Z_152012b`,
train_rgb_membership_sha256 `412e73a1e789347c` 박음. **RGB 미포함**.
worker 상대경로 data.yaml + FIXED_OBJECT_DATA_CONTRACT.md 포함.

### 미실행

00/01/03/04 ZIP, BUNDLE_INDEX, 전송 script, **REAL GT QA(R0~R12) 전체**, 11 ZIP.

## [22:35] RARE36 = KEEP (이전 오판 정정) · common bundle 완성 · GT QA C1

### ★ RARE36 정정 — 내 이전 판정이 틀렸다

이전에 36장을 "reflection corruption / 물리적으로 불가능한 거울상 라벨" 이라고 했는데
**근거로 쓴 것은 index permutation 의 parity 였고 그것은 physical mirror 가 아니다**
(사용자 정정이 옳다). 실제 3D 좌표로 다시 쟀다:

```
                     복원(inverse-perm) chirality   camera-facing chirality
dominant400          +1  x 400/400                  +1  x 400/400
rare36               +1  x  36/36                   -1  x  36/36
X4 shape-ratio 정합   최대 상대오차 0.00012          (dominant 와 동일)
```

즉 odd permutation 은 손상이 아니라 **손잡이가 뒤집힌 camera-facing 라벨을 바로잡는
순열**이다. inverse-perm 후 40,000 전부 chirality +1.
→ **RARE36 = KEEP**, membership 40,000 유지, 10 ZIP 그대로 유효.

부수 실측: `efront_kp12` 3D↔2D 로 world→camera 복원(fit residual median 0.000px) 후
`cuboid` 를 투영하면 identity 0.0000px 일치 → **저장된 cuboid 는 camera-facing 순서**,
pre-v4 순서는 저장돼 있지 않다(.orig/.presentinel/.bak/prev4 전부 0/36).
perm 방향 근거는 round-trip 이 아니라 `convert_to_camera_facing_v4.py:94` docstring
"perm[new_idx] = old_idx", origin = obj['cuboid'][:8].
폐기: 인접구조 테스트는 무효(face diagonal 0.863 < 최장 변 1.27 이라 항상 섞임).

### COMMON BUNDLE 완성 — 15 bundle / 35.49 GiB

```
00_code_env          26 files   0.2 MiB  5d04779db9de
01_weights_refs      11 files  20.0 MiB  b9ea7612085a
02_train_rgb x11  40,000 files 35.38 GiB  (기존, 재생성 안 함)
03_cf_labels     40,003 files  13.4 MiB  ac5c0343fd8f
04_realdev161       165 files  75.8 MiB  dacc533c8c45
BUNDLE_INDEX_COMMON + .sha256 + SEND_ORDER_COMMON.txt
```
전 archive CRC OK + count OK. counts 검증: TRAIN_RGB_TOTAL 40,000 = TRAIN 39,500 +
VAL 500, train∩val 0, REAL_DEV 161, B59 59.
★ 02 가 train+val 을 **둘 다** 담으므로 03 은 val RGB 를 중복 전송하지 않는다 —
index 와 WORKER_README 에 명시.
recipe 실측: yolo26n-pose.pt / batch 32 / imgsz 640 / SGD lr0 0.01 / seed 42 /
patience 0 / workers 4 / fliplr 0.0 / last.pt 선택. 5ep·60ep 가 epochs 와
save_period 만 다르다.

### REAL GT QA — C1 sentinel 교집합 실측 (기억값 미사용)

```
SENTINEL_HISTORICAL_N (저장소 .presentinel 백업)      16
SENTINEL_IN_REAL161_N (백업 보유)                      0   <- 과거 복구가 real161 을 안 건드림
SENTINEL_CURRENTLY_BROKEN_N                          14 프레임 / 19 코너
SENTINEL_ALREADY_REPAIRED_N                           0
session별: cad 4/7 · night08 4/4 · night09 5/7 · pallet09 1/1 · 나머지 0
```
★ 기억의 "72프레임/153코너" 는 real161 에 해당하지 않는다. 실측은 **14/19**.
provenance 양호: 161/161 이 `manual_kps` 보유, `gt_source=manual`,
`reproj_error_px` median 1.317px → CLICK_PROVENANCE = AVAILABLE.

QA 디렉터리: `/home/minjae/pallet_worker_transfer_20260821T105141Z/REAL_GT_QA_20260821T133405Z`

### 미실행
C2 risk scoring · C3 contact sheets · C4 review queue · R7~R12 · 11 ZIP · 전송.
