# 2026-09-03

## 코드 변경 없음 — 세션이 자정을 넘겼다

이 세션에서 편집한 파일(V1~V4 트랙의 스크립트·lock·테스트 39 개)은 전부 **09-02 에**
바뀌었고 `_docs/history/2026-09-02.md` 에 파일명·Before/After·이유로 기록돼 있다.
마지막 커밋도 `0c36084` (09-02 22:43) 다.

확인: `find ... -newermt "2026-09-03 00:00"` 결과 없음.  오늘 한 작업은 **읽기 전용
측정** 뿐이다.

## R0 의 절대 localisation 수준을 처음 쟀다

사용자 질문: "localisation 이 안 되는 게 self-training 때문인가?"

그동안 보고한 것은 전부 **R0 대비 Δ** 였고 R0 자체가 어느 수준인지는 한 번도 정면으로
본 적이 없다.  그래서 전제를 확인하려고 쟀다 (PAPER_EVAL, 검출된 프레임의 supervised
keypoint).

```text
group        n_kp  median px   p90 px   NME med   >20px   >40px
ALL          2756       6.62    38.67    0.0208   17.2%    9.6%
daytime       609      10.56    51.54    0.0261   30.2%   14.8%
nighttime     378       7.69    23.45    0.0262   11.9%    5.8%
none         1769       5.41    28.23    0.0187   13.8%    8.6%

cuboid diagonal 중앙값 313 px
```

**self-training 을 하기 전부터 keypoint 6 개 중 1 개가 20 px 초과, 10 개 중 1 개가
40 px 초과다.**  주간은 gross 가 30% 다.  중앙값 6.62 px 은 괜찮아 보이지만 p90 이
38.67 px 로 **5.8 배** 벌어진 이봉 분포다.

## self-training 이 움직인 폭은 그 위에서 ±3~6%

```text
R0 baseline NME               0.0208
R0 자체 산포 (p90/median)      5.8배
V1 Proposed Δ                +0.0011   (+5.3%)
V3-B Δ                       +0.00067  (+3.2%)
V3-B night Δ                 +0.00168  (+6.4%)
```

즉 self-training 은 localisation 을 부순 것도 고친 것도 아니고 **거의 건드리지
못했다.**

## 다만 야간 꼬리는 실제로 나빠졌다 — 뭉개지 않는다

공통 검출 프레임에서 절대 gross 판정 (metric_split_lock §2.2 의 20/40 px):

```text
group        n_kp  R0 >20px  V3B >20px  R0 >40px  V3B >40px
ALL          2747     17.0%      19.3%      9.4%       9.6%
daytime       609     30.2%      30.5%     14.8%      13.8%
nighttime     378     11.9%      22.2%      5.8%       9.0%
```

야간 gross 가 11.9% -> 22.2% 로 거의 두 배다.  중앙값(NME)으로는 +6% 로 작아 보였는데
꼬리에서는 크다.  같은 야간에서 검출이 0.840 -> 0.960 으로 올랐으므로 **새로 잡은
어려운 프레임이 gross 를 끌고 들어온 몫**과 **실제 열화**가 섞여 있다 (V1 진단의
selection effect 와 같은 구조).

## 원인의 층위

```text
1 근본   R0 가 이미 gross 17% · 주간 30%.  합성->실물 전이 자체의 한계로,
         self-training 이 손대기 전의 문제다.
2 부차   self-training 은 그 위에서 ±3~6% 만 움직인다.  V1 은 나쁘게, V2/V3 는
         거의 중립으로.  네 트랙의 방법 차이는 전부 이 폭 안에서 논다.
3 국소   야간 꼬리만 실제로 악화(11.9% -> 22.2%)했고 그중 일부는 새 검출 탓이다.
```

**"self-training 으로 localisation 을 개선한다" 는 목표 자체가 잘못 겨냥됐을 수
있다.**  teacher 가 이미 6 개 중 1 개를 크게 틀리는데 그 예측을 학생에게 먹여 학생이
teacher 를 넘기를 기대하는 구조다.  V4 가 그 벽에 정면으로 부딪혔다 — 고칠 만한
"애매한" 코너는 1.2% 뿐이고, 나머지 gross 는 teacher 가 **높은 확신으로 틀린** 것이다.

> **정정 (같은 날 오후, `FILTER_SEPARABILITY.md`)**: 원래 여기 "어떤 신뢰도 필터로도
> 안 걸린다" 고 썼는데 **과했다**.  임계를 떼고 AUC 로 재니 프레임 단위 조합이
> 0.812 다 — 무작위가 아니다.  정확히는 "현재의 **임계 AND 방식**으로는 거의 못
> 거른다(47.4% -> 40.8%)" 이고, 결합하면 절반까지 줄일 수 있다.  아래 절 참조.

이 측정은 앞서 제시한 세 선택지 중 `PAPER_CLAIM_PIVOT` 의 근거가 된다.
다음 방향은 사용자 결정 대기.

## 필터 분리력을 처음으로 AUC 로 쟀다

지금까지 신호를 **임계로만** 썼다 (`s_* <= 0.05`).  그래서 "안 걸린다" 는 결론이
방식의 한계인지 신호의 한계인지 구분되지 않았다.  임계를 떼고 쟀다.

새 스크립트 `scripts/self_training_yolo/v4/measure_filter_separability.py`
(즉석 계산으로 끝내면 재현이 안 되므로 스크립트로 남겼다).
산출: `data/pallet/results/paper_selftrain_v4/FILTER_SEPARABILITY.json`,
`_docs/paper/FILTER_SEPARABILITY.md`.

### 프레임 단위 — 이 프레임이 gross 코너를 갖는가

```
signal              AUC          모집단 194 프레임, gross 보유 47.4%
s_reproj          0.741
s_flip            0.735
s_remove          0.726
box_conf          0.725
valid_corners     0.520          무의미
4개 조합 (CV)      0.812
```

```
유지율   남은 n   gross    제거 gross   버린 clean
 90%      174    42.5%    18/92         2/102
 80%      155    38.1%    33/92         6/102
 70%      135    32.6%    48/92        11/102
 60%      116    25.9%    62/92        16/102
 50%       97    22.7%    70/92        27/102

현재 Proposed 필터: 142 유지(73%), gross 40.8%
```

### Keypoint 단위 — 이 코너가 gross 인가

```
signal                  AUC      모집단 1,979 코너, gross 15.2%
r_flip                0.657
r_remove              0.632
kp_conf               0.602
corner_only           0.679
frame_only            0.713      ★ 코너 신호보다 프레임 신호가 낫다
corner_plus_frame     0.726
```

```
유지율   남은 n   gross   제거 gross    버린 clean
 90%     1781   13.4%    61/300      137/1679
 80%     1583   11.1%   125/300      271/1679
 70%     1385    8.8%   178/300      416/1679
 60%     1187    6.8%   219/300      573/1679

현재 V3 규칙(r_remove<=.05 AND r_flip<=.05 AND conf>=.5):
  1835 유지(93%), gross 12.4%
```

### 새로 확인한 사실 두 가지

**1. `kp_conf >= 0.5` 는 코너 단위에서 아무것도 안 거른다.**  supervised 코너
1,979 개 중 floor 미만이 **0 개**다.  V2/V3 의 confidence floor 는 코너 단위에서
완전히 무효였다.  frame 단위 `box_conf >= 0.85` 만 실제로 걸렀다.

**2. 프레임 단위 분리력이 코너 단위보다 높다** (0.812 대 0.726).  직관과 반대인데,
코너 하나가 틀리는 것이 대부분 **프레임 전체가 틀린 것의 일부**이기 때문으로 보인다
`[추정]` — 앞서 본 최악 2 장이 gross 8/8 이었던 것과 일관된다.

### 함의

현재 V3 규칙(93% 유지, gross 12.4%)은 결합 곡선의 85~90% 지점과 거의 같다.  즉
**임계 AND 가 정보를 크게 버리고 있던 게 아니라 신호 자체가 약하다.**  더 좋아지려면
코너를 30~40% 버려야 하고(gross 15.2% -> 6.8%), 그 대가로 멀쩡한 코너 573 개를
같이 버린다.  gross 를 0 으로 만들 수는 없다.

### 쓰면 안 되는 방식

이 수치는 PAPER_EVAL 에서 나왔다.  **이걸로 결합 가중치를 고정하면 개발셋을 다섯
번째 소비하는 것이다.**  측정은 측정으로만 쓴다.  학습된 per-keypoint reliability 를
실제로 도입하려면 가중치를 배울 별도 데이터가 필요하다 — 그건 앞서의 세 선택지와
별개인 **네 번째 선택지**이고, 역시 새 데이터 문제가 걸린다.

문서·JSON·스크립트에 이 경고를 명시했다.

## paper_selftrain_v5 — reliability weighting.  CASE B 로 실패

FILTER_SEPARABILITY 가 "임계로 자르면 멀쩡한 라벨을 수백 개 버린다" 를 보였으므로,
V5 는 **자르지 않고 덜 반복 노출**하는 쪽을 한 번만 검증했다.

V1~V4 + FILTER_SEPARABILITY 를 105 항목으로 봉인
(`V1_V2_V3_V4_IMMUTABILITY_LOCK.json`, V3-B pseudo 라벨 273 개 집합해시 포함).

### 점수 — 학습된 것이 아니다

`scripts/self_training_yolo/v5/reliability_score.py`.  FIXED · UNSUPERVISED ·
MONOTONIC · RANK-FUSION.

```
u = (r - 0.5) / N            Day/Night 안에서 mid-rank, 동점은 average
R_corner_i   = (q_kp * q_remove * q_flip)^(1/3)
R_kp_frame   = geometric mean over usable corners  (centroid 제외)
R_frame_geom = (q_box * q_reproj * q_remove * q_flip)^(1/4)
R_total      = sqrt(R_frame_geom * R_kp_frame)
```

가중치 전부 동일.  0.5/0.5 는 PAPER_EVAL 최적화값이 아니라 두 정보원을 동등하게 묶는
구조적 정의다.  Day/Night 를 따로 정규화해 절대값을 곱하지 않는다.

신호 복사본이 아님을 확인 — 성분과의 최대 Spearman 0.787, 값 분포 0.03~0.77.

unit test 18 개로 못 박았다: quality 가 (0,1) 열린구간이라 기하평균이 0 으로 무너지지
않는 것, 결측이 **가장 나쁜 쪽**으로 가는 것, 단조 변환 불변, 배분 결정론성,
`numpy` 외 import 없음, GT 성 인자 없음(AST).

중간 사고: AST 검사가 docstring 의 안전 문구("PAPER_EVAL 최적화값이 **아니다**")를
위반으로 셌다.  docstring 을 제외하고, 안전 문구가 실제로 있는지도 따로 검사하도록
바꿨다 (검사가 공허해지지 않게).

### 노출 — V3-B 계약을 그대로 유지

```
item                   V3-B                V5     동일
entries                2880              2880      O
pseudo                  720               720      O
synthetic              2160              2160      O
unique                  273               273      O
Day slots               327               327      O
Night slots             393               393      O
label set hash    c9f303e5b0903b4f  c9f303e5b0903b4f  O
exposure hist    {2:99, 3:174}   {1:14, 2:96, 3:138, 4:25}
```

프레임마다 1 회를 먼저 깔고 남은 슬롯만 R_total 비례로 largest-remainder 배분.
무작위 없음.

### 메커니즘 — 학습 전 PASS

```
signal        AUC       n=79, frame gross 51.9%
R_total     0.763       <- 어떤 단일 신호보다 높다
s_reproj    0.745
s_remove    0.694
box_conf    0.653
s_flip      0.583

metric              uniform V3-B   weighted V5     변화
frame_gross               0.5190        0.4615   -0.0575
corner_gross              0.2078        0.1823   -0.0255
median_error_px          20.0479       18.2491   -1.7988
p90_error_px             31.4206       28.7036   -2.7170
```

M1·M2·M3 전부 PASS.  학생이 기대상 보는 라벨이 실제로 깨끗해진다.

### 그런데 학생은 그대로였다

```
metric                    R0      V3-B        V5     V5-R0    V5-V3B
ALL detection          0.975     0.991     0.984    +0.009    -0.006
Day detection          1.000     1.000     0.986    -0.014    -0.014
Night detection        0.840     0.960     0.960    +0.120    +0.000
ALL paired NME        0.0207    0.0219    0.0220   +0.0013   +0.0001
Day paired NME        0.0260    0.0284    0.0289   +0.0029   +0.0005
Night paired NME      0.0262    0.0283    0.0280   +0.0018   -0.0004
Axis all               0.047     0.041     0.044    -0.003    +0.003
Axis q>=.75            0.096     0.084     0.084    -0.012    +0.000
kp_conf p05            0.976     0.982     0.985
```

```
paired NME delta (V5 - R0)
ALL        n=308  Δ +0.00086  [+0.00018, +0.00140]   resolved
daytime    n= 69  Δ +0.00011  [-0.00155, +0.00228]   NOT_STATISTICALLY_RESOLVED
nighttime  n= 42  Δ +0.00174  [+0.00006, +0.00356]   resolved
```

```
PASS G1 Night det 유지 0.960      FAIL G5 V3-B 대비 0.0219 -> 0.0220
PASS G2 Day det 0.986             PASS G6 Night V3-B 대비 0.0283 -> 0.0280
FAIL G3 ALL NME Δ +0.00086        PASS G7 축 0.047 -> 0.044
FAIL G4 Night NME Δ +0.00174      PASS G8 q>=.75 축 0.096 -> 0.084
FAIL G9 (= G3/G4)
```

### 판정 — §27 CASE B

기대 corner gross 를 **12% 낮췄는데** 학생의 localisation 은 **0.5% 도 안 움직였다**.

> pseudo-label purity 를 높여도 student localisation 이 개선되지 않는다.
> single-frame pseudo-label selection 을 더 이상 붙잡지 않는다.

이건 V5 만의 결론이 아니라 다섯 트랙의 수렴점이다.

```
V1  frame-level 필터        localisation 악화
V2  per-keypoint mask       악화 감소, R0 미달, kp_conf 억압
V3  true-ignore             kp_conf 회복, localisation 그대로
V4  geometry repair         복원 대상 1.2%, 메커니즘 단계 종료
V5  reliability weighting   라벨 품질 개선 확인, localisation 그대로
```

**선택 방식을 다섯 번 바꿨고 그중 둘은 대리 지표에서 실제 개선을 만들었다.  그런데
학생의 keypoint 위치는 한 번도 R0 를 넘지 못했다.**

원인은 이미 잰 것으로 설명된다 — R0 자체가 supervised keypoint 의 17.2% 를 20 px
이상 틀린다(주간 30.2%).  선택을 아무리 정교하게 해도 **teacher 가 모르는 것을
만들어내지는 못한다.**

### 상태

```
V5_METHOD_STATUS = FAILED   (mechanism PASS, dev gate FAIL)
single_frame_selection_tracks_exhausted = 5
confirmation_opened = false
IMMUTABILITY = PASS (105/105)
```

다음 필요 행동: NEW_DEV_DATA / PAPER_CLAIM_PIVOT / TEMPORAL_MULTIFRAME / **BETTER_TEACHER**.
네 번째가 이번에 새로 뚜렷해졌다 — 다섯 트랙이 전부 selection 을 건드렸으므로 남은
레버는 teacher 자체다.

## paper_strong_teacher_v1 착수 — 감사까지, 학습은 사용자 결정 대기

V1~V5 가 전부 selection 을 건드려 실패했으므로 이번엔 teacher 를 바꾼다.
V1~V5 산출물 **150 항목** 봉인 (`PRIOR_TRACK_IMMUTABILITY_LOCK.json`).

### §2~§4 감사

금지 teacher 확인 — `release/*-ft`, `*-livegt`, `runs_ft`, `runs_live_gt`,
`weights/*_ft_manual` 은 전부 real label 계열이다.  `pretrained_yolo/yolo26m-pose.pt`
는 COCO pretrained 로 teacher 가 아니라 **init 후보**다.

T0(R0) 확인: `g38_legacy_v1v2_p0_tex20k`, 60ep, batch 32, seed 42.
**train 55,980 장이 전부 synthetic symlink 이고 `raw_data`/`01_real`/`real_data`
경로가 0 건** — §4 의 real frame 0 을 데이터로 확인했다.

기존 source-only medium 은 **사용 불가**:

```
run                            data                  결과
_aborted_m_b16_oom_20260815    configs/stage_a.yaml  중단(OOM)
_aborted_m_actual_b8_20260815  configs/stage_a.yaml  중단
stage_a_m_640_b8_seed42        configs/stage_a.yaml  12 / 60 epoch
```

데이터셋이 R0 와 다르고(stage_a 73,916 vs g38 55,980) 60ep 중 12 에서 멈췄다.
§5 의 membership 동일 요건과 checkpoint contract 를 둘 다 못 채운다.

### T1 비용 실측

중단된 run 의 results.csv 에서 **~1,640 s/epoch**(73,916 장, b8/nbs64).
R0 데이터셋은 0.757 배이므로 **~1,240 s/epoch → 60ep ≈ 21 시간**.
RTX 3080 10 GB 에서 medium 은 b16 OOM 이력이 있고, b8·nbs64 면 R0 의 b32·nbs64 와
effective batch 가 같다.

### 21 시간 쓰기 전에 — consensus 절반을 공짜로 확인

T0 캐시에 original + flip 이 이미 있어 2-view 합의를 잴 수 있다.
**§13 계약이 아니다**(3 개 이상 요구, 2 view 에서는 median 이 평균) — 탐색용이고
gate 판정에 쓰지 않는다.  그 사실을 JSON 의 `exploratory` 필드와 출력에 박았다.

```
set                                n_kp   NME med   NME p90   px p90  gross20   cat40
R0 baseline / ALL                  1979    0.0195    0.0857    29.93    0.152   0.079
consensus (accepted only) / ALL    1284    0.0182    0.0553    22.25    0.115   0.049
consensus (same keypoints) / ALL   1807    0.0189    0.0993    38.41    0.160   0.099
```

**세 번째 줄이 정직한 비교다.**  둘째 줄은 어려운 keypoint 35% 가 빠진 모집단이다
(ambiguity 448 · disagreement 84 제외).  같은 keypoint 에서 좌표만 바꾸면:

```
NME median  0.0195 -> 0.0189   미미
NME p90     0.0857 -> 0.0993   악화
gross20     0.152  -> 0.160    악화
cat40       0.079  -> 0.099    악화
야간 gross20 0.106 -> 0.141,  cat40 0.046 -> 0.099
```

**자기 flip 과 평균 내면 중앙값은 조금 좋아지고 꼬리는 나빠진다.**  flip view 는 어려운
곳에서 어긋나고 두 점의 중점이 좋은 예측을 나쁜 쪽으로 끌어당긴다.  그리고
"accepted only" 의 개선은 coverage selection — V1~V5 가 다섯 번 보여준, 학생으로
전이되지 않는 바로 그 종류다.

이 대조군("same keypoints")을 안 넣었으면 "합의가 라벨을 개선한다"고 잘못 보고했을
것이다.

### 멈춘 지점

T1 gate(§10)는 consensus 와 **독립된 질문**이다 — medium 이 nano 보다 keypoint 를 잘
찍는지는 그 자체로 검증할 가치가 있고, PASS 하면 4-view median 이 2-view 평균과
다르게 나올 수도 있다.  다만 21 시간은 사용자 결정이 필요한 규모라 여기서 멈췄다.

분석: `_docs/paper/STRONG_TEACHER_V1_AUDIT.md`

## paper_fast_teacher_v1 — 학습 없이 teacher 를 이길 수 있나.  세 번 다 실패

YOLO26m 60ep(~21시간)를 보류하고, R0 를 여러 view 로 돌려 R0 단독보다 나은 2D teacher
가 되는지 먼저 봤다.  **새 학습 0 회.**  teacher 가 R0 를 못 이겨 **student 를 시작하지
않았다.**

### 먼저 flip cache 계약 감사 (§1)

`|orig - flip|` 중앙값 **3.80 px** — 이미 unflip + index 복원 상태다.  한 번 더 mirror
하면 95 px.  재mirror 하지 않았다.  V2 에서 이 실수로 1.9px → 127px 이 된 이력이 있어
데이터로 확인하고 코드에 가드를 넣었다(20px 초과면 정지).

### 비교 규칙이 결론을 가른다

candidate 가 어려운 keypoint 를 버리면 지표가 저절로 좋아진다.  그래서 gate 는
**candidate 가 값을 낸 keypoint 에서 R0 도 같이 재는 paired 비교**만 쓴다.
이 대조 없이는 세 후보 전부 "R0 보다 낫다" 로 잘못 보고된다.

### 결과 — 셋 다 같은 축에서 실패

```
Teacher            ALL NME   Night NME   NME p90   gross20   cat40   축 순열
R0 (single view)    0.0190      0.0203    0.0795     0.144   0.084    0.048
FAST-A flip 평균     0.0189      0.0224    0.0993     0.160   0.099    0.044
FAST-B 640+960       0.0190      0.0204    0.0996     0.146   0.100    0.049
FAST-C R0+G38        0.0183      0.0208    0.1054     0.159   0.099    0.068
```

**중앙값은 미미하게 개선, 꼬리는 뚜렷하게 악화.**  세 번 모두.

- FAST-A (새 추론 0): A3 p90 · A4 gross20 FAIL
- FAST-B (새 추론 638, imgsz 960, OOM 없음, sweep 안 함): B3·B4·B5 FAIL
- FAST-C (새 추론 638): C2~C5 FAIL.  **가장 나쁘다** — 주간 p90 0.0820 → 0.4372
  (40px → 179px), 축 순열 0.048 → 0.068.  야간만 조금 나아졌다(gross 0.101 → 0.095).

FAST-C membership 은 **결과 보기 전에 동결**했다(`FAST_C_MEMBERSHIP_FREEZE.json`).
호환 후보 셋(G38_GENERIC_ONLY / G38_EXP73916 / BROAD40K_FIXED)을 전부 감사해 기록하고,
§11 이 지명한 `G38_ONLY_60EP` 를 택했다 — 결과를 보고 고르지 않기 위해서다.
호환성은 데이터로 확인: kpt_shape [9,3] · flip_idx 동일 · nc 1 · real 경로 symlink 0 건.

### 왜 전부 같은 방향인가

합의는 관측이 **독립적으로** 틀릴 때 이득이다.  여기선 아니다.

- FAST-A/B 는 **같은 가중치**의 다른 view — R0 가 틀리는 곳에서 flip 도 960 도 같이
  틀린다.  평균/median 이 오류를 못 지우고 좋은 관측만 오염시킨다.
- FAST-C 는 다른 모델이지만 **더 나쁘다** — 독립성을 얻은 대신 품질이 낮은 관측을
  median 에 넣었다.

**관측을 더 모으는 것만으로는 R0 의 teacher ceiling 을 넘지 못한다.**

### 이 probe 가 답하지 못하는 것

```
확인됨   같은 R0 의 view 를 늘리는 것으로는 안 된다
확인됨   비슷한 급의 다른 source-only nano 를 섞는 것도 안 된다
미확인   더 큰 용량(YOLO26m)이 nano 보다 keypoint 를 잘 찍는가
```

FAST-C 실패는 "앙상블이 안 된다" 가 아니라 **넣은 모델이 R0 보다 못했기 때문**일 수
있다.  용량 증가는 그 점에서 다르며 이 probe 로 배제되지 않는다.

다만 세 번 모두 꼬리가 나빠졌다는 사실은, **꼬리(p90·gross20)가 개선되지 않으면
student 는 움직이지 않는다**는 V1~V5 의 교훈과 이어진다.  21 시간을 쓴다면 T1 gate 의
판정 기준은 중앙값이 아니라 **p90 과 gross20** 이어야 한다.

분석: `_docs/paper/FAST_TEACHER_V1.md`

## [09:05:14] ⚠️  Auto Compact (컨텍스트 한계 도달)

- Working directory: `/home/minjae/Documents/github/pallet-pose`
- Trigger: `auto`
- Session ID: `483d110b-a1f3-46f9-a25c-292bdcf291d7`
- Transcript: `/home/minjae/.claude/projects/-home-minjae-Documents-github-pallet-pose/483d110b-a1f3-46f9-a25c-292bdcf291d7.jsonl`
- Note: 컨텍스트가 가득 차서 자동 트리거됨 — 다음 세션에서 이 마커 위쪽 작업을 반드시 복원할 것

**TODO (session resume)**: compact 이후 이 마커 위쪽의 작업 내역을 확인하고 이어서 진행



## REAL_FT_V1 착수 — pseudo-label 대신 real manual GT 를 같은 예산에 넣는다

V1~V5 + FAST teacher 여섯 트랙이 전부 selection/ensemble 을 건드려 localisation 을
못 올렸다.  사용자 결정으로 **1순위 = 소량 real 라벨 fine-tune** 로 전환.

### R0 가 무엇을 틀리는지 먼저 측정 (추천 근거)

```
gross 프레임 99/252 의 성격     일부(<50% 코너) 63.6%  전부 16.2%
축 순열로 설명되는 프레임        12/99
조건별 gross                    near 54% / mid 21% / far 23%
                                trunc mild 59% / none 36%
                                elev low 51% / mid 37% / high 29%
```

가설 "truncation 데이터 부족" 을 세웠다가 **스스로 기각**했다 —
GT 코너의 화면 경계 여유별 gross 를 재니 화면 밖 코너는 33/1979(1.7%)뿐이고
gross 대부분이 화면 한복판(margin>=150px, gross 16.4%)에 있다.  경계·잘림이 원인이 아니다.

분포 대조에서 나온 사실 (별개로 유효):
```
화면 밖 코너를 가진 프레임   EVAL 16.0%   SYNTH 0.0%
물체 크기(대각선비) 중앙값    EVAL 0.377   SYNTH 0.286
```

### 학습 라벨 확보 — challenge/data/01_real/live_capture_gt 402 장

```
schema real_pallet_gt_v2 · gt_source manual · keypoint_frame camera_dynamic_0123_v4
split "train" (402/402)          <- 저장소 자체 계약이 이미 평가셋과 갈라놓음
reproj_error_px  med 0.60  p90 1.34  max 3.19
8 코너 완비 400/402
extrapolated_mask 0개=18 3개=91 4개=86 5개=207
pose_status UNCONFIRMED_SIGNED_AXIS (402/402)   <- 축 미확정, 3d-expert 검증 중
```

### 누수 검사 — 402/402 픽셀 sha256 대조, 중복 0

처음엔 어노 폴더에 이미지가 같이 있는 112 장만 대조했다.  kimjihoon 290 장은 이미지가
`_live_captures/handheld_20260902/.../rgb` 에 따로 있어 빠져 있었다 — **그 구멍을 막고**
402 장 전수 대조로 다시 확인했다.  중복 0.

독립적 분리 근거 3 개: 물체(plastic 110x110x15 vs PAPER_EVAL wood 세션),
해상도/intrinsics(640x480 CALIBRATED vs 1280x720 SENSOR_PROFILE_SCALED legacy_import),
저장소 split 계약(train vs eval).

### 사전등록

- `data/pallet/results/paper_real_ft_v1/PURPOSE.md` — [소비처]/[문장], 실패해도 논문에 쓴다
- `.../REAL_FT_V1_METHOD_LOCK.json` — G1~G6 gate, label_conversion, preflight P1~P3

핵심 설계: `SELFTRAIN_EXPOSURE_LOCK` 을 **한 글자도 바꾸지 않는다**.
1440 슬롯의 라벨 출처만 pseudo -> real manual GT.  그래서 차이를 라벨 품질로 귀속 가능.
(실측 정정: 노출 계약은 1440+1440 이지 720+2160 이 아니다.  lock 이 정본.)

라벨 변환 규칙도 결과 보기 전에 고정:
- box = 화면 클립된 visible keypoint 의 bbox  ([확인] 합성 라벨 400/400 일치)
- 화면 밖 코너는 v=0  ([확인] 합성 라벨에 [0,1] 밖 keypoint 0/400,
  belief head 가 격자 밖에 peak 를 못 찍음).  해당 87/3212 코너(2.7%), 46/402 프레임.
- loss 는 stock PoseLoss26 (V3 true-ignore 아님) — 변화를 하나로 유지

### 코드

- `scripts/self_training_yolo/real_ft_v1/build_real_ft_dataset.py`
  replay 1440 은 V3 데이터셋에서 그대로 가져와 멤버십 동일성 보장.
  빌드 결과: real 402 accepted, 2880 노출, 프레임당 3.582 회
- `scripts/self_training_yolo/real_ft_v1/run_real_ft_dev.py`
  train -> eval -> G1~G6 -> notify 한 파일.  지표 함수는 `v3_dev_metrics` 에서
  **import** (복사하면 조건이 뒤집힌 이력 있음).
  `eligibility_gate != "VERIFIED"` 면 학습 자체를 거부한다.

### 대기

`3d-expert` 에 keypoint 인덱스 규약 검증 위임 (C1 규약 일치 / C2 UNCONFIRMED_SIGNED_AXIS
의 의미 / C3 외삽 코너의 90도 yaw 순열 여부).  정사각 footprint 110x110 이라 W/D 축이
원리적으로 모호한 게 핵심 위험 — 우리가 고치려는 실패모드가 라벨에 들어 있을 수 있다.
판정 전 학습 금지.

**외부 선행사례 미조사** — 이 설계는 전적으로 저장소 내 측정에서 나왔다.

### 학습 전에 잡은 버그 — train.txt 의 resolve() 가 real 1440 장을 전부 배경으로 만들었다

P1 preflight 를 미리 돌려 본 덕에 학습 전에 잡았다.  돌렸으면 결과가 조용히
무의미해졌을 것이다 ("real 라벨은 도움 안 된다" 로 잘못 읽혔을 것).

```
증상   Scanning ... 1440 images, 1440 backgrounds, 0 corrupt
       label["keypoints"].shape == (0, 9, 3)
       im_file = .../\_live_captures/.../rgb/002689.png   <- 심링크가 풀린 원본 경로
원인   train.txt 를 str(p.resolve()) 로 썼다.  ultralytics 는 im_file 의 `/images/` 를
       `/labels/` 로 바꿔 라벨을 찾는데, 심링크를 resolve 하면 원본 촬영 폴더를 보게
       되고 거기엔 labels/ 가 없어 전 프레임이 조용히 background 로 들어간다.
       V3 는 심링크 경로를 그대로 쓴다 — 그게 정답이었다.
수정   str(p) 로 변경 + 주석으로 이유 명시
```

P1 을 '배경 0' 검사까지 하도록 강화했다 (좌표 drift 만 봤으면 못 잡는다 — 배경은
비교할 라벨 자체가 없어서 루프가 그냥 건너뛴다).

수정 후 검증:
```
총 2880   replay 1440   real 1440   배경 0
최대 drift 2.98e-08
visibility  v=2 12617   v=0 343   (합 12960 = 1440 x 9)
            343/12960 = 2.6%  ~= 앞서 잰 화면밖 코너 2.7% + 결측 4  정합
P1 = PASS
```

## 실험 종료 · 논문 최종화 (PAPER FINALIZATION ONLY)

사용자 지시로 `pallet-6d-pose` 논문 실험을 종료했다.  이번 작업에서 GPU 학습 0회,
추론 0회, 새 숫자 생성 0건.  기존 experiment artifact 수정 0건 (git 으로 확인).

REAL_FT_V1 은 학습 전에 중단 — 3d-expert 규약 감사가 학습 라벨의 26.4%(106/402)가
base 모델 규약과 다른 LR 순서라고 판정했고, 187/402 는 keypoint_annotations 가
projected_cuboid 대비 90도 stale 이었다.  고치려던 실패모드가 라벨에 들어 있었다.

### 증거 감사에서 나온 정정 3건 (이게 이번 작업의 핵심)

**1. "야간 검출 0.840 → 0.960 은 우리 기하 필터의 성과" — 기각**

```
R0 0.840 / R1_NAIVE 0.960 / R2_CONF 0.980 / R3 0.960 / R4 0.960 / R5_PROPOSED 0.960
```

naive self-training 이 이미 0.960 을 내고 confidence-only 가 0.980 으로 더 높다.
게다가 R0 vs R5 검출 차이는 PAIRED_UNCERTAINTY.json 에서
`detection p_better = 0.121`(frame) / `0.244`(session_cluster) — 노이즈와 미분리.
→ 논문은 "self-training 이 야간 검출을 올린다" 까지만 주장한다.

**2. 필터가 실제로 최고인 축은 검출이 아니라 랭킹**

```
              AUROC     FPR95     night FPR95
R0            0.9921    0.0417       0.1949
R5_PROPOSED   0.9953    0.0283 ★     0.0588
```

7개 arm 중 R5 가 AUROC·FPR95 둘 다 최고.  상대 32% FPR95 감소.
논문의 유일한 필터 긍정 주장은 여기에 건다.

**3. Tier 분류 — V1 블록만 진짜 사전등록**

커밋 순서로 실증:

```
V1 (R0,R0-CONT,R1~R5)  lock 이 결과보다 3h46m~4h11m 앞선 독립 커밋, 이후 무수정  TIER A
V2/V3/V5               lock 은 앞서나 PAPER_EVAL 진단을 본 뒤 설계 (자체 자백)   TIER B
V4 / STRONG / FAST     lock 과 결과가 같은 커밋 → git 으로 선후 증명 불가        TIER B
R6_CONF_FLIP, B_CONF_* EXPOSURE_LOCK arms[] 에 없음, 결과 본 뒤 신설            TIER B
DOPE control, Real-FT  reference / upper bound                                  TIER C
```

발견된 결함 2건: `STRONG_TEACHER_V1_PROTOCOL.md` 가 참조되는데 저장소에 없음(게이트
사전등록 검증 불가) / `PRE_RESULT_LOCK` 자기참조 SHA `0a872a19` 가 현 히스토리 조상이
아님(amend 흔적, 백데이팅 아님) → 논문에 인용 금지, 15f0cb5 를 쓸 것.

### 기존 문서에서 색출한 수정 대상

```
ABSTRACT_RESULT_SLOTS.md:10   "Improvement X  -9.0 %"   슬롯명이 improvement 인데 값이 음수
APPENDIX_TABLES.md:116        "Proposed (label-free)"   금지어 + 필터가 registry 치수를 씀
TABLE_M5.md:1 / EXPERIMENTS.md:496  "generalization"    8조건 중 7조건에서 R5 가 R0 보다 나쁨
SELF_TRAINING_TRACK_AUDIT.md  "LOO" 4건
AP 이름 충돌                   TEMPLATES "AP=ranking AP" vs TABLE_M1 "AP50-95" 열
V3_DEV_RESULTS.md / V5_DEV_RESULTS.md  제목이 둘 다 "V2 development results" (생성기 버그)
CITATION_INVENTORY            없음 (.bib 0개, 인용 0건) → Related Work 인용 지어내기 금지
```

### 생성물

`_docs/paper/final/` 15개 + `generated/` 5개.  `EXPERIMENTS.md` 에는 §26 이 허용한
배너 5줄만 추가(대규모 rewrite 없음).

`scripts/paper/build_final_paper_summary.py` — 읽기 전용 조립기.  모델 로딩·추론·재채점
없이 기존 JSON 에서만 표를 만들고 **114개 수치 전부에 대해 `값+파일경로+키`** 를
`RESULT_SOURCE_MAP.json` 에 남긴다.

빌드 중 버그 2건 자체 수정: CI 키가 `frame_delta.ci95` 인데 `.low/.high` 로 읽어 전부
`—` 로 나옴 / V3·V5 metrics JSON 이 비교용으로 이전 트랙 arm 을 함께 실어 중복 행 발생
(`__FULL` 정규화로 dedupe).

`EXPERIMENT_STOP_LOCK.json` — `EXPERIMENTATION_STOPPED`, HEAD c23959a 기록.

## FT 판정 뒤집힘 — split 과 augment 가 원인이었다

전날 "어노 402장만으로 FT 하면 REGRESSED" 로 결론냈는데 **틀렸다**.
사용자 지적: *"평가데이터로 재면 당연히 낮아지지 … 지금 과제꺼로 과적합중인데"*.

두 가지를 잘못했다.

**(1) split 이 목적과 어긋났다.** `train=handheld 344 / val=forklift rec 58` 로 촬영을
갈라놓고 쟀다. 잰 것은 FT 효과가 아니라 **두 촬영 사이의 도메인 갭**이다.
프로젝트 CLAUDE.md 는 "과제용(challenge): v1/v2 과적합, forklift 실배포" 라고 못박는데
나는 과적합을 피하는 지표(held-out 일반화)로 판정했다.
→ `--split-mode interleave` 추가. 모든 세션에서 6장마다 1장을 val 로.

**(2) augment 가 base 학습 계약과 달랐다.** ultralytics 기본값을 그대로 썼다.
```
              내 FT      base(synthetic) 계약
fliplr         0.5              0.0
mosaic         1.0              0.3
scale          0.5              0.25
translate      0.1              0.0
```
402장짜리 소량 FT 에 mosaic 1.0 은 특히 공격적이다.
→ 드라이버에 `--aug base_contract` 추가 (SGD lr0 0.01 / lrf 0.01 / cos_lr /
warmup 3 / mosaic 0.3 / close_mosaic 10 / scale 0.25 / fliplr 0 / hsv[.015,.5,.35]).

### flip 은 이미 켜져 있었다
"flip 으로 데이터를 늘릴 수 있나" 라는 질문의 답은 **이미 적용 중**이었다는 것이다.
파일 증식이 아니라 on-the-fly 로 매 배치 50%. `flip_idx` 정합성도 402장 전수로
확인했다 — 좌우 순서 위반이 flip 전 212건 / flip 후 212건으로 **완전히 동일** `[확인]`.
즉 flip_idx=[1,0,3,2,5,4,7,6,8] 이 이 데이터에서 맞다.

## crop 증식

`challenge/scripts/dataset/gen_truncation_crops.py` 를 그대로 재사용했다 — 입력이
`projected_cuboid + centroid` 인 JSON+PNG 쌍이라 우리 GT v2 와 포맷이 같다.
(kimjihoon 290장은 sibling PNG 가 없어 세션 rgb 에서 하드링크로 채웠다.)

```
402장 × 3 variants = 1,206 crop
cut-family   측면(L/R) 71.5%  하단 24.5%  상단 4.1%   ← 지게차는 좌우 패닝
in-image kp  min 5 median 6 max 9      visible area  min 10.1% median 21.8%
```

**누수 차단**: val 원본에서 파생된 crop 210개(=70×3)를 train 에서 뺐다.
`prepare_yolo_pose_from_live_gt.py --crop-dir` 이 파일명 `<세션>_<프레임>_t<k>` 에서
원본을 역추적해 거른다.

## 결과 — 두 실험, 같은 val

```
clean val 70장                    box mAP50-95   pose mAP50-95
  base (FT 전)                       0.8877         0.3523
  A  원본 332장         (v2)         0.9679         0.9837
  B  crop 포함 1,328장  (v3)         0.9552         0.9703

truncation val 210장               box mAP50-95  pose mAP50  pose mAP50-95  recall
  base (FT 전)                       0.5318       0.2019       0.1376      0.7657
  A  원본만                          0.5000       0.4630       0.2847      0.6880
  B  crop 포함                       0.8788       0.9457       0.9002      0.9274
```

A 2.2분 / B 6.9분 (RTX 3080).
**잘린 프레임에서 B 가 base 대비 6.5배, A 대비 3.2배.** clean 은 A 가 1.4%p 우세 —
메모리 [[cropaug-synthetic-ratio-tradeoff]] 의 "clean 후퇴 / truncation 향상" 과 같은 패턴.

배포 선택은 **B** — 지게차 접근 시 팔레트가 자주 잘리므로 clean 1.4%p 를 내주고
truncation 61.6%p 를 얻는 교환이 맞다.

## 4-fold 정규화 구간을 배포 규약에 맞춰 재조정

전날 `yaw ∈ [0,90)` 이라는 임의 구간으로 정규화했는데 배포 모델 규약이 아니었다.
예측 vs GT keypoint 거리로 확인: k=0(정규화본) 107.9px vs k=1(90° 되돌림) **5.1px**.
→ `--offset-deg` 추가(기본 -90 = 배포 규약), 402개 재정렬 후 k=0 에서 5.1px 확인 `[확인]`.

## HuggingFace
https://huggingface.co/CanelE452/pallet-pose-yolo26n-livegt (public) 을 **B 로 갱신**.
model card 를 실측 표로 전면 개정했다. 원격 sha256 이 v3 best.pt 와 일치함을 확인.

## paper 폴더 재편 + 최종 표 완성 + metric semantics 정정

학습 0 · 추론 0 · evaluator 재실행 0.  `data/pallet/results` 와 `challenge/**/weights`
sha256 전수 대조 결과 **차이 0줄** (각각 26,420 / 3,432 파일).

### 폴더 재편 — `git mv` 25건, 내용 변경 0건

```
_docs/paper/  28개 → 4개 (EXPERIMENTS.md · PRE_RESULT_LOCK.json · README.md · final/)

_docs/audits/paper/                      3건  (POSE_METRIC_READINESS 등 기술 감사)
_docs/archive/paper_pre_final_20260903/
    annotation_and_dataset_review/       7건
    diagnostics/                        12건  (V2~V5 · teacher probe, 전부 Tier B)
    legacy_paper_outputs/                3건 + evaluation_tables/ + generated/
```

**함정 하나**: 생성 스크립트 12개가 `_docs/paper/` 안으로 **출력**하고 있었다.
경로를 안 고치면 다음 실행 때 root 가 되살아나 4개 불변식이 깨진다.  전부 archive 로
돌렸다.  `_docs/history/**` 와 `data/pallet/results/**` 는 의도적으로 손대지 않았다
(전자는 기록 위조, 후자는 무변경 보증 위반).  세션 초반 `paper_eval_v1/PURPOSE.md` 를
한 번 고쳤다가 되돌렸다.

상위 문서 4개(`_docs/README.md`·`readme.md`·`metric_split_lock.md`·
`paper_strategy_master.md`)는 경로 치환이 아니라 **정본 포인터가 낡은 것**이라
`final/` 로 의미까지 고쳤다.

### 사용자 제시 표값 전수 대조 — 100/100 MATCH

Table 2 / 3A / 4 / Panel B 의 모든 셀을 artifact 와 대조했고 불일치 0.
Table 3A 의 출처를 특정했다 — `paper_selftrain_v1/M4_FILTER_QUALITY.json`
(`filters.<F*>.{accepted, retention, pass.median_px, pass.p90_px, pass.gross_rate}`,
population `PAPER_EVAL_PLASTIC_POS` n=194).

★ 라벨 함정: 3A 5행은 `F4_PROPOSED`(= conf + removal + flip, reprojection 미포함)이고,
같은 JSON 의 `F5_CONF_FLIP`(143/0.737/6.516/23.764/0.134)은 **다른 행**이다.

### 표 완성

```
TABLE_FINAL_1  Panel A 통제 arm 5행 + Proj@5/10/20 보조 / Panel B DOPE vs R0 (ranking 열 제거)
TABLE_FINAL_2  본문 4행, scale 민감도 경고 추가
TABLE_FINAL_3  3A frozen 필터 품질 · 3B 하류 학생 (분리)
TABLE_FINAL_4  8조건 robustness — localisation 개선은 Far 하나뿐
TABLE_FINAL_DIAGNOSTIC  신호 AUC 표를 여기로 이동
```

빌드 스크립트 버그 2건 자체 수정: `cell()` 이 None 을 폭 없는 대시로 반환해
`0.7245—` 처럼 앞 열과 붙었다 / Table 2 헤더 f-string 에 쉼표 오타.
`RESULT_SOURCE_MAP.json` 이 **265개 수치**를 `값+경로+키` 로 추적한다.

### ★ metric semantics 정정 — px 는 pose 지표가 아니다

`metric_split_lock.md` §2 를 직접 읽어 레이어를 확인했다.

```
2.1 Detection    READY
2.2 Keypoint     READY   ← px, Proj@N, PCK, gross20, catastrophic40 이 여기 있다
2.3 Pose         BLOCKED
2.4 Operational  NOT EVALUATED
```

px 는 새로 만든 지표가 아니라 원래 keypoint layer 소속이다.  pose layer 가 막혀서
**현재 계산 가능한 가장 세밀한 localisation endpoint 로 남은 것**이지 승격된 게 아니다.

evaluator 실제 구현 확인 (`paper_real_eval.py:2409`):
`np.linalg.norm(pred.keypoints_xy - target.keypoints_xy, axis=1)`,
좌표는 `INFERENCE_PAD=100` 제거된 original-image 좌표.

**불일치 발견 — 계약 문구와 구현이 다르다.** §2.2 는 *"8 corner order-free Hungarian"*
이라 쓰는데 evaluator 는 **인덱스 대응**이고 `linear_sum_assignment` 호출이 0회다.
이건 사소하지 않다 — order-free 였다면 90° 축 순열이 오차로 안 잡혔을 것이고,
축 순열 진단 자체가 성립하지 않는다.  `METRIC_NAMING_LOCK.md` 와 LIMITATIONS 에 명시했다.

신규 `_docs/paper/final/METRIC_NAMING_LOCK.md` 가 reader-facing 이름의 정본이다.
표 헤더를 `Pooled kp med[px]` 로 바꾸고, scale 민감도 경고를 Table 2·4 에 넣었고,
NME 는 "post-hoc scale-normalised diagnostic" 으로만 쓰도록 고정했다.

### 검사 결과

```
[A] paper root = 4개 정확히 일치
[B] 최종 표 6개 전부 존재
[C] 표에 TBD/TODO/placeholder 0 · 6D 열 0 (빈칸이 아니라 열 자체 제거)
    분리력 표의 대시는 "그 레이어에 없는 신호" — 사유를 표에 명시
[D] 금지 표현 hit 전부 금지목록·부인문
[E] 상대 마크다운 링크 broken 0
[F] 표 숫자 265개 전부 source map 추적
```

### EXPERIMENTS.md 의 `—` 에 대한 사용자 질문 → 배너 보강

사용자가 "표 실험 다 채운 거 맞아? 다 비어있는데?" 라고 물었다.  확인 결과:

- 이번 세션에서 EXPERIMENTS.md 의 표 셀은 **한 칸도 건드리지 않았다**
  (c23959a→HEAD diff = 배너 + 경로 치환뿐).  `—` 3개 이상인 행 104개는 그 전부터 그대로.
- 그 문서는 원래 숫자 저장소가 아니다 — 본문 스스로
  *"여기 숫자를 손으로 복붙하지 않는다"* 고 선언한다.
- 내 보고가 "표 완성" 이라고 해서 오해를 불렀다.  완성한 건
  `_docs/paper/final/generated/` 의 Table 1~4 다.

대시의 성격이 세 가지로 섞여 있는데 배너가 그걸 설명하지 않아 혼동을 줬다:

```
1  pose 열       영구 BLOCKED (selector 0.65 vs gate 0.95). 최종 표에선 열 자체 제거
2  SSP · PVNet   INCOMPATIBLE / NEEDS_TRAIN — 채울 계획 없음
3  값이 있는 행   숫자는 _docs/paper/final/generated/ 에 있다
```

배너에 이 분류와 정본 표 경로를 명시했다.  삽입 34줄, **삭제 0줄**, 표 셀 무변경(104행 유지).

## POSE_METRIC_CLOSURE_V1 착수 (1차) — 학습 0 · 추론 0 · evaluator 재실행 0

6D pose metric 이 BLOCKED 인 이유를 코드·artifact 로 재현했다.  기존 결과 수정 0건.

### blocker 재현 (사용자 제시 수치와 일치)

```
population = DEV_POS140 (정본 eval 7세션, PAPER_EVAL 부분집합)
                     overall   night   min_session   gate
frozen selector       0.5929  0.4643      0.3333     >=0.95 / >=0.90 / >=0.85
R0                    0.6500  0.6786      0.3030
R5_PROPOSED           0.5929  0.8214      0.2121
```

★ 사용자가 언급 안 한 **세션 게이트(0.85)** 가 하나 더 있고, 거기서 가장 멀다.
gate 상수 출처 = `challenge/evaluation_v2/pnp_selector.py:26-28`.

### 왜 실패하는가 — 측정 4개

```
a  두 가설이 모두 기하적으로 성립       138/138 (100%)   기하가 배제하는 프레임 0장
b  |score_margin| AUC                  0.5562           자기 확신이 정답을 모름
c  fail-closed 임계 스윕                최선 cov 0.145 / acc 0.750 — 게이트 도달 불가
d  상수 추측 대비 이득                  0.5725 -> 0.6014 = +2.9%p
틀렸을 때  rotation 85.3deg · yaw 85.3deg · translation 0.065 -> 0.219 m
```

구조적 원인: plastic footprint 1.10 x 1.30, **비 1.182**.  18% 차이를 미지의
yaw·depth 가 흡수한다.

★ 자기 정정: "구조적으로 정보 없음" 은 과했다.  `eval_cad` 는 정답이 2종 혼재
(long 3 / short 15)인데 18/18 을 맞췄고 선택 분포까지 일치한다 — 상수 추측 상한은
15/18.  즉 **신호는 있으나 조건에 따라 무너진다**.  `eval_noapril` 의 12/12 는 정답이
전부 long 이라 무의미(상수 추측으로도 12/12).

### 누수 — 선언이 아니라 코드로 확인

```
pnp_selector.py:495  select_pnp_hypotheses(predicted_keypoints, camera_intrinsics,
                                           physical_dimensions, config)
```
인자 4개, GT 없음.  `gt_`/`ground_truth`/`target_pose` 참조 0건.
결과의 `axis_assignment` 는 출력이지 입력이 아니다.
→ **0.5929/0.6500 은 누수를 걷어내면 떨어질 값이 아니라 이미 정직한 값**이다.

### wood 감사 (3d-expert 위임) — 막는 건 기하가 아니라 정책

```
W1 symmetry   FAIL — 그러나 {I, Ry(180)} 는 측정으로 지지됨
              (직사각 0.80x0.59 라 90/270 배제, deck mirror NCC 0.957, slat 잔차 <=6mm)
              실제 블로커: FROZEN 으로 바꾸려면 OBJECT_GEOMETRY_REGISTRY.json 편집 필요,
              그 sha256 이 62파일 198곳에 pin.  loader 가 status/contract 동시 변경 강제.
W2 canonical  FAIL — 기하 불일치 없음 (canonical<->CF 왕복 max 2.3e-13 px, 125프레임).
              잔차는 어노 클릭 오차(DEV45 median 1.94px).  미결 자유도는 항상 Y축 180도 하나.
              유일한 결손 입력이 W1 이다.
```
wood 를 열어도 selector NOT_RUN + FINAL_WOOD_POS 미확보 2개가 남는다.
wood aspect 1.356 > plastic 1.182 라 기하적으로는 더 유리할 수 있으나 **미검증**.

### evaluator unit test — 19개 통과

`scripts/paper/pose_metric_closure_v1/test_pose_metric_contract.py` (모델 미로딩).
identity / 알려진 평행이동 / 알려진 yaw / yaw 축(Y) / 90도 swap 비용 /
ADD vs 무제한 ADD-S / AUC 해석해 / diameter / blocked gate 가 null 반환.

★ 테스트 작성 중 **내 가정이 틀린 것을 코드가 잡았다** — 정사각이면 90도 swap 이
ADD 0 일 거라 가정했으나 1.10 m 가 나왔다.  `add_error_m` 이 **인덱스 대응**이라
코너 집합이 자기 자신으로 사상돼도 인덱스가 돌면 벌점이 붙는다.  코드가 맞다.
반대로 **무제한 `adds_error_m` 은 그 swap 을 0.0 으로 용서한다** — 90도를 근거 없이
symmetry 에 넣으면 selector 실패가 지표에서 사라진다는 증거.

### evaluator 구현 결손 2건 (사용자가 나열한 5개 blocker 밖)

```
IoU3D      challenge/evaluation_v2/ 에 구현 없음.
           scripts/stage0/real_eval/re_metrics.py:171 에만 있다.
pose_auc   구현 2개 — pose_metrics.py:297 (정규화 오차, 1001 step) vs
           re_metrics.py:73 (raw + diameter, 100 step).  하나를 정본으로 선언해야 한다.
```

### 생성물

`_docs/paper/pose_metric_closure_v1/` — PURPOSE · CURRENT_BLOCKERS · CLOSURE_PLAN ·
METRIC_DEFINITIONS · LEAKAGE_RULES · WOOD_SYMMETRY_REVIEW · WOOD_CANONICAL_CLOSURE
`data/pallet/results/paper_pose_metric_closure_v1/POSE_CLOSURE_METHOD_LOCK.json`
`scripts/paper/pose_metric_closure_v1/test_pose_metric_contract.py`

`_docs/paper/` 루트가 4개 -> 5개가 되어 README 불변식을 갱신했다
(`final/` 이 유일 정본이라는 규칙은 유지, 이 트랙은 아직 논문에 안 들어감).

**미완**: DATA_SPLIT_PLAN.md · FINAL_STATUS.md · POSE_CLOSURE_STATUS.json —
population 재고 감사(data-engineer 위임) 진행 중.

### POSE_METRIC_CLOSURE_V1 1차 완결 — BLOCKED_SELECTOR

population 감사(data-engineer)가 도착해 나머지 문서를 채웠다.

**결정적 사실 (직접 재현)**: `axis_assignment_confirmed == True` 인 real 프레임이
**저장소 전체에 0장**이다 (pose_transform 보유 JSON 2,004개 전수; False 700 +
legacy 필드없음 1,304).  축 확정된 real pose GT 가 어디에도 없다.

```
SELECTOR_SOURCE  가능    synthetic 60,000 (PROBE_METADATA_60K, audit 60000/60000)
SELECTOR_DEV     불가    후보 121장은 있으나 (a) selector 정답 라벨 없음
                        (b) DATASET_CONTRACT 가 "not paper eligible" (c) n=121 이라
                        night 게이트 0.90 을 0.84~0.96 과 구분 못 함
POSE_FINAL       불가    FINAL manifest 5개 전부 items 0.  물리 FINAL 229장 중
                        146장이 SHA 기준 PAPER_EVAL 에 흡수
NEW_CAPTURE_REQUIRED = YES  (세션 독립 잔여는 야간 plastic 1,581장뿐)
```

★ incoming 42,465장은 SHA 로는 깨끗하지만 **세션 독립이 아니다** — day 000000~029027 /
night 029028~042610 로 프레임 번호가 연속이고 session.json 이
`promoted_from_sessions` 로 자백한다.  incoming 매니페스트의
`duplicate_in_active_evaluation: false` 는 stale 이니 믿으면 안 된다.

★ synthetic selector 는 **이미 학습·측정됐다** — synthetic dev 0.904 / AUROC 0.966,
real 0.59~0.65.  dims-shuffle ablation 0.745 라 신호가 dims 에서 온다.
즉 데이터 부족이 아니라 sim2real 격차가 병목이다.

**최종**: `POSE_METRIC_CLOSURE_V1_STATUS = BLOCKED_SELECTOR`
(`BLOCKED_DATA_CONTRACT` 도 독립적으로 참이지만, 스펙상 그 라벨은 selector 가
동작할 때 쓰는 것이라 selector 를 1차 라벨로 둔다)

산출물 8개 완성.  `_docs/paper/final/` 미수정, placeholder pose 수치 삽입 0건.

## POSE_METRIC_CLOSURE_V1 2차 — human axis review 도구 일습 (1차 실행분)

기존 319장을 그대로 쓰고, 사람이 "긴 축이 A냐 B냐"만 눌러 GT 축 모호성을 닫는 경로를
만들었다.  §29 대로 도구까지만 하고 pose 수치는 계산하지 않았다.

### manifest — 319/319

```
plastic_standard_110x130x11  194   long 1.30 / short 1.10
wood_small_80x59x14          125   long 0.80 / short 0.59
domain  none 199 / night 50 / day 70      8코너 좌표 완비 319/319
```

★ `object_type` 이 비어 있는 legacy 프레임이 129장이라 **치수로 역판정**하게 했다
(129 + 65 = plastic 194).

★ **핵심 발견**: GT 어노에 `dimensions_m` 이 프레임마다 다른 parity 로 이미 저장돼 있다
(plastic CF_DEPTH 109 / CF_WIDTH 85, wood CF_DEPTH 54 / CF_WIDTH 71).  즉 축 배정이
이미 있으나 `axis_assignment_confirmed` 가 319/319 False 라 **아무도 확인한 적이 없다**.
이 값을 manifest 에 `_hidden_stored_long_axis` 로 넣되 **GUI 에는 절대 표시하지 않는다**
(사람 판정 편향 방지).  검수 후 "기존 저장값과 사람이 얼마나 다른가" 감사에만 쓴다.

★ **session propagation 은 비활성**.  319/319 이 `camera_dynamic_0123_v4` 라 카메라
기준 축이 프레임마다 재유도된다 — 같은 세션이라도 parity 가 바뀔 수 있어 전파는 위험하다.

### 도구

```
build_axis_review_manifest.py   manifest + progress
review_physical_axis.py         OpenCV GUI.  A/B/U, V(가설 큐보이드 오버레이),
                                Backspace, ←/→, G, S, Q.  매 클릭 autosave, resume.
validate_axis_review.py         319/194/125 검증 + 고립 flip 탐지 + 저장값 대조
                                + --contact-sheet.  라벨을 자동으로 고치지 않는다.
build_reviewed_pose_gt.py       확정 축으로 GT pose 재풀이 (원본 미수정)
symmetry_aware_pose_metrics.py  yaw mod180(0~90) · S={I,Ry180} 회전 · group-aware ADD
                                · pose_auc 1001점 고정
pose_evaluation_paths.py        predict_pose_without_gt / score_pose_against_gt 분리
                                + oracle 경로를 is_oracle=True 로 태그
challenge/evaluation_v2/oriented_iou3d.py   정확한 oriented IoU (12 half-space +
                                            정점열거 + ConvexHull, 근사 아님)
```

### 테스트 62개 통과

IoU3D 16 + 대칭 지표 27 + 기존 계약 19.  해석해로 검증:
90도 swap 이 plastic 0.79 / wood 0.53 / 정사각 1.0, 45도가 <0.95(축정렬 근사면 ~1.0),
f 만큼 이동 시 (1-f)/(1+f).

자체 수정 3건 — `np.trapezoid` 가 numpy 1.26 에 없어 `trapz` 폴백 /
`wrap180(180)` 이 -180 을 반환해 문서화한 (-180,180] 과 불일치 /
GUI 헤더 텍스트 겹침·우측 잘림.

### 다음

사용자가 `review_physical_axis.py` 로 319장 검수 → "검수 끝" → 2차 실행.

### smoke 5 실행 — PASS

사용자가 GUI 를 직접 조작해 5장 검수.  종료코드가 아니라 저장 내용으로 판정했다.

```
A/B 입력   PASS   CF_DEPTH 3 + CF_WIDTH 2 — 두 키 모두 동작
autosave   PASS   5장 즉시 기록
resume     PASS   "starting at 1, already reviewed: 0"
격리       PASS   AXIS_REVIEW_LABELS.json 미생성 (본 검수 무오염)
원본       PASS   source_annotations_modified=False
누수       PASS   model_predictions_shown_to_reviewer=False
```

★ 5장 모두 기존 저장 parity 와 일치했으나 **판단 근거로 쓰지 않는다** — 표본 5장이고
`eval_cad` 는 selector 가 18/18 맞춘 유일한 세션(=최이지 구간)이다.

부수: `pgrep -f review_physical_axis` 가 자기 자신을 잡아 "잔존 프로세스 있음" 으로
오판했다.  `ps -eo pid,cmd | grep ... | grep -v grep` 으로 정정 — 실제 잔존 0.
(memory `pgrep-self-match-hangs-wait-loops` 와 같은 함정.)

다음: 사용자 319장 본 검수 → "검수 끝" → 2차 실행 (`.pending.md` 에 기록).

### 사람 축 검수 319/319 완료 — 그런데 전제를 흔드는 결과가 나왔다

```
검수      319/319  unclear 0  coverage 1.000  (plastic 194 / wood 125)
long_axis CF_WIDTH 170 / CF_DEPTH 148  — 반반이라 session propagation 을 끈 판단은 옳았다
고립 flip 7장
기존 저장 parity 와 불일치  33/319 (10.3%)   eval_pallet09 는 14/33 = 42.4%
```

**결정적 검사**: GT 키포인트에 두 가설을 각각 PnP 로 맞춰 잔차를 비교했다.

```
                        사람 판정 잔차   반대 가설 잔차   사람이 더 잘 맞는 비율
일치 286장                  0.99 px        10.87 px            100.0%
불일치 33장                 5.28 px         0.93 px              3.0%
```

```
저장 parity == 잔차최소 가설   318/319 (99.7%)   ← 저장값은 사실상 "잘 맞는 쪽" 이었다
사람 판정  == 잔차최소 가설   287/319 (90.0%)
두 가설 잔차 비   median 9.9x   p10 3.7x   최소 1.5x   (비<2 인 프레임 1장뿐)
```

★ 두 가지가 동시에 드러났다.

1. **GT 키포인트에서는 축이 기하로 결정된다.** 1.10 vs 1.30 의 18% 차이가 ~1px 정확도의
   사람 어노 좌표에서는 잔차 9.9배 차이로 갈린다.  즉 "축 확정 GT 가 없다" 는 전제는
   결정 불가라는 뜻이 아니라 **확인된 적이 없다** 는 뜻이었다.
2. **그러면 selector 실패의 원인이 재해석된다.** 예측 키포인트(median 6.6px 오차)에서는
   138/138 이 양쪽 다 성립했는데, GT 키포인트(~1px)에서는 9.9배로 갈린다.
   → 축 모호성이 원리적인 게 아니라 **keypoint 정확도 문제**일 수 있다.
   MAIN poor / ORACLE good 이 나올 가능성이 높아졌다.

★ 그리고 사람 판정 32장이 기하와 어긋난다(잔차 5.28 vs 0.93).  사람 오류일 가능성이 높다.
   `rotation_delta_vs_stored_pose_deg` 로는 이걸 못 잡았다 — 두 가설은 같은 상자의 90°
   회전이 아니라 같은 이미지 점에 다른 치수를 배정하는 것이라 카메라 좌표 회전이 비슷하다.
   지표를 잘못 골랐던 것이고, 잔차 비교로 바꿔서 잡았다.

**pose 평가는 시작하지 않았다.**  GT 를 어느 쪽으로 확정할지 사용자 결정 대기.

### blind 재검수 40장 — 사람은 일관됐고, 원인은 원근 착시였다

기하와 어긋난 33장 + 고립 flip 7장 = 40장을 **blind 재검수**했다.  1차 판정도 기하
판정도 화면에 표시하지 않기 위해 별도 라벨 파일(`AXIS_REVIEW_LABELS_RECHECK.json`)로
분리했고, 초기 상태가 전부 `not reviewed` 인 것을 헤드리스로 확인한 뒤 띄웠다.

```
결과   40/40   CONFIRMED 36 · UNCLEAR 4
       2차 == 1차   36/36 (100%)      ← 사람은 완전히 일관됐다
       그중 기하와 여전히 반대   28장
```

사람이 두 번 독립적으로 같은 답을 냈으므로 **기하 쪽을 의심**했다.  이 저장소의 알려진
실패모드가 "코너는 제자리, 인덱스만 90도 회전" 이라 그 가능성이 실재했다.

**결정적 검사 — 앙각 분포**

```
기하와 일치 287장   앙각(상면 기준) 중앙값 18.6°
기하와 불일치 32장  앙각             중앙값  3.1°

앙각 15° 미만 152장 → 불일치 31 (20%)
앙각 15° 이상 167장 → 불일치  1 ( 1%)      ← 20배 차이
```

→ **저앙각에서 깊이축이 원근으로 크게 단축된다.** 거의 수평에서 보면 130cm 깊이가
50px, 110cm 폭이 300px 로 찍힌다.  눈에는 A 가 명백히 길어 보이지만 물리적으로는 B 가
길다.  사람 판정은 실수가 아니라 **일관된 착시**였고, PnP 는 원근을 보정하므로 옳다.

증거 3개가 한 방향:
```
1  사람 판정 == 투영상 더 긴 축   29/36        눈에 보이는 대로 고른 것
2  불일치가 저앙각 집중            3.1° vs 18.6°
3  기하 승리 가설 잔차             0.93 px  (반대 5.28 px)
   8개 코너가 1.10x1.30 상자와 서브픽셀로 맞는다 — 우연히 나올 수 없다
```

★ 이 발견 자체가 논문 가치가 있다 — **저앙각 관측에서는 사람도 축을 못 가른다.**
selector 실패가 알고리즘 결함이 아니라 관측 조건의 정보 부족이라는 강한 증거다.
(앞서 기록한 "GT 키포인트에서는 축이 기하로 결정된다" 와 합치면: 정보는 있지만
저앙각 + 예측 키포인트 오차 6.6px 조건에서 사라진다.)

**GT 확정은 사용자 결정 대기** — A(기하 채택) 권고, B(32장 UNCLEAR), C(사람 유지).
pose 평가는 시작하지 않았다.

## [15:19:05] ⚠️  Auto Compact (컨텍스트 한계 도달)

- Working directory: `/home/minjae/Documents/github/pallet-pose`
- Trigger: `auto`
- Session ID: `483d110b-a1f3-46f9-a25c-292bdcf291d7`
- Transcript: `/home/minjae/.claude/projects/-home-minjae-Documents-github-pallet-pose/483d110b-a1f3-46f9-a25c-292bdcf291d7.jsonl`
- Note: 컨텍스트가 가득 차서 자동 트리거됨 — 다음 세션에서 이 마커 위쪽 작업을 반드시 복원할 것

**TODO (session resume)**: compact 이후 이 마커 위쪽의 작업 내역을 확인하고 이어서 진행


## [POSE_METRIC_CLOSURE_V1] 7개 frozen arm 6D pose 평가 완료 — STATUS = EVALUATED

### 실행 순서 (게이트 → 추론 → 평가 → 표)

새 스크립트 `scripts/paper/pose_metric_closure_v1/verify_inference_parity.py` 추가.
R0 를 새 runner 로 다시 추론해 기존 캐시
(`data/pallet/results/paper_eval_v1/arms/R0_per_frame.csv`, PAPER_EVAL_ALL_POS 319행)를
재현하는지 먼저 확인했다. 실패하면 R1~R5 를 돌리지 않기로 사전 합의된 게이트다.

```
detection agreement            319 / 319
box_xyxy delta                 max 0.000000 px   (비트 단위 일치)
box confidence delta           max 0.000000      (비트 단위 일치)
supervised keypoint err delta  max 5.0e-07 px    (n 287)
keypoint 목록 비교 불가          32 프레임
verdict                        PASS
```

★ keypoint delta 5e-7 은 모델 차이가 아니라 **CSV 가 소수점 6자리로 직렬화**된
탓이다. 허용치 5e-7 px 을 관측값에 맞춰 정한 게 아니라 6자리 반올림 반폭에서
유도했음을 아티팩트에 명시했다. 비교 불가 32 프레임은 옛 CSV 가 오차 목록을
비워 두었거나 visibility flag 기반 supervised 정의를 써서 길이가 다른 것이며,
좌표 불일치가 아니다 — `INFERENCE_PARITY_R0.json` 에 프레임 목록까지 기록.

체크포인트 7개 모두 `POSE_ARM_CHECKPOINT_LOCK.json` 선언 sha256 과 일치.
새 학습 0, 새 pseudo-label 0, checkpoint 재선택 0, selector 수정 0.

### MAIN 표 (배포 가능 경로, GT 축 미사용)

```
Method                              PoseCov  AxisAcc   R med  Yaw med  t med cm   IoU3D  ADDsym AUC
───────────────────────────────────────────────────────────────────────────────────────────────────
Synthetic-only (R0)                   1.000    0.749    2.26     1.23      7.90   0.603       0.428
Source-only continuation              0.997    0.726    2.50     1.26      8.54   0.594       0.409
Naive self-training                   1.000    0.768    2.37     1.25      7.80   0.590       0.420
Confidence self-training              1.000    0.724    2.48     1.33      7.78   0.599       0.416
Reprojection self-training            1.000    0.749    2.35     1.21      7.74   0.600       0.415
Removal self-training                 1.000    0.727    2.52     1.28      8.05   0.600       0.412
Full consistency self-training        1.000    0.734    2.53     1.29      8.83   0.587       0.400
```

★ **어떤 self-training arm 도 R0 의 6D pose 를 개선하지 못한다.** R0 가 IoU3D
(0.603)·ADDsym AUC(0.428) 모두 최고이고, R5 가 둘 다 최저(0.587 / 0.400)이며
translation 도 최악(8.83 cm)이다. 2D 결과와 방향이 정확히 같다.

### ORACLE 진단 (GT 축 공급 — 상한이지 배포 성능 아님)

```
Method                              MAIN Axis  MAIN IoU  ORA IoU    dIoU  MAIN AUC  ORA AUC    dAUC
───────────────────────────────────────────────────────────────────────────────────────────────────
Synthetic-only (R0)                     0.749     0.603    0.609  +0.006     0.428    0.456  +0.027
Source-only continuation                0.726     0.594    0.614  +0.020     0.409    0.441  +0.032
Naive self-training                     0.768     0.590    0.602  +0.012     0.420    0.429  +0.009
Confidence self-training                0.724     0.599    0.618  +0.018     0.416    0.446  +0.030
Reprojection self-training              0.749     0.600    0.619  +0.020     0.415    0.435  +0.020
Removal self-training                   0.727     0.600    0.613  +0.013     0.412    0.441  +0.029
Full consistency self-training          0.734     0.587    0.623  +0.036     0.400    0.433  +0.033
```

★ 축을 공짜로 줘도 IoU3D 가 최대 +0.036 밖에 안 오른다 → **MAIN poor / ORACLE poor**.
축 선택은 어떤 arm 에서도 지배적 오차원이 아니다. 앞서 R0 단독으로 얻은 결론
(틀린 축 프레임에 oracle 을 줘도 IoU 0.362→0.432 에 그침)이 7개 arm 전체에서 재현됐다.

### material / lighting

```
                      AxisAcc          IoU3D          t med cm
              R0      R5        R0      R5        R0      R5
─────────────────────────────────────────────────────────────
Plastic 194   0.706   0.655     0.586   0.573    10.47   11.16
Wood    125   0.816   0.856     0.626   0.634     4.20    4.09
Daytime  70   0.486   0.400     0.564   0.556    11.03   11.19
Night    50   0.760   0.780     0.532   0.556    12.59   14.40
```

★ Daytime AxisAcc 0.486 은 사실상 동전던지기다. Daytime 세션이 저앙각 관측에
몰려 있다는 앞선 진단(사람도 못 가르는 조건)과 일치한다. Wood 는 footprint
aspect 1.356 으로 두 가설이 더 벌어져 있어 0.816~0.856 으로 높다.

### 생성물

- `_docs/paper/final/generated/TABLE_FINAL_POSE.md`
- `_docs/paper/final/generated/TABLE_FINAL_POSE_BY_MATERIAL.md`
- `_docs/paper/final/generated/TABLE_FINAL_POSE_BY_LIGHTING.md`
- `_docs/paper/pose_metric_closure_v1/POSE_AXIS_ORACLE_DIAGNOSTIC.md`
- `data/pallet/results/paper_pose_metric_closure_v1/INFERENCE_PARITY_R0.json`
- `POSE_CLOSURE_STATUS.json` → `POSE_METRIC_CLOSURE_V1_STATUS = EVALUATED`,
  `POSE_METRICS_STATUS = REPORTABLE`. 1차 pass 의 BLOCKED_SELECTOR 기록은
  `first_pass_status_preserved` 에 보존(덮어쓰지 않음).

### 아직 안 한 것

- §17 paired bootstrap (frame-level + session-cluster) — 사용자가 허용한 대로
  별도 step 으로 미룸. 현재 표는 전부 point estimate.
- ABSTRACT / CONTRIBUTIONS / RESULTS_STORY 는 **건드리지 않았다**. 결과 보고 후
  사용자가 narrative 를 결정한다.
