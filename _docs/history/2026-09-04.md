# 2026-09-04

## [FAST_6D_SCREEN_V1] 학습 없이 pose formulation 후보 스크리닝 — NO_PROMOTABLE_SIGNAL

LOCK `6a0584c`(측정 전 단독 커밋·원격 확인) → screen → 판정.
새 학습 0 · 새 checkpoint 0 · depth 0 · self-training 0 · parameter sweep 0.
기존 실험(V1~V5·temporal·depth·pose closure·s2_diffpnp·s2_multihead·weights) 무변경.

### 결과

```
arm                            PoseCov     R    Yaw   t cm    IoU3D   ADDsym
S0 YOLO current                  1.000  2.26   1.23   7.90   0.6032   0.4285
S1 Point translation control     1.000  2.26   1.23   7.90   0.6032   0.4285
S2 Point+BBox translation        1.000  2.26   1.23   8.05   0.5816   0.4187
D0 Raw DOPE                      0.715  ...                  0.5222   0.3724
S3 BBox-gated DOPE               0.715  (D0 와 완전 동일)      0.5222   0.3724
S4 Square-context DOPE           0.693                       0.3883   0.3079
S5 Line-R + BBox-t               BLOCKED_INCOMPATIBLE_PROVENANCE

DOPE 국소화     DetCov  corners  kp med  kp p90  gross20
D0               0.850     2232   11.42   75.84    0.271
S3               0.850     2232   11.42   75.84    0.271
S4               0.796     2135   13.13  196.48    0.402
```

```
S2-S0  IoU -0.0216 [-0.0339,+0.0206]cluster   AUC -0.0098 [-0.0233,+0.0098]
S4-D0  IoU -0.0917 [-0.2120,-0.0073]★배제     AUC -0.0477 [-0.1043,-0.0066]★배제
S1-S0, S3-D0  정확히 0
모든 arm STOP → FAST_6D_SCREEN = NO_PROMOTABLE_SIGNAL
```

### ★ 가장 정보량 큰 음성 결과 — S3

게이트는 **전 프레임에서 실행됐고 코너 선택을 단 하나도 바꾸지 않았다**(305 비교
프레임 중 차이 0). DOPE 의 최고점이 이미 1.25배 정사각 context 안에 있다.
→ DOPE 의 오차는 "이미지의 엉뚱한 데를 본다"가 아니라 **"이미 찾은 물체 영역 안에서
정밀도가 모자란다"** 는 것이다. spatial prior 로 손댈 여지가 원천적으로 없다.

### S1 이 no-op 인 것이 정상

frozen selector 가 이미 SQPnP+RefineLM 을 돌려 point 목적이 최적점이다. 그래서
S2 의 변화가 있었다면 bbox 때문임을 단정할 수 있었다 — control 이 비어 있어서 제 역할을 했다.

### S2 는 lock 이 경고한 함정조차 아니다

"목적함수는 줄고 GT 는 나빠지는" 패턴이 아니라 **predicted reprojection 마저 악화**
(3.55→3.75px). YOLO box 와 keypoint 가 서로 어긋나고, 둘 다 만족시키려 하면
keypoint 만 쓰는 것보다 진실에서 멀어진다.

### 작업 중 잡은 버그 4건 (전부 측정 전/중)

```
1 HypothesisResult 필드명을 pose_transform 으로 추측 → 실제는 rotation/translation_camera_facing
2 IoU3D 를 예측 extents 로 계산 → frozen evaluator 는 양쪽에 GT extents (0.5943 vs 0.6032)
3 lock 의 huber_scale 을 "1.0 after ..." 산문으로 써서 f_scale 예외 → 조용히 t0 반환,
  모든 arm 이 no-op 이 됨.  숫자만 파싱하고 예외를 세어 출력하도록 고침
4 solver 종료 허용오차 기본값(1e-8)이 정규화 잔차(~1e-4) 앞에서 즉시 수렴
```
★ 3번이 특히 위험했다 — `except Exception` 이 실패를 삼켜 "S2 효과 없음" 처럼 보였다.

### S5 는 시도하지 않았다

line artifact 가 전부 synthetic 모집단(D0_MH_SEEN512 / D2_MH_DEV512)이고 실제 319
프레임 line 캐시도, adapter 도 없다. 없는 대응을 지어내지 않고 BLOCKED 로 남겼다.

### DIMENSION_HEADROOM = WEAK

correct/zero/shuffled dims 가 갈리지 않았다는 기존 기록. 다만 probe 가 keypoint
국소화가 아니라 classifier/pose 결과를 쟀으므로 NONE 이 아니라 WEAK.
dimension-conditioned 학습 0.

### 적용범위

population 은 PAPER_EVAL positive 319 — **이미 개발에 쓰인 셋**이다.
held-out·independent·final 이라고 부르지 않는다. 이 screen 은 method selection 전용.

## [01:44:48] ⚠️  Auto Compact (컨텍스트 한계 도달)

- Working directory: `/home/minjae/Documents/github/pallet-pose`
- Trigger: `auto`
- Session ID: `483d110b-a1f3-46f9-a25c-292bdcf291d7`
- Transcript: `/home/minjae/.claude/projects/-home-minjae-Documents-github-pallet-pose/483d110b-a1f3-46f9-a25c-292bdcf291d7.jsonl`
- Note: 컨텍스트가 가득 차서 자동 트리거됨 — 다음 세션에서 이 마커 위쪽 작업을 반드시 복원할 것

**TODO (session resume)**: compact 이후 이 마커 위쪽의 작업 내역을 확인하고 이어서 진행


## FAST_6D_SCREEN_V1B — V1 의 두 사실오류 교정 (야간 자동 작업)

`data/pallet/results/paper_fast6d_screen_v1b/` · `scripts/paper/fast6d_screen_v1b/`
status = POST_STOP_EXPLORATORY_CORRECTION.  `_docs/paper/final/EXPERIMENT_STOP_LOCK.json`
은 수정하지 않았다.  V1 은 읽기 전용으로 그대로 둔다.

### 왜 V1B 를 열었나

1. **S2 의 bbox semantics 불일치.**  V1 은 투영된 8 코너 전부의 min/max 를 상자로
   썼는데(`run_translation_arms.py:71-73`), YOLO 가 학습한 상자는 화면 안 코너만의
   min/max 다(`build_real_ft_dataset.to_yolo_label`, 합성 규약과 400/400 일치).
2. **S5 의 provenance 오판.**  "real line inference path 가 없다" 는 내 판단이
   틀렸다 — `ft_f0f3_eval.py` 가 real 이미지에서 그 경로를 이미 수행하고
   checkpoint 두 seed 가 실재한다.  cache 부재는 arm 을 닫을 사유가 아니었다.

### 순서 (lock 먼저, 측정 나중)

```
6cb2b00  FAST_6D_SCREEN_V1B_LOCK.json + PURPOSE + supersession note  (측정 전 단독 push)
         geometry gate -> unit test -> line cache -> arms -> bootstrap -> verdict
```

### 게이트

- GEOMETRY_CONVENTION_GATE: model point 생성기 두 개가 좌표까지 동일(diff 0.0).
  GT pose 투영 vs 수동 어노 index-wise 중앙값 plastic 0.94px / wood 0.45px,
  90도 재라벨 대조군은 163/165px.  0~3 이 near face 인 비율 1.000.
  -> `line_population = ALL`, WOOD_LINE_STATUS = OK.
- LINE_FUSION_IMPLEMENTATION_GATE: `mh_fusion.run_tests` 를 손대지 않고 OUT 만
  V1B 로 우회해 실행.  T1~T7 이 과거 기록과 **비트 동일**, read-only artifact 무변경.

### 결과 (PAPER_EVAL positive 319, 개발용 셋)

```
arm                              R      Yaw    t cm     IoU3D    ADDsym   reproj
C0 frozen YOLO R0              2.262   1.231   7.897   0.6032   0.4285    3.55
C1 관측 semantics bbox + t      2.262   1.231   9.816   0.5372   0.3793    4.25
L3 line rot + t refit  seed1   4.680   2.371   8.431   0.5132   0.3623    6.47
L3 line rot + t refit  seed2   2.718   1.358   8.106   0.5879   0.4234    3.91
L4 yaw-only + t refit  seed1   3.048   2.035   7.891   0.6034   0.4194    4.51
```

C0 는 frozen `POSE_EVALUATION_R0` 와 절대오차 **0.00e+00** 일치(6 지표).
예외 0, unresolved 0, line 예외 0.  arm 이 실제로 움직였다 — C1 은 translation 을
7.90→9.82cm 옮겼고 line rotation 은 3.084°(seed1)/0.836°(seed2) 돌렸다.

### 판정

```
C1 = STOP    ΔIoU -0.0660, ΔADDsym -0.0492 (session CI 둘 다 0 배제)
L3 = STOP    두 seed 모두 음수, 조건 A/B/D 불충족
CORRECTED_FAST6D = NO_PROMOTABLE_SIGNAL   PROMOTED_METHOD_CANDIDATE = 없음
runtime benchmark 미실행 (사전등록 trigger 미충족)
```

### 남길 교훈

- **틀린 상자를 맞추던 V1 S2(ΔIoU -0.0216)보다 맞는 상자를 맞추는 C1(-0.0660)이 더
  나쁘다.**  predicted reprojection 도 같이 나빠졌으므로 과적합 함정이 아니라 bbox 와
  keypoint 가 서로 다른 물체 위치를 말한다는 뜻이다.  bbox 를 translation 단서로
  쓰는 방향이 닫힌다.
- **line 은 회전을 옮기지만 방향이 틀렸다.**  lambda 가 큰 seed1 이 더 크게 나빠진다
  = 더 믿을수록 더 나쁘다.  L4(yaw-only) 만 중립인데, line 이 pose 를 거의 못
  건드리게 했을 때만 손해가 사라진다는 뜻이다.
- 과거의 "theta-only line = 회전 이득" 과 모순되지 않는다.  그건 **약한 base(합성
  DOPE 코너)** 위의 이야기고, 여기 point cue 는 훨씬 정확한 YOLO R0 다.

## PAPER_FRAMING_CLOSURE_V1 — negative branch 자동 수행

`data/pallet/results/paper_framing_closure_v1/` · `scripts/paper/framing_closure_v1/`
C1·L3 가 모두 STOP 이므로 lock 의 negative branch 대로 새 후보를 만들지 않고
논문 framing 확정으로 넘어갔다.  새 추론 0 · 새 학습 0 · 새 threshold 0.

### §29 정적 감사에서 나온 것

- **G5 — 문서 간 불일치 발견(최우선).**  `_docs/paper/final/PAPER_CLAIM_LOCK.json`
  과 `LIMITATIONS.md §3` 은 `POSE_METRICS_STATUS = BLOCKED` 이고 "pose 열을
  표에서 제거했다" 고 적는데, `_docs/paper/final/generated/TABLE_FINAL_POSE.md` 에
  7 arm 6D 표가 이미 존재하고 `POSE_CLOSURE_STATUS.json` 은 second pass 에서
  `REPORTABLE` 로 뒤집혀 있다.  현재 상태는 second pass 가 맞다(차단 사유는
  selector 가 아니라 GT 축 부재였고, 얼린 규칙으로 geometry-resolved GT 를 만들어
  selector 를 안 건드리고 보고 가능해졌다).  **바뀌지 않는 것은
  `can_claim_6d_improvement = false`** — 24 개 session-cluster 구간이 전부 0 포함.
  claim lock 은 자율 작업이 편집하지 않으므로 고치지 않고 사용자 결정으로 남겼다.
- **G1 — ranking 구간 공백을 채웠다.**  `LIMITATIONS.md §8` 이 "구간이 artifact 에
  없다" 고 적은 것을 frozen per-frame 점수만으로 계산(정의는 `evaluate_arms.ranking`
  그대로).  `paired R5-R0 AUROC +0.00318 [+0.00009, +0.00690]` 0 배제,
  `FPR95 -0.01339 [-0.02566, +0.00558]` 0 포함.  negative 2,689 행의 session_id 가
  전부 빈 값이라 완전한 session-cluster 구간은 여전히 계산 불가 — lock 의
  UNAVAILABLE 판단은 옳았다.  이 구간은 점추정을 본 뒤 계산됐으므로 Tier B.
- G2 main 2D 표 정합 OK · G3 모집단 수 정합 OK(319 = 194+125, negative 2,689) ·
  G4 6D 표 정합 OK.  유일한 불일치는 R0_CONT 인데 **설명된다** — 318 프레임에서만
  짝지어지므로 그 비교 안의 R0 기준값이 0.60341(318)이고 표의 0.60318(319)과 다르다.
- filter 품질 신뢰구간은 `BLOCKED_MISSING_ARTIFACT` — FILTER_SEPARABILITY.json 에
  항목별 배열이 없어 다시 측정해야 하는데 그것은 새 계산이다.

### 산출물

```
PAPER_EVIDENCE_TIER.md · PAPER_MAIN_CLAIMS.md · PAPER_NO_CLAIMS.md
PAPER_TABLE_PLAN.md · PAPER_FIGURE_PLAN.md · PAPER_REVIEWER_GAP_AUDIT.md
PAPER_FRAMING_DECISION.md · PAPER_STATIC_STAT_AUDIT.json
data/pallet/results/OVERNIGHT_6D_DECISION_20260904.md   (야간 마스터 보고서)
```

`_docs/paper/final/` 은 이미 성숙한 framing 을 갖고 있어 **평행 문서를 만들지 않고**
그것을 가리키며 새로 채운 통계와 발견된 불일치만 더했다.  `EXPERIMENT_STOP_LOCK.json`
· `PAPER_CLAIM_LOCK.json` · `LIMITATIONS.md` 는 수정하지 않았다.

### 한 가지 기록해 둘 관찰

2D 표에서 유일하게 크게 움직인 것은 REALFT 계열이다
(`AP50-95 0.7688 → 0.8399`, `pooled keypoint median 6.616 → 5.990 px`).
자기-학습 일곱 arm 중 어느 것도 근처에 못 온다.  이것은 논문 서사를 **강화**하지만
REALFT 는 같은 현장·같은 팔레트 실제 라벨로 학습된 **상한선**이지 통제된 비교가
아니므로 adaptation arm 과 같은 열 블록에 넣지 않는다.

## PAPER CANONICAL SYNC — 최신 evidence 와 논문 문서 일치시키기

새 실험이 아니다.  새 학습 0 · 새 checkpoint 0 · 새 추론 0 · 새 metric 정의 0 ·
기존 result 숫자 수정 0.  READ ONLY namespace 는 git 기준 전부 무변경.

### 무엇이 어긋나 있었나

어젯밤 감사에서 찾은 것을 이번에 실제로 정리했다.  `PAPER_CLAIM_LOCK.json` 과
`LIMITATIONS.md §3` 등 8 개 활성 문서가 `POSE_METRICS_STATUS = BLOCKED` 라고
적는데, `generated/TABLE_FINAL_POSE.md` 에는 7 arm 6D 표가 이미 있었다.

**현재 맞는 것은 second pass 다.**  차단 사유는 selector 가 아니라 **GT 물리축의
부재**였고, 결과를 보기 전에 얼린 규칙으로 geometry-reconstructed reference 를
만들면서 selector 를 전혀 건드리지 않고 6D 지표가 열렸다.

```
POSE_METRICS_STATUS       BLOCKED -> REPORTABLE
can_claim_6d_improvement  false (그대로)
```

둘을 혼동하지 않는다 — 측정 가능성과 개선은 다른 진술이다.

### amendment 방식 (과거를 지우지 않았다)

`PAPER_CLAIM_LOCK.json` 의 `pose_metrics` 를 다시 썼되 first-pass BLOCKED 블록을
`pose_metrics.historical_first_pass.original_block` 에 **원문 그대로 보존**했다.
"없었던 일" 처럼 지우지 않는다.

### 숫자는 전부 artifact 에서 읽었다

```
6D    R0 R 2.2625 · yaw 1.2306 · t 7.8969cm · IoU 0.60318 · ADDsym 0.42847
      R5 R 2.5345 · yaw 1.2938 · t 8.8265cm · IoU 0.58677 · ADDsym 0.40010
      개선 방향으로 session-cluster 구간이 0 을 배제한 metric block  0 / 24
ranking  paired R5-R0 AUROC +0.003180 [+0.000092, +0.006898] 0 배제
                          FPR95 -0.013388 [-0.025660, +0.005578] 0 포함
         session-cluster 는 negative 행에 session_id 가 없어 여전히 계산 불가
2D    R0 6.6157px · 최고 adapted(R4) 6.9987px · R5 7.2099px
site  A8_DAY_ONLY vs R0, 88 프레임 / 7 cluster — 네 지표 모두 구간이 0 포함
```

출처는 `_docs/paper/final/PAPER_CANONICAL_NUMBER_SOURCES.json` 에 파일·키 단위로.

### ★ 내 어제 진술 하나를 정정했다

어젯밤 V1B 보고에서 "line 은 lambda 가 클수록 더 나빠진다" 라고 썼는데 **과한
진술**이었다.  seed1 은 lambda 3.0, seed2 는 lambda 1.0 이라 **seed 와 lambda 가
confounded** 다.  쓸 수 있는 것은 "두 historical seed-specific 구성이 모두 강한
YOLO 기준선을 개선하지 못했고, 더 큰 악화는 seed-1 구성" 까지다.
`PAPER_MAIN_CLAIMS.md` · `PAPER_NO_CLAIMS.md` · memory 를 전부 고쳤다.
(사용자 지적 — §15)

### site-matched 상태 종결

소규모 arm `A8_DAY_ONLY` 는 **이미 평가됐다**(88 프레임, recording cluster 7).
full-site 2,227 수량 확대 학습은 `NOT_RUN_AND_NOT_PLANNED` — 새 과학적 질문이
아니라 pseudo-label 수량 확대이고 method search 종료 이후 실행하지 않는다.
"유일하게 남은 실험" 표현을 폐기하고 `.pending.md` 항목도 닫았다.

### generated 파일은 손으로 안 고치고 generator 를 고쳐 재생성

```
build_final_paper_summary.py            -> TABLE_FINAL_1.md
                                           265 개 숫자 전부 동일, 캡션 한 문단만 변경
build_pose_tables.py                    -> TABLE_FINAL_POSE*.md + oracle 진단
                                           숫자 행 전부 동일, 헤더·주석·타임스탬프만 변경
build_experiment_tables.py              캡션 수정.  출력이 _docs/archive 라 재생성 안 함
```

### 감사와 테스트

새 스크립트 `scripts/paper/framing_closure_v1/canonical_sync_audit.py`.
stale 문자열을 활성/역사 namespace 로 나눠 훑고 ACTIVE_FIXED / HISTORICAL_KEEP /
FALSE_POSITIVE / NEEDS_USER 로 분류한다.  "이 표현을 쓰지 말라" 고 적은 금지 목록
줄과 일부러 남긴 historical 블록, 테스트 픽스처, 감사 자신의 출력은 false positive
로 걸러진다(이것 때문에 세 번 다시 돌렸다).

```
ACTIVE_FIXED 24 · HISTORICAL_KEEP 15 · FALSE_POSITIVE 19 · NEEDS_USER 0
정적 테스트 12 / 12 PASS
```

### 최종 canonical state

```
EXPERIMENTATION_STATUS   STOPPED       POSE_METRICS_STATUS  REPORTABLE
CAN_CLAIM_6D_IMPROVEMENT FALSE         FULL_SITE_TRAINING   NO
NEW_METHOD_CANDIDATE     NONE          WOOD_POSE            INCLUDED (194 + 125 = 319)
PAPER_FRAMING            CONTROLLED_EMPIRICAL_DIAGNOSTIC_STUDY
NEXT_ACTION              PAPER_WRITING
```
