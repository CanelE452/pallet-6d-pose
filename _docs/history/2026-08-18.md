# 2026-08-18

## 세션이 08-16 → 08-18 로 이어짐 (같은 작업 흐름)

이 세션은 08-16 밤에 시작해 이틀치 자정을 넘겼다. **경위·실측표·오진 기록은 전부
`_docs/history/2026-08-16.md`** 에 있다(복붙하지 않고 가리킨다). 관련 절:

- "★ engine 은 task=pose 없이 로드하면 검출 0% — Jetson 배포 블로커"
- "3080 실측 — PyTorch vs TensorRT" / "Orin Nano 추정"
- "base(합성 전용) 모델 — 손 어노 real 전수 평가"
- "정본 real 342장 — DOPE(paper_s1/s2) vs YOLO26n 동일지표 비교"
- "night 대비 케이스 시각화" / "사용자 지정 2프레임 — s2 vs yolo"
- "vdoframes 4 프레임 검출" / "vdoframes 높이 역추정 — f 와 H 가 얽히는가"
- "internet_pallet 10장 — yolo26n base vs ft A/B"

여기에는 **변경 파일 4개의 Before/After 만** 남긴다.

## 1. `challenge/pallet_jetson_deploy/infer_fps.py` (수정)

Jetson 배포용 standalone. **그리기 경로 + 모델 로딩만** 손댔고 PnP·keypoint 순서·3D
모델·dims 등 "검증된 로직" 은 건드리지 않았다.

```
항목                    Before                      After
──────────────────────────────────────────────────────────────────────────────
모델 로딩               YOLO(args.model)            YOLO(args.model, task="pose")
yaw 각도                없음                        yaw_from_R()
roll/pitch              없음                        roll_pitch_yaw_from_R()
방향 표시               draw_axes XYZ 3축 0.5m      + draw_yaw_arrow (local -Z, 2.0m)
draw_axes 시그니처      (img,R,t,K,length)          + viewer_axes, labels
  └ Y 축                local +Y (OpenCV down)      viewer_axes 면 -Y (up)
  └ Z 축                local +Z (far, 뒤)          viewer_axes 면 -Z (front)
자세 위젯               없음                        나침반 + roll/pitch 자세계 (우하단 고정)
표시 모드               단일                        --yaw-only 플래그 (기본 동작 불변)
```

**이유**

1. **`task="pose"` 가 최우선 수정이다.** `.engine`/`.onnx` 는 메타에 task 가 없어
   ultralytics 가 detect 로 가정하고, 그러면 `r.keypoints` 가 None 이라 **검출률이
   0% 로 떨어진다**. `.pt` 는 멀쩡해서 로컬 테스트로는 절대 안 잡히고 Jetson 에서
   engine 을 물리는 순간에만 터진다. 3080 TRT FP16 에서 det 0/400 → 383/400 으로 회복.
2. **화살표 방향** — 이 파일 모델은 near face(0~3, fork-pocket) = `z=-d/2` 라
   팔레트 정면이 **local −Z** 다. 기존 파란 +Z 축은 far 쪽이라 늘 소실점 방향으로
   뻗었다(사용자 "카메라 기준 뒤로 보인다"). 실측: near 평균 z 2.36m vs far 3.61m.
3. **화살표 길이 0.6 → 2.0m** — 0.6m 일 때 553 프레임 중 65.8% 가 14px 미만이라 ⊙ 로
   그려졌다. ⊙ 그룹의 |yaw|·앙각·거리가 전체와 **동일**했으므로 원인은 각도가 아니라
   거리(median 3.72m) 대비 짧은 길이였다.
4. **오일러 순서 = 내재 Y-X-Z** — 이 순서에서만 `atan2(R02,R22) == yaw` 가 항등이라
   기존 `yaw_from_R`(= deployment psi) 과 값·부호가 같다(uniform SO(3) 200k 대조
   0.000e+00). 다른 순서면 `align.py` 의 `psi>0 → ROT_RIGHT` 분기가 깨진다.
5. **나침반 위젯** — 정면 벡터의 화면 투영은 카메라를 정확히 향할 때 길이가 0 이라
   방향이 정의되지 않는다(원리적 한계). 화면좌표계 위젯으로 빼면 원근과 무관하다.
6. **docstring 정정** — 내가 08-15 에 쓴 "psi>0 → 팔레트 정면이 카메라 우측" 은
   반대다. 정면 법선은 **좌측**이고 지게차가 **우회전**해야 한다.

## 2. `challenge/yolo_pose_one_model/scripts/eval_real_all.py` (신규)

손 어노 real 전수 평가(폴더별 검출률 + keypoint 중앙오차). `eval_ft_fp.py` 의
`predict_batch`/`gt_kps` 재사용. FT-SEEN(finetune 학습 포함 세션) 표시로 in-sample 구분.

**1차 실행이 오염됐다** — `01_real/*/*` 를 전부 glob 해서 손 어노가 아닌 폴더가 섞였다:
`pallet11_gt`(AprilTag 자동 GT, kp med 499.72px 로 튀어 발각), `capturepallet07_augmented`
(증강본, GT 에 반쪽 -1 sentinel: x=-1 인데 y=314). `[-1,-1]` 만 거르는 필터로는 반쪽
sentinel 이 안 걸린다. **`_manual_gt` 접미사가 손 어노 구분자**라 glob 을 좁혀 해결.

결과(플라스틱 342장, conf 0.4): det 89.2% / kp med 9.07px / p90 28.25px.

## 3. `scripts/stage0/paper_s2/canonical_det_kp_add.py` (신규)

DOPE(paper_s1/s2/base_v2) vs YOLO26n(base/ft) 을 **같은 셋·같은 후처리**로 비교.
지표 = det rate(corner>=6 AND PnP) / kp err<20px / ADD(m) / yaw.

- **셋은 정본만.** 기존 `paper_s2_real_eval.py` 의 filterval N123 은 `data/_eval_sets/`
  기반인데 CLAUDE.md 가 평가 금지로 지정한 구본이다(memory: 구본에서 뽑아 판정 4건이
  뒤집힌 이력). `challenge/data/01_real/*_manual_gt` 에서 main 237 + final-test 105 로 재구성.
- **eval-parity 유지**: s2=squash(400x400) / s1·base_v2=aspect no-pad / YOLO=reflect pad 100.
  전처리만 모델별이고 그 뒤 경로는 완전히 동일.
- **ADD 전제 검증**: GT `pose_transform` + `make_pallet_keypoints_3d(W,D,H)` 로
  projected_cuboid 가 0.844px(max 2.05)로 재현. W/D 를 바꾸면 9.31px → object frame 일치 확인.
- **env 분리**: DOPE=pallet-pose / YOLO=pallet-yolo26(구버전 ultralytics 가 yolo26 못 읽음)
  → `--part dope|yolo|report` 2단계 실행 구조. `E.hungarian`/`E.K_from_json` 은 env 독립
  로컬 함수로 대체(DOPE 모듈을 YOLO env 에서 import 못 함).

**부수 발견**: `stage25_paperbase_eval.py` 의 `ECAD_PATH` 가 죽어 있다. 가리키는
`stage16_truncation_addon/` 이 `eval_results/achieve/multi_model_comparison/` 아래로
옮겨져 지금 그 스크립트를 import 하면 FileNotFoundError 다.

## 4. `challenge/yolo_pose_one_model/scripts/internet_pallet_yolo_ab.py` (신규)

인터넷 팔레트 10장에 base vs ft A/B. `scripts/stage0/internet_pallet_infer.py`(s2 전용,
DOPE env)의 **파일명 치수 파싱**과 **K 미지 → HFOV 스윕 focal 추정** 두 함수를 복제했다
(그 스크립트는 pallet-pose env 전용이라 import 불가).

판정: base 우세 4 / ft 우세 3 / 동률 2 = **유의한 차이 없음**. det 9/9 동일.
미검출 1장은 쇼핑몰 상세페이지 캡처라 팔레트 사진이 아니다(모델 실패 아님).

## 미해결 (다음 세션)

- `PALLET_DIMS=(1.1,1.3,0.11)` 이 contract target_synth `(1.30,1.10,0.12)` 와 어긋난다.
  188 프레임 실측에서 contract 쪽이 127/188 최적(reproj 4.24 vs 5.79px)이고 그 차이가
  yaw 에 median 3.22°/max 7.26° bias 로 실린다. 프레임별 두 후보를 풀어 reproj 낮은 쪽을
  고르는 게 정공법 — 사용자 요청 범위 밖이라 미수정.
- pitch/roll 에 카메라 마운트 틸트가 섞여 있다(하향각 미캘리브). yaw 는 무관.
- `pose6d_adapter.py:74`(Korea-Railroad-project)에 같은 오류 문구 — 다른 repo 라 미수정.
- `stage25_paperbase_eval.py` ECAD_PATH 死경로.
- **Orin Nano 실측 미완** — 추정(40~65 FPS)만 있다. TODO 가 2026-05-14/05-27 부터
  열려 있다. engine 은 하드웨어 종속이라 그 보드에서 빌드해 `infer_fps.py` 를 돌려야 한다.
- vdoframes 거리(z) 추정 불가 — f 가 프레임별 730/491/1167 로 결정 불가. 높이 H≈0.14m 는
  f 와 무관하게 결정된다(weak-perspective affine 폐형식으로 독립 확인).

## ★정정 — PnP cuboid 왜곡의 원인은 내 그리기 버그였다

사용자: "점은 제대로 찍혔는데 왜 PnP가 제대로 안 되지?" (`[1016×1220×120] 목재 파렛트`)

**내 1차 진단이 틀렸다.** "keypoint 높이비가 실제보다 37% 과대(화면 0.162 vs 실제 0.118)라
PnP 가 그 모순을 자세 기울임으로 흡수한다" 고 답했는데, 높이비 수치 자체는 맞지만
왜곡의 원인이 아니었다.

**진짜 원인** (`challenge/yolo_pose_one_model/scripts/internet_pallet_yolo_ab.py:draw()`):

```
Before   X = APNP.make_pallet_keypoints_3d(*dims)[:8]        # 파일명 dims
After    X = APNP.make_pallet_keypoints_3d(*pose["dims"])[:8] # pose 가 실제로 쓴 dims
```

`solve_pose(auto_swap_dims=True)` 는 as-given 과 W/D swap 을 모두 풀어 reproj 낮은 쪽을
고른다. 반환된 `(R,t)` 는 **선택된 `pose["dims"]` 에만 맞는다**. 그런데 그리기 쪽은
파일명 dims 로 상자를 다시 만들어 투영했으니 pose 와 다른 모양을 그렸다 —
큐보이드가 39px(최대 82px) 어긋났다.

**헤더의 reproj 5.49px 은 정상값이었다.** PnP 가 푼 자세의 오차이고, 그림만 다른 상자를
그리고 있었다. 즉 **숫자와 그림이 따로 놀았고**, 나는 그 불일치를 모델·keypoint 탓으로
돌렸다. `auto_swap_dims` 가 dims 를 바꿀 수 있다는 걸 알고도(그 직전 진단에서 스왑을
배제하려 직접 확인했다) 그리기 경로가 같은 dims 를 써야 한다는 데 생각이 닿지 않았다.

**교훈**: 지표(reproj)와 시각화가 어긋나면 **둘 중 하나가 다른 입력을 쓰고 있는지부터**
본다. 모델을 의심하기 전에 파이프라인 내부 일관성을 먼저 확인해야 했다.

수정 후 재실행: base 의 큐보이드가 팔레트에 맞는다(눈 검증). ft 는 여전히 어긋나는데
이쪽은 그리기가 아니라 keypoint 자체가 부정확한 것이다(reproj 7.88px) — **base 우세
판정은 유지**. 10 장 산출물 전부 재생성했다.

(3d-expert 위임은 API 529 로 중단됐으나 이 수정은 그 진단에서 나왔다.)

## 모델 파라미터 실측 + claim 범위 판정 (코드 변경 없음, 결정 기록)

### 파라미터 — 체크포인트에서 직접 셈

앞서 초록 논의에서 "3M" 이라고 어림으로 말했다가 검증했다.

```
모델                            파라미터        파일
──────────────────────────────────────────────────────
yolo26n-pose  base(synth)      3,043,704       6.2 MB
yolo26n-pose  ft(real+neg)     3,043,704       6.2 MB   (구조 동일, 가중치만 갱신)
yolo26m-pose  stage_a          23,543,044      90.6 MB  (nano 대비 7.7배)
```

`requires_grad` 가 0 으로 나오는 건 추론 로드 시 ultralytics 가 고정하기 때문이며 정상.

### "foundation model 이라 불러도 되나" → **불가**

사용자 질문. 정의 6 축 어디로도 닿지 않는다: 학습 데이터(합성 74k + real 400 vs 웹 스케일),
다양성(팔레트 1종 vs 무제한), 태스크(6D pose 단일 vs 다운스트림 범용), zero-shot 평가
(인터넷 9장 GT 없음 vs 벤치마크 수십 개), 파라미터(3.04M vs 수억~수십억), 조건부 일반화(불가).

**내가 앞서 "일반화 확인" 이라고 보고한 것의 실제 무게도 정정했다**: n=9(실질 인스턴스 4~5종),
GT 없어 정량 불가, pose 는 실제로 틀렸던 상태(그 직후 그리기 버그로 판명), 실내 창고에서는
4장 중 1장 오검출. 말할 수 있는 건 **"detection 이 처음 본 팔레트로 전이된다"** 까지고,
**"6D pose 가 일반화된다"는 근거가 없다.**

쓸 수 있는 표현: `cross-instance generalization` / **`category-level pose estimation`**
(NOCS 계열 표준 용어, 지금 목표와 정확히 일치) / `zero-shot detection transfer`.
쓰면 안 되는 것: `foundation model` / `general-purpose` / `universal`.

### 초록 강도 — master §0/§3.6 이 이미 잠가 놓았다

`paper_strategy_master.md` 확인 결과 **[LOCKED] 로 이미 결정돼 있다**:

> claim 범위 = B (단일 파렛트). 미지지 = 새 모양 파렛트 → limitation + T1 future work.
> **헤드라인 "처음 본 파렛트" 는 그대로 못 씀**

근거(§3.6, 시각 아님 파일계보): capturepallet/night 전 세션 GT 치수가 `1.3×0.11×1.1m`
동일 = palletobj 동일 개체. 지지되는 건 (가) sim(인터넷 무료 파렛트)→real 단일 파렛트,
(나) cross-session 강건성뿐이다.

초록에서의 강도 3 단계로 정리했다:

```
[A] 지금 가능    "detection transfer to unseen pallet instances ...
                 pose accuracy on unseen geometries remains unvalidated"
                 근거 = 오늘 인터넷 9장 A/B (conf 0.92~0.99, det 9/9)
[B] GT 만들면    "ADD X cm on unseen pallet instances spanning four dimension classes,
                 without any real-data finetuning"  ← 목표
                 필요 = 인터넷 사진 손 어노 (치수가 파일명에 이미 있음, 1~2시간)
[C] 불가         foundation model / generalizes to any pallet / universal
```

배치: main claim 은 suppression filter(§1.4)이므로 unseen 은 **마지막 보조 문장**이어야
한다. 헤드라인으로 올리면 리뷰어가 unseen 평가표를 요구하고 논문 전체가 그 축으로 심사된다 —
지금 그 표가 없다.

**T1 이 가깝다**: 오늘 만든 `internet_pallet_yolo_ab` 산출물이 바로 T1(unseen-pallet
테스트팩)의 재료다. 치수가 파일명에 있어 PnP GT 를 만들 수 있고 `scripts/annotate/annotate.py`
가 이미 있다. 다만 **ADD 가 나쁘게 나올 가능성을 각오해야 한다** — 그게 진짜 숫자이고,
지금처럼 "된다고 믿고 안 재는" 상태가 더 위험하다.
