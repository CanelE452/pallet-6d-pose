# 2026-08-13

## [00:0x] partial-edge supervision ablation — B0 학습 진행 중 (날짜 넘어감)

이 세션의 코드 변경(신규 `scripts/stage0/partial_edge_supervision_ablation.py`,
mask-factor proof, P0 reuse qualification, span bin lock, 판정 gate)은 전부
**2026-08-12 23:2x 에 커밋 `a6a3f80`** 으로 기록됨 — 상세는 `2026-08-12.md`
"partial-edge supervision ablation lock" 섹션 참조(여기 복붙하지 않음).

날짜가 넘어간 뒤 이 파일에는 **실행 상태만** 남긴다. 코드/설정 추가 변경 없음.

### 실행 상태
```
프로세스   PID 2787231 (setsid nohup, detached), GPU 2246 MiB
러너       scripts/stage0/partial_edge_supervision_ablation.py train
학습 arm   B0_FULL_ONLY 만 fresh (B1 = historical P0 재사용, QUALIFIED)
로그       scratchpad/b0.log
```

### 중간 mark (판정 아님 — 결정은 @25,545 단독, D2 전체이지 T0/T1 층화도 아님)
```
step     B0 angle  B0 offset  B0 p90   B0 L_sup   roles sup   (참고) B1=P0 angle/offset
──────────────────────────────────────────────────────────────────────────────────────
 1,703    6.0385    5.0483    51.252   7.899219    136,683     5.4846 / 4.5703
 5,000    3.2516    1.9807    18.500   6.487561    401,440     3.1396 / 2.0944
```
- `L_sup` 이 B1 과 근사한 스케일에서 시작 = mask 가 평균의 모집단만 바꾸고
  스케일은 안 바꿨다는 것과 정합(rescale 금지 조건 유지).
- @5,000 에서 angle 은 B0 열세, offset·p90 은 B0 우세로 방향이 갈림 → 해석 보류.
- 종료 예상 02:45 경 **[추정: 선형 외삽]**.

다음: 완료 후 `evaluate` → T0/T1 층화 median · span bin · role별 → 사전등록 gate A/B/C 판정.

## [02:35] partial-edge supervision ablation — 결과

`evaluate` 완료. 결과 `partial_supervision_result.json`,
정리 `_docs/audits/.../edge_mandatory_fast_search/PARTIAL_EDGE_SUPERVISION_RESULT.md`.

### 판정 (사전등록 gate, 변경 없음)
```
A 두 median ≥15% 개선   +24.88% / +18.35%          PASS
B both-gate ≥ +10pp     +1.90pp                    FAIL
C T0 열화 ≤10%          angle -8.29% / offset +11.20%  FAIL
DECISION                PARTIAL_EDGE_SUPERVISION_NOT_USEFUL
```

### ⚠ 사전등록 결함 (먼저 보고)
등록한 4분기(A+B+C / A+B&C실패 / A실패 / 양쪽악화) 중 **관측 상태(A통과·B실패)에
해당하는 분기가 없음** → 러너 else 로 NOT_USEFUL 이 나옴. 라벨은 결과 본 뒤 못 바꾸므로
유지하되, **"효과 없음"으로 읽으면 틀림**. 원인: B 가 both-gate(1.0°+0.5cell 동시)
절대치 3.72%/5.62% 인 population 에 +10pp 를 요구 — 이 stage 가 도달한 적 없는 수준이라
어떤 결과로도 판별 불가능한 bar 였음. 사후 수정 금지, 결함으로 기록.

### T1_PARTIAL (n=3,737) @25,545
```
              angle med  offset med  ang p90  off p90  both-gate
B1 T1 감독     2.391045   1.544678   14.0633   6.0836    5.62%
B0 T1 제외     3.182896   1.891941   17.3562   7.7085    3.72%
개선            +24.88%    +18.35%   +18.97%  +21.08%   +1.90pp
tail   >5° 34.44→26.84% · >10° 16.72→13.09% · off>2 47.82→39.23%
role별 angle 12/12 개선, offset 11/12 (role 7 offset -6.62% 만 손해)
```

### span bin (사전 고정, gate 아님) — 덜 남을수록 이득 큼
```
chord/span    n     B0ang→B1ang     B0off→B1off    offset 개선
0.00-0.25    302   4.4951→3.7457   3.4834→2.0500    +41.1%
0.25-0.50    520   3.7202→2.6742   2.5898→1.5052    +41.9%
0.50-0.75    967   3.5677→2.4727   2.0783→1.5369    +26.0%
0.75-1.00   1948   2.8244→2.1782   1.5783→1.4815     +6.1%
```

### 비용 — T0_FULL (n=23,936)
```
angle med 2.0137→1.8467 (+8.29%)   offset med 0.8613→0.9577 (-11.20%)
role별 angle 9/12 개선 · offset 11/12 악화 → 노이즈 아님
```
T1 감독이 T0 의 offset 캘리브레이션을 약 1/10 밀고 angle 을 얻는 교환.
**단, 한 factor 가 두 population 을 동시에 바꿔서 "추가 감독" 때문인지
"loss 모집단 변화" 때문인지 분리 안 됨** — 이 screen 은 그 답을 못 냄.

### provenance
```
B0 fresh  sha 3eab4f750dffae6a551b9842f992804a240433c822db4581c5d744514dd51138
B1 = P0   sha b37dfc2617a7e0f0fb13f1c549f2377459ba9b20477caacedc2d68a6fec2e43c
seed 1 · sigma 1.5 · split sha 70ba7f1e · 두 arm 모두 25,545
```

### 해석 제한 (결과와 무관하게 유지)
EXTERNAL/SELF_OCCLUSION_LEARNABILITY · HIDDEN_CORNER_RECOVERY ·
CIGM_RECOVERY · POSE_IMPROVEMENT = 전부 False. OCCLUSION_LABEL_SOURCE=MISSING.
여기 모든 edge 는 **이미지 경계에 의한 truncation** 이고 category 는 segment
clipping 산출 — occlusion 은 어디서도 평가하지 않았다.
architecture NOT_LOCKED / replicate · role shuffle · whole LINE_DEV · CIGM BLOCKED.
`validation512`·`untouched`·`eval56`·`wood45`·final-test 미개봉.

문서 신규: `PARTIAL_EDGE_SUPERVISION_RESULT.md`. 코드 변경 없음(러너는 어제 a6a3f80).
