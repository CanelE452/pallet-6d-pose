# 2026-08-13

## [22:2x] coefficient lock — ONE_PASS_GRADIENT_BALANCED_L2SP (실행 전)

### 0. HARD_BLOCK provenance push
`d543529` 가 local-only 1개이고 내용이 (adapted A1 weight 부재 / calibrate·run HARD_BLOCK /
913 tests) 임을 확인 후 push. local == origin == `d543529` **[확인]**. amend·rebase·squash 없음.
이 commit 은 실패가 아니라 `HISTORICAL_SP_CALIBRATION_REFERENCE_MISSING` provenance 로 유지.

### 1. coefficient-lock addendum
`L2SP_COEFFICIENT_LOCK_ADDENDUM.md` 신규. **철회**: "historical F1 @1,703 W 로 R_SP_ref 계산"
(adapted W 가 checkpoint 에 없음 — 92개 .pth 실측). **금지 표현**: "fresh calibration 이 historical
@1,703 을 재현" / "historical W 를 근사 복원". 학습경로 비재현성(20 step 1.435e-3) 기록되어 있으므로
fresh 1,703 은 **다른 W**. 이름 `SP_CALIBRATION_STATE_1PASS` (≠ `HISTORICAL_F1_1703`).

### 2~4. 새 규칙과 수치체제 분리
```
λ_sp = ‖g_task‖₂ / ‖g_sp‖₂     (λ=1 기준 R_SP gradient)
근거: 항의 optimizer 영향력은 값이 아니라 gradient. loss magnitude 균등화는 실제 force 를 안 맞춤
W=W0 에서 R_SP=0, ∇R_SP=0 → step0 calibration 불가 → 1 pass(1,703 step) task-only state 필요
CALIBRATION_NUMERICAL_REGIME = deterministic (계수 재현성·감사)
ACTUAL_TRAINING_NUMERICAL_REGIME = default   (F1 과 비교 가능성)
두 역할이 다르므로 의도적으로 다름 — 섞으면 계수가 재현 불가하거나 비교가 무효
```

### 6. SP 정식화 (module 단위)
conv l 마다 numerator = ‖W−W0‖²_F + ‖b−b0‖²  /  denominator = ‖W0‖²_F + ‖b0‖² + eps.
weight·bias 가 **denominator 를 공유**하므로 작은 bias norm 이 penalty 를 지배 못 함
(per-tensor 정규화면 가능했음). R_SP = 4개 late conv 의 mean.

### 7~10. calibration 결과 **[확인 — 실제 2회 실행]**
```
‖g_task‖ (full LINE_TRAIN 13,618 frame 누적, optimizer step 없음)  1.3439385890960693
‖g_sp‖   (λ=1)                                                    0.008939512073993683
gradient cosine                                                   -0.0011302164057269692
R_SP_cal 0.04438373073935509    CE_cal_fulltrain 7.508383416520259
λ_sp = 150.3369063067523   (반올림 없음, full precision JSON)
재현성: 2회 실행 relative difference **0.000e+00**, task/sp grad 동일, checkpoint sha 동일
leakage guard: D0/D2/validation512/untouched/eval56/wood45/final-test 정적 검사 **clean**
```
★ **cosine ≈ −0.0011** — calibration state 에서 SP gradient 가 task gradient 와 사실상 직교.
기록만 하고 해석하지 않음.
calibration checkpoint `sp_lambda_calibration_step_01703.pth` 저장(late A1·decoder·optimizer·
seed·schedule·deterministic flag·sha). **S1 초기화에 사용 금지**, λ 감사·재현 전용.
run 은 sha 대조 후에만 진행(`locked_coefficient`).

### 12~16. preflight 전부 PASS
```
step0    F50 0.000e+00 · descriptor 0 · logits 0 · task loss 0 | R_SP 0.000e+00
gradient R_SP@step0 0 (정상). 1 step 후 ‖g_task‖ 7.385e-04, ‖λ·g_sp‖ 1.407e-02, cos −0.2989
memory   batch 8 유지, peak 1,192.2 / 9,853 MiB
plan     trainable late 5,014,912 · A1 LR 1e-4 · decoder LR 1e-3 · WD 1e-4 (F1 parity)
```
G1~G6 및 G3 임계(≤+10% 열화 AND 두 ratio 20% 이상 1 에 근접)는 `d543529` 정의 **불변**.
계수를 보고 수정하지 않음.

`scripts/stage0/regularized_late_a1_l2sp_run.py` 신규(기존 blocked runner import·재사용, 무수정).
테스트 23 pass. 전체 936 passed.

★ 테스트 결함 2건 자체 수정: sealed 토큰 검사가 **차단 목록 `FORBIDDEN_IN_CALIBRATION` 자신**에
걸림 → 그 리터럴 제외. write_text 추출이 지역변수 경유 경로를 놓침 → alias 해석 추가.

## [23:3x] 결과 — REGULARIZED_LATE_A1_OVERCONSTRAINED (G4)

**decision @25,545 D2 (raw JSON 재계산), λ=150.3369063067523 고정**:
```
                angle med   offset med   angle p90   offset p90   gate
S1 F1+L2-SP      2.615707     1.213144   12.913452     5.090273   0/4 FAIL
F1 historical    2.070244     1.077348    9.702057     4.125696   0/4 FAIL
G3 두 조건       degradation ≤+10%  → angle **+26.35%** / offset **+12.60%**  미달
                 gap closure ≥20%   → angle **73.96%** / offset **48.39%**   충족
→ 하나만 충족 → G4
```
S1 ladder:
```
 1,703 D0 6.2304/4.4017 | D2 6.6519/4.7551 | task 7.912153 R_SP 0.001460 λR_SP 0.219440 | dW/W 0.0385 F50 0.7116 cos 0.8054 | D2/D0 1.068/1.080
 5,000 D0 3.2730/1.9144 | D2 3.4913/1.9643 | task 6.432412 R_SP 0.002737 λR_SP 0.411480 | dW/W 0.0517 F50 0.9187 cos 0.7013 | 1.067/1.026
 8,515 D0 2.7871/1.4737 | D2 2.9888/1.5381 | task 6.050255 R_SP 0.003497 λR_SP 0.525677 | dW/W 0.0574 F50 0.9217 cos 0.6798 | 1.072/1.044
17,030 D0 2.5009/1.2045 | D2 2.7447/1.3356 | task 5.713382 R_SP 0.004291 λR_SP 0.645090 | dW/W 0.0635 F50 0.9234 cos 0.6387 | 1.098/1.109
25,545 D0 2.3548/1.0508 | D2 2.6157/1.2131 | task 5.581613 R_SP 0.004659 λR_SP 0.700482 | dW/W 0.0666 F50 0.9303 cos 0.6168 | 1.111/1.155
tail @25,545  >5° 0.2799 · >10° 0.1321 · >2cell 0.3143   n=5,921   finite=True
```
- **기전은 설계대로 작동**: weight drift 3.85%→6.66% 로 억제, **F50 drift 는 5,000 이후 0.92~0.93 에
  고정**되는데 cosine 은 0.805→0.617 로 계속 하강 = 가중치가 **거의 일정 거리에서 회전**.
  SP 항 비중 2.7%→10.1%. drift 가 늘면 penalty 가 커져 되당기는 구조가 실제로 평형을 만듦.
- L1(low-rank) 대비 F50 이동이 **4배 보수적**(0.9303 vs 3.9068, cos 0.617 vs 0.372).
- ★ **6개 arm 을 정확도 순으로 놓으면 특화 비가 역순**:
```
                  angle med   D2/D0 angle
F1 late unfreeze   2.070244     1.4255
L1 low-rank        2.450502     1.2336
S1 F1+L2-SP        2.615707     1.1108
R1 role depth      2.909729     1.0461
F2 adapter         3.084991     1.0445
F0 frozen          3.735687     1.0691
```
  F1 정확도에 가까워질수록 F1 의 특화를 더 가져온다. **6개 run 의 관찰이지 법칙 아님, 인과 미규명.**
- per-role: angle 에서 F1 을 이긴 role **0개**. offset 은 role 7·9 만(둘 다 F1 offset 이 유난히 나빴던 곳).
  비용이 high-angle 그룹에 집중 — 2/4/6/7 −34.5% vs 3/9/10 −10.9%.
- **금지 표현 준수**: "λ 가 너무 크다"·"drift 가 특화의 원인"·"L2-SP 가 adaptation 을 보편 해결"
  **쓰지 않음**. JSON 의 `CAUSAL_LIMIT` = "explicit anchoring changes the accuracy and
  generalization tradeoff; drift is not shown to cause specialization".
  SCOPE = "this fixed calibration only; lambda is not called large".

**Standing**: architecture **NOT_LOCKED**. replicate·role shuffle·whole LINE_DEV 전부 **BLOCKED**(G1 아님).
CIGM/PnP/dimensions/K-pose BLOCKED. 산출 `l2sp_result.json`, 문서 `L2SP_RESULT.md`.
