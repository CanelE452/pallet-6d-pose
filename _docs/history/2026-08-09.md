# 2026-08-09

## [07:32] direct-Hough role heatmap 사전등록 + 4 oracle — 전부 PASS

image-space map 계열 종료(`0f41954`, ROLE_CONDITIONED_GLOBAL_MAP_FAIL, best 4.1793°/1.8788cell)
이후, raster 를 없애고 line parameter space 자체를 출력/감독으로 쓰는 계열로 전환.
커밋 `620bda9` (실행 전 lock).

### 변경 파일
- **신규** `scripts/stage0/direct_hough_role_heatmap.py`
  - encoder 는 Q1 그대로(frozen A1 F50 + `ARCH.AbsoluteXY` + `RQ.RoleQueryGlobal` 의
    12 role query·cross-attention). **descriptor 이후만** 교체.
  - Before: descriptor → map residual → MAP100 raster → Hough decoder → line
    After : descriptor → hypothesis embedding 과 bilinear score → argmax → (θ,ρ)
  - `SupportingLineHead` / `map_loss` / MAP100 logits 미사용. theta·rho 별도 head 없음
    (단일 joint score), scalar regression 금지.
  - lattice: θ 1° × ρ 1 MAP100px = 180×150 = 27,000 가설, 유효 90.0%,
    ceiling 0.5°/0.25cell = task budget 의 절반.
  - `hypothesis_features` 는 (θ,ρ)~(θ+180,−ρ) 불변식을 구조로 보장
    (cos2θ, sin2θ, ρ̂cosθ, ρ̂sinθ, ρ̂²). 7개 wrap case 최대오차 7.8e-07.
  - `line_distance` 는 k∈{−1,0,1} 최소화 + ρ 부호 flip → wrap 이웃을 멀다고 안 부름.
    "nearest" 는 θ·ρ 독립 반올림이 아니라 joint argmin.
- **신규** `challenge/tests/test_direct_hough_role_heatmap.py` (12건)
- **신규** `_docs/audits/.../IMAGE_MAP_STAGE_CLOSURE.md`
  - 정정: "Hough confidence 불변 → ridge 만 이동" → **"측정한 confidence proxy 개선 없이
    decoded geometry 만 개선"**. margin·entropy 는 한 가지의 proxy 두 개일 뿐이고
    "확신도 불변"·"ridge 만 이동" 은 측정이 못 담는 기전 주장이라 철회.
  - 이번 screen 을 `REPRESENTATION_AND_READOUT_FAMILY_SWITCH` 로 기록(단일 factor 아님).

### 4 oracle (학습 전, 순서대로 게이트)
```
O_DOMAIN   LINE_DEV 전수 27,684 role · domain 밖 0건 · role bias 없음
           role 1  ρ p1/p50/p99 0.37/14.42/55.40 · boundary p1/p50 9.40/45.84
           role 3  ρ 0.51/15.25/54.77 · boundary 11.21/45.39
O_GRID     nearest angle max 0.5006 (ceil 0.5) · offset max 0.2500 (ceil 0.25)  PASS
O_TARGET   0.2503 / 0.1241 · ceiling met · GT-bin gradient negative 100%        PASS
O_SCORER   0.2591 / 0.1245 · p90 0.4980 / 0.2509 (gate 0.75/0.35/1.00/0.50)     PASS
```
★ **role 1/3 의 offset 이상은 Hough domain artifact 아님** — 학습 전에 닫아야 했던 질문.
★ O_SCORER 가 free descriptor 로 격자 천장에 도달 → 이후 실패 범위가
  `HOUGH_SCORER_FORMULATION_FAIL` 이 아니라 encoder/network 로 좁혀짐.

### 내 반복 실수 5번째
금지 리터럴 테스트를 `source()` 문자열 검색으로 짜서 docstring 의 `4.1793` 에 걸림.
**이 규칙을 memory 에 적은 두 턴 뒤**에 재발. `ast.Constant` 의 float 값만 검사하도록 수정.
memory `forbidden-token-tests-must-use-ast.md` 에 5번째 사례 + "테스트 쓰는 순간 문자열
검색이 손에서 나오면 멈춰라" 추가.

### 진행 중
overfit32(1500/3000, 판정 3000·실제 task gate) → FULL(LINE_TRAIN 13,618, marks
1250/2500/5000/8515, D2 primary). Q1 baseline 은 JSON 전정밀도 로드, 40% 기준
2.5076/1.1273. tests 758 passed.

## [07:50] direct-Hough overfit32 — DIRECT_HOUGH_NETWORK_FIT_FAIL (사전등록 3,000 step 판정)

```
              angle med   angle p90   offset med   offset p90   train CE
@1,500         1.728384   11.002086     2.752819    12.592102    6.10696
@3,000         0.597847    2.095702     0.523695     2.213698    4.27863
gate           <= 1.0      <= 2.0       <= 0.5       <= 1.0        n=368
tail @3000: >5° 0.0408 · >10° 0.0109 · >2cell 0.1168
```
4개 중 3개 미달(angle p90 +4.8%, offset med +4.7%, offset p90 +121%), angle med 는 통과.
1,500→3,000 에서 전 지표가 절반 이하 = **수렴 후 실패가 아니라 선택한 step 에서의 미달**.
`NEAR_GATE_BUT_STILL_FAIL` / `RAPIDLY_IMPROVING_TRAJECTORY`. FULL 미시작(러너가 먼저 raise).
★ gate 이동 금지 — 0.5237 을 본 뒤 임계값 논의 불가. 3,000 은 `620bda9` 에 고정.
★ 0.598 을 image-map 4.1793 으로 나눠 "7배" 라 쓰지 않음 — 32장 암기값 vs holdout 값이라
  비교 가능한 양이 아님.
산출 `direct_hough_overfit.json`, 문서 `DIRECT_HOUGH_OVERFIT_RESULT.md`.
