# 2026-08-28

## [00:0x] 세션이 자정을 넘김 — 코드 변경은 08-27, 본문은 거기 있다

Stop hook 이 지목한 18개 파일은 **전부 08-27 에 작성·수정된 것**이고
`_docs/history/2026-08-27.md` 에 Before/After 와 사유가 이미 기록돼 있다.
복사하지 않고 가리킨다 (두 벌이 갈라진다).

```
2026-08-27.md
  [11:00~13:56]  hard_negative_v1 — Phase A + 3-arm 10ep screen
                 hn_loss / hn_trainer / hn_build_datasets / hn_train_arm /
                 hn_evaluate / hn_verdict / preflight/hn_preflight /
                 PHASE_A/phase_a_hardness · METHOD_SPEC · PURPOSE
  [12:2x~12:5x]  mc_auc_report / mc_table / mc_table_all /
                 mc_ap_dump / mc_ap_dump_dope
  [13:5x]        mc_overlay_pair
  [14:2x]        _docs/paper/current_real_dataset/ 문서화
  [14:5x]        scripts/annotate/audit_gt_data.py — GT 데이터 감사 도구
  (mc_overlay_vdo 는 [12:5x] 이후 vdoframes 비교에서 작성)
```

이 파일들은 커밋 `eb8ed3a` 에 포함돼 원격에 올라가 있다 (다른 세션이 커밋).

## [00:1x] 오늘 실제로 한 것 — 검증과 push 확인뿐. 코드 변경 0

사용자 "push해줘" 요청으로 상태를 확인했다.

```
git rev-list origin/main...HEAD   뒤처짐 0 · 앞섬 0
작업 트리                          변경 0
→ 푸시할 것이 없다.  다른 세션이 이미 커밋·푸시를 마쳤다.
```

```
dac561e  Record the catch-up commit and the template merge in the 08-27 history
eb8ed3a  Migrate real GT to v2 and rebuild the metric battery around AUC  ← 내 08-27 작업 포함
299cc87  docs: add blank result table templates
2a8254c  docs: add paper evaluation table templates
3fbfe1a  (내가 08-27 에 올린 것)
```

### 커밋 전 검증으로 전체 테스트를 돌렸다

```
1,119 passed · 2 failed · 4 skipped  (93.8s)
FAILED  challenge/tests/test_mh_poseaware.py::test_pose_gradient_does_not_reach_line
FAILED  challenge/tests/test_role_query_global.py::test_the_global_branch_emits_a_map_residual_not_a_coordinate
```

★ 두 실패는 **이번 세션과 무관한 기존 실패**다 — 테스트 파일도 대상 모듈
(mh_poseaware / role_query)도 이번에 변경되지 않았다 `[확인]`. 다만 방치돼 있으므로
기록해 둔다. 원인 미조사.

## [00:2x] 다른 세션이 내 문서를 개정한 것 — 확인만 하고 손대지 않았다

`_docs/paper/current_real_dataset/` 의 문서 5개가 08-28 에 크게 개정됐다.
내가 08-27 에 "legacy 라벨의 W/D 두 변종(81/59)" 으로 기록한 문제를 GT v2 로 해결한 것으로 보인다.

```
physical dimensions   (1.10, 0.11, 1.30) m 로 140/140 고정
pose                  canonical yaw-180 equivalence class
legacy dimensions_m   evaluator 입력에서 제외 (provenance 필드로 격하)
per-kp visibility     schema 는 생겼으나 현재 140/140 unknown
```

★ 그리고 새 판정이 들어왔다:

```
selector   83/140 overall · 13/28 NIGHT · min session 4/12 = 1/3
           네 pose tail 각각 13/14 가 selector 실패
status     POSE_METRICS_BLOCKED_NO_RELIABLE_AXIS_SELECTOR
FINAL_POS / FINAL_NEG   UNAVAILABLE
```

**예측만으로 W/D 축을 고르는 selector 가 실패해 paper pose 지표가 막혔다.**
내가 세션 내내 짚은 축 누수(memory `evaluator-receives-gt-per-frame-axis-assignment`)의
해결 시도가 실패로 끝난 것이다. 이 결과는 내가 만들지 않았고 검증하지도 않았다 —
다른 세션의 기록을 그대로 옮긴다.
