# 2026-08-07

## PEQ Round-1: subset 정의 교체 + 학습 배선 완성

### 1. difficulty 를 rank 기반으로 교체 (`scripts/stage0/edge_mandatory_fast_search.py`)

Before: `phase_subsets` 가 absolute threshold 사용
```
easy = (visible_kp_count == 8) AND (bbox_min_side >= 60)
```
→ easy pool **0개**, `HARD_BLOCKED_SUBSET_QUOTA: easy 0 < 1200`

After: train split 내부 percentile rank
```
D = 3*(8-geom_in)/4 + 2*trunc + 2*occ_rank + 1.5*(1-size_rank)
    + 1*(1-luma_rank) + 1*border + 0.5*tiny
hard 상위 45% / medium 35% / easy 하위 20%,  tie-break = sha256(index)
```
결과 pool: hard 7,205 / medium 5,604 / easy 3,202 → quota 충족.
appearance group 당 상한(search2k 20, confirm6k 50) 선적용, 512 ⊂ 2,000 ⊂ 6,000 ⊂ train,
holdout inclusion 0. 누락값은 easy 로 대체하지 않고 `HARD_BLOCKED_DIFFICULTY_MISSING_VALUE`.

이유: 컬럼 의미·분포 확인 없이 절대 임계를 쓴 것이 원인. rank 는 비지 않고, train
metadata 만으로 결과 보기 전에 결정되므로 사후 튜닝이 아님.

### 2. `index.csv` mask 경로 수정 (내 버그)

Before: `_shards/.../mask_visible/f0000_mask.png`
After:  `_shards/.../mask_visible/f0000.png`   (실제 파일명)

40,000개 필드 수정. 이 버그 때문에 `mask-audit` 이 `missing 10000 / checked 0` 으로
나와 **E3 가 부당하게 BLOCKED** 됐음. 경로 수정 후 재실행:
`missing 0 · unreadable 0 · visible-outside-amodal p99 0.0 · amodal>=visible 100%` → E3 OK.
⚠ `index.csv` SHA 변경 → `projection_lock/paper_split_provenance.json` 의
`index_csv_sha256` 이 어긋남. 새 SHA 는 `index_csv_sha.json` 에 기록, provenance 갱신은 후속.

### 3. 학습 배선 완성 (스텁 제거)

Before: `phase_smoke` / `phase_round1` 이 `{"status":"PENDING_TRAINING_WIRING"}` 만 기록,
`phase_decide` 는 `DATA_GATES_ONLY`.

After: `run_training()` 신설 — batch 당 frozen A1 forward **정확 1회**, F50/base belief
detach 후 E1·E2 공유. arm 별 독립 module·optimizer.
- `edge_targets()` : `refine_keypoints[:8]`(이미 50-grid)에서 12 role 의
  centre/direction/half_length/edge_support/regression_mask 생성. ×8·/8·K/pose 재투영 없음.
  `sample["visibility"]` 참조 0.
- `edge_losses()` : centre SmoothL1 / orientation 1-|dot| / length SmoothL1(log) /
  support BCE, + CIGM incidence SmoothL1(in-frame·valid 한정), + belief masked MSE(ch 0:8).
- lambda: train 16 batch, update 0 으로 median 측정 후 산출·freeze.
- E3 는 이번 Round1 에서 `SKIPPED_OPTIONAL`.

### 4. 실행 결과

smoke (smoke512, 50 step): E1·E2 둘 다 PASS.
PEQ/EGCR/HCRM gradient >0, residual 0→>0, zero-init A1 정확 일치, centroid delta 0,
A1 checksum 불변, checkpoint 저장·재적재.

round1 (search2k, 1 epoch): 166 step, A1 forward 166(batch당 1), checkpoint 저장.
`weights/paper_s2_edge_fast/{E1,E2}/round1.pth`

### 5. ★ 기록해야 할 λ clamp 효과 (조정하지 않음)

```
component      median     lambda      기여     비중
belief        0.00283    1.0000    0.00283    3.8%
centre       37.0945     0.001     0.03709   50.2%
incidence    31.2137     0.001     0.03121   42.3%
```
지정 규칙 `target_share × median(L_belief)/median(component)` 이 하한 1e-3 에 걸려
5개 중 4개가 floor 로 **올라감**(낮아진 게 아님). 의도한 centre 비중(belief 의 0.50 =
0.00141) 대비 실제 0.03709 로 **26배**. loss 의 92%가 edge 항, belief 3.8%.

clamp 는 건드리지 않음 — 수치를 본 뒤 바꾸는 것은 금지. 다만 Round1 의 ID1·2 / R4
게이트는 **belief 헤드가 거의 학습되지 않은 조건**에서 읽어야 함.

### 6. 미실행

validation512 평가, EDGE_ZERO / EDGE_SHUFFLE / GT_EDGE_ORACLE ablation, Round1 PASS 판정.
`round1_metrics.json` 의 `validation: PENDING`, `decide` 미실행.
학습은 됐으나 **게이트 판정은 나오지 않음.**

### 커밋

- `26c51e1` Fix the PEQ subset definition
- `32046c2` Wire the actual PEQ smoke and Round-1 training

tests 604 passed (신규 7, 누적 27). final-test UNOPENED.

## [13:17:07] ⚠️  Auto Compact (컨텍스트 한계 도달)

- Working directory: `/home/minjae/Documents/github/pallet-pose`
- Trigger: `auto`
- Session ID: `1f06bfde-fd3e-4103-a3ef-711599683949`
- Transcript: `/home/minjae/.claude/projects/-home-minjae-Documents-github-pallet-pose/1f06bfde-fd3e-4103-a3ef-711599683949.jsonl`
- Note: 컨텍스트가 가득 차서 자동 트리거됨 — 다음 세션에서 이 마커 위쪽 작업을 반드시 복원할 것

**TODO (session resume)**: compact 이후 이 마커 위쪽의 작업 내역을 확인하고 이어서 진행


## [13:58] V2 line-capacity — population/oracle 수정 후 최종 판정

### 1. 7009c10 runner 의 결함 4건 (전부 내 것, feature 결과 0회 상태에서 수정)

- `scripts/stage0/line_feature_capacity_v2.py`
  - **coverage 표본화**: `dev_ids[:256]` / `train_ids[:256]` ← 실제 split 2,393 / 13,618.
    전수 + geometry-only(`load_geometry` 가 `camera_data.width/height` 만 읽음, PNG decode 0).
  - **모집단 오정의**: 전체 p0→p1 을 채점 → Liang-Barsky 로 `[0,49]²` clip 후
    IN_FRAME_FULL / IN_FRAME_PARTIAL / OFF_FRAME_FULL 분류. off-frame 은 count 만.
  - **O1B = O1A**: `span = low+(high-low)*alpha` 를 다시 `low<=span<=high` 로 검사 →
    항상 True. `sample_strip` 이 `t`(along-line 좌표) 반환하고 clip 된 GT 끝점을
    같은 축에 사영해 비교. 실측 support fraction 0.151.
  - **masked loss**: `budget_losses` 가 mask 전에 scalar mean → `reduce=False` +
    `masked_mean`, `step_batch` 의 `train` 플래그 제거(경로 1개·mask 1개).
    coarse validity 는 canonical grid 기준 → arm 간 population 동일.
  - **checkpoint**: 문서에만 있고 `torch.save` 0건 → epoch 1/3/5 저장 + provenance,
    epoch5 는 디스크에서 reload 재평가(6 arm 전부 max delta 0.0).
  - **`--seed`**: 선언만 되고 아무 데도 안 감 → 제거, `SEED=1` 고정.
- `challenge/tests/test_line_capacity_v2.py` 신규 30건.
- `challenge/tests/test_edge_mandatory_fast_search.py::test_27` — `calibrate_v2` 재작성
  이후 stale 이던 `l["lambda"]` 스키마 단언을 `l["components"]` 로 수정 (기존 red).

### 2. coverage (전수, geometry-only)

```
                r=6       r=8       r=10      r=12      r=14
dev    pair     0.92136   0.98486   0.99830   0.99996   1.00000
train  pair     0.92235   0.98380   0.99856   0.99998   1.00000
```
gate 0.995 를 통과하는 최소 radius = **10** (설정값과 동일 → sampler 불변).
LINE_TRAIN 13,618 frame / 788,790 pair / off-frame 5,658,
LINE_DEV 2,393 / 27,684 / off-frame 1,032.

### 3. O0 재현 (population 변경으로 재실행)

`angle 0.0283° / offset 0.0158 cell` — gate(0.20/0.10, p90 0.50/0.25) PASS.

### 4. 최종 (n=5,889, population_sha 46378206fe5f5708, epoch5)

```
arm            angle med   offset med   vs identity
identity          3.938        1.974
gate             <=1.000      <=0.500
O1A               3.651        1.882     -7.3% / -4.7%
O1B               3.596        1.695     -8.7% / -14.2%   support 0.151
C0_F50            2.703        1.310    -31.3% / -33.7%
C1_F100           3.002        1.503    -23.7% / -23.9%
C2_MULTI          2.727        1.365    -30.8% / -30.9%
C3_RGB_STEM       3.335        1.889    -15.3% / -4.3%
```
overfit32 는 6 arm 전부 ~0.00 (O0 도 PASS) → refiner·optimization 은 병목 아님.

**판정 = `LOCAL_EDGE_EVIDENCE_PRECISION_FAIL`** (O0 PASS + O1B FAIL + learned 전부 FAIL).

### 5. 배제 못 한 confound (명시)

overfit32 완벽 + dev 30% 개선 = 데이터 부족 서명이기도 함. arm 은 `line_search2k`
2,000 frame 으로 학습(LINE_TRAIN 13,618 중). "어떤 feature 도 이 정밀도를 못 낸다" 와
"2,000 frame 으로는 못 낸다" 는 다른 문장이고 이 run 은 둘을 못 가른다.
다음 측정은 동일 6 arm × full LINE_TRAIN (다른 것 불변).

산출: `line_capacity_v2_arms.json`, `line_capacity_v2_decision.json`,
`coverage_fullsplit.json`, `o0_reproduction.json`,
문서 `LINE_CAPACITY_V2_POPULATION_ADDENDUM.md`, `LINE_CAPACITY_V2_RESULT.md`.
tests 634 passed. eval56 / wood45 / untouched / final-test UNOPENED.

## [14:35] 발표용 wood pose figure (raw / 추론-only + yaw-pitch-roll)

- 신규 `scripts/stage0/wood_pose_figure.py`
  - 목적: `wood_gt_overlays/TP_acc-good_184309_000132.jpg` 는 GT(green)+pred(blue)+
    reproj(red) 를 겹쳐 그린 **진단용** → 슬라이드엔 부적합(정답을 같이 보여줌).
    같은 프레임을 **GT 없이 추론 결과만** + 축 triad + yaw/pitch/roll/거리로 렌더.
  - 추론 경로는 `wood_infer_filter` 그대로 재사용 (Stage B `net_epoch_0057`,
    squash-parity 전처리, `solve_pose(dims=WOOD_DIMS)`) — 새 모델·새 전처리 없음.
  - 각도 정의: 카메라 상대 Tait-Bryan **Y-X-Z**, OpenCV 카메라 프레임(X右/Y下/Z前),
    `R = Ry(yaw)·Rx(pitch)·Rz(roll)`. 분해 검증은 역변환 round-trip
    (`rotation_from_yxz`) 최대오차 <=1e-6 를 코드가 강제(초과 시 SystemExit).
  - ⚠ 중력 기준 아님 — 카메라 자체가 기울어 있어 pitch 는 대부분 카메라 하향각을 읽음.
- 결과 (`pallet_20260618_184309/000132`, 1280x720, K=1080p 16:9 선형유도)
  - det 8/8 + centroid, `yaw +25.6° / pitch +35.9° / roll +17.8°`,
    t = [+0.011, -0.143, +1.618] m (|t| 1.624 m), posdepth True
  - **reproj self-consistency 4.19px** = 기존 오버레이 헤더 `reproj=4.2px` 와 일치
    → 평가 때 채점된 것과 같은 pose 재현 확인.
- 산출: `_docs/figures/wood_184309_000132_{raw,pose,pair}.png`
- ⚠ 이 프레임은 wood45(SEALED) 소속. 기존 오버레이가 repo 에 이미 있어 과거에 추론된
  프레임이고, 이번 용도는 발표 그림(모델 판정 아님)이라 진행. 새 판정·새 수치 채택 없음.
- ⚠ TP_acc-good = 잘 된 예시로 선별된 프레임. 대표성 주장 금지 (wood N=45 소표본, UNSEEN).

### [14:41] 표시 프레임 변경 — Y 위 / Z 관측자 쪽 (사용자 요청)

repo 규약은 OpenCV식(X右 / Y**下** / Z**전방**)이라 축을 그리면 Y 가 팔레트 안으로,
Z 가 화면 뒤로 향한다. 슬라이드에서 읽기 어려워 **표시 프레임**을 도입:

- `DISPLAY_FLIP = diag(1, -1, -1)` — 자기 자신이 역, 우수좌표계 유지(X × (−Y) = −Z).
- triad: `triad @ DISPLAY_FLIP` → Y=위(상판 밖), Z=관측자 쪽. 라벨 `Y up` / `Z out`.
- 각도: `R_display = DISPLAY_FLIP @ R @ DISPLAY_FLIP` 에서 추출.
  **object 와 camera 에 같은 flip** 을 걸었으므로 pose 가 바뀐 게 아니라 표시 basis 변경.
  round-trip 검증은 `R_display` 기준으로 그대로 강제(<=1e-6).
- 값 변화: `yaw +25.6 → −25.6`, `roll +17.8 → −17.8`, `pitch +35.9` 불변.
  (Y·Z 축 뒤집으면 그 두 축 회전의 부호가 반대가 되고 X 축 회전은 그대로)
  t / reproj 4.19px / posdepth 는 불변 — 같은 pose.

### [14:52] 축 라벨을 X/Y/Z → yaw/pitch/roll (사용자 요청)

계산된 각도는 이미 `R_display = Ry(yaw)·Rx(pitch)·Rz(roll)` = **intrinsic Y→X→Z**
(yaw 후 새 X 로 pitch, 그 후 새 Z 로 roll) 이므로, 항공/로보틱스의 body-axis 관례
그대로 Y=yaw축 / X=pitch축 / Z=roll축 으로 라벨. **값 불변** (−25.6 / +35.9 / −17.8).
패널 글자색도 화살표 색과 일치시켜(yaw 초록 / pitch 빨강 / roll 파랑) 대응 가독.

⚠ Euler 는 순차 회전이라 "세 화살표가 각각 그 회전의 축" 은 마지막(roll) 만 최종
object 축과 정확히 일치한다. body-axis 라벨링은 sequence 를 명시할 때 성립하는 관례이며
docstring 에 intrinsic 순서를 적어 뒀다. 슬라이드 캡션에도 필요하면 한 줄 넣을 것.

### [15:03] 슬라이드 렌더 조정 — 텍스트 패널 제거 + 오버레이 굵게 (사용자 요청)

- `draw_pose(..., thick=3 → 6, panel=False)` 기본값 변경.
  - cuboid: 검은 casing(`thick+4`) 위에 노란 선 → 밝은 콘크리트 배경에서 대비 확보.
  - keypoint: r5 테두리 → r9 검정 + r7 흰색.
  - 화살표: casing + `tipLength 0.18→0.20`, 라벨 `0.85/2 → 1.1/3` + 검정 외곽 6px.
  - 좌상단 수치 블록 제거. `--panel` 로 복원 가능(캡션 없이 단독으로 쓸 때).
- CLI 추가: `--thick`(기본 6), `--panel`.
- ⚠ 수치는 그림에서 빠졌으므로 슬라이드 캡션에 넣을 것:
  `yaw −25.6° / pitch +35.9° / roll −17.8° / 1.62 m` (stdout 및 위 기록에 보존).
- 재렌더 후 값 불변 확인: reproj 4.19px, t=[+0.011,−0.143,+1.618], det 8/8.

## [15:05] post-V2 진단 — O1C(hard-support oracle) 결과

### 준비 (실행 전 커밋 `2def93c` / 디스패치 수정 `af…`)

- V2 판정 격하: `LOCAL_EDGE_EVIDENCE_PRECISION_FAIL` → **PROVISIONAL**,
  확정된 것은 `SEARCH2K_LINE_REFINEMENT_FAIL`. 문서 `V2_SEARCH2K_QUALIFICATION.md`.
- 용어 정정: 788,790 은 pair 가 아니라 **5-epoch role exposure**.
  LINE_TRAIN = 13,618 frame / 163,416 role / off-frame 5,658 /
  **unique supported 157,758** / exposures 788,790. LINE_DEV unique supported 27,684.
  `coverage_full` 이 `unique_supported_roles` 와 `role_exposures` 를 분리 출력.
- O1C 신설: `gradient_hard` — O1B 는 support 를 **채널로만** 줬고 segment 밖 feature 는
  그대로 남았다. O1C 는 magnitude·gx·gy 를 support 로 곱해 **실제로 0** 으로 만든다.
  GT orientation/rho 는 여전히 input 금지.
- step 수는 runner 의 실제 chunking 에서 유도(하드코딩 금지):
  `S_SHORT=835`(167/pass×5, 2,000 frame), `S_LONG=5675`(1,135/pass×5, 13,618 frame).
- 버그 1건(내 것): `o1c` 가 argparse choices 에는 있는데 모든 branch guard 를 통과 못 해
  **아무것도 안 하고 성공 종료**. 실행 로그가 바로 scale 로 넘어가 발견. 결과 나오기 전에
  발견했고, runner_sha 정합을 위해 진행 중이던 scale 도 중단 후 재실행.
  테스트 추가: 선언된 모든 subcommand 가 어떤 guard 에든 등장하는지 AST 검사.

### O1C 결과 (search2k, epoch1/3/5, dev512 동일 jitter)

```
                 angle med   offset med
identity            3.938       1.974
gate               <=1.000     <=0.500
O1C overfit32       0.015       0.007
O1C epoch1          3.940       1.767
O1C epoch3          3.825       1.737
O1C epoch5          3.821       1.723      (-3.0% / -12.7% vs identity)
참고 O1A epoch5     3.651       1.882
참고 O1B epoch5     3.596       1.695
```

**O1C FAIL.** target edge 의 gradient 만 남겨도 budget precision 이 안 나온다.
→ clutter/support-selection 이 병목이라는 가설 기각. raw Scharr 표현 자체의 한계.
★ O1C 가 O1B 보다 angle 이 오히려 나쁨(3.821 vs 3.596) — 주변 context 를 지우면
정보가 줄어든다. "hard mask 가 항상 낫다" 는 직관과 반대.

### 진행 중

data × step factorial (F50/MULTI/RGB × {2k, full} × {835, 5675}).
★ `C0_F50 k2@835 = 2.703 / 1.310` 으로 **기록된 search2k epoch5 와 정확히 일치** →
visit-count 스케줄이 기존 run 과 bit-compatible, 조건 A 재사용 유효(재학습 안 함).

## [17:40] data × step factorial 완료 — LOCAL_EDGE_REPRESENTATION_PRECISION_FAIL

```
arm          A 2k/835      B full/835    C 2k/5675     D full/5675   D vs A
C0_F50       2.703/1.310   2.837/1.333   2.259/1.188   2.184/1.183   -19.2%/-9.6%
C2_MULTI     2.727/1.365   3.108/1.363   2.342/1.212   2.307/1.219   -15.4%/-10.7%
C3_RGB_STEM  3.407/1.825   3.532/1.712   2.847/1.479   2.846/1.506   -16.5%/-17.5%
identity 3.938/1.974   gate <=1.000/<=0.500
```
사전 기준(40% 감소, 1.5°/0.75cell 접근) **전부 미달**. SLQ NOT BUILT.

레그 분해 (3 arm 일치):
```
             C-A(step×6.8)   D-C(data×6.8)   B-A(data×6.8, step 동일)
C0_F50         -0.444°          -0.075°           +0.134°
C2_MULTI       -0.385           -0.035            +0.382
C3_RGB_STEM    -0.559           -0.002            +0.125
```
개선은 거의 전부 step 에서. 데이터 6.8배는 고정 step 예산에선 **악화**, 긴 예산에서도
0.002~0.075° 뿐. `scaling_decision` 이 사전 규칙(`B<A` **또는** `D<C`)대로
`DATA_DIVERSITY_HELPS` 를 붙였으나, 두 레그 부호가 반대이고 발화한 레그 폭이 0 근처라
데이터 지지로 읽으면 안 됨 — 기록은 규칙대로 남기고 해석만 명시.

### C3_RGB_STEM 재현 실패 = 비결정성 [확인]

`CONDITION_A_NOT_REPRODUCED` drift 9.735e-02 (F50/MULTI 는 0.0 정확 재현).
동일 20-step 재실행 대조: RGB 4.008037090 vs 4.009006500(다름) / F50 동일.
`cudnn.benchmark=False, deterministic=True` 상태. 기전 = stem 만 파라미터가
`grid_sample` 상류라 **input-grad(atomicAdd)** 경로를 타고, frozen arm 은 detach 라 안 탐.
처리: frozen arm 은 0 drift 강제 유지, stem arm 만 `NON_DETERMINISTIC_ARM` 기록 +
자기 `k2@S_SHORT` 를 A 로 사용(같은 궤적 내 비교). gate·사전임계값 불변.
`scaling_decision.json` 에 `condition_A_source` / `condition_A_drift_vs_recorded` 기록.
⚠ RGB 의 A→D 폭에는 미정량 run-to-run 성분 있음(20 step 에 1e-3 확인이 전부).

산출: `scaling_plan.json`, `scaling_arms.json`, `scaling_decision.json`,
checkpoints `<ARM>_{k2,full}/epoch_{000835,005675}.pth`,
문서 `V2_SEARCH2K_QUALIFICATION.md`, `V2_DATA_VERSUS_STEP_RESULT.md`. tests 646 passed.

## [18:00] structural-line map 스크린 — O_MAP 에서 정지 (MAP_TO_LINE_DECODER_FAIL)

- 신규 `scripts/stage0/structural_line_map_capacity.py` (커밋 `20eb590`, 실행 전 lock).
  12 role-fixed map(100×100) → **weighted TLS** 로 line 복원. coordinate head 없음,
  Hungarian 없음, pose/dims/K 없음. arms M0_F50_MAP / M1_F50_RGB_MAP.
- 062363a qualify: `GENERIC_LOCAL_STRIP_REPRESENTATION_FAIL` 로 좁힘.
  step 표현은 "saturated" 가 아니라 `6.8X_OPTIMIZATION_BUDGET_INSUFFICIENT`.
  용어: 12개 = **structural cuboid lines** (image edge 아님).
- 사전 점검에서 2건 발견(학습 전):
  (1) eigenvector 부호가 임의 → rho 부호 뒤집힘. `line_errors` 에서 undirected 정렬,
      offset p90 66.3 → 0.095 로 교정. 테스트로 불변성 고정.
  (2) 깨끗한 내부 segment 는 4e-5° 로 정확한데 경계 근처에서 tube 가 잘림.

### O_MAP (LINE_DEV 2,393 frame / 27,684 supported role)

```
                 measured   gate
angle  median     0.0015   <=0.05   pass
offset median     0.0006   <=0.05   pass
angle  p90        0.3780   <=0.10   FAIL
offset p90        0.1380   <=0.10   FAIL
```
```
                    n       angle med  angle p90  offset p90
border >= 1.5    22,258      0.0007     0.0132     0.0053
border <  1.5     5,426      0.3920     5.8421     2.6406
IN_FRAME_FULL    23,943      0.0009     0.0249     0.0098
IN_FRAME_PARTIAL  3,741      0.5654     8.2677     3.8127
visible < 2cell   6,574      0.0084     0.3692     0.0998
```
기전 = 유한 grid 위 weighted moment 는 경계 근처에서 tube 가 비대칭으로 잘려
centroid 가 선에서 밀림. 짧은 stub 은 주원인 아님(p90 0.369).
**판정 `MAP_TO_LINE_DECODER_FAIL` → 학습 차단. M0/M1 미학습, capacity 미측정.**
decoder 를 결과 보고 고치지 않음 — 수리안 3가지는 문서에 적고 다음 lock 으로 넘김.
산출 `structural_line_map_omap.json`, 문서 `STRUCTURAL_LINE_MAP_OMAP_RESULT.md`,
`LOCAL_REFINER_FINAL_QUALIFICATION.md`. tests 659 passed.

---

## 논문 Method 비교표 빈칸 채우기 — Naïve ST 학습 착수 (18:24)

### 배경 (조회 결과, 코드변경 아님)
사용자 제시 표(Synthetic only / Naïve ST / Reproj-only ST / Ours × ADD-S↑·Yaw↓·Success↑)를
기존 `ralph_selftrain` h8(loo+flip) 산출로 채울 수 있는지 확인.
- 그림 2장 = `ralph_selftrain/phase1style_cross_domain_s2_looflip.png`(NN20) +
  `ralph_meas_v3/measC_yaw/phase1style_add_matrix_looflip.png`(ADD) 계열.
- 구성 [확인, train.log 실측]: base=`weights/paper_s2_stageB/net_epoch_0057_noseg.pth`(synthetic-only),
  anchor=`aug_squash_v2`(2212), pool=`real_unlabeled_ralph_{outside,night,noapril}`(500/500/170, eval GT 홀드아웃),
  filter=`ransac_loo_flip`, R2 정지. PL R1/R2 = outside 44/81 · night 30/124 · noapril 37/49.
- 채울 수 있는 값: yaw(°)·centroid(cm)·ADD(m) = `measC_yaw/threeway_pose_metrics.json` 에 R0/loo/reproj+flip/loo+flip 전부 존재.
- 못 채우는 것: (1) **Naïve ST 모델 부재**, (2) ADD-S(대칭 fold) 미계산 — 저장본은 표준 ADD,
  (3) Success Rate 정의 미확정. `weights/paper_s2_rf_hipl` 은 reproj∧flip 이라 "Reproj-only" 라벨 부정확.
- 대체 지표로 **det rate = N/eval_gt_n** 이 이미 계산돼 있음(추가 GPU 0):
  outside 73/113→99/113, night 28/40→32/40, noapril 15/18→15/18 (R0→loo+flip).

### 신규 파일
- `scripts/stage0/_run_naive_all.sh` — Naïve ST(무필터) 체인. h8 과 base/anchor/pool/rounds 동일,
  `--filter_type none` 만 다름(paired). 출력 `ralph_selftrain/h9_s2_{outside,night,noapril}_naive`.
- `ralph_selftrain/h9_s2_*_naive/PURPOSE.md` — [소비처] 논문 Method 표 Naïve 행 /
  [문장] "무필터 ST 는 PL 오염으로 R0 이하로 악화 = 개선의 원인은 loo+flip 필터".

### 실행 상태
detached(setsid) 로 outside→night→noapril 체인 실행 중, 로그 `ralph_selftrain/_naive_chain.log`.
outside R1 **accepted 313/500 (62.6%)** — loo+flip 44/500(8.8%) 대비 7배 = 무필터 대비 성립 [확인].

## [18:31] O_MAP forward-parity 재판정 — LOCKED_SOFTPLUS_TLS_FAIL

### 수정 (커밋 `116ae43`, 실행 전)

`batch_terms` 는 `sigmoid(logit)` 을 target 에 맞추고 `softplus(logit)` 을 TLS 에 준다.
그러므로 perfect prediction 이 decoder 에 주는 weight = `softplus(logit(target))`
= `-log1p(-target)`. 기존 `run_omap` 은 `weighted_tls(target)` 을 직접 호출 →
**forward 가 만들지 않는 map 을 decode** 했다. label `OMAP_ORACLE_SEMANTICS_MISMATCH`.
등가식 오차 9.5e-7 (기준 1e-6, 테스트로 강제). clamp 는 dtype `1-eps`(임의 cap 아님),
`target==0 → weight 0` 정확. 바꾼 것은 **oracle input 뿐** — weighted_tls·target·
sigma·grid·gate·sign alignment·population 전부 20eb590 그대로 (parity 가
statement-level branch 를 만들지 않음을 AST 테스트로 고정).

### P1 결과 (LINE_DEV 2,393 frame / 27,684 supported role)

```
                 P0 target   P1 parity   gate
angle  median      0.0015      0.0485   <=0.05  pass
angle  p90         0.3780      0.4378   <=0.10  FAIL
offset median      0.0006      0.0172   <=0.05  pass
offset p90         0.1380      0.1889   <=0.10  FAIL
eigen ratio med       8.5        12.8
```
**P1 이 P0 보다 전 지표 악화.** 예상(뾰족한 weight 가 경계 절단에 강할 것)과 반대.
기전=weight 를 sharpen 하면 moment 의 유효 footprint 가 줄어 방향 추정이 격자에 민감해짐
(eigen ratio 8.5→12.8 이 같은 현상의 다른 면).

### cross-tab — 548585b 의 "border 가 원인" 주장 철회

```
quadrant                            n     angle med  angle p90  offset p90  eig p10
A border>=1.5, visible>=2      16,404      0.0199     0.1513     0.0624      4.84
B border>=1.5, visible< 2       5,854      0.1546     0.5047     0.2236      1.28
C border< 1.5, visible>=2       4,706      0.1584     1.3240     0.5857      6.49
D border< 1.5, visible< 2         720      4.9088    65.6833    22.5728      1.22
```
A(전체 59%, 경계에서 멀고 충분히 김)조차 p90 0.1513 로 gate 미달. border 와 short-stub 은
각각 비슷한 비용이고 겹치면 파국(D)이지만, **깨끗한 다수를 떨어뜨리는 원인은 둘 다 아님.**

**판정 `LOCKED_SOFTPLUS_TLS_FAIL` = `MAP_TO_LINE_DECODER_FAIL_CONFIRMED`.**
M0/M1 계속 차단(학습 0회), capacity 미측정. 이번 세션에서 decoder 수리 구현 안 함.
다음 후보만 기록: **normalised Radon/Hough readout** (공간 평균을 안 만들므로
bounded-grid centroid shift 와 다른 실패 기전). 별도 locked screen 에서.
산출 `structural_line_map_omap_parity.json`, 문서
`STRUCTURAL_LINE_MAP_OMAP_SEMANTIC_PARITY.md`, `STRUCTURAL_LINE_MAP_P1_PARITY_RESULT.md`.

## yaw 악화 원인 진단 (paired) — "코드 버그 아님, 유의하지도 않음" (19:55~20:10)

사용자 질문: "2D 점이 잘 찍히면 yaw 도 좋아져야 하는 것 아닌가? 코드 문제 아닌가?"

### 신규 파일
- `data/pallet/results/ralph_meas_v3/measC_yaw/yaw_paired_diagnosis.py` — R0 vs Ours(h8 loo+flip R2)
  를 **같은 프레임집합**에서 비교(Wilcoxon), 2D front/rear 분해, Δ2D↔Δyaw 상관, yaw raw 밴드.
- `data/pallet/results/ralph_meas_v3/measC_yaw/fourway_pose_metrics.py` — 논문 표 4행(R0/Naive/rf/Ours)
  평가용. threeway 는 filter별 matrix JSON 을 요구해 naive 를 못 끼우므로 별도 스크립트.
- `data/pallet/results/ralph_meas_v3/PURPOSE.md` — 측정 하네스 목적(purpose_gate 통과용 겸).

### 코드 감사 결과 [확인]
`_yaw_deg=atan2(R00,R20)` · `err_180=min(raw,180-raw)` · `auto_swap_dims=False` + 프레임별 GT dims
→ GT·pred 동일 파이프라인이라 상쇄. **버그 없음.** GT dims 는 1.3x1.1(정사각 아님),
raw 오차 80~100° 밴드 outside 2→1건 = 90° swap 아님, 180 flip 0건.

### PAIRED 결과 (같은 프레임, median / Wilcoxon p)
```
dom       N(both)  yaw R0->Ours     p       e2d_all R0->Ours   e2d_front      e2d_rear
────────────────────────────────────────────────────────────────────────────────────────
outside     69     6.62 -> 7.19    0.398    14.19 -> 17.12    6.91 -> 7.21   24.29 -> 28.41
night       27     5.74 -> 6.58    0.290    16.66 -> 14.33    8.85 -> 8.37   29.22 -> 28.45
noapril     15     0.89 -> 0.73    0.0067    6.90 ->  7.60    6.34 -> 7.51    8.71 ->  7.73
```
- **unpaired 아티팩트 아님**(내 1순위 가설 기각): paired outside 6.62->7.19 가 unpaired 6.54->7.19 와 동일.
  새로 검출된 30장의 yaw_med 6.59 = 기존과 비슷(어렵지 않음). det both69/R0only4/Oursonly30.
- **yaw 악화는 통계적으로 유의하지 않다**(p=0.398/0.290). 유의한 것은 noapril 의 **개선**(p=0.0067, 11/15).
  → "self-training 이 yaw 를 악화시킨다"는 서술은 근거 없음. 정확히는 **변화 없음(flat)**.
- **Δ2D ↔ Δyaw 양의 상관**: outside rho=0.496(p=1.4e-5), night rho=0.534(p=0.004).
  = 2D 가 좋아진 프레임은 yaw 도 좋아진다(사용자 직관 성립). 기전은 정상 작동.
- **왜 전체적으로 안 오르나 = rear**: front 6.9~8.8px 대비 rear 24~29px(3~4배), self-training 이
  rear 를 못 고침(outside rear 24.3->28.4). yaw 는 rear 위치가 결정 → yaw flat.
- ADD paired 는 개선 추세이나 경계: outside 0.351->0.262(p=0.055), night 0.258->0.199(p=0.106).

### GT 자체 한계 [확인]
GT projected_cuboid 의 정수(클릭) vs 실수(PnP 외삽) 판별:
```
        idx      0    1    2    3    4    5    6    7
outside clicked 110  108  107  103   61   70    9   12   (n=117)
outside extrap    5    9   10   11   56   47  108  105
night   clicked  38   36   36   36   39   41    1    0   (n=43)
noapril clicked  18   16   16   18   18    7    0    0   (n=18)
```
**rear 코너 6,7 은 92~100% 가 PnP 외삽**(사람 클릭 아님). rear 2D 오차는 모델 오차 + GT 외삽
불확실성의 혼합이며, GT yaw 의 실효 제약도 front 4개 + 코너 4,5 에서 나온다.

### 논문 표 함의
yaw 열은 "악화"가 아니라 **no significant change (p>=0.29)** 로 써야 한다.
산출: `yaw_paired_diagnosis.{json,md}`.

### Naïve ST 학습 완료 (19:55)
PL accepted R1/R2 = outside 313/383, night 375/451, noapril 158/159 (모두 /500,/500,/170).
loo+flip(44/81, 30/124, 37/49) 대비 4~9배. fourway 평가 대기.

### [20:03] 재확인 — 테스트 실패는 환경, P1 은 결정적

- 직전 전체 스위트 1건 실패(`test_reevaluate_refuses_an_intermediate_epoch`)는
  **CUDA OOM**. 원인 = 외부 `self_train.py`(PID 1542599)가 10GB 중 8.4GB 점유.
  내 모듈을 `--ignore` 해도 동일 실패한 것으로 무관함을 이미 확인했고,
  self_train 종료 후(GPU 1,045MiB) 재실행 → **666 passed / 0 failed**.
- P1 오라클 재실행 대조: angle med/p90, offset med/p90, n, eigen ratio, cross-tab
  **전부 bit-identical** (0.048484 / 0.437793 / 0.017231 / 0.188910, n=27,684).
  GPU 경합이 P1 수치에 영향 없었음이 실측으로 확인됨.
- 테스트 정리: `test_structural_line_map.py` 의 순수 기하 테스트를 CPU 로 이동 +
  teardown 에서 `empty_cache`. (OOM 의 원인은 아니었지만 스위트가 불필요하게 GPU 를
  잡지 않도록 하는 게 맞음 — "고쳤다"고 주장하지 않음.)
- 판정 불변: `LOCKED_SOFTPLUS_TLS_FAIL` = `MAP_TO_LINE_DECODER_FAIL_CONFIRMED`.
  M0/M1 여전히 차단(학습 0회), decoder 수리 미구현.

## 논문 Method 4행 표 완성 (fourway, 20:10~20:30)

`data/pallet/results/ralph_meas_v3/measC_yaw/fourway_pose_metrics.py` 확장(success rate 추가) 후 실행.
success rate 는 pose 지표와 **같은 분모(n_gt=113/40/18)** 로 계산 = 앞서 지적한 분모 불일치(117/43/18) 해소.

```
domain   method            N       succ%  det%    yaw°   cent cm  ADD m
──────────────────────────────────────────────────────────────────────────
outside  Synthetic only   73/113   45.1   64.6    6.54    27.56   0.358
outside  Naive ST         95/113   48.7   84.1    6.81    35.52   0.417
outside  Reproj+flip ST   97/113   44.2   85.8    7.94    41.56   0.452
outside  Ours (loo+flip)  99/113   48.7   87.6    7.19    25.10   0.296
──────────────────────────────────────────────────────────────────────────
night    Synthetic only   28/ 40   47.5   70.0    6.04    23.61   0.267
night    Naive ST         33/ 40   60.0   82.5    7.23    32.07   0.332
night    Reproj+flip ST   34/ 40   40.0   85.0    6.77    31.49   0.451
night    Ours (loo+flip)  32/ 40   65.0   80.0    6.48    17.54   0.189
──────────────────────────────────────────────────────────────────────────
noapril  (4 방법 전부 15/18, succ/det 83.3 포화; ADD 0.056/0.061/0.076/0.053)
──────────────────────────────────────────────────────────────────────────
```
- **핵심 대비 확보**: Naive·Reproj+flip 은 det 를 올리지만 **ADD 를 악화**(outside 0.358→0.417/0.452,
  night 0.267→0.332/0.451). **Ours 만 det 와 ADD 를 동시 개선**(outside 0.296, night 0.189).
  = "PL 수가 아니라 품질" 논지가 표로 성립.
- 단, **outside success 는 Naive 와 동률(48.7)** — 2D success 만으로는 Ours 우위 없음. 구분은 3D(ADD).
- yaw 는 4 방법 모두 R0 근처, paired 검정에서 유의하지 않음(앞 절) → "no significant change" 로 표기.
- noapril 은 n=18 에 전 방법 포화라 판별력 없음.
- Naive PL 수(R2): outside 383/500 · night 451/500 · noapril 159/170 (loo+flip 81/124/49 의 3~5배).
산출: `fourway_pose_metrics.{json,md}`.

## yaw 레버 진단 — "6°는 노이즈 아님, 레버는 rear 코너" (20:30~20:50)

사용자 질문: "yaw 개선하려면? 6° 이하면 그냥 오차인가?"
신규 파일 `data/pallet/results/ralph_meas_v3/measC_yaw/yaw_lever_diagnosis.py`
(oracle 코너 치환 4-arm + elevation 층화 + Spearman. 측정 only).

### (1) 6°는 노이즈가 아니다 [확인]
measB GT 불확실성 바닥(σ=1med) = outside **0.41°** / night **0.69°**.
모델 yaw 6~7° = 바닥의 **10~16배**. 개선 여지 실재.

### (2) oracle 치환 — 레버는 rear 코너 (yaw median deg)
```
dom      model    N     ALL  GTrear  GTfront  GTtoprear   e2d_front  e2d_rear
────────────────────────────────────────────────────────────────────────────
outside  R0      73    6.54    1.08     5.56       3.12       8.03     24.38
outside  Ours    99    7.19    1.21     5.81       3.76       8.83     30.67
night    R0      28    6.04    1.77     5.91       4.27       8.75     31.18
night    Ours    32    6.48    1.94     5.13       3.35       8.38     25.84
```
- rear 4코너를 GT로 치환 → **6.54 → 1.08°**(바닥 0.41 근처). front 치환은 5.56(거의 무효).
- 순환 완화 arm(GT_TOPREAR = 코너 4,5만; outside 61~70/117이 실제 클릭) 도 **6.54 → 3.12°** = 절반 회복.
  (코너 6,7은 92~100% PnP 외삽이라 GT_REAR 1.08°는 상한으로만 읽을 것.)

### (3) 상관 — rear가 yaw를 지배
```
                 rear2d vs yaw          front2d vs yaw        elev vs yaw
outside R0    rho 0.709 (p 2.3e-12)   0.441 (p 9.6e-5)    -0.250 (p 0.033)
outside Ours  rho 0.700 (p 7.7e-16)   0.436 (p 6.4e-6)    -0.207 (p 0.040)
night   R0    rho 0.868 (p 2.3e-9)    0.059 (p 0.77 n.s.) -0.258 (p 0.19 n.s.)
night   Ours  rho 0.790 (p 7.8e-8)   -0.188 (p 0.30 n.s.) -0.362 (p 0.042)
```
### (4) 앙각은 판정 불가 (교란)
elevation 층화 결과 데이터가 전부 0~20°에 몰림(outside 0-5° 44 / 5-10° 20 / 10-20° 9 / **20°+ 0장**).
상관도 약함(rho -0.21~-0.36). = "저앙각이 원인"을 이 데이터로는 입증도 반증도 못 함(범위 부재).

### (5) 계열 재평가 필요 [CLAUDE.md 규칙 발동]
"rear 코너를 잘 찍게 한다" 계열은 이미 다수 실패 기록:
STAGE22 coord loss(부분·front 비용) / crop-refine NEGATIVE / mixup NEGATIVE / cutpaste NEGATIVE /
PVNet vector voting NEGATIVE / truncation_addon REJECT / PPD REJECT / DiffPnP3D PARTIAL.
→ 같은 계열 재시도 전에 [소비처] 기준 재평가할 것. 직교 축 후보:
  (a) **depth 센서 활용** — eval frame depth 존재(outside 117/night 43/cad 44, color-aligned mm,
      팔레트 valid 96~100%; noapril만 없음). rear의 flat-view depth 붕괴를 관측으로 대체.
  (b) **rear 비의존 yaw 추정기** — front 면(이미 8px 정확)의 평면 호모그래피/소실점 기반.
      단 GT_FRONT arm 이 5.56인 것은 "현 PnP가 rear에 지배됨"을 뜻할 뿐, 새 추정기의 성능은 미측정.
  (c) **rear GT 자체가 학습신호가 아님** — real rear 라벨 92~100%가 GT pose 외삽이므로
      real 감독으로 rear 위치를 배우게 할 신호가 애초에 없음. depth 실측 라벨링이 선결.
산출: `yaw_lever_diagnosis.{json,md}`.

## depth 가용성/사용성 확인 — 존재 O, rear 관측 X, 앞면평면 yaw NEGATIVE (20:50~21:20)

신규 파일 `data/pallet/results/ralph_meas_v3/{depth_usability_check.py, depth_frontplane_yaw.py}`.

### 1. 존재/포맷 [확인]
- 원본 = `data/pallet/raw_data/{outside,night}/capture*/depth/{fid}.png`, **rgb 와 1:1**
  (outside 8,894 / night 9,134 등, 인덱싱된 depth png 총 19,028).
- uint16 480x640, mm. rgb 와 동일 해상도. `cam_K.txt` = fx 614.18 fy 614.31 cx 329.28 cy 234.53.
- eval GT 프레임 매칭률: **outside 113/113, night 40/40, noapril 0/18**(noapril 은 rgb only — 07-20 기록과 일치).

### 2. 품질 [확인]
- GT 8코너 hull 내 valid depth = outside 98.3% / night 99.1%.
- **color 정렬 확인**: hull 점군 3D centroid vs GT 앞면 중심 3D — 예 [-0.13,0.45,3.20] vs [-0.12,0.45,3.19]
  (수 cm 일치). x,y 까지 맞으므로 aligned [확인]. hull median depth 가 GT centroid z 보다 -0.52m =
  팔레트 depth/2 (앞면을 보는 것) 로 설명됨 = 계통오차 아님.

### 3. ★rear 코너는 depth 로 관측 불가 [확인]
코너 그룹별 (5x5 창 median depth) vs GT pose 의 그 코너 z:
```
dom      group              n     valid%   |z_gap| median
─────────────────────────────────────────────────────────
outside  front(0-3)        447    90.8       0.19 m
outside  rear-top(4,5)     226    77.4       1.04 m
outside  rear-bottom(6,7)  226    94.2       0.98 m
night    front(0-3)        155    89.7       0.26 m
night    rear-top(4,5)      79    96.2       0.73 m
night    rear-bottom(6,7)   78   100.0       0.96 m
```
rear 코너 픽셀의 측정 depth 가 GT 보다 ~1m **가깝다** = 그 픽셀이 보는 것은 팔레트 앞면/윗면.
팔레트는 불투명 상자이므로 rear 코너는 물리적으로 가려짐 = 센서 품질 문제 아님.

### 4. 앞면 평면 -> yaw = NEGATIVE (oracle 인데도 실패)
앞면 영역을 **GT 코너로** 정의(=상한), SHRINK 0.65, 최전면 껍질 15cm, 평면 SVD:
```
dom      N    yaw err med   p25    p75   <3deg%   plane rms   pts
─────────────────────────────────────────────────────────────────
outside 110     14.22      6.85   33.27   13.6     0.011 m   3413
night    36     12.85      5.19   14.92   19.4     0.011 m   3947
```
현재 모델 yaw 6.54/6.04° 보다 **나쁨**. 기전 = 피팅된 법선이 GT 앞면 법선과 74~78° 어긋나고
실제로 거의 ±Y(수직) = **바닥/윗면 수평 슬랩에 피팅됨**. 원인: 저앙각에서 앞면이 14~21px 로 얇고
팔레트 앞면은 포켓(구멍) 투성이라 영역 내 유효 depth 의 상당수가 뒤쪽 바닥을 본다.
(최전면 껍질 선택으로도 해결 안 됨: 13.14→14.22, normal_ang 85→78°.)

### 판정
- depth 는 **거리/translation** 에는 확실히 쓸 수 있다(정렬·품질 확인, centroid 수 cm 일치).
- depth 로 **rear 코너를 관측**하는 경로는 원천 불가(가림).
- depth 앞면평면 -> yaw 라는 값싼 경로는 **기각**(oracle 상한에서 이미 모델보다 나쁨).
- 남은 depth 경로 = 팔레트 전체 점군에 3D 박스/CAD 정합(ICP) 또는 윗면 사각형 경계 검출.
  **미검증·고비용** -> 착수 전 [소비처] 재평가 필요.
산출: `depth_usability_check.{json,md}`, `depth_frontplane_yaw.{json,md}`.

## [20:32] normalized-Hough decoder oracle — O_NUM 에서 정지

- 신규 `scripts/stage0/structural_line_hough_decoder.py` (lock 커밋 `07914a7`).
  centroid/covariance 없이 line hypothesis 와 correlation. supervision·readout 모두
  `p=sigmoid(logit)` (softplus 폐기). arms H0_RAW / H1_TEMPLATE_NORM / **H2_ZERO_MEAN_NCC(primary)**.
- 구현 중 발견 2건 (lock 전, 결과 보기 전):
  (1) **정규화 arm 이 격자 밖 가설을 고름.** RHO_MAX=√2·c+3σ 는 대각 전용 상한이라
      θ=0 에서 |ρ|≤54 만 유효한데 ρ=−54.5 가 후보에 있었고, template 이 비어 0 으로
      나눠 5.7e4 점(H0 2.6e2). → 지지조건 `|ρ| ≤ (|cosθ|+|sinθ|)·c + 3σ` 로 정의역 고정
      (격자의 90.3%). 튜닝 임계 아니라 RHO_MAX 가 근사하던 정확한 기하식.
  (2) **synthetic strata 가 요인 혼재.** border 가 모서리 스침(=짧음), interior_long 이
      full chord(=끝점이 경계 위). → border=긴 clipped chord / interior=margin 3 /
      short=짧지만 interior 로 재작성.
- 계산: coarse 는 Radon 구조(t(θ,q) 가 role·frame 무관 → binning matrix·ΣK·ΣK² 전역 1회).
  naive 는 3e13 연산. coarse 는 근사라 이웃만 고르고 fine 은 정확 평가.
  사전 확인: 긴 strata 는 coarse peak 이 100% fine window(±0.75°) 안, 최대 0.46°.

### O_NUM (10,000 synthetic, H2)

```
                measured   gate
angle  median    0.0114   <=0.02  pass
angle  p99      51.9619   <=0.08  FAIL
offset median    0.0093   <=0.02  pass
offset p99      19.3990   <=0.08  FAIL
```
```
stratum          n      angle med   angle p99
theta_0       1,667      0.0063      0.0169
theta_180     1,666      0.0062      0.0173
theta_90      1,666      0.0066      0.0175
border        1,667      0.0146      0.1903
interior_long 1,667      0.0490      0.3138
short_chord   1,667      8.1565     80.3762
```
**핵심: 경계 절단이 해결됨.** border 가 0.0146/0.1903 — 같은 조건에서 weighted-moment 는
p90 5.84(border<1.5), 8.27(partial) 이었다. 공간 평균을 없앤 것이 그 실패 기전을 제거.
남은 것: (가) oblique long 0.049/0.314 = 유한 segment 를 무한 line template 로 상관하는
편향 + 격자 비등방성(둘 미분리), (나) short chord 는 원리적 불식별(coarse peak 자체가
7~64° 오차, 94%가 window 밖) — decoder 무관.
**판정 `HOUGH_DISCRETIZATION_OR_IMPLEMENTATION_FAIL` → LINE_DEV 미개봉.**
O_HOUGH·floor diagnostic 미실행. M0/M1 학습 0회 유지. 수리 미구현(다음 lock 으로).
산출 `hough_decoder_lattice.json`, `hough_decoder_onum.json`,
문서 `NORMALIZED_HOUGH_DECODER_PROTOCOL.md`, `HOUGH_ONUM_RESULT.md`. tests 680 passed.

## 사용자 제안 검증: 앞면 코너 depth -> Kabsch -> yaw (21:20~21:50) = INCONCLUSIVE

제안: "키포인트를 depth 로 확인해서 그 자세로 풀면 yaw 되지 않나?"
신규 `data/pallet/results/ralph_meas_v3/{depth_kabsch_yaw.py, depth_kabsch_yaw_sweep.py}`.
방향은 타당 — rear 는 가림으로 못 보지만 **앞면 4코너는 관측 가능**하고, 앞면 4점의 metric 3D +
known dims 면 6D 가 결정되므로 **rear 병목을 우회**한다.

### 1차 (depth_kabsch_yaw.py) — inset 의존성 발견
2D=GT(ORACLE) / 2D=R0 예측(REAL), 코너에서 면중심 쪽으로 inset 후 창 내 저-percentile depth:
```
stage   dom      inset0_med  inset0_p20  inset6_p20  inset12_p20  inset12_p35
ORACLE  outside    13.27       12.08        8.61        4.10         5.46
ORACLE  night      12.33       11.16        7.51        5.82         5.81
REAL    outside    13.46       15.05        7.81        5.77         6.18
REAL    night      12.98       12.29        9.94        5.25         6.05
```
= 코너 픽셀 그대로(inset0) 쓰면 13deg 로 최악(경계 혼입 확인). inset 12px 에서 최선.

### 2차 sweep + ★페어 검정 (2D = R0 예측, 실사용 조건) — 유의하지 않음
inset {8,12,16,20,24} x pctl {10,20}, 9x9 창, 3D 모델점 대응 편향 교정(corr), Umeyama(scale) 대조.
같은 프레임의 RGB-only PnP yaw 와 Wilcoxon:
```
dom      best arm    paired N   depth yaw   rgb yaw   better    p
──────────────────────────────────────────────────────────────────
outside  i16_p10        55        5.68       6.65     29/55   0.530
outside  i12_p20        55        5.77       6.65     31/55   0.361
night    i8_p10         20        4.72       6.83     11/20   0.841
night    i12_p20        20        5.26       6.83     11/20   0.674
```
- median 은 개선(6.65->5.68 / 6.83->4.72)이나 **프레임별 승률 53~55%, p=0.23~0.95 = 유의하지 않음**.
- corr(대응 편향 교정)·scale(Umeyama) 은 결과 불변 -> 편향은 원인 아님.
- **커버리지 손실**: depth arm 은 앞면 4코너가 모두 검출돼야 함 -> N=59/113(outside), 20/40(night).
  RGB PnP(73/113, 28/40)보다 적다.
- 기전 [추정]: 코너 4점 depth 샘플링 노이즈가 지배. 이론 한계는 depth 노이즈 2~3cm / baseline 1.1m
  -> atan(0.03/1.1)x2 ~ 3deg 이므로 5~6deg 는 아직 노이즈 지배 구간.

### 판정
**INCONCLUSIVE (기각도 채택도 아님).** 방향은 유효(rear 우회 + 이론 한계 ~3deg < 현재 6.5deg)이나
현 구현(코너 4점 percentile 샘플링)으로는 유의차 없음 + 커버리지 손실.
다음이 있다면: 코너 4점이 아니라 **앞면 표면 세그먼트(최전면 shell)의 수천 점 ↔ 모델 면 정합**,
또는 코너별 국소 평면 피팅 후 교점. 착수 전 [소비처] 확인 필요.
산출: `depth_kabsch_yaw.{json,md}`, `depth_kabsch_yaw_sweep.{json,md}`.

## Method 표 스펙 작성 + ★정본 위반 3건 발견 (21:50~22:10)

신규 `_docs/experiments/self_training/C3_method_table_spec.md` (행/열/캡션/각주/재생성 절차).

### ★오늘 산출한 4행 표는 논문에 그대로 못 넣는다 (위반 3건, 전부 [확인])
1. **평가셋 비정본**: ralph_meas_v3 하네스가 쓰는 프레임의 `objects[0].split` 실측 =
   outside 117(eval 22 / none 92 / **train 3**) · night 43(eval 0 / none 33 / **train 10**) ·
   noapril 18(eval 12 / none 6). 정본 규칙은 `split=="eval"` **56장**(_outside 22/noapril 12/**cad 22**).
   → train 13장 포함(금지) + split 없는 131장을 eval 로 사용(폐기된 구 규칙) + **cad 22장 누락**.
2. **night 은 정본 eval 프레임 0장**: `_night_eval_manual_gt` 43장 중 `split=="eval"` = 0.
   → 표에서 가장 강한 결과(night ADD 0.267→0.189, Success 47.5→65.0)에 **정본 근거 없음**.
3. **inductive 아님**: `metric_split_lock.md` §1.6 세션 split 은 final-test=p09,p07/n09,n08,
   filter-val=p08,p02~p05/n06,n07,n05. ralph PL 풀 실측 = outside p02~p05,p08+cad / night n05~n07
   = **전부 filter-val 세션**이고 평가 프레임도 같은 세션(프레임 단위 홀드아웃뿐)
   → lock §1.3 기준 **transductive(appendix)**, main 불가.

### 표 구성 결론 (구조는 유효)
- 행 4개 유지. 단 "Reproj-only ST" → **"Reproj+flip ST"** 로 정정(실제 가중치가 reproj∧flip).
- 열 = Success rate↑ / Detection rate↑ / ADD↓ 3열. **yaw 열 제외**(4방법 전부 p≥0.29 유의차 없음,
  Limitation 문장으로 처리). **ADD-S 금지**(180° 근사대칭이 미해결 yaw 오차를 흡수).
- 도메인 = 정본 기준 outside22/noapril12/cad22. night 은 eval split 부여 전엔 표에 못 넣음.
  noapril 은 포화 예상 → 각주. cad 는 검출붕괴 이력 → 검출률만 or 제외.
- 캡션·각주 영문 확정본 스펙 문서에 수록(pseudo-GT floor, rear 92~100% 외삽, ADD≠ADD-S, yaw 생략 사유).
- Target 줄에 PL 채택수 대비(Naïve 383/451/159 vs Ours 81/124/49)를 반드시 노출 — 표의 핵심 서사.

### 남은 구멍
페어 검정이 **R0 vs Ours 만** 존재(ADD outside p=0.055 / night p=0.106). **Naïve vs Ours,
rf vs Ours 는 미검정** → 별표 붙이려면 추가 검정 필요.

## [21:00] target semantics — SUPPORTING_LINE_HOUGH_DECODER_VALID

신규 `scripts/stage0/structural_line_target_semantics.py` (lock 커밋 `3be037e`).
**바꾼 것은 target 하나뿐** — decoder(H2)·lattice·support rule·coarse/fine·sigma·gate·
population(27,684) 전부 1b9685c 그대로. S0=기존 유한 segment tube, S1=무한 supporting
line tube `exp(-d²/2σ²)`.

### O_NUM (동일 10,000 synthetic)

```
                 S0 finite    S1 line     gate
angle  median      0.0114     0.0063    <=0.02
angle  p99        51.9619     0.0170    <=0.08
offset median      0.0093     0.0063    <=0.02
offset p99        19.3990     0.0171    <=0.08     → ONUM_PASS
```
```
stratum          S0 med    S0 p99     S1 med   S1 p99
short_chord      8.1565   80.3762     0.0063   0.0176   ← p99 4,600배 개선
interior_long    0.0490    0.3138     0.0061   0.0187
border           0.0146    0.1903     0.0065   0.0187
theta_0/90/180   0.006x    0.017x     0.006x   0.012x
Q1 True / Q2 True → FINITE_SEGMENT_EXTENT_MISMATCH_CONFIRMED
```
theta 10° bin 18개 전부 평탄(med 0.0055~0.0072, p99 0.0126~0.0219) — 격자 비등방성 잔차 없음.

### LINE_DEV (2,393 frame / 27,684 role, sha 00c605b9116e214b)

```
                 S0 finite    S1 line     gate
angle  median      1.4661     0.0066    <=0.05
angle  p90        14.5698     0.0128    <=0.10
offset median      0.5309     0.0061    <=0.05
offset p90         5.7738     0.0128    <=0.10     → PASS
paired: line better 98.8%(angle) / 97.4%(offset), median delta +1.457°
```
```
quadrant                    n      S0 med   S0 p90    S1 med  S1 p90
A border>=1.5 vis>=2    16,404    0.9809   5.3648    0.0064  0.0123
B border>=1.5 vis< 2     5,854    5.1390  29.6020    0.0064  0.0120
C border< 1.5 vis>=2     4,706    1.0410  14.1425    0.0072  0.0176
D border< 1.5 vis< 2       720   41.8257  70.9336    0.0080  0.0368
IN_FRAME_FULL           23,943    1.4444  12.1805    0.0064  0.0124
IN_FRAME_PARTIAL         3,741    1.6033  39.1858    0.0073  0.0215
```

**★ 내 주장 철회**: "short chord 는 어떤 decoder 로도 불식별"(1b9685c) 은 틀렸다.
그 주장은 *line* 에 대한 것이었는데 측정은 전부 *유한 segment target* 이었다.
supporting line 은 다른 객체이고 격자 천장까지 복원된다.
→ `SHORT_SEGMENT_TARGET_ORIENTATION_POORLY_CONDITIONED` 로 대체.

**판정 `SUPPORTING_LINE_HOUGH_DECODER_VALID`.** 단 이건 **완벽한 map 을 읽는 decoder
oracle** 이고 "network 가 그런 map 을 낼 수 있다"는 말이 아니다.
`STRUCTURAL_LINE_MAP_CAPACITY` 여전히 미측정, M0/M1 학습 0회.
⚠ `20eb590` M0/M1 protocol 재사용 금지 — 유한 segment target 을 감독하므로
supporting-line target 기준 새 protocol 을 lock 해야 함.
off-frame role 확장은 다음 screen(모집단+target 동시 이동 금지).
산출 `target_semantics_onum.json`, `target_semantics_linedev.json`,
문서 `STRUCTURAL_LINE_TARGET_SEMANTICS.md`, `TARGET_SEMANTICS_RESULT.md`. tests 689 passed.

## ★정본 평가셋 재계산 — 결론 뒤집힘 (22:10~22:40)

신규 `measC_yaw/{fourway_canonical.py, fourway_canonical_paired.py}`.
정본 56장 전수 확인(outside 22 / noapril 12 / cad 22, split=="eval", reproj>5 탈락 0).

```
domain   method            weight        N       succ%   det%    ADD m    yaw°
──────────────────────────────────────────────────────────────────────────────
outside  Synthetic only    R0           18/22    50.0    81.8    0.228    4.41
outside  Naive ST          h9_outside   21/22    59.1    95.5    0.291    6.15
outside  Reproj+flip ST    rf_outside   21/22    40.9    95.5    0.562    5.94
outside  Ours (loo+flip)   h8_outside   20/22    59.1    90.9    0.158    3.61
noapril  Synthetic only    R0           11/12    91.7    91.7    0.056    0.89
noapril  Naive ST          h9_noapril   11/12    91.7    91.7    0.061    0.95
noapril  Reproj+flip ST    rf_noapril   11/12    91.7    91.7    0.083    1.19
noapril  Ours (loo+flip)   h8_noapril   11/12    91.7    91.7    0.053    0.73
cad      전 방법 검출 붕괴(R0 2/22, rf 0/22, Ours 2/22, Naive NA) -> 표 제외
```
PAIRED (Wilcoxon, ADD, 같은 프레임):
```
dom       vs                N     other    Ours    Ours승    p
outside   Synthetic only    16    0.126    0.158    6/16    0.706
outside   Naive ST          19    0.241    0.170   11/19    0.210
outside   Reproj+flip ST    19    0.545    0.170   14/19    0.0046 ★
noapril   Synthetic only    11    0.056    0.053    7/11    0.206
noapril   Naive ST          11    0.061    0.053   11/11    0.0010 ★
noapril   Reproj+flip ST    11    0.083    0.053    9/11    0.0049 ★
```

### ★판정 (주장 축소)
1. **"self-training 이 synthetic-only 를 이긴다"는 정본에서 성립 안 함.** Ours vs R0 ADD paired
   outside p=0.706(같은 16장에서는 0.126 vs 0.158 로 **R0 가 나음**), noapril p=0.206.
   비정본 셋의 우위(0.358→0.296)는 **검출집합 차이 아티팩트** — R0 는 쉬운 프레임만 푼다.
2. **살아남는 주장 = 필터 품질 비교**: Ours > Reproj+flip(outside p=0.0046, noapril p=0.0049 양쪽 유의),
   Ours > Naive(noapril p=0.0010 유의, outside p=0.210 추세).
   헤드라인 = "같은 self-training 예산에서 **필터가 결과를 가른다 — 잘못된 필터는 baseline 보다 나쁘다**".
3. 검출/Success 상승(det 81.8→90.9~95.5, succ 50.0→59.1)은 **Naive 도 동일하게 달성** → Ours 고유 기여 아님.
4. 표기 규칙: ADD 별표는 vs Reproj+flip / vs Naive(noapril) 에만. **vs Synthetic only 는 별표 금지.**

### 표 강화 옵션 (사용자 결정 필요)
`_night_eval_manual_gt` 43장(33 none / 10 train)에 eval split 부여 시 판별력 있는 2번째 도메인 확보.
비정본에서 night 가 가장 강했음(ADD 0.267→0.189). 단 **정본 평가셋 변경**이라
`_docs/EVAL_SET_CANONICAL.md` + `challenge/tests/test_eval_set_canonical.py` 동시 갱신 필요 = 사용자 결정.

문서 `_docs/experiments/self_training/C3_method_table_spec.md` §6~§8 갱신.

## [21:52] supporting-line map capacity — overfit32 FAIL (양 arm), search2k 차단

신규 `scripts/stage0/supporting_line_map_capacity.py` (lock 커밋 `cc82012`).
target=S1(theta,rho 만으로 생성, 끝점·길이 미사용), loss=map-only balanced MSE,
Hough argmax 는 `no_grad` 라 gradient 없음, unsupported role 은 gradient 0(mask),
support head 제거, arms M0_F50_SLINE / M1_F50_RGB_SLINE.

- 범위 정정 문서 `SUPPORTING_LINE_TARGET_SCOPE_ADDENDUM.md`:
  extent mismatch 는 map-target×line-decoder 조합에만 적용. V2 strip refiner·PEQ·
  dense 12-edge·HCRM 미적용. "앞선 모든 스크린" 표현 철회.
- population 확인: LINE_DEV 27,684 role, sha `00c605b9116e214b` 일치.

### O_LOSS (free logits, 32 frame / 373 role, 500 step)
`angle med 0.0065 p90 0.0129 | offset med 0.0062 p90 0.0116` → **PASS**.
loss 배선·decoder 는 병목 아님이 확정.

### overfit32 (1,500 step)
```
                   ang med  ang p90  off med  off p90  finite
O_LOSS free logit   0.0065   0.0129   0.0062   0.0116
M0_F50_SLINE        0.1268   0.4531   0.0586   0.1822   1.000
M1_F50_RGB_SLINE    0.1426   0.4715   0.0614   0.2139   1.000
overfit gate       <=0.10   <=0.25   <=0.05   <=0.15
primary budget     <=1.00   <=2.00   <=0.50   <=1.00
```
**양 arm 4/4 gate 실패 → `SUPPORTING_LINE_MAP_OPTIMIZATION_FAIL`, search2k 미실행.**
★ 단 두 arm 다 **primary budget 안에 8배 여유로** 들어가 있음(PASS/SAFETY/APPROACH 전부 True).
overfit gate 를 budget 보다 10배 좁게 잡은 결과라, 라벨을 "32장도 못 맞춘다" 로 읽으면 오독.
gate 는 옮기지 않음(cc82012 에 사전 고정).

진단: `map NCC 0.77~0.79`, `peak prob 0.98`, positive MSE 가 negative 의 5배
→ ridge 는 있고 확신도 있으나 흐릿/어긋남. free logit 대비 약 20배 나쁨.
RGB stem(M1)은 전 지표에서 M0 보다 약간 나쁨 — V2 때와 같은 순서.
★ **partial role 붕괴가 사라짐**: full 0.1255 vs partial 0.1478(M0) — 유한 segment target
시절엔 자릿수 차이였다. extent 교정이 oracle 뿐 아니라 학습 상황에서도 유지됨. 단 partial n=47 소표본.

산출 `supporting_line_map_oloss.json`, `supporting_line_map_arms.json`,
`supporting_line_map_population.json`, checkpoints `<ARM>/overfit32.pth`,
문서 `SUPPORTING_LINE_MAP_OVERFIT_RESULT.md`. tests 703 passed.

### 정정 + 정본 PAIRED yaw (22:40~22:55)
- **내 오기 정정**: 위 정본 표의 yaw 열을 6.36/5.99/9.28/5.90 (outside), 0.91/1.11 (noapril) 로
  적었으나 실제 `fourway_canonical.md` 값은 **4.41/6.15/5.94/3.61**, **0.95/1.19** 였다. 파일 값으로 수정함.
  (succ/det/ADD 는 처음부터 정확했고 yaw 열만 오기.)
- 정본 PAIRED yaw (Wilcoxon, 같은 프레임):
```
dom       vs                N     other    Ours    Ours승    p
outside   Synthetic only    16     4.41    5.29     4/16    0.065  ← Ours 가 나쁜 쪽 경계
outside   Naive ST          19     6.15    3.73     6/19    0.225
outside   Reproj+flip ST    19     6.27    3.73     7/19    0.275
noapril   Synthetic only    11     0.89    0.73     8/11    0.024 ★
noapril   Naive ST          11     0.95    0.73    10/11    0.0068 ★
noapril   Reproj+flip ST    11     1.19    0.73    10/11    0.0029 ★
```
- **판정**: yaw 는 도메인 의존. **outside(어려운 도메인)에서는 개선 없음 — 오히려 R0 대비 악화 경계**.
  noapril 만 유의 개선이나 절대값 0.73~1.19° 로 GT floor(0.25°) 근처인 쉬운 도메인.
  outside 의 vs Naive/rf 는 median 은 Ours 우세인데 승률 32~37% = 꼬리(파국)에서 이기고 전형 프레임에선
  약간 지는 분포 [추정, 꼬리 분해 미실시].

## 평가셋 프로파일 — 왜 셋마다 판정이 다른가 (22:55~23:15)

신규 `data/pallet/results/ralph_meas_v3/dataset_profile.py` (GT 기반, 모델 무관).
```
set                   n  dist m   elev°  width px  trunc%  GTreproj  rearClick%
────────────────────────────────────────────────────────────────────────────────
outside_CANON        22    3.66    3.38       269       0     0.68        25
outside_NONcanon     95    2.82    4.94       318      16     1.46        34
noapril_CANON        12    3.48   19.10       273       0     0.52        27
cad_CANON            22    1.68   31.71       547      23     1.80        50
night_all            43    2.60    6.04       384      23     2.42        49
```
앙각 분포(프레임 수):
```
set                 0-5   5-10  10-20  20-90
outside_CANON        14      8      0      0
outside_NONcanon     47     32     14      0
noapril_CANON         0      0      9      3
cad_CANON             0      3      0     19
night_all             6     35      2      0
```

### 판정
1. **noapril 의 yaw 유의 개선은 앙각 때문 [확인]**. noapril 은 median 19.1°, 전 프레임 10° 이상 =
   윗면이 실제로 보이는 레짐. outside(3.4°, 전부 <10°)는 edge-on 이라 rear 가 안 보인다.
   앞서 "noapril 은 쉬운 도메인" 이라던 [추정] 의 실측 근거. **두 도메인은 다른 문제를 풀고 있다.**
2. **cad 검출 붕괴 설명 [확인]**: 거리 1.68m(최근접) + 팔레트 폭 547px(640 중) + 잘림 23% + 앙각 31.7°
   = 근접·고앙각·부분잘림 = 학습 분포 밖. 전 방법 2/22 이하가 당연.
3. **정본 outside 22 vs 비정본 117 의 yaw 차이(4.41 vs 6.54) 설명 [확인]**:
   - 세션 구성이 다름. 정본 = p08(13),p02(3),p05(3),p03(2),p04(1).
     비정본 = **p09(36), p07(27)** + p08(12) 등.
   - 정본이 더 멀고(3.66 vs 2.82m), **잘림 0% vs 16%**, GT 어노 품질 2배 좋음(reproj 0.68 vs 1.46).
   → 정본 22장은 "깨끗하고 잘림 없는" 프레임이라 yaw 가 더 낮게 나온다.
4. **★lock 충돌 발견**: `metric_split_lock.md` §1.6 의 outside final-test = **p09, p07**.
   그런데 정본 eval 22장에는 **p09/p07 이 0장**이고 전부 filter-val 세션(p02~p05, p08)이다.
   = 사용자가 eval 로 표시한 정본 평가셋과 lock 의 final-test 세션이 **완전히 불일치**.
   비정본 117장 쪽이 오히려 final-test 세션 63장을 담고 있었다.
   → 둘 중 하나를 고쳐야 한다(정본 재지정 or lock 갱신). 현 상태로는 "final-test 에서 평가했다"고
     쓸 수 없다.
산출: `dataset_profile.{json,md}`.

## (A)안 실행 — outside final-test(p07/p09) 정본 편입 + 재평가 (23:15~24:00)

### 1단계: split 부여 + 정본/테스트 갱신 (성능 미열람 상태에서 확정)
- `capturepallet07_manual_gt` 27 + `capturepallet09_manual_gt` 36 = **63장에 `split="eval"` 부여**.
  전부 기존 `(none)` 이라 `train` 덮어쓴 것 0. 백업 = scratchpad/split_backup/.
- GT 품질 선확인: 전부 `gt_source=manual`, reproj med 2.30/1.05, >5px 3장(평가 시 자동 제외), 결손 코너 1장.
- **PL 풀 미포함 확인 [확인]**: `real_unlabeled_ralph*` 어디에도 p07/p09 없음 → **inductive-clean**.
- `_docs/EVAL_SET_CANONICAL.md` 갱신(합계 56→**119**, 변경 사유·봉인 소진 명기),
  `challenge/tests/test_eval_set_canonical.py` 갱신(EVAL_FOLDERS/EXPECTED_TOTAL=119,
  신규 `test_final_test_sessions_are_not_in_any_pseudo_label_pool`). **pytest 7 passed**.
- ⚠️ **봉인 소진**: 정본 문서의 `capturepallet07/09` final-test 봉인은 이 시점에 해제. 재봉인 불가.
- night 은 부여하지 않음(사용자 지시).

### 3단계 먼저 보고: p07 과 p09 는 **다른 레짐**
```
set                 n  dist m   elev°  width px  trunc%  GTreproj  rearClick%
outside_ft_p07     27    2.04   10.12       503      37     2.30       31
outside_ft_p09     36    2.85    2.36       313       6     1.05       31
outside_fv         22    3.66    3.38       269       0     0.68       25
noapril            12    3.48   19.10       273       0     0.52       27
cad                22    1.68   31.71       547      23     1.80       50
앙각 bin  p07: 0-5=0, 5-10=12, 10-20=14   /   p09: 0-5=35   /   fv: 0-5=14, 5-10=8
```
→ **p09 = outside_fv 와 같은 edge-on 레짐**(0-5°). **p07 = 근접·중앙각·잘림 37% 의 다른 레짐**.
합치면 해석이 섞이므로 **세션 분리 보고**로 전환(사용자 경고 적중).

### 2단계: 정본 재평가 (weight 는 outside 학습분, p07/p09 는 inductive)
```
domain          method            N       succ%   det%    ADD m
──────────────────────────────────────────────────────────────────
outside_ft_p09  Synthetic only  20/34     44.1    58.8    0.941
(edge-on)       Naive ST        27/34     41.2    79.4    0.447
                Reproj+flip ST  31/34     38.2    91.2    0.581
                Ours            32/34     32.4    94.1    0.351
──────────────────────────────────────────────────────────────────
outside_ft_p07  Synthetic only  15/26     46.2    57.7    0.292
(근접·잘림)      Naive ST        16/26     46.2    61.5    0.290
                Reproj+flip ST  16/26     42.3    61.5    0.319
                Ours            19/26     61.5    73.1    0.262
──────────────────────────────────────────────────────────────────
outside_fv      R0/Naive/rf/Ours  succ 50.0/59.1/40.9/59.1  det 81.8/95.5/95.5/90.9
                                  ADD 0.228/0.291/0.562/0.158
noapril         4방법 전부 11/12, succ·det 91.7, ADD 0.056/0.061/0.083/0.053
cad             전 방법 검출 붕괴(R0 2/22, rf 0/22, Ours 2/22, Naive NA)
```
PAIRED (Wilcoxon):
```
dom              vs               met   N    other -> Ours   better    p
outside_ft_p09   Synthetic only   ADD   20   0.941 -> 0.412   13/20   0.040 ★
outside_ft_p09   Naive ST         ADD   27   0.447 -> 0.350   18/27   0.019 ★
outside_ft_p09   Reproj+flip ST   ADD   30   0.517 -> 0.342   21/30   0.0017 ★
outside_ft_p09   (yaw 전부)                                            0.15~0.82 n.s.
outside_ft_p07   전 쌍 (ADD·yaw)  —    14~16                          0.17~0.98 n.s.
outside_fv       Reproj+flip ST   ADD   19   0.545 -> 0.170   14/19   0.0046 ★
outside_fv       Synthetic only   ADD   16   0.126 -> 0.158    6/16   0.706 (R0 우세)
noapril          Naive/rf         ADD·yaw 11                          0.001~0.007 ★
```

### 판정 (정본·inductive 기준)
1. **★p09(edge-on final-test)에서 Ours 가 Synthetic-only 를 ADD 로 처음 유의하게 이김**
   (0.941→0.412, p=0.040). Naive·rf 대비도 유의(p=0.019 / 0.0017). = 필터 주장 + self-training
   주장 둘 다 이 레짐에서 성립.
2. **단 같은 셋에서 Success(2D<20px)는 오히려 하락**(R0 44.1 → Ours 32.4). 검출은 58.8→94.1 로
   크게 늘지만 새로 검출된 프레임이 20px 를 못 넘고, 2D 정확도 자체는 나빠짐.
   = **3D pose 는 좋아지고 2D 키포인트 정밀도는 나빠지는 분리 현상**. 표에 둘 다 실어야 함.
3. **p07(근접·잘림 37%)은 전 쌍 유의차 없음**(n=14~16 소표본). Ours 가 succ 61.5 로 최고이나 n.s.
4. yaw 는 전 도메인 n.s. (noapril 만 유의, 절대값 0.7~1.2° 로 floor 근처).
5. outside_fv(filter-val 세션)의 "Ours 0.158" 은 final-test 에서 재현되지 않음(p09 0.351).
   → **filter-val 세션 수치를 주 결과로 쓰면 과대보고**.
산출: `fourway_canonical.{json,md}`, `fourway_canonical_paired.{json,md}`, `dataset_profile.{json,md}`.

## [22:42] supporting-line map budget 일반화 — SUPPORTING_LINE_MAP_GENERALIZATION_FAIL

lock 커밋 `d430d82`. `search2k` 원래 eligibility(overfit 통과)는 그대로 두고
**별도 진입점 `search2k-budget`** 추가 — 7c6602a 판정 보존, gate 삭제·완화 없음.
architecture/target/loss/decoder/population/optimizer/seed 전부 cc82012 그대로.
진단 문구 3건 철회(NCC=오차비율 아님 / peak=ridge 증명 아님 / overfit 만으로 RGB 기각 불가).

### 결과 (LINE_DEV512, 5,921 role, epoch5 only)

```
arm                overfit32         e1              e3              e5
M0_F50_SLINE     0.1268/0.0586   9.4189/3.9292   7.2438/3.0222   6.8450/2.7717
M1_F50_RGB_SLINE 0.1426/0.0614  11.4823/4.8387   8.2026/3.5537   7.3931/3.0916
budget <=1.0/<=0.5   APPROACH <=1.5/<=0.75   safety p90 <=2.0/<=1.0
M0 e5 p90 52.2924/24.7414   M1 e5 p90 53.0513/26.1205
train map loss  M0 0.08698→0.07897   M1 0.08940→0.07999
```
**양 arm PASS·APPROACH 전부 False → `SUPPORTING_LINE_MAP_GENERALIZATION_FAIL`,
confirm6k 차단.** 암기 0.1268° → unseen 6.8450° = **54배**, p90 52° = 선이 아예 다른 곳.
정밀도 문제가 아니라 일반화 붕괴.

### 배운 것 / 못 배운 것
- **role semantics 학습됨**: shuffle 50.90 vs normal 6.845 (+44.1°, 기준 +5°). M1 도 +42.8.
  채널 k = role k 는 확실. "어느 선인지"는 알고 "어디인지"를 모름.
- reload parity 0.0, finite 1.000 (양 arm).
- map diagnostics e5: M0 pos MSE 0.1345 / neg 0.0233 / NCC 0.2912 / mass 1468 / peak 0.820
  (overfit 대비 전부 악화). ★ NCC 는 상관 진단일 뿐 오차 비율 아님.
- **full vs partial**: M0 6.6390 vs 8.1009 (n=5,109 / 812) — 20% 차이. 자릿수 붕괴 아님.
  extent 교정은 유지되며 `TARGET_SEMANTICS_MISMATCH` 로 설명 금지.
- **RGB stem**: M0 가 전 epoch·전 지표에서 M1 우세 — unseen 기준 비교라 overfit 때와 달리
  유효. 단 seed 1개·config 1개. `LINE_SUPERVISED_RGB_ADAPTATION_REQUIRED` 근거 없음.

다음은 architecture-capacity screen 이며 후보 2개를 **동시 변경 금지**로 기록만:
A head capacity(더 깊/넓은 map decoder) / B spatial resolution(MAP100 vs MAP200).
산출 `supporting_line_map_arms.json`, checkpoints `<ARM>/search2k_epoch{1,3,5}.pth`,
문서 `SUPPORTING_LINE_MAP_OVERFIT_QUALIFICATION.md`, `SUPPORTING_LINE_MAP_BUDGET_RESULT.md`.

## [00:39, 08-08] seen-vs-unseen 진단 — SEARCH2K_MODEL_UNDERFIT_CONFIRMED

lock 커밋 `24dab23`. 학습 0·architecture 변경 0. 기존 epoch1/3/5 checkpoint 를
세 모집단에서 재평가. D0_SEEN512(학습에 쓴 512) / D1_TRAIN_UNSEEN512(LINE_TRAIN 나머지,
D0 와 group histogram 동일) / D2_LINE_DEV512(기존).

```
M0_F50_SLINE       D0 seen           D1 train-unseen    D2 line-dev
e1              9.2391/3.9424      9.3091/3.9028      9.4189/3.9292
e3              7.2092/3.0027      7.5678/3.0729      7.2438/3.0222
e5              6.6040/2.7023      6.9439/2.8335      6.8450/2.7717
M1_F50_RGB_SLINE
e5              7.3727/3.1009      7.5186/3.0775      7.3931/3.0916
budget <=1.0 / <=0.5      n 5,928 / 5,936 / 5,921
e5 ratio  M0 D1/D0 1.051·1.049  D2/D0 1.036·1.026 | M1 1.020·0.992 · 1.003·0.997
```

**판정: 모델이 자기 학습 데이터에서도 6.60° (budget 의 6.6배).**
seen/unseen 차이 5% 이내, M1 은 offset 이 오히려 unseen 쪽이 나음(0.992).

### 철회
- `5dd6036` 의 **"54배 generalization gap"** — 0.1268(32장×1,500step 전용 fit)을
  6.845(2,000장×1,250step)로 나눈 값이라 **서로 다른 실험**의 차이였다. 무효.
  같은 checkpoint 로 제대로 재면 train→holdout 비율은 **1.04**.
- **"generalization collapse"** 표현 폐기. 일반화 격차는 관찰된 적이 없다.
- `SUPPORTING_LINE_MAP_GENERALIZATION_FAIL` 은 그 screen 의 historical label 로 보존하되
  "epoch5 checkpoint 가 dev budget 을 못 넘었다" 로 읽어야 한다.
- role semantics 는 `ROLE_CHANNEL_IDENTITY_LEARNED` 까지만 (앞 턴에서 정정).

### 검증된 것
- D2 가 `5dd6036` 기록과 **6/6 전부 소수점 일치** → 하네스 불변 확인.
- D0/D1 group histogram 동일·overlap 0·shortfall 0 → 단일 요인 비교 성립.
- map diagnostics 도 세 모집단이 소수점 셋째 자리까지 동일
  (M0 e5 pos 0.134x / neg 0.0232 / NCC 0.28~0.29 / mass 1467~1469 / peak 0.81).

### 다음
architecture 변경 금지. **M0 단독 data × step 2×2**(2k/full × short/long).
head depth·MAP200 근거 없음(oracle 0.006°, 32장 fit 0.1268°).
`GLOBAL_CONTEXT × POSITIONAL_COORDINATES` 는 D0/D1 PASS·D2 FAIL 분기 전용이라 미개시.
산출 `seen_unseen_manifests.json`, `d0_seen512_manifest.csv`,
`d1_train_unseen512_manifest.csv`, `seen_unseen_diagnostic.json`,
문서 `SEEN_VERSUS_UNSEEN_DIAGNOSIS.md`, `SEEN_VERSUS_UNSEEN_RESULT.md`.

## [01:18, 08-08] pose degeneracy vs supporting-line label — POSE_ONLY_DEGENERACY

신규 `scripts/stage0/degeneracy_line_audit.py`. 학습 0·삭제 0·기존 M0 e5 checkpoint 1회 forward.

### provenance 실패 (먼저 보고)
`1,056` / `1,692` 를 repo 에서 못 찾음. `eligibility_audit.csv` = 20,000 frame,
`tiny=True` 537(1,692 아님), coplanar/collinear 컬럼 없음. 데이터셋 `safety_gates` 는
20,000 전부 all_pass. history 에 해당 숫자 없음. → 외부 감사(다른 pool 추정)이므로
**그대로 적용하지 않고** 자체 기준으로 유도해 나란히 보고.

### 기준 (새 임계값 도입 안 함)
projected cuboid(canonical50)에서 `thickness` = 상/하면 대응 4쌍의 중앙 분리거리,
`spread` = 중심화 8점의 작은 특이값. 둘 다 **0.75 canonical50 cell**(= label tube
sigma 1.5 MAP100 px, `cc82012` 에 이미 고정)과 비교.
G2 spread<σ / G1 thickness<σ / G0 나머지 / G3 role 붕괴·비유한.

```
pool             frames    G0      G1     G2   G3   thickness p1/p50
LINE_TRAIN       13,618  10,660  2,500   458    0     0.218 / 1.45
line_search2k     2,000   1,586    349    65    0     0.239 / 1.49
D0_SEEN512          512     405     84    23    0     0.274 / 1.42
```
이 기준으론 LINE_TRAIN 의 21.7% 가 degenerate — 인용된 2.64% 와 정의·모집단이 다름.

### G3 = 0 (전 pool)
LINE_TRAIN 163,416 role 에서 LINE_COLLAPSED 0 · LINE_NONFINITE 0, 모든 frame 12/12 유효.
**pose degeneracy 가 line-label degeneracy 를 만들지 않음** → `STRUCTURAL_LINE_LABEL_DEGENERACY`
미발동, role masking 불필요. coplanar PnP 구성이어도 투영된 8점은 일반위치이고
그 사이 직선은 잘 정의된다.

### M0 e5 stratified (D0_SEEN512)
```
group                frames  roles   ang med  ang p90  off med  loss mean  share%  leverage
G0_nondegenerate        405   4,658   5.8343   50.659   2.2905    0.07776   77.75    0.98
G1_pose_coplanar         84     997   9.0189   65.540   4.3063    0.08099   17.33    1.06
G2_pose_collinear        23     273  12.8970   56.609   5.1943    0.08385    4.91    1.09
```
degenerate 가 실제로 더 어려움(G2 는 G0 의 2.2배) but **leverage 0.98~1.09** — 크기에
비례하는 loss 만 기여. 판정 임계 3.0 에 한참 못 미침.
★ underfit 설명 불가: 전부 지워도 데이터·loss 22% 가 빠질 뿐이고, **G0 만 봐도 5.83°**
로 budget 의 5.8배.

**판정 `POSE_ONLY_DEGENERACY` / filter `KEEP`.**
정책: line 학습은 전부 유지·role mask 0 / unique-6D pose 평가에서만 G1·G2 를
AMBIGUOUS_POSE 버킷 분리 / detection·keypoint 평가는 제외 없음.
tiny 는 지시대로 이 필터에 섞지 않음(의도된 난이도).
**다음 data × step 2×2 는 UNCHANGED** — 필터 규칙을 커밋하지 않으므로 pool·step 수 불변.
산출 `degeneracy_classification.json`, `degeneracy_stratified.json`,
문서 `POSE_DEGENERACY_VERSUS_LINE_LABEL.md`.
