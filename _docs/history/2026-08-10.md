# 2026-08-10

## [00:0x] DIRECT_HOUGH_TOKEN_XY_V0 FULL 판정 — DIRECT_HOUGH_TOKEN_XY_FULL_FAIL

(실행은 08-09 20:31 종료, 판정·기록은 오늘.)

```
step     D0_SEEN512                        D2_LINE_DEV512                     CE
1,250   11.1139/5.4098 p90 61.35/15.59    11.3232/5.5618 p90 61.46/16.81   8.43693
2,500    7.7093/4.4669 p90 48.51/13.19     8.0217/4.7464 p90 50.98/13.99   7.97747
5,000    5.2020/2.7727 p90 39.31/ 9.73     5.4482/2.8880 p90 33.24/ 9.47   7.22405
8,515    4.2185/2.3396 p90 31.36/ 8.73     4.4296/2.4452 p90 30.72/ 8.86   6.84112

primary @8515 D2: 4.429565 F / 2.445153 F / 30.717606 F / 8.864645 F  → overall 0/4 FAIL
tail: >5° 0.4614 · >10° 0.2586 · >2cell 0.5736   n=5,921
Q1 baseline 4.179304 / 1.878792 → **angle +5.99%, offset +30.14% 악화**
40% 기준 2.507582 / 1.127275 미달 → branch A
```
**`DIRECT_HOUGH_TOKEN_XY_FULL_FAIL`. replicate·shuffle·promotion 미실행, CIGM 차단.**
러너가 shuffle 을 스스로 건너뜀(result JSON 에 `shuffle` 키 없음).

- D0−D2 격차 5.0%, CE 8.44→6.84 로 계속 하강 → **일반화 격차가 아니라 FULL 규모에서 underfit**.
  같은 architecture 가 32장·6,000 step 에서는 0.338° 였다.
- per-role: offset 은 1.93~3.23 로 거의 균일(map 계열에서 role 1·3 이 튀던 것과 다른 모양),
  angle 은 2·4·6·7 이 7~8° vs 3·9·10 이 2.4~2.7° 로 갈림. **기록만, 설명 안 함.**
- 범위: `DirectHoughModel` 이 `AbsoluteXY` 를 만들고 호출 안 함(`5040aab` 기록).
  위치정보는 token 으로 들어가나 map 계열에서 −20.1% 를 낸 **additive 분기는 부재** →
  "line-native 표현이 틀렸다" 로 일반화 금지. `DIRECT_HOUGH_TOKEN_XY_V0` 한정.
- 4 oracle(O_DOMAIN/O_GRID/O_TARGET/O_SCORER)은 유효하며 `DirectHoughModel` 을 안 쓴다
  → lattice·target·wiring·scorer 는 무혐의, 실패한 곳은 **encoder→descriptor 경로**.
- 다음 질문: `FROZEN_A1_FEATURE_OR_ENCODER_LIMIT`. 이번엔 아무것도 만들지 않음.
산출 `direct_hough_full.json`, 문서 `DIRECT_HOUGH_TOKEN_XY_FULL_RESULT.md`.

## [08:58:47] ⚠️  Auto Compact (컨텍스트 한계 도달)

- Working directory: `/home/minjae/Documents/github/pallet-pose`
- Trigger: `auto`
- Session ID: `1f06bfde-fd3e-4103-a3ef-711599683949`
- Transcript: `/home/minjae/.claude/projects/-home-minjae-Documents-github-pallet-pose/1f06bfde-fd3e-4103-a3ef-711599683949.jsonl`
- Note: 컨텍스트가 가득 차서 자동 트리거됨 — 다음 세션에서 이 마커 위쪽 작업을 반드시 복원할 것

**TODO (session resume)**: compact 이후 이 마커 위쪽의 작업 내역을 확인하고 이어서 진행


## [13:5x] 사전등록 — direct-Hough long-schedule exposure screen (실행 전 lock)

**질문 하나**: `FULL_OPTIMIZER_EXPOSURE_INSUFFICIENT ?` — @8515 에서 CE 하강 중·geometry 개선 중·
D0/D2 gap 5.0% 이므로 feature limit 을 선언하기 전에 step 축부터 닫는다.
아직 주장 금지: `FROZEN_A1_FEATURE_LIMIT_CONFIRMED` / `ROLE_ENCODER_LIMIT_CONFIRMED` /
`LINE_NATIVE_REPRESENTATION_FAIL`.

신규 `scripts/stage0/direct_hough_full_step_extension.py` (기존 direct_hough_*.py **수정 없음**):
```
변경   optimizer exposure 8,515 → 25,545 step (5 → 15 pass, 정확히 3배)
불변   13,618 frame · frozen A1 · token XY · 12 role query · cross-attention ·
       DirectHoughHead · θ/ρ lattice · target · CE · batch8 · LR1e-3 · WD1e-4 · seed1 · 모든 gate
marks  1703 / 5000(진단) / 8515 / 17030 / 25545   decision = 25545 only, best-step 선택 금지
primary D2_LINE_DEV512 · D0_SEEN512 는 진단 전용 · per-role 은 8515/17030/25545 만 기록
```
- **resume 금지 근거 [확인]**: `DH_full/step_08515.pth` 를 실제 로드 → keys =
  tag/step/model/provenance, **optimizer state 없음** → fresh 0→25,545.
- dead `self.position` 은 삭제·호출·수정 금지 (encoder/head 앞에서 RNG 를 소비하므로
  건드리면 초기화 전체가 바뀌어 step 실험이 아니게 됨). AST 테스트로 강제.
- 판정 라벨 A1~A4 + 미해당 시 `..._INCONCLUSIVE` 사전 고정. 지시가 정량화하지 않은
  "약함/plateau/강하게" 를 사전등록: WEAK_IMPROVEMENT 20% · GEOMETRY_PLATEAU 5% ·
  CE_PLATEAU drop<0.02 & last-pass slope>=-1e-5/step · CE_STRONG_DROP>=0.10.
  A2 의 "40%" 는 기존 Q1 baseline reduction_40 (기록 FULL 이 쓴 것과 동일).

### ★ 발견 — FULL 학습 경로는 bit 재현되지 않는다
재구성 루프 검증용 parity 를 default kernel bit 동일성으로 짰더니 7.2e-4 로 실패했고,
**locked runner 를 자기 자신과 비교해도 실패**했다:
```
locked vs locked, 20 step, default kernel   max |Δ| 1.435e-03   28개 중 21개 tensor 불일치
                                            최대 head.hypothesis.body.2.bias
                                            단 mark 에서 decode 지표는 동일(9.9706°/7.6035cell)
```
→ 전제가 틀린 것이므로 **결과를 보고 임계를 푼 게 아니라 계측기를 교체**.
`torch.use_deterministic_algorithms(True)` + `CUBLAS_WORKSPACE_CONFIG=:4096:8` 에서
control(locked vs locked) **0.000e+00**, structural(locked vs 재구성) **0.000e+00** = 구조 동일 확인.
실제 run 은 결정적 모드를 켜지 않아 기록 FULL 과 동일한 수치 영역 유지.
기존 미해결 "component drift 16~21%" 와 연결되나, 기전(24k hypothesis × batch × 12 role
reduction 순서) 은 **[추정]** — 검증 안 함.

파일: `scripts/stage0/direct_hough_full_step_extension.py`,
`challenge/tests/test_direct_hough_full_step_extension.py` (20 pass),
`_docs/audits/.../DIRECT_HOUGH_FULL_OPTIMIZATION_SCOPE.md`. 전체 786 passed.

## [16:0x] Phase A 결과 — LONG_SCHEDULE_STILL_OPTIMIZING_BUT_TASK_FAIL (A4)

25,545 step(15 pass, 기록의 3배) 완주. **decision @25,545 D2 = 0/4 FAIL** (raw JSON 재계산):
```
step     D0 (진단)                       D2 (primary)                    CE        slope/step
 1,703   9.7019/4.8385 p90 56.44/15.05   9.7985/5.3051 p90 58.52/15.93  8.252227  -4.882e-04
 5,000   5.3469/2.7777 p90 40.59/ 9.97   5.5276/2.9119 p90 34.12/ 9.39  7.244919  -2.015e-04
 8,515   4.3194/2.3224 p90 30.10/ 8.67   4.4309/2.4293 p90 30.90/ 8.70  6.845638  -1.013e-04
17,030   3.8075/2.0109 p90 29.33/ 7.95   3.9864/2.0692 p90 29.00/ 8.23  6.576459  -6.253e-05
25,545   3.4941/1.9265 p90 27.99/ 7.81   3.7357/1.9729 p90 27.84/ 8.28  6.436799  -4.942e-05
gate      1.0 / 0.5 / 2.0 / 1.0  → 네 항목 전부 FAIL
tail @25,545  >5° 0.4077 · >10° 0.2304 · >2cell 0.4938  (n=5,921)
```
**A4 판정 근거** (사전등록 임계 그대로, 사후 조정 없음):
```
WEAK_IMPROVEMENT  두 median<20% (8515→25545)   15.69% / 18.78%   true
GEOMETRY_PLATEAU  두 median<5%  (17030→25545)   6.29% /  4.65%   false ← angle 만 초과
CE_PLATEAU        drop<0.02 & slope>=-1e-5      0.139660, -4.94e-05  false
CE_STRONG_DROP    drop>=0.10                    0.139660          true
```
A3 는 셋 다 필요 → 두 개가 false 라 A4. **추가 step 연장 FORBIDDEN, FEATURE_LIMIT_CONFIRMED=False.**
`FROZEN_A1_FEATURE_LIMIT_CONFIRMED`/`ROLE_ENCODER_LIMIT_CONFIRMED`/`LINE_NATIVE_REPRESENTATION_FAIL`
여전히 **주장 금지**.

- CE slope 는 exposure 가 2배 될 때마다 대략 절반: -4.88e-4 → -2.02e-4 → -1.01e-4 → -6.25e-5 → -4.94e-5.
  아직 내려가지만 이 방식으로 1.0° 에 닿지 않는다.
- **★ 9e1ad5e 의 주장 하나를 좁힌다**: "대체한 image map 보다 나쁘다" 는 @8,515 에서 참이었고
  @25,545 에서는 **angle 축에서 뒤집힌다** — Q1 4.179304 대비 −10.61%(우세). offset 은 여전히
  +5.01%(열세, 단 8,515 의 +30.14% 에서 축소). 기록 FULL 은 수정하지 않되(자기 decision step 에선 참)
  그 비교를 표현 자체의 성질로 이월하지 말 것.
- per-role: 8,515 의 angle 분화(2·4·6·7 최악 / 9·10 최상)가 15 pass 내내 순서 유지, 격차만 압축.
  role 0·3 은 angle 이 거의 안 움직임(3.814→3.766, 2.745→2.670). 기록만, 설명 안 함, 선택 미사용.
- **재현 진단 @8,515** (tolerance·block 없음): D2 angle +0.03% / offset −0.65% / CE +0.07%,
  qualification class 동일. 파라미터 수준 비결정(20 step 1.435e-3, 21/28 tensor)에도 집계 median 은
  0.03~1.89% 안. component 일치 여부는 별개 질문이며 미해결.
- D0−D2 격차 전 구간 5~6% 유지 → 이 run 은 끝까지 일반화 문제가 아니었다.

**결론**: exposure 질문 종료, 답은 "아니다(주 병목 아님)". 단 A3 가 아니므로 feature limit 확정도 아니다 —
step 축이 더 이상 답이 있는 곳이 아니라는 것까지만. **Phase B 는 질문으로서 개방 가능**(A1 아님),
단 이 결과 commit → 별도 사전등록 commit 이후에만 착수. architecture NOT_LOCKED, CIGM BLOCKED.
산출 `direct_hough_long.json`, 문서 `DIRECT_HOUGH_LONG_SCHEDULE_RESULT.md`.

## [18:4x] 사전등록 — Phase B late-A1 adaptation screen (실행 전 lock)

Phase A 가 A4(=A1 아님)이므로 Phase B 개방. 질문 하나:
`DOES_LINE_SUPERVISED_LATE_FEATURE_ADAPTATION_HELP ?`

신규 `scripts/stage0/late_a1_adaptation_screen.py` (기존 파일 무수정):
```
F0_FROZEN_A1          Phase A 그대로
F1_LATE_A1_TRAINABLE  동일 + net.vgg[19:27] 에만 gradient
동일   role-query encoder · DirectHoughHead · lattice · target · CE · token XY ·
       query수 · attention depth · batch8 · WD1e-4 · seed1 · pool · marks · gate
```
**PHASE 13 실측 (이름 추측 아님)** `[확인]`:
```
A1            DopeNetwork 54,814,296 params, child 15개
F50 생산자    net.vgg (Sequential 27), 7,340,480 params, out 128x50x50
마지막 pool   net.vgg[18] (이후 pool 없음)
unfreeze      net.vgg[19:27] = conv 256→512→512→256→128, 5,014,912 params (8 tensor)
              net.vgg 의 68.3%,  A1 전체의 9.15%
BN/GN/LN/IN   A1 전체 0개 · vgg dropout 0개  → running-stat 정책 자체가 공허
              → 양 arm 모두 A1 은 .eval() 유지 (차이를 더하는 게 아니라 없애는 선택)
net.vgg(x) == net(x) hook feature   max|Δ| 0.000e+00 → grad 경로서 vgg 직접 호출 안전
```
- ★ **범위 명시**: "마지막 block" 이 net.vgg 의 2/3 다. DOPE VGG trunk 가 마지막 stage 에
  파라미터를 몰아둔 탓. block 경계로는 [19:27] 이 맞지만(18 이후 pool 없음, 쪼개면 임의 경계),
  null 결과는 `LATE_A1_BLOCK19_26` 에 귀속되지 "A1 adaptation 일반" 이 아니다.
- LR 사전고정: head/role encoder 1e-3 그대로, unfrozen A1 = **1e-4 (head×0.1) 하나**, sweep 금지.
  F1 실패 시 "다른 A1 LR 이면 됐을지도" 는 이 screen 의 한계로 명시하되 추격 금지.
- **F0 재사용 = 증명 후에만**: 결정적 모드에서 Phase B trainer(unfreeze off) vs locked
  DH.train_network → control 0.000e+00, **F0 vs locked 0.000e+00** → `F0_SOURCE=phase_a_reuse`.
  Phase A 의 25,545 결과가 그대로 F0 arm (2시간 절약, 코드경로 동일성 증명됨).
- 판정 라벨 4종 + `LATE_A1_ADAPTATION_INCONCLUSIVE` 사전 고정. 미정량 "F0≈F1" 은
  `SIMILAR_TO_F0` = 두 D2 median 모두 상대 5% 이내로 사전등록. OVERFITS = F1 train CE < F0
  인데 D2 median 하나라도 F0 보다 높음. 라벨 배타적이지 않아 기재 순서대로 평가하되
  overfit 조건은 라벨과 무관하게 항상 보고.
- PASS 해도 candidate 일 뿐. `LINE_STAGE_ARCHITECTURE_LOCKED` 는 4개(task+safety /
  replicate 동일 class / role-shuffle causal / whole LINE_DEV 2,393장·27,684 role) 전부 필요.
  그 전엔 ARCHITECTURE_COMPLETE 라 쓰지 않음. CIGM 계속 차단.

파일: `late_a1_adaptation_screen.py`, `challenge/tests/test_late_a1_adaptation_screen.py`(17 pass),
`_docs/audits/.../LATE_A1_ADAPTATION_SCOPE.md`.
