# 2026-08-01

## 코드/설정 변경 — **없음** (working tree clean)

이 세션은 07-29에 시작해 날짜를 넘겼다.  Stop hook 이 지목한 아래 파일들은
**07-29 세션의 산출물이고 이미 커밋·push 되었으며**, 상세 기록(파일별 Before/After·
이유·디버깅)은 `_docs/history/2026-07-29.md` 에 있다.  여기 복붙하지 않는다(drift 방지).

```
파일                                                   커밋       기록 위치
──────────────────────────────────────────────────────────────────────────────────
scripts/stage0/paper_s2_mechanism_diagnostic.py        9045c69   2026-07-29.md
challenge/tests/test_paper_s2_mechanism_diagnostic.py  9045c69   2026-07-29.md
Deep_Object_Pose/common/belief_residual.py             a6bf187   2026-07-29.md
Deep_Object_Pose/common/utils_belief.py                a6bf187   2026-07-29.md
Deep_Object_Pose/common/utils_dataset.py               a6bf187   2026-07-29.md
scripts/stage0/paper_s2_micro_arch_screen.py           a6bf187   2026-07-29.md
challenge/tests/test_paper_s2_micro_arch_screen.py     a6bf187   2026-07-29.md
```

확인: `git status --porcelain` 공백, 각 파일 `git log -1` = 2026-07-29.
원격/로컬 HEAD 일치(a6bf187).

## 조회 작업 — 과거 벡터 트랙 실패 회상

사용자 질문("vector 써서 모델 만들려다 안 됐던 것 자세히")에 답하기 위해 내부 기록만
조회했다(재실행·재학습 없음).  1급 소스 = memory `pvnet-dense-vector-voting-negative-result`
+ `_docs/history/2026-06-22~29, 07-05`.

정리 내용: dense voting(STEP5) / affinity decode(STEP6) / sparse edge(STEP7) /
center-offset(STEP8) / side-face anchor(STAGE15) / STAGE24 재시험 6종 전부 heatmap 패배.
원인이 3단계로 좁혀진 과정(마스크 문제 기각 → undertrain 기각 → 표현/feature 한계),
그리고 "base feature에 depth 부재 → 데이터로 해결"은 **미확정 논리 점프**라는 정정까지
포함해 전달.

### ★ 조회 중 발견한 사실 (기록 가치 있음)

벡터 트랙 산출물의 **평가 결과 폴더 4종이 삭제되어 있다**:

```
경로                                                   상태
──────────────────────────────────────────────────────────────
weights/stage5_voting/                                 존재
weights/stage24_vec_newdata/{voting,sparse,control}/   존재
data/pallet/eval_results/stage6_affinity/              없음
data/pallet/eval_results/stage7_sparse/                없음
data/pallet/eval_results/stage8_offset/                없음
data/pallet/eval_results/stage15_face_center_offset/   없음
```

즉 affinity·sparse edge·center-offset·side-face anchor 의 **원자료 수치는 재확인
불가**하고, 근거는 memory 와 `_docs/history/2026-06-2*.md` 서술에만 남아 있다.
weights 는 남아 있어 필요하면 재평가는 가능하다.
(memory `pvnet-dense-vector-voting-negative-result` 는 이 경로들을 "결과 보존됨"으로
적고 있으므로, 그 서술은 현재 파일 상태와 어긋난다 — 인용 시 주의.)

## 연결 — 벡터 실패와 최근 진단의 관계

07-29 micro screen 에서 나온 수치가 벡터 트랙 실패의 배경을 보강한다:
synthetic 학습 pool 의 far/depth 2D error 중앙값 **2.12px** (max 16.02, >20px 0개) vs
real F2 **43.3px** (max 160.7).  벡터 5종이 고치려던 far/back 부정확이 **학습 데이터에
존재하지 않는** 실패 모드이므로, decode/head 를 바꾸는 접근이 전부 진 것과
B1 residual 이 시험조차 안 된 것이 **같은 sim2real 전이갭**으로 설명된다.

---

## PalletGraph-6D (MSL + DGP) oracle line utility screen — MSL REJECT

### 목적

MSL(Mask-Supported Semantic Lines) + DGP(Dimension-Guided Graph Pose)가 ep57
point-only 보다 유효한지 **oracle gate 로 먼저** 판정. full architecture 학습 아님.
과거 실패한 vector/offset/voting/endpoint 표현은 재도입하지 않고, line 을 keypoint 로
되돌리지 않는 **raster line map = DGP 가 직접 읽는 2D residual** 로만 표현.

### 신규 파일

- `Deep_Object_Pose/common/pallet_graph_geometry.py` — 3D graph, semantic edge class,
  180° symmetry. **edge class 를 하드코딩하지 않고 실제 3D 좌표에서 유도**해
  convention 변경 시 조용히 뒤집히지 않게 함.
  검증: width(X)=1.1=W, depth(Z)=1.3=D, vertical(Y)=0.12=H,
  permutation (5,4,7,6,1,0,3,2,8) residual 5.6e-17 (= 기존 ADD-S permutation 과 동일).
- `Deep_Object_Pose/common/dimension_guided_graph_pose.py` — point+line full SE(3)
  damped Gauss-Newton (Lie algebra, depth/condition/step guard, 180° 두 가설 refine).
- `scripts/stage0/paper_s2_palletgraph_line_screen.py` — P0~P4 runner.
- `challenge/tests/test_paper_s2_palletgraph_line_screen.py` — 25 테스트.

### 결과 (strict mechanism-val N87, baseline gate PASS 재현)

```
arm        succ    yaw(mod180)  corner_sym   reproj    비고
──────────────────────────────────────────────────────────────────────
P0         0.805   6.025        0.4516       23.16     기존 OpenCV PnP
P1         0.805   6.734        0.3326       22.22     DGP, lambda_line=0
P2(oracle amodal)  6.5~7.3      0.399~0.411            FAIL (3개 lambda 전부)
P3(oracle assoc)   6.9~7.4      0.378~0.384            FAIL
P4(generic edge)   7.2~7.3      0.367~0.399            FAIL
```
pose success 는 **모든 arm 에서 0.805 로 완전 동일** — line 이 실패 프레임을 하나도
살리지 못했다.

### 디버깅 2건

1. **solver 가 전혀 안 움직임**(iters=1, 개선 0). 원인 = Huber 선형영역에서 유한차분
   Hessian 이 0 으로 붕괴 → step 이 clip 한계까지 튀어 전부 기각.
   Before: `hessian_diag = max(fd, 1e-8)` / After: **trust-region floor**
   `max(fd, |grad|/trust)` (trust: rot 0.05rad, trans 0.05m) + backtracking 6회.
   수정 후 8°/0.147m 섭동 → point+line 이 0.01°/0.006m 로 회복, line-only 도 1.04°.
2. **lambda_line 미보정**. E_point median 27.97(max 319) vs E_line 5.58 로 point 가
   5배 압도 → line 이 pose 를 0.002° 만 움직임. "line 이 안 통한다"가 아니라
   "가중치를 안 줬다"가 될 뻔함. Phase G4 원칙을 이식해 **E_line/E_point fraction
   0.25/0.50/1.00 으로 사전 보정**(lambda 2.76/5.52/11.04), 셋 다 보고.

### ★ 방법론 발견 — 집계 median 아티팩트

P3(fraction 1.0)가 close-range 에서 corner error **20.4% 감소**로 PASS 처럼 보였으나,
**같은 frame paired 로는 개선 6/13, Δcorner +0.0004m 로 오히려 악화**.
subset median 은 frame 순위가 바뀌기만 해도 움직인다.
→ gate 에 **paired 보강 조건**(같은 frame 이 65% 이상 개선)을 추가해 허위 PASS 제거.
같은 이유로 P0→P1 parity 도 집계로는 FAIL(yaw +0.71°)이나 paired 로는 중립
(Δyaw median +0.029°, pose success 동일).

### ★ 이 실험이 시험하지 못한 것

DGP 는 초기 pose 가 필요하고 그 초기값은 point-only PnP 다. **point 가 실패한
17/87 프레임은 초기 pose 가 없어 fallback** 되어 line 이 개입할 기회가 없었다.
그런데 최상위 가설이 바로 그 경우다 → **가설의 핵심 population 미검증**.
실제 비교는 point 가 이미 성공한 70 프레임이고, truncated common-success 는 6 프레임.

### ★ MSL 전제 붕괴 — mask support

ep57 seg mask 가 real N87 에서 매우 약하다: **31/87(36%) 프레임은 mask 최대 확률조차
0.5 미만**, mask 면적 median 0.40%. "Mask-Supported" 의 mask 가 real 에 사실상 없다
→ Phase F 의 L2(mask support) arm 은 oracle gate 와 무관하게 현재 mask 로 시험 불가.

### 판정

- **MSL: REJECT** (지시문 Phase E4: P2 FAIL → MSL 종료). Phase F 미실행.
- **DGP: INCONCLUSIVE** (parity 집계 미달, paired 중립).
- **SAP: DEFERRED**.
- 단 '시험하지 못한 것' 때문에 "line 은 원리적으로 무용" 으로 일반화 금지.

### 무결성

final-test open count 0, checkpoint SHA 불변, 데이터셋 파일 수정 0건(테스트로 확인),
신규 렌더링 없음, 학습 없음. pytest `challenge/tests/` **117 passed** (신규 25 포함).

### 판정 정정 (시각화로 발견) — MSL REJECT → INCONCLUSIVE

사용자 요청("시각적으로 안 되는 부분")으로 에너지 지형을 그려보다 **판정을 뒤집는
사실**을 발견. 스크립트 `scripts/stage0/paper_s2_line_failure_viz.py`,
산출 `.../paper_s2_palletgraph_line_screen/why_line_failed/`.

1. **line 정보는 있다** — oracle map 을 그린 GT pose 기준 ±10° slice 에서 E_line 은
   0.276(GT) → 3.9/4.5 로 정상 최소·단조. 즉 "line 에 pose 정보가 없다" 가 아니다.
2. **도달을 못 한 것** — DGP 가 실제 출발하는 point-PnP pose 근처에서는 E_line 최소가
   GT 방향이 아니다. basin 이 GT 주변에 좁고, 6 iteration × trust 2.9° 로 진입 실패.
3. **구현 불일치** — P2 는 amodal line map 인데 energy 는 `visibility_aware=True` 로
   계산해 pose 마다 edge 집합이 바뀐다. mean-over-edges 라 edge 수가 바뀌면 값이
   점프 → 지형이 계단형(그림 1 오른쪽에서 육안 확인).

→ 이번 FAIL 은 **line 무용의 증거가 아니라 최적화 도달 실패**.
**MSL: REJECT → INCONCLUSIVE 로 정정.** Phase F 는 여전히 미실행이나 사유가
"MSL 이 틀렸다" 가 아니라 "현재 설계로는 시험 불가" 로 바뀐다.

부수 확인: yaw>60° 프레임 3개는 corner error 1.03m / reproj 110px 로 **실제로 크게
틀린 것**이지 W/D 90° 혼동 metric 아티팩트가 아니다(내 초기 추정 반증됨).

다음 admissible experiment 수정:
1. DGP energy 연속화(visibility 고정 또는 soft weight) + sample 단위 정규화 +
   iteration/trust 확대 또는 multi-start. 이걸 고치기 전 line 결론 확정 불가.
2. point 실패 17 프레임을 시험 가능하게 — line + 알려진 W/D/H 만으로 초기 pose 를
   세우는 경로 필요.
3. mask support 선결(real 31/87 붕괴).

---

## PalletGraph-6D 재검증 (DGP v2) — 직전 REJECT 뒤집힘, G0/G1 PASS

### 목적

직전 oracle-line FAIL 이 "line 정보 부재" 가 아니라 (1) 계단형 energy (2) basin 미도달
(3) point-fail 17 프레임 미실행 때문이었으므로, 세 원인을 고치고 global search 로 재검증.

### 코드 변경 (기존 파일 확장, 신규 runner 없음)

- `Deep_Object_Pose/common/dimension_guided_graph_pose.py` — **continuous line energy v2** 추가.
  Before: candidate 마다 visibility 재판정 + `mean over edges` → edge 집합이 7↔9 로 튀며 계단형.
  After: `fixed_edge_set()` 로 **frame 당 1회 고정** + **sample-wise 정규화**
  (`sum_sample / sum_weight`) + **distance field**(sigma = 0.020/0.010/0.005 × image diagonal)
  + **forward 0.5 + reverse 0.5**(관측 support → 모델 edge 역방향).
- `scripts/stage0/paper_s2_palletgraph_line_screen.py` — `--g0`, `--g1` 서브커맨드 추가.
  ⚠ `if __name__` 블록이 파일 중간에 있어 뒤에 append 한 정의가 NameError 를 냈다 →
  블록을 EOF 로 이동 (2회 발생, 이후 append 시 주의).
- `challenge/tests/test_paper_s2_palletgraph_line_screen.py` — 12 테스트 추가(총 37).

### Phase B5 continuity — 전부 통과

```
T1 GT 최소 0.001416      T3 인접 0.25° max|dE| = 0.004043 (v1 계단 제거)
T4 order |dE| = 0        T5 sweep 중 edges 9 고정 (v1: 7↔9)
T6 class permute 448x↑   T7 180° |dE| = 0        T8 grad finite
```

### G0 — global yaw identifiability (UPPER BOUND, GT t/roll/pitch)

```
arm      overall  point-fail  truncated  top1_err_med
G0-LA    1.000    1.000       1.000      0.0°
G0-LV    1.000    1.000       1.000      0.0°
G0-LO    1.000    1.000       1.000      0.0°   ← 실제 image gradient fragment 만
G0-P     0.678    0.000       0.353      3.0°
G0-PL    0.931    0.882       1.000      1.0°
```
gate(overall≥0.80 / point-fail≥0.60 / truncated≥0.60) **PASS**.

### G1 — translation upper bound (roll/pitch/ty = GT, yaw/tx/tz 탐색)

search prior 는 `paper_4pallet_mask_v1` 3,000 frame 에서만 유도(N87 GT 미사용, 테스트로 강제).
tz search 1.481~4.677 m (train p1..p99 = 1.747~4.411).

```
slice           n    yaw_med   t_med(m)  corner(m)  reproj(px)
ALL            87    0.188°    0.0044    0.0051     2.32
point-fail     17    0.250°    0.0036    0.0055     2.77
truncated      17    0.313°    0.0043    0.0055     2.77
```
point-fail 17/17 이 yaw≤10°(≤5° 도 17/17), positive depth 17/17 → gate **PASS**.
point-success 70 도 corner 70/70 개선 (0.4516 → 0.0051 m).

### ★ 이 수치를 능력으로 읽으면 안 된다

line map 을 **GT pose 로 그리고** roll/pitch(+ty)도 GT 다. corner 5.1mm 는 물리적 정확도가
아니라 **oracle 자기참조**(GT 로 만든 신호에서 GT 되찾기)다. semantic class 라벨도 GT.
→ G0/G1 이 증명한 것은 **정보량 상한**뿐, deployable 성능이 아니다.

### E1 ground prior audit — 전부 부재

camera-ground normal / camera height / ground-plane constraint / IMU **없음**
(N87 JSON 의 `camera_data` = width/height/intrinsics 뿐).
→ G2 는 roll/pitch 를 prior grid 로 탐색해야 해 **5D**(yaw×tx×tz×roll×pitch)가 되고,
G1(3D, 87프레임 350초) 기준 3×3 grid 로도 ~52분, 5×5 면 ~2.4시간. **이번 실행에서 미완**.

### 데이터셋 audit — paper_4pallet_mask_v1

10,000 frames / mask_rle·dims·pose·K·cuboid 100% / **V=8 100%, truncation 0%**.
⚠ 지시문은 "asset 4종" 이라 했으나 실제 `dimensions_m` 은 **621종 이상 연속 랜덤화**
(W 0.87~1.33 / D 0.87~1.33 / H 0.12~0.22). 이번엔 search prior 유도에만 사용, 학습 0건.

### 판정

- **DGP-v2: ACCEPT** (연속 energy 없이는 global search 자체가 불가능했음)
- **Semantic line information: ACCEPT (upper bound 한정)**
- **Learned MSL: HOLD** (G2 미완)
- 직전 세션의 "MSL REJECT" 는 **뒤집힘**.

### 무결성

final-test open count 0, checkpoint SHA 불변, 데이터 파일 수정 0건, 학습 0건,
search prior 는 허용 root 하나에서만. pytest `challenge/tests/` 통과.

---

## SAI (Semantic-Axis Initialization) — GT-free geometry capability 검증

### 목적

G0/G1 은 GT 성분(translation/roll/pitch)을 써서 **정보량 상한**만 증명했다.
이번엔 GT R,t 와 point-PnP 초기값을 **전혀 쓰지 않고** semantic line + known W,D,H,K
만으로 full rotation/translation 후보를 만들 수 있는지 판정.

### 신규/수정

- `Deep_Object_Pose/common/semantic_axis_initialization.py` (신규) —
  weighted TLS line fitting → class 별 vanishing point → `normalize(K^-1 v)` 로
  camera-frame axis → SVD orthogonalization → sign 후보 열거 → line-plane
  objective(`l^T K R a = 0`) SO(3) refinement.  **함수 시그니처에 GT R,t 없음**(테스트 강제).
- `scripts/stage0/paper_s2_palletgraph_line_screen.py` — `--sai-rotation`,
  `--sai-fullpose` 추가.  blind evidence(npz, key whitelist) 생성/감사 포함.
- `challenge/tests/...` — SAI 테스트 15개 추가 (총 143 passed).

### ★ 디버깅 — 서브샘플 누출이 FAIL 로 위장

첫 rotation gate 가 FAIL(observable axes median 1.0, unobservable 78/87).
원인: `build_continuous_field` 의 `MAX_SUPPORT_SAMPLES=60`(G1 reverse-term 비용 제어용)
서브샘플이 SAI evidence 로 그대로 나갔다.  60 px 가 여러 edge 에 흩어져 connected
component 가 조각나 길이 필터에 전부 탈락.
Before: `support_out[c] = _subsample(stacked, 60)` 항상
After: `subsample_support` 파라미터 추가, evidence 생성 시 **False**(full support).

```
                        수정 전   수정 후
observable axes median   1.0      3.0
unobservable             78/87    1/87
point-fail top3_rot<=10° 0/17     17/17
overall  top3_rot<=10°   0.034    0.851
```
이번 세션 **두 번째**로 구현 결함이 FAIL 로 위장한 사례.

### Rotation gate — PASS

```
point-fail top3_rot<=10°   17/17   (>=10)
point-fail top1_yaw<=10°   17/17   (>=12)
overall    top3_rot<=10°   0.851   (>=0.70)
truncated  top3_yaw<=10°   1.000   (>=0.60)
```

### Full-pose (SAI translation + CGR) — 형식상 PASS, 그러나 반박됨

L0 valid 86/87, point-fail 17/17.  yaw 0.74° / rot_sym 1.84° / t 0.032 m /
corner 0.045 m (전부 중앙값) 로 좋아 보이나 —

**fixed-GT reprojection 중앙값 155.6 px** (baseline C0 23.16 px).

원인 [확인]: `rotation_sym_deg > 90°` 인 프레임이 **30/86**.  `rotation_error_sym_deg`
는 180° 등가를 이미 흡수하므로 179° 는 flip 이 아니라 **완전히 다른 회전(pallet 상하 반전)**.
2D line 에는 방향 부호가 없어 `l^T K R a = 0` 이 `a -> -a` 에서도 성립 → 모든 sign 후보의
line-plane 에너지가 **동일** → vertical 축 위/아래를 line 만으로 고를 수 없다.
(합성 검증에서 top1_rot ~180°, top3<=10° 15/15 로 이미 관측했던 현상.)

**gate 설계 결함**: symmetry-aware 지표만 요구하고 index-wise reprojection 을 조건에
넣지 않아 축 부호 실패를 통과시켰다.  다음 gate 는 둘을 함께 요구해야 한다.

### 부수

- point activation 프레임 **0개**(사전 고정 threshold 미달) → point integration 미측정.
- blind evidence audit 87 frames, key whitelist 위반 0.

### 판정

- **SAI rotation: ACCEPT** — GT 없이 올바른 rotation 을 **후보 집합 안에** 만든다(핵심 성과).
- **SAI translation: INCONCLUSIVE** — 옳은 rotation 위에서는 t 오차 0.03 m 로 좋으나
  잘못된 후보가 선택되면 함께 무너진다.
- **CGR: ACCEPT** — 연속 energy 로 안정 정제, negative depth 0.
- **Learned MSL: HOLD** — 축 부호 disambiguation 선결.

### 다음

1. 축 부호 disambiguation: 상하 비대칭 evidence(포크 슬롯/그림자/접지선) / ground prior /
   후보별 appearance consistency 재순위.  line 기하만으로는 원리적으로 불가.
2. gate 를 symmetry-aware + index-wise reprojection 동시 요구로 강화.
3. point activation threshold 재보정.
4. learned mask/MSL 은 1 이후에만.

---

## PPD (Pallet Polarity Disambiguation) — oracle gate PASS, frozen heatmap FAIL

### 목적

SAI 가 만든 unsigned rotation 후보 중 **vertical polarity(top/base)** 를 고를 수 있는가.
직전 실패: 30/86 상하 반전, indexed reproj 155.6px.

### 신규/수정

- `Deep_Object_Pose/common/pallet_polarity_disambiguation.py` (신규, 허용된 1개) —
  signed metric(`vertical_polarity_error_deg`, `signed_rotation_error_deg`,
  `fixed_indexed_reprojection`) + candidate grouping + frozen heatmap scorer +
  5-class polarity edge 유도.
- `scripts/stage0/paper_s2_palletgraph_line_screen.py` — `--polarity {heatmap,oracle_line}` 추가.
- 테스트 13개 추가 (총 **156 passed**).

### ★ Metric 정정 — 직전 "full-pose PASS" 는 FAIL

`rotation_error_sym_deg` **자체는 결함이 아니었다**. 검증:

```
                    기존 sym   signed    polarity   indexed reproj
yaw+180 (허용)        0.00°     0.00°     0.0°       0.00 px
width축 180 (금지)    180.00°   180.00°   180.0°     128.22 px
depth축 180 (금지)    180.00°   180.00°   180.0°     128.22 px
```
metric 은 inversion 을 180° 로 표시했고, **gate 가 subset median 을 써서 놓쳤다**
(30/86 이 뒤집혔어도 절반이 정확하면 median 1.84°). 직전 보고의 PASS 를 FAIL 로 정정.

### 결과 (n=86)

```
scorer              inversion    indexed reproj   yaw     point-fail correct
S0 line-only        30/86        155.6 px         —       —
H0 frozen heatmap   26/86         16.5 px         1.81°   8/17
O0 oracle line       3/86          7.4 px         1.75°   17/17
baseline ep57       —             23.16 px        —       —
```

- **Frozen heatmap gate FAIL**: inversion 26/86(기준 ≤10), signed>90° 0.314(≤0.10),
  point-fail 8/17(≥12) 미달. 단 reproj 는 89% 감소(155.6→16.5px)로 재순위 자체는 작동.
  point 가 약한 곳에서 heatmap 도 약한 것은 같은 feature 라 당연 [추정].
- **Oracle polarity-line gate PASS**: polarity accuracy 0.965, inversion 3/86,
  signed>90° 0.035, point-fail 17/17, reproj 7.39px(155.6 대비 95%↓, baseline 의 32%),
  yaw 1.75°, negative depth 0. 전 기준 통과.
- **`gt_upright_available` 86/86** — 올바른 upright 후보가 **항상 후보 집합 안에 있었다**.
  즉 generation 이 아니라 순수 **selection** 문제였음이 확증됨.

5-class(top_width/top_depth/base_width/base_depth/vertical)는 keypoint id 하드코딩이
아니라 object-frame vertical 좌표에서 유도(검증: 2/2/2/2/4).

### 판정

- SAI-U candidate generation: **ACCEPT**
- Frozen heatmap PPD: **REJECT** (가장 싼 해결책이나 불충분)
- Oracle polarity-line representation: **ACCEPT (upper bound)**
- Learned PPD: oracle gate PASS 이므로 capability test 진행 대상
- Point uncertainty: DEFERRED

### 무결성

final-test open 0, checkpoint SHA 불변, data 수정 0건, 학습 0건(이 단계까지),
oracle evidence 는 GT pose 로 생성하되 scorer 에 GT 미전달.

---

## Learned PPD 준비 — target gate FAIL 로 학습 미실행

### 목적

oracle 5-class polarity line(inversion 30→3/86)을 학습된 head 가 재현하는지 판정.

### 신규/수정

- `Deep_Object_Pose/common/polarity_aware_line_head.py` (신규) — 5-class target
  generator, fresh mask head, PolarityLineHead(5 raster logits only), soft gate,
  line/contrast/outside loss.  runtime 100×100 feature 탐색(레이어 index 하드코딩 금지).
- `pallet_polarity_disambiguation.py` docstring 정정 — "sym metric 이 inversion 을
  숨겼다"는 **틀린 설명**. 실제로는 metric 이 180° 로 정확히 표시했고 **gate 가 subset
  median 을 써서 30/86 tail 을 놓친 것**.
- 테스트 10개 추가 (총 **166 passed**).

### Oracle 실패 3/86 triage — 분류 E (energy tie)

3/3 전부 night, non-truncated, corner 8개 가시, upright 후보 존재.
margin 0.0156 / 0.0122 / 0.1569 (정상 median 0.3275, p10 0.0673).
**F(evidence 오류)·G(projection 오류) 0건** → target/evidence 수정 불필요,
oracle ceiling 한계로 기록.

### 데이터 audit 정정

지시문의 "asset 4종"이 **맞다**(Pallet_0~3). 이전 보고의 "621+ dimensions"와 별개로
**4 mesh × 연속 랜덤 치수** 구조. 둘 다 사실.
hdri 7 / floor 11 / background 1 / camera_mode 6.

### Split lock

group key = `hdri|background|floor`(pose·GT 미사용) → 77 groups.
hdri 별 round-robin 배정으로 편중 완화(0.349 → 0.134).

```
train 3,039 (23 groups) / val 1,045 (8) / untouched 5,916 (46)
train-val group overlap 0
stratification: asset 0.019, mode 0.018, hdri 0.134, floor 0.240
overfit32 = 32 (asset × camera_mode 커버)
```
floor 0.240 은 group key 에 floor 가 들어가서 생기는 구조적 한계.

### ★ Target gate FAIL — 학습 중단

deterministic 200 train frames audit:

```
검사                     값      기준     판정
nonempty target         1.000   >=0.95   PASS
top&base 모두 존재        0.895   >=0.90   FAIL
mask-supported          0.788   >=0.95   FAIL
class derivation        0       0        PASS
yaw+180 invariance      0       0        PASS
```
class 별: top_width 0.755 / top_depth 0.620 vs base_width 0.995 / base_depth 1.000
→ **top edge 가 선택적으로 사라진다**.

원인 [확인] — 구현 버그 아님:
- self-visible 필터로 top edge 탈락 **0/60건** (정상 작동)
- `mask_rle` 면적 / cuboid hull = **0.52~0.80**. 팔레트는 포크 슬롯이 뚫린 구조라
  실루엣이 외곽 박스보다 작고, cuboid edge 일부가 필연적으로 mask 밖에 놓인다.
- mask_supported 가 camera_mode 무관하게 0.767~0.803 → **구조적**.
- 지시문이 고정한 dilation 2 cells(원본 ~13px)로는 덮이지 않음 [추정].

지시문 Phase E4 "Gate FAIL 이면 학습하지 않는다" 에 따라 **32-image overfit 이하
전 학습 단계 미실행**. dilation 을 키우면 통과시킬 수 있으나 그것은 "gate 결과를 보고
설정 변경"이라 무결성 규칙에 어긋나므로 하지 않았다.

### 사용자 결정 필요

1. `MASK_DILATION_CELLS` 2 → 4~6
2. mask 필터를 self-visible 필터로 대체 (oracle O0 와 조건 일치 효과도 있음)
3. gate 기준 `mask-supported >=0.95` 재설정

어느 쪽이든 결정 후 target gate 재실행 필요.

### 무결성

final-test open 0, checkpoint SHA 불변, data 수정 0건, **학습 0건**,
training root 는 paper_4pallet_mask_v1 단독, N87 membership 0.

---

## PPD target semantics v2 (T0/T1/T2) — T2 polarity utility 완벽, gate 1항목 미달

### 목적

learned PPD 를 막은 mask-filtered target 을 oracle O0 와 동일한 observed-gradient
target 으로 교체하고 재판정. dilation 확대·gate 완화는 금지.

### 신규/수정

- `polarity_aware_line_head.py` — `gradient_association_mask`(O0 helper 공용화),
  `build_polarity_targets_v2(target_mode=...)` 3 모드 추가. 기존 함수는 legacy 유지.
- 테스트 9개 추가 (총 **175 passed**).

### O0 helper parity [확인]

runner `association_keep_mask` vs 신규 `gradient_association_mask`:
실제 N87 20 프레임 **불일치 0**. canny (100,200), radius 4.0 동일.
새 threshold 를 만들지 않았으므로 learned T2 와 oracle O0 는 **같은 정의**다.

### T0/T1/T2 비교 (동일 200 train frames)

```
metric              T0(mask)  T1(visible)  T2(observed)
nonempty rate         1.000     1.000        1.000
top&base 모두          0.895     1.000        0.865
retained fraction     0.788     1.000        0.418
class derivation      0         0            0
yaw+180 invariance    0         0            0
```
T2 는 gradient 없는 구간을 버려 retained 가 가장 낮다.
T1/T2 에는 mask-supported fraction 을 gate 로 쓰지 않음 — **해당 없음**
(mask occupancy 는 cuboid line validity 의 정의가 아님).

### ★ Target polarity utility — T2 완벽

동일 unsigned SAI-U candidate set, 동일 scorer, 원본 해상도 support.

```
mode                  n    polarity acc   inversion
mask_filtered        48    0.979          1/48
self_visible_full    48    0.979          1/48
observed_fragment    48    1.000          0/48
candidate>=2 frame 48/60,  upright 후보 존재 48/48
```

### 디버깅 — 측정 경로 결함

첫 측정에서 candidate 0개.  원인은 target 이 아니라 **측정 경로**:
100×100 target 을 원본으로 nearest-resize 하면 픽셀이 6.4×4.8배 뭉쳐(105→3200px)
TLS component 가 전부 파괴됨.  원본 해상도 support 로 만들면 component 3/2/0,
candidate 2, top3 rot err 0.90°, upright 후보 존재.

### T2 gate — FAIL (1항목)

```
PASS  nonempty >=0.95                 1.000
FAIL  top&base 모두 >=0.90              0.865
PASS  class derivation mismatch 0     0
PASS  yaw+180 invariance mismatch 0   0
PASS  NaN/Inf 0                       0
PASS  candidate polarity acc >=0.95   1.000
PASS  vertical inversion <=0.05       0.000
```

미달은 **`top&base 모두 존재` 하나뿐**이고 목적에 직결된 polarity utility 는 완벽하다.
[추정] top 과 base 가 **둘 다** 있어야 polarity 가 결정되는 것은 아니다 — 한쪽만
관측돼도 그것이 top 인지 base 인지 알면 방향이 정해진다.  따라서 이 기준은 polarity
목적에 과도할 수 있다.

지시문 무결성 규칙 5(gate 완화 금지)·D4(FAIL 시 학습 금지)를 따라
**32-image overfit 이하 전 학습 단계 미실행**.

### 판정

- T0 mask-filtered target: **REJECT** (mask 가 유효 line 을 구조적으로 삭제)
- T2 observed-fragment target: **polarity utility 유효 (1.000)**, 단 gate 1항목 미달
- Learned L0/M0/M1: **NOT RUN**
- Learned PPD: **INCONCLUSIVE**

### 사용자 결정 필요

`top&base 모두 존재 >=0.90` 을 유지할지, polarity utility(1.000)를 근거로 이 항목을
polarity 목적에 맞게 재정의할지.  전자면 학습 중단, 후자면 32-frame overfit 부터 진행.

### 무결성

final-test open 0, checkpoint SHA 불변, data 수정 0건, **학습 0건**,
training root paper_4pallet_mask_v1 단독, N87 membership 0, split hash 불변.
