# 2026-07-05 STAGE22 coord-diag 3-part chain

## PART A — "윗면 보임" 앙각 실측 (학습·재추론 X) [완료]
스크립트: `scripts/stage0/stage22_partA_elevation_band.py`
산출: `data/pallet/eval_results/stage22_coord_diag/partA/` (partA.json, summary.md, overlays/ 10장)

재사용: stage17 corner01_diagnosis.json(real 98) + stage18 elev_from_pose.
band_px = |mean_y(kp4,5)-mean_y(kp0,1)| (top-front↔top-rear 화면 수직간격, 수평근사; tilt>15° 1/98뿐).

결과 (real 98):
- <3°:  n=34 band_med=1.0px  front=11.9 rear=19.2 spk=0.77
- 3-8°: n=58 band_med=17.2px front=11.1 rear=18.5 spk=0.72
- 8-15°:n=1  band=37.5 rear=53.1
- 25+:  n=5(전부 CAD) band_med=301.8px front=14.1 rear=9.8 spk=0.4
- 저앙각<8°: 92/98 (94%), band_med=12.8px, rear_med=19.0px
- 고앙각≥8°: 6/98, band_med=292px, rear_med=13.9px
- spearman elev↔band=0.895(강), band↔rear=0.124, elev↔rear=0.075

판정: "윗면 보인다" 인상은 실측과 불일치. real 94%가 <8° edge-on(band ~1~17px 얇은 sliver),
<3°는 band 1px(윗면 사실상 선). 윗면이 진짜 보이는(band 300px, ≥25°) 프레임은 5개 전부 CAD,
거기선 rear 정확(9.8px). 즉 "윗면 보이는데 rear 실패"가 아니라 "윗면 실은 안 보임(저앙각)→rear 붕괴"
= flat-view depth collapse 재확인. ★단 고앙각=CAD라 appearance confound 있음(주의).
눈검증: elev5.9° outside=얇은 sliver rear43px, elev35° CAD=윗면 완전보임 rear6.8px [확인].

## PART B — crop-and-refine 2단 추론 진단 (학습 X) [완료, CONFOUNDED negative]
스크립트: `scripts/stage0/stage22_partB_crop_refine.py`
산출: `data/pallet/eval_results/stage22_coord_diag/partB/` (partB.json, summary.md, crop_examples/)
재사용: corner01 infer machinery, pad=0(aspect-only, 패딩X). GT=projected_cuboid[:8].

결과 (recs=248, skip_pass1=det<6=97, skip_pass2=0):
- REAL same-frame pairs: front n=8 delta +63.5px, rear n=14 delta +78.5px (대폭 악화), reproj n=3 -0.5px
- rear good(<10)% pass1=0.175→refine=0.071, gross(>20)% pass1=0.325→refine=0.929
- SYN: 마찬가지 악화(full8_hung n=6 +54px)
- elev bin: 3-8° n=13 rear +78.6px 악화, 8+° n=1 rear -0.4(정상 crop)

★기전 [확인]: 저앙각 flat 팔레트의 tight bbox = 얇은 strip(aspect~5, 예 458×83).
short-side→400 aspect-resize 시 pass2 입력이 1104×400, 2200×400 극단 wide strip = OOD.
팔레트가 거대 얇은 밴드로 stretch → pass2 예측 붕괴(rear 100~170px). 좌표역매핑은 정상
(reproj 페어 값 sensible로 검증). = 스케일/aspect 분포 이탈 confound(near-large 전례 정합).

판정: crop-refine(지정 프로토콜)은 flat edge-on 물체엔 부적합 → 유효해상도 가설 clean test 실패.
"해상도 기각" 단정 금지(CONFOUNDED). 해상도 병목 질문은 OPEN. ★부수확인: flat PnP reproj는
rear per-corner 126px인데도 0.4px(degenerate fit) → flat 물체 reproj honest 아님(기존 교훈 정합).

## PART C — coord loss paired ablation (2-arm pilot) [진행중]
코드 변경 (Deep_Object_Pose/train/train.py):
- 신규 `--epoch_size N` 인자: >0면 WeightedRandomSampler num_samples 캡(2:1:1 ratio 유지). 파일럿용.
- vis_coord_loss에 λ step-ramp 추가(하드코딩 _VIS_RAMP=800; 신규 CLI param 없음): loss_vis=λ·min(1,gstep/800)·vis_total. 초기 soft-argmax 불안정 방지.
- r_coord 1회 로깅(global_step<=1): raw vis_total/loss_belief 비율 + 7.5% λ 제안.

런처: `data/pallet/eval_results/stage22_coord_diag/partC/run_partC_arm.sh <arm> <λ> <epochs> <epoch_size>`
- control = B2 ep0084 init, 2:1:1(base:v3:addon, trunc제외), enc×0.1+freeze750, mask_aux w0.01, coord없음.
- coord = 동일 + --vis_coord_loss --vis_lambda 0.24 --vis_warmup 0.
- 파일럿: epoch_size 6000 × 8ep(cumulative 0084→0092).

캘리브: r_coord smoke(λ=1.0) → vis/bel 비율 0.30~0.33 → **λ=0.24**(~7.5%, loss-ratio proxy≠gradient, 정직표기).
스모크: control 2-iter Loss 0.0117 finite 통과 / coord 2-iter r_coord 로깅 통과. NaN경고="input tensor"(tensorboardX 히스토그램 무해).
eval 스크립트: `scripts/stage0/stage22_partC_eval.py` (syn val ckpt선택 + real 페어 front/rear/elev/good-gross/full8 + μ↔argmax 병리진단). B2로 dry-test 통과(real front~8 rear~17).

## PART C 결과 — coord loss ablation [완료]
학습: control(2:1:1 B2 recipe, no coord) + coord(λ0.24 vis_coord_loss, ramp 800step) 각 B2 init +8ep(0084→0092), epoch_size 6000. 각 34분. NaN 없음. λ 캘리 검증: vis/bel 0.072~0.079(target 0.05-0.10 정확).
ckpt: 둘 다 syn val로 ep0085 선택(val이 ckpt 거의 구분못함=+8ep가 synth 분포 거의 안바꿈, 기존 STAGE16 정합).
eval: real 145(pad100), same-frame 페어 N=97.

핵심 결과 (control→coord):
- rear 페어: 19.86→17.64, median delta -0.81px, 45 improve/23 worse, **sign test p=0.010(방향 유의)**, mean delta -0.31
- front 페어: 11.71→13.63, delta +0.78px, 20 imp/42 wrs (coord가 front 악화)
- full8(order-free): 16.34→15.68, delta -0.07 (거의 무변화)
- rear good(<10): 0.04→0.12(3x), gross(>20): 0.49→0.45
- V=8(n91): rear 19.71→17.64(회귀 없음), front 11.51→13.46(소폭 악화)
- rear elev bin: 전 구간 개선(<3:-0.75, 3-8:-1.28[핵심], 8+:-0.77)
- μ↔argmax(coord rear 채널): rear 0.95 vs front 0.73 belief px — **둘 다 sub-pixel=병리(원거리 false-peak 견인) 없음**

판정: coord = **작지만 방향-유의(p=0.01)한 rear 재배분 정규화자**(front→rear precision 이전). 매그니튜드 작고(-0.8px) front 비용 있어 full8 flat=screening "weak" 정합. 단 screening에 없던 front/rear 분리로 "targeted rear regularizer" 성격 규명. μ 진단상 soft-argmax 병리 없음→"국소5×5 변형" 후속 미동기(rear 천장=flat-view depth, PART A 정합). 슬라이드 방어숫자: rear -0.8px(p=0.01)+good 3x+V=8 무회귀, "표준 coord(Integral Pose Regression 계열) 소폭 rear 기여" 로 정직 하향.

## STAGE23 coordset confound + 전 트랙 재검증 (추론만, 새 손어노 셋 17장)
- 새 셋=testset_full8_manifest(cad11+noapril6, 8/8). 대조=filter-val+manual 123. final-test 봉인. 산출 data/pallet/eval_results/stage23_coordset_confound/(eval.json, run_stage23.py).
- ★새 셋 앙각 분포 = 고앙각 극단 쏠림[확인]: <8°=0장, 25+°=13/17(cad 9/11, noapril 4/6), 전프레임 elev≥12.6°. real 운용(94% <8°)과 정반대 → rear-붕괴 레짐 통째 회피.
- PART1 B2 confound → **(a) 확정**[확인]: B2(coord無) rear 13.3 ≈ coord_control 13.1 ≈ coord_coord 14.3, good% 오히려 B2(42.9)>coord(22.2). 새 셋 rear≈front(붕괴없음) vs 기존셋 rear16.8>front12.1(붕괴존재). → **"손어노 셋서 coord 됐다"=셋이 고앙각이라 rear 원래 관측가능해서지 coord+edge가 rear 고친 것 아님. STAGE22 coord rear성공=셋 아티팩트(confound).**
- 전 트랙 재검증(N=17, 과거결론 유지 여부):
  - 벡터 voting(S4/S5): **참패 유지·강화**[확인]. voting-arm이 자기 heatmap-arm에 cor 50-57 vs 7px, front 120/64 vs 6px, honest8 71-75 vs 14-16. det%(voting 41-47>heatmap 17-24)=항상8점 착시(honest8로 교차=8점 쓰레기). 벡터 종료 유지.
  - coord=이득없음(정합), mixup=셋의존 미결(정합, cor10.8/rear11.8 중상위나 det35.3·N17), cutpaste=붕괴 유지(det17.6/good0), challenge0123 rear25.5 최악(과적합).
  - best=노이즈 내 승자 없음, B2 견고(어느 모델도 B2 결정적으로 못이김).
- 대조 기존셋(N123): rear>front(붕괴존재, B2 16.8/12.1) vs 새셋 rear≈front → **셋 성질이 rear 난이도 지배**(confound 재확인). 두 셋 난이도축 다름(truncation vs elevation).
- ★대표성 경고: 손어노 셋=고앙각+truncation, real(94% 저앙각)과 분포 정반대. "이 셋서 잘됨"≠real 대표, 이 셋 rear 수치로 real rear-붕괴 해결 주장 금지. N=17 극소.

## STAGE24 — 벡터 트랙 최종 종료 (voting/sparse, 새 데이터+real mask) [완료]
목적: 과거 벡터 negative의 3 confound[(1)옛데이터+박스마스크 (2)STAGE10 undertrain (3)새데이터 미시험] 동시 제거 후 마지막 시험. init=challenge0123, 새 데이터만(v3 000-008+addon+trunc, 옛 v1/v2/mixed_v8 제외), real mask_rle, quick-screen 3000×4ep 3-arm.
스크립트(신규): scripts/stage0/run_stage24_{quickscreen,seeneval,evalall}.sh, eval_pvnet_heads.py에 `manifest` evalset(--manifest) 추가.
게이트=FAIL → full 안 감(정당). 
- voting: L_vec 0.51→0.32 plateau(미수렴), L_bel/L_seg 수렴. SEEN(v3 batch_000)서도 voting 77-82px vs heatmap 6.7px = fit실패(undertrain아님), STAGE10 53.8px보다 나쁨. real seg마스크·GT마스크 둘다 77-82 → 마스크문제 아님=벡터필드 자체.
- held-out(same-weights heatmap vs voting-seg): manifest17 7.1 vs 126, filter-val N87 11.4 vs 132, manual 8.6 vs 211px. voting det% 착시(17/17·87/87 all gross).
- sparse: L_edge 1.10→0.72, refine vs control-HM = filter-val 12.9 vs 10.1(win 3/22)·manual 10.4 vs 10.4(good 4vs8) = 짐(STEP7 재확인). sparse-HM<control-HM 미세 오염.
- control(새데이터 heatmap-only) ≈ voting/sparse 자기 heatmap-arm → vec/edge multitask 이득 0.
결론: real mask+full 새데이터로도 negative 확정, 재시도 금지. memory pvnet-negative에 STAGE24 최종확정 추가. 산출 weights/stage24_vec_newdata/{voting,sparse,control}/, data/pallet/eval_results/stage24_vec_newdata/logs/, seen표 stage10_v3mask/eval_v3_s24_seen.txt.
한계: quick-screen 스케일(게이트=seen-fit 조기포착 설계, voting이 seen조차 못맞춤이라 full무의미). 17장=고앙각소표본→filter-val N87 주신호(일치). final-test 미개봉.

## STAGE24 벡터 구현 검증 6항목 (사용자 체크리스트) — target/loss 버그 없음, negative 유지·강화
- 목적: L_vec가 seen조차 안 내려간(0.51→0.38 plateau) 게 target/loss 버그인지 표현 불가인지. STAGE24 voting weights 로드, seen=v3 batch_000·held=batch_009 각 64프레임 forward 텐서검산(재학습X). 산출 impl_check/impl_check.json, 스크립트 scripts/stage0/_stage24_impl_check.py.
- ★스케일 주의: 로그 L_vec=2-stage 합, 검산은 1-stage(≈로그/2). predict-zero baseline 1stage 0.25→2stage 0.50=로그 초기 0.51 일치(vec헤드 초기=상수0 예측).
```
항목                  결과(seen/held)                          판정
1 per-img norm       batch-global denom(NOT per-img)이나       정상(무해)
                     global 0.179≈per-img 0.175
2 GT unit norm       mean1.0 std3.6e-8 NaN無                   정상
3 mask binary        unique{0,1} 판자구멍 98%                   정상
4 scale별 L_vec      small/mid/large 0.173/0.177/0.172         정상(균일stuck)
5 corner별 L_vec     corner0-7 0.17~0.22 균일, centroid8 0.057 정상(필드전체)
6 seen vs held       seen0.179 ≈ held0.176 (★seen도 높음)      핵심
```
- 종합: **6항목 다 정상(버그無)+seen==held(학습데이터조차 못fit)+균일stuck+centroid만fit → 표현/용량 한계, target/loss 버그 아님. 벡터 negative 확정 유지·강화.**
- ★메커니즘(신규): centroid 방사형 필드(0.057)는 fit되나 8코너 방향필드(0.17-0.22)는 못fit. 픽셀마다 "먼 큐보이드 코너 방향" 찍으려면 전역 포즈를 픽셀단위 인코딩해야=얕은 conv헤드(50×50 제한 receptive field)의 구조적 한계. gradient는 흐름(0.25→0.178, r_vec1-10)=최적화 죽은게 아니라 얕은 minimum. 동일 VGG feature가 heatmap(L_bel0.02)은 fit하는데 vec헤드만 못fit=feature부족 아니라 dense-voting 표현 자체가 이 헤드/해상도서 부적합.
- 유보(§5): quick-screen 3k×4ep 짧으나 (a)L_vec step~200 plateau후 800step 무추세 (b)같은예산 heatmap수렴 (c)seen memorizable이면 추세보여야하는데 없음=undertrain 아님. 미검토 caveat[추정]: belief는 180°swap-min 관대/vec GT canonical 고정, belief-swapped vs vec-canonical 긴장 가능성 남음(단 코너 균일stuck을 특정코너 버그로 만들진 않음).

## STAGE25 — paper_base_v2 논문 base 정직한 위치 평가 (vs B2 vs challenge0123)

목적: 논문 트랙 baseline paper_base_v2(palletobj scan 미학습=cad/noapril 진짜 unseen)를 B2/challenge0123 과 **같은 셋·같은 지표**로 평가해 논문 출발점 숫자 확정.
- 신규 스크립트: `scripts/stage0/stage25_paperbase_eval.py` (eval_capturecad_b2.eval_frame 엔진 재사용: order-free Hungarian corner + solve_pose(order-free W/D) + honest8 reproj + reflect-pad100 + per-frame K. 3모델 동일 조건).
- 산출: `data/pallet/eval_results/stage25_paperbase_eval/{eval.json,summary.md}`.
- 모델: paper_base_v2=weights/paper_base_v2/final_net_epoch_0060.pth, B2=stage11_16k_B2_maskaux/final_net_epoch_0084, challenge0123=challenge0123/final_net_epoch_0060.
- 셋: handannot17(manifest cad11+noapril6, 8/8, ★고앙각 25-90deg 13/17=정성) / filterval(collect_val outside44+night43+manual36=123, ★저앙각 -90~10deg 121/123=주신호). final-test(p09/p07,n08/n09) SEAL.

핵심 수치 (pad100, median px):
```
filterval overall  n    det%  front  rear  corner  honest8  good%  gross%
─────────────────────────────────────────────────────────────────────────
paper_base_v2      123   79    16.2  33.5   22.6    25.1    21.8   51.6
B2                 123   75    12.1  16.8   13.5    15.6    29.1   29.1
challenge0123      123   72    12.4  18.7   14.8    18.4    27.0   31.6
```
- **GAP paper_base_v2 − B2 (filterval)**: corner +9.1, rear +16.7, honest8 +9.5px. det% 오히려 +4(79 vs 75) = under-detect 아님, **mis-locate**(특히 rear). gross corner 51.6% vs 29.1%.
- **rear=병목 재확인**: 전 모델·전 셋에서 rear>>front, paper-vs-B2 gap 이 rear(+16.7)에 front(+4.1)보다 집중. (memory corner01-premise-debunked 정합).
- **regime-specific 결손**: 고앙각 handannot17 에선 paper_base_v2 경쟁~우세(det 47>41>35, honest8 18.9<20.0<24.8, noapril best). 저앙각 flat-view filterval 에서만 붕괴. elev bin: filterval -90~5(n61) corner 19.7 / 5~10(n60) corner 31.0 vs B2 12.6/16.2 → 저앙각일수록 gap↑ (flat-view depth 붕괴, memory diag-filter-not-reliable-flat-edgeon 정합).
- **truncation(V<8, n17)**: 3모델 다 붕괴(paper corner 37.6·gross 68.9 / B2 24.5 / chal 40.6). V=8 이 유효비교: paper corner 21.8 vs B2 13.3 vs chal 14.8.
- **unseen 일반화(cad=palletobj, paper_base_v2 미학습)**: det 27%(3모델 동일)·corner 13.9(경쟁적)이나 honest8 29.6(최악). noapril 은 best. 검출은 되나 저앙각·rear pose 정확도가 gap. N 극소=방향성.

판정: **paper_base_v2 = 정직한 논문 base로 타당.** filter-val 대표셋에서 B2 대비 corner ~9px·rear ~17px·honest8 ~9.5px 뒤짐 = "v3/addon replay 없이 치른 비용". 검출률은 동등+(79%)이라 결손은 순수 localization(rear+저앙각 flat-view). 개선(self-train/filter/aug)은 이 gap 회복량으로 측정. B2 는 v3/addon 본 internal ceiling(논문 직접비교 부적합, 상한 참고).
