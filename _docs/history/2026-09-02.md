# 2026-09-02

## YOLO26 geometry-filtered self-training 트랙 — 자정 넘긴 세션의 파일 목록

이 세션은 2026-09-01 저녁에 시작해 자정을 넘겼다. **설계 근거·수치·판정은 전부
`_docs/history/2026-09-01.md` 에 있다** — 여기 복붙하지 않고 가리킨다.
이 문서는 오늘 날짜로 넘어온 뒤 바뀐 파일의 목록과 Before/After 만 남긴다.

최종 커밋 `cc924c0` (local == origin/main). 학습 13 run, pytest 433 passed / 2 skipped.

### 신규 파일 (전부 이번 트랙에서 처음 만듦)

```
scripts/self_training_yolo/
  build_adaptation_pool.py        pl_pool 세션에서 Day/Night 500+500 을 leakage-free 로 동결
  dump_teacher_predictions.py     R0 teacher 를 1000장에 한 번만 추론 (원본 + flip 동시 캐시)
  lock_confidence_threshold.py    TAU_BOX 를 unlabeled pool 만 보고 선택 (GT 미사용)
  pseudo_label_filters.py         무차원 geometry score 3종 (reproj / keypoint-removal / flip)
  build_pseudo_manifests.py       teacher cache 하나에서 F0~F4 manifest 생성
  build_pseudo_datasets.py        arm 별 Ultralytics pose dataset (epoch 물리 크기 고정)
  freeze_exposure_contract.py     self-training strength 4축 동결
  run_paper_selftraining.py       smoke → full 드라이버 (산출물로 완료 판정, discord 알림)
  filter_quality_m4.py            M4 — filter 가 남긴 예측을 GT 로 채점
  evaluate_arms.py                전 모델 PAPER_EVAL 평가 + subgroup 오프라인 집계

scripts/evaluation/build_paper_eval_manifests.py
  workspace 에서 PAPER_EVAL membership 을 재계산해 manifest 로 내보냄 (숫자 복붙 금지)

scripts/paper/build_experiment_tables.py
  result artifact → TABLE_M1~M5 · APPENDIX_TABLES · ABSTRACT_RESULT_SLOTS
  §33 불변식(70/50/194/125/319) 위반 시 표를 만들지 않고 실패

challenge/tests/test_pseudo_label_filters.py
  20 tests — 무차원성 · GT 미수용(서명 검사) · 3D 규약 회귀 · LOO 용어 금지(AST)
```

### 세션 후반에 고친 것 (Before → After)

```
파일                              Before                     After                        이유
──────────────────────────────────────────────────────────────────────────────────────────────
pseudo_label_filters.py           pnp_solver.make_pallet     annotate_pnp.make_pallet     폐기된 v1~v5 규약
                                  _keypoints_3d_isaac        _keypoints_3d (v6)           (near face +d/2) 때문에
                                  near face = +d/2           near face = -d/2             GT 로도 s_reproj 0.11.
                                                                                          정본 재사용으로 0.0029
pseudo_label_filters.py           정사각 footprint 에서       단일 hypothesis 로 먼저      upstream 이 short/long
                                  upstream 예외              처리하는 가드                face 명명에 X!=Z 요구
run_paper_selftraining.py         workers 미지정 (기본 8)     workers=2, cache=False       worker 당 ~900MB →
                                                                                          OOM killer 가 VS Code 까지
                                                                                          죽임 (EXIT 137)
run_paper_selftraining.py         (없음)                     preflight() 메모리 가드      MemAvailable<6GB 면 시작 안 함
run_paper_selftraining.py         last.pt 존재 = 완료         results.csv epoch 수로 판정   last.pt 는 매 epoch 갱신 →
                                                                                          중단된 run 을 완료로 오인
run_paper_selftraining.py         seed 고정                  --seed override + _S 접미사   repeatability 시도용
build_pseudo_datasets.py          sampling seed 고정          --sampling-seed / --suffix   ultralytics seed 가
                                                                                          dataloader 에 안 가서
                                                                                          진짜 replicate 를 여기서 만듦
build_pseudo_datasets.py          MAIN arm 6개                + A2_NAIVE_MATCHED_S1~3      unique-quantity-matched 대조
evaluate_arms.py                  Daytime = lighting=="day"  Daytime = paper_domain       lighting 은 168장(전 object),
                                  (168장)                    =="daytime" (70장)           MAIN 은 plastic 만 = 70장.
                                                                                          §33 불변식이 이 수를 요구
evaluate_arms.py                  supervised 오차만 집계      + all-annotated 진단 집계     MAIN Daytime 70장은
                                                                                          supervision mask 가 비어
                                                                                          strict metric 이 전부 None
evaluate_arms.py                  seed_S43/S44 8모델          replicate _P43/_P44 4모델     seed run 은 가중치 비트 동일
                                                             + A2_MATCHED_S1~3            이라 독립 반복이 아님
filter_quality_m4.py              `value or float('nan')`    show() 헬퍼 (None 만 —)       0.0 이 falsy 라 gross_rate 0 이
                                                                                          nan 으로 둔갑
build_experiment_tables.py        M2 primary = corner        M2 primary = detection       Daytime strict corner 가
                                                             + ranking, corner 는 secondary UNAVAILABLE_METADATA
build_experiment_tables.py        (없음)                     A2 · A3 표 추가               quantity-matched / repeatability
```

### 함께 수정한 기존 파일

```
challenge/evaluation_v2/real_dataset_contract.py   PAPER_EVAL population 3종 등록 (7지점)
challenge/evaluation_v2/paper_real_eval.py         per-frame CSV 에 supervised /
                                                   all-annotated keypoint 오차 열 추가
_docs/paper/EXPERIMENTS.md                         FINAL_EVAL → PAPER_EVAL, LOO 용어 제거,
                                                   R0-CONT · A12 추가, generated 표 포인터
.gitignore                                         paper 트랙의 작은 계약 JSON/CSV 예외
```

### 신규 문서

```
_docs/paper/SELF_TRAINING_TRACK_AUDIT.md    PHASE 0 감사
_docs/paper/PRE_RESULT_LOCK.json            결과 보기 전 동결 (commit 15f0cb5)
_docs/paper/POSE_METRIC_READINESS.md        POSE_METRICS_STATUS = BLOCKED, 축별 분리
_docs/paper/EXTERNAL_BASELINE_AUDIT.md      DOPE / SingleShotPose / PVNet / Real-FT 상태
_docs/paper/generated/TABLE_M1~M5.md        generator 산출 (손으로 안 씀)
_docs/paper/generated/APPENDIX_TABLES.md    A1 · A2 · A3 · A7
_docs/paper/ABSTRACT_RESULT_SLOTS.md        YAW_RESULT_SLOT = BLOCKED
```

### 남은 것

```
A2 겹침        corner · FPR95 가 개수 맞춘 무작위 선별과 구간 겹침.
               구간 분리는 AUROC 하나 — 현재 method 주장의 핵심 취약점
pose BLOCKED   6D 열 전부 비어 있음. 초록의 yaw 문장 사용 불가
M1 2행뿐       DOPE 는 evaluator 가 Ultralytics 전용이라 offline prediction 경로 필요
```

## M1 의 DOPE 행을 채웠다 — evaluator 에 offline prediction 경로

M1 이 2행(YOLO26 R0 · Proposed)뿐이라 "main method comparison" 이라 부르기 어려웠다.
DOPE 는 checkpoint 가 있는데도 못 넣고 있었다 — evaluator 가 Ultralytics 전용이라서다.

```
challenge/evaluation_v2/paper_real_eval.py:1724   self.model = YOLO(str(weights), task="pose")
```

**해결 방식**: DOPE 전용 채점기를 따로 만들지 않았다. 그러면 M1 의 행들이 서로 다른
자로 잰 값이 되어 비교가 성립하지 않는다. 대신 evaluator 에 예측 주입 경로를 열었다.

```
Before                          After
──────────────────────────────────────────────────────────────────────────
predictor = _Ultralytics        --predictions 있으면 _CachedPredictor
Predictor(weights, device)      없으면 기존 _UltralyticsPredictor (기본 동작 불변)
(분기 없음)                     weights 는 provenance 로 계속 기록
                                selection_policy = OFFLINE_CACHED_PREDICTIONS
```

`_CachedPredictor` 는 `image path -> [{score, box_xyxy, keypoints_xy}]` 를 읽어
`_UltralyticsPredictor.predict()` 와 **같은 튜플 형태**를 돌려준다. 그래서 AP·keypoint·
subgroup 채점 코드는 한 줄도 바뀌지 않는다.

### 신규 `scripts/self_training_yolo/dump_dope_predictions.py`

추론 규약은 지어내지 않고 정본 함수를 그대로 썼다.

```
s1_cad_9filters.infer_belief / belief_to_pred
paper_s2_real_eval.E.load_model / pad_frame / belief_to_orig_pad
PAD 100 reflect → 학습 전처리 → belief(9,H,W) → 원본 픽셀
belief threshold 0.3 (mc_dump_dope 와 같은 값)
```

reflect-padding 은 선택이 아니다 — plain squash 는 truncation·근접에서 체계적
과소검출을 만들어 DOPE 를 부당하게 나쁘게 만든다 (2회 교정 이력).

`mc_dump_dope.py` 는 쓰지 않았다. 그건 **다른 모델**(SplitLate multihead,
`weights/paper_s2/...`)용이고 161장 canonical 셋에 묶여 있다 `[확인]`.

### 결과 — 같은 evaluator·같은 319/2689·같은 metric

```
Method                              corner↓   det↑   AP50-95↑   AUROC↑   FPR95↓
────────────────────────────────────────────────────────────────────────────────
DOPE (same-data backbone control)    10.083  0.737     0.3412   0.9903   0.0409
YOLO26n-Pose (synthetic-only)         4.420  0.975     0.7688   0.9921   0.0417
Proposed                              4.180  0.984     0.7585   0.9953   0.0283
```

DOPE 는 검출 자체가 약하다 (192/319 매칭, det 0.737). memory 의
[DOPE 병목은 pose 가 아니라 검출] 과 일치한다.

**비대칭을 표 각주로 남겼다.** DOPE 에는 box head 가 없어 box 를 검출된 cuboid
코너의 bounding box 로 유도했다. 따라서 `AP50-95` 는 같은 양이 아니고, score 도
DOPE 는 belief peak · YOLO 는 box confidence 라 척도가 다르다.
**직접 비교가 성립하는 열은 corner 와 det** 이다 (GT 2D keypoint 와 IoU 만 씀).

### 함께 바뀐 것

```
evaluate_arms.py       MODELS 값이 checkpoint 또는 (checkpoint, predictions) 쌍
                       결과에 scored_from = LIVE_ULTRALYTICS / OFFLINE_CACHED_PREDICTIONS
build_experiment_tables.py  build_m1 재작성 — DOPE 행 + 비대칭 각주 + 미완 행 상태
dump_dope_predictions.py    manifest 의 image_path / image 두 필드 모두 지원
                            (DEV_NEG2689 는 legacy `image` 필드를 쓴다)
data/pallet/results/paper_eval_v1/PURPOSE.md   신규 (purpose_gate 요구)
_docs/paper/EXTERNAL_BASELINE_AUDIT.md         DOPE 를 NEEDS_BRIDGE → 완료로 갱신
```

DOPE 덤프 3,008 장 1분 49초 (27.3 img/s), 검출 407/3008.
pytest 433 passed / 2 skipped — 기존 동작 회귀 없음.
