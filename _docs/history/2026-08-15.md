# 2026-08-15

## 과제용 YOLO26-pose 단일 모델 파이프라인 신설 (08-14 저녁 착수 → 08-15 새벽)

사용자 지시로 논문용 DOPE 경로와 **완전히 분리된** 과제용 YOLO26n-pose 학습 파이프라인을
새로 만들었다. 착수 중 사용자가 범위를 축소 — **이번 라운드는 합성(G+T)만**, real 133장은
이후 finetune 단계로 미룬다.

기존 `challenge/yolo_pose`, 논문용 weight, 기존 데이터 원본은 **하나도 건드리지 않았다.**

### 신규 루트

```
challenge/yolo_pose_one_model/
  PURPOSE.md                  [소비처]/[문장] + 범위·봉인 규칙
  README.md                   실행 순서 · 고정 계약 · 미해결 목록
  contract/pallet_pose_contract.yaml   단일 source of truth
  manifests/  configs/  datasets/  reports/  runs/  final/  scripts/
```

### 스크립트 (전부 신규)

```
discover_and_audit.py       G/T/R 탐색 → manifests/all_samples.csv (60,635행) + reports/01
verify_kp_contract.py       ★배포 PnP 로 G/T/R 키포인트 의미 일치를 실증
verify_v4_conversion.py     v1/v2 가 camera-facing v4 인지 재실행으로 증명
test_padding_contract.py    padding 왕복 + PnP 좌표계 등가 단위 테스트 (negative control 포함)
build_splits.py             G 층화 / T scenario 그룹 split + 누수 assert
prepare_yolo_pose.py        JSON → padded YOLO-pose 라벨 (기존 convert_to_yolo_pose 계약 준수)
prepare_stage_a.sh          60k 프레임 일괄 변환
build_stage_dataset.py      T 2배 alias(symlink) + data yaml 생성
audit_yolo_pose_labels.py   라벨 전수 감사 (token/bbox/visibility/centroid/sha256 중복)
render_overlays.py          라벨 overlay (near 초록 / far 파랑 / centroid 마젠타)
run_smoke.sh                batch32 2 epoch 스모크
train_stage_a.sh            Stage A 60 epoch
chain_stage_a_m.sh          ★nano 종료 감지 → m batch32 probe → 32 or 16 자동 학습
eval_task_pose.py           합성 val task 평가 (PnP·keypoint 오차·yaw)
select_final_checkpoint.py  checkpoint 재평가 후 final/ 로 복사
gpu_monitor.sh              GPU util/mem/temp/power CSV 기록
notify_at_epoch.sh          목표 epoch 도달/조기종료 시 Discord 알림
export_jetson.sh            Jetson 에서 TensorRT FP16 빌드 (여기서 실행 안 함)
```

### ★계약 감사 — 실증으로 확정한 것

세 도메인의 키포인트 의미가 같은지를 **순열 재계산이 아니라 배포 PnP reprojection** 으로
확인했다 (`pose6d_adapter.object_points` 순서 그대로, near/far 뒤집은 대조군과 비교).

```
도메인                as-is median    near/far swap    비율
────────────────────────────────────────────────────────────
G  paper_release        0.00 px          8.03 px       inf
T  v1 palletobj         3.49 px         23.76 px       6.8x
T  v2 palletobj         3.75 px         23.14 px       6.2x
R  real manual_kps      1.53 px         31.16 px      20.4x
```
→ 0~3 이 실제 near face 이고 배포 3D 모델점 순서와 일치. **변환 함수 불필요.**

**T 의 dims 필드 순서를 실증으로 확정**: `cuboid_dimensions_m = [1.1, 1.3, 0.12]` 은
문서가 없었는데, 네 해석을 전부 PnP 로 돌려 `[depth, width, height]` 로 판명
(median 0.00px, 나머지는 3.8~109px). registry 는 이 순서로 읽는다.

**★`convert_to_camera_facing_v4.py` 를 G/T 에 재적용 금지**. 이 스크립트의 2D polygon-area
휴리스틱은 G 표본 400 중 159(40%)를 non-identity 로 판정하지만, G 의 as-is reprojection 이
0.00px 이므로 **틀린 쪽은 스크립트**다 (저앙각에서 무너짐). 다시 돌리면 데이터가 깨진다.

### ★미해결 충돌 (real finetune 전 사용자 확인 필요)

```
팔레트 height     real GT 0.11  vs  배포 config/T합성 0.12      → 실측 확인 필요
width/depth       뷰에 따라 1.10/1.30 이 바뀌는데 배포는 1.10 고정
                  → real 프레임 52%(183/351)에서 reproj 7.59px (자기 dims 면 1.55px)
PnP 방법          프롬프트 SQPnP  vs  배포 EPnP+LM(>=6점)
real 학습 가용    133장 13세션뿐 — 161장은 봉인된 정본 평가셋(학습 금지)
```
상세: `challenge/yolo_pose_one_model/reports/02_contract_conflicts.md`

### 작업 중 잡은 결함 3건

```
1. split 층화 실패
   G 파일명이 4자리(f0000~f9999 1만장)와 5자리(f10000~ 3만장)로 섞여 있고
   records.jsonl 의 unique idx 가 38,955/40,000 이라 idx↔파일 조인이 조용히 어긋났다.
   Before: records.jsonl 의 idx 로 조인 → unknown stratum 2,028
   After : 각 프레임 JSON 에서 직접 읽음 → 32 strata, unknown 0

2. T scenario 누수
   v1/v2 가 같은 "R000xxx" scenario id 를 써서 train/val 에 걸쳤다.
   누수 assert 가 실제로 잡았다. 키를 "v1:R000336" 으로 네임스페이스화 + scenario 를
   최빈 background stratum 하나에만 배치하도록 수정.

3. audit centroid 규칙 오탐
   경고 11건이 전부 visible corner 4개인 truncation 프레임이었다. 8개 중 4개만
   보이면 그 평균이 centroid 와 먼 게 정상 → 라벨이 아니라 내 규칙이 틀렸다.
   Before: len(corners) >= 4 이면 검사
   After : len(corners) == 8 일 때만 검사
```

### 데이터셋

```
split (seed 42, 결정적)
  G  train 38,002 / val 1,998   층화(pallet_type × scene_preset × background_asset)
                                 seed 가 40,000/40,000 전부 고유 = 프레임 독립 렌더
  T  train 17,957 / val 2,011   frame_meta.scenario 그룹 split (v1 2782 / v2 1305 scenario)

변환 (100px BORDER_REFLECT_101, 실패 0)
  datasets/stage_a  train 73,916 (G 38,002 + T 35,914 = 원본 17,957 + symlink alias 17,957)
                    val    4,009 (alias 없음)   약 48 GB
  generic:target = 1.06 : 1

audit  train/val 오류 0 · sha256 중복 0 (77,925 파일 전수) · flip_idx involution OK
```

### ★padding 계약 — G 는 840x680 이 아니다

G 는 해상도가 4종(640x480 / 960x540 / 720x480 / 560x560)이라 +100px 해도 크기가 제각각이다.
사용자 결정(08-14): **해상도와 무관하게 각 이미지에 +100px**. YOLO 정규화 좌표는 이미지별
기준이라 학습에 문제없고, contract 의 `padded_width/height: 840/680` 은 640x480 입력에만
해당한다고 명시했다.

단위 테스트 전원 통과:
```
왕복 오차       1.14e-13 px  (기준 1e-4)
PnP 등가성      |dt| 5.7e-13 m, |drvec| 2.6e-13 rad
negative control  좌표계를 섞으면 t 가 최대 3.60 m 어긋남 → 테스트가 vacuous 하지 않음
```

### Stage A (nano) 결과 — 완주

```
run     runs/stage_a_synth_640_b32_seed42/
설정    yolo26n-pose.pt · imgsz 640 · batch 32 · nbs 64 · 60 epoch · patience 15
        SGD lr0 0.01 · cos_lr · warmup 3 · mosaic 0.30 · close_mosaic 10 · fliplr 0
        seed 42 · deterministic · AMP · save_period 5
소요    8.45 h (epoch 당 약 510초), peak VRAM 5.58 GiB / 9.62

ep   BoxmAP50 Box50-95 PosemAP50 Pose50-95 | v_pose v_box
30   0.9889  0.8983   0.9579  0.9311      | 0.416 0.476
40   0.9914  0.9347   0.9605  0.9412      | 0.367 0.376
50   0.9937  0.9540   0.9749  0.9528      | 0.322 0.307
60   0.9943  0.9608   0.9765  0.9595      | 0.292 0.275

best = epoch 60 (Pose·Box 모두). patience 한 번도 소진 안 됨.
checkpoint 13개 보존 (epoch0/5/.../55 + best + last)
```

★ 30 epoch 시점에 "Box 는 15 epoch 이후 과적합" 이라 진단했으나 **틀렸다**.
Box mAP50-95 가 0.8909(27) → 0.9608(60) 로 회복했다. 과적합이 아니라 높은 lr 구간의
불안정이었고 cos_lr 이 낮아지며 해소됐다. 중간 지표로 과적합을 단정하지 말 것.

★ 합성 val 은 이미 포화(Box mAP50 0.994 / Pose 0.977)라 **checkpoint 변별력이 없다.**
최종 선택은 mAP 가 아니라 PnP 성공률 → keypoint 정규화 오차 → median reproj 순으로 한다
(`select_final_checkpoint.py`). 그리고 이 값은 전부 합성 분포 내 성능이며 **real 성능이 아니다.**

### ★결함 4 — 내가 만든 감시가 내가 만든 체인을 2h30m 막았다 (자기참조)

nano 는 02:20 에 끝났는데 medium 체인이 계속 "waiting" 이었다.

```
체인:      while pgrep -f "yolo pose train"; do sleep 120; done
감시 셸:   ... while pgrep -f "yolo pose train" ...     ← 이 명령줄 자체가 패턴에 매칭
```
`pgrep -f` 는 **명령줄 전체**를 보므로, 학습을 감시하려고 띄운 Monitor 셸(PID 883917/883920/
883924)이 "학습 프로세스"로 오인돼 루프가 영원히 안 끝났다. 같은 세션에서 `pkill -f` 가
자기 셸을 죽인 것(exit 144)과 **동일한 자기참조 함정을 두 번째로 밟았다.**

```
Before  pgrep -f "yolo pose train"
After   ps -eo comm,args | awk '$1 ~ /^python/ && /bin\/yolo pose train/ {f=1} END {exit !f}'
        → 인터프리터가 python 인 프로세스만. 셸(comm=bash)은 구조적으로 제외된다
```
`pgrep -f "bin/yolo pose train"` 도 시험했으나 **확인용으로 터미널에서 직접 칠 때** 자기 셸이
걸린다(체인 내부에서는 안 걸림). comm 기반이 확실하다.

교훈: 프로세스 감시에 `pgrep -f <문자열>` 을 쓰면, 그 문자열을 포함한 **모든 셸**이 잡힌다.
감시 스크립트와 피감시 프로세스가 같은 키워드를 공유하면 반드시 오탐한다.

손실 = 유휴 GPU 2h30m. 데이터·nano 결과에는 영향 없음.
1차 시도 로그는 `runs/_stage_a_m_chain_attempt1.log` 로 보존.

### medium 자동 체인 (진행 중)

사용자 요청: nano 가 정확도 부족일 때 대비해 yolo26m-pose base 를 미리 확보.
```
chain_stage_a_m.sh (PID 1425228, setsid detach)
  1. nano 종료 대기 → 2. GPU 안정화 30s → 3. m batch32 probe (fraction 0.004)
  4. 통과 → batch 32 / lr0 0.010,  OOM → batch 16 / lr0 0.005  ★자동 폴백
  5. 60 epoch 학습 (나머지는 nano 와 동일)  6. Discord 알림
```
★ 프롬프트 원문은 "batch 자동 변경 금지" 였으나 **사용자가 명시적으로 16 폴백을 지시**해
그에 따랐다. lr 은 `0.01 x batch/16` 규칙으로 함께 조정해 optimizer step 비교가 깨지지 않게 했다.

가중치 3종 확보: `challenge/weights/pretrained_yolo/yolo26{n,s,m}-pose.pt`
(n 3.68M / s 11.81M / m 24.22M). s·m 은 자동 다운로드분을 repo 루트에서 이 폴더로 옮겼다.

### ★medium batch 확정 — 이 GPU 에서 m 은 batch 8 이 한계 [확인]

probe 를 믿고 batch 32 로 시작했다가 세 번 재시작했다. 결론부터:

```
시도                                              결과
──────────────────────────────────────────────────────────────────
1차  batch 32 (probe peak 8.34 GiB 로 "통과")      즉시 OOM → Ultralytics 가
                                                  자동으로 32→16→8 축소해 진행
2차  깨끗한 GPU 8,829 MiB + expandable_segments    batch 16 즉시 OOM → 8 로 축소
3차  batch 8 명시                                  정상. 6,290 MiB, epoch 1,698 s
```

**단편화가 아니라 순수 용량 부족이다.** 2026-04-10 의 allocator 단편화 사례가 떠올라
`PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True` 를 넣었는데도 16 이 실패했고,
batch 8 이 6.2 GB 를 쓰므로 16 이면 약 12 GB — 10 GB 카드에 원리적으로 안 들어간다.

★ **내 probe 설계 결함 2가지**
```
1. fraction=0.004(약 300장) · val=False 로 쟀다.
   실학습은 73,916장 + val 4,009장 + worker 4개가 더 붙는다.
2. 판정 기준이 "OOM 예외만 안 나면 ok=true" 였다.
   peak 8.34 / 가용 8.9 GiB = 94% 는 통과가 아니라 경계선인데 통과로 적었다.
   → 안전 마진(예: 85% 이하)과 val 로더를 넣었어야 했다.
```

★ **Ultralytics 는 OOM 시 batch 를 자동 축소한다** (32→16→8, 최대 3회).
프롬프트의 "batch 를 자동으로 변경하지 않는다" 를 라이브러리가 무시하는 셈이라,
`args.yaml` 과 run 이름은 요청값(b32)인데 실제는 b8 이 된다. **step 수로 검증할 것**
(73,916/batch). 로그의 `Reducing to batch` 경고를 감시에 반드시 포함한다.

★ **그래도 nano 와 비교는 유효하다** — `nbs=64` 가 accumulate 를 맞춰준다.
```
              batch   accumulate   유효 batch   lr0
nano            32         2           64       0.01
medium           8         8           64       0.01
```
optimizer 가 보는 조건은 동일. 차이는 BatchNorm 통계가 batch 8 기준이라 noisy 한 점뿐이며
model card 에 명시한다. (처음에 "lr 이 4배 과다" 로 오판했으나 nbs 때문에 틀렸다.)

최종 run: `runs/stage_a_m_640_b8_seed42/` (epoch 1: 1,698 s / Pose mAP50-95 0.7732)
중단본 보존: `_aborted_m_actual_b8_20260815/` (1차) · `_aborted_m_b16_oom_20260815/` (2차)
60 epoch 약 25~28 h 예상 → 08-16 아침.

### ★자기참조 함정을 하루에 세 번 밟았다 — 프로세스 정리 규칙

```
1. pkill -f "one_run.py..."        자기 셸이 매칭돼 셸이 죽음 (exit 144)
2. pgrep -f "yolo pose train"      감시 셸이 매칭돼 체인이 2h30m 정지
3. pkill -f "chain_stage_a_m.sh"   또 자기 셸 사망 (exit 144)
```
`pgrep -f` / `pkill -f` 는 **명령줄 전체**를 본다. 감시자와 피감시자가 같은 키워드를 쓰면
반드시 오탐한다. 앞으로는:
```
정리:  ps -eo pid,comm --no-headers | awk '$2=="yolo"{print $1}' > pids.txt
       xargs -r kill < pids.txt
감지:  ps -eo comm,args --no-headers | awk '$1 ~ /^python|^yolo/ && /bin\/yolo pose train/'
```
★ comm 은 15자로 잘리고 이 경우 `yolo` 로 나온다 — `^python` 만 보면 놓친다(실제로 놓쳤다).

### probe 결과 원문 (02:26): batch 32 "통과" 로 판정했으나 실패했다
```
probe        {"ok": true, "peak_gib": 8.34}   (fraction 0.004, val 없음)
선택         batch 32 · lr0 0.01   = nano 와 완전히 동일한 조건 → 공정 비교 가능
run          runs/stage_a_m_640_b32_seed42/
```
★ 마진이 0.5 GiB 뿐이다(가용 8.9 vs peak 8.34). probe 는 300장·val 없이 잰 값이라
실학습이 더 쓸 수 있어 **첫 epoch 의 val 이 관문**이었는데, 통과했다.

```
              epoch 1 소요   epoch1 Pose mAP50-95
nano            580 s          0.5806
medium        1,707 s          0.7720     ← 큰 모델이 초기 수렴 빠름
배수            2.94x
```
60 epoch 환산 약 25 h [추정] → 완료 예상 08-16 새벽.
단 초기 수렴이 빠른 것과 최종 성능이 높은 것은 별개다. nano 도 60 epoch 에서 0.9595 까지
갔고 합성 val 은 포화 구간이라 둘 다 0.96 근처로 수렴할 수 있다 [추정].
**medium 의 가치는 합성 점수가 아니라 real finetune 때 비교할 대안 base 확보에 있다.**

### 환경 함정

```
conda env 가 둘로 갈림
  pallet-pose     ultralytics 8.0.120  ← YOLO26 불가 (CLAUDE.md 기본 env)
  pallet-yolo26   ultralytics 8.4.60   ← YOLO26 은 반드시 여기
project= 는 절대경로   상대면 SETTINGS['runs_dir']("runs") 아래 runs/pose/... 로 묻힌다
data yaml 의 path 도 절대   상대면 SETTINGS['datasets_dir'] 기준으로 풀린다
```

### 관찰 — G·T 모두 라벨되지 않은 팔레트가 함께 찍혀 있다

overlay 육안 확인 중 발견. G 는 `objects` 가 항상 1개인데 `n_context_visible=3` 인 프레임이
54%(21,778/40,000)이고, 그중 팔레트인 사례가 눈으로 확인된다. T val 에서도 좌우에 라벨 없는
팔레트가 보였다. 라벨 없는 팔레트는 배경으로 학습되어 **detection recall 을 떨어뜨릴 수 있다**
(과제 1순위 지표). 재렌더는 범위 밖이라 이번엔 그대로 진행했고, real finetune 후 recall 이
낮으면 **가장 먼저 의심할 원인**으로 기록한다.

### 다음

```
1. m probe 결과 확인 → 학습 진행 감시
2. nano checkpoint 13개 합성 val 평가 (GPU 여유 생길 때)
3. 최종 선택 → final/pallet_yolo26n_pose_640_b32_final.pt + inference_config.yaml + model_card.md
4. reports/06 작성
5. real finetune 라운드 (별도) — 133장 13세션, eval 161장은 봉인 유지
```

## 파일 정리 — 백업 잔재 (앞선 폴더 재편의 마무리)

08-14 세션에서 폴더 재편·중복 조사를 한 뒤, 파일 단위로 남은 것을 훑었다.

### 조사한 축과 결과
```
내용이 완전히 같은 .py    0건
버전/백업 잔재            .orig 5 · .bak 1
빈 폴더                   7 (대부분 의도적)
0바이트 .py               __init__.py 뿐 = 정상
```

### 지운 것
```
scripts/self_training/self_train.py.orig        원본과 783줄 차이 = 옛 버전
scripts/data_prep/isaac_sim/sdg_scene.py.orig   2216줄 차이
challenge/scripts/run_live.py.orig · annotate.py.orig
scripts/data_prep/eval/evaluate_on_val.py.bak   ★git 에 커밋돼 있던 유일한 백업
weights/paper_s2_pdg/_smoke_{A1,A2}             빈 폴더, 참조 0
```
전부 현재 원본이 git 에 있어 복구 가능하다.

### ★건드리지 않은 것 — 이름만 보면 쓰레기 같지만 아니다
```
data/pallet/training_data/mixed_v8_train/*.json.orig   7,205개 114M
  challenge/scripts/annotate/restore_orig.py 가 실제로 읽는 복원 소스다.
  지우면 v8 변환 이전으로 되돌릴 수단이 사라진다.
real_unlabeled_ralph_EMPTY        이름부터 의도적 빈 셋
_live_captures · capturenight01_manual_gt   사용자가 보존 지시한 빈 폴더
Deep_Object_Pose/**/*.orig        upstream 영역
```

### 검증
`pytest challenge/tests/` 943 passed / 4 skipped / 0 failed.

## 25y_automatic_lifter 의 데이터 711M 을 원 프로젝트로 이관

### 먼저 확인한 것 — "저쪽에 있으니 지워도 된다" 는 성립하지 않았다
```
두 사본 비교   diff -rq 결과 32개 항목이 다르다.
               pallet-pose 쪽에만 fsm.py / Korea-Railroad 쪽에만 align_logger.py
최근 수정      Korea-Railroad 2026-08-15 (yolo_inference·truckalignment·top) — 지금도 작업 중
               pallet-pose   2026-08-14 (내가 주석 경로 고친 것), 그 외 2026-06-08
참조           challenge/tests/test_decoder_reconciliation.py:144 가
               depth_cam/calib/dope_inference.py 를 검사한다
```
→ 폴더 자체는 **지우지 않았다**. 갈라진 사본이고, 테스트가 코드를 참조한다.

### 옮긴 것 — 코드가 아니라 데이터
717M 중 711M 이 데이터였다.
```
depth_cam/rec/    524M  mp4 18   forklift 촬영 원본
depth_cam/runs/   168M  101      YOLO seg finetune 산출물 (.pt 43M×2 등)
depth_cam/docs/    19M  2        demo.mkv
→ /home/minjae/Documents/github/Korea-Railroad-project/25y_automatic_lifter-master/depth_cam/
남은 코드 6.9M
```
대조 [확인]: 파일 수 18/101/2, bytes 548452402/175694957/19496807 양쪽 일치.

### 안전 근거
```
저쪽 .gitignore 가 이미 3중으로 막는다
  :27 25y_automatic_lifter-master/**/runs/
  :28 25y_automatic_lifter-master/**/rec/
  :46 *.mp4   :49 *.mkv
→ 이동 후 저쪽 git status 의 untracked 0. 524M 이 커밋될 위험 없음.
이쪽에서는 runs/ 의 추적 10개(args.yaml·results.csv·tfevents)만 삭제로 잡혔다.
rec/ 와 docs/ 는 원래 .gitignore(depth_cam/rec/) 라 git 이 몰랐다.
```
★rec/ 의 mp4 는 **이 저장소에만 있던 유일본**이었다(Korea-Railroad 쪽 mp4 0개).
git 밖이라 백업도 없었으므로, 지우는 대신 원 프로젝트로 옮긴 것이 맞다.

pytest 943 passed / 4 skipped / 0 failed.
