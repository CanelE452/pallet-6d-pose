# 2026-06-06

## C1 self-training R0 vs R1 평가 (3d-expert)

### 추가
- `scripts/data_prep/eval/c1_r0_vs_r1.py` — R0(paper_base) vs R1_outside/R1_night를 outside(128)/night(90)/indoor(440) GT셋에서 평가. order-free 검출률(≥6 corner) + 9kp reproj(Hungarian 8corner + centroid, GT projected_cuboid 대비) + good(<10px)%. 학습 없음, 추론만. belief 추출/Hungarian 함수는 `filter_pr_camfacing.py`에서 재활용. `_exclude.txt` 반영.
- 산출: `scripts/data_prep/eval/c1_r0_vs_r1_results.json`

### 결과
- R1이 검출률은 ↑(outside +6.2%, night +17.8%)지만 reproj median은 모든 도메인 악화(outside +7.3px, night +4.3px), good(<10px)% outside 12.1→0% 붕괴. cross-domain도 악화.
- 결론: diag/diag∧ratio 2D 기하 필터가 centroid 일관성은 맞추나 전체 8 corner 절대 위치가 부정확한 PL을 다량 통과 → 모델이 "더 자주 더 거칠게" 학습. 발표 교훈(다량 PL인데 성능↓) 품질 필터로도 재현.
- 교차검증: R0 synthetic val PCK@5px 98.8%지만 corner2d_median 11.7px → real 18px와 일관, 평가 코드 신뢰.
- provenance 주의: paper_base header에 `mixed_v8_train` 포함 → CLAUDE.md v8 폐기 방침과 충돌 가능, base 라벨 별도 점검 필요.

### 문서
- `_docs/experiments/self_training/C1_rounds.md` 결과/결론 작성, 상태 "R0/R1 완료(R1 회귀)".
