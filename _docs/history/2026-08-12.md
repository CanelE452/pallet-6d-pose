# 2026-08-12

## [19:1x] 사전등록 — low-rank late-A1 adaptation screen (실행 전 lock)

### 0. role-depth 2 commit push
`git log origin/main..HEAD` = 정확히 2개(fd6db8b·df1c2d0), diff 는 role-depth screen 6개 파일.
push 후 local HEAD == origin HEAD == `df1c2d0` **[확인]**. amend/rebase/squash 없음.

### 1. role-depth scope addendum + 표현 정정
`ROLE_ENCODER_DEPTH_SCOPE_ADDENDUM.md` 신규 (기존 결과 무수정).
★ **표현 철회**: 이전 문서/history 의 "block worked at maximum" / "engaged completely"
→ **`STRONGLY_EXERCISED`**. dchg 1.60 은 "크게 행사됨"이지 "천장 도달"이 아니며,
beta 정체 + body 성장은 오히려 천장 해석의 반증.
not established: role encoder capacity 충분/소진 · 1 block 이 최적 depth ·
attention 이 엉뚱한 곳을 봄 · 더 넓은 role decoding 이 도움 안 됨.

### 2. 다음 질문
`CAN_CONSTRAINED_IN_BLOCK_LATE_A1_ADAPTATION_RECOVER_THE_F1_SIGNAL_WITHOUT_FULL_UNFREEZE?`
```
F1  late conv 내부 full adaptation  2.070244/1.077348, 최고 정확도, D0/D2 gap 42.5%
F2  F50 밖 residual adaptation      3.0850/1.5541, 특화 없음, 17~21%
R1  F2 + 더 깊은 role decoder       2.9097/1.2309, 특화 없음, 5.68%/20.80%
→ 미검증 칸 = 내부(F1 위치)이되 제약된 adaptation
```

### 5. late-stage conv 실측 (추측 금지, HARD_BLOCK 대조)
```
[19] Conv2d 256→512 k3 pad1 bias  1,180,160
[21] Conv2d 512→512 k3 pad1 bias  2,359,808
[23] Conv2d 512→256 k3 pad1 bias  1,179,904
[25] Conv2d 256→128 k3 pad1 bias    295,040
Conv2d 4개 · 합 5,014,912 (= F1 이 푼 수와 정확히 일치) · 최소 채널차원 128
```

### 3~10. 신규 runner + 구조
`scripts/stage0/late_a1_low_rank_adaptation_screen.py` (기존 4개 runner 무수정).
```
L0 = DIRECT_HOUGH_TOKEN_XY_V0 + 완전 frozen A1 (= Phase A F0). post-F50 adapter 없음, extra role block 없음
L1 = L0 + 각 late conv 에 low-rank additive branch
     y = Conv_base(x) + B(A(x))
     A: 1x1 in→8 bias 없음, 표준 init   B: 3x3 8→out bias 없음, **정확히 0 init**
     A·B 사이 비선형 없음(선형 low-rank update) · 별도 scalar gate 없음(B=0 만으로 exact identity)
rank 8 고정, 4/16/32 sweep 금지. 근거는 결과가 아니라 사전 구조 제약(최소 채널 128 대비 충분히 작음)
```
parameter audit (코드 계산):
```
[19] A 2,048 + B 36,864 = 38,912 | [21] 4,096+36,864=40,960
[23] 4,096+18,432=22,528 | [25] 2,048+9,216=11,264
LOW_RANK_FEATURE_PARAMS 113,664 = F1 unfreeze 5,014,912 의 **2.2665%**
total trainable 177,409 (encoder 55,041 + head 8,704 + low-rank 113,664)
A1-origin trainable **0**
```
※ "params 적음 = generalization 좋음" 인과 주장 금지 — 비율은 diagnostic.

### 9,11~15. preflight 5종 전부 PASS **[확인]**
```
merge      max|Δ| 2.980e-06 ≤ 1e-5 → LOW_RANK_CONV_MERGEABLE (B 를 non-zero 로 채워 시험,
           zero-init 상태면 자명 통과라서. throwaway 모듈로 canonical pair 미접촉)
canonical  decoder 2개 먼저 생성·state 복사 → low-rank 를 **마지막에** 생성, checksum 동일
STEP-0     late F50 0.000e+00 · descriptor 0 · logits 0 · loss 0
           + **frozen path vs 공식 A1 0.000e+00** (내 forward 분해가 vgg(x) 와 동일함을 확인)
wiring     step0 up grad 4개 전부 >0 (19:5.86e-6 21:6.75e-6 23:1.26e-5 25:4.60e-5),
           1 step 후 up 이동 >0, step2 down grad 4개 전부 >0,
           **base grad 보유 0 · A1-origin trainable 0**, encoder 3.083e-3 · head 1.543e-1
memory     batch 8 유지, peak 1,385.3 / 9,853 MiB
parity     결정적 모드 control 0.000e+00, **L0 vs locked runner 0.000e+00**
           → F0 를 Phase A 에서 재사용해도 code path 동일 (2시간 절약, 비교 정당성 확보)
```

### 16~24. 학습·판정
low-rank branch LR = head LR 1e-3 (F1 의 0.1× 는 pretrained 가중치용이라 재사용 안 함), WD 기존.
scheduler·clipping·sweep 없음. FULL 13,618 · batch 8 · 25,545 step ·
marks 1703/5000/8515/17030/25545 · **decision 25,545 only**.
selection baseline = `direct_hough_long.json` F0 @25,545 full precision.
F1/F2/R1 은 context only — AST 테스트가 reduction test·DECISION 분기 진입 차단.
진단(선택 미사용): per-conv ‖ΔW‖/‖W0‖ · F50 relative L2 drift · F50 cosine vs frozen ·
descriptor drift · D2/D0 비 · per-role 12개 · tails.

판정 E1~E5 사전 고정. **E3 면 "low-rank adaptation cannot work"·"full unfreeze is required" 금지** →
`LOW_RANK_ADAPTATION_REFUTED=False`, `FULL_UNFREEZE_PROVEN_REQUIRED=False`,
`F1_SIGNAL="unchanged"`, SCOPE("rank 8, one formulation, one LR") 를 JSON 에 강제 기록.
E5(NaN)면 LR·rank 수정 재실행 금지.

테스트 `test_late_a1_low_rank_adaptation_screen.py` 23 pass. 전체 882 passed.

## [20:2x] 결과 — LATE_A1_LOW_RANK_INSUFFICIENT (E3) + LOW_RANK_A1_SPECIALIZES (E4 진단)

**decision @25,545 D2 (raw JSON 재계산)**:
```
                angle med   offset med   angle p90   offset p90   gate
L1 low-rank      2.450502     1.114699   11.359642     4.747742   0/4 FAIL
F0 frozen        3.735687     1.972937   27.843582     8.279793   0/4 FAIL
L1 vs F0          +34.40%      +43.50%     +59.20%      +42.66%
40% 기준          2.241412     1.183762   → angle **미달**(4.4pt), offset 통과 → 양축 필요라 E3
```
L1 ladder:
```
 1,703 D0 7.5026/4.8296 | D2 7.9907/5.0584  CE 8.032967  drift 1.3867 cos 0.5788 dW/W 0.294~0.439
 5,000 D0 3.1850/2.0492 | D2 3.4564/2.1463  CE 6.479651  drift 2.0716 cos 0.4640 dW/W 0.528~0.690
 8,515 D0 2.4158/1.3190 | D2 2.7881/1.5146  CE 5.888951  drift 2.8137 cos 0.4202 dW/W 0.710~0.889
17,030 D0 2.0361/1.0172 | D2 2.4362/1.2210  CE 5.394779  drift 3.2360 cos 0.3846 dW/W 1.001~1.187
25,545 D0 1.9865/0.9074 | D2 2.4505/1.1147  CE 5.165383  drift 3.9068 cos 0.3724 dW/W 1.163~1.352
tail @25,545  >5° 0.2699 · >10° 0.1172 · >2cell 0.2944   n=5,921   finite=True
```
- ★ **"low rank ≠ small" — 이 screen 의 전제를 정정해야 한다.** 최종 ‖ΔW‖/‖W0‖ 가 네 conv 모두
  **1을 넘음**(1.163~1.352). rank 8 은 ΔW 를 rank-8 부분공간에 가두는 제약이지 **크기 제약이 아님**.
  즉 이 arm 이 시험한 건 "적은 adaptation" 이 아니라 **"저차원 구조로 제한된 adaptation"**
  (F1 의 2.2665% 파라미터 + F1 이상의 크기 자유도). → 작은 D0/D2 gap 을 "변화가 작아서" 로
  설명 불가, 중앙값 미달을 "branch 가 굶어서" 로도 설명 불가.
- ★ **특화 발생**: D0 angle 3.494102→1.986481 = **+43.15%(40% 통과)** vs D2 +34.40%(미달)
  → `LOW_RANK_A1_SPECIALIZES=True`. hard decision 은 E3 그대로.
  D2/D0: L1 1.2336/1.2284 · F1 1.4255/1.2994 · F2 1.0445/1.0766 · R1 1.0461/1.0283
  → **late conv 내부를 건드리는 arm 은 특화되고, 밖에서 건드리는 arm 은 안 됨**(각 2점, 법칙 아님).
- context(선택 미사용): L1 vs F1 angle −18.37% · offset **−3.47%** · p90 −17.08%/−15.08%
  → Pareto False(F1 이 4지표 우세). 단 **파라미터 2.27% 로 offset 은 F1 의 3.5% 이내**,
  F2·R1 은 전 지표에서 앞섬.
- 마지막 구간: angle median 2.4362→2.4505 로 **−0.59% 악화**(17,030 이 더 좋았음). 단 그 지점도
  기준 대비 8.7% 부족이라 어느 mark 를 택해도 미달. decision step 은 사전 고정대로 25,545.
- merge: 학습 전 2.980e-06 ≤ 1e-5 → `LOW_RANK_CONV_MERGEABLE` (inference 시 frozen kernel 에
  합칠 수 있음). 진단, 선택 미사용.
- per-role: **12개 전부 두 축 개선**(R1 은 2개 role angle 악화였음). high 2/4/6/7 5.864→3.274(−44.2%,
  F1 −54.6%), lower 3/9/10 2.334→1.757(−24.7%, F1 −27.9%). role 9·2·7 은 최소 한 축에서 F1 을 능가.
- **금지 표현 준수**: "low-rank adaptation cannot work"·"full unfreeze is required" **쓰지 않음**.
  JSON 에 `LOW_RANK_ADAPTATION_REFUTED=False`, `FULL_UNFREEZE_PROVEN_REQUIRED=False`,
  `F1_SIGNAL="unchanged"`, SCOPE("rank 8, one formulation, one LR") 기록.

**Standing**: architecture **NOT_LOCKED**. replicate·role shuffle·whole LINE_DEV 전부 **BLOCKED**(E1 아님).
CIGM/PnP/dimensions/K-pose BLOCKED. NEXT = `REGULARIZED_LATE_A1_FULL_ADAPTATION` 별도 lock 검토
(미구현·미생성). 산출 `low_rank_a1.json`, 문서 `LOW_RANK_A1_RESULT.md`.

## [21:1x] 사전등록 — L2-SP pretrained-weight anchoring (실행 전 lock) + **HARD_BLOCK**

### 0. low-rank 2 commit push
`git log origin/main..HEAD` = 정확히 2개(012b010·f321345), diff = low-rank screen 5개 파일.
push 후 local HEAD == origin HEAD == `f321345` **[확인]**. amend/rebase/squash 없음.

### 1. low-rank scope addendum
`LOW_RANK_A1_SCOPE_ADDENDUM.md` 신규. **금지 표현 4종 명시**:
"low rank was too restrictive" / "low rank kept the backbone close to pretrained"
→ **이 run 자체가 반증**(‖ΔW‖/‖W0‖ 네 conv 모두 >1, F50 drift 3.9068).
"full unfreeze is required" / "internal adaptation necessarily specializes" → 근거 없음(각 2점).
정확한 표현: `LOW_RANK = LOW-DIMENSIONAL STRUCTURAL CONSTRAINT, NOT SMALL-MAGNITUDE CONSTRAINT`.

### 2~6. 신규 runner + L2-SP
`scripts/stage0/regularized_late_a1_full_adaptation.py` (기존 runner 무수정).
```
S0 = historical F1 그대로 (net.vgg[19:27] full trainable, A1 LR 1e-4, encoder/head 기존)
S1 = S0 + L2-SP.   r_l = ‖W_l−W_l0‖²_F / (‖W_l0‖²_F + eps),  R_SP = mean_l r_l
     L_total = L_task + λ_sp·R_SP.  **W0 쪽으로 당김(0 쪽 아님)**, 기존 WD 대체 안 함
SPReference: canonical A1 로드 직후 late param clone, requires_grad=False,
             name/shape/norm/sha256 기록 + mark 마다 verify() 로 변조 검출
```

### 7~8. λ calibration → **SP_CALIBRATION_REFERENCE_MISSING (HARD_BLOCK)**
λ 는 D0/D2 를 보지 않고 train-only 로 1회 결정론적 정규화:
`λ_sp = CE_ref / R_SP_ref` at historical F1 @1,703.

`CE_ref = 7.590148` 은 JSON 에 있으나 **`R_SP_ref` 계산 불가** — 이유:
```
DH_f1/step_01703.pth 실측: top-level = tag/step/model/a1_trainable/provenance
  model = decoder 28 tensor (position/encoder/head), **vgg 텐서 0개**
  a1_trainable = 카운트 dict (vgg_params:int, trainable_fraction_of_vgg:float) — 가중치 아님
92개 .pth 전수 스캔: 적응된 net.vgg[19:27] 가중치 저장본 **어디에도 없음**
원인: Phase B train_arm 이 model.state_dict() + a1.report() 만 저장(가중치 미저장)
```
→ §7 지시대로 **추정·재구성 금지**. 재실행 복원도 금지(default kernel 비재현성 1.4e-3/20step
이미 기록됨 → 재실행 W ≠ historical W). `calibrate` 가 실제로 RuntimeError 발생하고
`run` 도 `locked_lambda()` 에서 차단됨 **[확인 — 두 커맨드 실제 실행]**.
→ **별도 coefficient lock 필요. 학습 착수 안 함.**

### 9~22. 나머지는 구현 완료(λ 확정 시 즉시 실행 가능)
step0(F50/descriptor/logits/task loss ≤1e-6 + R_SP≈0) · gradient sanity(**step0 SP grad=0 이 정상**,
1 task step 후 SP grad>0 요구 + cos(g_task,g_sp) 진단) · memory · 25,545 step ·
marks 5개 · decision 25,545 only · baseline = historical F1 full precision ·
context F0/F2/R1/L1 은 selection 차단(AST 테스트).
G1~G6 사전 고정, **G3 정량 사전등록**: F1 대비 두 median 열화 ≤+10% AND 두 ratio 가
1 에 20% 이상 근접. G5 = median ±5% & gap 변화 <10%. G4 는 "λ 가 일반적으로 크다" 확대 금지.
`CAUSAL_LIMIT` 을 JSON 에 기록 — G2/G3 라도 "drift 가 특화의 원인" 주장 금지.

테스트 `test_regularized_late_a1_full_adaptation.py` 26 pass. 전체 913 passed.
