# 2026-08-12

## [19:1x] 사전등록 — low-rank late-A1 adaptation screen (실행 전 lock)

### 0. role-depth 2 commit push
`git log origin/main..HEAD` = 정확히 2개(fd6db8b·df1c2d0), diff 는 role-depth screen 6개 파일.
push 후 local HEAD == origin HEAD == `df1c2d0` **[확인]**. amend/rebase/squash 없음.

### 1. role-depth scope addendum + 표현 정정
`ROLE_ENCODER_DEPTH_SCOPE_ADDENDUM.md` 신규 (기존 결과 무수정).
★ **표현 철회**: 이전 문서/history 의 "block worked at maximum" / "engaged completely"
→ **`STRONGLY_EXERCISED`**. dchg 1.60 은 "크게 행사됨"이지 "천장 도달"이 아니며,
beta 정체 + body 성장은 오히려 천장 해석의 반증.
not established: role encoder capacity 충분/소진 · 1 block 이 최적 depth ·
attention 이 엉뚱한 곳을 봄 · 더 넓은 role decoding 이 도움 안 됨.

### 2. 다음 질문
`CAN_CONSTRAINED_IN_BLOCK_LATE_A1_ADAPTATION_RECOVER_THE_F1_SIGNAL_WITHOUT_FULL_UNFREEZE?`
```
F1  late conv 내부 full adaptation  2.070244/1.077348, 최고 정확도, D0/D2 gap 42.5%
F2  F50 밖 residual adaptation      3.0850/1.5541, 특화 없음, 17~21%
R1  F2 + 더 깊은 role decoder       2.9097/1.2309, 특화 없음, 5.68%/20.80%
→ 미검증 칸 = 내부(F1 위치)이되 제약된 adaptation
```

### 5. late-stage conv 실측 (추측 금지, HARD_BLOCK 대조)
```
[19] Conv2d 256→512 k3 pad1 bias  1,180,160
[21] Conv2d 512→512 k3 pad1 bias  2,359,808
[23] Conv2d 512→256 k3 pad1 bias  1,179,904
[25] Conv2d 256→128 k3 pad1 bias    295,040
Conv2d 4개 · 합 5,014,912 (= F1 이 푼 수와 정확히 일치) · 최소 채널차원 128
```

### 3~10. 신규 runner + 구조
`scripts/stage0/late_a1_low_rank_adaptation_screen.py` (기존 4개 runner 무수정).
```
L0 = DIRECT_HOUGH_TOKEN_XY_V0 + 완전 frozen A1 (= Phase A F0). post-F50 adapter 없음, extra role block 없음
L1 = L0 + 각 late conv 에 low-rank additive branch
     y = Conv_base(x) + B(A(x))
     A: 1x1 in→8 bias 없음, 표준 init   B: 3x3 8→out bias 없음, **정확히 0 init**
     A·B 사이 비선형 없음(선형 low-rank update) · 별도 scalar gate 없음(B=0 만으로 exact identity)
rank 8 고정, 4/16/32 sweep 금지. 근거는 결과가 아니라 사전 구조 제약(최소 채널 128 대비 충분히 작음)
```
parameter audit (코드 계산):
```
[19] A 2,048 + B 36,864 = 38,912 | [21] 4,096+36,864=40,960
[23] 4,096+18,432=22,528 | [25] 2,048+9,216=11,264
LOW_RANK_FEATURE_PARAMS 113,664 = F1 unfreeze 5,014,912 의 **2.2665%**
total trainable 177,409 (encoder 55,041 + head 8,704 + low-rank 113,664)
A1-origin trainable **0**
```
※ "params 적음 = generalization 좋음" 인과 주장 금지 — 비율은 diagnostic.

### 9,11~15. preflight 5종 전부 PASS **[확인]**
```
merge      max|Δ| 2.980e-06 ≤ 1e-5 → LOW_RANK_CONV_MERGEABLE (B 를 non-zero 로 채워 시험,
           zero-init 상태면 자명 통과라서. throwaway 모듈로 canonical pair 미접촉)
canonical  decoder 2개 먼저 생성·state 복사 → low-rank 를 **마지막에** 생성, checksum 동일
STEP-0     late F50 0.000e+00 · descriptor 0 · logits 0 · loss 0
           + **frozen path vs 공식 A1 0.000e+00** (내 forward 분해가 vgg(x) 와 동일함을 확인)
wiring     step0 up grad 4개 전부 >0 (19:5.86e-6 21:6.75e-6 23:1.26e-5 25:4.60e-5),
           1 step 후 up 이동 >0, step2 down grad 4개 전부 >0,
           **base grad 보유 0 · A1-origin trainable 0**, encoder 3.083e-3 · head 1.543e-1
memory     batch 8 유지, peak 1,385.3 / 9,853 MiB
parity     결정적 모드 control 0.000e+00, **L0 vs locked runner 0.000e+00**
           → F0 를 Phase A 에서 재사용해도 code path 동일 (2시간 절약, 비교 정당성 확보)
```

### 16~24. 학습·판정
low-rank branch LR = head LR 1e-3 (F1 의 0.1× 는 pretrained 가중치용이라 재사용 안 함), WD 기존.
scheduler·clipping·sweep 없음. FULL 13,618 · batch 8 · 25,545 step ·
marks 1703/5000/8515/17030/25545 · **decision 25,545 only**.
selection baseline = `direct_hough_long.json` F0 @25,545 full precision.
F1/F2/R1 은 context only — AST 테스트가 reduction test·DECISION 분기 진입 차단.
진단(선택 미사용): per-conv ‖ΔW‖/‖W0‖ · F50 relative L2 drift · F50 cosine vs frozen ·
descriptor drift · D2/D0 비 · per-role 12개 · tails.

판정 E1~E5 사전 고정. **E3 면 "low-rank adaptation cannot work"·"full unfreeze is required" 금지** →
`LOW_RANK_ADAPTATION_REFUTED=False`, `FULL_UNFREEZE_PROVEN_REQUIRED=False`,
`F1_SIGNAL="unchanged"`, SCOPE("rank 8, one formulation, one LR") 를 JSON 에 강제 기록.
E5(NaN)면 LR·rank 수정 재실행 금지.

테스트 `test_late_a1_low_rank_adaptation_screen.py` 23 pass. 전체 882 passed.
