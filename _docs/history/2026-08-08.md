# 2026-08-08

> 08-07 밤부터 이어진 연속 세션. **결과·판정 본문은 `2026-08-07.md` 에 있다**(복붙하지 않음).
> 이 문서는 날짜가 넘어간 뒤 만진 파일의 변경 내역만 기록한다.

## 변경 파일 (measC_yaw 하네스)

### `fourway_canonical.py` — 정본 평가셋 확장(56 → 119) 반영
- **Before**: `CANON` 이 도메인당 폴더 **1개**(`{dom: folder}`), 도메인 = `outside / noapril / cad`,
  `EXPECT` 합계 56. 표 라벨 폭 `{d:8s}`.
- **After**: `CANON` 이 도메인당 **폴더 리스트**(`{dom: [folder, ...]}`) 로 바뀌고 도메인이
  `outside_ft / outside_ft_p07 / outside_ft_p09 / outside_fv / noapril / cad` 로 분화.
  `EXPECT` = {outside_ft 63, p07 27, p09 36, outside_fv 22, noapril 12, cad 22}.
  가중치 조회용 `WDOM` 신설(outside_* → outside 학습분 매핑), `W(m, dom_key)` 시그니처가
  도메인 키를 받아 `WDOM` 으로 변환. config 에 `outside_split` 설명 추가, 라벨 폭 `{d:14s}`.
- **이유**: lock §1.6 의 outside final-test 세션(p07/p09)을 정본에 편입했고(`EVAL_SET_CANONICAL.md`),
  프로파일 결과 **p07(앙각 10.1°·근접·잘림 37%)과 p09(앙각 2.36°·잘림 6%)가 다른 레짐**이라
  합산하면 해석이 섞인다 → 세션 분리 보고가 필요했다.

### `fourway_canonical_paired.py` — 도메인 확장 + yaw 지표 추가
- **Before**: `DOMS = ["outside", "noapril"]`, ADD 한 지표만 페어 검정.
  per-frame 저장이 `{fid: v["ADD"]}` 로 ADD 값만 보관.
- **After**: `DOMS = ["outside_ft_p07", "outside_ft_p09", "outside_fv", "noapril"]`,
  per-frame 을 `{fid: v}`(dict 전체)로 보관해 **ADD 와 yaw 를 함께** Wilcoxon 검정.
  출력 md 에 `met` 열 추가, 라벨 폭 `{dom:14s}`.
- **이유**: (1) 정본 확장으로 도메인이 늘었고, (2) "yaw 가 좋아졌냐"는 질문에 unpaired median 으로
  답하면 안 되기 때문(ADD 에서 이미 unpaired → paired 로 결론이 뒤집힌 전례).

### `fourway_pose_metrics.py` — Success rate 열 추가
- **Before**: yaw / centroid / ADD / det rate 만 산출.
- **After**: `cv2`·`yaw_paired_diagnosis`(`gt_corners8`, `err2d_hungarian`) 임포트 추가,
  `success_rate()` 신설(Hungarian 2D 오차 median < 20px, 미검출은 실패, 분모 = pose 지표와 동일 n_gt),
  프레임에 `gt8` 부착, 표/JSON 에 `success_rate`·`n_success` 추가.
- **이유**: 논문 표의 Success 열을 pose 지표와 **같은 분모**로 맞추기 위해
  (기존 NN20 하네스는 분모가 117/43/18 로 달라 나란히 못 놓음).

### `yaw_paired_diagnosis.py` (신규, 08-07 작성분 유지) / `yaw_lever_diagnosis.py` (신규)
- 08-07 에 만든 진단 스크립트. 이후 다른 스크립트들이 `gt_corners8` / `err2d_hungarian` /
  `err180` 등을 여기서 임포트해 쓰는 **공용 헬퍼**가 됐다(별도 유틸 파일을 새로 만들지 않음).
- 내용·결과는 `2026-08-07.md` 의 "yaw 악화 원인 진단(paired)" · "yaw 레버 진단" 절 참조.

## 정본/테스트 (08-07 기록분, 파일은 그대로 유효)
`_docs/EVAL_SET_CANONICAL.md`(56→119, p07/p09 편입·봉인 소진 명기),
`challenge/tests/test_eval_set_canonical.py`(EVAL_FOLDERS·EXPECTED_TOTAL=119,
신규 `test_final_test_sessions_are_not_in_any_pseudo_label_pool`) — **pytest 7 passed**.

## 미결 (다음 세션 인계)
- **night 은 정본 eval 0장**. lock §1.6 의 night final-test = **n08(17) + n09(25) = 42장**이
  이미 어노테이션돼 있고(전량 PNG, split 없음) night PL 풀(n05~07)에 **미포함 = inductive-clean**.
  → eval split 부여 여부 **사용자 승인 대기**(사용자가 "night 에 부여 말 것"을 명시했으므로 임의 실행 금지).
  GT 프로파일만 측정 완료: n08 앙각 5.89°/잘림 41%, n09 5.95°/28% (기존 night 5~7 과 같은 레짐, 잘림만 증가).
- **cad 는 표 불가**: 전 방법 검출 붕괴(R0 2/22, rf 0/22, Ours 2/22, Naive 는 cad 전용 모델 없음).
  원인 = 거리 1.68m·폭 547px·잘림 23%·앙각 31.7°(19/22 가 20° 이상) = 학습 분포 밖.

## night final-test(n08/n09) 정본 편입 + 재평가 — 사용자 승인 실행

### 1단계: split 부여 (성능 미열람 상태에서 확정)
- `capturenight08_manual_gt` 17 + `capturenight09_manual_gt` 25 = **42장에 `split="eval"` 부여**.
  전부 기존 `(none)` → `train` 덮어쓴 것 0. 백업 = scratchpad/split_backup/.
- 사전 GT 품질: 전부 `gt_source=manual`, reproj med **1.48 / 2.20px**, >5px = 0 / 3장(평가 시 제외),
  `(-1,-1)` 코너 보유 프레임 n08 4 / n09 5(PnP 는 ≥6점이면 성립).
- **PL 풀 미포함 [확인]**: night 풀 = n05(176)/n06(160)/n07(164) 뿐 → n08/n09 **inductive-clean**.
- `_docs/EVAL_SET_CANONICAL.md`: 합계 **119 → 161**, "2026-08-08 변경" 절 신설(봉인 소진·보고 규칙),
  "봉인 유지" 목록에서 `capturenight08/09` 제거 → 남은 미접근 holdout = `testset_full8_manifest`,
  `handannot17` 뿐.
- `challenge/tests/test_eval_set_canonical.py`: EVAL_FOLDERS 에 night 2폴더(17/25),
  `EXPECTED_TOTAL=161`, `FINAL_TEST_FOLDERS` 에 night 2폴더 추가(누수 테스트 적용). **pytest 7 passed**.

### 3단계(먼저 보고): n08 vs n09 는 **같은 레짐**
```
set               n  dist m  elev°  width px  trunc%  GTreproj  rearClick%
night_ft_n08     17   2.59    5.89      419     41     1.48        49
night_ft_n09     25   2.70    5.95      333     28     2.20        47
night_fv(n05-07) 43   2.60    6.04      384     23     2.42        49
```
앙각 5.89 vs 5.95° 로 동일(outside 의 p07 10.1° vs p09 2.36° 같은 분화 없음). 차이는 잘림(41 vs 28%)뿐.
→ **합산(night_ft) 보고를 주로 하되 세션별도 병기**.

### 2단계: 정본 재평가 (weight = night 학습분, n08/n09 는 inductive)
```
domain        method            N       succ%   det%    ADD m
────────────────────────────────────────────────────────────────
night_ft      Synthetic only  24/39     38.5    61.5    0.281
(n08+n09)     Naive ST        25/39     35.9    64.1    0.393
              Reproj+flip ST  29/39     38.5    74.4    0.430
              Ours            26/39     46.2    66.7    0.297
────────────────────────────────────────────────────────────────
night_ft_n08  R0/Naive/rf/Ours  succ 47.1/58.8/47.1/64.7  det 70.6/76.5/70.6/70.6
                                ADD 0.160/0.359/0.204/0.208
night_ft_n09  R0/Naive/rf/Ours  succ 31.8/18.2/31.8/31.8  det 54.5/54.5/77.3/63.6
                                ADD 0.417/0.470/0.448/0.350
```
PAIRED (Wilcoxon):
```
dom            vs               met  N    other -> Ours    better   p
night_ft       Synthetic only   ADD  23   0.281 -> 0.272   14/23   0.105
night_ft       Naive ST         ADD  24   0.376 -> 0.277   18/24   0.0022 ★
night_ft       Reproj+flip ST   ADD  26   0.439 -> 0.297   19/26   0.053 (경계)
night_ft       yaw (전 쌍)                                          0.12~0.78 n.s.
night_ft_n08   Naive ST         ADD  12   0.336 -> 0.208    9/12   0.016 ★
night_ft_n08   Synthetic only   ADD  12   0.160 -> 0.208    6/12   0.791 (R0 우세)
night_ft_n09   Synthetic only   ADD  11   0.446 -> 0.333    8/11   0.042 ★
night_ft_n09   Naive/rf         ADD  12~14                         0.064 / 0.153
```

### ★판정 — transductive 수치는 재현되지 않았다 (사용자 사전 경고 적중)
1. **ADD 0.189(transductive n05~07) → 0.297(final-test n08/n09)**. **1.57배 악화**.
   outside 의 0.158 → 0.351(2.2배)과 같은 방향. **filter-val 세션 수치를 주 결과로 쓰면 과대보고**가
   night 에서도 확인됨.
2. **Ours vs Synthetic-only 는 night 에서 유의하지 않음**(합산 p=0.105, n08 은 오히려 R0 우세 p=0.791).
   n09 만 유의(p=0.042). = outside_ft_p09 에서 성립했던 "Ours > R0" 가 night 에서는 성립 안 함.
3. **살아남는 주장은 여전히 필터 비교**: Ours > Naive ADD 합산 **p=0.0022**(18/24),
   n08 p=0.016. Ours > rf 는 경계(p=0.053).
4. Success 는 Ours 가 최고(46.2 vs R0 38.5)나 검출률은 rf(74.4)가 더 높음 → 검출 자체는 Ours 고유 아님.
5. yaw 전 쌍 n.s.
산출: `fourway_canonical.{json,md}`, `fourway_canonical_paired.{json,md}`, `dataset_profile.{json,md}`.

### 변경 파일 (이번 절)
- `challenge/data/capturenight0{8,9}_manual_gt/*.json` 42개: `objects[0].split` 없음 → `"eval"`.
- `_docs/EVAL_SET_CANONICAL.md`: 합계 119→161, 08-08 변경 절 추가, 봉인 목록 축소.
- `challenge/tests/test_eval_set_canonical.py`: EVAL_FOLDERS +2, EXPECTED_TOTAL 119→161,
  FINAL_TEST_FOLDERS +2.
- `measC_yaw/fourway_canonical.py`: CANON/EXPECT/WDOM/DOMS 에 `night_ft`, `night_ft_n08`,
  `night_ft_n09` 추가. config 의 `night` 키를 `night_split` 설명으로 교체.
- `measC_yaw/fourway_canonical_paired.py`: DOMS 에 night 3종 추가.

## 정성 비교 그림용 프레임 재선정 (조건 강제)

신규 `measC_yaw/pick_qualitative_frames.py`. 기존 그림 프레임이 self-train 쪽 conf-wrong(sev 6.63)
태그라 "검출이 늘었다"의 근거로 부적합하다는 지적 → **눈으로 고르지 않고 조건을 코드로 강제**.
조건: ① R0 det<6(PnP 불가) ② Ours(h8) det==8 ③ Ours ADD <= 도메인 Ours ADD median.
셋 = 정본 final-test(Day=outside_ft p07+p09 / Night=night_ft n08+n09).

### Day = 통과 3개
```
fid(끝12)      R0det  Ours det  ADD    yaw    dist    conf-wrong sev  nwrong  maxerr
884903302656     4       8     0.115   5.74   2.81m       1.86         2/8    29.6px  ★추천
804674198784     0       8     0.227   5.25   3.66m       2.33         3/8    23.8px
798391620864     0       8     0.236   7.93   3.50m       3.15         4/8    35.1px
```
- 추천 = `challenge/data/capturepallet09_manual_gt/1778653884903302656.png`
  (도메인 median 0.334 대비 ADD 0.115 = 1/3, sev 1.86 = 기존 6.63의 1/3.6).
- ★sev=0 프레임은 없다. ①②만 만족하는 7개 전수 확인 결과 sev 1.86~6.56 이며
  **ADD 필터가 sev 하위 3개와 정확히 일치**(ADD 컷이 conf-wrong 도 같이 걸러냄).

### Night = **조건 통과 0개** (완화하지 않고 그대로 보고)
```
깔때기            outside_ft(Day)   night_ft(Night)
① R0 det<6             25               15
② + Ours det==8         7                0   ← 여기서 전멸 (최대 det=6)
③ + ADD<=median         3                0
```
- ① 통과 15장의 Ours 상태: det=6 이 3장(ADD 0.311 / 0.874 / 1.080, 전부 median 0.297 초과),
  det=5 이하가 12장(PnP 불가).
- = **night 에서는 "R0 미검출 → Ours 완전검출" 사례가 존재하지 않는다.**
  night_ft 검출률이 R0 24/39 → Ours 26/39 (+2장) 로 미미했던 것과 정합하는 독립 확인.
- 선택지(사용자 결정): (a) Ours det>=6 로 완화 → 1장(`capturenight09 ...596017728000`,
  ADD 0.311 = median +4.7%) / (b) Night 정성 그림 포기하고 본문에 "night 엔 해당 사례 없음" 서술 /
  (c) night_fv(transductive)에서 선정 = 격 불일치, 비추천.
산출: `pick_qualitative_frames.{json,md}`.

### 변경 파일 (이번 절)
- `measC_yaw/pick_qualitative_frames.py` 신규.

## Day 정성 비교 그림 생성 (Night 은 (b) 채택으로 미생성)

신규 `measC_yaw/fig_qual_day.py` → 출력 **`_docs/figures/qual_day_R0_vs_ours.png`** (2400x1200, 2.00:1).
- 프레임 `challenge/data/capturepallet09_manual_gt/1778653884903302656.png`.
- 좌 R0(det 4/8, PnP 실패) / 우 Ours(det 8/8, ADD 0.115 m). **동일 크롭**(GT 큐보이드 기준,
  패널 종횡비에 맞춤 + `CROP_YBIAS=0.18` 로 위로 치우쳐 하단 빈 아스팔트 축소).
- 오버레이 규약: GT 초록 / 예측 파랑 / 재투영 빨강 / 20px 초과 코너 주황 사각.
  ★R0 는 PnP 실패라 **좌패널에 빨강 없음**(없는 것을 그리지 않음).
- 상단 태그는 det 개수 + ADD 만. conf-wrong / peak / sev 표기 제거(스펙대로).
- 폰트 A0 기준 확대(제목 1.7 / 태그 1.45 / 범례 1.15, 두께 3~4).
- **주의(캡션용)**: 20px 초과 코너가 **양 패널 모두 2개**(R0 는 4검출 중 2, Ours 는 8검출 중 2).
  동일 규칙을 두 패널에 적용한 결과이며 캡션에 분모를 함께 적지 않으면 오독됨.
- Night 은 조건 통과 0개라 미생성. 근거·깔때기를 `pick_qualitative_frames.md` "확정" 절에 기록.

### 변경 파일 (이번 절)
- `measC_yaw/fig_qual_day.py` 신규.
- `measC_yaw/pick_qualitative_frames.md`: "확정" 절 추가(Day 채택 프레임 / Night 0개 사유).
- `_docs/figures/qual_day_R0_vs_ours.png` 신규.

## Day 정성 비교 — heatmap 판 추가

신규 `measC_yaw/fig_qual_day_heatmap.py` → **`_docs/figures/qual_day_heatmap_R0_vs_ours.png`**
(2400x1200, 2.00:1). wireframe 판(`qual_day_R0_vs_ours.png`)과 **같은 프레임·같은 크롭**이라 짝으로 쓴다.
- heatmap 규약은 기존 `scripts/stage0/ralph_heatmap.py::heat_overlay` 를 그대로 따름:
  `hm = max(belief[:9])` → 원본 크기 resize → `COLORMAP_JET` → `addWeighted(0.55, 0.45)`.
  (상수를 새로 만들지 않고 기존 관례 재사용.)
- 옛 그림(`ralph_heatmap_confwrong.py` 산출, sev=6.63 프레임)과 다른 점:
  (a) 프레임이 조건 강제 선정본, (b) 태그에서 peak / conf-wrong / sev 제거하고 det + ADD 만 표기.
- 프레임·크롭·색 상수는 `fig_qual_day.py` 에서 import (단일 출처, 두 그림이 어긋나지 않게).
- 범례에 belief 0→1 컬러바 추가.
- 시각적 판독: 좌(R0)는 **왼쪽 코너군과 centroid 에만 반응**, 오른쪽 GT 코너 2개는 응답 없음(det 4/8).
  우(Ours)는 좌우 코너군 모두 반응(det 8/8). = "검출이 늘었다"가 belief 수준에서 보인다.

### 변경 파일 (이번 절)
- `measC_yaw/fig_qual_day_heatmap.py` 신규.
- `_docs/figures/qual_day_heatmap_R0_vs_ours.png` 신규.

## 정성 그림 패널 개별 저장 추가
`fig_qual_day.py` 에 `save_single(pan, title, out_path)` 신설(제목 + det/ADD 태그 포함, 범례 없음,
1190x1100) → wireframe/heatmap 두 스크립트가 공용으로 사용. 포스터에서 좌/우를 따로 배치하기 위함.
합본은 그대로 유지. 신규 산출:
```
_docs/figures/qual_day_R0_vs_ours__R0.png          / __ours.png          (wireframe 단독)
_docs/figures/qual_day_heatmap_R0_vs_ours__R0.png  / __ours.png          (heatmap 단독)
```

## 주황 사각(20px 초과 표시) 기본 off
`fig_qual_day.py` 에 `MARK_BAD = False` 스위치 신설(2026-08-08 사용자 요청). 그리기와 **범례 항목을
동시에** 조건부로 바꿔 어긋나지 않게 함. heatmap 스크립트는 `FQ.MARK_BAD` 를 참조.
개수(`corners>20px`)는 콘솔 로그에는 계속 출력되므로 정보 손실 없음(R0 2/4, Ours 2/8).
`MARK_BAD = True` 한 줄로 원복 가능하므로 표시본을 별도 파일로 남기지 않고 6장 전부 덮어씀.
→ 캡션에 "주황 = 20px 초과" 문구가 더 이상 필요 없다(앞서 안내한 분모 병기 주의사항 철회).

## 텍스트 없는 순수 패널(bare) 저장 추가
`fig_qual_day.py` 에 `save_bare(pan, out_path)` 신설 — 헤더 밴드·제목·det/ADD 태그·테두리 전부 없이
패널 이미지(1170x940)만 기록. 두 스크립트가 공용 사용. 기존 태그본·합본은 그대로 유지(덮어쓰지 않음).
신규 산출:
```
_docs/figures/qual_day_R0_vs_ours__R0_bare.png          / __ours_bare.png          (wireframe)
_docs/figures/qual_day_heatmap_R0_vs_ours__R0_bare.png  / __ours_bare.png          (heatmap)
```
→ 포스터에서 det/ADD 수치와 범례를 레이아웃 쪽에서 직접 다는 경우용.

## 정성 그림 가독성 개선 (A0 1~2m 기준) — 4항목
`fig_qual_day.py` / `fig_qual_day_heatmap.py` 수정.
1. **미검출 코너 빨간 X**: `draw_miss()` 신설. 예측이 없는 코너를 **GT 위치**에 X 로 표시
   (없는 예측을 그릴 수 없으므로 "있어야 하는데 없는 자리"를 가리킴). 상수 `C_MISS=(60,60,255)`,
   `MISS_ARM/MISS_TH = 30/9` (heatmap 판은 2배). 실측 결과 **R0 4개 / Ours 0개** = 스펙대로
   좌패널 전용. 범례에 `not detected`(X 글리프) 추가 — `draw_legend()` 신설로 dot/x 혼용 지원.
2. **히트맵 알파**: `ALPHA_IMG, ALPHA_HEAT = 0.55, 0.45` → **0.65, 0.35**.
   ★사용자 표기는 "0.55→0.35" 였으나 코드의 0.55 는 **이미지** 가중치이고 히트맵은 0.45 였다.
   의도(히트맵을 옅게)를 따라 **heat 를 0.35** 로 내리고 img 를 0.65 로 올림. 기존 관례
   (`ralph_heatmap.py` 0.55/0.45)와 달라진 점은 스크립트 주석에 명기.
   마커 2배: GT 링 r16 th4 → **r32 th8**, 예측점 r12 → **r24**.
3. **주황 `corner err > 20px` 제거 유지** (`MARK_BAD=False`). 개수는 콘솔 로그에만(R0 2/4, Ours 2/8).
4. **상단 태그 유지**, belief 컬러바는 우패널 아래로 축소(420x34 → **280x24**, 라벨 1.05→0.85).

산출(총 10장 갱신): 합본 2 / 태그본 4 / bare 4. bare 는 헤더·텍스트·테두리 없이 1170x940.

## 히트맵 정성 그림 A0 확정 (한 장만 사용)
`fig_qual_day_heatmap.py` 최종 조정. 산출 = **`_docs/figures/qual_day_heatmap_R0_vs_ours.png`** (2400x1200).
1. 미검출 코너 빨간 X — `FQ.draw_miss()`(GT 위치에 표시). 실측 R0 4개 / Ours 0개. 범례 `not detected`.
2. 히트맵 알파 **0.35**(img 0.65). 마커는 히트맵 전용 배율 **HEAT_MS = 2.0**
   (기준값 대비 2배: GT 링 r32 th8 / 예측점 r24 / X arm40 th12).
   ★wireframe 판의 `FQ.MS = 1.5` 와 **독립** — 배율 지시가 왕복(2→1.5→2)했으므로 그림별로 분리해
   서로 덮어쓰지 않게 함.
3. 주황 `corner err > 20px` 표시·범례 제거 유지(`MARK_BAD=False`).
4. 상단 태그 유지 + 우패널에 `(median 0.334)` 병기.
   ★median 은 하드코딩하지 않고 `fourway_canonical.json`
   `table.outside_ft["Ours (loo+flip)"].ADD` 에서 읽어 표기(앞선 yaw 열 오기 재발 방지).
   문자열이 길어져 우패널 태그 폰트만 1.45 → 1.25.
5. belief 컬러바 우패널 하단 소형(280x24) 유지.
wireframe 판은 유지하되 포스터에는 미사용.
