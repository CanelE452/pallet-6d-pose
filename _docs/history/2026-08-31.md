# 2026-08-31

## [세션] 커밋 범위 결정 — data/ 는 gitignore 방침에 걸린다

### 앞선 작업의 기록 위치

이 세션의 코드 변경(EXPERIMENTS.md 검증 4개 추가, mc_overlay_vdo PICK,
backbone_dope_final_v1 신설, hard_negative_v1, mc_* 표/AUC 도구)은 자정 전에
이루어져 **`_docs/history/2026-08-30.md` 에 이미 기록**돼 있다. 여기 복붙하지 않는다.
커밋 `4e99151` 로 push 완료 (문서·스크립트 6파일, +1,325).

### 오늘 새로 확인한 것 — `.gitignore` 가 data/ 를 의도적으로 막는다

"data 폴더도 push 했나" 라는 물음에 확인한 결과, **push 에 data/ 는 0건**이었다.
그 뒤 (가) "GT/매니페스트만" 으로 스테이징하려다 규칙에 막혔다.

```
.gitignore:11   data/**          data 하위 전부 차단
.gitignore:12   !data/**/        폴더 구조만 허용
.gitignore:13   !data/**/*.md    ★.md 만 예외
```

같은 패턴이 `weights/**/*.md`(18행), `challenge/data/**/*.md`(33행)에도 있다 —
**데이터는 빼고 문서만 추적**한다는 방침이 repo 에 설계돼 있다.

★ 내가 앞서 "`.gitignore` 가 data/evaluation 을 막고 있지 않다" 고 한 것은 **틀렸다**.
`git status` 의 `?? data/evaluation/` 만 보고 단정했는데, 그 표시는 `!data/**/` 로
폴더가 허용돼서 나온 것이었다. `git check-ignore -v` 로 확인했어야 했다.

### 현재 스테이징 (커밋 안 함 — 사용자 판단 대기)

```
경로                                스테이징    비고
─────────────────────────────────────────────────────────────
data/evaluation/**/*.md                6건     .md 예외로 통과
data/evaluation/**/*.json,csv          0건     ★data/** 로 차단 (646건 12MB)
challenge/real_gt_v2/**               65건     이 경로는 ignore 대상 아님
                                              (manifests 8, migrated_gt_wood 45,
                                               wood_audit 10, registry, README)
─────────────────────────────────────────────────────────────
합계                                  71건     이미지 0건
```

GT json/csv 를 넣으려면 `.gitignore` 방침을 깨야 하므로(강제 add 또는 예외 규칙 추가)
임의로 진행하지 않고 멈췄다. `data/evaluation` 전체는 1,840파일 605MB(PNG 1,143장).

### DOPE 백본 학습 — 진행 중

```
epoch 27 / 60   loss 0.016~0.023 (초기 0.067 대비 하강)
완료예정        09-01 10:22 (실측 22.4장/초 기준)
run             weights/backbone_dope_final_v1/run
```

### 어노테이션 진입점 (질문 답변, 코드 변경 없음)

```
scripts/annotate/annotate.py        ★main entry — 9kp 클릭 + PnP + NDDS JSON 저장
  annotate_pnp.py                   PnP / 3D 모델 / projection
  annotate_draw.py                  cuboid wireframe / overlay / UI
  annotate_io.py                    State / save / load
scripts/annotate/annotate_wood.py   wood 전용 런처 (레지스트리에서 기하 명시 선택)
scripts/annotate/audit_gt_data.py   저장 후 GT 검증 (T1~T5)
```
