# 2026-08-14

## [00:0x] batch size 상한 실측 — 코드 변경 없음, 측정만

배경: "이 컴퓨터는 batch 8 이 최대였다 / DOPE 16 은 안 됐다" 는 기억을 확인.
repo 파일 변경 없음(측정 스크립트는 scratchpad). 날짜만 넘어가서 이 파일을 만든다.
직전 작업(partial-edge supervision ablation)은 `2026-08-13.md` 가 정본, 커밋 `03b08d4` push 완료.

### 하드웨어 — 전제 정정
```
실제 GPU   NVIDIA GeForce RTX 3080, 10,240 MiB (4070 아님)
driver     580.173.02
idle       845~877 MiB (데스크톱)
```

### 실측 1 — DopeNetwork 합성 probe (448×448, fwd+bwd+Adam ×3)
```
batch   peak alloc   reserved     결과
    4      2,834       3,382      OK
    8      4,635       5,536      OK
   16      7,854       8,596      OK
   24        —           —        CUDA OOM
   32        —           —        CUDA OOM
```
⚠ 첫 시도에 `net(x)` 를 두 번 호출해 그래프를 2개 잡는 버그가 있었고(그때 16 이 OOM),
수정 후 위 수치. 잘못된 측정으로 "16 불가" 라 답할 뻔함.

### 실측 2 — 실제 `Deep_Object_Pose/train/train.py` (결정적 증거)
```
python train.py --data challenge/data/capturepalletcad_manual_gt \
  --batchsize 16 --imagesize 448 --epochs 1 --workers 4
→ exit=0, 2 epoch 정상 (Loss 0.0589 / 0.0439)
   target/output belief (16,9,50,50) · aff (16,16,50,50)
   peak GPU used 8,677 / 10,240 MiB  (데스크톱 포함)
```
**결론: DOPE batch16@448 은 현재 이 머신에서 돈다. 단 여유 1.5GB 뿐.**

### history 조회 결과 — "batch16 실패" 기록은 없음
검색 범위: `_docs/history/*.md` 의 batch·batchsize·OOM·메모리 문자열 전수,
repo 전체 `batch=16`/`--batchsize 16`, git log(batch·OOM), `weights/*/header.txt` 전수.
```
실제 실행된 DOPE batchsize (header.txt)
  challenge0123_ft_v2 / ft_manual        8 @448
  stage11_16k_B0_v3 / B1_v3addon         8 @448
  dope_cropaug_ft_s1 / s2                4 @448
  challenge_pretrain / ft_otftrunc       4 @448
  paper_s2 stageA/B (history)           12 @400, GPU 7,500MiB
→ DOPE 에서 16 을 쓴 적이 없다. batch16 은 stage5/stage7
  (finetune_pvnet/edgevec, @400) 에서만 사용, OOM 언급 없음.
```
history 의 OOM 3건은 전부 원인이 다름:
```
2026-04-10  Windows 시절, batch=4 에서 3연속 OOM. 원인 allocator 단편화 +
            Windows virtual memory 94% commit. 해결 = 앱 정리 +
            PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True
2026-08-07  외부 self_train.py 가 10GB 중 8.4GB 점유 → OOM
2026-06-18  512/640 OOM "가능" 사전 우려 → 실측 후 본인이 "과한 우려" 로 정정
```
→ 기억이 틀렸다고 보지 않음. 여유 1.5GB 라 **다른 프로세스가 있으면 실제로 터진다**
(08-07 이 그 사례). "원리적 불가" 가 아니라 "여유 없이 겨우 들어가는 값".

### 판단 (장기 학습 권고)
- 장시간 학습에 16 은 비권장 — 단편화 한 번이면 몇 시간 손실.
- 올리려면 `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True` 병행(04-10 에서 실효).
- 안전한 대안 = **12 + gradient accumulation** (7월 이후 paper_s2 계열이 이미 12/400).
- line-stage screen 은 batch 8 에 2,246 MiB 뿐 — 여기선 VRAM 이 병목이 아님.

### 4070 이전 검토 (추정, 해당 머신 미접근)
```
4070 데스크톱 12GB   +2GB → batch16 여유 3.5GB 로 안정. 24 는 여전히 어려움
4070 Ti Super 16GB   확실한 이득
4070 Laptop    8GB   3080보다 -2GB → 오히려 악화
속도                 대역폭 504 vs 3080 760 GB/s, L2 36MB vs 5MB
                     → 이 conv-heavy 워크로드에서 뚜렷한 우위 없음
```
★ 이전 시 비용: **진행 중 screen 계열의 parity 증명이 무효**가 된다. B1(historical P0)
재사용을 `0.000e+00` 로 자격검증한 것은 이 GPU·이 커널 기준이라, GPU 가 바뀌면 두 arm
재학습이 필요. → 현 계열은 3080 에서 종료하고, 새 계열부터 이전하는 편이 맞다.

### [01:0x] 제안받은 4090 머신 스펙 검토 (추가, 코드 변경 없음)
```
제안: B760M AORUS ELITE / i5-14400 (10코어 16스레드) / RTX 4090 24GB /
      RAM 16G×2 / SSD 1TB / Ubuntu 22.04 / CUDA 11.7

현재: i9-11900 (16스레드) / RTX 3080 10GB / RAM 31GB /
      디스크 1.8T 중 680G 사용 / torch 2.1.1+cu118 (CUDA 11.8)
      arch list sm_50/60/70/75/80/86/37/90
```
판정 요약 (근거는 위 실측 + 아래 실측):
```
GPU     10GB → 24GB   ★유일한 확실한 대형 이득. batch 상한 문제 소멸
CUDA    11.7          ▲정정(01:2x): 블로커 아님. 이 머신엔 nvcc 자체가 없고
                      (시스템 toolkit 미설치) torch wheel 이 CUDA 런타임을
                      번들한다. 실제 요건은 **드라이버** — 4090(sm_89)이면
                      525 이상. 시스템 CUDA 11.7 은 무관, torch 를
                      cu118/cu121 wheel 로 깔면 된다
SSD     1TB           ★빠듯. 현재 data 133G + challenge/data 90G +
                      weights 142G, 디스크 실사용 680G → 1TB 면 70% 점유
RAM     32GB          현재 31GB 와 동급 = 개선 아님. 08-02 의 RAM OOM
                      (5,916 프레임 일괄 적재, 31GB)은 그대로 재발 가능
CPU     i5-14400      i9-11900 과 대등. dataloader workers 4~6 이라 병목 아님
```
★ 이전 비용은 4070 항목과 동일 — GPU/커널이 바뀌면 진행 중 screen 계열의
parity 증명(B1 재사용 `0.000e+00`)이 무효가 되어 두 arm 재학습이 필요.

### [01:2x] 용도 확정 — 학교 서버, 학습 전용
전제가 바뀌어 위 판정 일부를 정정·재평가한다.
```
정정  CUDA 11.7 = 블로커 아님 (위 표 참조). nvcc 없음 확인:
      `which nvcc` → 없음. torch 는 wheel 번들 런타임 사용.
      요건은 드라이버 ≥525 (Ada). 현재 머신 드라이버 580.173.02.
이득  headless 라 데스크톱 VRAM(약 850MiB) 회수 → 24GB 전부 사용 가능
위험  ★공용 서버면 08-07 재현 위험이 오히려 커짐
      (외부 프로세스가 10GB 중 8.4GB 점유 → CUDA OOM).
      GPU 전용 배정 여부를 먼저 확인할 것
제약  SSD 1TB 가 가장 날카로움. 학습 전용이면 weights 가 가장 빨리 는다:
      현재 weights/ 142G, 단일 run 이 3.7~7.6G
      (paper_s2_rgb1_improve_v1 7.6G / paper_s2_q1 6.8G /
       micro_arch_screen 6.6G / stageA 4.0G / stage22_coord_pilot 3.7G)
      → 2TB 이상 권장. data 133G + challenge/data 90G 전송도 필요
불변  RAM 32GB = 현재 31GB 와 동급. 08-02 호스트 OOM 제약 그대로
```

### [01:4x] ⚠ 교차머신 혼동 — 위 실측값은 **이 리눅스 머신** 것이 맞다
다른 세션(Windows)에서 "앞 판단 3가지가 틀렸다(GPU 4070 12GB / CPU i7-13700F /
디스크 2개 2.3TB)" 는 정정이 들어왔으나, **그건 다른 컴퓨터다**. 근거:
```
이 머신  Linux minjae-MS-7D17, Ubuntu 22.04
         RTX 3080 10,240 MiB, driver 580.173.02, i9-11900   [확인: uname/nvidia-smi/lscpu]
저쪽     Windows 11, C:\Users\User, RTX 4070 12,282 MiB, i7-13700F
증거     저쪽 세션이 `_docs/history/2026-08-14.md` 를 C:\Users\User 아래에서
         못 찾았다고 보고 → 이 파일은 여기 리눅스 repo 에 있다(9f4b404)
```
→ **위 실측(3080 10GB, peak 8,677/10,240)은 정정 대상이 아니다.**
앞으로 이 값을 "틀렸다" 며 4070 으로 고치지 말 것. 두 대가 있고 서로 다른 값이다.

### DOPE 기본 batchsize = **32** (16 아님)
```
Deep_Object_Pose/train/train.py:1545   "--batchsize", default=32   [확인]
config/default.yaml                     pretrain/finetune batch_size: 4
실제 실행 이력(header.txt)              4 / 8, paper_s2 계열 12
```
기본값 32 는 이 프로젝트에서 한 번도 쓰인 적이 없다(10GB 에서 OOM).

### VRAM 선형모델 (3080 실측 4·8·16 에서 적합, @448)
```
peak_alloc(MiB) ≈ 1,034 + 450 × batch
검산  b4 1,834(실측 2,834) b8 4,634(4,635) b16 8,234(7,854)
추정  b24 ≈ 11.8GB · b32 ≈ 15.4GB → 12GB 카드에서도 24 이상은 불가
```

## 논문용 데이터셋 — 40k clean 셋 배치 (기존 10k 는 보존)

사용자 요청: "논문용 데이터셋에 오류가 있어 다운로드한 clean 4개로 교체". 실제로는
포맷이 달라 in-place 교체가 불가해, **신규 배치 + 기존 보존** 으로 진행(사용자 선택).
⚠ 기존 셋의 "오류" 가 무엇인지는 아직 사용자 확인 전 — 확인되면 여기에 추가할 것.

### 소스
```
~/Downloads/pallet6d_v2_clean_10k_part1~4.zip   각 6.8G (압축 27G)
zip 내부 경로가 data/pallet/... 로 적혀 있어 repo 루트에서 그대로 해제
part 별 프레임 범위  1:f0000-9999  2:f10000-19999  3:f20000-29999  4:f30000-39999
```

### 배치 결과 (신규, 덮어쓴 파일 0)
```
data/pallet/runs/diagnostics/v2_prod40k_clean_merged/   29G
  rgb/           f{n}_rgb.png      40,000
  labels/        f{n}_label.json   40,000
  mask_visible/  f{n}.png          40,000
  mask_amodal/   f{n}.png          40,000
  records.jsonl                    40,000 lines
data/pallet/release/attribution/ATTRIBUTION.md          (신규, 기존에 없던 파일)
```
검증 [확인]: 4개 서브폴더의 프레임 ID 집합이 f00000~f39999 와 완전 일치
(누락 0, 여분 0). `unzip -n` 사용이라 기존 파일 덮어쓰기 없음. 디스크 41% (1.1T 여유).

### 기존 셋과의 차이 — in-place 교체가 안 되는 이유
```
                   기존 pallet6d_v2_10k (17G, 보존)     새 v2_prod40k_clean_merged (29G)
프레임              20,000 (run1 10k + run2 10k)        40,000
레이아웃            all/000000.png + .json / _shards/    rgb/ labels/ mask_visible/ mask_amodal/
인덱스              index.csv (17 컬럼)                  records.jsonl
shard 이름          v2_prod10k2_s810x / v2_prod10k3_s820x   (merged, shard 구분 없음)
objects[0] 키       ... safety_gates                     ... safety_gates, pnp_conditioning ★추가
mask                all/ 에는 없음(_shards 에만)          프레임별 visible/amodal 둘 다 동봉
```
`pnp_conditioning` 이 새로 생긴 키다. 08-12 기록의 "`pallet6d_v2_10k` 는 mask_rle/
mask_bbox/visibility/segmentation 0건" 지적(§0)은 새 셋에서 mask 동봉으로 해소된 것으로
보이나, 그 자체가 사용자가 말한 "오류" 인지는 미확인 [추정].

### 후속 (아직 안 함)
기존 셋을 하드코딩으로 참조하는 스크립트 — 새 셋으로 돌리려면 경로 + 파일명 규칙 둘 다 수정 필요:
```
scripts/stage0/edge_mandatory_fast_search.py:30      DATA = .../pallet6d_v2_10k
scripts/stage0/line_feature_capacity_v2.py:188       DATA = .../pallet6d_v2_10k
scripts/stage0/r1c_fusion_capacity.py:28             DATA = .../pallet6d_v2_10k
scripts/stage0/partial_edge_truncation_screen.py:5   (docstring 언급)
```

### 정리 — 기존 셋 삭제 (사용자 지시)
```
삭제  data/pallet/training_data/pallet6d_v2_10k   17G   [확인] ls → No such file
      (Claude 의 rm 은 권한 거부되어 사용자가 직접 실행)
잔존  data/pallet/training_data/pallet6d_v2/PURPOSE.md   8K  (건드리지 않음)
잔존  ~/Downloads/pallet6d_v2_clean_10k_part1~4.zip     27G  (삭제 대기)
디스크  709G → 693G
```
★삭제로 아래 스크립트 3개의 DATA 경로가 **존재하지 않는 경로**가 됐다. 새 셋
(`data/pallet/runs/diagnostics/v2_prod40k_clean_merged/`, rgb/labels 레이아웃)으로
전환하려면 경로 + 파일명 규칙을 함께 고쳐야 한다. 이번엔 손대지 않았다.
```
scripts/stage0/edge_mandatory_fast_search.py:30
scripts/stage0/line_feature_capacity_v2.py:188
scripts/stage0/r1c_fusion_capacity.py:28
```

zip 4개도 사용자가 직접 삭제 완료 [확인]. 최종 상태:
```
data/pallet/runs/diagnostics/v2_prod40k_clean_merged   29G   유일한 v2 논문용 셋
디스크  709G → 666G (43G 회수: 기존 셋 17G + zip 27G)
```

### ★정정 — 사용자가 교체하려던 대상은 `paper_release` 였다
위에서 지운 `training_data/pallet6d_v2_10k` 는 그 압축본의 해제 사본이었고,
진짜 교체 대상은 논문 릴리스 정본 폴더였다.
```
data/pallet/training_data/paper_release/
  전     pallet6d_v2_10k_run1.zip  6.8G  subsets v2_prod10k2_s8101~8105
        pallet6d_v2_10k_run2.zip  6.8G  subsets v2_prod10k3_s8201~8205
        → 삭제한 pallet6d_v2_10k/_shards/run1,run2 와 shard 이름 완전 일치 [확인]
  후     v2_prod40k_clean_merged/  29G   (mv 로 이동, 재압축 안 함 — 사용자 선택)
        README.md / SHA256SUMS 갱신
```
봉인 해제: 폴더가 `dr-xr-xr-x`(555), zip 이 444 였다. `chmod u+w` 로 풀어서 교체했고
**다시 잠그지 않았다** — 이제 내용물이 sealed archive 가 아니라 ingestion 코드가
직접 읽는 디렉터리이기 때문. README 에 그 사유를 적어 두었다.

무결성 [확인]: 이동 후 재검증 — rgb/labels/mask_visible/mask_amodal 각 40,000,
프레임 id 집합 = f0..f39999 (누락 0/여분 0). `sha256sum -c SHA256SUMS` → OK.
zip 이 아니라 디렉터리라 per-file 체크섬은 없고 `records.jsonl` 하나만 걸어 두었다.

디스크 709G → 653G (56G 회수: 해제사본 17G + 다운로드 zip 27G + 구본 릴리스 zip 14G).
★새 셋은 이제 이 머신의 **유일본**이다 (다운로드 zip 4개 삭제됨, `data/` 는 .gitignore).

## challenge/data 재편 — Phase 0 (안전망, 이동 없음)

사용자 요청: "찍은 것 / 합성한 것" 으로 폴더를 나누고 **하드코딩 경로를 풀어달라**.
실측 제약이 커서 4단계로 쪼갰고, 이번 턴은 Phase 0 만.
```
제약  challenge/data 참조 = 92 파일 / 272 라인
      정본 평가셋 3개가 이 폴더 안 (test_eval_set_canonical.py 가 경로 강제)
      상위 5개(training 129 / cad 23 / pallet07 23 / noapril 21 / outside 18)
      = 214/272 = 79%  ← Phase 2 의 공략 지점
정정  "그냥 찍기만 한 것" 은 challenge/data 에 사실상 없다 [확인].
      촬영 폴더는 전부 png+json 쌍(=어노테이션 완료). json 0 인 8개는 raw 가
      아니라 추론 산출물. 라벨 없는 raw 는 data/pallet/real_unlabeled_ralph*
      (7개, 36M) 로 challenge/data 밖에 있다.
```

### 산출물 1 — 트리 스냅샷
`_docs/audits/2026-08-14_challenge_data_tree_snapshot.md` (83+ 줄)
최상위 항목별 size/files/json + 참조 집계 + 되돌리기 절차. `data/` 는 .gitignore
라 git 복구가 없어서 이 표가 유일한 복구 기준이다.

### 산출물 2 — 경로 레지스트리 `challenge/data_paths.py` (신규)
기존에 경로 중앙화 모듈이 **없었다** [확인]. 관행(`ROOT=parents[2]` + ROOT 상대
문자열)에 맞춰 신설. Phase 0 이라 값은 **재편 이전 경로 그대로**.
```
그룹     EVAL_CANONICAL(3) REAL_MANUAL_GT(21) REAL_PSEUDO_GT(9) REAL_MISC(2)
         SYNTHETIC(8) DERIVED(15) RESULTS(20)   합 78
python   from challenge.data_paths import EVAL_CANONICAL, get
         (__init__.py 없이 namespace package 로 import 됨 — 신설 안 함)
셸/yaml  python challenge/data_paths.py --get eval_cad [--abs] / --list / --check
```

### 검증 [확인]
```
--check                     78/78 present, exit 0
커버리지 역검사             디스크 최상위 71개 중 미등록 0
                            (등록만 있는 7개는 training/ 하위라 정상)
pytest test_eval_set_canonical.py   7 passed   ← Phase 1 이후 비교 baseline
```
빈 폴더 5개는 사용자 지시로 **보존**(레지스트리에 `_EMPTY` 접미어로 명시).

### 다음 — Phase 1 (승인 후)
01_real/{eval_canonical,manual_gt,pseudo_gt,augmented} · 02_synthetic · 03_derived ·
04_results 골격 생성 → mv → 옛 이름에 `ln -s` → data_paths.py 값 갱신 →
pytest 7 passed 재확인.

## challenge/data 재편 — Phase 1 (실제 이동 + symlink)

71개 최상위 항목을 4구획으로 `mv` 하고 옛 이름 자리에 **상대 symlink** 를 남겼다.
매핑은 스크립트로 일괄 처리(손으로 71개 옮기면 실수) — DRY RUN 으로 커버리지
100% 확인 후 `--apply`.
```
01_real/eval_canonical   3     정본 56장
01_real/manual_gt       21     촬영+손어노 (빈 폴더 4개 보존)
01_real/pseudo_gt        9
01_real/augmented        1     + _live_captures 는 01_real 直下
02_synthetic             1     training/ 통째 (37G)
03_derived              15     truncation_crops · yolo_pose · _train_* (53G)
04_results              20     추론 산출물 + mp4 9개
                        71
```

### 검증 [확인]
```
파일 수      before 398,116 → after 398,116   diff 0
깨진 링크    0 / 71
레지스트리   --check 78/78 present, exit 0 (새 경로 기준)
옛 경로      capturepalletcad_manual_gt · training/v1 · yolo_pose ·
             holdout_stems.txt 모두 exists=True (링크 경유)
pytest       943 passed / 3 failed / 1 skipped  ← 이동 전과 동일
```
★실패 3건은 **이번 이동과 무관**하다. 전부 `test_line_capacity_v2.py` 이고 원인은
`data/pallet/training_data/pallet6d_v2_10k/all/000005.json` 부재 — 이 세션 앞부분에서
삭제한 그 폴더다. challenge/data 와 관계없음. 해당 스크립트
(`scripts/stage0/line_feature_capacity_v2.py:188`)를 새 40k 셋으로 전환해야 풀린다.

★conda env 함정: base python 에는 cv2 가 없어 15개가 collection error 로 뜬다.
`conda activate pallet-pose` 후 실행해야 실제 결과가 나온다.

### 산출물
```
challenge/data/INDEX.md          신규 — 4구획 지도 + data_paths 사용법 + 되돌리기
challenge/data_paths.py          71개 경로를 새 위치로 갱신, docstring 현행화
_docs/audits/2026-08-14_challenge_data_tree_snapshot.md   (Phase 0)
```

### 다음 — Phase 2 (하드코딩 풀기)
참조 상위 5개(training 129 / cad 23 / pallet07 23 / noapril 21 / outside 18 =
214/272 = 79%)부터 `data_paths` import 로 전환. 전환마다 pytest 재실행.

## challenge/data 재편 — Phase 2 (하드코딩 풀기)

### ★계획 수정 — 전부 고치지 않았다
재집계하니 "272 라인" 에 문서가 섞여 있었고, 전환 실익이 균등하지 않았다.
```
py 206라인/76파일 · sh 49/7 · yaml 25/10 · md 166/45(문서, 대상 아님)
py 76개 소재지  stage0 24 · _legacy 21 · challenge/scripts 19 ·
                data_prep/validate 4 · tests 3 · 기타 5
```
`_legacy` 21개와 `scripts/stage0` 의 종결 실험 24개는 **의도적으로 제외**했다 —
결과가 이미 문서화된 기록물이라 손대면 과거 실험의 재현성이 흔들린다. 이것들은
Phase 1 의 symlink 로 계속 동작한다. 참조 대부분이 docstring 사용예·argparse
default 여서, 실익은 정본 평가셋 한 곳에 몰려 있었다.

### ★불일치 발견 — 정본은 56장이 아니라 161장
```
CLAUDE.md / memory / 내 Phase 0 레지스트리   56장, 폴더 3개
실제 (test_eval_set_canonical.py)            161장, 폴더 7개
차이  2026-08-07/08-08 에 lock §1.6 final-test 세션 4개
      (pallet07 27 · pallet09 36 · night08 17 · night09 25)를
      봉인 해제해 정본에 편입 → CLAUDE.md·memory 가 한 세대 뒤처짐
```
Phase 0 에서 낡은 문서를 근거로 만든 `EVAL_CANONICAL`(3개)이 **틀렸다**. 7개로
바로잡고 실측으로 확인했다: 폴더별 22/12/22/27/36/17/25 = **161, 선언값과 일치**.
`FINAL_TEST` · `FILTER_VAL_SESSIONS` · `FORBIDDEN_EVAL_SOURCES` 도 레지스트리로 이관.
→ CLAUDE.md 와 memory 의 "56장" 은 아직 안 고쳤다. 사용자 확인 후 갱신할 것.

### 전환한 것
```
challenge/tests/test_eval_set_canonical.py   EVAL_FOLDERS/FINAL_TEST_FOLDERS 를
    data_paths 에서 유도. 프레임 수(_EXPECTED_COUNTS)만 테스트가 따로 보유 —
    경로가 맞아도 내용이 바뀌면 잡아야 하므로.
_docs/EVAL_SET_CANONICAL.md                  표의 7개 경로를 새 위치로 갱신
    (테스트가 "문서에 폴더명이 있는가" 를 검사해서 실제로 잡아냈다)
challenge/scripts/*.py  8개                  augment_dataset · run_live ·
    gen_truncation_crops · make_apriltag_gt · restore_orig ·
    convert_to_camera_facing_v4 · visualize_v4_preview · _repnp_with_new_dims
```

### 검증 [확인]
```
--check                     82/82 present
정본 실측                   7폴더 split==eval 합 161 = 선언값 (전수)
pytest test_eval_set_canonical.py   7 passed
pytest challenge/tests/     943 passed / 3 failed / 1 skipped  ← Phase 1 과 동일
--help 스모크               전환 7개 전부 OK
```
★삽입 결함 1건: 보일러플레이트를 "마지막 import 줄 다음" 에 넣는 로직이
`from apriltag_gt import (` 같은 **여러 줄 import 의 괄호 안**에 넣어 make_apriltag_gt
가 SyntaxError. 위치를 옮겨 수정. --help 스모크가 없었으면 못 잡았다.
★`_repnp_with_new_dims.py` 는 --help 가 실패하지만 **원래부터 깨져 있다** —
`annotate.py` 에 `make_pallet_keypoints_3d_diagram` 이 없다(HEAD 시점 코드로 확인).
내 변경과 무관.

### 다음 — Phase 3 (선택)
전환 끝난 경로의 symlink 제거. 다만 stage0/_legacy 가 계속 옛 경로를 쓰므로
당분간 링크 유지가 맞다. 급하지 않음.

## 파일 정리 — symlink 제거 + 루트/challenge 최상위 (사용자 지시 3건)

### 1. challenge/data — symlink 71개 제거, 트리 5개 항목으로
링크를 없애려면 옛 경로를 쓰는 참조를 전부 새 경로로 바꿔야 했다.
```
1차 정적 치환   py/sh/yaml/md  116파일 357건   (history/audits 는 과거 기록이라 제외)
2차 정규식      9파일 21건 — 1차의 가드 조건("이미 새 경로가 파일에 있으면 스킵")이
                같은 파일의 짧은 이름을 건너뛰는 결함. yolo_pose_manual 이 치환된
                파일에서 yolo_pose 가 누락됐다. negative lookahead 로 재작성.
★f-string      challenge/data/capturepallet0{i}_manual_gt 를 9파일 18곳에서 발견.
                정적 치환이 못 잡고 링크가 없으면 glob 0건 = 조용한 실패.
★glob          capture*_manual_gt 가 재편으로 두 구획(eval_canonical/manual_gt)에
                갈려 한 패턴으로 못 잡는다 → gen_truncation_crops 는 두 줄로 분리.
★json          결과 manifest 12개에도 경로가 있었다(첫 검사가 *.json 을 빠뜨림).
                membership_sha256 은 "|".join(sorted(frame_id)) 라 경로 무관 —
                코드로 확인 후 갱신, assert 로 해시 불변 검증.
```
`face_centers.json` 16,374건은 **고치지 않았다**. `source` 는 생성 시점 출처 메모일
뿐 이를 읽어 파일을 여는 코드가 없다(stage15_gen_face_centers.py:204 가
os.path.relpath 로 기록만). 동작 무관.

### 2. repo 루트 — 옮길 것이 없었다 [확인]
```
metric_split_lock.md · paper_strategy_master.md   memory 가 "repo 루트 canonical" 로 명시
session_inventory_v2.py   _docs/_docs_todo.md:17 "repo 루트 배치(2026-06-08)" 결정 기록
split_lock.py             위 3개와 한 세트
```
전부 의도된 배치라 이동하면 memory·문서와 어긋난다. 대신 `runs/` 를 .gitignore 에
추가했다(output·logs 는 이미 등재, runs 만 누락).

### 3. challenge/ 최상위
```
yolo26n-pose.pt · yolo26n.pt · yolov8n-pose.pt  →  weights/pretrained_yolo/
   (코드 참조 0건 확인 — ultralytics 자동 다운로드분)
리프터_한글.pdf                                  →  _docs/reference/
리프터_한글 (1).pdf                              삭제 대기 (md5 동일한 완전 중복)
__pycache__                                      삭제 대기
   ※ rm 권한이 거부되어 사용자 직접 실행 필요
```

### 검증 [확인]
```
challenge/data 트리   01_real 02_synthetic 03_derived 04_results INDEX.md  (링크 0)
파일 수               398,116 → 398,117 (INDEX.md 증가분, 손실 0)
옛 경로 잔존          실경로성 0건 (face_centers sidecar 제외)
레지스트리            --check 82/82 present
manifest 실재         3개 학습 manifest 각 표본 15개 부재 0
                      ※ 경로에 백슬래시 = Windows 생성분이라 정규화 후 검사
pytest                943 passed / 3 failed / 1 skipped  ← 재편 전과 동일
```
중간에 test_threshold_audit 1건이 추가 실패했으나 eval56_manifest.json 갱신으로 해소.
최종 3건은 여전히 `pallet6d_v2_10k` 삭제 건으로 이번 작업과 무관.

문서 3곳의 "symlink 로 남아 있다" 문장을 "제거했다" 로 정정
(INDEX.md · data_paths.py docstring · EVAL_SET_CANONICAL.md).

## 데이터셋 카드 — 이미지 없이 GitHub 에서 내용 파악

요청: 이미지를 못 올리니 어떤 데이터가 있는지 md 로 남겨 GitHub 에서 보이게.

### ★선결 문제 — 카드도 ignore 되고 있었다
`.gitignore` 가 `data/`·`challenge/data/` 를 통째로 제외해서, 앞서 만든
`challenge/data/INDEX.md` 조차 **GitHub 에 안 올라가고 있었다** [확인].
폴더를 통째 제외하면 하위 예외가 안 먹으므로 패턴을 바꿨다(기존 관행
`challenge/model/*` + `!challenge/model/README.md` 와 같은 방식).
```
data/**  +  !data/**/  +  !data/**/*.md
challenge/data/**  +  !challenge/data/**/  +  !challenge/data/**/*.md
```
★부작용 1건 발견·수정: `data/` 는 슬래시가 끝에만 있어 **모든 깊이**의 data 폴더를
잡지만 `data/**` 는 **루트 기준만** 잡는다. 그래서 깊은 data 폴더의 ignore 가 풀렸다.
```
challenge/pallet_jetson_deploy/data/   forklift_sample.mp4 16.5MB → 명시적으로 재차단
_docs/experiments/data/                실험 문서 md 3개 → 원래 의도치 않게 무시되던
                                       문서라 그대로 열어둠(개선)
```

### 산출물
```
scripts/data_prep/validate/gen_dataset_cards.py   신규. 재실행 가능
  DATASET_GLOBS 로 "데이터셋 단위" 를 정하고 하위를 재귀 집계.
  ※ 처음엔 이미지 담은 폴더마다 카드를 만들어 146장이 나왔다(overlays/,
    images/train/ 까지). 단위를 올려 96장으로.
  프레임 수·용량 = 전수. split/gt_source/해상도 = JSON 을 열어야 해서 400개
  표본 상한, 카드에 표본 크기 명시(표본 비율을 전수로 읽으면 안 되므로).
  PNG 해상도는 헤더 24바이트만 읽어 디코딩 없이 구한다.

DATASET.md × 96      각 데이터셋 폴더
_docs/DATASETS.md    전체 요약표 (구획별 그룹, 이미지 수 내림차순)
```

### 검증 [확인]
```
총계          96 데이터셋 · 이미지 659,127장 · 222.9G
git 인식      디스크 96개 = git 추적가능 96개
이미지 유출   png/jpg/mp4/pth/pt 추적 0건
카드 정확성   capturepalletcad_manual_gt = split eval 22 / train 12 / none 10,
              gt_source 전부 manual → 정본 22장과 일치
버그 1건      list 형 사이드카 JSON 에서 AttributeError → isinstance 가드 추가
```
raw_data 도 포함해서, 앞서 "challenge/data 에 없다" 고 한 **라벨 없는 순수 촬영본**
(raw_data/outside 20,733장 · night 18,268장 등)도 이제 카드로 드러난다.

## 미해결 2건 마무리 — 정본 161장 정정 + line-capacity 테스트 처리

### 1. line_capacity 테스트 3건 — 전환이 아니라 skip 이 옳았다
실패 3건(`test_coverage_is_geometry_only_...` / `test_every_role_...` /
`test_off_frame_roles_...`)은 전부 `v2.coverage_full(dev[:N])` 로 실제 프레임을 읽어
삭제된 `pallet6d_v2_10k` 를 찾고 있었다.

**새 40k 셋으로 갈아끼우지 않았다.** 이 파일의 docstring 이 스스로 밝히듯 목적은
"a screen whose population moves is not a screen" 를 핀으로 박는 것인데, 다른
population 으로 돌리면 같은 스크린의 재현이 아니라 **다른 스크린**이 된다.
게다가 line-capacity 계열은 이미 종결(LOCAL_EDGE_REPRESENTATION_PRECISION_FAIL,
SLQ NOT BUILT)이라 재실행할 이유도 없다.
```
challenge/tests/test_line_capacity_v2.py
  + require_dataset(v2)   v2.DATA 부재 시 사유를 적어 skip
    (이 파일에 이미 pytest.skip 관행 2곳 존재 — 일관)
결과  39 passed / 3 skipped  (실패 → 사유 명시 skip)
```

### 2. 정본 "56장" → 161장 정정
Phase 2 에서 발견한 불일치를 문서·메모리까지 반영했다.
```
CLAUDE.md   §평가셋 표를 7폴더 161장으로 교체 + "56장은 폐기 수치" 경고 추가
            + final-test 4개의 튜닝 금지·분리 보고 규칙 명시
            §Compaction Rules 의 "56장 + wood45" → "161장(7폴더) + wood45"
memory      eval-set-canonical-objects-split-eval.md 전면 갱신
            (경로를 data_paths import 로, membership hash 가 경로 무관임 추가)
            MEMORY.md 인덱스 헤더 2줄 갱신/추가
신규 memory challenge-data-reorg-2026-08-14.md
            4구획 구조 · 옛 경로 제거 · data_paths 사용법 ·
            ★정적 치환으로 못 잡는 참조 3종(f-string/glob/json)을 기록
```

### 검증 [확인]
```
161 일치     디스크 실측 161 = data_paths 선언 161 = 테스트 EXPECTED_TOTAL 161
             = CLAUDE.md = MEMORY.md   (5곳)
pytest       943 passed / 4 skipped / 0 failed
             ← 이 세션 들어와 처음으로 실패 0
```

---

## 세션 3 — YOLO26 pose 학습 환경 batch 조사 (측정만, 학습 없음)

### 배경
과제용 YOLO26 pose 파이프라인(`challenge/yolo_pose_task/`, 미생성) 착수 전
"이 PC 가 batch 몇까지 되는가"를 확인. 파이프라인 자체는 아직 만들지 않았다.

### 하드웨어 [확인]
```
GPU        RTX 3080  9.62 GiB (nvidia-smi 10240MiB, torch 9.62 GiB)
상시 점유  841 MiB (Xorg + gnome-shell + rustdesk) → 가용 ~8.8 GiB
CPU/RAM    16 core / 31 GB
```

### conda env 가 두 개로 갈려 있음 ★ [확인]
```
pallet-pose     ultralytics 8.0.120  torch 2.1.1+cu118   ← YOLO26 미지원
pallet-yolo26   ultralytics 8.4.60   torch 2.1.1+cu118   ← YOLO26 학습은 여기서
```
`pallet-pose` 는 CLAUDE.md 에 적힌 기본 env 지만 YOLO26 을 못 쓴다.
YOLO26 작업은 반드시 `conda activate pallet-yolo26`.

### 파일 변경
```
추가  challenge/weights/pretrained_yolo/yolo26s-pose.pt  (23 MB)
      Ultralytics assets v8.4.0 에서 자동 다운로드됨. repo root 에 떨어진 것을
      기존 pretrained_yolo/ 로 이동 (루트 방치 금지).
      아키텍처 pilot 후보 C(yolo26s-pose@640) 에 필요.
```
그 외 repo 파일 변경 없음. 측정 스크립트·산출물은 전부 scratchpad 에만 두었다.

### 측정 결과
autobatch(`batch=-1`) probe — `ultralytics.utils.autobatch.check_train_batch_size`,
fraction=0.60:
```
후보                      제안 batch   비고
─────────────────────────────────────────────────────────────
A  yolo26n-pose  640        146       ★신뢰 불가
B  yolo26n-pose  768         70       ★신뢰 불가
C  yolo26s-pose  640         70       ★신뢰 불가
```
★ 세 run 모두 프로파일 표의 `backward (ms)` 가 전부 `nan` = backward 프로파일 실패.
   backward 활성화 메모리가 반영되지 않은 과대추정으로 보인다 [추정].
   실측 batch=16 이 2.71 GiB 인데 146 을 선형 외삽하면 20 GiB 를 넘어 GPU 용량과 모순.
   → **autobatch 제안값을 그대로 쓰지 말 것.**

실측 (실제 `YOLO.train()`, epochs=1, 256 train / 64 val, 산출물 폐기).
후보 A(yolo26n-pose @640) 기준:
```
batch   상태    peak VRAM   가용 8.8 GiB 대비   실행자/시각
──────────────────────────────────────────────────────────────────
16      OK      2.71 GiB    31%                Claude 16:37
32      OK      5.25 GiB    60%                Claude 16:49 (재측정)
48      미측정   ~7.8 GiB    89%   [추정]
64      HANG    —           ~117% [추정]        사용자 16:38, 9분42초 진행 0
```
**batch 32 는 된다. 64 는 안 된다.**

★ batch 64 의 실패 양상이 특이하다 — OOM 예외를 던지지 않고 **hang** 했다.
```
증상   9분 42초 경과, args.yaml 만 생성(weights 폴더 비어 있음)
       GPU 8,890 MiB 점유 · utilization 5% · CPU 0.9% · WCHAN futex_wait_queue
       프로세스 30여 개(부모 + dataloader worker)가 살아 있으나 진행 0
정리   ps 로 PID 수집 후 kill → GPU 9,716 MiB → 806 MiB 로 해제
```
16→32 가 2.71→5.25 로 거의 정확히 선형이므로 64 는 약 10.3 GiB 가 필요하고
가용 8.8 GiB 를 넘는다. hang 관측과 수치가 일치한다 [확인: 수치 일치 /
추정: OOM 대신 hang 이 된 정확한 기전은 미규명].

★ 이 실측으로 autobatch 제안값 146 이 틀렸음이 확증됐다 — 같은 기울기로는 약 24 GiB.

★ 측정 함정 1건: 사용자가 띄운 b64 프로세스가 GPU 를 8.9GB 점유한 상태에서
   batch 32 를 측정해 `CUDA error: out of memory` 가 났다(1초, peak 0.00 GiB).
   **batch 32 의 한계가 아니라 GPU 경합**이었다. 측정 전 `nvidia-smi` 로
   GPU 가 비었는지 확인할 것 (08-07 의 self_train 점유 OOM 과 같은 유형).

### 결정 — batch 는 미확정 (16 또는 32)
| 값 | 근거 |
| 16 | 기존 `challenge/yolo_pose/scripts/ft_*.sh` 5개 실적 · lr 스케일 항등 · 여유 6.1 GiB |
| 32 | 실측 OK · 여유 3.6 GiB · lr0 2배 필요 |

lr (`lr0 = base * batch / 16`):
```
batch    Stage A   Stage B   Stage C
────────────────────────────────────
16       0.005     0.001     0.0001
32       0.010     0.002     0.0002
```
★ 프롬프트 batch 규칙 6번: batch 를 키우고 epochs 를 그대로 두면 optimizer step 이
절반이 되므로 **같은 학습량이 아니다.** 32 를 쓰면 보고서에 step 수를 명시할 것.

### 미해결 / 다음 세션 주의
- 후보 B(768) · C(yolo26s) 의 batch 상한 **미측정**. A 기준 선형모델을 그대로 쓰면 안 됨
  (B 는 픽셀 1.44배, C 는 파라미터 3.2배).
- probe 측정은 mosaic=0.5 · workers=8 · 256장 조건. Stage B/C 의 blur/noise 증강 미반영.
- 측정용 mini 셋은 `yolo_pose_padded` 의 **v1 유래 파일만** 링크(v8 회피). 품질 판단에 쓰지 말 것.
- scratchpad 보존물: `probe_runs/b32_user_1638`(사용자 완주분) ·
  `probe_runs/b64_user_1638_HANG`(hang 증거). 세션 종료 시 사라진다.

## 코드·산출물 폴더 정리 — Phase A·B (C·D 는 미실행)

### 사전 조사로 확정한 제약
`scripts/stage0` 177 py + sh 19 = 196개가 최대 집중. 데이터 재편처럼 문자열 치환으로
끝나지 않는다 — **모듈 해석**이 걸린다.
```
테스트 30개가 ROOT/"scripts/stage0/<name>.py" 를 하드코딩해 직접 로드
stage0 62개가 sys.path.insert(ROOT/"scripts/stage0") 후 서로 import
import 대상 모듈 32개는 어디 있든 항상 path 에 있어야 한다
```
→ 계열별 폴더 분할(Phase D)은 이 셋을 다 건드려야 하고 검증 수단이 빈약해 보류.
사용자와 A·B 만 합의.

### Phase A — output/ → data/pallet/results/_pl_experiments/
```
7.8G / 22,415 파일. 전부 pseudo-label 실험 산출물(pl_* · filter_ablation_* · f1~f3_*)
.pth 12개는 각 실험 폴더 안의 체크포인트라 분리하지 않고 폴더째 이동
같은 파티션이라 mv 즉시. 대조: 22,415 = 22,415 파일, 8,325,414,891 = 동일 bytes
참조 갱신 2파일 — plot_pl_pool.py · plot_pl_dist_per_seq.py (경로 5개 전부 실재 확인)
```

### Phase B — 참조 없는 26개를 scripts/stage0/_archive/ 로
```
196 → 170 (루트) + 26 (_archive)
```
★**분석 구멍 2개를 실행 중에 발견해 메웠다.**
```
1) 계획 때 *.py 만 세서 .sh 19개가 빠졌다 → 재산출 33 → 38
2) "참조되는가" 만 보고 "이들이 남는 파일을 참조하는가" 를 안 봤다.
   한 단계 깊어지면 자기위치 기준 계산이 전부 밀린다:
     py  parents[N] 2개 / HERE+"/../.." 4개 / sys.path.insert(0,HERE) 후 형제 import 4개
     sh  cd "$(dirname "$0")/../.." 4개   ← py 만 검사하다 놓칠 뻔했다
   → 38 중 12개 제외, 26개만 이동. 절대 ROOT·repo루트상대 경로는 위치 무관이라 안전.
```

### 검증 [확인]
```
문법        26개 py_compile / bash -n 전부 OK
참조 잔존   남은 코드가 이동 파일을 가리키는 것 0건
pytest      943 passed / 4 skipped / 0 failed  (이동 전과 동일)
문서        scripts/stage0/_archive/README.md — 판정 기준과 제외 목록 기록
```
Phase C(공용 모듈 _lib 분리) · D(계열별 폴더)는 하지 않았다. C 없이 D 를 하면
ImportError 가 쏟아지므로 순서는 반드시 C → D.

## Phase C 취소 · D 를 보정과 함께 실행

### ★C 는 전제가 틀려 실행하지 않았다
"공용 모듈 32개를 _lib/ 로" 였는데 실측하니 **32개 중 31개가 `__main__` 을 가진
실행 스크립트**였다. 라이브러리가 아니라 실행 스크립트가 서로를 재사용하는 구조라
_lib 라는 이름도 실체도 맞지 않고, 옮기면 실행 경로만 깨진다.
D 의 진짜 전제는 "모듈 모으기" 가 아니라 **위치 의존 코드의 depth 보정**이었고,
그것은 이동하는 순간에 같이 해야 한다 → C 를 독립 단계로 둔 것 자체가 잘못된 분할.

### D 도 원안대로는 불가능했다
import 의존을 그래프로 보니 **159개 중 88개가 하나의 연결 덩어리**(55%).
sys.path 에 stage0 루트만 들어가므로 흩으면 서로를 못 찾는다.
→ 계열별 8폴더 원안 폐기. 그래프상 고립된 69개 중 접두어가 뚜렷한 46개만 이동.
```
stage0 루트 170 → 124
  line/ 17 · paper_s2/ 19 · stage_screens/ 7 · diffpnp3d/ 3   (+ _archive/ 26)
총계 대조 196 = 196 (손실 0)
```

### ★사고 3건 — 전부 "참조 형태가 하나가 아니다"
```
1) 중첩 괄호   os.path.join(os.path.dirname(__file__), "..", "..")
               정규식 [^)]*? 가 첫 ')' 에서 멈춰 7개 미보정 → ROOT 가 /scripts 를 가리킴
               ★ROOT 검산 하네스가 잡았다. 없었으면 조용히 깨졌다
2) 동적 로드   _load("이름", "scripts/stage0/x.py") 45건
               import 문만 본 그래프 분석에 안 잡혀 "고립" 판정이 부정확했다
3) 변수 경유   STAGE0 / "x.py" 10건 → 20 failed / 50 errors
               문자열·Path조립·변수경유·동적로드 네 형태를 다 봐야 했다
```

### 검증 [확인]
```
ROOT 검산    이동 46개 중 45 정확 / 1 ROOT없음 / 틀림 0
             (파일을 실행하지 않고 ROOT 대입식만 평가 — 학습 스크립트라 실행 불가)
문법         46개 py_compile 전부 OK
옛 경로      네 형태 전수 grep 후 잔존 0
pytest       943 passed / 4 skipped / 0 failed
문서         scripts/stage0/README.md — 구조 + 이동 시 확인할 네 가지 참조 형태
```

## OS 의존 하드코딩 정리 (A)

"다른 머신에서 안 도는" 문제를 훑었다. 폴더 정리보다 이쪽이 실제 고장이다.

### 실측
```
/home/minjae 절대경로를 박은 파일   95   (stage0 48 + _archive 26 + 기타)
Windows 경로가 섞인 활성 코드        4   conda/python 실행기
FoundationPose(다른 repo) 경로      32   yolo_pose 13 · _legacy 12 · 외부복사본 2 · 기타
conda 활성화 방식                    8종  제각각
```

### 고친 것
```
conda 활성화 6건   source /home/minjae/anaconda3/... 
                   → source "$(conda info --base)/etc/profile.d/conda.sh"
                   ★저장소 안에 이미 있던 모범 사례(run_pipeline.sh:50)를 따랐다
  scripts/ft_dope_cropaug.sh · ft_challenge_otftrunc.sh
  challenge/yolo_pose/scripts/ft_{cropaug,cropaug_v2,manual_nopad,holdout_ab}.sh

python 실행기 4건  PY="C:/Users/minjae/anaconda3/envs/pallet-pose/python.exe"
                   → PY="${PY:-python}"   (활성 conda env 사용, 환경변수로 덮어쓰기 가능)
  scripts/{eval,run}_loss_ablation_all.sh
  scripts/data_prep/eval/eval_{3,6d_3}_domains.sh   (기본값이 Windows 경로였다)

yolo_pose yaml 3건  C:/Users/.../FoundationPose/... → 이 저장소 절대경로
  ★내 실수 정정: 08-14 폴더 재편 치환이 이 yaml 들에도 03_derived 를 끼워 넣었는데
    그 경로는 다른 프로젝트(FoundationPose)의 Windows 위치라 그쪽이 깨졌을 수 있다.
    이 머신엔 데이터가 실재하므로 저장소 절대경로로 통일.
  ★"저장소 상대경로" 를 먼저 시도했으나 불가 [확인]: ultralytics 는
    `if not path.is_absolute(): path = (DATASETS_DIR / path).resolve()` 라
    상대경로를 DATASETS_DIR 기준으로 해석한다. repo 상대경로는 안 먹는다.
```

### 검증 [확인]
```
bash -n        수정 10개 전부 OK
yaml 경로      8개 파일의 path/train/val 전수 실재 확인, 부재 0
pytest         943 passed / 4 skipped / 0 failed
남은 하드코딩  source /home/minjae 0건
```

### 남겨 둔 것
```
stage0 등의 /home/minjae 절대 ROOT 79개   Linux 에서는 동작. 근본 해결(B)은 별건
challenge/yolo_pose 의 .bat 4개 + spawn/auto_restart *.py   Windows 전용 서브트랙
challenge/scripts/_legacy 12개 · 25y_automatic_lifter 복사본   손대지 않는 영역
challenge/weights/*/args.yaml   ultralytics 가 남긴 학습 기록이라 과거 그대로 둔다
```

### challenge/scripts 폴더 분할 — 가능하나 실익 작음
```
py 34 + sh 2 = 36 (_legacy 37 별도)
연결요소 17개: 13개 덩어리(annotate 계열) + 6개 덩어리 + 고립 15
stage0(88/159=55% 한 덩어리)보다 훨씬 덜 얽혀 나눌 수는 있다.
다만 36개면 한 폴더로도 읽히고, annotate 13개는 어차피 통째로 가야 해서 보류.
```

## stage0 ② — sys.path 부트스트랩으로 얽힌 덩어리까지 분할

앞서 "95개가 한 덩어리라 못 나눈다" 고 했는데, 그건 **sys.path 에 stage0 루트만
들어간다**는 전제 하의 결론이었다. 전제를 바꾸면 나눌 수 있다 — 각 파일이 stage0 의
모든 하위 폴더를 path 에 넣게 하면 어느 폴더에 있든 형제를 찾는다.
"쪼갤 수 없다" 와 "옮길 수 없다" 는 다르고, 내가 그 구분을 안 하고 답했었다.

```
루트 124 → 68
  paper_s2/ 34 · stage_screens/ 19 · line/ 17 · diag/ 13 · ralph/ 8
  diffpnp3d/ 5 · eval_harness/ 4 · paper_s1/ 2 · _archive/ 26
총계 196 = 196 (손실 0)
```

### ★사고 4건 — 매번 새로운 형태였다
```
1) 부트스트랩 삽입 위치   "마지막 import 다음" 에 넣었더니 여러 줄 import 괄호 안에
                          들어가 SyntaxError 2건. AST 로 end_lineno 를 봐야 했다.
2) 부트스트랩 실행 순서   형제 import 도 import 문이라 "마지막 import 뒤" 는 이미 늦다.
                          → docstring 직후로 재배치.
3) 참조 5번째 형태        from scripts.stage0 import x  (패키지 import)
                          문자열·조립·변수경유·동적로드에 이어 다섯 번째.
4) ★자기 해시 의존        paper_s2_mechanism_diagnostic 은 cache_key 에
                          script_sha256 = sha256(자기 파일) 을 넣는다. 옮기면 캐시가
                          영구 무효. frozen_diagnostic·frozen_post_analysis 도 동일.
                          → 3개를 루트로 되돌리고 원본과 byte 일치까지 확인.
                          찾는 법: grep "sha256_file(.*__file__\|script_sha256"
```
★D 단계(46개)로 옮긴 파일에는 부트스트랩이 없었다("고립" 판정이었으나 실제로는
동적 로드로 형제를 부른다). 하위 폴더 전체에 일관 적용해 45개 추가.

### 테스트 쪽 — conftest.py 신설
여러 테스트가 `import paper_s2_frozen_diagnostic` 처럼 모듈명으로 부르는데,
예전엔 어느 스크립트가 먼저 로드되며 sys.path 를 채워 주는 **부수효과에 얹혀** 있었다
(= 실행 순서 의존). 폴더를 나누자 깨졌고, conftest 로 확정했다.

### 검증 [확인]
```
문법·ROOT·부트스트랩   하위폴더 102개 전수 → 문제 0
총계                   196 = 196
pytest                 943 passed / 4 skipped / 0 failed
해시 보존              루트 복귀 3개, git diff 없음 = 캐시 유효
```
README 를 참조 5형태 + 해시 의존 파일 + 검산법으로 갱신.
