# 2026-07-04 — STAGE19 confidence/filter diagnostics + domain-mixup pilot

## PART A — 신뢰도/필터 진단 (B2 추론만, 학습 X)

새 스크립트:
- `scripts/stage0/stage19_partA_infer.py` — B2(stage11_16k_B2_maskaux/final_ep0084)
  reflect-pad(pad=100) 추론으로 프레임당 per-keypoint 신호 기록:
  belief peak(top1), peak2(NMS 2등), peakratio, flip-TTA per-index 불일치(px,
  reflect-pad 경로 un-flip+swap), diag_resid(centroid→SPACE_DIAG **point-to-line**
  정규화, 교점계산 금지), LOO SQPnP reproj(dims-known), channel-aligned GT err.
  프레임셋: filter-val(outside 39/night 29, SEAL) + manual 36→24 + cad 22→6 +
  noapril pool 188→87 (n_det>=6). 총 185 records (98 GT / 87 pool). final-test 봉인.
- `scripts/stage0/stage19_partA_analyze.py` — calibration/PR/funnel/GATE(CPU).

### 결과 (partA_summary.md / partA_calibration.json / partA_pr_curves.txt / partA_funnel.txt)

핵심 수치 (filter-val, channel-aligned err<10px, 소표본 주의):
```
scope   base_good%   peak>=0.95      peak>=0.9&flip<=10
FRONT      47.7       73%P/19%Y       72%P/29%Y   ← 채택 operating point
REAR       16.7       39%P/15%Y       (달성 불가)
ALL        31.9       58%P/17%Y
```

- **peak(신뢰도)는 정보 있음**: filter-val Spearman(peak↔err) all=-0.38, front=-0.44,
  rear=-0.44 (outside front -0.555). flipTTA=+0.24(약), loo=-0.20(약),
  diag_frame↔rear err night=0.40/outside=0.13.
- **REAR = confidently-wrong**: rear peak median 0.903 ≈ front 0.898 (분포 동일)
  인데 rear err 17.3px(good 18%) vs front 11.9px(good 38%). peak은 face **내부**
  에선 유효하나 front→rear err SHIFT는 못 가름 → 전역 peak gate는 rear 과소정제.
  처방 = peak per-face, FRONT PL 채택 / REAR spatial-mask.
- **Funnel (B2, diag_resid<=0.02 & flip<=8px)**: noapril det87→diag27→diag&flip5,
  outside 39→10, night 29→3 (옛 base paper_base_v2 clean=0 대비 non-zero). 단
  full-frame(8코너) precision 15.4%(13장) — rear가 끌어내림 → **FRONT-only PL이
  유일 경로**.

### ★ GATE A = A-PASS (FRONT-corners only; REAR = A-FAIL)
cond-i(FRONT combo 72%P/29%Y≥70/20) ✓ + cond-ii(peak |rho|=0.44≥0.3) ✓.
→ PART B real감독 = confidence-selected PL heatmap: FRONT{0,1,2,3} peak>=0.9&flip<=10
채택, REAR{4,5,6,7} real 팔레트 bbox만 spatial-mask, teacher=B2 고정, PL loss 낮게.
디스코드 1회 발송(GATE A).

## PART B — domain mixup 파일럿 (2-arm, A-PASS 분기)

새 스크립트:
- `scripts/stage0/stage19_partB_mixup_train.py` — DM-ADA core(도메인 선형보간+soft
  PL, adversarial 제외). B2 init, 동일 sim데이터/순서/스케줄(600 step, lr1e-4).
  sim=CleanVisiiDopeLoader(mixed_v8_train+addon+v3b0+aug_trunc, 19329장, bel 50x50
  full fidelity), real=RealPLDataset(PART A records noapril, front-accepted PL
  peak>=0.9&flip<=10, CreateBeliefMap 재사용, rear-suppress=all-corner hull mask).
  x̃=λ·sim+(1-λ)·real(λ~U(0.5,1)); accepted FRONT ch tgt=λ·H_sim+(1-λ)·pl_w·H_realPL
  (pl_w=0.3), 비채택/REAR ch tgt=λ·H_sim + real 팔레트 bbox 영역만 belief loss 공간
  마스킹(채널전체 마스킹 X). 2-iter smoke 통과(shape 8x9x50x50, NaN/OOM none), NAN-GUARD.
- `scripts/stage0/stage19_partB_eval.py` — paired eval(same-frame both-detect,
  filter-val+manual, reflect-pad, front/rear per-corner + honest full-8 SQPnP reproj).

real-PL 수율 = noapril 87 중 36장만 ≥1 accepted front(대부분 1-2 코너) = sparse.

### ★ 결과 (partB_pilot_paired.txt / partB_summary.md) — HONEST NEGATIVE + REGRESSION
paired n=80 (control vs mixup, 동일 프레임):
```
arm       front_med rear_med reproj_med good<10 gross>20
control     10.86    20.07     17.83      176     223
mixup       12.20    21.14     19.23      153     243   ← 전부 악화
V=8 mixup   12.20    21.10     19.23      150     233   ← full-view 동일 악화
```
- mixup이 front +1.34/rear +1.07/reproj +1.40px, good -23, gross +20 전부 악화.
  control≈B2(front 10.9 vs B2 11.9) = 깨끗한 continuation → 악화 원인=mixup blend.
- 해석(§5 적중): 픽셀 ghosting이 px-정밀 pose 해침 + appearance mixup은 rear depth-cue
  갭 못 풂(rear=confidently-wrong=기하문제, appearance 아님). sparse noisy front-PL은
  upside 적고 blend는 전역 noise 추가.
- **판정: soft 픽셀보간 접기. full 승격 X.** master(suppression+structural+
  co-primary dataset, naive PL/mixup 아님)와 정합. 레버=rear/저앙각 flat-view DATA +
  front-only confidence를 suppression 신호로.
- 디스코드 2회 발송(GATE A / PART B). 소표본 주의(paired n=80, front-PL 36장).


## STAGE21 front-only PL suppression 트랙 (추론만, STAGE19 records 재사용) — 판정 NEGATIVE(엔진), POSITIVE(설명/분석)
- front-only(peak≥.9∧flip≤10) vs 전체9kp PL precision/수율 (STAGE19 partA_records 185, B2):
```
domain         front acc  P    Y    all9kp P   STEP1 full-9kp frameP
outside        43/154    67%  28%   53%        10%
night          32/106    78%  30%   48%         0%
manual(heldout) 49/96    16%  51%   18%         0%  ★transfer 붕괴
filter-val     75/260    72%  29%   51%         6% (STEP1 src)
```
- ★핵심: calib(outside+night)선 front-only 진짜 이김(kp P 51→72%, rear 제거=STEP1 독 제거 확인). BUT unseen manual선 72→16% 붕괴. ordering 버그 아님[확인]: manual front median 13.2px≈outside 10.9px, rho -0.34(랭킹 정상)인데 절대 정확도 floor가 ~12.7px라 고정 10px gate가 거의 통과X. = "72%는 calib 낙관, gate는 상대선택자지 도메인전이 clean-PL 생산기 아님".
- R1 pool(noapril 87, GT無): front 채택 55코너/335, 프레임 36/87(≥1), ≥2 17장, ★all-4 완성 0장, 평균 0.63코너/프레임. 수율16%(calib 29의 절반)=manual regime. STEP1 452-PL 대비 극소.
- 판정: (설명○) front/rear split=깨끗한 실패모드 결과, face내 peak informative(rho-0.44~-0.55)/face간 shift 못가름→per-face gating(front채택/rear 공간마스크)이 옳은 suppression. master(suppression NOT selection)·thesis-framing 정합. (엔진✗) self-training PL 소스로는 NEGATIVE: ①수량(≤4코너/프레임, R1 완성포즈 0) ②전이(manual 16%, 채택 front≈base라 신호≈0) ③실증(PART B가 근사변종 이미 학습→REGRESSION). front-only는 STEP1의 설명(rear 오염)을 대체하지 결과(여전히 positive ST 없음)를 대체 못함.
- rear 트레이드: front{0,1,2,3} 감독/rear{4,5,6,7} 마스크→rear는 base(confidently-wrong ~17px). PnP/6D는 8코너 필요→front-only는 2D/detection 부분패치지 6D 해결 아님. corner01-premise·dimsfree 정합.
- 결론: 현 base에 front-only self-training 돌리지 말 것. 유효 레버는 여전히 rear/저앙각 flat-view DATA(STAGE17). 산출 data/pallet/eval_results/stage21_front_suppression/(front_pl_quality.json, analyze_front_pl.py). 소표본 예비.

## STAGE20 cut-paste 파일럿 (2-arm, B2 init 600step) — 판정 NEGATIVE+REGRESSION (appearance 경로 3연속 실패)
- 신규 scripts/stage0/stage20_cutpaste_{train,eval}.py. cutpaste=addon+v3 mask_rle 오림→real배경(noapril+c0403, 376장) 합성, 타깃=합성 GT 그대로.
- paired same-frame(both-detect n=66, filter-val+manual, 봉인):
```
arm       front_med rear_med reproj good<10 gross>20
control   11.63     19.94    19.43   145    179
cutpaste  34.41     41.22    16.48    56    373   ← 전부 파국
manual    14.7→61.3  night 15.9→25.2  outside 14.3→32.9 (도메인별 corner med)
```
- ★reproj_med 16.48<19.43은 red herring[확인]: reproj=pred↔PnP 자기일관성이지 GT정확도 아님(코너 GT서 30-60px 벗어나도 self-consistent면 낮음). 진실=corner median 파국+gross 373.
- 기전[추정]: (a)real 배경의 무라벨 real 팔레트→cut-paste가 "real팔레트 억제/붙인 합성 반응" false-negative 명시학습(STAGE19보다 억제 강함=완전무라벨) (b)붙인 합성 팔레트 날카로운 경계+도메인충돌 아티팩트. →"배경 appearance 돕는다" 깨끗이 분리 못함(§5 flag 적중).
- ★핵심: "ghosting 없애면 나아질 것" 가설 틀림 — ghosting 제거해도 cut-paste가 더 해로움. appearance 경로 3연속 실패(STAGE16 REJECT, STAGE19 mixup, STAGE20 cutpaste) → 병목=texture appearance 아니라 기하/label-policy 재강화. "rear 기하문제면 cut-paste도 못고침" 예상 negative 실측 확인.
- 한계: n=66 소표본, manual over-fire 편향. clean test엔 팔레트없는 real배경 필요하나 3연속 negative로 사전확률 낮음=재시도 저우선. 방향=rear/기하(partial-keypoint·label policy·flat-view 데이터), master 정합.
- 산출 data/pallet/eval_results/stage20_cutpaste/(cutpaste_paired.txt, cutpaste_eval.json, example 5장, diag_pred 오버레이), weights/stage20_cutpaste_pilot/{control,cutpaste}/.

## STAGE22 손 어노(cad22+noapril6) 재평가 — ★mixup 악화 불재현(셋 의존), cutpaste 재현+붕괴
- 사용자가 오늘 21:15 noapril 손라벨 6장 신규(`challenge/data/capture0403noapril_manual_gt/`, dims w1.3/h0.11/d1.1, reproj~1.2px) → cad22+noapril6=28장으로 STAGE19/20 모델 재평가. 스크립트 scripts/stage0/stage22_myannot_eval.py(eval_capturecad_b2 재사용, order-free+honest8+pad100+per-frame K).
- 인프라 검증: B2 on cad front14.8/honest27/det27%=과거 기록 정확 일치[확인].
- paired same-frame (Δ=treat−control, +=악화):
```
pair                  set      npair  Δfront  Δrear  Δhonest8
mixup vs control      cad       5     -1.2    0.0    -2.8   개선쪽
mixup vs control      noapril   3     -1.2   -1.0   +7.3   혼조(n=3)
cutpaste vs control   cad       5     +8.1   +0.3   +1.7   악화
cutpaste vs control   noapril   1    +249   +117   +172   붕괴(n=1)
```
- ★mixup: filter-val front+1.3 악화였으나 손어노 cad Δfront-1.2/Δhonest-2.8=중립~미세개선 → **"mixup 악화"는 filter-val 특정셋 아티팩트일 수 있음, 폐기 근거 셋의존**[확인,소표본]. 유지/추가검토 여지.
- ★cutpaste: 손어노서도 재현+증폭. cad Δfront+8.1, noapril(unseen) 완전붕괴 det83→17%(5/6 미검출), 남은1장 front260. 오버레이 검출실패 확인. → **부적합 확정, 탈락**(appearance aug로 도메인/geometry 못잡음, trackb 정합).
- front/rear: rear≥front 유지(cad s20c 9.3/14.1, noapril B2 11.2/13.5)=STAGE17 rear 병목 재확인[확인,소표본]. cutpaste 붕괴는 rear특이 아니라 front·rear·det 전부 무너뜨림=도메인 붕괴.
- base 천장 유지: 컨트롤군 finetune 후 front 미세개선(9-10px)이나 honest 제자리~악화(base-11px-ceiling 정합).
- ⚠소표본: noapril N=6·s20 paired n=1(개별Δ 단정불가, 단 absolute det17%=5/6 미검출은 체계적). cad paired n=5 방향신뢰 ±수치 예비. 산출 data/pallet/eval_results/stage22_myannot_eval/(myannot_paired.txt, eval.json, overlays 6).

## STAGE22 filter funnel — best 모델(B2)이 우리 필터 통과하나? → clean PL≈0 (GT 있는 손어노서 재확인)
- 질문: 현 best 예측이 배포 기하필터(diag/flip/loo) 통과=쓸만한 clean PL 만드나. cad22+noapril6(GT有→precision 계산가능).
- 통과 개수(B2, threshold 배포값):
```
필터              cad  noapril   clean(GT<10px)?
diag<0.02          0    0        —
diag<0.05          1    0        NOT clean
loo(ransac_loo)    0    0        ★전멸(flat EPnP 붕괴)
diag∧flip          0    0        청정 0
flip<=8(참고,단독X) 3    1        cad 2clean but honest16px / noapril 1clean but ★honest68px(corner8px인데 pose붕괴)
```
- 판정: 배포필터(diag/loo/diag∧flip)로 두 셋 모두 **clean PL≈0**, 6/18(옛base clean≈0)과 동일결론 GT셋서 재확인(dimsfree-2d-geometry 정합). flip 단독만 한줌 통과하나 deployed standalone 아님+model-dependent(mixup clean0%)+pose 붕괴(honest8 68px=flat depth 모호)라 신뢰불가. confidently-wrong(통과했는데 rear>20px) 존재.
- B2 vs mixup: mixup 검출 근소↑(cad7vs6)이나 통과분 품질 나쁨(cad detect clean 14%mixup vs 33%B2, flip통과 clean0%, conf-wrong2). → best=B2 유지, STAGE22 cad honest 미세우위는 clean-PL 관점서 역전.
- cad(극근접)=검출27% 자체가 병목, noapril(unseen)=검출되나 flip통과분 pose 붕괴(honest25-86px). 둘다 clean PL 실패 동일.
- ⚠N=28 극소, threshold 관례 고정(sweep 아님). 산출 scripts/stage0/stage22_filter_funnel.py, data/pallet/eval_results/stage22_myannot_eval/filter_funnel/.
- ★결론: best 모델도 우리 필터 못 통과 = self-training PL 소스로 부적합 재확인. 병목=base 코너/pose 정밀도(특히 rear/flat depth), 필터가 아니라 base가 천장.

## STAGE22 오버레이 edge 버그 수정
- 미어노 코너 sentinel [-1,-1](projected_cuboid null→sentinel)을 draw_cuboid가 nan만 skip하고 안 걸러 그 점으로 선 그림=X자 꼬임. nan+[-1,-1] 둘다 invalid 처리로 수정. cad_11/cad_17 검증. ★어노 정상, 그리기만 버그. 평가 수치는 점위치 기반이라 STAGE22 결론 불변. scripts/stage0/stage22_myannot_individual_overlays.py 수정, 28장 재생성.
