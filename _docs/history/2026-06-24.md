# 2026-06-24

## 트랙B 진단 1·2 — heatmap 실패 분해 (학습 X)
스크립트 `diagnose_failure_modes.py`(diag1), `diag2_raw_decode_stages.py`(diag2). base=challenge0123/ep60. real GT: filter-val outside44+night43 + manual36. front=0-3/back=4-7, depth pair (0,4)(1,5)(2,6)(3,7).

**diag1 (도메인×separation×ρ)**: separation 중앙값 outside53/night133/manual9px → night는 deep(flat 가설 기각). 검출분 ρ 0.7~1.1, 검출률 night 저조. ⚠ 검출분만 본 ρ=survivor bias(diag2가 정정).

**diag2 (미검출 단계 분해, raw top-1 threshold/grouping 우회)**:
```
단계분류    N   detected gate_postp corner_loc competing no_response
outside    44   68%      0%         11%        11%       9%
night      43   35%      0%         9%         14%       42% ★
manual     36   78%      0%         6%         6%        11%
raw정확도/ρ:  rawCornMed  ρ_rawAll  ρ_detOnly
  night        35.5       0.25      0.68   (검출분만 0.68=survivor bias, 전체 0.25)
  outside      18.7       0.77      0.83
threshold sweep night: τ=0서 cover100%·condAcc16%·e2e16%, τ낮춰도 e2e 16% 고정
```
- **diag2 확정**: night 저검출은 confidence threshold/postprocessing 아니라 **raw heatmap 단계의 no-response(42%)·competing-peak 증가**에서 발생. gate_postprocess=0%·threshold 제거해도 e2e 회복 안 됨 → **calibration 주원인 아님**. Oracle-C는 완전미검출 제외+부분미검출 코너 GT로 채움 → structured residual의 PnP영향은 보수적 측정(저검출 안 섞임, 내 이전 진술 정정).
- **별개 두 문제 동시존재**: A=night raw heatmap response 소실/오위치 / B=검출 코너 residual도 Gaussian보다 PnP 불리한 구조.
- ⚠ **약화(과결론 금지)**: (1) "geometry 배제"는 아님 — separation 하나로 geometry 다 통제 못 함. PnP가 night *미검출* 원인은 아니나, 전체 pose에서 PnP conditioning 문제 없다고 결론 못 냄. (2) ρ_rawAll=0.25를 "depth flatten 확정"으로 해석 금지 — no-response 프레임 argmax는 임의 배경이라 ρ 무효. → "실패 프레임 포함 raw 출력은 유효 front-back 구조 유지 못 함"이지 "모델이 깊이 보고 25% 압축"이 아님.
- **선도 가설**: night-associated appearance/sensor domain gap (조명 단독 인과는 미확정 — night에 거리/truncation/배경/ISP 등 동반). 다음=diag3 geometry-matched 비교 + 동일프레임 photometric intervention으로 검증.


## STEP8 center-conditioned offset 진단 (NVIDIA식, staged) — offset도 negative, 중단

목표 "작동되게". 외부 정밀 프로토콜(Oracle PnP + 32 overfit gate + 4096 teacher-forced probe). center-conditioned offset = 중심 heatmap peak에서 8 corner full-2D offset 회귀(★마스크 없음, support=center 주변 r2, output-map 정규화). STEP5(dense voting+마스크)·STEP7(sparse global-FC)와 다른 미시험 표현. base=challenge0123/ep60, stride8(50×50). 스크립트 `oracle_pnp_sweep.py`/`offset_overfit32.py`/`offset_probe4096.py`(--unfreeze).

**§0 STEP5 loss 점검**: STEP5 vec_loss = valid-mask 정규화(`(diff*mask).sum()/(mask_px*ch)`) = **정상**(zero-collapse도 dilution도 아님). STEP5 실패는 box-hull 마스크 noisy supervision + 추론 mask 도메인갭이지 loss 버그 아님.

**§1 Oracle PnP (manual 36, clean-GT-PnP 기준 perturbation 민감도)**:
```
조건 A(8코너 Gaussian): σ4→rotΔ1.8° reproj7.4 / σ8→3.4° 9.5 / σ12→5.3° 12.3, PnPfail0%
조건 B(back만):         σ8→2.6° / σ12→3.7° (A보다 둔감)
조건 C(★실제 모델 residual): σ0인데 rotΔ3.96°(p95 14.2°) trans8% reproj12.8 PnPfail3.6%
```
→ Gaussian 독립오차는 4~8px까지 pose 안정(=corner 일반 감소 이득 제한적). **★ 조건 C: 현재 모델 오차는 σ0에서도 Gaussian σ8보다 pose 더 흔듦 = 오차 "크기"가 아닌 systematic(flip/flatten) 방향성이 병목.** (synthetic sweep은 PnP runaway 1h23m → kill, manual로 충분.)

**§2 32장 overfit 게이트**: ✅ PASS median 0.074 cell, p95 0.149 (<0.5/1.0). 구현 정확(인덱싱/stride/부호/denom OK), 표현 암기 가능.

**§3 4096 frozen probe + §6 decoder-unfreeze (A=heatmap/B=GT-center off/C=pred-center off, ov_med)**:
```
                §3 B(frozen)  §6 B(unfrozen)  같은런 heatmap A
synthetic val      31.2   →     28.0           12.6   (B 2.2× 나쁨)
manual real        43.4   →     33.4           11.6   (3×)
filter-val real    44.7   →     37.3           12.1   (3×)
```
- **★ offset 표현 일반화 실패 확정**: 32장 암기는 되나(§2 PASS) 4096 학습→held-out에서 GT-center offset(teacher-forced)이 heatmap보다 2.2~3배 나쁨. decoder 마지막 block 풀어도(§6) 10~25% 개선뿐 여전히 크게 짐. 프로토콜 판정="효용 낮음 → 중단".
- ⚠ confound: probe는 squash 전처리(A도 평소 8.8→12px), synthetic val 분포 train과 다를 수 있음. 단 real(manual/filter-val)도 B≫A라 결론 견고. C(pred-center)도 동일.

**★ 종합 — 벡터/offset 표현 전 계열 종결 (실무결론 견고 / 인과는 1단계 약하게)**: voting(STEP5)·affinity(STEP6)·sparse edge(STEP7)·center-conditioned offset(STEP8) 4종 모두 현재 데이터·backbone·학습 설정에서 heatmap보다 **일반화 성능 낮음** [확정]. Oracle PnP: 현재 heatmap residual은 같은 규모 독립 Gaussian보다 pose에 불리한 **구조(systematic flip/flatten)** [강한증거]. ⚠ **여기서 "base feature에 depth 정보가 없다 / 데이터면 해결"까지는 미확정**(논리 점프 — 이전 종합서 과결론했음, 정정). 확정된 병목 = **viewpoint-dependent 구조적 keypoint bias(flip/flatten)**, 그 원인(sim-to-real appearance gap / monocular geometric ambiguity / PnP conditioning)은 **아직 분해 안 됨**. 결정: 표현 트랙 종료(프로젝트 목적상 추가투자 대비 가능성 낮음 — "이론적 불가능"이 아님). 다음=병목을 위 3원인으로 분해하는 진단(★ `도메인(appearance) × projected front-back separation(geometric observability)` 표 + flip/flatten ρ 정량화부터; 거리·카메라높이는 confound 많은 간접변수라 직접 쪼개지 말 것). 산출 `data/pallet/eval_results/stage8_offset/`.


## 트랙B 진단 3 — 동일프레임 photometric intervention (diag3_photometric.py)
outside P0-검출 30장에 photometric만 night-like 단계변환(P1 노출=실제night luma 보정 0.286 / P2 +shot·read noise / P3 +blur·WB / P4 +glare·clip), 재추론. 저조도지표: night vs outside luma_p50 0.32→0.17, dark_ratio 0.04→0.31, contrast↓, grad_energy 1146→1290, clip 0.
```
level          det%  noResp%  rawErr  GTmargin
P0 원본         100%   0%      11.9    0.163
P1 노출↓(night)  100%   0%       9.8    0.168
P2 +noise         93%   0%      11.1    0.168
P3 +blur/WB       63%  13%      31.4    0.028
P4 +glare/clip    10%  87%     125.4   -0.008
```
- **정정된 결론**: 동일프레임 intervention에서 **전역 exposure 감소와 현재 noise 모델은 baseline-success outside 검출을 붕괴시키지 못함** → scalar brightness reduction + 해당 noise 설정만으로 real-night failure 설명 불가. blur·WB·비균일조명 복합변환엔 저하됐으나 **변환강도가 real night 분포로 보정 안 됐고 요인 결합**이라 원인 확정 X(P3 blur+WB 묶임, P4 glare 과함=real clip 0). raw gradient energy는 noise·객체scale 영향받아 **blur 부재 증거로 못 씀**(이전 주장 철회).
- ⚠ 약화: P1 기각은 "어둠 기각"이 아니라 **global-exposure-only 가설 기각**(P0-detected 30장 조건부 표본·국소대비/shadow/ISP/color-response 미재현). P2도 "현 noise모델 기각".
- 선도가설 유지: night-associated appearance/sensor gap, 단 **geometry confound 미해결** → diag4 먼저.

## 다음: diag4 geometry-matching (real night gap이 geometry 통제 후에도 남나)
설계: 전체(outside44/night43) 사용(+P0-success subgroup 별도). 매칭변수=GT geometry만(A=log hull/HW, S=scale-norm separation, T=truncation, V=in-frame kp, P=perspective skew); photometric은 매칭변수 금지(측정대상). common-support 검사→robust-norm→1:1 NN(without replacement)→SMD before/after. 주outcome 연속값(local_score_med/q25, margin_med, raw_corner_med), 보조 binary(no_response/competing/final_det). 프레임단위(코너 독립표본 금지, off-frame 제외). 통계=Wilcoxon/McNemar+matched-pair bootstrap. matched night<10이면 "common support 부족, 식별 불가"로 기록.

## 트랙B 진단 4 — geometry-matched (diag4_geometry_matching.py) + 정정
GT geometry만(A,S,T,V,P) robust-norm 1:1 NN(caliper2.0). night 43→22 pairs(21 제외=common support 밖). balance SMD 매칭후 A0.56/S0.31/P0.26(불완전).
```
matched(22쌍) night vs outside  Δmed   Wilcoxon p
  local_med     0.721/0.840  -0.046  0.147
  local_q25     0.317/0.563  -0.098  0.014 ★(BH보정~0.084)
  margin_med    0.119/0.116  -0.001  0.975
  raw_corner    13.86/15.39  +0.34   0.610
binary: no_response night0.18/out0.09(p0.625) / final_det night0.45/out0.73 b1c7 p0.070
```
- **★정정(내 이전 과결론 수정)**: "geometry 통제로 detection gap 대부분 사라짐"은 **틀림**. 최종검출 갭 = 매칭전 33%p(68/35)→매칭후 **28%p(73/45), 5%p만 감소·여전히 큼**. p0.070은 "같다"가 아니라 discordant 8쌍뿐이라 불확실(유의하지않음≠동일). **크게 준 건 no_response(33%p→9%p)** — no-response 성분이 geometry non-overlap 표본에 집중됐던 것.
- **local_q25 = 약한 잔차 아니라 핵심 후보**: local_med/margin/raw_corner 비슷한데 local_q25만 night 낮음+final_det 28%p 낮음 → "대부분 코너 OK, night은 프레임당 1~2개 약한 코너가 무너져 전체 검출 실패". raw_corner_med(8개 중앙값)가 이 tail 숨김. (단 6 outcome 탐색이라 BH보정 p~0.084=기전상 일관 신호지 확정 아님.)
- caveat: 22쌍·A SMD0.56 잔존·21 night 제외(실운용 문제일 close-up/skew/trunc일 수 있음)·marginal p.
- **정확한 결론**: no-response는 geometry로 상당 설명, 단 **최종검출 28%p 잔차는 남음**(geometry로 대부분 설명 단정 못 함). 잔차는 median이 아니라 weak-corner tail에서.
- 다음(정확한 이름) = **geometry coverage + weak-corner failure attribution**: ① 22쌍 28%p 검출갭을 failure type별 분해 ② weak-corner 통계(weakest id·front/back·count) ③ 제외된 night 21장(G2) vs matched(G1) vs outside(G3) ④ synthetic geometry kNN coverage ⑤ diag4b overlap weighting 견고성.

## 트랙B 진단 5 — weak-corner attribution + G1/G2/G3 (diag5_weakcorner_coverage.py)
```
① matched 22쌍 검출갭(-27%p, ≈6프레임) 분해: corner_loc +14%p(≈3) / no_response +9(≈2) / competing +5(≈1)
② G1 matched-N(22) det45/noResp18/cornLoc18  G2 excluded-N(21) det24/noResp67★/cornLoc0/medA-2.12(큰객체)  G3 outside(44) det68/noResp9
③ weak_count(GT-local<p5): 실패프레임 night3.8/outside2.5개, 성공 0.5/0.3
```
- **정정**: (1) G2는 "synthetic coverage 부족"이 아니라 **outside real과 geometry common support 없는 night** = real 수집이 close-up/large-projection geometry에 편향. synthetic에도 부족한지는 **diag6에서 판정**(미확정). (2) corner_loc +14%p = 22쌍 중 ≈3프레임 → "주범 확정" 아닌 **선도 가설**(matched 검출갭 6프레임 중 corner-localization이 최대 성분). (3) weak corner는 1~2개 아니라 **복수(2.5~3.8개) 부분붕괴**. weak_count↔detection은 일부 기계적일 수 있어 weak_score_count vs weak_error_count 분리 필요(diag6).
- 분리된 두 문제: **(A) raw night no-response 붕괴는 G2(geometry-non-overlap)에 집중** / **(B) matched G1 잔차는 corner-localization/weak-corner**.
- 다음 diag6 = H1(synthetic geometry coverage 부족) vs H2(geometry-conditioned sim2real) vs H3(geometry/model limit) 구분. ★ synthetic **train support**와 **held-out 성능** 분리. + validity audit(G2 corners off-frame면 coverage 아닌 task-definition).

## 트랙B 진단 6 — synthetic coverage vs sim2real (diag6_synthetic_coverage.py) + 정정
syn_train 7999(coverage only) / syn_val 1500(held-out 추론). GVEC=[A,S,P,Bn,Emin], support thr=synth-val d20 p95/99.
```
[0 validity V<8]  G1 14% / G2 43% / G3 11%
[1 coverage]      G1/G2/G3 전부 100% in-support, ΔOOS(G2-G1)=0%p
[2 real vs localSynDet]  G1 real45/syn79  G2 real24/syn66  G3 real68/syn86
```
- **H1(coverage 부족) 기각 — 단 "coarse geometry descriptor 기준"**: 선택한 (A,S,P,Bn,Emin)·threshold에선 synthetic 이웃 발견(100% in-support). yaw/elevation/visible-face/occlusion/CAD차 등 누락변수 있으면 false in-support 가능 → montage 눈검증 필요.
- **H2(sim2real) 강하게 지지**: 같은 geometry서 synth held-out(79/66/86) ≫ real(45/24/68). geometry-conditioned sim-to-real appearance gap. night 한정 아님(G3도 갭).
- **정정: H2 단독 아닌 H2+H3+truncation 혼합**. G2 synth도 34% 실패=geometry 자체 난도(H3 동시존재), real penalty G1 34%p/G2 42%p/G3 18%p=sim2real 큼, G2 V<8 43%=truncation/task-def. → G2 실패 = geometry 난도 + sim2real + out-of-frame 혼합.
- 다음 diag6b(retraining 전 마지막 검증): ① V=8/V<8 분리 + same-V 이웃검색(G2 66%가 V혼합이면 불공정) ② montage(projection topology 눈검증, geometry feature 적정성) ③ out-of-frame label 정책(clamp/force-8이면 label 문제). 통과시 처방=트랙A domain randomization(DR0~4 ablation) + 트랙B partial-keypoint/subset-PnP.

## out-of-frame corner label 정책 확인 (diag6b 선행)
- **[확인] CreateBeliefMap(utils_belief.py:137)**: `p±2σ 전부 map 안`일 때만 Gaussian 배치 → **화면밖+경계 2σ(σ4=8px) 이내 코너는 GT belief=0 = 모델이 suppress하도록 학습**. clamp 아님(좋음), force-8 아님(gate n_det≥6). albumentations remove_invisible=False(좌표 보존). → G2 close-up no_response에 **label-policy 성분**(경계코너 미supervise) 존재 = appearance/coverage만 아님. 처방에 "경계코너 supervise(valid-mask loss) or partial-keypoint" 추가 후보.
- 단 synthetic도 동일 정책이라 양쪽 영향 → real penalty(sim2real)는 별도로 유효.

## 트랙B 진단 6b — V=8/V<8 분리 (diag6b_vstrat.py) ★ 두 원인 완전 분리
syn_train V=8:5985/V<8:2014, syn_val V=8:1363/V<8:137. same-V 이웃검색.
```
group        Vbin  N  realDet  locSynDet  gap   realNoResp
G1 matched   V=8  19   53%     90%        38%p
G2 excluded  V=8  12   42%     88%        46%p   58%
G3 outside   V=8  39   74%     94%        19%p
G1 matched   V<8   3    0%      3%
G2 excluded  V<8   9    0%     12%        12%p   78%
G3 outside   V<8   5   20%     10%
```
- **① V=8(full-view): 깨끗한 sim2real appearance gap** — synth 88~94% vs real 42~74%(gap 19~46%p). truncation 무관·geometry in-support(synth 됨) → 순수 도메인갭. diag6 "G2 66%"는 V혼합 산물(분리시 V=8 88%). → 처방 = domain randomization(배경/재질/조명/센서).
- **② V<8(truncated): real·synth 둘 다 실패**(synth도 3~12%) = appearance 아닌 **label/task 정책**(CreateBeliefMap 경계 suppress + n_det≥6 gate). → 처방 = partial-keypoint/subset-PnP + 경계코너 supervise (DR로 안 풀림).
- caveat: 셀 N 작음(G1V8 19/G2V8 12/V<8 3~9), localSyn=synth-val same-V KNN. 단 패턴 강함(V=8 synth~90%/V<8 synth~3-12% 일관). montage(geometry feature 적정성)는 미실시(후속 권장).
- ★ 최종: "작동되게" = 두 독립 트랙 — A) V=8 sim2real → domain randomization, B) V<8 truncation → partial-keypoint. (벡터/decode 트랙은 종료, heatmap 유지.)

## 트랙B 진단 6c — montage gate (diag6c_montage.py) → PASS
G2 V=8 12장 전부 montage(real+nearest synth-train/val 3장씩, cuboid overlay+A/S/P 라벨). 눈검증 3장(00/04/08): coarse geometry NN이 topology상 합리적 synth(평평 pallet, 비슷한 저각도 뷰) 찾음, gross false-match 없음. P(perspective)는 약간 흩어지나(예 #00 real0.45/synth0.25~0.32) catastrophic 아님. → **montage PASS, sim2real 결론 확정**(real이 geometry-유사 synth보다 실패=H2). geometry descriptor는 P 보강 여지(후속 선택). 저장 `stage9_diag/montage/`.
