# 2026-06-20

## 4-arm PL-level 필터 비교 @ matched pass-count (paper_strategy_master §206 생사)

질문: **같은 pass-count N에서 diag(기하필터)가 단순 size/aspect·conf·no-filter보다
gross(>20px 파국 PL)를 더 억제하나?** = 논문 thesis 핵심.

### 신규 파일
- `scripts/stage0/four_arm_pl_compare.py` — 5-arm(A1 no-filter / A2 conf /
  A4 size·aspect / A6 diag∧flip / A7 diag+size) matched-N 비교. filter-val GT
  ONLY, final-test 봉인(`collect_val_frames` 화이트리스트 + assert). CPU smoke=
  `--list_val_frames`. 기하 재발명 0 (filter_pr_camfacing / filter_flip_consistency
  / tau_calibrate 재사용). weights=`weights/paper_base_v2_s2/net_epoch_0066.pth`.

### A4 (size/aspect) 공정 정의
검출 코너 2D bbox의 size(bbox대각선/이미지대각선)+aspect(w/h). accept envelope을
**GT projected_cuboid bbox [p2.5,p97.5]**로 fit (model-independent → 순환 없음, GT
pass 최대화 = strawman 아님). 연속 score=envelope 밖 excursion(sweep 가능).

### 결과 (outside usable=17, good=11, gross=2) — ★ PRELIMINARY
matched-N gross (actualN/gross):
```
 N |  A2conf  A4size  A6diag∧flip  A7diag+size
 6 |   6/1     11/0      6/1          6/0
14 |  14/2     14/1      9/1          7/0
```
- **A6 diag∧flip: 모든 N에서 gross=1.  A4 size: N≤11 gross=0.  A7 diag+size: 전구간
  gross=0.** → 같은 N서 **diag가 size를 못 이김 → §206 thesis 약화(WEAKENED).**
- gross 2장 인과: (a) 1장은 diag PASS(flip이 구제), (b) 1장은 diag∧flip 둘다 PASS인데
  **A4가 거름**(size 0.174=원거리 sliver, scale-collapse). diag/flip은 projective
  invariant라 scale-collapse를 못 봄 → **size가 그 사각을 메움**.
- **night = 평가 불가**: paper_base_v2_s2가 night filter-val 43장 전부 <6코너 검출
  (31장=0코너). aspect preprocess로 재확인 = 진짜 detection collapse(저대비/depth),
  버그 아님. 정직 별도 보고.
- outside pool pass-count(quantity, 캐시): detectable 785 / diag 501 / diag∧flip 452.

### 함의
diag 단독 우위 주장 금지. **size-gate 채택 또는 diag+size 병행**이 gross 0 달성.
출력: `data/pallet/eval_results/stage0_four_arm/four_arm_compare.{txt,json}`.
N=17 소표본 → "예비 신호", 입증 아님.

### ★ 메타 교정 (메인 세션 검증 — agent "WEAKENED"는 과한 결론)
- **정확한 결론 = INCONCLUSIVE(미검증), NOT WEAKENED.** 비교 전체가 **gross 2장** 위에 섰고, 그 2장이 thesis를 시험 못 함:
  - gross #1(38.5px)=구조오류 → flip이 거름(diag∧flip 정상 작동).
  - gross #2(47.8px)=**scale 붕괴 sliver** → diag/flip은 projective invariant라 scale을 **설계상 못 봄**; size가 거름.
  - → **구조-gross 0장, scale-gross 1장.** diag의 실제 주장(*구조적* 파국 억제)을 시험할 프레임이 표본에 없음.
- **size는 thesis 경쟁자가 아니라 원래 설계의 always-on post-check** [확인 config:43 "size sanity always applied", 전략 A7="diag가 size 위에 추가효과"]. A7(diag+size) gross 0은 **설계대로**지 diag 반증 아님. "diag가 size 못 이김"은 표본 gross가 우연히 scale-type(size 영역)뿐이라 생긴 artifact.
- **진짜 발견**: filter-val GT(outside usable 17, night 0)가 **필터 thesis 검증에 표본 부족**. A4도 N<11로 못 좁혀 diag의 N=6과 "같은 N" 비교 자체가 깨짐. → 필터 검증의 진짜 블로커 = **검증셋 크기/검출**, diag vs size 우열이 아님.
- 다음 후보: (a) filter-val GT 확대(더 많은 GT 프레임 = 구조-gross 표본 확보), (b) full self-training은 같은 소표본이라 보류 정당, (c) night는 검출 붕괴로 PL-GT 검증 불가(별도 한계). agent memory의 "thesis_weakened"도 이 교정 반영 필요.

## (다) 프레이밍 결정 — 실패모드별 기하 신호 분석
4-arm INCONCLUSIVE 후 thesis 포지션 확정: "diag 단독 우위"(미검증)도 "단순 조합"(novelty약)도 아닌 **"각 기하 신호가 다른 실패모드 대응"** (diag=구조파국 / size=scale붕괴 / flip=구조보완). novelty=대응 통찰(조합 아님)→§206("size가 diag 설명") 무력화("다른 실패모드다"). 데이터 뒷받침: gross#1=flip(구조), #2=size(scale). master §1.4(suppression+structural+camera-facing)·§1.3(filter main + 3-domain GT co-primary)와 정합=sharpening. diag↔size 대립 서술 금지(상보). memory [[thesis-framing-failure-mode-geometric-signals]]. ⚠ master §1.4에 "실패모드별 신호" 한 줄 추가 = doc-todo(미적용).
