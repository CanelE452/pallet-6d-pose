# 2026-06-10 (06-08~10 종합)

## paper_base self-training C1 — R1 회귀 → base 천장 규명 (핵심)

### 흐름
paper_base 학습 완료(06-06) → diag/diag∧ratio PL 추출 → R1 학습 → **R1 회귀** → 원인 추적 → **필터가 아니라 base 모델이 병목** 확정.

### R0→R1 회귀 (C1_rounds.md)
- R0=paper_base/ep60, R1_outside=diag PL 696, R1_night=diag∧ratio PL 106.
- 결과: 검출률만 ↑, **reproj(9kp 정확도)는 전 도메인 악화**, good(<10px)% outside 12.1→0 붕괴.
- 원인: diag/diag∧ratio가 centroid·대각선만 봐서 **corner 부정확 PL 다량 통과** → R1이 "더 자주 더 거칠게" 학습.

### 필터 추가 실험 — 전부 같은 결론 (pr_screening.md)
- **ransac_loo threshold sweep (paper_base)**: tau 3~15 완화해도 good% **항상 0**. 양만 늘고 순도 천장 못 넘음. (대조군 s2 누수모델엔 sweet spot 있음 → 코드 정상, paper_base 0%는 실제.)
- **diag/fullkp 단독 (paper_base)**: good% 최고 25%(indoor diag). <10px 청정 PL 불가.
- **flip consistency**: diag의 2차 게이트로 상관 0.48(보완 신호)이나, paper_base 천장 안에선 단독·조합 모두 청정 PL 못 만듦.
- **paper_base 통과 PL overlay 육안검증**: diag good 1~3/도메인, fullkp ~4/도메인. cyan 예측이 magenta GT 안쪽 collapse/overshoot 실재. "천장" 숫자가 과장 아님 확정.

### 핵심 결론 — base가 병목, 필터 연구 종결
- **paper_base는 real에서 코너 9kp_med 11px+ → 어떤 필터(diag/ratio/fullkp/ransac_loo/flip)·threshold·31조합으로도 믿을만한 PL(<10px) 못 만듦.**
- A1: paper_base **합성 val corner도 11.7px** → sim2real 갭 이전에 **합성 학습 자체에서 코너 거침**.
- self-training 전제(base가 real에서 어느정도 정확)가 안 섬 → 필터는 더 팔 게 없음. **base 코너 정확도 개선이 다음 목적.**
- ⚠️ 함정: 보여준 diag/fullkp overlay 98장이 **ft_s2(누수·과제용)** 기준이었음(사용자 지적). paper_base로 재확인해 천장 결론 강화. 절대 N median은 평균이라 통과 PL 코너 어긋남을 가렸고, good%로 봐야 실상.

### squash 가설 검증 (A2) — 반증
- 사용자 가설: "예전 squash 없는 논문 모델은 indoor 괜찮았다". → **반증.**
- dope_cropaug_pretrain(squash X)이 indoor 9kp **35px 폭망**(front 66px, collapse). paper_base(squash) 13px가 최선.
- squash 효과는 indoor 특이적(정면 top-down flat에서 다양한 비율 학습이 extent 추정에 강함). **squash 유지/강화가 방향, 제거 아님.**
- indoor 약점 = back(4-7)·centroid·detection(26%).

### 산출물
- 스크립트: `filter_loo_sweep.py`, `filter_flip_consistency.py`, `paper_base_pass_overlay.py`, `fullkp_pass_all_overlay.py`, `diag_pass_all_overlay.py`, `squash_vs_nosquash.py`
- 결과: `data/pallet/eval_results/{filter_loo_sweep,filter_flip_consistency,filter_combo_9kp/paper_base_pass,squash_vs_nosquash}/`
- 문서: `C1_rounds.md`, `pr_screening.md`(sweep/flip 섹션), `A2_squash_ratio_ablation.md`(결과 채움)
- memory(3d-expert): ransac-loo-sweep, flip-consistency, diag-filter-not-reliable, paper_base_pass, squash_vs_nosquash

### 다음 (목적 = self-training 일반화 → base 코너 정확도 선결)
- [ ] base 코너 정확도가 왜 합성에서부터 11px인지 진단: sigma(4.0 퍼짐?) / loss(corner-weighted/geometric) / 합성 데이터 품질
- [ ] indoor: top-down flat 본질적 난점 — self-training에서 제외 vs back/detection 보강 결정
- [ ] 필터(diag 등)는 base가 <10px로 올라온 뒤 재평가 (현재는 의미 없음)
