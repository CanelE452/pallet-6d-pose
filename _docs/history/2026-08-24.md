# 2026-08-24

> 이 날 작업은 대부분 08-23 심야에 시작해 자정을 넘긴 것이다.
> camera-facing loss queue 폴더: `challenge/yolo_pose_one_model/runs_camera_facing_loss/ubuntu_cf_loss_queue_20260823T0930/`

## 흐름 요약

```
loss 축 종료 → 데이터 축(V2) → night 분해 → FT ranking 원인 → OLD Stage-A 원인 분해
```

## ★ 정정 2건 (덮지 않고 기록)

### 1. "A/C/E night correct-box = 0" 은 틀렸다 — `CORRECTION_NOTE.md`

`cf_real_eval.py` 의 게이트 모집단 `cbox_paired` 가 **A0 공통 correct-box** 인데
A0(V1-10K)의 night correct-box 가 0 이라, 어떤 모델을 넣어도 night 표본이 0 이 되어
`night_p90 = NaN` 이 나왔다. 이를 "night 에서 팔레트를 못 찾는다" 로 오독했다.

```
실제 night top1-cbox   A42 0/28   C42 13/28   C43 18/28   E42 13/28   FT 27/28
```

기존 `COVERAGE_EFFECT_10K` / `EXTRA_V2_SIGNAL` 판정은 유효하되 **적용범위를 DAY 로 한정**해 읽는다.

### 2. `TARGET_ADDITION_REQUIRED = False` 를 전역 결론으로 쓰지 않는다

ALL median 회수율 94.6% 는 DAY(100/128) 지배 지표다.

```
TARGET_ADDITION_REQUIRED_FOR_ALL_MEDIAN    = FALSE_BY_PREVIOUS_GATE
TARGET_CONTENT_REQUIRED_FOR_NIGHT_RANKING  = UNRESOLVED   ← G38EXP 가 판정
```

## 신규 스크립트 (전부 queue 폴더)

- `night_decomp.py` — night 실패를 N0/N1A/N1B/N2/N3/N4 로 분해. correct-box rule(IoU>=0.5)·
  conf·pad 를 새로 만들지 않고 기존 `cf_real_eval.py` 에서 읽어 재사용.
  N2 는 현 계약에 kp-valid gate 가 없어 구조적으로 0 임을 명시.
- `ranking_audit.py` — candidate 수준 margin/전이 분석. C43→FT 전이 T0/T1/T2/T3.
- `night_cand_one.py` — 임의 checkpoint 의 night candidate 거동.
- `old_support_audit.py` — OG/OT/V2 metadata 추출 + 1D/2D support + match feasibility.
- `g38_driver.py` — G38 generic-only 60ep → 동일 real → 판정.
- `g38exp_driver.py` — G38_EXP73916 exposure-matched control.
- `coverage_driver.py` / `extra_v2_driver.py` — V1-10K vs V2-10K, V2-10K vs V2-12.5K.
- `cf_real_eval.py` / `cf_audit.py` / `run_cf_loss_queue.py` — 아래 수정 참조.

## 수정 (Before/After)

- **`cf_real_eval.py`**
  - Before: 게이트 모집단 = `paired`(A0 공통 detected, threshold-free 140장 전체)
    After: `cbox_paired`(A0 공통 **correct-box**) 추가하고 게이트를 이걸로.
    이유: box 오검출 82장이 keypoint 신호를 덮었다. 후보 결과 나오기 전에 변경.
  - Before: `data_paths.EVAL_CANONICAL` glob (repo 상대경로)
    After: manifest 의 `image`/`label` 을 ROOT 와 join — cwd 무관.
    이유: 큐가 다른 폴더에서 돌아 membership 0 → `S3 = BLOCKED` 오탐.
- **`run_cf_loss_queue.py`** — `real()` 이 캐시에 `cbox_paired` 가 없으면 재측정.
  이유: 지표를 나중에 추가하면 옛 캐시로 KeyError.
- **`coverage_driver.py`** — 학습 subprocess 의 **전체 로그를 파일로** 저장.
  Before: `capture_output` 의 stderr 꼬리 700자만 로그에 남김 → CuDNN 경고가 실제 예외를
  밀어내 진단 불가(두 번 겪음). After: `_train_{name}.log` 에 stdout+stderr 전부.
- **`old_support_audit.py`** — OT 구형 스키마 어댑터.
  Before: `keypoint_convention` 이 `y_up_...` 이라 `cuboid_dimensions_m[1]` 을 높이로 사용
  → **r3d = 10.83** (팔레트로 불가능). After: `keypoints_3d_world` 의 ptp 와 대조해
  `[W, D, H]`(index 2 가 H)로 교정 → r3d 0.109. 이미지 크기는 `intrinsics.cx/cy × 2` 로 유도.
- **`test_cf_pevl.py` T10** — Before: 모든 corner 를 같은 벡터로 평행이동 → edge 벡터 불변 →
  PEVL=0 이라 FAIL. 이는 PEVL 의 평행이동 불변성이지 결함이 아니다.
  After: corner 0 만 +5.0 섭동 → 19/19 PASS. **PEVL 학습은 하지 않았다(PARKED_NOT_REJECTED).**

## 데이터셋 신규 (전부 symlink, 새 RGB 0)

```
datasets/v2_cf_early10k    train 9,875 / val 125    (scanner effective 9,704 / 123)
datasets/v2_cf_complete    train 12,375 / val 125   (effective 12,173 / 123)
datasets/g38_generic_only  train 38,002 / val 1,998 (stage_a 의 G_ 항목 그대로)
datasets/g38_exp73916      exposure 73,916 / unique 38,002 / alias 35,914 / target 0
```

`g38_exp73916` 의 alias 선정은 결과를 보기 전에 결정론적으로 고정했다 —
`sha256("G38_EXP73916_SEED42|{stem}")` 정렬 상위 35,914 를 ×2, 나머지 2,088 은 ×1.

## 주요 결과

```
SAME REAL n=128 (FT 누수 12장 전 모델 제외)
model   composition                  cbox    med     p90    gross   night cbox
A42     generic 10K                 0.422  53.46   87.91   0.917      0.000
G38     generic 38K                 0.852  12.03   66.66   0.314      0.536
OLD     generic38K + target×2       0.969   9.68   40.99   0.222      0.929
C43     V2 10K                      0.797  14.28   91.98   0.401      0.643
FT      OLD + real FT               0.984   6.47   25.40   0.135      0.964
```

- `COVERAGE_EFFECT_10K = POSITIVE` — V1→V2 로 real median 격차의 88% 를 닫음
- `EXTRA_V2_SIGNAL = NULL_OR_WORSE` — V2 10K→12.5K 는 오히려 악화
- night 은 **검출이 아니라 ranking** 문제 — any-cbox 는 OLD·FT·C43 모두 0.964 로 동일,
  차이는 candidate/frame(OLD 2.25 / FT 1.32 / C43 7.11)과 margin 에서 난다
- `GENERIC_SCALE_EFFECT = STRONG` (ALL median 회수율 94.6%), 단 night ranking 은 미회수

## ★ FT eval 누수 (`FT_EVAL_LEAK.json`)

FT positive 157장 중 **12장이 현재 eval membership 과 동일 프레임**이다(×20 노출).
`eval_outside` 가 별도 촬영이 아니라 `capturepallet02/03/04/05/08` 에서 뽑아 모은
큐레이션 셋이라, **세션 디렉토리 비교만으로는 안 잡힌다.** night 과의 누수는 0 이라
night 결론(FT 27/28)은 영향 없다. 이후 모든 비교에서 이 12장을 제외했다(n=128).

## Windows 전송

`OLD_STAGE_A_METADATA_FOR_WINDOWS_20260823T1830.zip` (56.8 MB, **RGB 0개**) —
lineage audit, OG/OT/V2 metadata CSV, support/joint audit, feasibility, exact membership.

## 진행 중

`OLD_ROOT_G38_EXP73916_60EP_SEED42` — epoch 58/60 (7.82분/epoch, batches/epoch 2,310 로
OLD 와 일치). 완료 시 `G38EXP_ROOT_STEP2.{json,md}` + RECOVERY 판정.
---

## [07:44] real negative 데이터셋 도입 — AP(FP/precision) 측정용

사용자가 받은 `~/Downloads/real_unlabeled.zip` (1.16 GB, 08-23 18:47) 을
`data/pallet/raw_data/negative_real_20260823/` 에 배치했다. **팔레트가 없는 실촬영**
프레임이고 AP 의 false-positive 측정에 쓴다.

### 배치 근거

`challenge/data/INDEX.md` 가 "01_real 은 png+json 쌍, **라벨 없는 순수 촬영본은 여기
없고** `data/pallet/` 아래" 라고 명시한다. 라벨 없는 negative 이므로 `raw_data/` 의
세션 관례(`rgb/` + `depth/` + `cam_K.txt` + `DATASET.md`)를 그대로 따랐다.
원본 파일명·내용 무수정, 확장자별로 제자리에 풀었다.

```
negative_real_20260823/
  rgb/     2,689 png      depth/  61 npy
  cam_K.txt  camera_info.json(원본)  MANIFEST.json  DATASET.md      1.4 GB
```

### ★ 잡은 것 3 건 — 파일명만 믿었으면 놓쳤다

**(1) 세 블록이 섞여 있다.** 단일 촬영이 아니다.
```
000000~000023     24     2026-05-13     1280x720
000024~000295    272     2026-08-21     1280x720
000296~002688   2393     2026-08-21      640x480
```
전처리에서 해상도를 하나로 가정하면 안 된다.

**(2) camera_info.json 의 K 는 640x480 전용이다.** 1280x720 296 장에는 안 맞는다.
게다가 `cy = 256.29` 인데 480/2 = 240 이라 640x480 블록에서도 중심과 어긋난다
(256 = 512/2 라 다른 센서 모드 값으로 보임 `[추정]`). detection AP 는 K 를 쓰지
않으므로 무관하지만 pose/PnP 로 쓰기 전 재검증이 필요하다.

**(3) 내용 중복 1 쌍** `000238.png == 000239.png` (sha 동일). 지우지 않고 기록만 했다
— 사용자가 "실제 데이터" 라고 했으므로 원본 보존을 우선했다.

### 검증 — 표본이지 전수가 아니다

000000~002688 균등 간격 **25 장** 육안 확인 → **25/25 팔레트 없음**.
박스/골판지 더미, 야외 나무 벤치(판자 구조), 책상·의자, 신발장 등 **hard negative**
가 다수라 FP 측정에 질이 좋다. 다만 2,689 장 전수를 본 것은 아니므로, FP 가 나오면
그 프레임을 개별 확인한다.

### 기존 negative 와의 차이

`REAL_NEG_DEV_V1`(259 장, forklift 시퀀스)은 `max_conf < 0.20` 선별이라 **편향 표본**
이었고 거기서 잰 FP/image 는 하한이었다(memory `yolo-conf-threshold-is-not-the-lever`).
이 셋은 그 선별을 거치지 않아 그 편향이 없다. 대신 촬영지가 사무실·교내라
팔레트 작업장과 다르다는 점을 해석에 반영해야 한다.

## [16:35:31] ⚠️  Auto Compact (컨텍스트 한계 도달)

- Working directory: `/home/minjae/Documents/github/pallet-pose`
- Trigger: `auto`
- Session ID: `742091d2-76ae-45b1-b584-8cfb1f1a04e0`
- Transcript: `/home/minjae/.claude/projects/-home-minjae-Documents-github-pallet-pose/742091d2-76ae-45b1-b584-8cfb1f1a04e0.jsonl`
- Note: 컨텍스트가 가득 차서 자동 트리거됨 — 다음 세션에서 이 마커 위쪽 작업을 반드시 복원할 것

**TODO (session resume)**: compact 이후 이 마커 위쪽의 작업 내역을 확인하고 이어서 진행


## [G38 + SUPPORT] target-free BEST-PERFORMANCE CANDIDATE 착수

S20 support probe 가 `SUPPORT_PROBE_SIGNAL = HARM` → `RENDER_ACTION =
CLOSE_RENDER_MOVE_TO_ADAPTATION` 으로 닫힌 뒤, 사용자 지시로 **causal ablation 이 아닌
성능 후보 1 개**를 만든다. G38(broad generic scale) + SUPPORT(thin/mid-elevation) 결합.

### 새 파일

- `challenge/yolo_pose_one_model/runs_camera_facing_loss/ubuntu_cf_loss_queue_20260823T0930/combo_driver.py`
  — smoke(1ep) → 60ep → args diff → same-real eval → gap closure → gate → discord.
  한 파일에서 판정까지 (전역 규칙 "장시간 작업은 결과까지 자동으로").
  완료 판정은 `results.csv` 행수 >= epochs (last.pt 는 매 epoch 갱신되므로 근거 아님).

### dataset (전부 symlink, 새 RGB 0)

```
datasets/g38_plus_support   train 39,935 = G38 38,002 + SUPPORT 1,933   val 1,998 (G38 val 그대로)
```

- 중복 0, G38∩SUPPORT 0, support oversampling 없음, G38 downsampling 없음
- corrupt 0, batches/epoch 1,188 → 1,248 (**steps +5.1% 교락**)

### 사전 freeze 산출물 (학습 전)

- `G38_PLUS_SUPPORT_SOURCE_FREEZE.json` — G38 sha `74eb31741c34addb`, SUPPORT sha `dea4669a59942026`
- `G38_SUPPORT_CONTRACT_PARITY.json` — **PARITY = True** (nkp 9, cls 0, vis 2,
  좌표 [0,1] 이탈 0, canvas 크기 집합 동일 = 동일 PAD100 exporter)
- `G38_PLUS_SUPPORT_MANIFEST.json` / `G38_PLUS_SUPPORT_SCANNER.json`
- `G38_PLUS_SUPPORT_GATE_PREREG.json` — hits 6 개 중 **3 개** + guard 3 개.
  결과를 보고 threshold 를 고치지 않는다.

### recipe (G38/OLD args 정본 복제)

`epochs 60 / batch 32 / imgsz 640 / SGD lr0 0.01 lrf 0.01 / cos_lr / warmup 3 /
mosaic 0.30 close_mosaic 10 / scale 0.25 / hsv 0.015-0.5-0.35 / fliplr 0 /
seed 42 / patience 15 / save_period 5 / STANDARD PoseLoss26`
허용 차이는 `data / name / save_dir / project` 뿐 — `G38_vs_G38SUPPORT_ARGS_DIFF.json`
에 검증, 그 외 차이가 있으면 드라이버가 죽는다.

- checkpoint policy = **last.pt** (`REAL_G38.json` 의 weights 경로에서 확인 `[확인]`)
- runtime audit 으로 `PC_CALL_COUNT == 0`, `ROLE_CALLS == 0`, criterion = PoseLoss 강제

### ★ 해석 제약 (보고에 반드시 동반)

- **CAUSAL ABLATION 아님** — support 1,933 만큼 exposure/steps 가 5.1% 증가한다.
- OLD/FT 는 REFERENCE / UPPER DIAGNOSTIC 이며 같은 training data 조건이 아니다.
- NIGHT n=28, seed 1 개.
- eval 은 FT leak 12 장 제외 후 n=128.

### 착오 (기록)

`~/miniconda3` 로 conda 를 활성화하려다 실패 — 이 머신 base 는 `~/anaconda3`.

### [21:14] G38 + SUPPORT 결과 — `TARGET_FREE_COMBO_SIGNAL = HARM`

60ep 완료(16:44→21:14, 4h30m). same real n=128(FT leak 12 장 제외), last.pt.

```
model             cbox   median      p90  gross20        NIGHT any  top1  margin
--------------------------------------------------------------------------------
A42              0.422    53.46    87.91    0.917
C43              0.797    14.28    91.98    0.401
G38              0.852    12.03    66.66    0.314            0.821 0.536  +0.036
COMBO            0.836    11.18    75.56    0.307            0.714 0.500  +0.115
OLD (ref)        0.969     9.68    40.99    0.222            0.964 0.929  +0.879
FT  (ref)        0.984     6.47    25.40    0.135            0.964 0.964  +0.962
```

- **hits 0/6** — 하나도 통과 못 함
- **guard 위반: `night_any_cbox` −3 frames** (한도 −2, N=28) → HARM
- 다른 guard 는 통과: all_cbox −1.56pp(한도 −3.0), all_p90 −13.35%(한도 −15%)

근접했지만 못 넘은 것: all_median **+7.07%** 개선(문턱 8%), night_margin **+0.079**(문턱 0.10).

**해석** — SUPPORT 는 *검출된 프레임의 점 정확도*는 조금 올리고(median 12.03→11.18,
gross20 0.314→0.307) **검출/랭킹은 떨어뜨린다**(p90 66.7→75.6, night any-cbox −3 frames).
DAY 도 같은 방향(cbox 0.940→0.930, med 11.61→10.89, p90 63.9→73.8)이라 night 국한이 아니다.
S20 support probe(30ep, effective 9,704)의 HARM 과 **독립 재현** — 규모·epoch·데이터
구성이 다른 두 실험에서 같은 방향이 나왔다.

**판정**
```
TARGET_FREE_COMBO_SIGNAL   = HARM
CURRENT_TARGET_FREE_BEST   = G38
RENDER_TRACK               = CLOSED     RENDER_RESUME = FALSE
```

**★ 해석 제약** — CAUSAL ABLATION 아님(steps +5.1% 교락). OLD/FT 는 REFERENCE/UPPER
DIAGNOSTIC. NIGHT n=28·seed 1 이라 guard 위반 폭이 정확히 1 프레임 차이지만,
**hits 가 0/6 이므로 guard 를 빼도 결론은 POSITIVE 가 아니라 NULL** — 즉 "결합이
이득" 이라는 주장은 어느 쪽으로 봐도 성립하지 않는다.

산출물: `G38_PLUS_SUPPORT_RESULT.json` / `.md`, `REAL_G38SUP.json`,
`NIGHT_CAND_G38SUP.json`(둘 다 21:14 신규 생성 확인), `G38_vs_G38SUPPORT_ARGS_DIFF.json`

## [21:5x] REAL ADAPTATION 2×2 SCREEN 착수 — Ubuntu NEG / Windows POS 분담

`CURRENT_TARGET_FREE_BEST = G38` 고정 후 adaptation 두 축을 host 분담으로 병렬 screen.

### 새 파일

```
runs_camera_facing_loss/ubuntu_cf_loss_queue_20260823T0930/
  setup_adapt.py                 U0~U3,U5 계약 freeze + replay + N0/N1 dataset
  neg_eval_one.py                단일 ckpt 의 POS(128)/NEG(2,689) 점수 덤프
  neg_screen_driver.py           U6~U9 train→eval→verdict→notify
  make_windows_pos_package.py    U4 Windows handoff 패키지
```

### U0 INIT LOCK — artifact 로만 확정

```
G38_INIT_PATH   runs_camera_facing_loss/OLD_ROOT_G38_GENERIC_ONLY_60EP_SEED42/weights/last.pt
G38_INIT_SHA256 1a806ca497fde5175334e908540ebae22a45deeb08862f014ac8460f8d43ac3c
source of truth REAL_G38.json["weights"]  (기억으로 best/last 고르지 않음)
```
N0/N1/P0/P1/M11 전부 이 exact checkpoint 에 bind.

### U2 DATA SPLIT LOCK — ★사용자 지시의 산술 하나가 실측으로 뒤집혔다

```
검사                                   결과
────────────────────────────────────────────────────
POSITIVE_TRAIN unique                  157   ✓
NEGATIVE_TRAIN unique                  259   ✓
REAL_EVAL (membership 140 − leak 12)   128   ✓
POS_TRAIN ∩ REAL_EVAL                    0   ✓
NEG_TRAIN ∩ NEG_TEST (내용)              0   ✓
NEGATIVE_TEST  선언 2,430 → 실측     2,689   ✗ 정정
```

지시는 `2,689 − 259 = 2,430` 이었으나, FT negative 259 는 `neg__forklift_raw__`
(배포영상, max_conf<0.20 선별)이고 2,689 는 `negative_real_20260823`(사무실·교내)로
**출처가 다르다**. 파일명 숫자 id 는 259 개 겹치지만 내용은 무관하다.

FT 쪽은 840×680 = 640×480 의 PAD100 캔버스라 처음 sha 비교가 무효였고, 센터크롭
후 다시 비교했다 `[확인]`:

```
비교 방식                        교집합     비고
raw pixel sha1                      0
32x32 gray perceptual sha1          0
같은 id 끼리 mean|diff|          ~70       완전히 다른 장면
```

→ **held-out negative = 2,689 전체**. 과거 `negative_ap.py` 의 `HELDOUT`(2,430) scope 는
같은 오해에서 나온 값이라 무효이고, 같은 스크립트의 `FULL` scope 가 맞는 primary 다.
memory `real-negative-set-20260823-for-ap.md` 에 정정 기록.

### U1 EXPOSURE CONTRACT (학습 전 freeze, TOTAL 13,554)

```
arm            synthetic  positive  negative
M00 / N0 / P0     13,554         0         0
N1                12,000         0     1,554   (259 × 6)
P1                10,414     3,140         0   (157 × 20)
M11 (future)       8,860     3,140     1,554
```

### U3 REPLAY POOL

namespace `G38_ADAPT_REPLAY_SEED42`. G38 generic pool 38,002 을
`sha1(namespace|filename)` 오름차순 정렬 후 앞에서 K 개 — 작은 셋이 큰 셋의
**부분집합(nested)** 이 되도록 했다. V2/C43·OLD target synthetic 미사용.

### U5 N0/N1 스캐너 (실측)

```
arm                    train  synthetic  negative  empty-label  batches/ep  val
adapt_n0_control      13,554     13,554         0            0         424  1,998
adapt_n1_negative     13,554     12,000     1,554        1,554         424  1,998
EXPOSURE_MATCH True   BATCHES_MATCH True   빈 라벨 계약 OK
```

### U6 RECIPE — OLD FT actual args 가 source of truth

`runs_ft/ft_a_real157_neg259_synth12k/args.yaml` 대조 결과 지시된 기대값과 전부 일치
`[확인]`: SGD / lr0 0.002 / lrf 0.01 / cos_lr / warmup 1.0 / mosaic 0.15 /
close_mosaic 10 / batch 32 / imgsz 640 / fliplr 0 / scale 0.25 / erasing 0.4 / seed 42.
FAST SCREEN 은 epochs 15, patience 0, primary = epoch15 `last.pt`.

### NEG_SCREEN_GATE_PREREG.json (학습 전 freeze)

hits 6 개 중 **2 개** 필요 + positive guard 3 개 전부 통과해야 POSITIVE.
guard 하나라도 실패하면 HARM. 결과 보고 threshold 변경 금지.

### U4 WINDOWS 패키지

```
/home/minjae/Desktop/G38_POSITIVE_SCREEN_TO_WINDOWS_20260824T215739.zip
sha256 1f3f6126ff2655dd37d5ac5bfcfed508af63ce6e82f8f16d3fcb694ecccfe854
161.07 MB · 325 files · positive unique 157 · READY
```

synthetic RGB 는 넣지 않고 **membership ID + 200 개 spot-check sha256**(전수 아님을
명시)만 넣었다 — Windows 가 자기 G38 generic pool 에서 해석하고 동일성부터 확인한다.
real evaluation 128 장은 **의도적으로 미포함**.

### 착오 (기록)

`nohup` 으로 드라이버를 띄워 detach 시켰다 — 알림 경로가 끊기는 금지 패턴
(memory `background-launch-must-stay-harness-tracked`). 부모만 죽이고 고아가 된
smoke 자식은 죽이지 않고 1ep 완주시킨 뒤, 하네스 추적으로 재기동해 이어받게 했다.
