# 2026-08-11

## [12:2x] 사전등록 — constrained F50 line-adapter screen (실행 전 lock)

### 0. 이전 4 commit push
`git log origin/main..HEAD` = 정확히 4개(080affe·ddc77e3·e9da247·58379ca) 확인 후 push.
local HEAD == origin HEAD == `58379ca` **[확인]**. push 전 수정·rebase·squash 없음.

### 1. scope addendum (기존 결과·commit 무수정)
`DIRECT_HOUGH_ADAPTATION_SCOPE_ADDENDUM.md` 신규. standing 확정:
```
Phase A  LONG_SCHEDULE_STILL_OPTIMIZING_BUT_TASK_FAIL
         LOCKED_3X_EXPOSURE_DID_NOT_RESCUE / OPTIMIZATION_REMAINS_ACTIVE
Phase B  LATE_A1_FEATURE_ADAPTATION_SIGNAL
```
**철회·금지**: "optimizer exposure is not a bottleneck"(측정은 1회 3배 tripling 뿐, 축 전체 아님) ·
"frozen A1 explains 45% of the error"(제약 제거로 얻은 개선 ≠ 오차 분해) ·
`FROZEN_A1_FEATURE_LIMIT_CONFIRMED` · `ROLE_ENCODER_CAPACITY_LIMIT_CONFIRMED`(screen 자체를 안 돌림).
다음 질문 = `CAN_CONSTRAINED_LINE_SPECIFIC_FEATURE_ADAPTATION_KEEP_THE_GAIN_WITHOUT_UNFREEZING_A1?`

### 2~5. 신규 runner + architecture
`scripts/stage0/direct_hough_f50_adapter_screen.py` (기존 direct_hough_*·late_a1_* 무수정).
```
RGB → frozen A1 → F50 → zero-init residual adapter → F50' → token XY → 동일 role encoder → 동일 head
adapter: Conv1x1 128→32 · ReLU · Conv3x3 32→32(pad1) · ReLU · Conv1x1 32→128
         F50' = F50 + alpha*Adapter(F50),  alpha = learnable scalar, init 0
bottleneck 32 고정(4:1). 16/64/128 sweep·depth 추가 금지. factor = CONSTRAINED_F50_LINE_ADAPTER 하나
```
parameter audit (코드 계산, 하드코딩 아님 — 테스트가 shape 에서 재유도해 대조):
```
adapter 17,601 | role encoder 55,041 | DirectHoughHead 8,704 | A1 trainable **0**
```

### 6~8. preflight 3종 전부 PASS **[확인]**
```
STEP-0 등가   logits 0.000e+00 · descriptors 0.000e+00 (tol 1e-6) · base checksum 동일
              → build_pair 가 base model 2개를 먼저 만들고 state_dict 복사 후 adapter 를 나중에
                생성해, adapter 의 RNG 소비가 encoder/head 초기화를 흔들지 못하게 함
gradient wiring  alpha grad 2.372e-04 > 0 → 1 step 후 alpha 1.000e-03 ≠ 0
              first conv grad 3.248e-08 · last conv 4.290e-07 (양쪽 다 > 0)
              **A1 grad 보유 param 0개, A1 trainable 0**
memory smoke  batch 8 그대로, peak 1,173.4 MiB / 9,853 MiB → OK (batch 축소 없음)
```

### 9~16. 학습·판정 설정
A1 완전 frozen · adapter/encoder/head 모두 기존 LR 1e-3 · WD 기존 · scheduler 없음 · clipping 없음 ·
LR sweep 금지. FULL LINE_TRAIN 13,618 · batch 8 · 25,545 step ·
marks 1703/5000/8515/17030/25545 · **decision 25,545 only, best-step 선택 금지**.
primary baseline = Phase A **F0** @25,545 (`direct_hough_long.json` full precision 로드, 하드코딩 금지).
Phase B **F1** 은 context 전용 — selection 에 미사용(테스트가 AST 로 강제).
gate 1.0/0.5, safety 2.0/1.0, relative = F0 대비 양축 40% 감소.
진단(선택 미사용): D2/D0 ratio · adapter relative L2 / cosine / alpha / output norm ratio /
param delta norm · per-role 12개 · tails.

판정 라벨 C1~C5 사전 고정. C4(`F50_ADAPTER_SPECIALIZES`)와 Pareto 기록은 진단이며
hard decision 은 C1/C2/C3 그대로. C5(NaN)면 LR 수정 재시도 금지.
`ROLE_ENCODER_DEPTH_SCREEN` 은 이번에 **파일도 만들지 않음**(테스트가 부재를 단언).

테스트 `challenge/tests/test_direct_hough_f50_adapter_screen.py` 24 pass. 전체 831 passed.

## [14:3x] 결과 — F50_ADAPTER_INSUFFICIENT (C3)

**decision @25,545 D2 (raw JSON 재계산)**:
```
                angle med   offset med   angle p90   offset p90   gate
F2 adapter       3.084991     1.554134   17.584229     6.242823   0/4 FAIL
F0 frozen        3.735687     1.972937   27.843582     8.279793   0/4 FAIL
F2 vs F0          +17.42%      +21.23%     +36.85%      +24.60%   40% 기준 미달 → C3
```
F2 ladder (alpha / relL2 / cos / D2·D0 비):
```
 1,703  D0 9.4527/5.1153 | D2 10.2486/5.2875  CE 8.274255  a+0.00685 relL2 0.02104 cos 0.999773  1.084/1.034
 5,000  D0 4.9934/2.5619 | D2  4.8882/2.5544  CE 7.065881  a+0.06212 relL2 0.19488 cos 0.980748  0.979/0.997
 8,515  D0 3.6443/1.8893 | D2  3.6727/1.9238  CE 6.540333  a+0.07340 relL2 0.23244 cos 0.972703  1.008/1.018
17,030  D0 3.1683/1.5437 | D2  3.3022/1.5929  CE 6.190232  a+0.07495 relL2 0.26676 cos 0.964224  1.042/1.032
25,545  D0 2.9535/1.4435 | D2  3.0850/1.5541  CE 6.006698  a+0.07404 relL2 0.29731 cos 0.955988  1.045/1.077
tail @25,545  >5° 0.3420 · >10° 0.1689 · >2cell 0.4042   n=5,921   finite=True (NaN 없음)
```
- @1,703 은 F0 보다 **약간 나쁨**(10.2486 vs 9.7985) — zero-init 이라 alpha 가 0 에서 커져야 하므로 정상.
  @8,515 에 이미 F0 **최종값** 초과(5 pass vs 15 pass). 이후 평탄화(마지막 5 pass 에서 6.6%/2.4%).
- ★ **alpha 만 보면 오독**: alpha 는 8,515 이후 정체(0.07340→0.07495→0.07404, 끝엔 감소)인데
  relL2 는 계속 상승(0.232→0.267→0.297), cos 계속 하강. **gate 는 멈췄고 body conv 가 계속 자람.**
  adapter delta norm 21.63. 즉 "붙었는데 안 쓰였다"가 아니라 **F50 을 30% 바꾸고도 17~21% 밖에 못 삼.**
- ★ **특화 없음**: D2/D0 = F2 1.045/1.077, F0 1.069/1.024, **F1 1.426/1.299**. F2 는 전 구간 0.98~1.08 로
  F0 쪽에 붙어 있음. `SPECIALIZES=False`(D0 만 좋아지고 D2 정체하는 형태 아님, 둘이 같이 이동).
  → **unfreeze 에 따라온 42.5% 격차는 line supervision 의 본질이 아니라 5M unfreeze 의 산물.**
  단 진단이며 "gap 작다=generalization 해결" 로 쓰지 않음 (F2 는 F1 보다 모든 지표에서 나쁨).
- F1 대비(context only, selection 미사용): angle −49.02% · offset −44.26% · p90 −81.24%/−51.32%
  → `PARETO_BETTER_THAN_LATE_UNFREEZE=False`. F1 이 네 지표 모두 우세.
- per-role: 2/4/6/7 high-angle 그룹 F0 5.864 → F2 4.455(−24%) → F1 2.665(−55%),
  3/9/10 그룹 2.334 → 2.086(−11%) → 1.684(−28%). 계열 내내 살아남은 분화가 이번에도 유지.
  role 10 offset 만 F0 대비 미세 악화(1.765→1.774).
- **금지 표현 준수**: "adapter 실패가 더 넓은 unfreeze 의 필요성을 증명한다" **쓰지 않음**.
  미확정: adapter 용량이 구속 조건인지도 미확정(alpha 가 멈춘 채 body 가 자란 건 용량 소진 형태 아님).
  한계: adapter shape 1종·bottleneck 32 단일·LR 단일, 어느 것도 sweep 안 함.

**Standing**: late-A1 signal 유지. NEXT=`ROLE_ENCODER_DEPTH_SCREEN`(미구현, 파일도 없음).
architecture **NOT_LOCKED**(4조건 중 0), CIGM/PnP/dimensions/K-pose **BLOCKED**.
산출 `f50_adapter.json`, 문서 `F50_ADAPTER_RESULT.md`.

## [14:5x] 사전등록 — role-encoder depth screen (실행 전 lock)

### 0. adapter screen 2 commit push
`git log origin/main..HEAD` = 정확히 2개(0320e9a·a6c987c), diff 는 adapter screen 5개 파일뿐.
working tree 의 이전 세션 잔여 변경(EVAL_SET_CANONICAL.md 등)은 **commit 에 미포함** 확인 후 push.
local HEAD == origin HEAD == `a6c987c` **[확인]**. rebase/squash/amend 없음.

### 1. F2 scope addendum
`F50_ADAPTER_SCOPE_ADDENDUM.md` 신규 (기존 F2 결과 무수정).
established = adapter 실제 동작(alpha 0→0.07404, F50 29.7% 변화, delta norm 21.63) ·
F0 대비 +17.42%/+21.23% · 40% 미달 · **broad late-A1 의 큰 D0/D2 gap 이 F2 에서 재현 안 됨**.
**not established** = broad unfreeze 의 **파라미터 수 자체**가 특화 원인 / 특화 해결됨 /
adapter 용량 소진 / frozen A1 충분 / **role encoder capacity 가 병목**(← 이번에 검증할 대상이지
사전 확정 아님).

### 2~5. 신규 runner + 두 번째 block
`scripts/stage0/role_encoder_depth_screen.py` (adapter/late_a1/role_heatmap 무수정).
```
R0 = F2 그대로
R1 = R0 + role-query refinement block 1개
     LN(query) → MHA(q=z_r, kv=동일 F50+XY tokens, heads 4) → residual
     → LN → FFN 64→128→64 → residual
     z_out = z_in + beta * DeltaBlock(z_in, tokens),  beta = learnable scalar, init 0
dim 64 / heads 4 / FFN 128 고정. 2·3·4 block sweep 금지
```
★ **새 query embedding 만들지 않음** — 첫 block 의 12 descriptor 를 그대로 refine.
role assignment·matching·Hungarian **전부 없음**, channel k = role k 유지
(테스트가 Embedding 부재 + permutation equivariance 로 강제).

### 9. parameter audit (코드 계산)
```
F50 adapter 17,601 | 기존 role encoder 55,041 | 새 block 33,473 | head 8,704 | A1 trainable 0
trainable 81,346 → 114,819  (+41.1%)
```

### 6~8, 10. preflight 4종 전부 PASS **[확인]**
```
canonical init  base 2개 + adapter 2개 먼저 생성·state 복사 → block 을 **마지막에** 생성
                shared checksum 동일
STEP-0 (hard)   adapted F50 0.000e+00 · first descriptor 0.000e+00 (둘 다 exact 요구)
                final descriptor 0.000e+00 · logits 0.000e+00 · loss 0.000e+00 (tol 1e-6)
wiring          beta grad 7.958e-04 > 0 → 1 step 후 1.000e-03 ≠ 0
                new attn 2.892e-06 · new ffn 3.203e-05 · adapter 3.250e-08 ·
                first block 3.081e-03 · head 1.543e-01 (전부 > 0), **A1 grad 0**
memory          batch 8 유지, peak 1,173.2 / 9,853 MiB
```

### 11~19. 학습·판정
전 파라미터 동일 LR 1e-3·WD 기존, scheduler/clipping 없음, LR sweep 금지.
FULL 13,618 · batch 8 · 25,545 step · marks 1703/5000/8515/17030/25545 ·
**decision 25,545 only**. baseline = `f50_adapter.json` @25,545 D2 full precision.
F1 late-A1 은 context only — AST 테스트가 reduction test·DECISION 분기 진입을 차단.
진단(선택 미사용): D2/D0 비 · beta · descriptor 상대변화 · cosine · **attention entropy** ·
FFN norm ratio · per-role 12개 · tails. attention 인과 주장 금지.

판정 D1~D5 사전 고정. **D3 면 "role encoder capacity is not the bottleneck" 금지** —
`ROLE_ENCODER_CAPACITY_EXONERATED=False` 와 SCOPE("이 폭·head 수의 block 1개 한정")를
결과 JSON 에 강제 기록. D5(NaN)면 LR 변경 재시도 금지.

★ 테스트 교훈: sealed 토큰 검사가 docstring 의 "modules untouched" 산문에 걸림 →
`non_docstring_literals()` 로 docstring 제외. 산문을 고치는 게 아니라 검사를 고침.

테스트 21 pass. 전체 856 passed.

## [17:1x] 결과 — ROLE_ENCODER_DEPTH_INSUFFICIENT (D3)

**decision @25,545 D2 (raw JSON 재계산)**:
```
                angle med   offset med   angle p90   offset p90   gate
R1 +block 1개    2.909729     1.230865   18.133209     5.730148   0/4 FAIL
R0 = F2          3.084991     1.554134   17.584229     6.242823   0/4 FAIL
R1 vs R0          +5.68%      +20.80%      -3.12%       +8.21%   40% 미달 → D3
```
angle p90 은 **R0 보다 3.12% 악화** — 유일하게 뒤로 간 지표.
R1 ladder:
```
 1,703 D0 9.4257/4.3904 | D2 9.9299/4.5180  CE 8.142158  b-0.17844 dchg 0.69409 cos 0.8899 ent 5.9350
 5,000 D0 4.5625/2.1673 | D2 4.5287/2.1558  CE 6.868146  b-0.35489 dchg 0.99279 cos 0.8081 ent 6.2446
 8,515 D0 3.5804/1.7974 | D2 3.6834/1.8090  CE 6.384469  b-0.41546 dchg 1.09381 cos 0.7776 ent 6.1545
17,030 D0 3.1510/1.4122 | D2 3.1970/1.4386  CE 6.004488  b-0.43162 dchg 1.38556 cos 0.7095 ent 5.8724
25,545 D0 2.7814/1.1970 | D2 2.9097/1.2309  CE 5.792634  b-0.42030 dchg 1.59989 cos 0.6623 ent 5.7646
tail @25,545  >5° 0.3285 · >10° 0.1670 · >2cell 0.3383   n=5,921   finite=True
```
- ★ **block 은 최대치로 작동했다**: 최종 dchg **1.59989** = block 기여 norm 이 원 descriptor 의 1.6배,
  cos 0.6623. "안 켜졌다" 가 아니라 **표현을 통째로 다시 쓰고도 angle 5.68%** 를 벌었다.
  beta 부호는 gate·block 출력이 부호를 흡수하므로 해석 대상 아님 — 크기만 읽음.
- ★ **adapter 와 동일한 형태 재현**: beta 는 8,515 이후 정체(-0.41546→-0.42030, 끝엔 반전)인데
  dchg 는 1.09→1.60 계속 상승. **gate 만 보면 두 컴포넌트 다 "포화" 로 오독**했을 것.
- 특화 없음: D2/D0 = R1 1.0461/1.0283, R0 1.0445/1.0766, F1 1.4255/1.2994 → `SPECIALIZES=False`.
  진단이며 "gap 작다=generalization 해결" 로 쓰지 않음(R1 은 F1 보다 4지표 모두 나쁨).
- F1 대비(context only): angle −40.55% · offset −14.25% · p90 −86.90%/−38.89% → Pareto **False**.
- per-role: offset 은 12개 **전부** 개선. angle 은 **role 2(4.524→4.664)·6(4.068→4.234) 악화**,
  둘 다 high-angle 그룹. 그룹 평균 high 4.455→4.393(−1.4%) vs lower 2.086→1.871(−10.3%)
  = **이미 문제가 작던 쪽만 개선**. 계열 내내 살아남은 분화를 압축하는 건 여전히 broad unfreeze 뿐.
- attention entropy 5.76~6.24 (균일 ln2500=7.82), FFN norm ratio 3.28~4.26 — 기록만,
  "attention 이 어디를 본다" 주장 없음.
- **금지 표현 준수**: "role encoder capacity is not the bottleneck" **쓰지 않음**.
  JSON 에 `ROLE_ENCODER_CAPACITY_EXONERATED=False` + SCOPE("이 폭·head 수의 block 1개 한정") 기록.
  용량 소진도 미확정(beta 정체 + body 성장 = 소진 형태 아님).

### ★ 3개 screen 에서 반복된 형태 (설명은 이 screen 의 일이 아님 — 기록만)
```
컴포넌트 추가 → 자체 진단상 강하게 작동(adapter F50 30% 변경 / block descriptor 160% 재작성)
→ train CE 개선 → **dev median 은 필요한 폭의 절반에 한참 못 미침**
```

**Standing**: architecture **NOT_LOCKED**. replicate·role shuffle·whole LINE_DEV 전부 **BLOCKED**(D1 아님).
CIGM/PnP/dimensions/K-pose BLOCKED. NEXT = A(broader feature adaptation)/B(joint feature+role
regularization)/C(different structural decoder) 중 **별도 lock 에서 결정**, 이번에 셋 다 미구현.
산출 `role_depth.json`, 문서 `ROLE_ENCODER_DEPTH_RESULT.md`.
