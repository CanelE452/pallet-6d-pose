# 2026-09-06

## 평가 solver 교체 — SQPnP+RefineLM → 미분가능 Gauss-Newton PnP (solver_swap_v1)

논문 pose 평가의 read-out solver 만 바꾼 병렬 경로를 만들었다. 학습 0 step, 새 추론 0 회.
정본 `metric_split_lock.md` §3.1 의 SQPnP LOCK 과 기존 `POSE_EVALUATION_*.json` 은
건드리지 않았다.

### 새 파일

- `scripts/paper/pose_metric_closure_v1/diffpnp_eval_solver.py` — 미분가능 GN PnP.
  투영·Jacobian 은 `Deep_Object_Pose/train/diffpnp3d_loss.py` 규약을 옮겼고, 회전 갱신만
  Jacobian 의 좌측 섭동에 맞춰 `R <- Exp(w) R` 로 정정했다(원본은 rvec 가산 근사).
  고정 step unrolled(accept/reject 분기 없음) — 그래야 2D 입력으로 grad 가 흐른다.
- `scripts/paper/pose_metric_closure_v1/test_diffpnp_eval_solver.py` — 게이트 1. 5 tests pass.
- `scripts/paper/pose_metric_closure_v1/evaluate_pose_solver_swap.py` — 드라이버.
  평가 → 게이트 → 판정 → 보고서까지 한 스크립트에서 끝난다. 76s.
- `_docs/notes/pnp-solver-swap.md` — 착수 문서 + 결과.
- `data/pallet/results/paper_pose_metric_closure_v1/solver_swap_v1/`
  — `PURPOSE.md`, `SOLVER_SWAP_METHOD_LOCK.json`(실행 전 고정),
    `SOLVER_SWAP_RESULTS.json`, `SOLVER_SWAP_REPORT.md`.

기존 파일 수정 0건.

### 결과

GATE 0(S0 이 정본 재현) PASS — 7 arm × 7 키 전부 1e-9 이내. 따라서 D arm 차이는 solver 만의 것.
R0 기준 D1_GN_LS 는 rot 2.262→2.296°(+1.48%), IoU3D 0.6032→0.6000, ADD AUC 0.4285→0.4272.
D2/D4(robust·confidence 가중)는 translation 을 당기고 rotation 을 깨는 같은 맞교환.
D3(SQPnP init + GN)은 최대 상대변화 3.67e-08 = 변화 없음.
판정 = solver 교체로 얻는 것 없음, SQPnP LOCK 유지. 분석은 `_docs/notes/pnp-solver-swap.md`.

### 함정 두 개 (둘 다 이번에 한 번씩 걸렸다)

1. **`arccos((tr-1)/2)` 는 0 근방에서 1.2e-6도 미만을 못 잰다.** float64 에서
   sqrt(2·eps)=2.1e-8 rad 이 최소 눈금이라, 기계정밀도로 맞은 회전이 "1.207e-6도 오차"로
   찍힌다. 이걸 solver 수렴 실패로 오독해 damping·step 을 의심하는 데 한 사이클 썼다.
   실제로는 Frobenius 1e-16. 0 근방 회전오차는 `atan2(||skew||/2, (tr-1)/2)` 로 잴 것.
2. **사전등록 게이트에 '의미 있는 차이의 하한'이 없으면 1e-9 차이가 개선으로 센다.**
   D3 가 그래서 ACCEPT 로 찍혔다. 규칙을 사후에 고치지 않고 산출물에
   `indistinguishable_from_baseline` / `effective_reading` 필드를 추가해 구분되게 했다.


## 학습측 DiffPnP — YOLO pose loss 에 미분가능 PnP 항 추가 (diffpnp_yolo_v1)

사용자 지시로 `SOLVER_LOSS_TRACK CLOSED` 를 다시 열었다. 평가측 교체(solver_swap_v1)가
REJECT 였으므로 남은 갈래인 학습측을 같은 기준으로 판정한다.

### 새 파일

- `pallet_yolo_loss/diffpnp.py` — `DiffPnPPoseLoss26`. 예측 2D 로 배치 GN PnP 를 풀고
  참조 pose(=GT 2D 로 푼 PnP)의 카메라 좌표 코너와 Huber 비교. `lambda_dp=0` 이면 항을
  계산조차 하지 않아 부모와 구성적으로 동일.
- `pallet_yolo_loss/model.py` / `trainer.py` — `DiffPnPPoseModel` · `DiffPnPTrainer` 추가
  (기존 PSPC 패턴 그대로, 기존 클래스는 건드리지 않음).
- `scripts/research/diffpnp_yolo_v1/` — `build_diffpnp_index.py`(사이드카),
  `smoke_train.py`, `q0_grad_band.py`, `run_screen.py`(학습→추론→평가→판정→보고), `PURPOSE.md`.
- `challenge/tests/test_diffpnp_yolo_loss.py` — 게이트 7 tests.
- `data/pallet/results/diffpnp_yolo_v1/` — 인덱스 npz + stems + META,
  `DIFFPNP_SCREEN_METHOD_LOCK.json`(실행 전 고정), 스모크 리포트, `Q0_GRAD_BAND.json`.
- `_docs/notes/diffpnp-yolo.md` — 계열 문서.

`pallet-yolo26` env 에 `pytest` 설치(개발 도구, 학습 수치와 무관).

### 사이드카 인덱스

학습셋 60,000 프레임(train 55,980 + val 4,020) 전부 인덱싱, 실패 0. 소스 라벨에서
K·world-frame cuboid·projected_cuboid 를 읽고 참조 pose 를 SQPnP+RefineLM 으로 계산.
세 스트림(G38 38,002 / P0 8,989 / TEX 8,989) 모두 GT 2D·3D·K 재투영 오차 **0.0000 px** 로
대응 사슬이 정확함을 확인.

### 배선에서 실제로 걸린 것 (상세는 notes)

- YOLO 라벨 = `projected_cuboid` 원본 순서 + pad(100,100), centroid 가 index 8.
  이 학습셋은 perm_v4 미적용 — `broad40k_fixed` 와 다르다.
- 증강 대응: 영상면 affine A 에 대해 `K' = A K` 로 보정. A 는 소스 2D 와 배치 GT 2D
  대응에서 최소제곱 복원.
- `fliplr=0.5` 이 keypoint 인덱스를 `flip_idx` 로 바꾼다 → 두 대응 다 풀고 잔차 낮은 쪽
  채택. 감독률 55% → 98.9%.
- mosaic 은 `im_file` 을 대표 1장만 남겨 3/4 인스턴스가 어긋난다 → affine 잔차 게이트로
  마스킹. R0 레시피(mosaic 켬) 감독률 19.4%.
- ★`E2ELoss` 는 `__call__` 이 아니라 `loss()` 를 부른다. `__call__` 만 덮었더니 항이 한 번도
  계산되지 않았다(스모크 통계 0). 둘 다 덮어 one2many·one2one 양쪽 적용 확인.
- 정규방정식 절대 릿지(1e-8)가 affine 해에 편향을 남김 → 상대 릿지 + Hartley 정규화로
  잔차 3.6e-6 → 1e-6 미만.

### Q0 (lambda 선택)

값 비율이 아니라 `pred_kpts` 기울기 노름 비율로 선택. λ=1 에서 4.5%(목표 5%), 5% 대응
λ 중앙값 1.13, IQR 0.079~3.78(산포 48배). λ\*=1.0 채택.

### 스크린 (진행 중)

대조 λ=0 vs 처치 λ=1.0, 10 epochs · batch 32 · seed 42, 그 외 R0 레시피 동일.
드라이버 평가 함수가 기존 R0 예측에서 정본 수치를 오차 0 으로 재현함을 먼저 확인(하네스 유효).
백그라운드 실행 중, 결과는 `DIFFPNP_SCREEN_RESULT.json`.

### 스크린 결과 — REJECT (개선 0/4)

```
지표                        대조 λ=0    처치 λ=1     상대변화
rotation median (deg)        2.6139      3.1928      +22.15%
translation median (cm)      6.9523      8.5838      +23.47%
IoU3D median                 0.6018      0.5573       -7.40%
symmetry-aware ADD AUC       0.4230      0.3830       -9.44%
axis accuracy                0.7398      0.7147       -3.39%
```

두 arm 다 10 epoch 완주(results.csv 11줄, best/last 존재), 추론 no_detection 0/319.
학습 시간 4,589s vs 5,144s(+12%) = 항이 실제로 돈 방증.

★ 진단: **합성 val 은 구분이 안 된다** — pose mAP50-95 0.91969 vs 0.91953(-0.02%).
합성에서 동일한데 실제에서만 7~23% 나빠진다. `paper-s2-six-screens-all-reject-final-base-dope`
의 공통 벽과 같은 모양.

판정에 따라 60 epoch full 확대는 하지 않는다(중단 기준). 분석은 `_docs/notes/diffpnp-yolo.md`.

## GT 신뢰도 감사 (accuracy_root_cause_v1) — 정본 140장, 코드·artifact 실측

기존 GT JSON 미수정 · 학습 없음 · 새 어노테이션 없음. `scripts/annotate/audit_gt_data.py`
의 함수를 import 해서 돌렸다(복제 아님).

새 파일
- `_docs/audits/accuracy_root_cause_v1/GT_TRUST_AUDIT.md` (판정 A~F + 근거)
- `data/pallet/results/accuracy_root_cause_v1/` 아래 8개
  (`GT_TRUST_PROBE_SUMMARY.json`, `GT_TRUST_PROBE_FRAMES.csv`,
   `GT_DEPTH_CROSSCHECK.json`/`_SAMPLES.csv`, `GT_DEPTH_NEARSURFACE.json`,
   `GT_WD_AXIS_IDENTIFIABILITY.json`, `GT_WD_IDENTIFIABILITY_CLICKONLY.json`,
   `GT_WD_IDENTIFIABILITY_CLICKONLY_STRATA.json`)

확정된 사실 (전부 [확인])
- 정본 = **140장**(22/12/18/27/33/12/16). 디스크 전수 집계 + `test_eval_set_canonical.py`
  8 passed 로 일치. quarantine 21장은 `_archive/real_gt_invalid_20260827/` 로 실제 이동됨.
  **CLAUDE.md 의 "161장 (22/12/22/27/36/17/25), 실측·테스트 선언값 모두 161" 은 stale**
  — `_docs/EVAL_SET_CANONICAL.md` 는 이미 140 으로 갱신돼 있다.
- 저장 `pose_transform` = 저장 2D 점 + `dimensions_m` 의 PnP 해 그 자체.
  재풀이와의 차이 R p50 0.000°(max 0.181°), t p50 0.000 m(max 0.001 m). 독립 계측 아님.
- `projected_cuboid` 는 클릭·PnP투영·sentinel 이 섞인 배열이고 구분 필드가 없다.
  `manual_kps` 는 139/140 에서 `projected_cuboid` 와 완전 동일(소수 좌표 포함) —
  이름과 달리 클릭 전용이 아니다. 코너별 visibility 필드 없음 →
  `TARGET_VISIBILITY_NOT_IDENTIFIABLE`.
- 프레임당 클릭 코너 중앙값 5/8. 코너 6·7 은 각각 136·131장이 외삽이라 저장 pose 대비
  거리 p50 = 0.00 px (평가하면 PnP 모델과의 일치도를 재는 셈).
- ★**W/D(yaw 90도) 분기는 클릭만으로 보면 근거가 약하다** — 외삽 포함 시 margin p50
  7.73 px·스왑 우세 0/140 인데, 클릭만 쓰면 p50 1.39 px·<1px 51/130·스왑 우세 9/130.
  n_click>=6 (44장) 에서는 p50 3.31 px·스왑 우세 0 → **증거량 문제**.
  같은 순환이 `build_geometry_resolved_pose_gt.py`(외삽점 포함해 두 가설 비교)에도 있다.
- depth 는 128/140 프레임에서 ID 매칭으로 존재(`data/pallet/raw_data/*/depth`). 근면 하단
  코너 2·3 에서 depth − pose_camz 중앙값 −0.017/−0.064 m → **거리 스케일은 독립 확인됨**.
  AprilTag 기반 정본 GT 는 0장(apriltag 243장은 전량 사용금지 `pallet11_gt`).
- GT repeatability 정보 없음 — 재어노테이션 표본은 quarantine 의 stale duplicate 2건뿐.

미해결/제안: depth 로 W/D 분기를 푸는 판별자, 클릭 4~5개 프레임 보강, 세션 단위 W/D
일관성 제약, 재어노테이션 20장 2회로 GT 산포 실측. (전부 [미검증])

## HOUGH_IMPLEMENTATION_AUDIT (accuracy_root_cause_v1)

- 신규: `_docs/audits/accuracy_root_cause_v1/HOUGH_IMPLEMENTATION_AUDIT.md`
- 코드 정적 추적만. 학습·추론 실행 0, 코드 수정 0.
- 판정 4줄: LINE_FEATURE_AGGREGATION_ALONG_SEGMENT=NO(Direct-Hough 기준) /
  LINE_TARGET_VISIBILITY_IDENTIFIABLE=NO(`TARGET_VISIBILITY_NOT_IDENTIFIABLE`) /
  HOUGH_TESTED_THE_EVIDENCE_ACCUMULATION_HYPOTHESIS=NO /
  DOWNSTREAM=교점(CIGM)→belief residual(EGCR)→기존 point `cv2.solvePnP`.
- 핵심: line 코드는 네 계열이고 서로 다른 것을 한다. 계열 C(Direct-Hough,
  현행 adaptive_hough_g38·model_compare·conditional_hough 전부)는 raster 를
  **설계상 제거**해 누적이 없다. 누적은 계열 B(`CoarseRadon`, `@no_grad` 밖으로
  못 나감)와 계열 D(DGP `line_energy`, numpy optimizer)에만 있고 둘 다 학습 안 됨.
- 따라서 `HOUGH_TRACK_CLOSED`·`HOUGH_INCREMENTAL_VALUE_NOT_ESTABLISHED` 는
  누적 가설의 기각 근거가 아니다. 결론은 notes 가 아니라 이 audit 문서에 있음.

## 메인 세션 재검증 — 위임 감사 결과를 직접 재현 (accuracy_root_cause_v1)

위임 결과를 그대로 수용하지 않고, 판정을 바꿀 무게의 주장만 메인 세션에서 다시 계산했다.

- 신규: `scripts/research/accuracy_root_cause_v1/verify_gt_claims.py`
  (저장소의 `challenge.data_paths.EVAL_CANONICAL` 과 `annotate_pnp.make_pallet_keypoints_3d_diagram`
   을 import 해서 쓴다. solver 는 `cv2.SOLVEPNP_SQPNP`. GT JSON 미수정.)
- 신규: `data/pallet/results/accuracy_root_cause_v1/MAIN_SESSION_GT_RECHECK.csv` (270행)
- 추가: `_docs/audits/accuracy_root_cause_v1/DEPLOYMENT_GEOMETRY_AUDIT.md` 에 부록 절

### GT 주장 독립 재현 결과

```
                                    위임 보고        메인 재현
eval 프레임 수(폴더별)              140              140  (22/12/18/27/33/12/16)  일치
manual_kps == projected_cuboid      139/140          139/140                      일치
프레임당 클릭 코너 중앙값           5/8              5/8  {2:1,3:9,4:13,5:73,6:43,8:1}
W/D margin (외삽 포함)              p50 7.73 px      p50 9.144 px · 스왑우세 0/140
W/D margin (클릭만)                 p50 1.39 px      p50 1.480 px · 스왑우세 5/130
  그중 |margin| < 1px               51/130           51/130                       일치
W/D margin (n_click>=6, 44장)       p50 3.31 px      p50 6.936 px · 스왑우세 0
```

크기는 solver 차이로 갈리지만 **방향과 결론은 동일**하다 — 외삽점을 빼면 W/D(yaw 90도)
분기의 증거가 6배 가까이 줄고, 절반(51/130)은 두 가설 차이가 1 px 미만이다.
단 클릭이 6개 이상인 44장에서는 스왑 우세가 0이라 **GT 가 틀렸다기보다 증거량 부족**이다.
순환성 자체는 코드로 확정된다 — `annotate_io.py:518-521` 이 클릭이 없는 코너를
채택 pose 의 투영으로 채우므로, 그 점을 포함한 가설 비교는 항상 채택 가설을 지지한다.

`dimensions_m` 은 dict(width/depth/height) 이고 140장에서 두 값만 나온다 —
`{w1.1,d1.3}` 81장 / `{w1.3,d1.1}` 59장. 이것이 프레임별 축 배정의 실체다.

### 배포 기하 감사 재검증

확증 4(registry SHA·치수, 물체별 symmetry_status, C4 62/155 순열 수치, 배포 원점 계약) /
정정 1(phase 시간 안정성은 `COMPARISON.md` §F 에서 이미 측정됨 — F0 99.06%, F1 99.68%,
따라서 세션 상수 오프셋이지 프레임 jitter 가 아님) /
검증 불가 1(`geometry.py`·`pose6d_adapter.py` 는 이 저장소에 없음, `newauto` 소속) /
신규 2(SYMMETRY_CONTRACT 의 90도 배제 근거인 "X·Z extent 가 다르다" 가 정사각 배포 물체엔
성립 안 함 · `pallet_jetson_deploy/infer_fps.py:46` 이 배포 물체 아닌 논문 물체 치수 사용).

## 축 D(capacity) · 축 H(real supervision) 기존 증거 감사 — 학습 0 · 추론 0

새 파일 1개: `_docs/audits/accuracy_root_cause_v1/CAPACITY_AND_REAL_SUPERVISION_AUDIT.md`.
기존 파일 수정 0건.

핵심 판정:

```
LARGER_MODEL_MATCHED_COMPARISON_EXISTS       = YES (단 현재 R0 계보가 아니다)
LARGER_MODEL_REJECTED_UNDER_MATCHED_CONDITIONS = NO  (Wilcoxon p=0.1433, NOT_ESTABLISHED)
REAL_FT_IMPROVES_LOCALISATION                = YES (challenge 트랙, new-session)
REAL_FT_NEW_SESSION_GENERALIZATION_MEASURED  = YES(challenge FT) / NO(live_gt FT)
LEAVE_ONE_SESSION_OUT_FEASIBLE               = YES (square 28폴더/851, rect 21폴더/391)
LEAVE_ONE_OBJECT_OUT_FEASIBLE                = YES (3종, wood 45장뿐)
REAL_LABELED_FRAME_TOTAL                     = 1,530 (manual 만 1,287)
```

- yolo26n vs yolo26m 은 FT args diff 5줄(data/device/model/name/project)뿐이라 거의 matched.
  단 medium 은 DDP 2GPU, base 는 서버 학습이라 로그가 이 머신에 없다(epoch 수 UNKNOWN).
  현재 R0 는 데이터셋(g38_legacy_v1v2_p0_tex20k 55,980)·patience(0)·평가모집단(PAPER_EVAL 319)이
  전부 달라 matched 비교가 아니다. s/l 변종은 학습 run 0개.
- `runs_arch_baseline` 는 v8n/11n/26n 전부 nano — architecture family 이지 capacity 가 아니다.
  문서 자체가 "60ep·s/m 확대 없음" 을 명기.
- memory 의 live-gt "pose 0.35 -> 0.98" 은 `_prepare_live_gt.json` 상
  `split_mode: interleave, val_every: 6` = **same-session memorization** 이었다.
  촬영단위 split 은 v1 뿐이고 그건 ultralytics 기본 aug 와 교락돼 있다
  (촬영단위 split + base-contract aug 조합은 미실행).
- REAL_FT_V1(cross-object, PAPER_EVAL 319)은 학습 전 중단 — 라벨 26.4% LR 위반.
  live_capture_gt 는 그 사이 402 -> 851 로 늘었고 추가 449장 규약 감사는 저장소에 없다.
- R0 epoch 체크포인트 13개 + `paper_real_eval.py --weights` 로 재학습 없이
  real 수렴곡선을 뽑을 수 있다(capacity 착수 전 0 GPU-hour 사전 판단 가능).
- 실측 불일치 기록: `objects[0].split=="eval"` 프레임은 **140** 으로
  `data_paths.py` 의 EVAL_CANONICAL_TOTAL 과 일치하고 CLAUDE.md 의 "161" 은 quarantine 이전 수치.
  `data_paths.py` 주석의 capturenight01/03 "0 files" 도 stale(실측 6 / 20).

## self-training + DiffPnP 스크린 착수 (사용자 요청)

아직 안 해본 칸: SQPnP 기반 R0 를 PL 로 self-training 할 때 DiffPnP 를 같이 켠다.

### 새 파일

- `scripts/research/diffpnp_yolo_v1/build_selftrain_index.py` — self-training 데이터셋용
  사이드카. replay 는 기존 합성 인덱스 재사용, PL 은 **PL keypoint 에 SQPnP+RefineLM** 을
  걸어 참조 pose 생성. 1,683/1,699 인덱싱(replay 1,440 + PL 243).
  PL 참조 재투영 median 2.00px / p90 4.23px.
- `scripts/research/diffpnp_yolo_v1/run_selftrain_screen.py` — Q0 → 학습 2 arm → 추론 →
  평가 → 판정 → 보고. `run_screen.py` 의 infer/evaluate/verdict 재사용.
- `data/pallet/results/diffpnp_yolo_v1/SELFTRAIN_DIFFPNP_METHOD_LOCK.json` — 실행 전 고정.
- `.../selftrain_R5_PROPOSED/` — 인덱스 npz + stems + META.

### 확인한 사실

- PL 세션 7개 전부 `cam_K.txt` 존재 → 실제 프레임 K 확보 가능.
- PL 라벨로 SQPnP 가 풀린다: 재투영 median 2.8px(초기 40장 표본), depth 3.8m.
- `train.txt` 2,880줄 = PL 1,440 + replay 1,440 → 매 배치 절반이 PL.
- Q0(이 설정): λ=1 기울기 비율 0.349, 5% 대응 λ 중앙값 **0.1432**(IQR 0.112~0.200),
  배치당 감독 110.5개, corner 오차 0.075. 합성 스크린(감독 15개·IQR 48배)보다 훨씬 건강.

### 한계 (기록)

PL 참조 pose 는 교사 자신의 좌표에서 읽은 것이라 새 정보를 주지 않는다. 이 항의 기전은
2D 오차를 pose 영향도로 재가중하는 것이다 — lock 에 명시.

---

## source ↔ real 조건 격차 감사 (SOURCE_REAL_GAP_AUDIT)

새 파일 1개. 학습·추론·synthetic 생성 0회, 기존 라벨/manifest/이미지만 읽었다.

- `_docs/audits/accuracy_root_cause_v1/SOURCE_REAL_GAP_AUDIT.md` (신규)
  — `FAILURE_DECOMPOSITION.md` 가 남긴 "Layer D 의 원인" 질문 중 source 분포 축을 닫는다.

측정 대상: R0 학습셋 55,980 전수(generic 38,002 + legacy P0 8,989 + TEX 8,989) ↔
`PAPER_EVAL_ALL_POS` 319 positive. 라벨 필드 산술 + grayscale 통계만 사용.

핵심 수치(상세는 문서):
- `UNIQUE_MESH_COUNT_IN_SOURCE = 4`. 프레임별 `pallet_shape_ratios` 는 40,000/40,000 이
  서로 다르지만 그 아래 topology 는 4개 — 스케일 랜덤화는 mesh 다양성이 아니다.
- 저앙각 marginal 은 이미 맞다(real <8° 28.2% vs R0 29.0%). 격차는 결합에 있다 —
  `elev<8 AND 두께<=0.0923` 셀의 유효자산수 1.01, `elev<8 AND 대형 AND thin` 은 1.00.
  4자산 균등 노출(유효 4.00)은 앙각 >=30° 구간에만 있다.
- real 실패는 앙각이 지배: NME 실패 <3° 51.9% / 3~8° 50.9% / >=30° 14.1%.
  야간 효과는 앙각 통제 후에도 남는다(DAY elev<15 38.4% vs NIGHT elev<15 50.0%).
- 야간 격차는 휘도가 아니라 대비/하이라이트다. 어두운 프레임은 source 가 더 많고
  (37.5% vs 23.8%), "어두우면서 대비>=68" 은 real 야간 62.3% vs source 8~10%.
  `scene_preset` 은 휘도를 전혀 가르지 않는다(day/indoor/night 모두 p50 53).
- 가림·truncation 은 source 가 real 보다 많다 → 격차 아님.
- generic 40,000 의 `camera_distance_actual_m` 최소 1.5001 m, <1.5 m 0건(렌더러 하드 바닥).
- hard negative: 데이터 격차 YES(source hard pair 95.2% duplicate vs real 75% distractor)
  이나 개입은 ranking 만 움직인다 — `cbox_paired` corner median 12.57/13.11/12.52 px 불변,
  6D 는 세 arm 전부 미측정 → 현재 병목(localisation)을 겨냥하지 않는다.
- `GENERIC_SCALE_EFFECT = STRONG` 재검: A42 10K 는 G38 38K 의 비례층화 부분집합이라
  자산 다양성은 교란이 아니다. 그러나 A42 는 패딩 안 된 이미지에 패딩 기준 라벨을 붙인
  run 이라(코너 중앙값 40.1px 어긋남) 낮은 기준선이 망가져 있다 — 장수 효과로도 못 읽는다.

개입 후보는 문서 §20 에 3개(A 저앙각×자산 다양성 재렌더 / B 근접 <1.5 m 재렌더 /
C 새 렌더 없는 failure-conditioned resampling). 순서는 C → A 로 권고(예산).

## accuracy_root_cause_v1 종결 — binding constraint 확정과 다음 실험 결정

메인 세션이 직접 계산한 것(위임 아님)과 최종 판정.

### 새 코드 (`scripts/research/accuracy_root_cause_v1/`)

- `verify_gt_claims.py` — GT 주장 독립 재현 (140장, SQPnP)
- `failure_decomposition.py` — R0 코너 오차를 GT provenance·가시성별로 분해 (2,799 코너)
- `selective_signals.py` — 배포 가능 거부 신호의 실패 예측력 (319 프레임)
- `elevation_check.py` — GT pose 로 카메라 앙각 유도, 실패율과 교차 (319 프레임)
- `ft_by_elevation.py` — real FT 이득의 앙각별 분포 (정본 140 / SEALED 73)

### 새 산출물 (`data/pallet/results/accuracy_root_cause_v1/`)

`MAIN_SESSION_GT_RECHECK.csv`(270) · `R0_CORNER_BY_GT_PROVENANCE.csv`(2,799) ·
`R0_SELECTIVE_SIGNALS.csv`(319) · `R0_ELEVATION_FAILURE.csv`(319) ·
`GT_REVIEW_FRAME_LIST.csv`(42) · GT 감사 8파일

### 판정

```
PRIMARY_ROOT_CAUSE = 저앙각(edge-on) 레짐에서 보이는 코너의 위치추정 실패
NEXT_EXPERIMENT    = real supervision 구성 ablation (저앙각만/고앙각만/분포대로)
```

R0 on PAPER_EVAL 319: 검출 실패 8 · 축순열 15 · **MISLOCATED 83**.
계약 대칭 {0,180}이 구제하는 프레임 0장, 8! 자유 배정도 98장 중 13장·p50 4% 개선뿐.
GT 외삽은 오차를 설명 못 함(외삽 7.38 vs 클릭 6.38 px, 가림 안에서는 클릭이 더 나쁨).
앙각 <8도 실패 48~52% 대 >=30도 14%, 투영 크기 층 안에서도 유지.
real FT 는 SEALED 세션 <8도에서 코너 9.22 → 5.86 px(-36.4%), >=30도에서는 오히려 악화.
기하 자기일관성 신호는 실패 예측 AUROC 0.44~0.65 = 무정보 → 구조 붕괴형 실패가 아니다.

### 문서 12개

`_docs/audits/accuracy_root_cause_v1/` — README · PRIOR_EXPERIMENT_MAP_paper(73실험) ·
PRIOR_EXPERIMENT_MAP_challenge(20) · GT_TRUST_AUDIT · ARTIFACT_REUSE_INVENTORY ·
HOUGH_IMPLEMENTATION_AUDIT · DEPLOYMENT_GEOMETRY_AUDIT(+메인 재검증 부록) ·
CAPACITY_AND_REAL_SUPERVISION_AUDIT · SOURCE_REAL_GAP_AUDIT · FAILURE_DECOMPOSITION ·
MODEL_HEADROOM_AUDIT · SELECTIVE_RISK_AUDIT · LINE_OBSERVABILITY_AUDIT(NOT_RUN 사유) ·
GT_SEMANTICS_REVIEW_PLAN(HUMAN_REVIEW_REQUIRED) · FINAL_DECISION

학습 0회 · 새 추론 0회 · 기존 GT/결과/registry 수정 0건.

### GPU 없이 먼저 고칠 것 (FINAL_DECISION §6)

1. `plastic_standard_110x110x15` 대칭 계약 정의 (현재 UNREVIEWED/null인데 identity-only 보고 중)
2. `symmetry_aware_pose_metrics.py` docstring 의 "hitting the deck" 문장 (두 팔레트 다 4-way)
3. `pallet_jetson_deploy/infer_fps.py:46` PALLET_DIMS 가 배포 물체와 다름
4. CLAUDE.md·memory 의 "161장" → **140장** (디스크 전수 22/12/18/27/33/12/16)
5. memory 정정 — live-gt FT 0.98 은 same-session(interleave/6) · medium 은 SEALED 에선 낫다
6. held-out 모집단 개설 (incoming 검수 대기분 DAY 27,279 / NIGHT 12,459)

### self-training + DiffPnP 결과 — REJECT (세 draw 전부)

```
지표                    D1(기본)   D2(P43)   D3(P44)    방향
rotation median deg      +5.56%    -6.82%    -0.98%    엇갈림 (2/3 개선)
translation median cm    -8.84%    +3.02%    -2.54%    엇갈림 (2/3 개선)
IoU3D median             +1.48%    -1.08%    -1.25%    엇갈림 (1/3 개선)
ADD AUC                  +2.58%    -0.62%    -3.20%    엇갈림 (1/3 개선)
사전등록 판정             REJECT    REJECT    REJECT
```

여섯 arm 전부 10 epoch 완주(results.csv 11줄), 추론 no_detection 0/319.
D1 만 보면 개선으로 읽히지만 D2·D3 에서 방향이 뒤집힌다 — 단일 draw 판정이면 오보였다.

★ 계획에 없던 검증: **λ=0 대조군이 동결 정본 R5_PROPOSED 와 7개 지표 오차 0.000e+00 일치**.
DiffPnP 학습 하네스가 원본 self-training 파이프라인과 동일함이 증명됐다.

★ 부산물: 대조군(λ=0)끼리만 비교해도 pseudo-label 표집 draw 간 폭이 rotation 6.7% /
translation 14.3% / ADD AUC 4.5%. DiffPnP 효과(±1~9%)가 이 잡음 바닥보다 작다.

seed replicate 는 무효 — `args.seed` 가 ultralytics dataloader 에 안 가 비트 동일해진다.
유효 replicate = pseudo-label 표집 draw(P43/P44). 분석은 `_docs/notes/diffpnp-yolo.md` §5.

## accuracy_root_cause_v1 보강 — §29 통계, 주장 1건 철회, 문서 정정

앞선 감사가 점추정만 낸 것을 바로잡고, 미완 항목을 마무리했다.

### 신규

- `scripts/research/accuracy_root_cause_v1/uncertainty.py` — 세션 클러스터 부트스트랩
  (B=10,000, seed 20260906). 프레임 부트스트랩과 나란히 낸다.
- `data/pallet/results/accuracy_root_cause_v1/UNCERTAINTY.json`
- `_docs/audits/accuracy_root_cause_v1/UNCERTAINTY.md`
- `data/pallet/results/accuracy_root_cause_v1/next_experiment/METHOD_LOCK_DRAFT.json`
  (status=DRAFT_NOT_FROZEN, blocker 5개)

### ★주장 1건 철회

">=30도에서 real FT 가 악화된다(gross 11.8% → 29.4%)" 는 **지지되지 않는다.**
그 표본은 N=17 이고 **전부 한 세션(eval_cad)** 이며, 짝지은 중앙차는 오히려 +0.81 px
(FT 가 약간 좋음), 프레임 95%CI [-1.54, +1.81] 이 0 을 포함한다. 11.8→29.4% 는
17장 중 2장→5장이다. 정정: **고앙각에서의 이득은 확인되지 않는다** — "없다" 도
"악화된다" 도 이 데이터로는 말할 수 없다.
`FINAL_DECISION.md` · `MODEL_HEADROOM_AUDIT.md` · `FAILURE_DECOMPOSITION.md` 에 반영.

### 살아남은 것

- 저앙각 > 고앙각: `3-8도` [37.2, 67.1] vs `15-30도` [10.0, 22.4] — 세션 CI 겹치지 않음.
- real FT 이득: 짝지은 차이 전체 +3.23 px 세션 CI [+2.72, +3.71] **0 배제**,
  `<8도` +3.33 px [+2.50, +4.69], 4개 세션 전부 같은 방향(+0.97 / +2.45 / +3.12 / +4.19).
- 해상도 부족으로 표시: 앙각 `<3`(세션 2개) · `8-15`(CI 폭 48 %p).

### 성공 임계 재설정

`success = L 이 H 를 20% 앞선다` 를 **구간 기준**으로 바꿨다 —
`L-H 짝지은 차이의 세션 클러스터 95%CI 가 0 을 배제`. 20% 는 `synth→FT` 효과에서 온
것인데 다음 실험은 **두 arm 모두 real 라벨을 받는** L vs H 라 효과가 더 작을 것이기 때문이다.
held-out 세션 **최소 6개** 를 split 단계에서 확보하도록 명시했다.

### 기존 파일 수정 2건 (이번 감사에서 처음으로 코드/설정을 건드림)

- `CLAUDE.md` — 정본 평가셋 **161 → 140** (폴더별 22/12/18/27/33/12/16).
  161 이 GT-QA 이전 값임을 명시하고, 161→140→128→194→319 계보표를 추가.
  `challenge/tests/test_eval_set_canonical.py` 8 passed 로 확인.
- `scripts/paper/pose_metric_closure_v1/symmetry_aware_pose_metrics.py` — docstring 만 수정.
  "For a forklift the difference between entering the pockets and hitting the deck is
  exactly that rotation" 은 저장소 자체 기록과 모순된다(두 팔레트 다 4-way 진입).
  90도 배제의 실제 근거인 X/Z extent 차이로 바꾸고, 배포 정사각 물체가 UNREVIEWED 임을 명시.
  코드 변경 0줄 — docstring 문자열만 교체했다.

  검증 실행 (감시 id `20260906_095832_832639`):
  ```
  python -m pytest scripts/paper/pose_metric_closure_v1/test_symmetry_aware_pose_metrics.py \
                   scripts/paper/pose_metric_closure_v1/test_pose_metric_contract.py -q
  -> 46 passed in 0.33s   (exit 0)

  python -m pytest challenge/tests/test_eval_set_canonical.py -q
  -> 8 passed in 0.92s    (exit 0)
  ```
  전자는 docstring 수정이 계약을 깨지 않았음을, 후자는 CLAUDE.md 의 140장 표기가
  코드 계약과 일치함을 확인한 것이다. 실패 0건이라 후속 조치 없음.

### memory

`accuracy-root-cause-v1-binding-constraint` 에 CI·철회 반영.
`MEMORY.md`·`eval-set-canonical-objects-split-eval` 의 161 → 140 정정.
`live-gt-ft-split-and-aug-decide-the-verdict`(0.98 은 same-session) ·
`hf-published-pallet-pose-models`(medium 판정은 seen/unseen 혼합 기준) 에 적용범위 절 추가.

---

## GT semantics 사람 리뷰 패킷 생성 (viz-expert)

계획 `_docs/audits/accuracy_root_cause_v1/GT_SEMANTICS_REVIEW_PLAN.md` 는 대상 42프레임
목록만 있고 이미지가 없었다. 사람이 실제로 채울 수 있는 패킷을 만들었다. 학습·추론 실행
없음, 기존 GT JSON 수정 없음(`git status challenge/data/01_real/{eval_canonical,manual_gt}`
= 0 files 로 확인).

### 신규 파일

- `scripts/research/accuracy_root_cause_v1/build_human_review_packet.py` (신규)
  읽기 전용 생성기. `challenge.data_paths.EVAL_CANONICAL` 을 import 해 경로를 얻고
  (문자열 하드코딩 없음), `annotate_pnp.make_pallet_keypoints_3d_diagram` 로 3D 모델을,
  `cv2.SOLVEPNP_SQPNP` 로 pose 를 푼다(`verify_gt_claims.py::pnp_err` 와 같은 계열).
- `_docs/audits/accuracy_root_cause_v1/human_review/<folder>__<frame>/` 42개 ×
  `01_raw.png` `02_gt_only.png` `03_geometry_A.png` `03_geometry_B.png` = 168 PNG.
  전부 640×480 원본 해상도 유지(확대·크롭 없음).
- `_docs/audits/accuracy_root_cause_v1/human_review/README.md` — 리뷰어 1페이지 안내.
- `.../REVIEW_FORM_corner.csv` 558행(62 frame_id × 9 corner, 값은 전부 빈칸) ·
  `.../REVIEW_FORM_frame.csv` 62행. 62 = 42 + `_rep2` 20 (repeatability 표본).
- `data/pallet/results/accuracy_root_cause_v1/_ANSWER_KEY.csv` 42행 —
  A/B ↔ stored/swapped 매핑 + 클릭전용 재투영 RMS. **리뷰 폴더 밖**에 둔다.
- `data/pallet/results/accuracy_root_cause_v1/HUMAN_REVIEW_MANIFEST.json` —
  프레임별 파일 목록·클릭 코너 수·click_indices·`03_*` 생성 여부·seed.

### anchoring 방지 설계 (이 패킷의 핵심)

- 모델 예측·재투영 수치·기존 실패 라벨을 이미지에 **하나도** 그리지 않았다.
- `02_gt_only.png` 는 저장 keypoint 9개만. 클릭(정수좌표)=채운 원 / 외삽(실수좌표)=빈
  사각형으로 **모양**이 다르다(색만 다른 게 아님). cuboid 와이어프레임 없음.
- `03_geometry_*` 의 cuboid pose 는 **클릭 코너만으로** PnP 를 다시 푼다. 외삽 코너는
  채택 pose 의 투영이라(`annotate_io.py:518-521`) 쓰면 채택 가설이 항상 이긴다.
- A/B 배정은 프레임별 무작위 — `random.Random(md5(f"{SEED}:{frame_id}"))`, SEED=20260906.
  결과 stored=A 23 / swapped=A 19. 이미지·파일명 어디에도 채택 표시 없음.
- 두 가설의 cuboid 색·선폭·라벨 형식이 동일하다(색이 단서가 되면 안 된다).

### 실측 확인

- 42프레임 전부 JSON·PNG 존재, `n_click` 이 CSV 값과 일치, sentinel/None 코너 0개 [확인].
- 클릭 4개인 12프레임 포함 **42/42 모두 PnP 가 풀렸다** → `geometry_skipped = 0`.
  계획이 대비했던 "클릭 4개 미만이라 못 푸는 프레임" 은 실제로 없었다 [확인].
- dims 는 `{w1.1,d1.3}` 21장 / `{w1.3,d1.1}` 21장으로 정확히 반씩 [확인].

### 주의

`_docs` 아래지만 `.gitignore:98 *.png` 에 걸려 **PNG 168장은 커밋되지 않는다**(로컬 전용,
74 MB). 재생성은 위 스크립트를 다시 돌리면 되고 seed 고정이라 A/B 배정도 동일하다 [확인].

## real 라벨 규약 전수 감사 (앙각 구성 실험 착수 전 게이트)

학습·추론 0회. 기존 GT JSON 수정 0건, registry 수정 0건 (읽기 전용).

### 새로 추가한 파일

- `scripts/research/accuracy_root_cause_v1/real_label_audit.py` — 프레임 단위 판정.
  경로는 `challenge/data_paths.py` 를 import 해서 얻는다(문자열 하드코딩 없음).
- `scripts/research/accuracy_root_cause_v1/real_label_audit_summary.py` — 집계.
- 산출물 `data/pallet/results/accuracy_root_cause_v1/REAL_LABEL_AUDIT.csv` (1,701행)
  · `REAL_LABEL_AUDIT_SUMMARY.json`
- 보고서 `_docs/audits/accuracy_root_cause_v1/REAL_LABEL_AUDIT.md`

### 판정

```
TOTAL_REAL_LABELED_FRAMES = 1516   (감사 행 1701 − gt_v2 사본 185)
LABEL_OK 814 / LR_ORDER_VIOLATION 0 / YAW90_STALE 398 / OTHER_DEFECT 49 / AMBIGUOUS 255
USABLE_BY_ELEVATION = {"<8": 398, "8-15": 171, ">=15": 53}
MAX_BALANCED_ARM_SIZE = 53   SESSION_COUNT_FOR_HELD_OUT_SPLIT = 33
```

### 지시받은 1차 판정법이 무의미했다 — 그래서 축을 셋 더 세웠다

"저장 pose 를 투영해 저장 keypoint 와 맞는지" 는 1,701장 전부 통과했다
(identity 클릭 최대오차 중앙값 1.29 px, p99 10.2 px). 저장 pose 가 그 점으로 푼
PnP 해라서 독립이 아니고, `projected_cuboid` 는 1,472/1,472 에서 `manual_kps[:8]`
과 1 px 이내로 같은 복사본이었다 [확인]. 추가한 독립 축:
(1) `keypoint_annotations` 대 `pose_transform`/`manual_kps` 교차필드,
(2) 0123 면 법선과 시선의 사잇각(점을 안 쓰므로 PnP 순환에 안 걸림),
(3) 같은 세션 인접 프레임의 90도 phase 튐.

### 핵심 사실 3개

- live_capture_gt 는 **851장**(28폴더). `REAL_FT_V1_METHOD_LOCK.json` 의 402 는
  6폴더 기준이고 22세션 449장이 추가됐다 [확인].
- **LR(거울) 순열 위반 0장.** 직전 중단 사유였던 "26.4% LR 순서 위반" 은 재현되지
  않았다. `det(R)=1`·직교성이 1,701/1,701 정상이라 반사는 발생할 수 없다 [확인].
- **어긋남의 방향이 알려진 것과 반대다.** 396/851 에서 두 필드가 정확히 `yaw270`
  로 어긋나는데(잔차 중앙값 0.00 px), 규약을 지키는 쪽은 `keypoint_annotations`
  다 — 0123 면 기울기 p50 12.1도, 60도 초과 0%, 인접 phase 튐 6/785쌍.
  `pose_transform`/`manual_kps` 는 p50 44.8도, 60도 초과 36.7%(312장),
  화면 좌우 뒤집힘 198장, phase 튐 22/785쌍 [확인].
  학습 라벨 변환기(`scripts/self_training_yolo/real_ft_v1/build_real_ft_dataset.py:58`)
  는 `keypoint_annotations` 를 읽으므로 **학습 라벨은 이 결함을 안 탄다.
  pose 기반 평가·PnP·거리추정이 탄다** [확인].

### 다음 실험에 대한 결론

- `>=15도` 층은 53장뿐이고 그 중 43장(81%)이 나무 팔레트다. 배포 물체(정사각
  플라스틱 110x110x15)만 보면 **2장**. 앙각 3층 ablation 은 앙각축이 아니라
  물체축을 재게 된다 → 2층(`<8` 대 `>=8`, 282 대 167)으로 줄이면 성립 [확인].
- 결함이 세션에 몰린다 — `142318` 64/65 전량 불량, 반대로 12개 세션 147장은 전량
  깨끗. 세션 단위 배정으로 통째로 뺄 수 있다 [확인].
- 착수 조건: `keypoint_annotations` 를 정본으로 못 박을 것. 지금 상태로 학습하면
  학습은 A 규약, 평가는 B 규약이 된다.

### 부수 확인

- `data_paths.REAL_MANUAL_GT` 의 `night01_EMPTY`(6장)·`night03_EMPTY`(20장)는
  이름과 달리 비어 있지 않다 — 등록부 주석이 낡았다 [확인]. `pallet01_EMPTY` 만 0장.
- `_night_eval_manual_gt`·`_outside_eval_manual_gt` 는 85 프레임의 stem 이 원본
  세션과 겹친다(나노초 타임스탬프 = 같은 원본 프레임) → 세션 held-out 에서 제외 [확인].
- live_capture_gt 안 56개 stem 이 두 세션 이상에 나타난다(188 프레임). 다른 영상의
  같은 번째 프레임이라 누수는 아니지만, stem 만으로 파일명을 짓는 변환기는 덮어쓴다 [확인].
- sentinel `(-1,-1)` 코너가 있는 23장은 전부 `pallet11_gt`(apriltag GT, 사용 금지) [확인].

## 라벨 감사 메인 세션 재현 — ★같은 파일 안에 keypoint 규약이 두 벌 있다

3d-expert 감사(`REAL_LABEL_AUDIT.md`)의 두 주장을 메인 세션에서 독립 재현했다. 둘 다 확증.

### (1) live_capture_gt 의 46.4% 가 90도 어긋나 있다

844 프레임(두 필드 모두 존재)에서 `keypoint_annotations` 를 8개 순열로 돌려
`manual_kps` 와 맞춰 봤다.

```
identity  452 프레임   잔차 중앙값 0.000 px
yaw90     392 프레임   잔차 중앙값 0.000 px      ← 46.4%
```

잔차 0.000 = 잡음이 아니라 **정확한 재라벨**이다. 두 필드는 서로의 복사본인데
인덱스 배정만 90도 다르다.

### (2) 규약을 지키는 쪽은 keypoint_annotations 다

camera-facing 0123 규약(0 왼쪽/1 오른쪽, 0·1 위/3·2 아래) 847 프레임 전수 검사:

```
keypoint_annotations   LR 위반   0 / 847  ( 0.0%)   TB 위반 0 / 847
manual_kps             LR 위반 197 / 847  (23.3%)   TB 위반 0 / 847
```

학습 라벨 변환기(`build_real_ft_dataset.py`)는 `keypoint_annotations` 를 읽고,
pose·평가 경로는 `manual_kps`/`projected_cuboid`/`pose_transform` 을 읽는다.
→ **고치지 않으면 학습은 A 규약, 평가는 B 규약으로 46% 프레임이 90도 어긋난 채 채점된다.**

[추정] 근인은 대칭 계약 부재다. 851장 전부 `pose_status="UNCONFIRMED_SIGNED_AXIS"` ·
`axis_assignment_confirmed=false` 이고 물체가 정사각(1.1x1.1)이라 yaw phase 를 정할 근거가 없다.
`plastic_standard_110x110x15` 의 `symmetry_status: UNREVIEWED` 가 문서 문제가 아니라
**46% 라벨 불일치로 실체화한 것**이다.

### (3) 직전 실험 중단 사유가 해소된다

`paper_real_ft_v1` 을 멈춘 "402장 중 106장(26.4%) 좌우 코너 순서 위반" 은
`manual_kps` 기준이다. `keypoint_annotations` 기준으로는 **0/847**.
재어노테이션이 아니라 **필드 선택으로 해소된다.**
거울(mirror) 순열은 0장 — `det(R)=1`·직교성이 1,701/1,701 정상이라 반사는 발생 불가.

### 다음 실험 설계 변경 (METHOD_LOCK rev2)

3-arm 앙각 설계가 **불가능**하다. LABEL_OK 의 앙각 x 물체 실측:

```
앙각      정사각 110x110   논문 110x130   wood     합
<8            282            116          0      398
8-15          165              6          0      171
>=15            2              8         43       53
```

`>=15도` 는 어느 물체로도 arm 이 안 된다 — 쓰면 앙각축이 아니라 물체축을 재게 된다.
→ **정사각 물체 단일, `<8`(282) 대 `8-15`(165) 2-arm, 2x2 전이 행렬**로 축소.
165장에 맞춰 절단. blocker 6개(정본 필드 확정 · 대칭 계약 · held-out 세션 봉인 ·
GT repeatability · stem 중복 188 프레임 · seed/캐시).

`data/pallet/results/accuracy_root_cause_v1/next_experiment/METHOD_LOCK_DRAFT.json` rev2 ·
`FINAL_DECISION.md` §5b 신설 · `README.md` 결론 블록 갱신.

## 함정 기록 — `# ETA=` 주석을 heredoc 종료자 뒤에 붙이면 종료자가 죽는다

감시 id `20260906_100625_847766` 의 실행이 이것이다. 결과는 **파일 편집 성공 + 마지막 줄에서
`NameError`** 라는 애매한 형태였다.

```bash
python3 - <<'PY'
...
PY  # ETA=1m        ← 종료자가 'PY' 가 아니라 'PY  # ETA=1m' 이 되어 매칭 실패
```

bash 가 `PY` 를 종료자로 인식하지 못해 heredoc 이 EOF 까지 이어졌고, 그래서
`PY  # ETA=1m` 이 **파이썬 코드 줄로 넘어가** `NameError: name 'PY' is not defined` 가 났다.
그 전까지의 `p.write_text(...)` 는 정상 실행됐으므로 **편집은 반영됐다** — `sed` 로 확인함.

교훈: `run-watch-gate.sh` 가 요구하는 `# ETA=` 주석은 **heredoc 을 여는 줄**이나
단일 명령 끝에만 붙인다. 종료자 줄에는 절대 붙이지 않는다.

```bash
python3 - <<'PY'  # ETA=2m     ← 이 위치가 맞다
...
PY
```

exit code 1 이 났지만 산출물은 정상이라, "종료코드로 완료를 판정하지 않는다"
(전역 규칙)의 사례이기도 하다. 실제 확인은 `sed -n '425,447p'` 로 했다.

## 사용자 결정 반영 — 대칭 계약 확정 · 정본 필드 코드 고정 · held-out 봉인

사용자 확인: **"4면 다 구멍 있어"** (정사각 팔레트 실물 관찰), **"코드 고쳐줘"**, **"일단 봉인해"**.

### 1. 대칭 계약 — C4 로 얼림

- 신규 `challenge/config/SQUARE_PALLET_SYMMETRY_CONTRACT.json`
  `equivalent_yaw_degrees [0, 90, 180, 270]`. 근거 두 개를 문서에 명시 —
  (i) registry 의 정사각 footprint x = z = 1.100 m, (ii) 소유자 실물 확인 4-way 진입.
  ★계약이 밝히지 **않는** 것: 상판 무늬의 90도 대칭 여부는 확인 안 함. 이 계약은
  pose 지표·포크 진입에 대한 것이지 "시각적으로 구분 불가" 주장이 아니다.
  `provenance_note` 에 사전등록이 아님(개발 결과를 이미 본 뒤 작성)을 적었다.
- `challenge/config/CHALLENGE_OBJECT_GEOMETRY_REGISTRY.json` —
  `plastic_standard_110x110x15` 만 `symmetry_status` UNREVIEWED → FROZEN,
  `symmetry_contract` 를 위 파일로. **논문 물체 2개는 원본과 바이트 동일** 확인.
  SHA `e923b44880b031a7` → `9ae991450c384f4f`. 이 SHA 는 내 감사 문서에만 핀돼 있었다
  (논문 registry `0c7a1072` 는 67곳에 핀 — 건드리지 않음).
- 테스트 `test_challenge_object_geometry_registry.py` 갱신 — UNREVIEWED 를 잠그던 단언을
  빼고, C4 군 폐쇄성·직교성·det=+1 과 "논문 물체는 여전히 {0,180}" 을 잠갔다.

### 2. 정본 keypoint 필드 = `keypoint_annotations` (코드 수정)

- `challenge/yolo_pose_one_model/scripts/prepare_yolo_pose.py::load_kps` —
  `keypoint_annotations` 우선, 없으면 기존대로 `projected_cuboid`.
  ★합성 GT 에는 그 필드가 없어(표본 0/40) **합성 데이터셋 재빌드 결과는 불변**.
- `challenge/scripts/dataset/gen_flip_noise_aug.py::flip` —
  `dict(obj)` 얕은 복사라 `projected_cuboid` 만 뒤집히고 `keypoint_annotations` 는
  원본으로 남았다. load_kps 가 그 필드를 우선하므로 고치지 않으면
  **뒤집힌 이미지에 안 뒤집힌 라벨**이 붙는다. 함께 뒤집도록 수정(x 미러 + FLIP_PERM_8,
  centroid 는 제자리).
- 신규 테스트 `challenge/tests/test_keypoint_field_contract.py` — 5 tests pass.
  단위 4개 + 실데이터 불변식 1개(live_capture_gt 851장에서 keypoint_annotations 규약 위반 0).
- **부수 효과: 쓸 수 있는 live_capture_gt 프레임이 449 → 833 장.**

근거 실측 (메인 세션): `projected_cuboid` 는 camera-facing 0123 규약을 198/851(23.3%)
어기고 `keypoint_annotations` 는 0/851. 위반 198 중 196이 좌우 **완전 뒤집힘**
(뒤집힘 폭 중앙값 35.9 px, 정상 프레임 여유폭 180.1 px), 5 px 미만 경계 잡음은 13건뿐.

### 3. held-out 봉인

`data/pallet/results/accuracy_root_cause_v1/next_experiment/HOLDOUT_SEAL.json`
+ `HOLDOUT_{HELD_OUT,TRAIN}_FRAMES.txt`

```
held-out   9 폴더  297 프레임   <8 155 / 8-15 141   sha256 c315f31b2bf9252a…
train     19 폴더  536 프레임   <8 352 / 8-15 178   sha256 f43d6bea745505e2…
균형 arm 크기 178
```

분할 원칙 — 폴더(물리 촬영 단위) 기준. audit 의 `session` 열은 한 폴더가 두 그룹에
걸쳐 모호하다. held-out 은 전부 forklift_v4 계열(지게차 장착 = 배포 도메인)이고,
09-02 손촬영 2건은 같은 날·같은 현장이라 **함께 train 에** 두었다.

★**이건 봉인이지 confirmatory 가 아니다.** `challenge_c4_track/per_frame.csv` 가
live_capture_gt 28 세션을 전수 평가했고, `live_gt_v4`·`live_gt_v5_nocrop` 의
`group_counts` 합이 851 = 전량이다. 봉인이 사는 것은 "결과를 보고 split 을 고르지
않는다" 와 "촬영 단위 분리" 뿐이다. 진짜 held-out 은 `pallet_eval_v1/incoming/` 의
미검수 신규 촬영(DAY 27,279 / NIGHT 12,459)을 어노 후 봉인하는 길뿐이다.

누수 검사 — 51개 stem 이 여러 폴더에 나타나지만 표본 8건 전부 이미지 SHA256 이 다르다
(중복 프레임이 아니라 프레임 번호 재사용). 다만 stem 만으로 파일명 짓는 변환기는
덮어쓰므로 폴더 접두 필요.

### 4. 내 과장 정정 — `infer_fps.py`

앞서 "배포 물체와 다른 치수를 쓴다" 고 적은 것은 **과했다**. 그 파일은 배포
파이프라인이 아니라 **Jetson 추론 속도(FPS) 측정 스크립트**이고, 문서화된 입력
`forklift_raw_20260528_163408.mp4` 세션의 GT 25장이 전수 110×130×11 이다
({w1.3,d1.1} 22장 / {w1.1,d1.3} 3장). `PALLET_DIMS = (1.1, 1.3, 0.11)` 은 맞는 값이다.
`DEPLOYMENT_GEOMETRY_AUDIT.md` 부록 8번과 `FINAL_DECISION.md` §6-3 을 정정했다.

### 5. 테스트

```
challenge/tests/test_keypoint_field_contract.py                    5 passed
challenge/tests/test_challenge_object_geometry_registry.py         8 passed
challenge/tests 전체(env 비호환 1개 제외)              1400 passed / 6 failed
```
6 failed 중 registry 1건은 내 변경 탓이라 테스트를 갱신했고(위 1번), 나머지 5건은
**기존 실패**다 — `data/pallet/results/paper_selftrain_v5/yolo26n.pt` 가 커밋 `7ef97b8`
에서 추적되고 있어 걸리는 것 2건, 세션 시작 시점부터 수정돼 있던 workspace 파일의
annotation SHA 불일치 1건, gradient/architecture 2건. 내가 만든 `.pt` 는 0개다.

### 6. METHOD_LOCK rev3

남은 blocker **1개** — GT repeatability 실측(42장 리뷰). 나머지 5개는 위에서 해소.

## 오검출 contact sheet + GT 리뷰 패킷 phase2 (viz-expert)

학습·추론 실행 없음. 기존 GT JSON 미수정. 기존 리뷰 산출물은 append 만 했다.

### 1. R0 오검출 79장 육안 검수 시트 (신규)

`scripts/research/accuracy_root_cause_v1/build_false_positive_sheets.py` 신설.
입력은 `data/pallet/results/accuracy_root_cause_v1/R0_FALSE_POSITIVES.csv` 79행뿐이다.

- 출력 A: `_docs/audits/accuracy_root_cause_v1/false_positive_review/sheet_01..04.png`
  (5×4 = 20셀, 마지막 시트만 19셀. conf 내림차순. 2400×1585 px)
- 출력 B: 같은 폴더 `full/<stem>.png` 79장 — 원본 전체 + 박스, 원해상도 유지
  (640×480 51장 / 1280×720 28장, `PIL` 로 실측 확인) [확인]
- 출력 C: `data/pallet/results/accuracy_root_cause_v1/FALSE_POSITIVE_MANIFEST.json`
  — 시트별 셀 순서 ↔ stem 매핑. 셀마다 `label` 을 **빈 문자열로** 둬서 나중에 채우게 했다.

★이 산출물은 오검출에 **이름표를 붙이지 않는다.** 셀 캡션은 `conf 0.905 | 000056`
형식으로 confidence 와 프레임 stem 두 개뿐이고, 카테고리는 이미지를 본 사람이 정한다.

크롭 규칙: 검출 박스를 1.6배 확장 → 셀 종횡비(4:3)에 맞춰 확장 → 이미지를 넘으면
**줄여서 안쪽으로 민다**(여백으로 채우지 않는다). 처음 구현은 넘친 만큼 회색 여백을
칠했는데, 큰 박스 셀에서 여백이 화면의 40%를 먹어 맥락이 오히려 줄었다 — 렌더해 보고
고쳤다 [확인]. 줄여도 박스보다 작아지는 경우에만 여백을 쓴다.

### 2. GT 리뷰 패킷 phase2 16장 (기존 생성기 재사용)

`scripts/research/accuracy_root_cause_v1/build_human_review_packet.py` 에 `--phase`
인자를 추가했다. 새 스크립트를 만들지 않았다.

- Before: `FRAME_LIST` / 정답키 / 리뷰 시트 경로가 전부 상수, 파일은 `"w"` 로 열림.
- After: `PHASES` dict 로 phase 1(기본, 기존 42장 그대로) / phase 2 를 분기.
  phase 2 는 `GT_REVIEW_FRAME_LIST_PHASE2.csv` 를 읽고
  `HUMAN_REVIEW_MANIFEST_PHASE2.json` 을 쓰며, 정답키·리뷰 시트는 **append**.
  `write_rows()` 가 append 전에 기존 헤더와 필드명을 대조해 어긋나면 멈춘다.
  manifest 의 프레임 레코드는 `wd_margin_px`(phase1) / `note`(phase2) 를 선택적으로 싣는다.
- 인자 없이 돌리면 phase 1 동작이 그대로다(`n_repeat=20`, `append=False`).

실행 결과 (`--phase 2`): 프레임 16, `03_*` 생성 12, 생략 4, 정답키 12행 추가.
생략 4건은 클릭이 3개뿐이라 PnP 가 안 풀린 프레임이다 [확인] —
`eval_night09__1779449604823769344`, `eval_night09__1779449661263803392`,
`eval_pallet07__1778652142480077056`, `eval_pallet07__1778652144496057088`.

기존 행 보존을 실측 확인했다 [확인] (실행 전 백업본과 `diff`, 전부 동일):

```
파일                          Before   After   추가
──────────────────────────────────────────────────────
_ANSWER_KEY.csv                  43      55     12
REVIEW_FORM_corner.csv          559     703    144
REVIEW_FORM_frame.csv            63      79     16
human_review/ 프레임 폴더         42      58     16
human_review/ PNG                168     224     56
```

### 3. `human_review/README.md` 에 phase2 절 추가

16장이 무작위가 아니라 **실패농축 표본**이라는 것과 `role` 5종의 뜻을 적었다.
`LINE_BETTER_THAN_POINT` / `LINE_WORSE_THAN_POINT` 두 role 은 **두 방식의 corner
예측이 완전히 동일하고 회전 오차만 다른** 프레임이라는 사실을 명시했다(겹치는 93개
프레임 전부에서 코너 좌표 동일 — 메인 세션 실측값을 옮겨 적음) [추정: 이 세션에서
재측정하지 않았다]. `03_*` 없는 4장 처리법도 적었다.

### 4. 부수 확인

- `_docs/` 아래 PNG 는 프로젝트 `.gitignore:98 *.png` 에 걸린다 [확인].
  false_positive_review 63 MB 라 무시가 맞고, 재생성은 위 스크립트로 결정적이다.
- 같은 시각 다른 세션이 `human_review/REVIEW.html` 을 만들었고, 거기 박힌 프레임
  목록에 phase2 16장이 이미 들어 있다(58개) [확인]. 내가 만든 파일이 아니다.

#### 4-1. 덮어쓰기 가드 추가 (같은 스크립트)

`--phase 1` 을 다시 돌리면 `"w"` 로 열려 phase2 가 뒤에 붙인 행이 날아간다. 그래서
`write_rows()` 에 "덮어쓰기 모드인데 파일이 이미 있으면 `--force` 없이는 멈춘다" 를
넣었다. 실제로 `--phase 1` 을 돌려 멈추는 것과 행 수가 그대로인 것을 확인했다 [확인]
(정답키 55행, frame form 79행 유지). 단 가드는 CSV 쓰기 직전이라 phase1 프레임 PNG
168장은 다시 렌더된다 — 내용은 결정적이라 동일하다.

## §19 오검출 육안 검수 — 메인 세션이 79셀 전수를 보고 분류

지시문 §19 는 "R0 failure candidate 를 crop 해 contact sheet 를 만들고 **이미지를 먼저 본 뒤**
category 를 만들라" 고 했다. 앞선 감사는 그 단계를 건너뛰고 기존 통계로만 답했었다.

- 후보 추출(메인): `data/pallet/results/accuracy_root_cause_v1/R0_FALSE_POSITIVES.csv`
  — `DEV_NEG2689`(팔레트 없는 real negative)에서 R0 가 배포 임계 conf 0.4 이상으로
  검출한 79 프레임. 새 추론 0 회, `R0_per_frame.csv` 의 negative 행에서 박스까지 재사용.
- 시트 렌더는 viz-expert 위임, **분류는 메인 세션이 직접** (사전 이름 금지가 핵심이라 위임 불가).
- 결과 `R0_FALSE_POSITIVES_LABELLED.csv` · 보고서 `HARD_NEGATIVE_REVIEW.md`

```
SLATTED_WOODEN_BENCH          49  62.0%  conf p50 0.665
RIBBED_TRANSLUCENT_LID        23  29.1%  conf p50 0.720   ★최고 오검출 conf 0.905
LOUVRE_VENT_GRILLE             3   3.8%
WALL_SIGN_PLAQUE / BRIGHT_PLANAR_SCREEN / KEYBOARD_KEY_ROWS /
DIFFUSE_GROUND_NO_STRUCTURE    각 1
```

**91%(72/79)가 "평면 위에 나란한 요소가 반복되는 것"** 이다. 팔레트를 닮은 물건
(랙·상자·지게차)은 하나도 없다 — 크기·맥락·3D 구조가 전부 다르고 슬랫 텍스처만 같다.
사전 후보로 흔히 드는 traffic cone·rack 은 실제로 하나도 안 나왔다.

[추정] R0 가 팔레트를 3D 구조가 아니라 상판 반복 텍스처로 찾는다면, 저앙각에서 상판이
얇은 띠(단축 34.9 px)로 압축될 때 주 단서가 무너진다. 저앙각 실패와 이 오검출 패턴이
같은 표현을 두 방향에서 보여주는 셈이다. `FINAL_DECISION.md` 에 6c·Q9b 로 넣었다.

★기존 판정은 안 바뀐다 — 이건 ranking·검출 축이고
`NEGATIVE_INTERVENTION_TOUCHES_LOCALISATION = NO` 는 그대로다.
"이 벤치들을 negative 로 더 넣자" 는 결론이 아니다(이미 해봤고 검출을 같이 죽였다).

★표본 한계 명시 — 79건이 사실상 **6개 장면**에서 나온다(프레임 번호가 연속:
000046~000060 이 전부 같은 컵). `DEV_NEG2689` 는 편향 표본이라 **비율이 아니라 종류만**
읽어야 한다.

## GT 리뷰 페이지 — 브라우저로 클릭해서 끝내는 도구

- 신규 `scripts/research/accuracy_root_cause_v1/build_review_page.py`
- 산출 `_docs/audits/accuracy_root_cause_v1/human_review/REVIEW.html` (58 프레임)

CSV 를 손으로 채우게 하면 리뷰가 굴러가지 않는다. 프레임당 두 질문(축 가설 A/B,
외삽 점이 실제 코너 자리인가)을 라디오 버튼으로 받고, localStorage 자동 저장,
끝나면 CSV 두 개를 내려받는다. 패킷 폴더를 매번 새로 훑으므로 프레임이 늘면 재실행하면 된다.

★**내가 anchoring 누출을 한 번 만들었다가 고쳤다.** 초판이 phase-2 프레임에
`GROSS_KEYPOINT_ERROR` · `identity_max=425.2px` 같은 **모델 실패 라벨**을 띄우고 있었다.
지시문 §3.3 이 금지한 "이전 failure label" 그 자체다. 제거했고, phase-1/2 가 순서로
묶여 보이는 것도 단서라 프레임 id 로 결정적 셔플을 넣었다.
누출 검사(role·오차수치·정답키 문자열) grep 0건으로 확인.

## GT 사람 리뷰 결과 — 축 배정은 옳고, 4-클릭 프레임 pose 는 검증 불가 (새 발견)

리뷰어 = 프로젝트 소유자. 58 프레임 중 A/B 가설이 있는 **54장 전수 응답**.
정답키는 리뷰 중 비노출(`_ANSWER_KEY.csv` 는 리뷰 폴더 밖), 대조는 응답 수집 후.
보고서 `_docs/audits/accuracy_root_cause_v1/GT_REVIEW_RESULT.md` ·
원자료 `data/pallet/results/accuracy_root_cause_v1/GT_REVIEW_RESULTS.json`

### 1. W/D 축 배정은 옳다

판정한 39건 중 30건(76.9%) 저장본 일치, 단측 이항검정 **p = 5.3e-4**.
확신도별 일치율이 완전 단조다 — 1: 0/2 · 2: 6/10 · 3: 4/5 · 4: 3/3 · 5: **14/14**.
확신도 4 이상은 **17/17 = 100%**.

클릭만의 margin 별로도 갈린다 — `>5px` 10건에서 **9/9 = 100%**,
`<1px` 35건에서 69.2%(모르겠음 26%).
→ `verify_gt_claims.py` 가 제기한 순환 논증 우려는 **"GT 가 틀렸다" 가 아니라
"증거량이 부족하다"** 였음이 사람 판정으로 확정됐다.

### 2. 28% 는 사람도 못 가른다

54건 중 15건이 "모르겠음". 야간·원거리·edge-on 에서 팔레트가 얇은 띠로 투영되면
두 가설 실루엣이 사실상 같다. → **그 구간의 6D rotation 수치는 인용 금지.**
2D keypoint 지표는 W/D 배정에 의존하지 않아 영향 없음.

### 3. ★새 결함 — n_click=4 프레임의 pose 는 검증 불가

리뷰어가 네 프레임에 내가 제공하지 않은 답을 적었다 — **"둘 다 틀림"**.

```
eval_pallet07__1778652176547299328   "a b 둘다 완벽하게 틀림"
eval_pallet09__1778653634641026304   "ab 둘다 완벽하게 틀림"
eval_pallet07__1778652174598774528   "또 둘다 다 틀림"
eval_pallet07__1778652140531310080   "둘다 아깝게 틀림 1 2 번이 안밎음"
```

넷 다 **`n_click = 4`** 이고, 클릭수로 완전히 갈린다:

```
클릭수   리뷰   모르겠음   둘다틀림
  4      13      6        4  (31%)
  5      29      5        0
  6      12      4        0
```

그런데 이들의 저장 `reproj_error_px` 는 **0.87~2.24 px 로 정상 범위 안**이다
(같은 두 폴더 정본 60장 p50 1.30 · p90 2.76 · max 4.48).

이유는 기하다 [확인] — PnP 는 4 점이면 정확결정이라 잔차가 거의 0 이 되고,
그 잔차는 **pose 를 정한 바로 그 점들로** 계산된다. 자기를 채점하므로 검증력이 없다.
한 프레임(`…140531310080`)은 스왑 가설이 클릭에 더 잘 맞는데(2.04 vs 2.78 px)
저장본은 반대를 골랐다.

정본 140장의 `n_click <= 4` 는 23장(16.4%)이다.

### 조치 제안

1. `reproj_error_px` 를 `n_click` 없이 GT 품질 지표로 쓰지 않는다 (n_click>=6 에서만 검증력).
2. `n_click <= 4` 정본 23장에 **재클릭 큐**를 만든다 — 새 촬영이 아니라 코너 2~3개 추가.
3. 6D 보고 시 `n_click` 층 분리.
4. 어노 도구에 클릭 4개 경고 (현재는 조용히 저장).

### 답하지 않은 것

**test-retest repeatability 는 여전히 미측정**이다. 계획한 "20장 x 2회" 대신 1회만 리뷰했다.
이번에 답한 것은 **축 식별 가능성**이지 산포가 아니다.
`METHOD_LOCK` rev4 에 그대로 남겨 두고, 대안(failure_criterion 을 'CI 가 0 포함' 만으로 정의)도 적었다.
코너 단위 시트(378행)와 외삽점 물리성 판정은 미수집 — 브라우저가 연속 다운로드 두 번째를
막은 것으로 보인다 [추정].

## self-training + DiffPnP 스크린 — 결과 (3 복제 전부 REJECT)

★**이 실행은 이 세션이 돌린 것이 아니다.** 같은 날 다른 세션이 착수해(위 "self-training +
DiffPnP 스크린 착수" 절) 돌렸고, 결과가 나왔는데 기록되지 않은 채 남아 있었다.
`run-watch` 가 미기록으로 잡아서, 이 세션이 **산출물에서 읽어** 옮겨 적는다.
감시 id `20260906_090056_738974`(인덱스 빌드) · `20260906_090151_741055`(스크립트 편집) ·
`20260906_090441_747343` · **`20260906_090552_749928`**(본 스크린, ETA 40m) ·
`20260906_091628_769113`(P43/P44 복제). 수치는 전부 결과 파일 실측이고 재실행하지 않았다 [확인].

산출물 타임스탬프로 대응을 확인했다 — `090552` 실행이
`SELFTRAIN_DIFFPNP_RESULT.json`/`REPORT.md`(09:14, 위 표의 "seed 기본" 행)를 냈고,
그 사이 09:05~09:09 의 `PROBE` · `ST_C_LAMBDA0` · `ST_T_DIFFPNP` 는 같은 실행의 중간 산출물이다.
`091628` 이 P43(09:27)·P44(09:35)를 냈다. **다섯 id 가 모두 위 표에 반영돼 있다.**

동일 데이터셋·동일 레시피에서 `lambda_dp` **하나만** 바꾼 2-arm 비교. 각 10 epoch ·
900 optimizer update · λ = 0.1432 · arm 당 8~9분.

```
복제        지표                    대조 λ=0    처치 λ>0    상대변화   개선
────────────────────────────────────────────────────────────────────────
(seed 기본)  rotation_median_deg      2.5345     2.6755     +5.56%
            translation_median_cm    8.8265     8.0458     -8.84%
            iou3d_median             0.5868     0.5955     +1.48%
            add_sym_auc              0.4001     0.4104     +2.58%     3/4
P43         rotation_median_deg      2.5999     2.4227     -6.82%
            translation_median_cm    7.6472     7.8778     +3.02%
            iou3d_median             0.5963     0.5898     -1.08%
            add_sym_auc              0.4186     0.4160     -0.62%     1/4
P44         rotation_median_deg      2.7107     2.6842     -0.98%
            translation_median_cm    8.3098     8.0983     -2.54%
            iou3d_median             0.5913     0.5839     -1.25%
            add_sym_auc              0.4161     0.4028     -3.20%     2/4
```

**세 복제 전부 `verdict: REJECT` · `실질 NOT_IMPROVED`** [확인,
`SELFTRAIN_DIFFPNP_RESULT{,_P43,_P44}.json` 의 verdict 필드].
개선 지표 수가 3/4 → 1/4 → 2/4 로 복제마다 뒤집히고, 부호가 일관된 지표가 없다.
가장 큰 변화폭도 8.8% → 6.8% → 3.2% 로 줄어든다. **seed 산포 안이다.**

### 이 감사(accuracy_root_cause_v1)와의 정합

`FINAL_DECISION.md` 의 `PNP_STATUS = PNP_NOT_PRIMARY_LEVER` 와 같은 방향이고,
그 판정을 **세 번째 축에서 재확인**한 셈이다 — 평가측 solver 교체(REJECT),
학습측 DiffPnP 단독(REJECT), 그리고 이번 **self-training 과 결합한 학습측 DiffPnP(REJECT)**.
`DO_NOT_RUN` 의 "PnP solver 탐색" 에 이 갈래도 소진됐다고 볼 수 있다.

★단 적용범위 — PAPER_EVAL 319 는 반복 사용된 development set 이고
(리포트 자체가 그 문장을 달고 있다), 이 결과도 held-out 이 아니다.
그리고 착수 절이 이미 적어 둔 한계가 그대로 남는다: **PL 참조 pose 는 교사 자신의
좌표에서 읽은 것이라 새 정보를 주지 않는다.** 이 항의 기전은 2D 오차를 pose 영향도로
재가중하는 것이지 새 감독을 넣는 것이 아니다.

## 6D 를 조건별로 나눠 낸다 — 2D 표에만 있던 분할을 6D 층에 (사용자 질문에서 출발)

사용자 질문: "다른 논문에서 안 가린 거 / 가린 거 이런 식으로 평가데이터를 분리하는데 우리도 하나?"

확인 결과 [확인] — **2D 는 하고 있고 6D 는 안 하고 있었다.**
`TABLE_FINAL_4` 는 Clean 184 / Occlusion 135 / Truncation 51 / Far 59 / Low·Mid·High 로 나눠
keypoint px 를 보고하는데, `TABLE_FINAL_POSE*.md` 세 파일은 occlusion·truncation·clean 언급이
**0건**이고 `POSE_BY_OCCLUSION` 표가 없다. 6D 는 material·lighting 만 나눈다.

선행연구는 표준으로 한다 [확인, `_docs/audits/paper/PALLET_POSE_LITERATURE_AUDIT.md`] —
P8(IEEE Access 2024)은 `>70% occlusion 0.42→0.49` 를 따로, P11 은 occlusion/overlap 을
별도 셋으로 분리한다. 감사 문서가 비교축에 `G8 occlusion/stacking` 을 열로 두고 있다.

### 만든 것

- `scripts/research/accuracy_root_cause_v1/pose_by_condition.py`
- `data/pallet/results/accuracy_root_cause_v1/POSE_BY_CONDITION.json`
- `_docs/audits/accuracy_root_cause_v1/POSE_BY_CONDITION.md`

**새 추론 0회 · 새 집계 코드 0줄.** 전부 import 했다 —
per-frame 은 `POSE_PER_FRAME_BY_ARM.json`(7 arm x 319), 집계는
`evaluate_pose_solver_swap.summarize`, subgroup 정의는 `evaluate_arms.SUBGROUPS`,
조건 라벨은 `eval_workspace.evaluation_population_views`.

★**재현 검사 통과** — 이 경로로 다시 계산한 `R0 · ALL` 이 기존 `POSE_EVALUATION_R0.json` 의
`paths.MAIN.ALL` 과 기계정밀도까지 일치(n 319 · rot 2.262457894970192 ·
t 7.896851501830845 · iou 0.6031800861143982 · auc 0.42847335423197497).
즉 새 지표가 아니라 같은 지표를 다르게 자른 것이다.

### 결과

```
R0          n    axis↑   R med↓   t cm↓    IoU3D↑   ADDsym↑
Clean      184   0.853    1.741    4.643   0.6670   0.3870
Occlusion  135   0.607    3.122   11.436   0.5244   0.2880
Truncation  51   0.667    4.010    7.788   0.6040   0.4136
Far         59   0.644    2.685   15.928   0.4350   0.1564
Low        122   0.623    3.002   10.811   0.5593   0.3076
Mid        138   0.797    1.747    6.077   0.6346   0.4832
High        57   0.895    2.685    4.204   0.6186   0.3995
```

- 가림: axis 0.853 → 0.607, translation 2.5배, rotation 1.8배.
- 앙각: High → Low 에서 axis 0.895 → 0.623, translation 2.6배.
  **이 감사가 2D 에서 확정한 저앙각 병목이 6D 에서 같은 크기로 재현된다.**
- `LIMITATIONS.md` §3 의 "축 선택기 0.59~0.65(게이트 0.95)" 가 **저앙각·가림 구간에
  몰려 있다**는 것이 처음 보인다.
- ★Far 는 **2D 로는 최고(3.8 px)인데 6D 로는 최악(15.9 cm · IoU 0.435)** 이다.
  투영이 작아서 픽셀이 작을 뿐이다. 픽셀과 미터를 같은 방향으로 읽으면 안 된다.

### 주의 (문서에 명시)

subgroup 이 겹친다(주변부이지 독립 요인이 아니다) · **가림 구간은 GT 자체가 약해서**
axis 하락이 모델 오차와 GT 불확실성의 혼합이다(사람 리뷰 28% 판별 불가가 그 구간) ·
role = DEV · arm 간 우열은 CI 없이 주장하지 않는다.

논문 `generated/` 로 옮길지는 사용자 결정 사항이라 감사 namespace 에 둔다.

## 리뷰 도구 — "둘 다 틀림" 선택지 추가

사용자 지적: "a b 둘 다 틀린 게 있어". 맞다 — 1차 페이지에 그 선택지가 없어서
메모로 남기셔야 했고, 그게 감사의 핵심 발견(n_click=4 pose 검증 불가)이 됐다.

`REVIEW.html`(질문 1)과 `REVIEW2.html`(C 재리뷰) 양쪽에 `both_wrong` 을 추가하고
다운로드 CSV 에 `both_wrong` 열을 넣었다. 기존 응답은 localStorage 에 그대로 남아 있어
해당 프레임만 다시 고르면 된다. 누출 재검사 grep 0건.

## GT 리뷰 재집계 — 메모를 답으로 승격 (재작업 없이)

사용자 지적: **"이미 이전에 처음 할 때 둘 다 틀렸다고 적어놨어."**
맞다. 다시 클릭하라고 한 것이 잘못이었고, 이미 적힌 메모를 데이터로 반영하면 되는 일이었다.

내려받은 CSV 의 메모를 전수 확인하니 4건이고 앞서 찾은 것과 정확히 일치한다 [확인].
그 넷을 `cannot_tell` → `both_wrong` 으로 승격해 다시 셌다.

```
                초판            정정
A/B 판정        39 (72.2%)      39 (72.2%)     저장본 30/39 = 76.9% (불변)
둘 다 틀림       —              4 ( 7.4%)      ← 초판은 '모르겠음' 에 섞여 있었다
모르겠음        15 (27.8%)      11 (20.4%)
이항검정 p      5.3e-4          5.3e-4 (불변 — both_wrong 은 판정군에 없다)
```

**"모르겠음" 과 "둘 다 틀림" 은 완전히 다른 정보다.** 전자는 GT 해상도의 한계이고
후자는 GT 가 틀렸다는 뜻이다. 초판이 둘을 섞어 세고 있었다.

클릭수별로 보면 여전히 완전히 갈린다:

```
클릭   N    둘다틀림   모르겠음   저장본일치
 4    13     4 (31%)     2          5
 5    29     0           5         18
 6    12     0           4          7
```

`GT_REVIEW_RESULT.md` 에 정정 절 추가, `FINAL_DECISION.md` · `POSE_BY_CONDITION.md` 의
"28%" 표기를 "20.4%" 로 고쳤다. `GT_REVIEW_RESULTS.json` 은 `pick` 에 both_wrong 반영.

### 리뷰 도구 결함 2건 수정 (원인)

1. **「둘 다 틀림」 선택지가 없었다.** 그래서 리뷰어가 메모로 적어야 했다.
   → `REVIEW.html`·`REVIEW2.html` 양쪽에 추가.
2. ★**A/B 가설이 없는 4프레임은 답을 해도 CSV 에서 통째로 빠졌다.**
   `if(f.has_geom && s.hyp) fr.push(...)` 라서 `has_geom=false` 인
   `eval_pallet07__1778652142480077056` · `…144496057088` ·
   `eval_night09__1779449661263803392` · `…604823769344` 넷은
   질문 2 답과 메모가 조용히 버려졌다. → 답이 하나라도 있으면 내보내도록 수정.
3. 다운로드를 **한 파일로 합쳤다.** 두 파일 연속 다운로드를 브라우저가 막아
   지난번에 `REVIEW_FILLED_extrapolated.csv` 가 유실됐다. 이제 열이 한 CSV 에 다 들어간다.

세 번째 결함 때문에 **질문 2(외삽점 물리성) 답은 여전히 미수집**이다 —
브라우저 저장소에는 남아 있으므로 페이지를 열고 「CSV 내려받기」 한 번이면 회수된다.

## GT 리뷰 2·3차 회수와 결과 — 마지막 blocker 해소

사용자 요청 "너가해줘" — 「내려받기」를 누르게 하는 대신 브라우저 저장소를 직접 회수했다.

### 회수 경로 (시도 3개 중 3번째가 통했다)

1. **Chrome 확장으로 localStorage 읽기 → 실패.** 확장이 붙은 Chrome 은 이 머신의
   `127.0.0.1:8765` 에 닿지 못한다(`chrome-error://chromewebdata/`, `SecurityError`).
   별도 환경이다 [확인].
2. **Chrome leveldb 직접 파싱 → 실패.** `~/.config/google-chrome/Default/Local Storage/leveldb`
   에 origin `127.0.0.1:8765` 과 키 `gtreview_v1` 이 실재하나 값이 **snappy 압축 블록** 안이고
   `snappy`·`plyvel`·`ccl_chromium_reader` 가 env 에 없다 [확인].
3. **POST 받는 서버 + 자동 전송 페이지 → 성공.**
   신규 `scripts/research/accuracy_root_cause_v1/review_server.py` (127.0.0.1 바인딩,
   POST 는 `/save` 하나, 지정 파일에만 기록) + `human_review/EXPORT.html`.
   `xdg-open` 으로 사용자 Chrome 에서 열리자 7,486 bytes 가 들어왔다.
   → `data/pallet/results/accuracy_root_cause_v1/REVIEW_LOCALSTORAGE.json`
   `gtreview_v1` 58 · `gtreview_corner_v1` 15 · `gtreview_rep2_v1` 20.
   ★사용자가 B·C 구간을 이미 끝내 두었던 것을 이때 알았다.

함정 하나 — `pkill -f "http.server 8765"` 가 **자기 셸을 매칭해** 죽였다(exit 144).
memory `pgrep-self-match-hangs-wait-loops` 와 같은 종류다. 포트를 먼저 비우고 재실행했다.

### 결과 1 — 외삽 코너는 허공에 있지 않다 (§3.4)

15 프레임 · 코너 71개:

```
physical   24  (33.8%)
ambiguous  47  (66.2%)
virtual     0  ( 0.0%)   ★한 건도 없다 (클릭수 4/5/6 전부 0)
```

PnP 로 채운 점이 물체를 벗어나 엉뚱한 곳에 찍힌 경우는 **관측되지 않았다.**
단 66%가 검증 불가이므로 **"틀렸다"를 배제할 뿐 "맞다"를 확인하지 않는다** —
가려진 코너로 채점하면 PnP 모델과의 일치도를 재게 된다는 지적은 그대로 유효하다.

프레임 단위(1차 질문 2, 58장 전수)로는 더 보수적이다:
`전부 맞다 25(43.1%) · 모르겠음 21(36.2%) · 일부 어긋남 11(19.0%) · 대체로 엉뚱 1(1.7%)`.
모순이 아니라 질문이 다르다 — 코너 단위는 "그 자리에 모서리가 **있나**",
프레임 단위는 "제 자리에 **정확히** 있나".

### 결과 2 — 재현성 (§4, 마지막 blocker)

20장을 1차 답을 가린 채 재판정:

```
A -> A 6 · B -> B 6 · cannot_tell -> ct 5        일치 17/20 = 85.0%
A -> ct 2 · B -> ct 1                            ★A -> B 뒤집힘 0건
양 회차 모두 A/B 를 고른 12건 중 일치 12/12 = 100%
저장본 일치율  1차 10/12 · 2차 10/12 (동일)
```

**판정은 재현되고 흔들리는 것은 "판정할 수 있는가" 다.** §1 의 확신도 보정
(확신도 4~5 에서 저장본 17/17)과 같은 방향.

한계 — 계획한 "3일 간격" 이 아니라 **같은 날 같은 세션**이라 85%는 상한이다.
리뷰어 1명, N=20(이항 95% 구간 대략 [62%, 97%] `[추정]`).

### METHOD_LOCK rev5 — 착수 blocker 0

`failure_criterion` 에서 "GT repeatability 이하" 조항을 **뺐다.** 이번 리뷰가 잰 것은
이산 판정의 재현성(뒤집힘 0%)이지 픽셀 산포가 아니라서 크기 비교의 바닥값으로 쓸 수 없다.
기준은 **"세션 클러스터 95% CI 가 0 을 포함하면 실패"** 하나로 둔다.
status = `READY_TO_FREEZE`.

## flip 증강 라벨 감사 — 연산은 정확, 소스 필드가 위반본 (코드 변경 없음, 조사만)

사용자 질문("flip 학습할 때 어노테이션도 되돌렸나 / flip 본에서 앞면 좌상이 0 이 맞나")에
답하기 위한 전수 실측. 새로 고친 코드는 없다.

### 확인된 것

`challenge/scripts/dataset/gen_flip_noise_aug.py` 의 flip 은 연산 자체가 정확하다 —
미러 `w-1-x` + 좌우 짝 순열 `(1,0,3,2,5,4,7,6)` + centroid 고정. 851장 전수에서
`projected_cuboid` 가 미러+순열 계산값과 851/851 일치. 규약 준수율도 원본과 한 장도
다르지 않다(0<1 653, 2>3 654, 4<5 846, 6>7 844 — 원본·flip 동일).

학습 라벨도 전수 대조했다. `datasets/live_gt_v5_nocrop` 의 flip 라벨 696장을
pad=100 역변환하니 696/696 이 flip json 의 `projected_cuboid` 와 <1px 일치.
학습 측 `fliplr=0.0` 이므로 on-the-fly flip 은 없고 파일 증강만 쓴다.

### 문제 — 소스 필드

flip 이 뒤집은 `projected_cuboid` 는 LR 규약 위반 198/851(23.3%)인 계열이다.
정본 `keypoint_annotations` 는 위반 0/847. memory
`live-capture-gt-two-keypoint-conventions` 의 독립 실측(197/847)과 일치한다.

C4 phase 분해:
```
원본 GT      phase 0  45.0%   phase 270  44.4%   모호 9.9%   위반 0.7%
flip 산출물  phase 0  50.5%   phase  90  39.2%   모호 9.4%   위반 0.8%
noise 대조군 원본과 완전 동일
```
반사는 C4 회전군의 원소가 아니라 270 이 90 으로 반전된다.

### 배포 영향

HF `CanelE452/pallet-pose-yolo26n-c4` = `challenge_c4_track/F2/weights/best.pt`
(sha256 `38c8bc1a2fc128f320af83e6edb709d083dd168e6883649aad176f6ddc1a7a76`, 두 파일 동일)
의 학습셋 `live_gt_v5_nocrop`(train 2,088 = 원본 696 + flip 696 + noise 696,
val 155 에 파생본 0)은 09-05 빌드라 구 `load_kps`(projected_cuboid 우선)로 만들어졌다.
따라서 train 의 약 23%가 0 을 앞면 좌상이 아닌 곳에 붙인 채 학습됐다.
base `ft_live_gt_v4`(train 3,087, flip 696 포함)도 같은 계열.

### 미해결

`f2b2739` 가 `gen_flip_noise_aug.py::flip` 에 `keypoint_annotations` 를 함께 뒤집는
블록을, `load_kps` 에 그 필드 우선 읽기를 넣었다. 그러나 산출물
`challenge/data/03_derived/flip_noise_aug_livegt/` 는 09-04 18:13 생성분 그대로이고
그 안의 `keypoint_annotations` 는 851/851 이 원본 그대로다(미러·순열 둘 다 미적용).
→ 지금 재빌드하면 뒤집힌 이미지에 안 뒤집힌 라벨이 붙는다.
순서: `gen_flip_noise_aug.py` 재실행 → 데이터셋 재빌드(라벨 캐시 삭제) → 재학습.
사용자 판단 대기.

### 감시 실행 결과 — `test_keypoint_field_contract.py` 5 passed (0.18s)

이전 세션(13:04) 이 띄운 감시 건. 재실행으로 확인: **5 passed in 0.18s**.

다만 통과가 위 감사 결과와 모순되지 않는다 — 테스트 범위가 코드에 한정되기 때문이다.
- `test_flip_also_flips_keypoint_annotations` 는 `flip()` **함수**를 합성 입력으로 단위
  검사한다. 디스크의 `challenge/data/03_derived/flip_noise_aug_livegt/` 는 안 본다.
- `test_live_capture_gt_keypoint_annotations_obey_convention` 은 원본
  `live_capture_gt` 만 훑는다. 증강 산출물은 대상이 아니다.

즉 계약이 코드에만 걸려 있고 산출물에는 안 걸려 있다. 낡은 증강본이 그대로 있어도
초록불이 나온다. 재생성할 때 산출물 불변식(flip json 의 `keypoint_annotations` 가
원본과 미러+순열 관계인지)을 검사하는 테스트를 하나 더 거는 것이 맞다.

## 정본 라벨 + flip 제외 C4 재학습 착수 (사용자 지시)

사용자 지시: flip 증강을 빼고 확대축소·색감·밝기 augmentation 으로, 대칭 키포인트
순열을 모두 정답으로 인정하는 loss 로 재학습. C4 loss 는 이미 `pallet_yolo_loss/c4.py`
에 구현돼 있어(F0/F1/F2 가 그것) 실질 변경점은 **flip 제거**와 **라벨 필드 교정**이다.

계획 합의는 `plan-consensus` 로 진행했고 두 갈래를 사용자가 골랐다 —
데이터는 원본만 + on-the-fly 증강 강화, base 는 정본 라벨 indexed FT 후 C4 (2단계).

### 새로 만든 것

- `_docs/notes/c4-rotation-symmetry.md` — 계열 문서. §1 제안(가설·방법·**사전등록 판정
  기준**·예상 실패 모드·중단 기준). 결과는 나오는 대로 §2 에 이어 쓴다.
- `challenge/yolo_pose_one_model/challenge_c4_track/clean_label/PURPOSE.md` — 2줄
  ([소비처] HF 배포 교체 결정 / [문장] 정본 라벨 + flip 제외가 F2 대비 개선).
- `challenge/yolo_pose_one_model/challenge_c4_track/scripts/run_clean_label.py` —
  train -> eval -> verdict -> notify 를 한 파일에서. 완료 판정은 exit code 가 아니라
  `weights/best.pt` + `results.csv` 최종 epoch. gate 는 notes 에 먼저 쓰고 하드코딩.
- `challenge/yolo_pose_one_model/datasets/live_gt_v6_clean/` — train 696 / val 155
  (F2 와 같은 interleave 분할). 파일 증강 전부 제외.

### 착수 전 검증 (전부 실측)

```
새 데이터셋이 정말 정본 필드를 쓰나
  옛 v5_nocrop 과 겹치는 851장 중 라벨 달라짐  398 (46.8%)   <- memory 의 46.4% 와 일치
  LR 규약 위반   새 7/851 (0.8%)   vs   옛 523/2243 (23.3%)

k-회전 검사 (배포 base yolo26n-ft 예측 vs 라벨, val 155, 검출 155/155)
  새 라벨  k=0 3.71px  k=90 98.97  k=180 157.73  k=270 101.24   -> k=0 최소, PASS
  옛 라벨  k=0 8.29px  k=90 143.50 k=180 141.13  k=270  66.08   -> k=0 최소지만 2.2배 나쁨
```
base 는 새 라벨을 본 적이 없는데도 새 라벨과 더 잘 맞는다 — 정본 필드가 배포 규약이라는
독립 증거다.

### 스모크 (2 epoch, 1.2분) — 배선 전부 작동

```
                fixed med   C4 med   collapse   검출     perm 분포
새 모델(2ep)      2.38      2.37       2       153/155  0:147  90:5  270:1
F2               4.59      2.67       1       154/155  0:88   90:65
v4              25.40      1.89       4       154/155  0:78   90:76
```
새 모델은 phase 0 에 147/153 이 몰려 **index 일관성이 확보**됐다. F2·v4 가 반반으로
갈리는 것은 옛 규약으로 학습된 결과다. 채점은 넷 다 같은 새 val 155 로 했다.

### 본 학습 진행 중

2단계 각 120 epoch (F2 의 2,088x40 = 83,520 샘플과 총 업데이트를 맞춘 값).
증강 override: scale 0.25->0.40, hsv_v 0.35->0.50, hsv_s 0.50->0.60, fliplr 0.0 유지.
실측 5.8초/epoch 이라 약 24분 예상. 완료 시 Discord 로 판정이 온다.

### 결과 — PASS (22.6분, 120ep x 2단계)

`challenge_c4_track/clean_label/RESULT.json`. 넷 다 같은 새 val 155 로 채점.

```
arm                검출     fixed med  fixed p90   C4 med  C4 p90  collapse  rescued  pose mAP50-95
stage1_indexed   155/155      1.80       3.78      1.79    3.55       1         2        0.978
stage2_c4        155/155      1.90       3.82      1.88    3.78       1         2        0.960
F2               154/155      4.59     180.55      2.67    5.31       1        55        0.323
v4               154/155     25.40     171.48      1.89    3.51       4        71        0.295

permutation 분포   stage1/stage2 phase0 98.7%  |  F2 57.1%  |  v4 50.6%
```

사전등록 gate: C4 median 1.88 vs F2 2.67 (-0.79px, 문턱 -0.15) / collapse 1 vs 1 /
검출 155 vs 154 → **PASS**.

### 판정을 어떻게 읽어야 하나 (과대해석 방지)

- **개선된 것은 index 일관성이다.** phase0 50.6~57.1% → 98.7%, fixed median
  25.40 → 1.90px, fixed p90 171 → 3.8px. 배포가 요구하는 것이 이것이다.
- **위치 정확도 자체는 거의 그대로다.** phase 무관 지표인 C4-equivalent 로 보면
  stage2 1.88 vs v4 1.89 로 사실상 동일하다. F2 대비 -0.79px 는 F2 가 55장에서
  phase 가 돌아간(rescued 55) 탓이 크다. "정확도가 좋아졌다" 로 읽으면 틀린다.
- **C4 loss 의 이득은 확인되지 않았다.** stage1(indexed only) 1.79 가 stage2(C4)
  1.88 보다 근소하게 낫고, 차이 0.09px 는 사전등록 문턱 0.15px 미만이라 구분되지
  않는다 — NOT_ESTABLISHED 이지 해롭다는 뜻이 아니다. 라벨 위반을 23.3% → 0.8% 로
  낮추자 C4 가 흡수할 모호성이 거의 남지 않은 것으로 보인다 [추정].
- **적용범위**: val 155 는 interleave(같은 세션)라 과제 트랙의 의도된 계약이지만
  **새 세션 일반화의 근거로 인용하면 안 된다**
  ([[live-gt-ft-split-and-aug-decide-the-verdict]] 의 적용범위 절).
  일반화를 말하려면 SEALED 105 가 필요한데 그건 봉인 소진이라 사용자 결정 사항이다.

배포 후보는 `stage1_indexed` 다(모든 지표에서 stage2 이상, pose mAP 0.978). 다만
HF 승격은 사용자 결정 대기.

## accuracy_root_cause_v1 — commit · push (사용자 요청)

`71ef40b..f2b2739  main -> main` [확인, push 출력]. 커밋 2개로 나눴다.

```
3d4461e  audit: the binding constraint is corner localisation at low camera elevation
         41 files · +10,236   감사 문서 21 · 결과 데이터 · 스크립트 13 ·
                              리뷰 도구(REVIEW/REVIEW2/EXPORT.html)와 사람 리뷰 답
f2b2739  fix: training read the keypoint field that violates the corner convention
          8 files · +268/-17  load_kps → keypoint_annotations · flip 동반 수정 ·
                              정사각 물체 C4 계약 · CLAUDE.md 161→140 · docstring 정정
```

착수 전 확인한 것 — 코드 변경분 테스트 **67 passed**, `git fetch` 후 원격이 뒤처지지
않고 fast-forward, 스테이징 후 의도 외 파일 0건.

★**작업 트리의 다른 작업 26건은 담지 않았다.** 세션 시작 시점에 이미 수정돼 있던
파일들(annotate 계열·pallet_eval_v1 workspace·pallet_yolo_loss 등)이라 개별 스테이징으로
내 파일만 올렸다. `git add -A` 를 쓰지 않았다.

PNG 247장(약 137 MB, contact sheet 4 + full 79 + 리뷰 패킷 224)은 `.gitignore:98 *.png`
로 제외된다. `build_human_review_packet.py` 와 `build_false_positive_sheets.py` 가
seed 고정이라 A/B 배정까지 동일하게 재생성된다.

### 이 커밋이 남긴 미해결 — 다른 세션이 이어서 찾았다

같은 날 다른 세션이 위 §"미해결" 절에 적었다: `f2b2739` 는 **코드**를 고쳤지만
산출물 `challenge/data/03_derived/flip_noise_aug_livegt/` 는 09-04 생성분 그대로여서
그 안의 `keypoint_annotations` 851/851 이 미러·순열 미적용 상태다.
지금 재빌드하면 뒤집힌 이미지에 안 뒤집힌 라벨이 붙는다.

그리고 그쪽이 정확히 지적한 대로 `test_keypoint_field_contract.py` 5 passed 는
이 문제를 못 잡는다 — `flip()` 함수를 합성 입력으로 단위 검사하고 원본
`live_capture_gt` 만 훑기 때문에 **디스크의 낡은 증강본은 검사 범위 밖**이다.
계약이 코드에만 걸려 있고 산출물에는 안 걸려 있다.
→ 산출물 불변식(flip json 의 `keypoint_annotations` 가 원본과 미러+순열 관계인가)을
검사하는 테스트를 하나 더 거는 것이 맞다. 이건 내 커밋의 빈틈이다.

### 다음

`METHOD_LOCK` rev5 = `READY_TO_FREEZE`, blocker 0.
리뷰 서버(`review_server.py`, 127.0.0.1:8765)는 detach 상태로 남겨 두었다 — 종료는 PID kill.
