# 2026-07-29

## PAPER_S2 ep57 target-semantics audit (Step 1~9, 진단 전용)

목적: ep57 real 실패가 (A) target semantics bug / (B) truncation augmentation
mismatch / (C) sim-to-real visual gap 중 무엇인지 판정. 성능 개선·신규 학습 없음.

보고서: `data/pallet/results/paper_s2_target_semantics_audit/AUDIT_REPORT.md`
provenance: 같은 폴더 `RUN_PROVENANCE.md` (checkpoint SHA 지정값 일치 확인)

### 신규 파일 (전부 읽기 전용 감사, 기존 데이터/가중치 무수정)

```
scripts/stage0/paper_s2_target_semantics_audit.py        loader 실제 경로로 target 생성·기록
scripts/stage0/paper_s2_target_contradiction_tables.py   C0~C4 집계 (Gate A)
scripts/stage0/paper_s2_truncation_distribution_audit.py aug vs real 분포 (Gate B)
scripts/stage0/paper_s2_clip_border_counterfactual.py    clip False/True 대조 (Step 4)
scripts/stage0/paper_s2_decoder_parity_audit.py          D0~D3 (Step 7)
scripts/stage0/paper_s2_failure_association.py           ep57 실패 연관 (Step 8)
scripts/stage0/paper_s2_audit_figures.py                 그림 11종
data/pallet/results/paper_s2_target_semantics_audit/     PURPOSE.md 포함 산출물 일체
```

### 확인된 사실 [확인]

1. **H1 target semantics defect = 실재.** `CreateBeliefMap(clip_at_border=False)`은
   `full_support_inside` 거짓이면 `centre_inside`여도 채널을 all-zero로 만들고,
   `belief_channel_mask`는 JSON `pseudo_keypoint_valid` 부재 시 all-ones이며
   **spatial transform 이전**에 계산된다 → mask=1 + all-zero target으로 MSE 감독.
   측정: 263,772 kp 중 **10,463 (3.967%)**, 그중 mask=0은 **0건**(예외 경로 없음).
   ep57은 sigma=2 → `w=4` → 400px 입력의 바깥 32px 테두리가 데드밴드.
   border-distance bin에서 0~3px = C2 100%, 4px 이상 = 0% (결정론적).

2. **H2 truncation aug mismatch = 실재, 설계에 기인.** `utils_dataset.py:104-114`가
   `_TRUNC_MARGIN_FRAC=0.20`으로 모든 코너를 중앙 [0.20,0.80] 밴드에 reflect-pad
   (= H1 데드존 회피 목적). 결과: aug_trunc_v2 in-band 96.5% / 화면밖 0.0% vs
   real truncated(17) in-band 43.1% / 화면밖 27.5%.

3. **H3 DiffPnP coverage = 0.0%.** ep57이 소비한 index 기준 aug_trunc_v2 /
   aug_squash_v2 / aug_scale_v2 전부 `pnp_valid_3d=0` (V8은 100%/95.9%/96.0%).
   → "DiffPnP가 truncation을 regularize했다"는 서술 **금지 확정**.

4. **H4 기각.** C2 최대는 kp2(6.58%)/kp3(6.50%), kp5=3.75%, kp7=2.50% 최소.
   near(5.82%) > far(3.10%). kp5 특이 버그 가설 폐기.

5. **H5 강등.** D0(training clamp) vs D1(border-safe) = **0.000px**, clamp
   duplicate는 783개 중 3개(0.4%). 단 D2(eval) vs D3 = 7.030px =
   `0.4395×hypot(12.8,9.6)` 상수 offset → train/eval convention 별건 이슈.

6. **clip=True 반사실.** C2 761개 전부(100%) peak 복구(중앙값 0.979),
   C1 control은 T0==T1 완전 동일, 화면 밖 keypoint는 T1도 0.

### 판정 — 단일 원인 아님, subset별로 갈림

- **B(truncation mismatch)**: real truncated 17 frame에 대해 지지. 그 위에서
  ep57 belief peak 중앙값 0.062, missing 69.9% = 완전 붕괴.
- **C(sim-to-real gap)**: non-truncated 70 frame(다수)의 잔여 설명. peak 0.85로
  잘 검출되는데 err 13.86px. 기존 memory(STAGE16 REJECT, REAR 병목)와 정합.
- **A(target bug)**: 결함은 확인되나 실패 분포와 **역방향**(C2 많은 near가 real에서
  더 정확) → 주원인 근거 없음. 별건으로 고칠 가치는 있음.

### 주의 / 한계

- Step 8은 연관 분석뿐 — 인과는 matched retraining 필요 ([추정]).
- real truncated 17 frame = 소표본. decoder_parity err는 index-wise(order-free 아님).
- final-test open count 0 유지. 신규 full training 미실행. checkpoint SHA 불변 확인.

---

## PAPER_S2 ep57 mechanism diagnostic harness (진단 전용, 학습 없음)

### 목적

의심 하나마다 감사 스크립트를 새로 만드는 방식을 끝내고, **1회 forward → cache →
오프라인 반복 개입** 구조로 전환한다. 산출물은 새 모델이 아니라 (1) 실패 프레임의
최초 붕괴 단계, (2) 그에 대응하는 architecture/loss 우선순위.

### 신규 파일

- `scripts/stage0/paper_s2_mechanism_diagnostic.py` — 통합 runner.
  subcommand `--build-cache / --interventions / --counterfactuals / --report / --all`.
  기존 감사 코드를 재구현하지 않고 import 재사용:
  `paper_s2_frozen_diagnostic`(manifest/decoder/PnP/sealed guard),
  `paper_s2_decoder_parity_audit`(D0/D1/D3), `filter_pr_camfacing`(D2 canonical),
  `annotate_pnp`(solve_pose/project_3d/3D model).
- `challenge/tests/test_paper_s2_mechanism_diagnostic.py` — Phase M 테스트 18개.
- `_docs/audits/` (신규 폴더) — `data/` 가 .gitignore 대상이라 핵심 Markdown 4종만 복사.
  기존 `_docs/` 에 감사 보고서용 폴더가 없어 신설. `_docs/README.md` 에 목차 추가.

### 산출물 (로컬, git 제외)

`data/pallet/results/paper_s2_mechanism_diagnostic/` — manifest, frames/keypoints/
poses/interventions/counterfactuals parquet, 그림 9종, 예시 오버레이 28장,
MECHANISM_DIAGNOSTIC_REPORT / ARCHITECTURE_DECISION / MICRO_TRAIN_PLAN / RUN_PROVENANCE.

### Baseline 재현 게이트 (Phase D) — PASS, 비트 단위 일치

frozen `full_ep57_frozen_20260728` 대비: strict N=87 동일 membership,
GT-2D PnP 87/87, predicted local-softargmax PnP 70/87,
yaw median **6.02521554515198°**, fixed-GT reproj median **23.161628981707747 px**
— 기존 감사와 소수점까지 동일.

### 디버깅 — belief cache float16 이 baseline 을 깼다

처음에 belief stage tensor 를 float16 으로 저장했더니 reproj median 이
23.162 → **22.641 px** 로 어긋나 게이트가 BLOCKED. 원인은 temperature 0.1
local softargmax 가 fp16 반올림에 민감해 좌표가 미세 이동 → PnP candidate 선택이
일부 프레임에서 뒤집힘. Before: `belief_stages.astype(np.float16)` /
After: `astype(np.float32)` (affinity·seg 는 진단 전용이라 fp16 유지).
캐시 46MB. **교훈: 좌표·pose 를 유도하는 tensor 는 fp16 금지.**

또한 truncation 정의를 frozen 과 맞춤 — `n_in < 8`(finite corner 중 화면 안 개수)
로 바꿔야 17/70 split 재현 (처음엔 "valid 이면서 화면 밖" 으로 세어 13/87 나옴).

### 결과 (strict N87, ep57 frozen)

```
failure class            frames   first_break_stage              frames
──────────────────────────────────────────────────────────────────────
F2_CONFIDENT_WRONG       35       REPRESENTATION_LOCALIZATION    33
F1_NO_RESPONSE           24       IMAGE_OR_RESPONSE              24
F5_MIXED                 23       POSE_REFINEMENT                14
F3_GEOMETRY_AMPLIFIED     4       DECODER                        10
F4_SOLVER_SPECIFIC        1       PNP_GEOMETRY                    3
                                  MIXED_OR_UNRESOLVED             3
```

- **F5_MIXED 23 중 20 은 사실상 실패가 아님** (pose 성공 + yaw ≤5°). catch-all 이
  정상 프레임을 흡수 — "혼합 실패"로 읽으면 안 됨.
- **decoder 는 주원인 아님**: D0~D5 어느 것도 판정 기준(pose success +5%p /
  yaw −20% / 2D −5px) 미달. frame 단위 10건 회복은 PnP candidate 불안정성 [추정].
- **stage-wise**: EARLY_WRONG 342 vs LATE_DRIFT 80. far face 오차 stage1 31.9px →
  stage4 21.4px(최저) → stage6 22.1px 정체, 같은 구간 peak 0.38→0.86(2.2배).
  near 는 10.0→6.9px. → **"late drift" 가설 기각**, 실체는 "초기 오류를 refinement 가
  못 고치고 신뢰도만 올린다".
- **oracle**: O10_all_corners/O11_all 이 실패 17 전부 회복. target class 안 yaw 개선은
  A(F1) −7.6°, B(F2) −2.5°. O9_centroid 0 회복(+0.18°) → centroid 보정 기각.
  단일 kp GT 치환은 프레임의 44~69% 에서만 개선 → 상수 픽셀 보정 기각.
  A_drop_*/LOO_* 는 실패 회복 0건, far 통째 제거는 14 프레임 손실 → "나쁜 점 제거·가중치
  하향" 만으로는 회복 불가 (Candidate C 보류 근거).
- **counterfactual (same-source, affine 정합 오차 5.7e-14px)**: crop-only 는 corner
  검출 −2(shallow)/−6(deep), pose success −42%p/−63%p. 같은 crop 을 원 캔버스로
  reflect-pad 하면 검출이 원본 수준 복귀(−8%p/−17%p). **padding 은 response 를
  회복시키지만 far 2D 오차·yaw 는 회복 못 함.** blur(C10)는 붕괴, 저조도(C11)·축소(C9)는 무해.
- **미검출 corner 의 위치**: F1 미검출 corner 157 중 **125 개가 GT 화면 안**
  (화면 밖 21, sentinel 11), 프레임 15/24 는 미검출이 전부 화면 안.
  → F1 을 "화면 밖 supervision 문제"로만 좁히면 대부분을 놓침.

### architecture 우선순위 (진단 결론, 구현 안 함)

1. B far/depth structured refinement (F2, 35프레임)
2. A visibility/outside-aware partial keypoint (F1, 24프레임) — crop-only 증강 동반
3. C calibrated uncertainty + robust PnP (F3, 4프레임)
4. D direct pose fallback (F4, 1프레임) — 표본 부족, 보류

### 무결성

final-test open count **0**, prohibited attempts 없음, checkpoint SHA 불변,
기존 데이터/JSON/PNG 덮어쓰기 없음, 신규 학습 미실행.
pytest `challenge/tests/` **70 passed** (신규 18 포함).

---

## PAPER_S2 micro architecture screen (M0_B / B1 / M0_A / A1 실행, B2 미실행)

### 목적

mechanism diagnosis 후보 2종이 자기 target failure class 를 고치는지 **matched
control 대비로만** 판정. 논문 최종 학습 아님.

### 신규/수정 파일

- `Deep_Object_Pose/common/belief_residual.py` (신규) — `BoundedBeliefResidual`.
  zero-init 출력 conv + `0.25*tanh` bound + detached 입력. frozen base 가 anchor 라
  별도 anchor loss 불필요.
- `Deep_Object_Pose/common/utils_belief.py` — `spatial_keypoint_validity()` 추가.
  transformed 좌표 기준 채널 validity (Case 2 화면밖 / Case 3 sentinel / Case 4 corrupt
  fail-closed). pseudo-label 전용 mask 함수에 의미를 섞지 않고 공용 함수로 분리.
- `Deep_Object_Pose/common/utils_dataset.py` — loader 에 `spatial_keypoint_mask`
  opt-in 추가 (default off = legacy byte-identical). 기존 `clip_belief_border` 재사용.
- `scripts/stage0/paper_s2_micro_arch_screen.py` (신규) — 통합 runner.
  evaluator 는 `paper_s2_mechanism_diagnostic` 재사용(신규 evaluator 없음).
- `challenge/tests/test_paper_s2_micro_arch_screen.py` (신규) — 22 테스트.

### ★ 최대 발견 — B 계열은 설계대로 시험 불가 (transfer gap)

`manifest_B_screening.parquet` (training pool 4000장 ep57 frozen inference):

```
                        far/depth 2D error   median   max     >20px
synthetic training pool (usable 3870)        2.12px   16.02   0
real mechanism-val F2   (N=35)               43.3px   160.7   35
```

**F2_CONFIDENT_WRONG 은 synthetic 학습 데이터에 존재하지 않는 실패 모드**다.
수를 억지로 채우지 않고 hardest available(>=3.66px)로 대체 후 manifest 에
`hard_criterion.criterion_met=false` 로 기록. B1 의 FAIL 은 **후보 기각이 아니라
시험 불가**이며, "B1 FAIL → backbone 으로 이동" 규칙은 적용하지 않는다.
근거 보강: B1 은 **학습 도메인(synthetic hard)에서도** far 4.566→4.546px(0.4%)만
변함 = 고칠 오차가 없었다(일반화 실패 아님).

### 결과

```
arm    F2 far2D   전체 matched2D   전체 yaw   전체 peak   F1 inframe검출률
────────────────────────────────────────────────────────────────────────
ep57   43.344     13.656          6.025      0.822       0.2188
M0_B   43.344     13.656          6.025      0.822       0.2188
B1     43.342     13.652          5.973      0.824       0.2188
M0_A   44.020     13.719          5.822      0.838       0.2188
A1     43.763     13.626          6.089      0.840       0.2188
```

- **M0_B**: 2 epoch 연속 악화로 조기중단, best=epoch0 → 400장 fine-tuning 은
  F2 에 효과 0. (M0_A 는 10 epoch, ep57 대비 far 최대 Δ77.9px 로 실제 변함.)
- **B1 FAIL** (보류/시험불가). zero-init identity `max|H_final-H_base|=0` 검증됨.
- **A1 FAIL** (보류/판별력 부족). 단 **방향은 일관** — F1 frame median peak paired
  delta +0.00178, **20/24 프레임 상승**. 그러나 F1 peak 0.105 vs 검출임계 0.30 격차가
  두 자릿수라 GT-in-frame 검출률이 35/160 으로 **비트 단위 상수**. primary 지표가
  이 표본에서 판별력이 없었다.
- **B2 미실행** — B1 gate 미통과, 조건부 규칙 준수.

### 디버깅

1. early stop 이 "동일"을 "악화"로 세어 전 arm 이 2 epoch(≈100 step)에서 중단.
   지시문의 "2 epoch 연속 **악화**" = strictly worse 로 수정.
2. primary metric 이 tie 로 고정되면 best_state 가 epoch0(=ep57)에 머물러
   **학습 결과가 평가되지 않음**. tie-break 를 later-epoch 로 수정.
   (수정 후 M0_A 가 ep57 과 실제로 달라짐을 확인.)
3. ablation 테스트가 M0_B→B1 이 `residual`+`trainable` 둘 다 바꾼다고 실패 —
   지시문 Phase 12 표 자체가 그렇게 설계됨(residual 추가 = 학습 scope 변경).
   capacity 는 파라미터 수로 통제(residual 19,897 vs m6_2 tail 17,673 = +12.6%,
   ±20% 이내). 테스트를 그 사실에 맞게 수정.

### 다음 admissible experiment

1. B 계열 재시험 전 **real F2 분포를 담은 학습 소스 확보**가 선결. 없으면 어떤
   final-stage/backbone 후보도 F2 에 대해 판정 불가.
2. 그전까지 F2 는 학습후보 문제가 아니라 **데이터/도메인 문제(sim2real)** 로 다룬다.
   STAGE16 결론과 같은 방향.
3. A1 재시험은 검출률 대신 **연속 지표(frame median peak)** 를 primary 로.

### 무결성

final-test open count **0**, checkpoint SHA 불변, mechanism-val N87 은 학습 manifest 에
0장(테스트로 강제), 데이터 덮어쓰기 없음, full training 미실행.
pytest `challenge/tests/` **92 passed** (신규 22 포함).
⚠ mechanism-val 이 best-epoch 선택에 쓰였으므로 수치는 낙관적 상한이다.
