# 2026-07-06

## PAPER_STAGE_A — paper_base_v2 논문 protocol 고정 + S1 mask readiness 감사

3d-expert 위임. 학습 실행 없음(측정/감사/config-gate만). conda: pallet-pose.

### PART 1 — 패딩 ablation (논문 정량 main protocol 고정)
- 신규 스크립트: `scripts/stage0/paper_stage_a_pad_ablation.py`
  (stage25_paperbase_eval + eval_capturecad_b2.eval_frame 재사용, order-free Hungarian/solve_pose/honest8/per-frame K).
- weights = `weights/paper_base_v2/final_net_epoch_0060.pth` (무패딩 학습: aspect + truncation_aug_prob=0.0).
- 전처리 4종: A_nopad(pad0=aspect parity) / B_pad50 / C_pad100 / D_pad150 (reflect).
- 평가셋: filterval N=123(주 신호, split-lock) + handannot17 N=17(고앙각 정성).
- ★ cross-check: C_pad100 filterval = STAGE25 완전 일치(det79/corner22.6/rear33.5/honest25.1/good21.8/gross51.6) → 인프라 정합 검증.
- **결과 = metric축별 tradeoff (클린한 no-pad 우세 아님)**:
  - 순도 good%/gross% + parity → A_nopad (good 28.7, gross 48.1; outside 도메인 압도)
  - median corner/rear/honest8 → B_pad50 (22.2/30.9/24.2)
  - 검출/PnP 커버리지 → C_pad100 (det79/pnp79)
  - D_pad150 = dominated → 폐기
- **판정**: main 정량 protocol = **A_nopad**(parity + 순도, STAGE25 pad100 대비 good 21.8→28.7·gross 51.6→48.1 개선, 단 det 79→68·corner 22.6→27.5 비용). C_pad100 = detection/demo 보조. honest caveat 병기(no-pad=정밀우선/pad=커버리지우선, 소표본).
- 출력: `data/pallet/eval_results/paper_stage_a/{paper_base_v2_pad_ablation_report.md, pad_ablation_summary.md, eval.json}`.

### PART 2 — mask aux readiness (S1 게이트)
- paper_base_v2 학습 5소스(mixed_v8_train 9000 / v4_split_base 4000 / aug_squash_v2 2212 / aug_trunc_v2 2971 / aug_scale_v2 1125) 전수 스캔.
- **결과: visible mask 전무.** mask obj-key 0, frames_with_mask 0/19308, seg PNG/mask 디렉토리 0건. mixed_v8 `.json.orig`도 mask 없음(Isaac instance seg 미저장). aug류는 v4 warp 파생이라 상속 원천 없음.
- 대조: challenge track `addon_v1_train`(visible_mask/mask_rle) + v3(mask_rle) 만 valid mask 보유 → B2 mask_aux 출처지만 **paper 금지**(v1v2/v3/addon challenge 전용).
- 메커니즘 확인[확인]: utils_dataset.py mask_aux 경로 = mask_rle 없으면 pvnet_mask_valid=0 → mask loss 마스크아웃 → seg head 학습신호 0(가짜 S1).
- 출력: `data/pallet/eval_results/paper_stage_a/paper_mask_readiness_report.md`.

### PART 3 — Paper-S1 quick-FT config
- **BLOCKED**: paper 트랙 mask 부재 → config 미작성(억지 config = no-op이라 정직 보고).
- 대안: (1)정공법 Isaac 재렌더+instance seg annotator→mask_rle (2)Blender 재렌더 (3)근사 SAM(위험/보조). 하지말것=cuboid-hull/bbox 가짜 mask(과포함 confound).
- mask 확보 시 쓸 B2 recipe 대기 명시(mask_aux/mask_weight0.01/mask_warmup0/encoder_lr_scale0.1/freeze750/net_path=paper ckpt, paper 데이터만).

## PAPER_STAGE_B — paper_base_v2 no-pad 공식 baseline 확정 (재실행 없이 문서화)
- 사용자 지적: paper_base_v2는 무패딩/aspect 학습 모델, pad100은 추론 workaround. 지금 할 것=새 학습 아니라 no-pad 공식 평가/문서화. (mask 없어 지금 재학습=재현성 확인뿐, rear/mask/trunc 다 못 품.)
- PAPER_STAGE_A eval.json 재사용(재실행 X)해 공식 baseline 문서 생성: `data/pallet/eval_results/paper_stage_a/PAPER_STAGE_B_official_baseline.md`.
- ★official 정량 protocol = A_nopad(aspect): filterval good% 28.7/gross 48.1/corner 27.5/rear 34.7/honest8 31.7/det 68/PnP 71. demo = C_pad100(det 79이나 순도↓ good21.8/gross51.6). STAGE25가 pad100이라 good% 과소평가였음(21.8→28.7).
- 정직 caveat: median축(corner/rear/honest)은 B_pad50 최강(corner22.2)이나 parity 밖→robustness ablation행만. 소표본. rear>front 전 pad·전 set 일관(rear 병목).
- 학습 계획 확정: 지금 재학습 ✗(mask 없어 mask-aux no-op=PAPER_STAGE_A 확인, 새 데이터도 無=개선 여지 0). 진짜 Paper-S1 = paper-safe mask dataset 생긴 뒤 init=paper_base_v2, data=원본+새mask, loss=원본 heatmap-only/새데이터 heatmap+0.01 mask-aux, inference mask hard-gate 금지.
- mask 있는 non-palletobj 데이터 부재 확정: mask 보유 전부 palletobj_v1(v3/addon/trunc, 논문금지), paper 트랙(mixed_v8/v4_split=Pallet_3 procedural)은 mask 0. "논문OK∩mask"=공집합 → S1 위해 paper procedural 재렌더+seg 필요.

## PAPER_READY_UBUNTU — Windows 생성 전 Ubuntu 준비물(validator/config/docs)

3d-expert 위임. ★생성/학습 실행 없음(준비물만). conda: pallet-pose. 기존 인프라 재사용, 중복 폴더 금지.
산출: `data/pallet/results/paper_ready_ubuntu/` (validator만 `scripts/data_prep/validate/`).

### 작업1 — dataset validator (신규)
- 파일: `scripts/data_prep/validate/paper_dataset_validator.py` (argparse --root, --max_frames, --n_overlays, --border_px, --reproj_p99_tol).
- 재사용: audit_addon_v1(FLIP=diag(1,-1,-1)/Rwc^T(Xw-C) projection, COCO RLE decode, ordering_check) + stage16(V_geom/elev/frsep) + stage22 overlay(front0-3 cyan/rear4-7 orange, sentinel[-1,-1]+nan edge skip).
- 검사: RGB/JSON/mask_rle 존재 · RLE decode(compressed str 감지) · mask area==json area · projected_cuboid round-trip reproj p99<1px · face grouping(top{0,1,4,5}/bot{2,3,6,7}, front/rear) · clamp_suspect(화면밖 코너가 프레임 안으로 clamp됐나) · border_suppress(경계근접 코너 sentinel 처리됐나) · V_geom/heatmap_valid 분포 · overlay 20장. 출력 {root}/audit_report.json + bad_frames.txt + overlays/.
- ★sidecar json(`.face_centers.json`) 제외: `_is_frame_json`(단일 dot basename).
- smoke(addon_v1 300f, 논문금지지만 schema 동일): verdict PASS, mask 300/300 100%, reproj p50-of-p50 0.009px/p99-of-p99 0.045px, ordering 100%, clamp_suspect 0/offscreen_alive 151/border_suppress 0, V_geom{8:235,6:35,...}. addon smoke 산출물은 정리(삭제).

### 작업2 — Paper-S1 train config + 런처 (신규, 실행 안 함)
- `data/pallet/results/paper_ready_ubuntu/paper_s1_train_config.yaml` + `run_paper_s1.sh`(chmod +x, NEW_MASK TBD면 exit 2로 실행 거부).
- init=paper_base_v2 `weights/paper_base_v2/final_net_epoch_0060.pth`(★v3/addon/B2/challenge0123 금지). recipe=paper_base_v2 header.txt(5소스: mixed_v8_train/v4_split_base/aug_squash_v2/aug_trunc_v2/aug_scale_v2=19,308, imagesize448, sigma4.0, trunc_aug_prob0.0) + B2 mask-aux(--mask_aux --mask_weight0.01 --mask_warmup0, enc_lr_scale0.1, freeze750, epochs cumulative 60->70).
- [확인] train.py: --mask_aux→numSeg=1, mask loss는 pvnet_mask_valid==1 프레임만(L191-212), 추론 belief-peak(hard-gate 없음). utils_dataset L406-414: mask_rle 있을 때만 decode+INTER_NEAREST resize, L614 mask_rle 없으면 valid=0→mask loss 0. → paper-original(mask_rle 없음)=heatmap only 자동, 코드수정 불필요.
- placeholder(TBD): data.new_mask 경로, balance_groups NEW_MASK_TOKEN, replay비율(3:1 예시), epochs delta.

### 작업3 — official baseline doc
- `data/pallet/results/paper_ready_ubuntu/official_baseline_protocol.md` (PAPER_STAGE_B 이관/보강, 재실행 없음).
- paper_base_v2 A_nopad=official 정량 / pad100=정성·robustness ablation / B2=private-CAD 상한(논문 main 제외).
- ★paper↔B2 gap = detection 아닌 localization. STAGE25 filterval: det% paper79≈B2 75(under-detect 아님), corner 22.6 vs 13.5(~9px)·rear 33.5 vs 16.8(~17px)·honest8 25.1 vs 15.6(~9.5px) = rear+저앙각 flat-view localization 결손(= v3/addon replay 안 친 비용). 개선은 검출 고정한 채 이 gap 회복으로 측정.

### 작업4 — honest eval protocol doc
- `data/pallet/results/paper_ready_ubuntu/eval_protocol_honest.md`.
- ★STAGE20 교훈: selected reproj(pnp_reproj_click, pred↔PnP 자기일관성)=red herring / honest8(pnp_honest8, solved 8corner GTdim↔GT projected_cuboid, order-free hungarian)=진실. headline은 honest8만.
- bin: V=8/V<8 · front(0-3)/rear(4-7) · elev(-90~5/5~10/10~15/15~25/25~90) · near/large. false_accept(det+pnp_ok인데 honest8>20px) count 추가 지정. order-free hungarian + 극근접 swap 교차검증. no-pad official/pad100 별도.
- 함수 매핑: eval_capturecad_b2(pad_frame/hungarian/eval_frame→honest8·selected/agg) + stage25.summarize + paper_stage_a.summarize_breakdown/elev_dist + annotate_pnp.solve_pose(ITERATIVE) + stage18.elev_from_pose.

## PAPER_READY_UBUNTU 완료 — 준비물 4종 (생성/학습 없음)
- 산출: scripts/data_prep/validate/paper_dataset_validator.py + data/pallet/results/paper_ready_ubuntu/{paper_s1_train_config.yaml, run_paper_s1.sh, official_baseline_protocol.md, eval_protocol_honest.md}.
- 작업1 validator smoke(addon 300f 형식검증): PASS, mask area 300/300, reproj p99 0.045px, ordering 100%, clamp 0/offscreen 151/border_suppress 0. 재사용 audit_addon_v1+stage16+stage22 overlay. sidecar face_centers 제외 처리.
- 작업2 config: init=paper_base_v2 ep0060, heatmap+mask-aux. paper-original(mask_rle 없음)=heatmap only 자동(train.py pvnet_mask_valid, utils_dataset INTER_NEAREST, 코드수정 불필요 확인). 새 mask데이터=placeholder TBD, 런처 NEW_MASK TBD면 exit2. mask hard-gate 금지, no-pad, encoder_lr 0.1/freeze750/mask_weight0.01.
- 작업3 baseline doc: no-pad=official/pad100=demo/B2=private 상한. gap=detection 아닌 localization(det paper79≈B2 75, gap=corner~9/rear~17/honest~9.5px).
- 작업4 honest doc: selected reproj(red herring) vs honest8(진실) 분리, bin(V8/V<8·front/rear·elev5·near/large)+false_accept. final-test 봉인 명시.
- ★agent 의심 검증: config의 mixed_v8_train이 "challenge 계열 아니냐" 물음 → **기우, procedural로 안전** [확인]. mixed_v8_train 오브젝트=Pallet_2(procedural CAD, dims 1.09×0.20×1.10), palletobj_v1(name=palletobj_v1, 1.1×1.3×0.12) 아님. memory "mixed_v8=challenge0123셋"은 mixed_v8+palletobj v1/v2 합친 셋 얘기지 mixed_v8 단독 아님. paper_base_v2(mixed_v8+v4_split+aug)=palletobj 없음=논문안전, config init과 소스 일치 = 위반 아님. 뺄 필요 없음.

## paper_4pallet_mask_v1 audit (학습 전 게이트) — 품질 PASS, 성격=clean full-view base
- 데이터: Downloads train_4pallet_mask_v1.zip(9.7GB, 7/6) → data/pallet/training_data/paper_4pallet_mask_v1/ (10000, procedural 4 pallet, mask_rle, camera_dynamic_0123_v4, fx615). 논문 안전(palletobj_v1 아님).
- audit 12항목: 1개수 PASS(10000×4) / 2 asset분포 PASS(scene.usd=Pallet_0 49.8%, 나머지 각16.6-16.9%=기대일치) / 3 emissive PASS(빛남 0, clip0%) / 4 mask_rle PASS(area 10000/10000 일치, {0,1}, empty0/fullframe0, 슬랫구멍 있음) / 5 overlay PASS(60타일 눈검증 밀착) / 6 convention PASS(reproj p99~0, top/bot·sep·front-near 100%) / 7 clamp PASS(0) / 8 per-corner status ★부재 / 9 V PASS*(V=8 100%) / 10 truncation NONE(off-screen 0, cut 0) / 11 geometry PASS / 12 montage DONE.
- ★스키마 함정 2개[확인]: (a) camera_data에 카메라 rotation quaternion 없음→방금 만든 paper_dataset_validator.py의 project()(quaternion 사용) 이 데이터엔 작동불가, pose_transform(obj→cam OpenCV)+cuboid(world) 기반 재작성해 audit. (b) mask/*.png는 값{0,1,2,254,255} 화면 79% semantic viz=팔레트마스크 아님→학습은 반드시 mask_rle.
- ★per-corner status 부재: inside_image/heatmap_valid/V_geom/missing_bitmask 전부 없음. 있는키=visibility/raycast_visibility/front_facing_cos/front_is_camera_near/visible_mask/perm_v4/ratio_*. V는 projected_cuboid 화면안 개수로 계산(전프레임 8). off-screen 0이라 per-corner 불필요.
- 분포: asset Pallet_0(scene) 4983/49.8% Pallet_2 1693 Pallet_3 1666 Pallet_1 1658. V=8 100%/V<8 0. elev 15~60°(p50 35, edge-on<15° 없음), depth 1.6~4.5m(p50 3.3 far/mid), proj 319px, frsep 46-337px(depth 분리 건강), ratio_randomized 40%(squash aug 있음).
- ★★종합: 데이터 정합성/convention = PASS 학습가능. BUT 성격=**깨끗한 full-view base**(V=8 100%, truncation 0%, elev 하한 15°, far/mid). STAGE16 Phase A 병목(rear/depth=flat·truncated·near·저앙각)의 **어느 regime도 이 데이터엔 없음**. → 논문 clean base로는 PASS, truncation/저앙각/near 보강 목적이면 FAIL(그 목적은 별도 truncation 생성 필요). 산출 data/pallet/eval_results/paper_4pallet_audit/.

## Paper-S1 (mask-aux) 평가 이어서 실행 (학습 완료본, 워처가 놓친 평가)

학습 재실행 없이 평가만. Paper-S1 = paper_base_v2/final_ep0060 + train_4pallet_mask_v1 mask-aux replay(60/40), ep60→70.

### ckpt 선택 (synthetic Val-Old held-out, real 아님)
- 신규 스크립트 scripts/stage0/s1_ckpt_select.py (s16_ckpt_select 인프라 재사용: aspect-only, order-free corner, V=8 split).
- ep64~70 중 V=8 corner_med 최소 = **ep65** (V8 cm=7.9, det=95.5%, good%=68.8, h8=9.4). ep70(final)은 cm=8.6로 열화 → net_epoch_0065 선택.

### PaperBase-v2 vs Paper-S1 paired (A_nopad official, same-frame)
- scripts/stage0/eval_paper_s1_paired.py --s1_weights net_epoch_0065.pth.
- filterval V=8(full-view, N=123): corner_med 27.2→12.7, rear 34.2→20.2, gross% 47.7→34.8, good% 28.6→40.5, honest8 28.4→17.6, det 77→79, PnP 79→81.
- 성공기준 filterval overall = **7/7 PASS (FULL SIGNAL)**.
- handannot17(고앙각 N=17): 무변화~미세악화(corner 7.6→7.1, good% 76.9→73.1) — base가 이미 잘함(7.7px), 개선 여지 없음.

### §5 검증 (verify)
- 두 모델 모두 truncation_aug_prob=0.0 무패딩 학습 → A_nopad = 양쪽 train/infer parity(패딩 아티팩트 아님).
- base는 pad100(stage25)에서도 filterval corner_med 22.6로 약함 → S1 no-pad 12.7이 base BEST(22.6)도 ~10px 능가. 진짜 localization 개선.
- 이득은 **저앙각 full-view 한정**(지면 카메라). V<8 개선은 주장 안 함(데이터 V=8 100%).
- 교란: S1 = replay + mask-aux head + 5ep 추가 번들 → mask-aux 단독 분리 아님(패키지 효과).

판정: Paper-S1 패키지 = full-view 저앙각 real에서 STRONG POSITIVE, full-paper 후보. paper purity(v3/addon/B2 미사용) 유지. final-test 봉인.
산출: data/pallet/eval_results/paper_s1/{ckpt_select.txt,json, eval.txt, eval.json, summary.md}

## 코너 미검출 패턴 진단 (사용자 "ㄱ자, 앞면 빼먹음") — near-face 세로변 드롭, 인덱스 고정 아님
- 사용자 관찰: noapril_15서 코너 0,3 안찍힘, "자꾸 ㄱ자로 추론 앞에거 빼먹음". → per-corner belief peak 진단(191프레임 full-9 GT: cad11/noapril6/day100/night74, S1 ep65 + base v2, no-pad thr0.3).
- ★"0,3만 systematic" = 거짓[확인]. per-index miss%(S1): 0=24 1=48★ 2=49★ 3=33 / rear4-7=17-28. 최악은 1,2(near-right 세로변)지 0,3 아님. noapril_15는 그 프레임서 좌변(0,3)이 약했을 뿐(belief 0.02=완전 non-detection).
- ★진짜 패턴=near(front) face 세로변 한쪽 드롭="ㄱ자"[확인]. v4가 프레임마다 카메라-근접면 front 재인덱싱→0,3=near-왼세로변/1,2=near-오세로변, 어느쪽 빠지는지 azimuth로 프레임마다 뒤집힘. 고정=인덱스 아니라 기하역할("foreshorten/저대비/border/truncated된 세로변 드롭").
- ★rear 병목(STAGE17)과 다른 실패모드: near face miss 38%>rear 22%지만 성격 다름 — front는 검출시 정확(7-9px)/안뜨면 0(검출실패), rear는 검출돼도 24-37px(flat-depth squash=위치오류). = 두 실패 공존: ①앞면 세로변 검출드롭(저대비·foreshorten) ②rear 접힘(depth collapse).
- noapril_15 원인: (b)저대비(어두운데크 vs 밝은바닥)+극foreshortening(좌변 36px), 코너3은 border근접 가중. truncation 아님(화면안), rear-collapse 아님(rear 정상).
- ★S1이 앞면 악화 아님: base도 0,3 miss 28/36%로 동일. S1은 rear/centroid 소폭↑, near-right 소폭↓. ㄱ자는 base부터 있던 것. 산출 data/pallet/eval_results/paper_s1/corner_miss_diag/. ⚠day/night는 challenge-track eval+paper 모델=도메인갭 포함.
