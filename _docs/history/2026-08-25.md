# 2026-08-25

## [10:21] ★ GPU 12시간 25분 유휴 — 대기 루프의 자기매칭 함정

### 무슨 일이 있었나

전날 21:56 에 NEG screen 드라이버를 하네스 추적으로 재기동하면서, 고아가 된 smoke
자식 프로세스가 끝나기를 기다리려고 이런 대기 루프를 앞에 붙였다.

```bash
until ! pgrep -f "_tr_ADAPT" >/dev/null; do sleep 10; done
python -u neg_screen_driver.py
```

`pgrep -f` 는 **자기 자신을 포함한 모든 프로세스의 전체 cmdline** 을 훑는다. 이 셸의
cmdline 안에 `_tr_ADAPT` 문자열이 그대로 들어 있으므로 pgrep 은 언제나 자기 자신을
찾아냈고, `until` 조건은 영원히 거짓이 됐다. 결과는 무한 sleep.

```
PID 2577847   STAT Ss   ELAPSED 12:25:12   wchan do_wait
GPU           775 MiB, 5%   (12시간 내내 유휴)
NEGSCREEN_LOG 마지막 줄 [21:55:54] PHASE U7 N0 smoke 1ep   에서 정지
```

드라이버는 **시작조차 하지 않았다.** 로그의 마지막 줄은 그 전에 죽인 nohup 드라이버가
남긴 것이라 "진행 중" 처럼 보였고, 살아있는 셸도 있었기 때문에 겉으로는 정상이었다.
"프로세스가 살아있다" 는 진행의 증거가 아니라는 사례가 하나 더 늘었다.

### 왜 안 걸렸나

- 백그라운드 task 출력 파일이 `| tail -200` 때문에 파이프라인 종료 전까지 **0 바이트**였다.
  진행이 안 보이는 상태 = 정상과 구분 불가.
- Monitor 는 `NEGSCREEN_LOG.txt` 를 보고 있었는데, 그 파일은 죽은 드라이버가 남긴
  줄에서 멈춰 있었다. 새 줄이 안 생기는 것과 "조용히 학습 중" 이 같아 보였다.
- Stop hook 은 셸이 살아있다고 계속 알려줬고, 나는 그것을 "학습 진행 중" 으로 읽었다.

### 조치

- `TaskStop` 으로 멈춘 셸 정리.
- 대기 루프를 **제거**하고 드라이버를 직접 기동. 드라이버 자체가 `results.csv` 행수로
  완료를 판정하므로, 이미 끝난 smoke 는 재학습하지 않고 그대로 재사용한다.
- 재기동 직후 `ps` + `nvidia-smi` 로 **산출물이 실제로 도는지** 확인했다.

```
[10:21:53] ADAPT_N0_SMOKE_SEED42 이미 완료
[10:21:53] PHASE U7 N0 smoke PASS  loss finite 12개
[10:21:53] PHASE U7 N0 15ep
GPU 1416 MiB   python -u neg_screen_driver.py (3529285) + 학습 자식 (3529301)
```

smoke 자체는 정상이었다 — `PC_CALL_COUNT 0 / ROLE_CALLS 0 / PoseLoss26 / batches 424`,
val `mAP50 0.994 / Pose mAP50 0.947`.

### 교훈 (memory `pgrep-self-match-hangs-wait-loops` 로 분리 기록)

프로세스 대기 조건에 `pgrep -f <패턴>` 을 쓸 때 **패턴 문자열이 그 명령 자신의
cmdline 에 등장하면 절대 종료하지 않는다.** 이번 세션에서 같은 자기매칭이 세 번
나왔다 (`pgrep -af "combo_driver|..."`, `pgrep -af "neg_screen_driver"`, 그리고 이
대기 루프). 앞의 둘은 조회라서 오탐만 났지만, 이 하나는 **12시간을 날렸다.**

## [진행 중] UBUNTU NEG SCREEN (N0 vs N1)

전날 freeze 한 계약 그대로 재개. 상세 설정·계약·누수검사는 `2026-08-24.md` 참조.

```
arm                    train  synthetic  negative  batches/ep   상태
adapt_n0_control      13,554     13,554         0         424   15ep 진행 중
adapt_n1_negative     13,554     12,000     1,554         424   대기
init = G38 last.pt  sha256 1a806ca497fde517...  (REAL_G38.json 이 실제 쓴 것)
gate = NEG_SCREEN_GATE_PREREG.json  (hits 2/6 + positive guard 3개 전부)
```

완료되면 SAME REAL n=128 + NEG held-out **2,689** 평가 → `DELTA_NEG = N1 − N0` →
gate 판정 → 디코 알림까지 `neg_screen_driver.py` 안에서 이어진다.

## [11:13] UBUNTU NEG SCREEN 판정 — `NEG_SIGNAL = HARM`

N0 10:21:53→10:46:03, N1 10:48:26→11:12:16 (각 24분), 평가 11:12:16→11:13:51.
batches/ep 424 = 424 로 optimizer step 수까지 동일 `[확인]`.

### negative 억제는 강하게 작동했다 (hits 4/6)

```
NEG HELD-OUT n=2,689           N0        N1         Δ
─────────────────────────────────────────────────────────
AP                          0.6934    0.7306    +0.0372   ★hit
FPR@TPR95                   0.2674    0.2012    -0.0662   ★hit
neg_p90                     0.1735    0.0219    -0.1515   ★hit
neg_cand_mean               3.3563    0.7542    -2.6021   ★hit (-77.5%)
neg_p95                     0.3969    0.0788    -0.3181
neg_median                  0.0059    0.0000    -0.0059
separation_margin          -0.1001   -0.0082    +0.0919
FP_per_image@0.40           0.0506    0.0175    -0.0331   (문턱 -0.10 미달)
neg_detect_rate@0.40        0.0498    0.0171    -0.0327   (문턱 -0.05 미달)
```

★ 절대량이 이미 작아서 @0.40 두 항목은 상대적으로 −66% 인데도 절대 문턱을 못 넘었다.
   문턱을 절대값으로 잡은 것의 한계 — 다만 사전등록이므로 사후 변경하지 않는다.

### 그런데 검출을 죽였다 (positive guard 2/3 실패 → HARM)

```
SAME REAL n=128            N0        N1         Δ        guard
────────────────────────────────────────────────────────────────
ALL cbox                0.836     0.789    -0.047    한도 -0.030  ✗
ALL median             12.339    11.938    -0.401    통과          ✓
NIGHT any-cbox (frames)    19        10        -9    한도    -2   ✗
NIGHT cbox              0.536     0.321    -0.214
recall_TPR@0.40         0.7656    0.6406   -0.1250
```

`neg_cand_mean` 이 3.36 → 0.75 로 줄었다는 건 **후보 자체를 안 내놓게 됐다**는 뜻이고,
그 억제가 negative 이미지에만 걸린 게 아니라 real positive 에도 걸렸다. night 이
특히 크게 무너졌다(any-cbox 19 → 10 frames).

### ★ 별도 발견 — control 자체가 이미 night 을 깎는다

```
NIGHT any-cbox (n=28)     G38 23 frames  →  N0 19  →  N1 10
ALL cbox                  G38 0.852      →  N0 0.836 → N1 0.789
ALL p90                   G38 66.66      →  N0 103.42 → N1 61.46
```

N0 는 negative 0 인 **synthetic replay 전용 control** 인데도 G38 대비 night 을 4 프레임
잃었다. 즉 `lr0 0.002 / 15ep` 짜리 adaptation 레시피 자체가 night 검출에 비용을 물린다.
N1 vs N0 대조는 matched 라 유효하지만, **두 arm 모두 G38 보다 나쁘다**는 사실을
routing 에서 빼놓으면 안 된다.

### 판정

```
NEG_SIGNAL = HARM      (hits 4/6, guard d_all_cbox_pp / d_night_any_cbox_frames 실패)
→ U11 CASE HARM 규칙: negative 축 단독 사용 금지
```

산출물: `UBUNTU_NEG_SCREEN_RESULT.json` / `.md`, `NEGSCORE_ADAPT_N{0,1}.json`(2,817행),
`REAL_ADAPT_N{0,1}.json`, `NIGHT_CAND_ADAPT_N{0,1}.json`.
두 arm 점수 배열이 서로 다름(상관 0.819)을 확인해 캐시·복사가 아님을 검증했다 `[확인]`.

### 대기

Windows P0/P1 수신 → 같은 evaluator 로 평가 → `DELTA_POS` → U11 routing.
지시대로 다른 학습은 자동으로 시작하지 않았다.

## [오후] ADAPTIVE POINT-HOUGH (G38) — STEP A 감사

새 namespace `data/pallet/results/adaptive_hough_g38/` (신규, 기존 결과 overwrite 0).
새 스크립트 `scripts/stage0/adaptive_hough/ah_audit.py`, `ah_stepA.py`.

### 계약 (전부 실측)

```
commit                  96ddf1967ecee2759e5d36578a84f2e4eb021efe  (main, origin 동기)
G38 train unique        38,002    val 1,998    train∩val = 0
generic_train.txt 대조  일치       generic_val.txt 대조  일치
DOPE 소스 pool          data/pallet/training_data/paper_release/v2_prod40k_clean_merged
                        rgb/labels 결측 0 (앞 2,000 표본)
keypoint_convention     camera_dynamic_0123_v4
target-specific / real-positive / new render / fixed-object   전부 0
```

### checkpoint inventory

```
A1_fully_shared    screen_A1_CORNER_LINE_long25k_seed1/step_25000.pth
E2_stopgrad        디스크에 없음 — mh_splitlate 문서상 line 은 E0 와 bit-identical 이라 불필요
E3_split_late      splitlate_E3_SPLIT_LATE_seed1/step_00000.pth  ★step 0 만 남음
                   (recovery 폴더에 0/250/500/1000/2000/3000 — 과거 overwrite 사고 잔재)
E4_capacity        capacity_E4_CAPACITY_MATCHED_CORNER_seed1/step_03000.pth
FINAL40K_seed1/2   screen_A1_CORNER_LINE_FINAL40K_seed{1,2}/step_25000.pth  (AdamW lr 1e-3 wd 1e-4 batch 8)
```

★ **FINAL40K 는 BROAD 40,000 전체(= G38 train 38,002 + generic val 1,998)로 학습됐다.**
target-free 이지만 GATE_FIT/GATE_DEV 의 출처인 val 1,998 을 이미 봤다 → **P0 initialization
으로 쓰면 gate split 이 오염된다.** P0 는 G38 train 만 보는 clean init 이어야 한다.

### feature tap audit (400×400 dummy 실측, index hard-code 안 함)

```
spatial  vgg child index
400      0,1,2,3
200      4,5,6,7,8
100      9..17          <- high tap = 17
50       18..26         <- middle tap = 26,  first_trainable_vgg = 19
25       없음            <- Hough neck 내부에서 stride-2 3x3 로 생성
```

### current same-real n=128 (REVIEWED_CLEAN_REALDEV_V2 140 − FT leak 12)

```
seed1   scope   arm              R med    R p90    t med    5cm5   corner px med
        ALL     F0              16.642  110.765   0.8652   0.117        49.98
        ALL     F3_HOUGH        12.318   90.591   0.7571   0.125
        ALL     F3_POINTTHETA   16.168  106.172   0.6810   0.125
        ALL     F3_ORACLE       10.937   92.051   0.7618   0.148
        NIGHT   (모든 arm)                                  0.000       91.86
seed2   ALL     F0              28.034  134.593   0.6306   0.070        66.82
        ALL     F3_HOUGH        19.345  152.474   0.5930   0.062
        ALL     F3_ORACLE       17.629  156.015   0.5445   0.117
```

### ★ n=128 은 구 n=56 과 전혀 다른 난이도다 (전처리 문제 아님)

```
group        n   F0 R med   corner med   5cm5
OPEN 3세트   40      5.76         8.46   0.350   <- 구 56 재현 (기존 artifact 7.155)
신규 4세트   88     31.76        89.39   0.011   <- ★final-test 개봉분
```

OPEN 3 세트가 기존 수치를 재현하므로 `preprocess_squash` 전처리는 기존 계약과 일치한다
`[확인]`. 즉 열화는 **새로 편입된 final-test 4 세션** 때문이며, n=128 의 69% 가 이
모델의 코너 검출이 애초에 무너지는(중앙값 89 px) 프레임이다.

### oracle headroom — 사전등록 stop gate 는 PROCEED

```
seed    R p90 point -> oracle   gain     5cm5 point -> oracle   gain    PASS
seed1      110.77 -> 92.05    +16.9%      0.117 -> 0.148      +3.1pp   R+5cm5
seed2      134.59 -> 156.01   -15.9%      0.070 -> 0.117      +4.7pp   5cm5
DECISION = PROCEED  (사전등록: 둘 중 하나라도 넘으면 진행)
```

edge 수준 complementarity 는 **강하다** — supported edge 1,536 개에서
`e_point` median 7.91° vs `e_hough` median 3.46°, Hough 승률 **0.630**(seed2 0.655).
YOLO 시절 조건부 Hough(프레임 승률 0.394)보다 훨씬 크다.

### ★ 그런데 §24 promotion gate 의 hard 조건이 상한에서 이미 불가능하다

per-frame ARM oracle(GT 로 프레임마다 F0/F3 중 더 좋은 쪽 선택 = 어떤 router 도 못 넘는 상한):

```
                    easy 50%                    hard 25%
seed    R p90 F0 -> ORACLE   gain     R p90 F0 -> ORACLE   gain
seed1      20.70 -> 14.64   +29.3%      159.46 -> 159.46    0.0%
seed2      33.11 -> 27.87   +15.8%      169.13 -> 161.30   +4.6%
```

§24 는 `hard R p90 >= 10% 개선` 을 요구하는데 **상한이 0.0% / 4.6%** 다. 즉 gate 를
아무리 잘 만들어도 이 조건은 통과할 수 없다. 그리고 이득의 방향이 의도한 논문 그림과
**반대**다 — 이득은 easy 에 있고 hard 에서는 tail 이 오히려 나빠진다.

원인은 명확하다: hard 88 프레임은 코너가 median 89 px 틀린 프레임이라 **orientation 을
고쳐도 회수할 pose 가 남아 있지 않다**. 5cm5 이득 +4 프레임도 전부 OPEN 3 세트에서 나왔다
(신규 4 세트 이득 seed1 0 프레임 / seed2 2 프레임).

### 또 하나 — F3_POINTTHETA 교락

`pred_theta` 를 Hough 가 아니라 **예측 코너에서 유도한 theta** 로 넣어도 5cm5 가 오른다
(seed1 0.117→0.125, seed2 0.070→0.102). 즉 F3 의 이득 일부는 Hough 정보가 아니라
residual 재가중 자체에서 온다. **F0 vs F3 차이를 "Hough 효과" 로 읽으면 안 되고,
F3_POINTTHETA 를 대조군으로 함께 보고해야 한다.**

### [방침 변경] full training 보류 — TRAINING-0 CONTROL 먼저

사용자 지시로 P0/H0/H1 학습(6k·25k)을 **실행하지 않는다.** wiring 코드는 보존.

- STEP A 의 GO 는 **overall oracle headroom 이 존재한다**는 데까지만 유효하다.
- 원래 논문 가설 "point-hard frame 을 Hough 가 rescue" 는
  `HARD_CRITERION_UNREACHABLE_BY_ORACLE` 이므로 **현재 brief 에서 FAIL 로 기록**한다.
  hard 정의는 real 결과를 보고 수정하지 않는다.

#### 새 상류 질문 — Hough 가 재가중 이상의 정보를 주는가

`scripts/stage0/adaptive_hough/ah_control.py`. 동일 point prediction / 동일 solver /
동일 프레임에서 네 arm:

```
F0  POINT_ONLY
FP  POINTTHETA    예측 코너 endpoint 에서 theta 유도 → rho-free rotation-only + t refit
FH  HOUGHTHETA    Direct-Hough theta, 나머지는 FP 와 완전히 동일
FO  ORACLE_SOURCE GT angle error 로 edge 별 FP/FH 중 선택 (diagnostic only)
```

- FP·FH 는 **같은 lambda grid `[0.03,0.1,0.3,1.0,3.0]` 과 같은 safety rule**
  (t median 악화 ≤3% / 5cm5 감소 0 / solve rate 감소 ≤1pp, 생존자 중 R median 최소,
  동률이면 작은 lambda) — 기존 theta-only solver 의 것을 그대로 재사용.
- lambda 선택은 **synthetic GATE_DEV 만**. real 128 튜닝 금지.
- GT support 미사용(예측 코너에서 유도), rho 는 objective 에서 소거.

#### gate split (한 번만, 결정론적)

namespace `G38_GATE_SPLIT_V1`. G38 generic val 1,998 을 `sha1(namespace|stem)` 정렬 후
50:50 → `GATE_FIT` / `GATE_DEV`, JSON + JSONL + SHA256 저장.

★ **FINAL40K 는 BROAD 40,000(train 38,002 + val 1,998)을 전부 학습했으므로 이 split 은
FINAL40K 에게 unseen 이 아니다.** lambda 절대값은 낙관 편향이다. 다만 FP/FH 가 같은
split·grid·rule 을 쓰므로 **대조 자체는 공정**하다. 이 한계를 결과 JSON 에 박아둔다.

#### 판정 규칙 (사전등록)

```
FH 가 FP 대비 두 seed 에서 일관되게 R median 개선
  AND t median 악화 ≤ 3%
  AND 5cm5 감소 없음
→ HOUGH_ADDS_INFORMATION_BEYOND_REWEIGHTING   (FPN/PAN + gate 계속)
그 외
→ HOUGH_INCREMENTAL_VALUE_NOT_ESTABLISHED     (FPN/PAN training STOP)
```

#### 별도 failure mode 기록

```
NEW88_FAILURE_MODE = POINT_LOCALIZATION_COLLAPSE
근거   NEW88 corner px median ~89  (OPEN40 ~8)
의미   orientation correction 으로 구할 수 없는 subset.
       adaptive-Hough 의 "hard rescue" 성능으로 해석하지 말 것.
범위   current same-real n=128 의 88/128 (69%)
```

### TRAINING-0 CONTROL 결과 — `HOUGH_INCREMENTAL_VALUE_NOT_ESTABLISHED`

새 파일 `scripts/stage0/adaptive_hough/ah_control.py`, `ah_lambda_matched.py`.
학습 0. 기존 파일 overwrite 0.

#### ★ 도중에 드러난 blocker — synthetic pose 평가가 GT 축배정을 요구한다

GATE_DEV(synthetic)의 baseline F0 가 **R median 175.8°, 5cm5 0.000** 으로 나왔다.
모델이 학습한 프레임에서 나올 수 없는 값이라 추적했다.

```
GT 코너로 직접 PnP  vs  labels 의 pose_transform  ->  R 정확히 180.000 deg, t 0.0000 m
R_fix 는 상수가 아님 (프레임간 산포 median 49.5 deg)  -> 고정 축 회전이 아니다
최근접 대응으로 permutation 복원:  model corner i  <->  projected_cuboid[7 - perm_v4[i]]
   f0039 perm_v4 identity      -> [7,6,5,4,3,2,1,0]   잔차 0.00 px
   f0284 perm_v4 [5,4,7,6,1,0,3,2] -> [2,3,0,1,6,7,4,5] 잔차 0.00 px
   f0231 perm_v4 [4,0,3,7,5,1,2,6] -> [3,7,4,0,2,6,5,1] 잔차 0.22 px
```

`perm_v4` 는 **GT 전용 프레임별 축 배정**이라 추론에서 쓸 수 없다. 이는 memory
`evaluator-receives-gt-per-frame-axis-assignment` 에 기록된 미해결 blocker 와 같은 사안이다.
→ **synthetic pool 에서 pose 기반 lambda/gate 선택은 성립하지 않는다.** 임의 보정은 넣지 않았다.

(real manual GT 경로는 정상이다 — OPEN40 F0 R median 5.76° 로 기존 artifact 재현.)

#### 대체 분석 — 같은 lambda 에서 FP vs FH (선택 아님, 전 grid 게재)

```
seed1 ALL128   F0  R med 16.642  R p90 110.765  t med 0.8652  5cm5 0.117
  lam    FP Rmed   FH Rmed    dRmed    FP p90    FH p90    t deg    d5cm5  fracFH  두seed통과
 0.03     19.283    19.285   -0.002    96.323    96.322  +0.0005  +0.0000   0.656   아니오
  0.1     19.267    19.260   +0.007    96.273    96.269  +0.0051  +0.0000   0.656   아니오
  0.3     19.134    18.936   +0.198    95.856    95.842  +0.0169  +0.0000   0.664   ★예
  1.0     18.899    17.783   +1.116    92.697   105.837  +0.0519  +0.0234   0.594   아니오
  3.0     16.168    12.318   +3.851   106.172    90.591  +0.1118  +0.0000   0.477   아니오

seed2 ALL128   F0  R med 28.034  R p90 134.593  t med 0.6306  5cm5 0.070
 0.03     26.872    26.873   -0.001   130.419   130.455  -0.0000  +0.0000   0.602   아니오
  0.1     26.867    26.870   -0.004   130.584   130.969  -0.0004  +0.0000   0.617   아니오
  0.3     27.336    26.854   +0.482   131.093   133.115  -0.0032  +0.0000   0.609   ★예
  1.0     25.917    22.821   +3.096   129.673   138.686  -0.0089  +0.0157   0.594   아니오
  3.0     24.429    19.345   +5.084   131.451   152.474  +0.0959  -0.0391   0.484   아니오
```

#### 판정

- 사전등록 조건을 두 seed 동시 만족하는 lambda 는 **5 개 중 1 개(0.3)뿐**. 4/5 에서
  실패하므로 "일관되게" 가 성립하지 않는다.
- 그 lambda 에서 효과 크기는 R median **1.0% / 1.8%** — line branch 의 알려진 seed 산포
  **15~19%** 보다 한 자릿수 작다 (memory `line-branch-seed-variance-exceeds-effect`).
- lambda=0.3 을 "통과하니까" 고르는 것은 **real 결과를 보고 고르는 것**이며 금지다.
- 효과가 큰 lambda 에서는 safety 위반: seed1 t +11.2%, seed2 5cm5 −3.9pp, seed2 R p90 131→152.

```
VERDICT = HOUGH_INCREMENTAL_VALUE_NOT_ESTABLISHED
NEXT    = Hough-private FPN/PAN training STOP
```

edge 수준에서 Hough theta 는 확실히 정확하다(e_hough median 3.1~3.6° vs e_point 8.3~9.7°,
edge 승률 0.63~0.72). 그런데 **그 정확도가 pose 로 전이되지 않는다** — 이 프로젝트에서
반복 확인된 "집계 통계가 pose 로 전이 안 됨" 패턴의 또 한 사례다.

산출물: `FINAL_VERDICT_TRAINING0.json`, `TRAINING0_CONTROL.json`(+per-frame CSV),
`LAMBDA_MATCHED_FP_VS_FH.json`, `ORACLE_HEADROOM.json`, `gate_split.json`.

---

## [12:12] Windows standalone 실험 데이터 ZIP — 4종 41GB

Windows 로 그대로 복사해 쓸 수 있는 데이터 패키지 1개.
`~/Downloads/PALLET_WINDOWS_EXPERIMENT_DATA_20260825{,.zip,.zip.sha256}`.
git `96ddf196`. repo 무수정, 원본 삭제·이동 0, GPU 0.

### 잡은 것 3 건

**(1) G38 38,002 장이 전부 symlink 였다.** `datasets/stage_a` 를 가리킨다.
`rsync -aL` 로 실체화했고 ZIP 내부 symlink 엔트리 **0** 을 확인했다.

**(2) synthetic negative 라벨이 빈 `.txt` 가 아니라 `*_label.json` 이다.**
`negative_synth_v1_train` 9,000 (revision `NEGATIVE_SYNTH_V1`). 임의로 `.txt` 를
만들지 않고 원본 계약을 provenance 에 기록했다.

**(3) ★ negative heldout 2,430 은 파일명 우연 일치였다 — 재검증으로 확인.**
```
파일명 id 교집합  259 / 259   (전부 겹침)
내용 동일          0 / 259     mean|diff| 중앙값 70.0  최소 49.0
```
FT 가 학습한 negative 는 배포영상(`forklift_raw_20260528_163408`) 프레임이고 이
2,689 는 `negative_real_20260823`(사무실·교내)이라 촬영 자체가 다르다. 이름만
`000389.png` 로 겹친다. 08-24 `G38_ADAPT_DATA_SPLIT_LOCK.json` 의 정정을 독립
재현했다. 지시 16 "억지로 만들지 말라"에 따라 **두 기준 membership 을 모두** 넣고
근거를 `NEGATIVE_OVERLAP_AUDIT.json` 에 기록했다.

### 구성 (ZIP 내부 재카운트로 검증)

```
g38/train  images 38,002  labels 38,002   unique stems 38,002  pair exact
g38/val    images  1,998  labels  1,998
synthetic_negative  images 9,000  labels 9,000 (json)
eval_positive_real128  images 128  labels 128   DAY 100 / NIGHT 28  FT leak 0
eval_negative_real     images 2,689
  negative_full_2689.txt 2689 · ft_overlap_259.txt 259 · heldout_2430.txt 2430
  heldout_bycontent_2689.txt 2689 · ft_overlap_bycontent_0.txt 0
```

`g38/data.yaml` 은 `path: .` 상대경로로 재작성(원본 `nc`/`kpt_shape [9,3]`/
`flip_idx`/`names` 보존, 원본은 provenance 에).

### 검증

```
staging  files 100,978  bytes 43,831,477,533
symlink 0 · 실행용 config 절대경로 0
SHA256SUMS 100,979 항목 · sha256sum -c 100,979/100,979 OK
ZIP  43,870,057,135 B  compression 0  unzip -t 무오류
     sha256 ee7632331bf2bc93b53ab0e80802ffca2806147859b780a935eefa218507d89b
```

### 중단·복구

세션 전환으로 백그라운드 2건이 killed(rsync `SIGINT/SIGTERM/SIGHUP`). 4종 중 3종은
완료돼 있었고 real negative 만 2,666/2,689 에서 끊겼다. 증분 rsync 로 23장을 채우고
membership/AUDIT/provenance 를 이어서 생성했다 — **재복사 0**.

staging 폴더는 사용자 Windows 검증이 끝날 때까지 **삭제하지 않는다**.

## [13:5x] Y0 vs Y1 — pose-quality-aware classification objective (G38 30ep)

새 namespace `challenge/yolo_pose_one_model/runs_posecls_g38/`.
새 loss `pallet_yolo_loss/posecls.py`. 기존 파일 overwrite 0.

### Phase 0 감사 (전부 실측)

```
commit        96ddf1967ecee2759e5d36578a84f2e4eb021efe
python        3.10.20  (envs/pallet-yolo26)   torch 2.1.1+cu118  CUDA 11.8
GPU           RTX 3080
ultralytics   8.4.60   (pip install/upgrade 하지 않음)
Pose26        ultralytics/nn/modules/head.py:666
PoseLoss26    ultralytics/utils/loss.py:800
G38           train 38,002 (파일 실측 일치) / val 1,998 / train∩val 0
              convention camera_dynamic_0123_v4, YOLO 라벨 kpt 9
INIT          challenge/weights/pretrained_yolo/yolo26n-pose.pt  sha eb3bb8268828aeaf...
              Y0·Y1 공유. G38 60ep fine-tune 아님 — clean pretrained 에서 30ep
```

working tree 에 다른 연구 변경 159 건이 있어 **삭제·reset 하지 않았다.** git worktree 도
만들지 않았다 — 이 실험이 쓰는 dataset/weights 가 전부 untracked/ignored 라 worktree 에는
딸려오지 않아 오히려 해롭다. 격리는 새 namespace 로 했다.

### Y1 설계 — stock 코드를 복사하지 않는다

`pallet_yolo_loss/posecls.py::PoseAwareClsLoss26(PoseLoss26)`.

```
q_pose = mean_valid(exp(-e))      e 는 stock KeypointLoss 의 것 그대로
                                  e = d / ((2*sigmas)^2 * (area+1e-9) * 2)   loss.py:327-330
t_pose = detach(t_det) * detach(q_pose)
L_align = |t_pose - sigmoid(logit)|^2 * BCEWithLogits(logit, t_pose)   fg only, /target_scores_sum
loss[3] += hyp.cls * 0.25 * L_align     (표준 cls 와 동일 scale, 이중 weight 없음)
```

구현은 `self.bce` 와 `self.keypoint_loss` 를 **일시적으로 감싸 stock 이 실제로 넘기는
텐서를 읽는** 방식이다. 반환값은 그대로 통과시키므로 표준 loss 경로가 한 줄도 안 바뀌고,
upstream 이 바뀌어도 수식이 갈라지지 않는다. 상세는 `LOSS_PATH_AUDIT.md`.

`E2ELoss` 가 `loss_fn` 을 두 번 독립 생성하므로 one2many/one2one 이 각자 자기
`fg_mask / target_gt_idx / target_scores / pred_kpts` 만 본다 — 복사·공유 없음 `[확인]`.

### HARD BLOCK 테스트 (학습 전, 둘 다 통과)

```
GRADIENT_ROUTING   cls_head 11.505 > 0 | kpt_head 0.0 | sigma_head 0.0
                   one2many reached True (0.2301) · one2one reached True (0.0946)
                   shared feature 43.24 > 0 — classification 경로라 허용(§11)
ZERO_WEIGHT_PARITY Y0 vs Y1(lambda=0) 20 step replay, 498 param
                   max_param_diff = 0.0  (exact zero, tolerance 1e-6)
SMOKE dataset      g38_smoke256  256 frames, sha1 정렬 결정론적, random 미사용
```

### ★ §17 held-out negative 재검산 — 브리프 산술이 실측과 다르다

```
full pool 2,689 · FT-used 259 · 내용 교집합 0 · held-out primary = 2,689 전체
```

FT negative 259 는 `neg__forklift_raw__`(배포영상)이고 2,689 는 사무실·교내 촬영으로
출처가 다르다. 파일명 숫자 id 만 겹친다. 빼기를 적용해도 제거되는 프레임이 0 이라
두 정의의 평가 결과는 같다. `REALNEG_MEMBERSHIP.json` 에 근거와 함께 기록.

### 사전등록 gate (학습 전 freeze)

safety 4 개 전부 + 조건 A/B/C 중 2 개 이상 → `Y1_POSEAWARE_LOSS_PROMOTE`,
1 개 이하 → `Y1_POSEAWARE_LOSS_NO_SIGNAL`. `GATE_PREREG.json`.

### 참고한 과거 측정치

memory `correct-box-bad-keypoint-is-the-bottleneck` — box_conf 의 correct-keypoint 판별
AUC 0.72, rerank 상금 9 장. 즉 가설의 전제(confidence 가 keypoint 품질에 정렬 안 됨)는
이미 확인됐고, 조건 A(NIGHT top1 +10pp = 28 장 중 +2.8 장)는 그 상금 범위 안이다.

---

## [14:07] Y26 Ubuntu contract 패키지 — Windows Y2 unblock 용

`~/Downloads/Y26_UBUNTU_CONTRACT_20260825{,.zip,.zip.sha256}` · 62 MB · 학습 0 · GPU 0.
데이터셋·real 이미지 미포함, pretrained init 포함.

### ★ Y0 30ep 이 진행 중인 상태에서 만들었다

`runs_posecls_g38/Y26_G38_Y0_VANILLA_30EP_SEED42` 가 13:58 시작해 패키징 시점
**epoch 1/30**, `DRIVER.lock` 존재. 지시대로 **기다리지도 새로 돌리지도 않고** 현재
있는 것만 묶고 `status/MISSING_STATUS.json` 에 스냅샷임을 명시했다. GPU 미사용.

### 내용

```
init/         yolo26n-pose.pt 7,878,574 B
              sha eb3bb8268828aeaf515cec23a4bfafd793944a86fe9af94ba7823609c14522a9
environment/  py 3.10.20 · torch 2.1.1+cu118 · ultralytics 8.4.60 · RTX 3080
              ★Pose26/PoseLoss26 은 stock ultralytics (site-packages), 로컬 fork 아님
              loss.py sha 8b4efa8f0c9e734c…  ← Windows 와 이걸로 대조
source/       git bundle --all 55,270,414 B  verify OK
              exp/y26_pose_arch_20260825 branch 생성 + origin push 성공
              git_status/diff/diff_cached (provenance 전용, 자동 적용 금지 명시)
data_contract/ G38 train 38,002 / val 1,998 / overlap 0  (이미지 없음, stem 목록만)
y0/           args.yaml · results.csv(1/30 스냅샷) · 학습 로그 · smoke · _shared contract 10종
y1/           Y1_METHOD_SPEC · LOSS_PATH_AUDIT · pallet_yolo_loss/posecls.py · smoke · tests
Y0_Y1_EXPERIMENT_CONTRACT.json   Y0 실제 args.yaml 에서 읽은 recipe (기억 전사 0)
```

### init SHA 대조 — 부분 일치로 단정하지 않았다

Windows 가 준 값은 `eb3bb826...22a9` 로 앞 8자·뒤 4자뿐이다. 실측 전체 SHA 와
prefix·suffix 는 맞지만 지시대로 **`WINDOWS_FULL_SHA_NOT_AVAILABLE`** 로 기록하고,
Windows 에서 전체 문자열을 재계산해 대조하라고 README 에 적었다.

### 검증

```
files 53  bytes 64,058,361  symlink 0
sha256sum -c 53/53 OK
ZIP 62,016,475 B  unzip -t 무오류
     sha 4ce4cb7db2cad71bc752287ca5a52bd682770430eb4c0c5cca88d7affd1dcba2
WINDOWS_FULL_RUN_CONTRACT = READY  (block_reasons 없음)
```
Y0/Y1 30ep 결과 부재는 Y2 full-run blocker 가 아니라 최종 promotion 비교 blocker 다.
staging 폴더는 Windows 전달 완료 전까지 삭제하지 않는다.

### [18:19] Y0 vs Y1 결과 — `Y1_POSEAWARE_LOSS_NO_SIGNAL`

Y0 13:58:29→16:07:36, Y1 16:07:36→18:17:37 (각 2시간 09~10분), 평가 18:19:30.
batches/ep 1188 = 1188, args diff `name/save_dir` 뿐 `[확인]`.

```
                        Y0         Y1          Δ
ALL cbox             0.828      0.828     +0.000
ALL median          12.523     12.885     +0.362
ALL p90             69.710     71.049     +1.339
ALL gross20          0.334      0.344     +0.011
DAY cbox             0.920      0.980     +0.060
DAY p90             59.701     77.638    +17.937
NIGHT median        23.674     16.852     -6.822
NIGHT p90          151.213     38.517  -112.697

NIGHT candidate
any_cbox      20/28      17/28     -3 frames
top1_cbox     14/28       8/28     -6 frames  (-21.4pp)
cand/frame     7.893      5.714     -2.179
margin_median  0.315     -0.014     -0.329

held-out negative n=2,689
AP_AUPRC       0.6460     0.5954    -0.0506
AUROC          0.9374     0.8818    -0.0556
FPR@TPR95      0.3020     0.5816    +0.2797
FP/img@0.40    0.0941     0.1045    +0.0104  (+11.1% 상대)

synthetic (G38 val 1,998)
box_map50      0.9938     0.9940    +0.0002
pose_map       0.9059     0.9137    +0.0078
```

#### 판정

```
safety      ALL_cbox True · ALL_median True · NIGHT_any_cbox FALSE(-10.7pp, 한도 -2pp)
                                             FP_img@0.40 FALSE(+11.1%, 한도 +10%)
conditions  A NIGHT top1 False · B ALL p90 False · C NIGHT p90 True   (1/3, 필요 2)
VERDICT = Y1_POSEAWARE_LOSS_NO_SIGNAL
```

#### ★ 통과한 조건 C 는 모집단 아티팩트다

NIGHT median/p90 은 **correct-box 프레임에서만** 계산된다. Y1 은 그 모집단이
top1 기준 14 → 8 장으로 줄었고, 남은 8 장이 더 쉬운 프레임이라 p90 이 151 → 38 로
내려간 것이다. Y1 의 성능 개선으로 읽으면 안 된다 — 실질은 0/3 에 가깝다.
(gate 는 이미 NO_SIGNAL 이므로 판정 자체는 불변.)

#### 가설 결과

**"pose quality 를 classification objective 에 반영하면 NIGHT ranking 이 개선된다" = 반증.**
30ep · λ=0.25 에서 NIGHT top1 이 14/28 → 8/28 로 오히려 **악화**했다.

전제("confidence 가 keypoint 품질에 정렬 안 됨", box_conf AUC 0.72)는 여전히 유효하다.
반증된 것은 **해법**이다 — cls target 에 q_pose 를 곱하면 고쳐진다는 부분.

부작용: negative 분리능력이 함께 무너졌다(AUROC 0.937→0.882, FPR@TPR95 0.302→0.582).
q_pose 가 foreground candidate 의 target 을 전반적으로 낮추면서 positive 쪽 confidence 가
눌렸고, 그 결과 negative 와의 분리가 나빠진 것으로 보인다 `[추정 — 메커니즘 미검증]`.

synthetic 은 오히려 소폭 좋아졌다(pose_map +0.0078). **synthetic 목적함수는 내려가는데
real 전이가 실패하는** 이 프로젝트의 반복 패턴이 또 나왔다.

#### 산출물 / 패키지

`Y0_VS_Y1.json`, `FINAL_UBUNTU_REPORT.md`, `Y{0,1}_{SYNTH,REAL128,REALNEG}.json`,
`tests/{GRADIENT_ROUTING,ZERO_WEIGHT_PARITY,SMOKE_RESULT}.json`, `ARGS_PARITY.json`.

```
~/Downloads/Y26_Y0_Y1_UBUNTU_20260825.zip   31,111 B
sha256 22ff96585d469f98a345e300a8b6bac9c7adf5848f077c42bb8f543aae8cfad9
dataset·가중치 미포함(0 건 확인), checkpoint SHA 만 동봉
  Y0 last.pt 37f904b975db3e95…   Y1 last.pt 39bf3b978ffd5168…
```

Windows Y2 결과와 비교하기 전에는 60ep 을 시작하지 않는다.

## [18:3x] ARCHITECTURE BASELINE — YOLOv8n / YOLO11n / YOLO26n Pose (G38 30ep)

Y1 트랙 CLOSED. 새 loss/architecture 개발 없이 **stock pose criterion 만** 써서
lightweight pose backbone 세 개의 sim-to-real 차이를 같은 자로 잰다.

새 namespace `challenge/yolo_pose_one_model/runs_arch_baseline/`
(`ab_common.py`, `ab_driver.py`, `EVALUATOR_AUDIT.md`). 기존 결과 overwrite 0.

### pretrained

```
yolov8n-pose.pt   repo 기존   sha c6fa93dd1ee4a2c1…  params 3,295,470
yolo11n-pose.pt   신규 다운로드(ultralytics assets v8.3.0)
                              sha 869e83fcdffdc737…  params 2,874,462
yolo26n-pose.pt   repo 기존   sha eb3bb8268828aeaf…  (Y0 가 쓴 것과 동일)
```

### ★ evaluator 감사 — 무엇이 같은 자이고 무엇이 아닌가

```
YOLOv8n / YOLO11n   Detect + NMS      (predict 기본 iou=0.7, max_det=300)
YOLO26n             Pose26 end2end    one2one branch, NMS 없음
```

- **직접 비교 가능** — cbox / 9kp median·p90 / gross20 / NIGHT top1-cbox / any-cbox /
  synthetic mAP / params / GFLOPs. 전부 "top-1 by box conf" 하나만 쓰므로 후보 집합
  크기와 무관하다.
- **직접 비교 금지** — `candidates/frame`, `wrong-candidate presence`. v8/11 은 NMS 후
  남은 박스 수, 26 은 one2one 출력 수라 정의 자체가 다르다. 표에는 싣되 금지 표시.
- **단서** — negative `FP/image@0.40` 은 박스 **개수**를 세므로 NMS 유무 영향을 받는다.
  절대값보다 순서·자릿수로 읽고 프레임 단위 `neg_detect_rate` 를 함께 싣는다.
- **6D/PnP 제외** — Y0 정본 real evaluator(`cf_real_eval.py`)는 2D keypoint 전용이라
  동일한 PnP evaluator 가 존재하지 않는다. 새 solver 도입 금지이므로 이번 표에서 뺀다.

### synthetic 9kp median/p90

기존 `Y0_SYNTH.json` 에는 mAP 만 있어 9kp 항목이 없다. real 과 **같은 정의**
(top-1 by box conf, correct_box IoU>=0.5, keypoint L2 px)를 G38 val 1,998 에 적용해
세 모델 모두 새로 계산한다. synthetic val 은 이미 PAD100 캔버스라 **추가 padding 없음**
(이중 패딩 방지). mAP 계열은 ultralytics val 정의 그대로 둔다.

### 계약 강제

드라이버가 매 arm 마다 런타임 감사로 `PSPC / symmetry role / posealign` 호출이 **전부 0**
인지, `kpt_shape == [9,3]`, `fliplr == flipud == 0.0` 인지 확인하고 어긋나면 죽는다.
Y0 수치는 기억이 아니라 `runs_posecls_g38/Y0_SYNTH.json` 등 **파일에서 읽는다**.

실행 순서: smoke256 (B11, B8) → B11 30ep → B8 30ep → 평가 → 표.
최종 판정은 승자 선정이 아니라 `ARCHITECTURE_BASELINE_TABLE_COMPLETE`.

### [23:24] ARCHITECTURE_BASELINE_TABLE_COMPLETE

B11 20:35:26→22:00:08 (1h25m), B8 22:00:08→23:20:51 (1h21m), 평가 23:20:52→23:24:24.
셋 다 batches/ep 1188 = 38,002/32 로 동일 노출·동일 step `[확인]`.

```
model                params  GFLOPs  synPose   cbox  9kp med  9kp p90  NIGHTtop1  FP/img@.4    FPS
YOLOv8n-Pose      3,142,182     8.7   0.9058  0.773    12.13    77.00      0.357     0.0041  193.2
YOLO11n-Pose      2,721,174     6.9   0.9006  0.836    14.46   104.59      0.536     0.1629  145.3
YOLO26n-Pose      3,043,704     7.6   0.9059  0.828    12.52    69.71      0.500     0.0941  115.2
```

#### 읽을 거리 — synthetic 은 셋이 같은데 real 이 갈린다

synthetic pose mAP 는 0.9006~0.9059 로 **사실상 구분이 없고**(9kp median 2.28~2.90 px,
cbox 0.994~0.997), real 로 가면 갈린다.

```
                  synth 9kp med -> real 9kp med   배율
YOLOv8n-Pose           2.88          12.13        4.2x
YOLO11n-Pose           2.90          14.46        5.0x
YOLO26n-Pose           2.28          12.52        5.5x
```

즉 **synthetic 지표로는 architecture 를 고를 수 없다.** 이 프로젝트에서 반복된
"synthetic 목적함수는 내려가는데 real 전이가 실패" 패턴의 architecture 판이다.

#### 세 모델이 서로 다른 축에서 이긴다 — 단일 승자 없음

```
real cbox        11n 0.836 > 26n 0.828 > 8n 0.773
9kp p90          26n 69.71 < 8n 77.00 < 11n 104.59
NIGHT top1       11n 0.536 > 26n 0.500 > 8n 0.357
negative AP      8n 0.8673 > 26n 0.6460 > 11n 0.4326
FP/img@0.40      8n 0.0041 < 26n 0.0941 < 11n 0.1629   (40x 차이)
FPS              8n 193.2 > 11n 145.3 > 26n 115.2
```

11n 은 검출을 많이 하지만(NIGHT any-cbox 0.964, cand/frame 16.75) **오검출도 같이
많다**(neg p90 0.5584, FP/img 0.1629). 8n 은 정반대로 보수적이다(NIGHT any 0.571,
cand 4.00, FP/img 0.0041). 26n 이 그 사이에 있으면서 **9kp p90 이 가장 낮고 ranking
margin 이 +0.315 로 가장 크다**(11n +0.048, 8n +0.035).

★ FP/image·cand/frame 차이의 상당 부분은 NMS 유무에서 온다 — 절대값 비교 금지 항목.
프레임 단위 `detrate@0.4` 도 같은 순서(0.0041 / 0.1592 / 0.0915)라 방향 자체는 유효하다.

#### 제외·단서

- **6D/PnP 제외** — Y0 정본 real evaluator 가 2D keypoint 전용이라 동일 PnP evaluator 가
  없다. 새 solver 도입 금지이므로 표에서 뺐다.
- **cand/frame·wrong%** 는 v8/11(NMS) 과 26(end2end one2one) 의 후보 정의가 달라
  모델 간 직접 비교 금지로 표시했다.
- NIGHT n=28, seed 1, 30ep, synthetic-only.

산출물: `ARCHITECTURE_BASELINE_TABLE.{json,md}`, `RESULT_{B8,B11,Y0}.json`,
`PRETRAINED_AUDIT.json`, `SMOKE_RESULT.json`, `EVALUATOR_AUDIT.md`,
`SYNTH{,9KP}_AB_*.json`. checkpoint·pretrained SHA256 전부 표에 수록.

## [23:46] P26 INFERENCE PATH ABLATION (training-0) — `INFERENCE_PATH_NOT_FACTOR`

새 namespace `challenge/yolo_pose_one_model/p26_inference_path_audit/`. **학습 0 회**,
checkpoint sha·mtime 불변 확인. 기존 artifact overwrite 0.

### 질문

YOLO26 이 YOLOv8n 보다 real-negative rejection 이 약한 이유가 backbone/representation
때문인가, Pose26 의 one2one/end-to-end candidate-selection 경로 때문인가.

### source 감사에서 나온 함정 둘

```
1. Detect.forward 는 one2many 를 항상 계산하지만 추론은 one2one 만 쓴다 (head.py:157-171)
2. ★ nn/tasks.py:253 이 end2end 모델의 head.fuse() 를 불러 one2many head 를 지운다
   -> Pose26.fuse: cv2=cv3=cv4=cv4_kpts=cv4_sigma=None
   -> 아무 조치 없이는 one2many 에 접근 자체가 불가능
```

patch 는 `fuse` 를 no-op 으로 만들고 `Pose26.forward` 를 **stock 메서드만 재조합**해
대체했다(forward_head / _inference / postprocess). conf 0.001 / iou 0.7 / max_det 300 /
imgsz 640 은 기존 artifact·default.yaml 에서 읽어 고정, 사후 변경 없음.

### M0 PARITY (HARD BLOCK) — 통과

```
raw tensor (deterministic 64 frame)  conf 0.0 · box 0.0 · kps 0.0 · 후보수 0
evaluator level (128 frame)          conf 0.0 · iou 0.0 · err 0.0 · correct_box 불일치 0
기존 Y0 정본과 동일: ALL cbox 0.828 / med 12.523 / p90 69.710 / NIGHT top1 0.500
```

### 결과

```
mode            ALLcbox     med      p90  NIGHTany  NIGHTtop1    negAP   FPR@95   det@.4  FP/img@.4
M0 E2E            0.828  12.523   69.710     0.714      0.500   0.6460   0.3020   0.0915     0.0941
M1 O2M_RAW        0.828  12.940   77.928     0.786      0.429   0.6790   0.3737   0.1123     0.4429
M2 O2M_NMS        0.828  12.940   77.928     0.786      0.429   0.6790   0.3737   0.1123     0.1131
```

### ★ M1 ≡ M2 는 버그가 아니라 구조적 필연이다

NMS 는 실제로 걸렸다 — `p26_paths.CALLS = {'o2m_forward':2, 'nwrong'... 'nms_forced':1}` 실측.
그런데 primary 지표(cbox / 9kp median·p90 / NIGHT top1·any / neg AP·AUROC·FPR95·detect-rate)는
**전부 최고 confidence 박스 하나만** 쓰고, NMS 는 최고점 박스를 제거하지 않는다.
따라서 이 지표들은 NMS 에 대해 불변이다. 실제로 달라진 건 박스 개수를 세는
`FP/image@0.40` 뿐이다(0.4429 → 0.1131, 중복 약 75% 제거).

→ **이 설계에서 "NMS 가 rejection 을 회복시키는가" 는 top-1 지표로 원리적으로 검정 불가**이고,
개수 지표로만 답할 수 있다. 그리고 거기서도 AP/AUROC/FPR95 는 그대로다.

### ★ M1 의 synthetic mAP(0.2371)은 아티팩트

M1 은 `head.postprocess` 출력(B,300,6+nk)을 validator 의 NMS 경로에 넣어 형식 불일치로
mAP 집계가 깨졌다. 같은 M1 의 9kp median 2.26 / cbox 0.995 는 정상이고 이들은 top-1 만 쓴다.
M1 synthetic mAP 은 모델 품질 신호가 아니므로 해석에서 제외한다.

### 원인 분해

```
M0 -> M1 (branch)   NIGHT any 20->22 (+2 frame),  top1 14->12 (-2 frame)
                    neg AP 0.6460->0.6790,  FPR95 0.3020->0.3737(악화),  det@.4 0.0915->0.1123(악화)
                    = one2many 는 밤에 객체를 더 찾지만 랭킹이 나쁘고 rejection 은 혼합·소폭
M1 -> M2 (NMS)      primary 변화 0 (구조적), FP/image 만 0.4429->0.1131
                    = NMS 는 중복 박스만 정리한다
```

### 판정

```
safety 3/3 통과 · benefits 0/4 (필요 2)
VERDICT = INFERENCE_PATH_NOT_FACTOR
```

§14 CASE B 규칙에 따라 **inference architecture 트랙을 닫는다.**
다음은 v8n 의 강한 negative rejection 이 representation 수준에서 이미 존재하는지 보는
P3/P4/P5 foreground classification feature separability audit(training-0) 하나뿐이다.

---

## [09:56] G38 POSITIVE SCREEN 패키지 조립 — ★기록 복원 (08-26 재작성)

> 이 항목은 08-25 09:56 에 기록했으나 이후 다른 세션이 같은 파일을 덮어써 **소실**됐다.
> 산출물은 무사하므로 08-26 10:4x 에 실물 기준으로 복원한다. 12:12 · 14:07 항목은 남아 있었다.

Ubuntu 실재 자산으로 P0/P1 2-arm 패키지를 조립.
`~/gpu_handoff/G38_POSITIVE_SCREEN_TO_WINDOWS_20260825T0955/` + Downloads 사본.
학습 0 / GPU 0.

```
INIT      REAL_G38.json['weights'] -> OLD_ROOT_G38_GENERIC_ONLY_60EP_SEED42/weights/last.pt
          sha 1a806ca497fde517…  epoch 60  generic pool 38,002  target-specific 0
POSITIVE  ft_a 실측 unique 157 / exposure 3,140 (=157x20) / PAD100 REFLECT_101
LEAK      membership 140 - positive overlap 12 = leak-free 128 (FT_EVAL_LEAK 와 집합 일치)
RECIPE    runs_ft/ft_a args.yaml 실측 + epochs 40->15, patience 15->0
REPLAY    namespace G38_POSSCREEN_REPLAY_SEED42, sha256 ordering, nested
              P0 generic 15,140 / P1 generic 12,000 + positive 3,140  parity TRUE
QA        files 328 · missing 0 · pair 157/157 · non-positive image binary 0
ZIP       161,270,240 B · inner sha 327/327 OK · CRC 무오류
          outer sha 6d15d388f4494f87871915711559bc8e86f95849867021d03adb600e04b79c19
```

### ★ 이 패키지는 중복이었다 (08-26 확인)

08-25 브리프가 "기존 handoff ZIP 부재 확정, 더 검색하지 말라"고 전제해 재확인 없이
조립했는데, 그 시점 **Desktop 에 08-24 21:57 판이 이미 있었다**. 내 08-24 07:44 검색은
그때 기준으로는 맞았고(정말 없었다), 08-24 21:57 에 다른 세션이 만들었다.

두 판은 **exposure 설계가 다르다** — 같은 이름 계열이지만 다른 실험이다.

```
                        08-24 21:57 판(Desktop)      08-25 09:56 판(내 것)
TOTAL_EXPOSURE          13,554                       15,140
P0                      synthetic 13,554             generic 15,140
P1                      synthetic 10,414 + pos 3,140 generic 12,000 + pos 3,140
arms                    6 (M00/N0/N1/P0/P1/M11)      2 (P0/P1 only)
sha256                  1f3f6126ff2655dd…            6d15d388f4494f87…
```

13,554 는 N-arm(synth 12,000 + neg 1,554) 총량에 P1 을 맞추느라 P1 의 synthetic 을
10,414 로 깎은 값이라 "positive 추가" 가 아니라 "교체" 가 된다. 그래서 내 판은 FT 실측
SYN_REPLAY 12,000 을 그대로 두고 P0 를 15,140 으로 채웠고, 계약서에
`★DIFFERS_FROM_PRIOR_2X2_LOCK` 으로 근거를 남겼다.

**어느 판을 Windows 에 보냈는지에 따라 결과 해석이 달라진다 — 사용자 확인 필요.**

---

## 세션 요약 (2026-08-25 하루)

### 판정 6건 — 전부 사전등록 gate 로 냈고, 결과를 보고 문턱을 바꾸지 않았다

```
실험                                    판정                                       다음
────────────────────────────────────────────────────────────────────────────────────────
UBUNTU NEG SCREEN (N0/N1, 15ep)        NEG_SIGNAL = HARM                          negative 축 단독 사용 금지
ADAPTIVE POINT-HOUGH STEP A            PROCEED (overall oracle headroom 존재)      단, hard rescue 가설은 FAIL
  └ hard rescue 가설                   HARD_CRITERION_UNREACHABLE_BY_ORACLE       hard 정의 수정 안 함
  └ TRAINING-0 CONTROL (F0/FP/FH/FO)   HOUGH_INCREMENTAL_VALUE_NOT_ESTABLISHED    FPN/PAN training STOP
Y0 vs Y1 (pose-aware cls, 30ep)        Y1_POSEAWARE_LOSS_NO_SIGNAL                Y1 트랙 CLOSED
ARCHITECTURE BASELINE (v8n/11n/26n)    ARCHITECTURE_BASELINE_TABLE_COMPLETE       승자 선정 안 함
P26 INFERENCE PATH (training-0)        INFERENCE_PATH_NOT_FACTOR                  inference architecture CLOSED
```

**여섯 중 다섯이 부정 결과다.** 그리고 다섯 모두 "전제는 맞는데 해법이 틀렸다" 형태였다 —
negative 가 FP 를 줄이는 건 맞지만 검출도 죽이고, Hough theta 가 더 정확한 건 맞지만 pose 로
전이 안 되고, confidence 가 keypoint 품질에 정렬 안 된 건 맞지만 cls target 에 q_pose 를
곱하면 랭킹이 더 나빠진다.

### 반복해서 나온 패턴 하나

**synthetic 목적함수는 내려가는데 real 전이가 실패한다.** 오늘만 세 번 나왔다.

```
Y1        synthetic pose_map +0.008 개선   /  real NIGHT top1 14/28 → 8/28 악화
arch      synthetic pose mAP 0.9006~0.9059 로 셋이 동일  /  real 에서 4~5배로 벌어지고 순위 뒤집힘
Hough     edge theta 3.1° vs point 8.3° (승률 0.63)  /  pose 로 전이 안 됨
```

→ memory `arch-baseline-synthetic-cannot-select-architecture` 로 분리 기록.

### 실측이 브리프 전제를 뒤집은 것 3건

```
1. held-out negative "2,689 − 259 = 2,430"   → 실측 교집합 0, held-out = 2,689 전체
   (FT negative 는 배포영상, 2,689 는 사무실·교내. PAD100 벗기고 전수 교차해시)
2. synthetic pose 평가                        → GT perm_v4 없이는 불가 (PnP 가 정확히 180° 어긋남)
   model corner i ↔ projected_cuboid[7 − perm_v4[i]], 깨끗한 프레임 잔차 0.00 px
3. real n=128 난이도                          → OPEN 3세트 corner 8.46px vs 신규 4세트 89.39px
   구 n=56 재현 확인으로 전처리 문제가 아님을 확정
```

### 사고 1건 — GPU 12시간 25분 유휴

`until ! pgrep -f "_tr_ADAPT"` 대기 루프가 **자기 자신의 cmdline 을 매칭**해 무한 정지.
셸은 살아 있었고 Stop hook 도 계속 "백그라운드 셸 살아있음" 이라고 알려줘서 진행 중처럼
보였다. 이후 모든 기동은 `ps` + `nvidia-smi` + 산출물 증가로 확인한다.
→ memory `pgrep-self-match-hangs-wait-loops`.

### 오늘 추가한 memory 6건

```
pgrep-self-match-hangs-wait-loops
negative-supervision-suppresses-detection-too
synthetic-pose-eval-needs-gt-axis-perm-v4
pose-quality-in-cls-target-hurts-ranking
arch-baseline-synthetic-cannot-select-architecture
p26-inference-path-not-the-rejection-factor
```

### 새 namespace 4개 (기존 결과 overwrite 0)

```
data/pallet/results/adaptive_hough_g38/            STEP A + training-0 control
challenge/yolo_pose_one_model/runs_posecls_g38/    Y0 / Y1 30ep
challenge/yolo_pose_one_model/runs_arch_baseline/  v8n / 11n / 26n
challenge/yolo_pose_one_model/p26_inference_path_audit/  M0 / M1 / M2
```

### 학습 총량

```
N0 15ep · N1 15ep · Y0 30ep · Y1 30ep · B11 30ep · B8 30ep  (+ smoke 6회)
P26 ablation 은 학습 0 회 — checkpoint sha/mtime 불변 확인
```

### 현재 열려 있는 것

```
CURRENT_TARGET_FREE_BEST = G38 (변동 없음)
Windows            P0/P1 (positive screen) 수신 대기 → DELTA_POS → U11 routing
                   Y2 결과 수신 대기 → Y0/Y1/Y2 winner 선정 후에만 60ep
CLOSED             Render / Hough / Y1 pose-aware cls / inference architecture
다음 실험 (단 1개)  P3/P4/P5 foreground classification feature separability audit
                   (training-0, v8n vs 26n) — v8 의 강한 rejection 이 representation
                   수준에 이미 있는지 확인
```
