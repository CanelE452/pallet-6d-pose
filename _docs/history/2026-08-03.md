# 2026-08-03

## 코드/설정 변경: **없음**

세션이 08-02 에서 자정을 넘겨 이어졌다.  Stop hook 이 나열한 26 개 파일은 **전부
08-02 작업분**이며 이미 커밋됐고 `_docs/history/2026-08-02.md` 에 기록돼 있다.
오늘 날짜(08-03)로 새로 바뀐 코드/설정 파일은 0 개다.

확인 근거:

```
git status --porcelain      -> _docs/history/.last-compact-resume.md 만 (hook 관리 파일)
git log --since=2026-08-03  -> 커밋 0건
마지막 커밋                  -> c3557c0  08-02 23:07
```

08-02 커밋 목록 (전부 해당 날짜 history 에 기록됨):

```
c3557c0  08-02 23:07  Predicted-seed Gauss-Newton screen: REJECT, final architecture unchanged
a6aa4df  08-02 20:39  Stage-wise bias loss screen: STOP, the targeted behaviour got worse
60ee102  08-02 19:00  Raw-Q proposal re-evaluation: interface was only part of it, router not run
3ea5c70  08-02 18:23  Corner proposal replacement 5-epoch screen: STOP, gate never engaged
8dd5d06  08-02 16:30  PGBC feasibility gates: all three fail, corrector not implemented
1643d70  08-02        PPD overlays: the real map does not localise on the pallet
f1f0f65  08-02        PPD long-run: synthetic PASS, real polarity transfer fails
```

## 오늘 한 일 (읽기 전용)

논문 표 작성용으로 **s2 diffpnp = Stage-B ep57** 의 하이퍼파라미터를 실측 조회했다.
파일 생성·수정 0 건.

조회 출처와 값:

```
출처                                          항목
──────────────────────────────────────────────────────────────────────────
weights/paper_s2_stageB/header.txt            lr 5e-5 / batch 12 / epochs 57(누적) /
                                              sigma 2.0 / diffpnp λ0.005 temp0.1 ramp1000 /
                                              mask_aux True weight 0.01 / manualseed 42 /
                                              imagesize 400 / 6 roots + 60:40 sampler
weights/paper_s2_stageA/header.txt            lr 1e-4 / net_path None(scratch) /
                                              mask_aux False / diffpnp ramp 3000
Deep_Object_Pose/common/models.py:43          vgg19(pretrained=False)  ← ImageNet 아님
Deep_Object_Pose/train/train.py:1195-1221     optim.Adam 단일 LR, scheduler 없음,
                                              start_epoch = 42+1 → epochs 57 = 15 epoch 실행
Deep_Object_Pose/train/train.py:1104          output_size = 50 (belief grid 50x50)
config/stage3_selftrain.yaml                  loo_tau 0.05 / ransac 50·5·5.0px·min6 /
                                              size 0.5~2.5 / bc 0.35·0.10·0.02·0.05 /
                                              num_rounds 10
```

### 보고 시 주의로 남긴 두 가지 [확인]

1. **백본이 ImageNet pretrained 가 아니다** — `vgg19(pretrained=False)` 이고 Stage-A 는
   `net_path=None` 이므로 PAPER_S2 는 scratch 학습이다.  "ImageNet pretrained VGG19" 로
   쓰면 틀린 기술이 된다.
2. **`epochs=57` 은 누적 목표값** — `start_epoch = 42+1`, 루프가 57 까지이므로 Stage-B
   실행분은 15 epoch 이다.  "57 epoch 학습" 으로 쓰면 실제와 다르다.

### 표에 넣을 수 없는 항목

필터 임계값 τ(LOO/flip)와 self-training 라운드는 **ep57 학습에 사용되지 않았다**.
header.txt 에 해당 항목이 없고 ep57 은 지도학습 + DiffPnP/mask 보조손실만 쓴다.
위 config 값들은 별개 계열(`config/stage3_selftrain.yaml`)의 것이므로, 한 표에 섞으면
"ep57 이 self-training 10 라운드를 돌았다" 로 읽힌다.  분리 표기하거나 `— (해당 없음)` 으로 둔다.
