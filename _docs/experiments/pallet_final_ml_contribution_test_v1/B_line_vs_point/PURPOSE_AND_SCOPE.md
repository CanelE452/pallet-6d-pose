# Last controlled architecture test

Does existing structured line refinement outperform a parameter/evidence-matched generic point-local refiner on the same frozen YOLO26n, synthetic training source and historically reused DEV population?

This is the final bounded comparison, not permission for another module, loss, seed, training-budget, selector, active-learning or DHT/Hough rescue search. L's old primary experiment remains failed; this new comparison names the already trained `image_line_only` subarm explicitly. An integrity audit PASS is not performance PASS.

See `METHOD_SCOPE.md` for matching, mismatches and fixed canonical symmetry semantics. Source, geometry, parameter, evidence and order locks precede all three new P fits. Synthetic selection precedes heldout/real scoring. The master lock fixes primary/safety gates and the verdict hierarchy before P training.

Allowed result claims are development-only. Even a positive line-specific outcome requires new independent real sessions with frozen architecture/checkpoint/inference/evaluator/metrics, not promotion to a confirmed paper claim.
