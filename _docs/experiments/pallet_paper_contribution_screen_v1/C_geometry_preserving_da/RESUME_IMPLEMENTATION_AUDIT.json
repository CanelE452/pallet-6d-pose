{
  "scientific_protocol_changes": false,
  "authorization_guard": "Historical blocker retained; explicit resume authorization permits exactly one lost-fit repair plus remaining six fits",
  "checkpoint_retention": "Reject a new fit if last_unverified.pt exists; write exposure receipt immediately after final checkpoint persistence and before audit assertions",
  "logging": "Separate authorized_resume log preserves original C2 failure log",
  "aggregation": "Write complete comparison into new RESUME_COMPLETE directory, preserving partial reports",
  "independent_checks": "Reload all saved C2 states on CPU; independently enumerate trainable branch ownership and compare all frozen state keys; recompute canonical CSV statistics",
  "initialization_scope_note": "Initial tensor hash is before the C2 raw-pose probe. The probe updates only mutable detection BN running statistics, not parameters or frozen BN. Training output equality with/without that probe is unit-tested. Full BN counter equality at first optimizer update is not claimed.",
  "previous_environment_limitations_unchanged": ["Optional Albumentations initialization skips because installed API differs", "Strict cuBLAS deterministic workspace not enabled; actual batch hashes audited, bit-exact refit trajectory not claimed"]
}
