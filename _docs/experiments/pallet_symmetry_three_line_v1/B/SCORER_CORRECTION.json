{
  "phase": "first synthetic summary after frozen calibration",
  "error": "KeyError E_sym on NO_ANNOTATED_CORNERS frame",
  "cause": "Core correctly returns not evaluable for all-unannotated GT, but bootstrap caller initially assumed every frame had E_sym.",
  "fix": "Exclude only NO_ANNOTATION frames with explicit IDs and same population across every arm. Missing predictions with annotated GT remain penalized.",
  "calibration_or_method_change": false,
  "extra_optimizer_updates": 0,
  "extra_model_forwards": 0,
  "reexecution": "CPU scoring/serialization from already frozen prediction inputs only",
  "second_serialization_error": "Invalid annotated-target coordinates represented by NaN could not be written to strict JSON. Display-only target entries are now null; masks, distances, predictions and metric calculations are unchanged."
}
