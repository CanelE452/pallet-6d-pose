# 시각 검수

고정 규칙으로 뽑은 개선 5 / 악화 5 / 높은 모호성 5 / 높이 모서리 실패 5, 총 20 패널의 contact sheet를 검수했다.
원본 해상도 그림은 `VISUAL_MANIFEST.json`에 로컬 경로·SHA를 기록했다. Contact sheet는 해상도상 모든 숫자를 읽는 근거가 아니며 정량값은 CSV가 정본이다.

- GT는 오른쪽 사후 설명 패널에만 표시된다. 왼쪽 추론 증거와 아래 후보 확률은 GT로 선택·보정하지 않았다.
- P와 B3 표식은 자주 겹치고 확률 곡선 차이도 작다. 작은 수치 변화와 일관되며 큰 기하 복원으로 해석하지 않는다.
- 최상위 악화 예시에도 가림 구조·화물·영상 밖 코너가 있다. metadata 검증 없이 이를 독립 occlusion subgroup으로 통계화하지 않았다.
- 높이 모서리는 짧거나 중첩되어 영상 경계로 명확하지 않은 사례가 보인다. 표시된 MAP 직선은 설명용이며 B는 full posterior를 사용했다.
- 균일한 선들이 항상 합의하는 것처럼 그림을 정리하지 않았고, candidate 221 null도 확률 그래프에 포함했다.
