# Execution notes

- Start: main `f436342c3a3072662b16d82201e102c1df812cec`, identical to origin/main. Existing unrelated untracked research files were preserved and excluded from staging.
- GPU: actual host RTX 3080 CUDA was available. No reboot, driver change or foreign-process termination. Existing RustDesk remained running. Per-process GPU/memory/temperature samples are in `stage2/SYNTH_PREDICTIONS_LOCK.json` and `SCORER_TRAINING_AUDIT.json`.
- H_MANUAL synthetic inference completed in approximately 110 seconds, batch16, zero OOM retries. S1 synthetic arrays were bitwise reused. No keypoint optimizer was instantiated.
- Exactly two selector fits: S1 15 epochs (best10), H_MANUAL 20 epochs (best15), using fixed early stopping. No fit was restarted or changed after synthetic TEST or real results.
- Stage1 publication required a supplemental image-only commit because global repository ignore rules exclude PNG/JPG. All six charts and six cases were explicitly added. Later stages explicitly stage their own figures.
- Before any real decision lock or reference scoring, the real-decision adapter failed fast on missing native image-size metadata. Native RGB shape was then recovered from each exact hash-bound RGB. The reference guard initially blocked these RGB paths because they reside under the evaluation directory; it was narrowed to permit only the exact 128 RGB paths with image extensions. Annotation/reference paths remain denied. Neither failed adapter attempt emitted a decision lock or ran real reference scoring.
- The successful guarded real process reproduced all original candidates within tolerance and old-GEO choices exactly (256 model/frame checks), then froze all eight combinations (1,024 decisions). A separate process subsequently read GT-dependent candidate metrics. Stage1's earlier reference exposure is explicitly disclosed: these are reused DEV results, not an unseen experiment.
- All earlier keypoint checkpoints, the old selector and historical experiment artifacts remain unchanged. Both tiny new selector checkpoints are versioned under this experiment's data namespace. Current deployment configuration was not changed.
- Public CLI/Git receipts are timestamped snapshots. An after-push receipt necessarily lives in a subsequent publication commit; the final assistant handoff reports the latest independently checked local/remote SHA.
