# Reproduction and resume

`all` / `resume` also run `audit` and scoped main-only `publish`. Their completion does not manufacture missing confirmation or author approval. PDF compilation creates a pending visual-review state: render every current PDF page, inspect it, and record its exact hashes in `PDF_VISUAL_REVIEW.json` before the execution audit may declare all available work complete. A new PDF invalidates an old visual receipt. Remote-advance or unrelated staged changes are reported rather than automatically stashed or overwritten.

After actually inspecting every rendered page (never just after compiler success), use `record_visual_review.py --manuscript-sha <the inspected PDF SHA256> --supplementary-sha <the inspected PDF SHA256>`, then `run.py audit` and `run.py publish`. This receipt distinguishes assistant layout inspection from the outstanding human author review. An unchanged, hash-matching manuscript is reused on resume; changed results or sources trigger regeneration and a new review.

Run from `/home/minjae/Documents/github/pallet-pose`. Existing R0/P/D/L checkpoints and source data are read-only inputs, not regenerated by this workflow. Check `SOURCE_BINDING.json`, `MODEL_PANEL_LOCK.json`, `TRAIN_CODE_LOCK.json` and `NUMBER_SOURCES.json` in the manuscript root before interpreting output.

```bash
export PYTHONDONTWRITEBYTECODE=1
export OMP_NUM_THREADS=4
export MKL_NUM_THREADS=4
export MPLCONFIGDIR=/tmp/pallet-sensors-mpl
/home/minjae/anaconda3/envs/pallet-yolo26/bin/python -B scripts/research/pallet_sensors_submission_v1/run.py status
/home/minjae/anaconda3/envs/pallet-yolo26/bin/python -B scripts/research/pallet_sensors_submission_v1/run.py all
/home/minjae/anaconda3/envs/pallet-yolo26/bin/python -B scripts/research/pallet_sensors_submission_v1/run.py resume
/home/minjae/anaconda3/envs/pallet-yolo26/bin/python -B scripts/research/pallet_sensors_submission_v1/run.py confirmation --panel data/pallet/results/pallet_sensors_submission_v1/confirmation/incoming/panel.json
/home/minjae/anaconda3/envs/pallet-yolo26/bin/python -B scripts/research/pallet_sensors_submission_v1/run.py manuscript
```

GPU stages need actual host CUDA access. The sandbox's lack of device access is not a reason to replace a working driver or silently train on CPU. Do not run an additional `all` while a prior train process is active. Foreign compute is checked and never killed; display/RustDesk activity is retained.

Individual stages: `bind`, `implementation`, `train`, `evaluate`, `confirmation`, `manuscript`. Main fits are only PRIOR1/2/3, each6000 updates, nominal batch16 and the exact P order. Last checkpoint only. Each checkpoint binds code/protocol/init/order, optimizer, Python/NumPy/CPU/CUDA RNG and durable step; completion receipts also bind checkpoint bytes. Source RGB workers do not perform random augmentation. Prefetched rows are deterministic and resume from the saved cursor.

`evaluate` resumes through `evaluate_pipeline.py`. A hash-verified `DEV_COMPLETE` goes directly to runtime without repeating training, inference or bootstrap analysis. Incomplete DEV uses `evaluate_compatible.py`: the original immutable selection omitted the canonical scorer's literal `selection_population` field, so the compatibility entry point adds it in a separate metadata view with exact-equal prediction frames. See `SCORER_METADATA_CORRECTION.md`. Original selection, predictions, model code and weights are not rewritten. CPU scoring of stored predictions may proceed during another task's GPU use; the runtime guard still requires the other compute to finish. Later failures are archived under ignored `raw/stage_failures` before the latest status is updated.

The initial closeout deferred timing while `finetune_green_zip.py` occupied the GPU. On the user's subsequent resume, the GPU was free and the expanded panel completed. `runtime_recovery.py` preserves the first complete balanced timing attempt (1,690 samples) and measures allocation using `memory_one.py` in one fresh process per model. Original incomplete attempts and metadata corrections remain recorded. `RUNTIME_FLOAT_DIAGNOSTIC.json` locates fixed-input floating drift at the first prior upsampling layer; `RUNTIME_NUMERIC_AMENDMENT.json` explains the existing absolute crop tolerance, unchanged detector contracts and canonical pose checks. No trained model, DEV prediction or selection rule is changed. Timing-phase OpenCV/inter-op observations were not saved before the memory-release guard; separately observed memory-phase settings are labeled accordingly. There is no background waiter.

`run.py evaluate` now reuses the hash-verified completed result. Follow with `run.py confirmation`, `run.py manuscript`, inspect every changed PDF page and record visual hashes, then `run.py audit` and `run.py publish`. Only genuine independently reviewed new data may fill the confirmation gap. Final status distinguishes available-input completion from missing human data and author approval.

The official TF1 CPU environment was created only under the task's ignored raw directory:

```bash
/home/minjae/anaconda3/bin/conda create --prefix data/pallet/results/pallet_sensors_submission_v1/env_tf1 --override-channels -c conda-forge python=3.7 pip -y
data/pallet/results/pallet_sensors_submission_v1/env_tf1/bin/python -m pip install tensorflow==1.15.5 numpy==1.18.5 protobuf==3.20.3
```

The official PoseFix checkout is reused at `data/pallet/results/pallet_sensors_refinement_closeout_v1/external/PoseFix_RELEASE`, commit `5556364bb0f43b0743a5fcd820de48f34b3d4360`. Retrieve it from its official GitHub repository if not locally available; do not substitute another architecture. ImageNet archive:

```bash
curl -fL https://storage.googleapis.com/download.tensorflow.org/models/resnet_v1_152_2016_08_28.tar.gz -o data/pallet/results/pallet_sensors_submission_v1/resnet_v1_152_2016_08_28.tar.gz
```

The archive is checked against the Google Storage object MD5 and recorded with SHA256. No COCO/PoseTrack-trained PoseFix checkpoint is used. R0's own COCO-pose initialization is separately recorded in `R0_PRETRAINING_PROVENANCE.json`; do not call the full system entirely synthetic-from-scratch.

Standalone Tectonic0.17.0, official Linux-musl release, lives under the same raw directory. Its bundle cache is local to `tex_cache`; TeX assets/fonts are not committed. `manuscript` generates tables directly from actual JSON, compiles both PDFs and renders each page. Visual inspection is separately recorded; a successful compiler exit is not a scientific PASS.

Raw images/labels/features, videos, weights, external checkouts and installed environments are intentionally not in public Git. Dataset/checkpoint access must be arranged with the project owner under the existing rights policy. Hashes identify required artifacts but do not grant redistribution rights. New independently captured data are described in `HUMAN_INPUTS_REQUIRED.txt`; do not rename DEV as FINAL.
