[pytest]
# The repository also contains historical GPU experiment launchers named
# test_*.py.  They execute argparse/training setup at import time and are not
# pytest tests.  Keep root-level discovery on the maintained regression suite.
testpaths = challenge/tests
